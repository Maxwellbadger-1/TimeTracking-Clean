# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build outputs
dist/
build/
target/
*.tsbuildinfo

# Environment
.env
.env.local
.env.production.local
.env.development.local

# Database
# Ignore all database files (production and development)
*.db
*.db-shm
*.db-wal

# Specific database files to ignore
server/database.db                      # Production database (Oracle Cloud)
server/database.backup.db              # Production backup
server/database/development.db         # Development database (local copy)
server/database/development.backup.*.db # Development backups with timestamps

# Keep migration and seed SQL files (these should be versioned)
!server/database/migrations/*.sql
!server/database/seeds/*.sql

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop-ini

# Logs
logs/
*.log

# Testing
coverage/

# Tauri
desktop/src-tauri/target/
desktop/src-tauri/WixTools/

# Production builds
*.exe
*.dmg
*.deb
*.rpm
*.AppImage
GITHUB-SECRETS-VALUES.md

# SSH Keys (NEVER commit!)
.ssh/
*.key
*.pem
ssh-key-*
.env.ssh

# Backups (NEVER commit production data!)
backups/
*.backup.db
