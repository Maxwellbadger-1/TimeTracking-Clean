---
phase: 03-local-dev-sync-script
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - scripts/database/sync-prod.sh
autonomous: false
requirements:
  - SYNC-TEST
  - SYNC-DEPRECATE-OLD

must_haves:
  truths:
    - "npm run sync-dev-db completes without errors on Windows Git Bash and prints user count + latest time entry date"
    - "server/database.db is replaced with a fresh copy from production after the script runs"
    - "A timestamped backup of the previous local DB is created before overwriting"
    - "git status shows no untracked *.db files"
    - "Old sync-prod.sh has a deprecation comment pointing to the new script"
  artifacts:
    - path: "server/database.db"
      provides: "Fresh copy of production database"
      contains: "SQLite format"
    - path: "scripts/database/sync-prod.sh"
      provides: "Deprecated old script with comment"
      contains: "DEPRECATED"
  key_links:
    - from: "scripts/sync-dev-db.sh"
      to: "ubuntu@129.159.8.19:/home/ubuntu/databases/production.db"
      via: "scp download"
      pattern: "scp.*databases/production.db"
---

<objective>
Run the sync script end-to-end on Windows Git Bash, verify all success criteria, and deprecate the old sync-prod.sh.

Purpose: Validate the script works in the real environment and mark the old 3-Tier script as superseded.
Output: Verified working sync, deprecated old script, human sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-local-dev-sync-script/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure dependencies and deprecate old script</name>
  <files>scripts/database/sync-prod.sh</files>
  <read_first>
    - scripts/database/sync-prod.sh (current content)
    - scripts/sync-dev-db.sh (new script from plan 01)
  </read_first>
  <action>
**Pre-requisite check:** Verify `server/node_modules/better-sqlite3` exists. If not, run `cd server && npm install` first (better-sqlite3 is already in server/package.json as a dependency).

**Deprecate old script:** Add a comment block at the very top of `scripts/database/sync-prod.sh` (after the shebang line), before the existing header comment:

```bash
# ============================================
# DEPRECATED — 2026-04-02
# This script targets the old 3-Tier DB path:
#   /home/ubuntu/TimeTracking-Clean/server/database.db
# Use instead: npm run sync-dev-db
#   (scripts/sync-dev-db.sh — targets 2-Tier path)
# ============================================
```

Do NOT delete the old script. Only add the deprecation comment block.
  </action>
  <verify>
    <automated>grep -c "DEPRECATED" scripts/database/sync-prod.sh</automated>
  </verify>
  <acceptance_criteria>
    - scripts/database/sync-prod.sh contains "DEPRECATED" in the first 10 lines
    - scripts/database/sync-prod.sh contains "npm run sync-dev-db" as the replacement reference
    - scripts/database/sync-prod.sh still contains the original script body (not deleted)
    - server/node_modules/better-sqlite3 directory exists
  </acceptance_criteria>
  <done>Old sync-prod.sh has deprecation notice, better-sqlite3 is installed</done>
</task>

<task type="auto">
  <name>Task 2: Run sync script and verify all success criteria</name>
  <files>server/database.db</files>
  <read_first>
    - scripts/sync-dev-db.sh (the script to test)
    - .planning/ROADMAP.md (Phase 3 success criteria SC-1 through SC-5)
  </read_first>
  <action>
Execute the sync script end-to-end on Windows Git Bash:

```bash
npm run sync-dev-db
```

**After execution, verify ALL 5 success criteria from ROADMAP:**

**SC-1:** Script completed without errors and printed summary with user count + latest time entry date.
Check: Script exit code is 0. Output contains "Users:" and "Latest time entry:".

**SC-2:** `server/database.db` was replaced with a fresh copy.
Check: `ls -la server/database.db` shows a recent timestamp and non-zero file size.

**SC-3:** A timestamped backup was created (if a previous DB existed).
Check: `ls server/database.db.backup.*` shows a file with today's timestamp pattern.

**SC-4:** `npm run dev` note was printed (the script should print instruction about setting DATABASE_PATH=./database.db).
Check: Script output contains "DATABASE_PATH" instruction.

**SC-5:** `git status` shows no untracked *.db files.
Check: `git status --short` does not list any .db files as untracked (?? prefix).

**If script fails:** Read the error output carefully. Common issues:
- SSH key permissions: Verify chmod 600 ran on .ssh/oracle_server.key
- better-sqlite3 missing: Run `cd server && npm install`
- SSH connectivity: Check VPN / network access to 129.159.8.19
- Path issues: Verify BASH_SOURCE resolution works in Git Bash

If any fix is needed to the script, apply it to `scripts/sync-dev-db.sh` and re-run.
  </action>
  <verify>
    <automated>node -e "const D=require('./server/node_modules/better-sqlite3');const db=new D('./server/database.db',{readonly:true});const r=db.pragma('integrity_check');db.close();if(r[0].integrity_check!=='ok'){process.exit(1);}console.log('integrity_check: ok')"</automated>
  </verify>
  <acceptance_criteria>
    - server/database.db exists and has size > 0 bytes
    - node -e integrity check on server/database.db returns "ok"
    - git status --short does not contain any line matching "?? .*\.db"
  </acceptance_criteria>
  <done>sync-dev-db.sh ran successfully on Windows Git Bash, server/database.db replaced with production copy, integrity verified</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of sync workflow</name>
  <files>server/database.db</files>
  <action>
Present the sync results to the user for verification. The executor completed:
1. Created scripts/sync-dev-db.sh (Plan 01)
2. Added npm run sync-dev-db to package.json (Plan 01)
3. Updated .gitignore with server/*.db.backup.* (Plan 01)
4. Deprecated old scripts/database/sync-prod.sh
5. Ran npm run sync-dev-db end-to-end on Windows Git Bash
6. Verified PRAGMA integrity_check = ok on downloaded DB
  </action>
  <verify>
    <automated>ls server/database.db</automated>
  </verify>
  <what-built>
    Complete sync-dev-db workflow: script downloads production DB via SCP, verifies integrity with better-sqlite3, backs up existing local DB, and replaces server/database.db. Old sync-prod.sh marked as deprecated.
  </what-built>
  <how-to-verify>
    1. Check the script ran successfully (executor will have printed output above)
    2. Verify server/database.db exists: `ls -la server/database.db`
    3. Optionally: run `npm run dev:server` in server/ and confirm it starts (note: you may need to set DATABASE_PATH=./database.db in server/.env.development, or start with `DATABASE_PATH=./database.db npm run dev`)
    4. Run `git status` — no untracked .db files should appear
    5. Review scripts/sync-dev-db.sh content if desired
  </how-to-verify>
  <done>User has approved the sync workflow or provided feedback for fixes</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npm run sync-dev-db` exits 0 (full end-to-end run)
2. `node -e` integrity check on server/database.db returns "ok"
3. `grep DEPRECATED scripts/database/sync-prod.sh` finds deprecation notice
4. `git status --short | grep '?? .*\.db'` returns empty (no untracked db files)
</verification>

<success_criteria>
- sync-dev-db.sh executes end-to-end without errors on Windows Git Bash
- server/database.db contains valid production data (PRAGMA integrity_check = ok)
- Timestamped backup exists (if previous DB was present)
- Old sync-prod.sh has deprecation comment
- User has verified the workflow
</success_criteria>

<output>
After completion, create `.planning/phases/03-local-dev-sync-script/03-02-SUMMARY.md`
</output>
