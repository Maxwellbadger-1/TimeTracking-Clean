---
phase: 04-deploy-workflow-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/deploy-staging.yml
autonomous: true
requirements:
  - "Green Server on-demand"

must_haves:
  truths:
    - "deploy-staging.yml has a comment block at the top stating it is on-demand only"
    - "PORT=3001 is passed explicitly as a shell prefix to the pm2 start command"
  artifacts:
    - path: ".github/workflows/deploy-staging.yml"
      provides: "On-demand comment block and PORT fix"
      contains: "ON-DEMAND ONLY"
  key_links:
    - from: ".github/workflows/deploy-staging.yml"
      to: "pm2 start"
      via: "PORT=3001 shell prefix"
      pattern: "PORT=3001"
---

<objective>
Fix the Green Server PORT bug in deploy-staging.yml and add an on-demand comment block at the top of the file.

Purpose: PM2 does not load .env files, so PORT=3001 must be passed as a shell prefix. The on-demand comment documents that Green Server is not part of the standard 2-Tier workflow.
Output: Updated deploy-staging.yml with PORT fix and on-demand documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deploy-workflow-documentation/04-RESEARCH.md
@.github/workflows/deploy-staging.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add on-demand comment block and fix PORT in deploy-staging.yml</name>
  <files>.github/workflows/deploy-staging.yml</files>
  <read_first>
    - .github/workflows/deploy-staging.yml (current full file — understand PM2 start line at ~142, .env creation at ~115-122, existing on: block)
    - .planning/phases/04-deploy-workflow-documentation/04-RESEARCH.md (Pattern 2: PM2 env variable passing, Pattern 3: On-demand comment block, Pitfall 3)
  </read_first>
  <action>
Two changes to deploy-staging.yml:

**Change 1: Add on-demand comment block at the VERY TOP of the file (before line 1 `name:`):**

```yaml
# ============================================================
# STAGING / GREEN SERVER — ON-DEMAND ONLY
# ============================================================
# This workflow deploys to the Green Server (port 3001).
# It is NOT part of the standard 2-Tier development flow.
#
# Trigger: Manual only (workflow_dispatch) or staging branch push.
# The staging branch is not maintained in the 2-Tier workflow.
#
# Standard flow: npm run sync-dev-db -> develop on main -> git push main
# See: scripts/sync-dev-db.sh for local DB setup
# ============================================================
```

**Change 2: Add PORT=3001 to the pm2 start shell prefix.**

Current line 142 reads:
```
            TZ=Europe/Berlin NODE_ENV=staging SESSION_SECRET=$SESSION_SECRET ALLOWED_ORIGINS=$ALLOWED_ORIGINS \
```

Replace with:
```
            # PORT must be passed as prefix — PM2 does NOT load .env files
            # (see ENV.md Green Server Critical Notes for full explanation)
            TZ=Europe/Berlin NODE_ENV=staging SESSION_SECRET=$SESSION_SECRET ALLOWED_ORIGINS=$ALLOWED_ORIGINS PORT=3001 \
```

Do NOT remove the existing .env file creation block (lines 114-122) — it serves as documentation and fallback for manual server starts outside PM2. Only add PORT=3001 to the pm2 start prefix and add the explanatory comment.

Keep the existing `on: push: branches: [staging]` trigger — do not remove it (the comment block already documents that staging branch pushes are manual/on-demand).
  </action>
  <verify>
    <automated>head -12 ".github/workflows/deploy-staging.yml" | grep "ON-DEMAND ONLY" && grep "PORT=3001" ".github/workflows/deploy-staging.yml" | grep -v "echo" | grep -v "^#" | head -1 && grep "PM2 does NOT load .env" ".github/workflows/deploy-staging.yml" && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - First 12 lines of deploy-staging.yml contain `STAGING / GREEN SERVER — ON-DEMAND ONLY`
    - First 12 lines contain `NOT part of the standard 2-Tier development flow`
    - First 12 lines contain `npm run sync-dev-db`
    - The pm2 start prefix line contains `PORT=3001` (on the same line as `TZ=Europe/Berlin` and `pm2 start` or its continuation)
    - A comment line `# PORT must be passed as prefix` exists above the pm2 start prefix
    - The existing .env creation block (with `PORT=3001` inside the echo) is still present (not deleted)
    - The `on: push: branches: [staging]` trigger is still present (not removed)
  </acceptance_criteria>
  <done>deploy-staging.yml has an on-demand comment block at the top, PORT=3001 is passed as a shell prefix to pm2 start, and a comment explains why PORT is set explicitly</done>
</task>

</tasks>

<verification>
- `head -12 .github/workflows/deploy-staging.yml` shows the on-demand comment block
- `grep PORT=3001 .github/workflows/deploy-staging.yml` returns at least 2 hits (one in .env creation, one in pm2 start prefix)
- The staging branch push trigger is still present
</verification>

<success_criteria>
- deploy-staging.yml starts with the on-demand comment block
- PORT=3001 appears in the pm2 start shell prefix (not just in .env creation)
- The file is valid YAML (existing workflow structure preserved)
</success_criteria>

<output>
After completion, create `.planning/phases/04-deploy-workflow-documentation/04-02-SUMMARY.md`
</output>
