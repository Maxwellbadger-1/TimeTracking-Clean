---
phase: 08-kontoauszug-f-r-mitarbeiter-und-admin
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - CHANGELOG.md
autonomous: false
requirements: [REQ-11, REQ-12, REQ-13, REQ-14]

must_haves:
  truths:
    - "tsc --noEmit läuft für Server und Desktop fehlerfrei durch"
    - "Der Journal-Endpunkt ist auf dem Produktionsserver erreichbar und weist unauthentifizierte Aufrufe mit 401 ab"
    - "Der Health-Check des Produktionsservers meldet nach dem Deployment status ok"
    - "Carmens Auszug zeigt genehmigt −6 und storniert +6 in dieser Reihenfolge"
    - "Der im Auszug gezeigte Saldo stimmt mit der Spalte Verbleibend der Urlaubsliste überein"
    - "Ein Mitarbeiter kann das Journal eines anderen weder in der Oberfläche sehen noch über die API abrufen"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Eintrag zum Urlaubs-Kontoauszug"
      contains: "Kontoauszug"
  key_links:
    - from: "git push origin main"
      to: ".github/workflows/deploy-server.yml"
      via: "Auto-Deploy bei Änderungen unter server/**"
      pattern: "deploy-server"
---

<objective>
Das Qualitätstor vor dem Release: kompiliert, deployed, abgenommen.

Zweck: Die Serveränderungen aus Plan 08-01 erreichen die Produktion ausschließlich über
`git push origin main` und `deploy-server.yml`. Ohne dieses Deployment liefe die neue
Desktop-Oberfläche gegen einen Server ohne den Endpunkt — das Release wäre wertlos. Erst danach
ist eine Abnahme gegen echte Daten möglich, und genau dort entscheidet sich, ob der Milestone
sein Versprechen einlöst: Carmens sechs Tage müssen im Auszug als Geschichte lesbar sein.

Ergebnis: Server in Produktion aktuell, Endpunkt live und abgesichert, Abnahme durch den
Anwender erteilt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-kontoauszug-f-r-mitarbeiter-und-admin/08-01-SUMMARY.md
@.planning/phases/08-kontoauszug-f-r-mitarbeiter-und-admin/08-02-SUMMARY.md
@.planning/phases/08-kontoauszug-f-r-mitarbeiter-und-admin/08-03-SUMMARY.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Qualitätstor und Changelog</name>
  <files>CHANGELOG.md</files>

  <read_first>
    - CHANGELOG.md — die zu ändernde Datei; Aufbau der Abschnitte `[Unreleased]` mit
      `Added` / `Changed` / `Fixed`
    - .claude/CLAUDE.md, Abschnitt „Pre-Commit Checklist" und „Quality Gates" — die hier
      abzuarbeitenden Prüfungen
    - .planning/phases/08-kontoauszug-f-r-mitarbeiter-und-admin/08-01-SUMMARY.md,
      08-02-SUMMARY.md, 08-03-SUMMARY.md — was tatsächlich gebaut wurde; der Changelog-Eintrag
      wird daraus abgeleitet, nicht aus dem Plan
  </read_first>

  <action>
    Führe das Qualitätstor aus und dokumentiere das Ergebnis, bevor irgendetwas die Produktion
    erreicht. `.claude/CLAUDE.md` und die ROADMAP-Constraints verbieten ein Deployment ohne
    grünen Typcheck für **beide** Teilprojekte.

    - `cd server && npx tsc --noEmit`
    - `cd desktop && npx tsc --noEmit`
    - `cd server && npx vitest run src/services/vacationTransactionService.test.ts src/routes/vacationTransactionRoutes.test.ts`
    - `cd server && npx vitest run src/services/vacationConsistencyService.test.ts src/services/absenceVacationBooking.test.ts src/services/vacationCorrectionBooking.test.ts` — Regressionsnachweis, dass die Phase-5-bis-7-Logik unberührt ist

    Schlägt einer dieser Läufe fehl, wird nicht deployed, sondern der Fehler behoben.

    Bekannte Vorbelastung, die **kein** Abbruchgrund ist und im SUMMARY zu erwähnen ist: Laut
    Phase-7-SUMMARY schlagen `vacationEntitlementBooking.test.ts` und zwei Tests in
    `unifiedOvertimeService.test.ts` in der **Gesamtsuite** wegen einer über Dateigrenzen
    geteilten Test-Datenbank fehl, isoliert aber grün. Führe die Gesamtsuite
    (`cd server && npx vitest run`) einmal aus und vergleiche die Fehlerliste mit dieser
    Vorbelastung — es darf kein **neuer** Fehlschlag hinzukommen.

    Trage anschließend unter `[Unreleased]` in `CHANGELOG.md` einen `Added`-Eintrag zum
    Urlaubs-Kontoauszug ein: Endpunkt `GET /api/vacation-transactions` mit serverseitiger
    Rollenprüfung, Auszug für Mitarbeiter und Admin, Verlinkung auf den auslösenden
    Abwesenheitsantrag, Pflichtbegründung bei Admin-Korrekturen. Ergänze unter `Fixed`, dass
    `GET /api/vacation-balances/:userId` fremde Konten nicht mehr ausliefert.
  </action>

  <acceptance_criteria>
    - `cd server && npx tsc --noEmit` endet mit Exit-Code 0
    - `cd desktop && npx tsc --noEmit` endet mit Exit-Code 0
    - `cd server && npx vitest run src/services/vacationTransactionService.test.ts src/routes/vacationTransactionRoutes.test.ts` endet mit Exit-Code 0
    - `cd server && npx vitest run src/services/vacationConsistencyService.test.ts src/services/absenceVacationBooking.test.ts src/services/vacationCorrectionBooking.test.ts` endet mit Exit-Code 0
    - Die Gesamtsuite `cd server && npx vitest run` weist keine anderen fehlschlagenden Dateien auf als `vacationEntitlementBooking.test.ts` und `unifiedOvertimeService.test.ts`
    - `grep -c "Kontoauszug" CHANGELOG.md` ist mindestens `1`
    - `grep -c "vacation-transactions" CHANGELOG.md` ist mindestens `1`
  </acceptance_criteria>

  <verify>
    <automated>cd server && npx tsc --noEmit && npx vitest run src/services/vacationTransactionService.test.ts src/routes/vacationTransactionRoutes.test.ts && cd ../desktop && npx tsc --noEmit</automated>
  </verify>

  <done>Beide Typprüfungen und alle gezielten Testläufe sind grün, der Changelog beschreibt die Änderung.</done>
</task>

<task type="auto">
  <name>Task 2: Server nach Produktion deployen und Endpunkt live prüfen</name>
  <files>(keine — Deployment und Verifikation)</files>

  <read_first>
    - .claude/CLAUDE.md, Abschnitt „Deployment Verification Rules (CRITICAL!)" — die
      verbindliche Prüfreihenfolge nach jedem Deployment
    - .github/workflows/deploy-server.yml — Auslöser ist ein Push auf `main` mit Änderungen
      unter `server/**`
    - server/src/server.ts — der in 08-01 ergänzte Mount-Eintrag, der live erreichbar sein muss
  </read_first>

  <action>
    Bringe die Serveränderungen aus Plan 08-01 in die Produktion und weise nach, dass sie dort
    wirken.

    - Commits der Pläne 08-01 bis 08-03 sowie den Changelog-Commit aus Task 1 nach `main`
      pushen (`git push origin main`). `deploy-server.yml` startet dadurch automatisch, weil
      Dateien unter `server/**` geändert wurden.
    - Deployment-Status prüfen: `gh run list --workflow="deploy-server.yml" --limit 1` —
      erwartet wird `completed` mit `success`. Bei `failure` die Logs über
      `gh run view <run-id> --log-failed` auswerten und den Fehler beheben, bevor es weitergeht.
    - Nach 2–3 Minuten Health-Check: `curl -s http://129.159.8.19:3000/api/health` —
      erwartet `"status":"ok"` und eine verbundene Datenbank.
    - Live-Nachweis des neuen Endpunkts ohne Anmeldung:
      `curl -s -o /dev/null -w "%{http_code}" http://129.159.8.19:3000/api/vacation-transactions`
      — erwartet `401`. Ein `404` würde bedeuten, dass der Router nicht gemountet ist; ein `200`
      wäre ein Sicherheitsfehler. `401` belegt beides zugleich: Der Endpunkt ist da **und** er
      ist geschützt.
    - Gegenprobe, dass der Kontostand-Endpunkt weiterhin existiert und geschützt ist:
      `curl -s -o /dev/null -w "%{http_code}" http://129.159.8.19:3000/api/vacation-balances/1`
      — erwartet `401`.

    Keine Änderung direkt auf dem Server vornehmen — `.claude/CLAUDE.md` verbietet das
    ausdrücklich; der Weg führt ausschließlich über `git push`.
  </action>

  <acceptance_criteria>
    - `gh run list --workflow="deploy-server.yml" --limit 1` zeigt für den neuen Commit `completed` und `success`
    - `curl -s http://129.159.8.19:3000/api/health` enthält `"status":"ok"`
    - `curl -s -o /dev/null -w "%{http_code}" http://129.159.8.19:3000/api/vacation-transactions` gibt exakt `401` aus
    - `curl -s -o /dev/null -w "%{http_code}" http://129.159.8.19:3000/api/vacation-balances/1` gibt exakt `401` aus
    - `git log origin/main -1 --oneline` zeigt den Changelog-Commit aus Task 1
  </acceptance_criteria>

  <verify>
    <automated>gh run list --workflow="deploy-server.yml" --limit 1 && curl -s http://129.159.8.19:3000/api/health && curl -s -o /dev/null -w "%{http_code}\n" http://129.159.8.19:3000/api/vacation-transactions</automated>
  </verify>

  <done>Der Journal-Endpunkt läuft in Produktion, ist gemountet und weist unauthentifizierte Zugriffe ab.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Abnahme des Kontoauszugs gegen echte Daten</name>
  <files>(keine — Abnahme)</files>

  <read_first>
    - .planning/phases/07-saldo-aus-buchungen-backfill/SUMMARY.md — die erwarteten Zahlen aus
      dem Produktions-Backfill: Carmen Rothemund Rest 5, Benedikt Jochem Rest 15, Gesamt
      Verbleibend 2026 = 98 Tage, 52 Buchungen
    - .planning/ROADMAP.md, Abschnitt „Phase 8: Kontoauszug für Mitarbeiter und Admin" —
      die vier Erfolgskriterien der Phase
  </read_first>

  <action>
    Halte die Ausführung an und lege dem Anwender die unten stehende Prüfliste vor. Führe die
    Prüfung nicht selbst durch und werte sie nicht als bestanden — die Punkte 2 bis 7 verlangen
    eine Sichtprüfung gegen echte Produktionsdaten und eine Anmeldung als Mitarbeiter, die nur
    der Anwender vornehmen kann.

    Bereite dem Anwender die Ausgangslage vor: Stelle sicher, dass ein lauffähiger Stand
    verfügbar ist (entweder Desktop-App gegen Produktion über `/promote-to-prod` oder lokal
    `npm run sync-dev-db`, danach `cd server && npm run dev` und `cd desktop && npm run dev`)
    und nenne die konkreten Erwartungswerte aus dem Phase-7-Backfill: Carmen Rothemund Rest 5,
    Benedikt Jochem Rest 15, Gesamt Verbleibend 2026 = 98 Tage.

    Fahre erst nach dem Freigabesignal fort. Meldet der Anwender eine Abweichung mit
    Schrittnummer, wird diese behoben und die Prüfliste erneut vorgelegt — ohne Freigabe wird
    das Release in Plan 08-05 nicht gestartet.
  </action>

  <what-built>
    - `GET /api/vacation-transactions` mit serverseitiger Rollenprüfung (Plan 08-01)
    - Endpunkt `GET /api/vacation-balances/:userId` gibt fremde Konten nicht mehr heraus
    - Kontoauszug-Komponente auf der Abwesenheiten-Seite (eigenes Konto, Jahreswahl) und in
      der Urlaubskonto-Verwaltung (Auszug je Mitarbeiter, Jahr aus der Jahreswahl der Seite)
    - Verlinkung jeder Antragsbuchung auf die Daten des auslösenden Abwesenheitsantrags
    - Pflichtfeld „Begründung" im Admin-Dialog des Urlaubskontos
    - Server ist deployed, der Endpunkt ist live und geschützt (Task 2)
  </what-built>

  <how-to-verify>
    Starte die Desktop-App lokal gegen den Produktionsserver (`/promote-to-prod`) oder gegen den
    lokalen Dev-Server mit einer frischen Kopie der Produktionsdatenbank
    (`npm run sync-dev-db`, dann `cd server && npm run dev` und `cd desktop && npm run dev`).

    1. **Als Admin anmelden.** Öffne „Urlaubskonto-Verwaltung", stelle das Jahr auf 2026.
       Notiere für Carmen Rothemund den Wert der Spalte „Verbleibend" (erwartet: 5).
    2. Klicke in Carmens Zeile auf „Auszug".
       - Erwartet: Eine Buchung „Urlaub genehmigt" mit **−6** Tagen und danach eine Buchung
         „Storno" mit **+6** Tagen (Storno-Datum 18.08.2026, nach der Genehmigung).
       - Erwartet: Der Saldo oben rechts im Auszug ist **derselbe Wert** wie die Spalte
         „Verbleibend" in der Tabelle dahinter.
       - Erwartet: Die Spalte „Saldo" läuft von Zeile zu Zeile fort und endet auf demselben Wert.
    3. Klicke in der Storno- oder Genehmigungszeile auf „Antrag #…".
       - Erwartet: Ein Fenster mit Zeitraum, Typ „Urlaub", Status und Tagen des Antrags.
    4. Schließe den Auszug, klicke bei einem beliebigen Mitarbeiter auf „Bearbeiten".
       - Erwartet: Speichern ohne Begründung ist nicht möglich (Fehlermeldung am Feld).
       - Trage eine Begründung ein, ändere den Anspruch um einen Tag, speichere.
       - Öffne danach den Auszug desselben Mitarbeiters: Erwartet ist eine neue Buchung
         „Korrektur" mit **genau dem eingegebenen Text** als Beschreibung.
       - Setze den Anspruch anschließend mit einer zweiten Korrektur wieder auf den
         Ausgangswert zurück, damit die Produktionsdaten unverändert bleiben.
    5. **Als Mitarbeiter anmelden** (nicht Admin). Öffne „Abwesenheiten".
       - Erwartet: Der Kontoauszug zeigt das **eigene** Konto, mit Jahreswahl.
       - Erwartet: Es gibt keine Möglichkeit, in der Oberfläche ein fremdes Konto zu wählen.
    6. **Zugriffsprobe über die API.** Melde dich als Mitarbeiter in der App an, öffne die
       Entwicklerkonsole (F12) und rufe den Endpunkt mit der Id eines **anderen** Mitarbeiters
       auf: `fetch('http://129.159.8.19:3000/api/vacation-transactions?userId=<fremdeId>',
       { credentials: 'include' }).then(r => console.log(r.status))`.
       - Erwartet: `403`. Nicht 200, und insbesondere nicht die eigenen Daten unter fremder Id.
    7. **Gesamtsumme prüfen.** Zurück als Admin: Die Kachel „Gesamt Verbleibend" in der
       Urlaubskonto-Verwaltung zeigt für 2026 weiterhin **98 Tage** (abzüglich der in Schritt 4
       vorgenommenen und wieder zurückgenommenen Korrektur, die sich zu null aufheben muss).
  </how-to-verify>

  <acceptance_criteria>
    - Schritt 2: Carmens Auszug enthält eine Zeile mit −6 und eine spätere Zeile mit +6
    - Schritt 2: Der im Auszug angezeigte Saldo ist identisch mit der Spalte „Verbleibend"
    - Schritt 3: Das Antragsfenster öffnet sich und zeigt Zeitraum, Typ, Status und Tage
    - Schritt 4: Speichern ohne Begründung wird verhindert; die eingegebene Begründung
      erscheint als Beschreibung der Korrekturbuchung
    - Schritt 5: Der Mitarbeiter sieht ausschließlich sein eigenes Konto
    - Schritt 6: Der API-Aufruf auf ein fremdes Konto liefert Statuscode 403
    - Schritt 7: „Gesamt Verbleibend" 2026 steht weiterhin auf 98 Tagen
  </acceptance_criteria>

  <verify>
    <human-check>Die sieben Punkte der Prüfliste wurden vom Anwender gegen echte Daten geprüft und mit „freigegeben" bestätigt.</human-check>
    <automated>curl -s -o /dev/null -w "%{http_code}\n" http://129.159.8.19:3000/api/vacation-transactions</automated>
  </verify>

  <resume-signal>Antworte mit „freigegeben", wenn alle sieben Punkte zutreffen — sonst beschreibe die Abweichung mit Schrittnummer.</resume-signal>

  <done>Der Anwender hat den Kontoauszug gegen echte Daten abgenommen; die Phase ist releasefähig.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Entwicklungsrechner → GitHub → Produktionsserver | Der Deploy-Pfad ist die einzige zulässige Änderungsroute; direkte Eingriffe auf dem Server sind untersagt |
| Öffentliches Internet → `http://129.159.8.19:3000` | Der Server ist ohne TLS öffentlich erreichbar; jeder Endpunkt muss selbst autorisieren |
| Abnahmeschritt 4 → Produktionsdaten | Die Abnahme schreibt eine echte Korrekturbuchung in die Live-Datenbank |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-30 | Information Disclosure | Neuer Endpunkt in Produktion | mitigate | Live-Nachweis über einen unauthentifizierten `curl`-Aufruf: erwartet wird exakt 401, nicht 200 und nicht 404 |
| T-08-31 | Elevation of Privilege | Rollenprüfung unter echten Sessions | mitigate | Abnahmeschritt 6 ruft den Endpunkt mit der Session eines echten Mitarbeiters und fremder `userId` auf; erwartet wird 403 |
| T-08-32 | Tampering | Korrekturbuchung während der Abnahme verändert Produktionsdaten | mitigate | Schritt 4 verlangt ausdrücklich eine zweite, gegenläufige Korrektur; Schritt 7 prüft, dass die Gesamtsumme 2026 wieder bei 98 Tagen steht. Beide Buchungen bleiben als Historie erhalten — das ist gewollt und kein Datenverlust |
| T-08-33 | Denial of Service | Fehlgeschlagenes Deployment lässt den Server unten | mitigate | Verbindliche Prüfreihenfolge aus `.claude/CLAUDE.md`: Actions-Status, Health-Check, Endpunkt-Probe. Bei `failure` Logauswertung vor jedem weiteren Schritt |
| T-08-34 | Information Disclosure | Unverschlüsselter Transport (HTTP) zum Produktionsserver | accept | Vorbestehende Eigenschaft der Infrastruktur, unverändert durch diese Phase; eine TLS-Umstellung ist ein eigener Infrastrukturvorgang und nicht Teil von Milestone 2 |
| T-08-SC | Tampering (Supply Chain) | npm-Installationen | mitigate | Dieser Plan installiert **kein** Paket; verwendet werden `git`, `gh` und `curl`. Wird wider Erwarten eines nötig, ist vor der Installation ein blockierender Human-Checkpoint mit Prüfung auf `npmjs.com/package` einzulegen |
</threat_model>

<verification>
- `cd server && npx tsc --noEmit` und `cd desktop && npx tsc --noEmit` je Exit-Code 0
- `gh run list --workflow="deploy-server.yml" --limit 1` meldet `completed` / `success`
- `curl -s http://129.159.8.19:3000/api/health` enthält `"status":"ok"`
- `curl -s -o /dev/null -w "%{http_code}" http://129.159.8.19:3000/api/vacation-transactions` gibt `401`
- Abnahme durch den Anwender liegt vor (Task 3)
</verification>

<success_criteria>
- Kein Deployment ohne grünen Typcheck — beide Teilprojekte sind nachweislich fehlerfrei
- Der Journal-Endpunkt ist in Produktion erreichbar und geschützt
- Carmens Storno-Geschichte ist im Auszug nachvollziehbar (−6, +6)
- Der angezeigte Saldo stimmt mit der Urlaubsliste überein
- Der Zugriff auf ein fremdes Konto scheitert mit 403 — geprüft unter einer echten Session
</success_criteria>

<output>
Create `.planning/phases/08-kontoauszug-f-r-mitarbeiter-und-admin/08-04-SUMMARY.md` when done
</output>
