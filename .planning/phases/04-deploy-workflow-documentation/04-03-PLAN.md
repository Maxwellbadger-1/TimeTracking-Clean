---
phase: 04-deploy-workflow-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ENV.md
  - WINDOWS_PC_SETUP.md
  - .claude/CLAUDE.md
autonomous: true
requirements:
  - "deploy-server.yml DB-Pfad-Verifikation nach Deployment"
  - "Green Server on-demand"

must_haves:
  truths:
    - "ENV.md contains no references to database-shared.db or database-production.db as active paths"
    - "ENV.md documents npm run sync-dev-db as the standard local DB setup command"
    - "WINDOWS_PC_SETUP.md uses /home/ubuntu/databases/production.db as the production path"
    - "CLAUDE.md DB Rules section states DATABASE_PATH=/home/ubuntu/databases/production.db"
    - "CLAUDE.md describes the 2-Tier workflow instead of the 3-Tier workflow as standard"
  artifacts:
    - path: "ENV.md"
      provides: "Updated environment documentation with 2-Tier architecture"
      contains: "/home/ubuntu/databases/production.db"
    - path: "WINDOWS_PC_SETUP.md"
      provides: "Updated Windows setup with sync-dev-db command"
      contains: "npm run sync-dev-db"
    - path: ".claude/CLAUDE.md"
      provides: "Updated AI guidelines with 2-Tier workflow"
      contains: "2-Tier"
  key_links:
    - from: "ENV.md"
      to: "scripts/sync-dev-db.sh"
      via: "npm run sync-dev-db reference"
      pattern: "npm run sync-dev-db"
    - from: ".claude/CLAUDE.md"
      to: "/home/ubuntu/databases/production.db"
      via: "DB Rules section"
      pattern: "databases/production.db"
---

<objective>
Update all developer-facing documentation (ENV.md, WINDOWS_PC_SETUP.md, CLAUDE.md) to reflect the 2-Tier architecture established in Phases 1-3.

Purpose: Documentation that describes the old 3-Tier flow or stale DB paths will confuse future development sessions. All docs must reflect the current reality: single production DB at /home/ubuntu/databases/production.db, npm run sync-dev-db for local setup, and git push main as the standard deploy path.
Output: Three updated documentation files with consistent 2-Tier references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deploy-workflow-documentation/04-RESEARCH.md
@ENV.md
@WINDOWS_PC_SETUP.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ENV.md and WINDOWS_PC_SETUP.md with 2-Tier architecture</name>
  <files>ENV.md, WINDOWS_PC_SETUP.md</files>
  <read_first>
    - ENV.md (full file — identify all stale path references; RESEARCH says: line 9 "3-Tier Environment Overview", line 105 old DATABASE_PATH, line 118 staging path, line 285 old PROD_DB_PATH)
    - WINDOWS_PC_SETUP.md (full file — RESEARCH says: line 105 old DATABASE_PATH, line 154 old PROD_DB_PATH, missing sync-dev-db)
    - .planning/phases/04-deploy-workflow-documentation/04-RESEARCH.md (section "Exact Current State of Files to Edit" for ENV.md and WINDOWS_PC_SETUP.md, Pitfall 4)
    - scripts/sync-dev-db.sh (reference for documenting sync-dev-db usage)
  </read_first>
  <action>
**ENV.md changes:**

1. **Line 2 heading area:** Change `3-Tier Environment Configuration System` to `2-Tier Environment Configuration System`

2. **Line 7-12 overview diagram:** Replace the 3-Tier diagram with:
```
## 2-Tier Environment Overview

Development (Local)  →  Production (Blue:3000)
  server/database.db      /home/ubuntu/databases/production.db
  Synced via scp           Live customer data

Local DB setup: npm run sync-dev-db (pulls production DB via SCP)
Green Server (port 3001) is available on-demand but not part of the standard flow.
```

3. **Line ~105 (Blue Server section):** Replace `DATABASE_PATH=/home/ubuntu/database-production.db` with `DATABASE_PATH=/home/ubuntu/databases/production.db`

4. **Line ~118 (Green Server section):** Replace `DATABASE_PATH=/home/ubuntu/database-staging.db` with a note: `DATABASE_PATH=/home/ubuntu/databases/production.db` (Green Server uses a COPY of production data, synced via /sync-green)

5. **Line ~285 (Sync Script section):** Replace `PROD_DB_PATH=/home/ubuntu/TimeTracking-Clean/server/database.db` with `PROD_DB_PATH=/home/ubuntu/databases/production.db`

6. **Add a new section** after the Quick Start section titled "Local DB Setup (Standard)" with:
```markdown
### Local DB Setup (Standard)

```bash
# Pull production DB for local development (Windows Git Bash compatible)
npm run sync-dev-db

# What it does:
# 1. Backs up existing server/database.db (timestamped)
# 2. SCPs production.db from /home/ubuntu/databases/production.db
# 3. Runs PRAGMA integrity_check
# 4. Prints user count and latest time entry date
```
```

7. **Green Server PORT troubleshooting section (lines ~123-181):** Keep intact but add a note at the top: "UPDATE (2026-04-02): The PORT=3001 fix has been applied in deploy-staging.yml. PORT is now passed as a shell prefix to pm2 start."

8. Wherever `database-shared.db` appears as a "current" or "active" path, replace with a note: "The original /home/ubuntu/database-shared.db is retained as a safety backup but is no longer the active DB. The canonical production path is /home/ubuntu/databases/production.db."

**WINDOWS_PC_SETUP.md changes:**

1. **Line ~105:** Replace `DATABASE_PATH=./database/development.db` with `DATABASE_PATH=./database.db` (matches actual server config)

2. **Line ~154:** Replace `PROD_DB_PATH=/home/ubuntu/TimeTracking-Clean/server/database.db` with `PROD_DB_PATH=/home/ubuntu/databases/production.db`

3. **Add a section** after the dependencies installation step (or in the "Development starten" section) titled "Datenbank Setup" with:
```markdown
### Datenbank Setup

```bash
# Production DB fuer lokale Entwicklung herunterladen
npm run sync-dev-db

# Voraussetzung: SSH-Zugang zum Oracle Server
# Script: scripts/sync-dev-db.sh
# Ergebnis: server/database.db wird mit aktueller Production DB ersetzt
```
```

IMPORTANT: Do NOT delete any Green Server or staging references entirely — they are still valid on-demand tools. Only clarify they are not part of the standard 2-Tier flow.
  </action>
  <verify>
    <automated>grep -c "database-production.db" ENV.md | grep -q "^0$" && grep -c "3-Tier Environment Configuration" ENV.md | grep -q "^0$" && grep "databases/production.db" ENV.md && grep "npm run sync-dev-db" ENV.md && grep "databases/production.db" WINDOWS_PC_SETUP.md && grep "npm run sync-dev-db" WINDOWS_PC_SETUP.md && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - ENV.md contains 0 occurrences of `database-production.db` as an active/current path
    - ENV.md heading says `2-Tier` (not `3-Tier`)
    - ENV.md contains `/home/ubuntu/databases/production.db` as the canonical production path
    - ENV.md contains `npm run sync-dev-db` in a "Local DB Setup" section
    - ENV.md Green Server PORT troubleshooting section still exists and has the "UPDATE (2026-04-02)" note
    - WINDOWS_PC_SETUP.md contains `PROD_DB_PATH=/home/ubuntu/databases/production.db`
    - WINDOWS_PC_SETUP.md contains `npm run sync-dev-db` in a Datenbank Setup section
    - Neither file has deleted all Green Server/staging references (they are marked as on-demand, not removed)
  </acceptance_criteria>
  <done>ENV.md and WINDOWS_PC_SETUP.md reflect the 2-Tier architecture with correct DB paths, document npm run sync-dev-db, and clarify Green Server is on-demand only</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md with 2-Tier workflow and correct DB path</name>
  <files>.claude/CLAUDE.md</files>
  <read_first>
    - .claude/CLAUDE.md (full file — RESEARCH says: line 106 stale DATABASE_PATH, line 208 staging branch reference, lines 228-254 3-Tier workflow section)
    - .planning/phases/04-deploy-workflow-documentation/04-RESEARCH.md (section "Exact Current State of Files to Edit" for CLAUDE.md, Pitfall 5, Open Question 3)
  </read_first>
  <action>
**Change 1: DB Rules section (around line 106)**

Find the line containing `DATABASE_PATH=/home/ubuntu/database-production.db` and replace with:
```
DATABASE_PATH=/home/ubuntu/databases/production.db
```

Also update the "One Database" rule comment to clarify the symlink:
```
1. **One Database:** Production at `/home/ubuntu/databases/production.db`, locally `server/database.db` (symlinked on server to production.db)
```

**Change 2: Feature Development workflow (around line 208)**

Replace step 5-6 which currently read:
```
5. Push zu staging branch -> Auto-Deploy Green Server
6. Testen auf Green Server (/green command) mit echten Production-Daten
```

With:
```
5. Push zu main branch -> Auto-Deploy Blue Server (Production)
6. Verify: curl http://129.159.8.19:3000/api/health
7. Optional: Use Green Server (/green) for isolated testing before main push
```

Remove the line that says `**Wichtig:** IMMER 3-Tier Workflow nutzen (Development -> Staging -> Production)!` and replace with:
```
**Standard Flow:** Development (local) -> git push main -> Auto-Deploy Blue Server
**Optional:** Use staging branch + Green Server for isolated testing (on-demand only)
```

**Change 3: Production Deployment section (around lines 228-254)**

Replace the heading `## Production Deployment (3-Tier Workflow)` with `## Production Deployment (2-Tier Workflow)`

Replace the code-flow line:
```
**Code-Flow:** Local -> `staging` branch -> Green Server:3001 -> `/promote-to-prod` -> `main` branch -> Blue Server:3000
```
With:
```
**Code-Flow:** Local -> `main` branch -> Blue Server:3000 (auto-deploy via deploy-server.yml)
**Data-Flow:** Production DB -> `npm run sync-dev-db` -> Local `server/database.db`
```

Replace the Deployment Steps with:
```bash
# 1. Development (Local)
npm run sync-dev-db              # Pull fresh production DB
npm run dev                      # Develop & test locally

# 2. Deploy to Production
git add . && git commit -m "feat: ..."
git push origin main             # Auto-Deploy -> Blue Server:3000

# 3. Verify
curl -s http://129.159.8.19:3000/api/health
# GitHub Actions also runs DB path verification automatically

# 4. EMERGENCY Rollback
/rollback-prod                   # Git revert + Auto-Deploy
```

Replace the Best Practices:
```
- Standard flow: develop on main, push to deploy
- Green Server (/green, /sync-green) available for isolated testing but NOT required
- /promote-to-prod and /rollback-prod commands still work as emergency tools
- Database migrations must be backward-compatible
- After deployment: check CHANGELOG.md
```

**Change 4: Workflow Verbote section**

Find the line `- Auf main branch arbeiten -> Feature-Branch nutzen` and replace with:
`- Direkt auf Production server arbeiten -> Immer lokal entwickeln und via git push deployen`

(The old rule says "never work on main" which contradicts the 2-Tier flow where main IS the deploy branch.)

Do NOT delete `/promote-to-prod`, `/rollback-prod`, `/sync-green`, or `/green` command references — they are still valid tools. Only clarify they are optional/emergency, not required in the standard flow.
  </action>
  <verify>
    <automated>grep "databases/production.db" ".claude/CLAUDE.md" && grep "2-Tier Workflow" ".claude/CLAUDE.md" && grep -c "database-production.db" ".claude/CLAUDE.md" | grep -q "^0$" && grep "npm run sync-dev-db" ".claude/CLAUDE.md" && grep -c "3-Tier Workflow" ".claude/CLAUDE.md" | grep -q "^0$" && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains `DATABASE_PATH=/home/ubuntu/databases/production.db` (not the old `database-production.db` path)
    - CLAUDE.md contains `2-Tier Workflow` heading (not `3-Tier Workflow`)
    - CLAUDE.md contains 0 occurrences of `database-production.db` as an active path
    - CLAUDE.md contains `npm run sync-dev-db` in the deployment workflow section
    - CLAUDE.md still contains references to `/promote-to-prod`, `/rollback-prod` (as emergency tools)
    - CLAUDE.md still contains references to `/green` and `/sync-green` (as optional tools)
    - CLAUDE.md code-flow line says `Local -> main branch -> Blue Server:3000`
  </acceptance_criteria>
  <done>CLAUDE.md DB Rules section shows the correct production DB path, and the workflow sections describe the 2-Tier flow with Green Server marked as optional</done>
</task>

</tasks>

<verification>
- `grep -r "database-production.db" ENV.md WINDOWS_PC_SETUP.md .claude/CLAUDE.md` returns zero matches
- `grep -r "databases/production.db" ENV.md WINDOWS_PC_SETUP.md .claude/CLAUDE.md` returns matches in all three files
- `grep -r "npm run sync-dev-db" ENV.md WINDOWS_PC_SETUP.md .claude/CLAUDE.md` returns matches in all three files
- `grep "2-Tier" .claude/CLAUDE.md` returns matches; `grep "3-Tier Workflow" .claude/CLAUDE.md` returns zero
</verification>

<success_criteria>
- All three documentation files use /home/ubuntu/databases/production.db as the canonical production path
- All three files document npm run sync-dev-db as the standard local DB setup command
- CLAUDE.md describes the 2-Tier workflow as standard, with Green Server as optional
- No stale path references (database-production.db, database-shared.db as active path) remain
</success_criteria>

<output>
After completion, create `.planning/phases/04-deploy-workflow-documentation/04-03-SUMMARY.md`
</output>
