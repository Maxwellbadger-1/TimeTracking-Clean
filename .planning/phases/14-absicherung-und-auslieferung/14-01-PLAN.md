---
phase: 14-absicherung-und-auslieferung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/services/workPeriodChangeService.test.ts
  - .planning/phases/14-absicherung-und-auslieferung/14-REQ32-NACHWEIS.md
autonomous: true
requirements: [REQ-32]

must_haves:
  truths:
    - "Jeder der fünf REQ-32-Fälle ist namentlich einem einzelnen, laufenden Test zugeordnet — kein Sammelposten 'Tests grün'"
    - "Der Fall 'Erhöhung mit rückwirkendem Stichtag' wird von einem eigenen Test abgedeckt, der ohne den Fix rot wäre"
    - "Die Menge der roten Tests bleibt bei genau drei, mit unveränderten Titeln"
  artifacts:
    - path: ".planning/phases/14-absicherung-und-auslieferung/14-REQ32-NACHWEIS.md"
      provides: "Zuordnungstabelle der fünf REQ-32-Fälle zu Datei, Testtitel und Zeilennummer, plus die wörtliche vitest-Ausgabe je Fall"
      contains: "Erhöhung mit rückwirkendem Stichtag"
    - path: "server/src/services/workPeriodChangeService.test.ts"
      provides: "Neuer it-Block für den Erhöhungsfall (Basis 20 h → 40 h, rückwirkender Stichtag)"
      contains: "Erhoehung"
  key_links:
    - from: "14-REQ32-NACHWEIS.md"
      to: "server/src/services/workPeriodChangeService.test.ts"
      via: "Testtitel + Zeilennummer je REQ-32-Fall"
      pattern: "workPeriodChangeService.test.ts:[0-9]+"
    - from: "14-REQ32-NACHWEIS.md"
      to: "server/src/services/workPeriodDeletionService.test.ts"
      via: "Testtitel des Doppelzählungs-Nachweises"
      pattern: "workPeriodDeletionService.test.ts"
---

<objective>
REQ-32 verlangt fünf **einzeln nachgewiesene** Wechselfälle. Vier davon sind bereits als
laufende Tests vorhanden (Phase 12/13); genau einer fehlt. Dieser Plan weist die vier
vorhandenen namentlich nach und schließt die eine echte Lücke — er baut keine Testsuite neu.

Purpose: REQ-32 wird als geschlossene, prüfbare Nachweisliste belegt statt als pauschales
„Tests sind grün".
Output: `14-REQ32-NACHWEIS.md` mit fünf benannten Zuordnungen und ein neuer `it`-Block für
„Erhöhung mit rückwirkendem Stichtag".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-absicherung-und-auslieferung/14-CONTEXT.md
@.planning/phases/14-absicherung-und-auslieferung/14-PATTERNS.md
@.planning/phases/11-datumsabh-ngige-berechnung/11-AUSGANGSZUSTAND.md

<befund_aus_der_planung>
Am Code nachgeprüft (nicht aus PATTERNS.md übernommen), Stand HEAD 207dab9:

| REQ-32-Fall | Fundstelle | Status |
|---|---|---|
| Reduzierung (40→20), Stichtag in der Zukunft | `server/src/services/workPeriodChangeService.test.ts:172` — `it('REQ-28: Ein Stichtag in der Zukunft laesst jede Buchung davor unveraendert und erzeugt keine model_change-Buchung')` | vorhanden |
| Erhöhung, Stichtag rückwirkend | — | **fehlt** |
| Stichtag mitten im Monat | `workPeriodChangeService.test.ts:577` — `it('Stichtag mitten im Monat: midMonthEffective ist true, ein Tag davor traegt das alte, ein Tag ab dem Stichtag das neue Tagessoll')` | vorhanden |
| Wechsel über einen Jahreswechsel | `workPeriodChangeService.test.ts:670` — `it('Wechsel ueber einen Jahreswechsel: affectedMonths deckt zwei Kalenderjahre ab, der Lauf endet ohne Fehler')` | vorhanden |
| Periode löschen und danach neu rechnen | `server/src/services/workPeriodDeletionService.test.ts:180` — der Doppelzählungs-Nachweis mit den Zusicherungen A–D | vorhanden |

Beleg für die Lücke: Jeder `createEmployee(...)`-Aufruf in `workPeriodChangeService.test.ts`
legt den Nutzer mit `weeklyHours = 40` an; jeder **erfolgreiche** `applyWorkTimeChange`-Aufruf
setzt einen **kleineren** Zielwert (20/25/30). Die einzigen Aufrufe mit einem größeren
Zielwert stehen in Validierungstests, die vor dem Schreiben abbrechen
(Zeile 769 `doppelter Stichtag`, Zeile 897 `Tagesplan ausserhalb 0..24`, Zeile 1035 `isNoOp`).
Ein Lauf „von wenig auf viel" wird damit nirgends geschrieben und nirgends geprüft.
</befund_aus_der_planung>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nachweisliste der fünf REQ-32-Fälle anlegen und die vier vorhandenen Fälle einzeln laufen lassen</name>
  <files>.planning/phases/14-absicherung-und-auslieferung/14-REQ32-NACHWEIS.md</files>
  <read_first>
    - server/src/services/workPeriodChangeService.test.ts (Zeilen 160-300, 570-700 — die drei betroffenen it-Blöcke im Original lesen, nicht aus der Tabelle oben übernehmen)
    - server/src/services/workPeriodDeletionService.test.ts (Zeilen 170-270 — Zusicherungen A–D des Doppelzählungs-Nachweises)
    - .planning/phases/14-absicherung-und-auslieferung/14-PATTERNS.md (Abschnitt 1)
    - .planning/REQUIREMENTS.md (Wortlaut REQ-32)
  </read_first>
  <action>
Lege `.planning/phases/14-absicherung-und-auslieferung/14-REQ32-NACHWEIS.md` an. Aufbau:
eine Tabelle mit fünf Zeilen — je Zeile der REQ-32-Fall im Wortlaut aus `REQUIREMENTS.md`,
die Testdatei, der vollständige `it(...)`-Titel und die Zeilennummer, dazu eine Spalte
„Nachweislauf" mit der wörtlichen vitest-Ausgabezeile.

Prüfe jede Zeilennummer selbst per `grep -n` nach, bevor du sie einträgst — die Nummern im
Kontextblock oben sind eine Planungsangabe zum Stand 207dab9 und können durch Task 2
verschoben sein.

Fahre für die vier bereits vorhandenen Fälle je einen einzelnen, benannten Testlauf mit
`npx vitest run -t "<exakter Titel>"` aus `server/` und übernimm die Ausgabezeile
(`Tests  1 passed`) wörtlich in die Spalte „Nachweislauf". Kein Sammellauf, kein „alle grün".

Trage den fünften Fall („Erhöhung mit rückwirkendem Stichtag") mit dem Status
`OFFEN — wird in Task 2 geschlossen` ein; Task 3 schreibt ihn fort.

Ergänze unter der Tabelle einen Abschnitt „Warum vier Fälle nicht neu gebaut werden" mit dem
Belegtext aus dem Kontextblock oben (Zielwert stets kleiner als 40, Gegenbeispiele nur in
Validierungstests) — damit ein späterer Leser sieht, dass die Lücke gemessen und nicht
geschätzt wurde.
  </action>
  <verify>
    <automated>cd server &amp;&amp; npx vitest run -t "Wechsel ueber einen Jahreswechsel"</automated>
  </verify>
  <acceptance_criteria>
    - Die Datei `.planning/phases/14-absicherung-und-auslieferung/14-REQ32-NACHWEIS.md` existiert und enthält genau fünf Fallzeilen.
    - Jede der vier vorhandenen Zeilen nennt Datei, vollständigen `it(...)`-Titel und eine per `grep -n` bestätigte Zeilennummer.
    - Für jeden der vier vorhandenen Fälle steht eine wörtliche `vitest`-Ausgabezeile mit `1 passed` in der Datei.
    - Die fünfte Zeile trägt den Status `OFFEN — wird in Task 2 geschlossen`.
    - `grep -c 'workPeriodChangeService.test.ts:[0-9]' 14-REQ32-NACHWEIS.md` liefert mindestens 3.
  </acceptance_criteria>
  <done>Fünf REQ-32-Fälle sind einzeln benannt; vier tragen einen eigenen, wörtlich protokollierten Einzeltestlauf.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Den fehlenden Fall schließen — Erhöhung der Wochenstunden mit rückwirkendem Stichtag</name>
  <files>server/src/services/workPeriodChangeService.test.ts</files>
  <read_first>
    - server/src/services/workPeriodChangeService.test.ts (vollständig Zeilen 1-300: Kopfkommentar, `USERNAME_PREFIX`, `createEmployee`, `cleanupEmployee`, `isoDate`, `firstOfMonthOffset`, `lastOfMonth`, `insertWeekdayTimeEntries`, und der Vorlage-Test bei Zeile 224)
    - server/src/test-support/workPeriodFixtures.ts (Signatur von `insertTestWorkPeriod`)
    - server/src/services/workPeriodChangeService.ts (Vorzeichenkonvention von `preview.balanceDelta`)
    - .planning/phases/14-absicherung-und-auslieferung/14-PATTERNS.md (Abschnitt 1, Absatz „Basissetup für Erhöhung")
  </read_first>
  <behavior>
    - Ausgangslage: Nutzer mit `weeklyHours = 20` ab `2020-01-01`, eine Periode `validFrom = '2020-01-01'`, `weeklyHours = 20`, `workSchedule = null`.
    - Ein abgerechneter Vormonat (`firstOfMonthOffset(today, -3)`) mit Werktagseinträgen zu 8 h und einem vorab gefahrenen `rebuildOvertimeTransactionsForMonth`.
    - Rückwirkender Stichtag `firstOfMonthOffset(today, -2)`, Zielwert `weeklyHours: 40`.
    - Zusicherung 1: Alle `overtime_transactions` mit `date < validFrom` sind nach dem Lauf byteweise identisch zu vorher (`expect(after).toEqual(before)`).
    - Zusicherung 2: `outcome.preview.balanceDelta` ist **kleiner als 0** — mehr Sollstunden rückwirkend senken den Saldo. Das ist das umgekehrte Vorzeichen zum Reduzierungsfall bei Zeile 224, der `toBeGreaterThan(0)` prüft.
    - Zusicherung 3: Genau eine `model_change`-Zeile, `date === validFrom`, `referenceType === 'work_period'`, `referenceId === outcome.period!.id`.
    - Zusicherung 4: `outcome.preview.targetHoursDelta` ist größer als 0 (das Sollstundenvolumen steigt) — die Größe, die die Richtung des Wechsels ohne Saldovorzeichen belegt.
  </behavior>
  <action>
Füge in `server/src/services/workPeriodChangeService.test.ts` innerhalb des bestehenden
`describe('applyWorkTimeChange — Erfolgskriterien der Phase 12 (ROADMAP)')` unmittelbar nach
dem Test bei Zeile 224 einen neuen `it`-Block mit dem Titel
`REQ-32: Eine Erhoehung der Wochenstunden mit rueckwirkendem Stichtag senkt den Saldo und laesst jede Buchung vor dem Stichtag unveraendert`
ein.

Übernimm den Aufbau des Nachbartests wörtlich: `createEmployee('erhoehung-rueckwirkend', 20, '2020-01-01')`,
`insertTestWorkPeriod(userId, { validFrom: '2020-01-01', weeklyHours: 20, workSchedule: null })`,
`insertWeekdayTimeEntries` für Vormonat und Rückwirkungszeitraum, `rebuildOvertimeTransactionsForMonth`
für den Vormonat, `applyWorkTimeChange({ userId, validFrom, weeklyHours: 40, workSchedule: null, reason: ... }, { dryRun: false, createdBy: adminId })`,
`try/finally` mit `cleanupEmployee(userId)`.

Vier Punkte, die vom Vorlagetest abweichen und nicht mitkopiert werden dürfen:
1. Basiswert 20 statt 40 in **beiden** Stellen (`createEmployee` und `insertTestWorkPeriod`) — sonst ist es keine Erhöhung.
2. `weeklyHours: 40` im `WorkTimeChangeInput`.
3. `expect(outcome.preview.balanceDelta).toBeLessThan(0)` statt `toBeGreaterThan(0)`.
4. Begründungstext `Erhoehung der Wochenstunden rueckwirkend zum Stichtag` (die Begründung wird wörtlich ins Journal geschrieben, WR-04 trimmt sie).

Nutze ausschließlich das vorhandene Präfix `test-12-05-` über `createEmployee` — kein neues
Präfix. Der Aufräumnachweis am Dateiende (Zeile 1158) prüft genau dieses Präfix und muss den
neuen Nutzer mit abdecken. Keine eigene Datumsarithmetik über `new Date('YYYY-MM-DD')` oder
`.toISOString()` — ausschließlich die vorhandenen Helfer `isoDate`, `firstOfMonthOffset`,
`lastOfMonth`.
  </action>
  <verify>
    <automated>cd server &amp;&amp; npx vitest run -t "REQ-32: Eine Erhoehung der Wochenstunden mit rueckwirkendem Stichtag"</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && npx vitest run -t "REQ-32: Eine Erhoehung der Wochenstunden mit rueckwirkendem Stichtag"` meldet `Tests  1 passed`.
    - `grep -n "createEmployee('erhoehung-rueckwirkend', 20," server/src/services/workPeriodChangeService.test.ts` liefert genau einen Treffer.
    - `grep -n "toBeLessThan(0)" server/src/services/workPeriodChangeService.test.ts` liefert mindestens einen Treffer innerhalb des neuen Blocks.
    - `cd server && npx vitest run -t "Aufraeumnachweis"` meldet `Tests  1 passed` (kein Testnutzer bleibt zurück).
    - `grep -n "new Date('20" server/src/services/workPeriodChangeService.test.ts` liefert keinen Treffer im neuen Block.
  </acceptance_criteria>
  <done>Der fünfte REQ-32-Fall ist als eigener, grüner Test vorhanden und räumt seinen Testnutzer restlos ab.</done>
</task>

<task type="auto">
  <name>Task 3: Die drei Gates fahren und die Nachweisliste schließen</name>
  <files>.planning/phases/14-absicherung-und-auslieferung/14-REQ32-NACHWEIS.md</files>
  <read_first>
    - .planning/phases/14-absicherung-und-auslieferung/14-REQ32-NACHWEIS.md (Stand nach Task 1)
    - .planning/phases/11-datumsabh-ngige-berechnung/11-AUSGANGSZUSTAND.md (Abschnitt „Baseline npx vitest run" — die drei verbindlichen roten Titel)
  </read_first>
  <action>
Fahre die vier Pflichtprüfungen und protokolliere jede wörtlich in einem neuen Abschnitt
„Gates nach Plan 14-01" von `14-REQ32-NACHWEIS.md`:

1. `cd server && npx tsc --noEmit` — erwartet Exit 0, keine Ausgabe.
2. `cd desktop && npx tsc --noEmit` — erwartet Exit 0, keine Ausgabe.
3. `cd server && npx vitest run` — erwartet **genau 3 rote Tests** mit unveränderten Titeln;
   die grüne Zahl steigt gegenüber dem Ausgangsstand 486 um den einen neuen Test auf 487.
4. `cd desktop && npm run check:rules` — erwartet Exit 0.

Die drei zulässigen roten Titel (aus `11-AUSGANGSZUSTAND.md`, verbindliche Menge):
`unifiedOvertimeService.test.ts > ... > should respect hire date and not include pre-employment months`,
`unifiedOvertimeService.test.ts > ... > REGRESSION: User hired on 1st of month should calculate correctly`,
`vacationBackfillService.test.ts > vacationBackfillService > erkennt einen bereits gelaufenen Backfill`.
Erscheint ein vierter roter Titel oder ändert sich einer der drei, ist das ein Abbruchgrund —
den Befund wörtlich in die Datei schreiben und den Plan als blockiert melden, nicht
weiterarbeiten.

Schließe danach die Tabellenzeile des fünften Falls: Status von `OFFEN` auf die Fundstelle
(Datei + Titel + per `grep -n` bestätigte Zeilennummer) und die wörtliche
`1 passed`-Ausgabezeile aus Task 2 umstellen. Prüfe abschließend die Zeilennummern der vier
anderen Fälle erneut per `grep -n` — Task 2 hat die Datei verlängert und alle Nummern hinter
Zeile 224 verschoben.
  </action>
  <verify>
    <automated>cd server &amp;&amp; npx vitest run 2>&amp;1 | grep -E "Tests +[0-9]+ failed"</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && npx tsc --noEmit` endet mit Exit 0 und ohne Ausgabe.
    - `cd desktop && npx tsc --noEmit` endet mit Exit 0 und ohne Ausgabe.
    - `cd server && npx vitest run` meldet `3 failed`, und die drei Titel stimmen wörtlich mit den drei aus `11-AUSGANGSZUSTAND.md` überein; die grüne Zahl ist mindestens 487.
    - `cd desktop && npm run check:rules` endet mit Exit 0.
    - In `14-REQ32-NACHWEIS.md` steht in keiner der fünf Zeilen mehr das Wort `OFFEN`.
    - Alle fünf Zeilennummern in `14-REQ32-NACHWEIS.md` sind nach Task 2 erneut per `grep -n` bestätigt und stimmen mit dem Dateistand überein.
  </acceptance_criteria>
  <done>REQ-32 ist mit fünf einzeln belegten Fällen geschlossen; die drei Gates stehen unverändert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Testlauf → `server/database/development.db` | Die Testsuite läuft gegen die geteilte lokale Arbeitsdatenbank, nicht gegen `:memory:`. Jeder Test schreibt echte Zeilen. |
| Planungsangabe → Nachweisdatei | Zeilennummern und Testtitel aus PATTERNS.md sind Sekundärquelle; die Nachweisdatei behauptet Primärquelle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | `server/database/development.db` | mitigate | Neuer Test nutzt `createEmployee`/`cleanupEmployee` mit `try/finally`; der bestehende Aufräumnachweis am Dateiende (Präfix `test-12-05-`) wird als eigenes Akzeptanzkriterium gefahren. |
| T-14-02 | Repudiation | `14-REQ32-NACHWEIS.md` | mitigate | Je Fall ein eigener `vitest run -t`-Lauf mit wörtlich übernommener Ausgabezeile statt einer Sammelbehauptung. |
| T-14-03 | Information Disclosure | Testfixtures | accept | Testnutzer tragen Platzhalterdaten (`hash` als Passwort, `@test.local`), keine echten Personendaten. |
| T-14-04 | Denial of Service | Regressionsschutz | mitigate | Die rote Testmenge (genau 3, mit festen Titeln) ist Abbruchkriterium, nicht Hinweis — ein neuer roter Test blockiert den Plan. |
| T-14-05 | Tampering | Produktionsdatenbank | mitigate | Kein Schritt dieses Plans setzt `DATABASE_PATH` oder öffnet einen Pfad außerhalb von `server/database/development.db`; keine SSH-Verbindung. |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | Dieser Plan installiert kein Paket. Entfällt mangels Installationsschritt. |
</threat_model>

<verification>
- `cd server && npx tsc --noEmit` → Exit 0
- `cd desktop && npx tsc --noEmit` → Exit 0
- `cd server && npx vitest run` → ≥ 487 grün, genau 3 rot mit den drei bekannten Titeln
- `cd desktop && npm run check:rules` → Exit 0
- `14-REQ32-NACHWEIS.md` enthält fünf Fälle, jeder mit Datei, Titel, Zeilennummer und Einzellaufprotokoll
</verification>

<success_criteria>
REQ-32 ist einzeln nachgewiesen: fünf benannte Fälle, fünf Testfundstellen, fünf protokollierte
Einzelläufe. Die eine echte Lücke („Erhöhung mit rückwirkendem Stichtag") ist geschlossen. Die
drei Gates stehen unverändert.
</success_criteria>

<output>
Create `.planning/phases/14-absicherung-und-auslieferung/14-01-SUMMARY.md` when done
</output>
