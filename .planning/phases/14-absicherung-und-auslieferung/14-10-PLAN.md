---
phase: 14-absicherung-und-auslieferung
plan: 10
type: execute
wave: 7
depends_on: ["14-09"]
files_modified:
  - .planning/phases/14-absicherung-und-auslieferung/14-BACKFILL-PRODUKTION.md
  - .planning/phases/14-absicherung-und-auslieferung/14-UAT-SAMMLUNG.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [REQ-33]

must_haves:
  truths:
    - "Der Journal-Backfill der Phase 9.1 läuft als eigener, gesondert freigegebener Schritt nach der Verifikation der Phase 14"
    - "Vor dem Backfill liegt eine zweite, geprüfte Sicherung vor"
    - "Nach dem Backfill endet kein vollständig vergangener Monat mehr vor seinem letzten Solltag"
    - "Die Modellwechsel-Buchungen sind nach dem Backfill unverändert vorhanden"
  artifacts:
    - path: ".planning/phases/14-absicherung-und-auslieferung/14-BACKFILL-PRODUKTION.md"
      provides: "Freigabe, Sicherung, Fundliste, Schreiblauf und Verifikation des Journal-Backfills auf der Produktion"
      contains: "production.PRE-14-10"
  key_links:
    - from: "14-BACKFILL-PRODUKTION.md"
      to: "14-URTEIL-PHASE-9.1.md"
      via: "setzt Teil 1 und Teil 2 des Urteils um"
      pattern: "14-URTEIL-PHASE-9"
---

<objective>
Der zweite Datenlauf im selben Produktionsfenster: der Journal-Backfill aus Phase 9.1 — nach
der abgeschlossenen Verifikation des Phase-14-Laufs, mit eigener Sicherung und eigener
Freigabe.

Purpose: Teil 1 und Teil 2 des Urteils aus Plan 14-07 umsetzen. Ein zweites Fenster hieße
zweite Sicherung, zweites Risikofenster, zweite Freigabe für denselben Datenbestand.
Output: `14-BACKFILL-PRODUKTION.md` und der aktualisierte Status der Phase 9.1 in der ROADMAP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-absicherung-und-auslieferung/14-URTEIL-PHASE-9.1.md
@.planning/phases/14-absicherung-und-auslieferung/14-UMSTELLUNGSFALL.md
@.planning/phases/14-absicherung-und-auslieferung/14-ROLLBACK-RUNBOOK.md

<warum_erst_jetzt>
Aus Teil 2 des Urteils (Plan 14-07), am Code belegt: Der Backfill schreibt ausschließlich in
`overtime_transactions`. `getOvertimeBalanceAtDate()` summiert genau diese Tabelle, und der
Kontoauszug listet genau diese Zeilen. Beides ist der Gegenstand der Verifikation aus Plan
14-09. Liefe der Backfill vorher oder gleichzeitig, wäre eine Überraschung im Kontoauszug
nicht mehr eindeutig einer Ursache zuzuordnen — Modellwechsel oder Backfill.

Deshalb: erst nachdem Plan 14-09 abgeschlossen und verifiziert ist.

Was der Backfill **nicht** bewegt (ebenfalls in Plan 14-07 belegt und in dessen Probelauf mit
einem leeren `diff` gemessen): die von `snapshotBalances` gemessene Größe und
`getOvertimeBalance()`. Der D5-Vergleich aus Plan 14-09 bleibt gültig.
</warum_erst_jetzt>

<werkzeug>
`server/src/scripts/backfillOvertimeJournal.ts` aus Plan 14-07, dort auf einer
Produktionskopie geprobt. Aufrufform gegen die Produktion:
`DATABASE_PATH=/home/ubuntu/databases/production.db NODE_ENV=production npx tsx src/scripts/backfillOvertimeJournal.ts --allow-production [--maxMonths=<n>] [--apply]`

Eigenschaften, die hier tragen:
- Ohne `--apply` nur Fundliste, kein Schreibzugriff (in Plan 14-07 zahlenmäßig belegt)
- Zählt `model_change`-Zeilen vor und nach dem Lauf und bricht bei Abweichung mit Exit 1 ab
- Transaktional ist der **Monat**, nicht der Lauf: bei Abbruch bleiben verarbeitete Monate
  geschrieben; das Werkzeug gibt „X von Y Monaten verarbeitet" aus
- `--maxMonths` begrenzt den Lauf — der Sicherheitsgurt für den ersten Produktionslauf

Vorbedingung: Das Skript muss auf dem Server liegen. Es entsteht in Plan 14-07 und erreicht
den Server mit dem Push aus Plan 14-08. Vor dem Aufruf per SSH prüfen, dass
`/home/ubuntu/TimeTracking-Clean/server/src/scripts/backfillOvertimeJournal.ts` existiert.
</werkzeug>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Freigabe für den zweiten Datenlauf einholen</name>
  <read_first>
    - .planning/phases/14-absicherung-und-auslieferung/14-URTEIL-PHASE-9.1.md (vollständig, insbesondere der Probelauf und der leere Diff)
    - .planning/phases/14-absicherung-und-auslieferung/14-UMSTELLUNGSFALL.md (vollständig — die abgeschlossene Verifikation der Phase 14)
    - .planning/phases/14-absicherung-und-auslieferung/14-ROLLBACK-RUNBOOK.md (Abschnitte 1 bis 3)
  </read_first>
  <decision>Läuft der Journal-Backfill der Phase 9.1 jetzt im selben Fenster, oder wird er auf ein eigenes Fenster verschoben?</decision>
  <context>
Lege dem Anwender vor:

1. **Was der Backfill tut**: Für jeden Nutzer und jeden vollständig vergangenen Monat, dessen
   gespeicherte `earned`-Zeilen vor dem letzten Solltag enden, wird der Monat über
   `rebuildOvertimeTransactionsForMonth` neu aufgebaut. Es entsteht keine neue Zeitbuchung —
   es werden fehlende Journaltage nachgetragen.
2. **Was sich dadurch sichtbar ändert**: der Kontoauszug betroffener Mitarbeiter und der Wert
   von `getOvertimeBalanceAtDate`. **Nicht** der Saldo, den Dashboard und Berichte zeigen —
   der wird live berechnet (belegt in `14-URTEIL-PHASE-9.1.md`).
3. **Der Umfang, jetzt gemessen**: Fahre vor dem Checkpoint den Trockenlauf gegen die lokale
   Kopie `server/database/14-prod-nach-umstellung.db` aus Plan 14-09 und lege die Zahl der
   betroffenen Nutzer und Monate vor. Nicht die Zahlen aus dem Probelauf der Plan-14-07-Kopie
   zitieren — der Bestand hat sich seither durch das Deployment und die Umstellung verändert.
4. **Der Rückweg**: eine zweite Sicherung vor dem Lauf; Rückweg A des Runbooks reicht, weil
   der Backfill nur Daten und keinen Code ändert.
5. **Die Alternative**: eigenes Fenster später — dann eine zweite Sicherung, ein zweites
   Risikofenster und eine zweite Freigabe für denselben Bestand, und Phase 9.1 bleibt bis
   dahin mit einem offenen Datenlauf stehen.
6. **Empfehlung des Planners**: jetzt, mit `--maxMonths` als Sicherheitsgurt beim ersten Lauf,
   und danach ein zweiter Lauf ohne Begrenzung, falls die Fundliste größer war als das Limit.
  </context>
  <options>
    <option id="jetzt">
      <name>Backfill jetzt im selben Fenster fahren</name>
      <pros>Eine Sicherung, ein Risikofenster, eine Freigabe; Phase 9.1 verliert ihren offenen Datenlauf</pros>
      <cons>Zweiter Schreibzugriff auf echte Daten am selben Abend</cons>
    </option>
    <option id="spaeter">
      <name>Auf ein eigenes Fenster verschieben</name>
      <pros>Der Phase-14-Lauf steht für sich; kein zweiter Schreibzugriff heute</pros>
      <cons>Zweite Sicherung, zweites Risikofenster, zweite Freigabe; Phase 9.1 bleibt offen</cons>
    </option>
    <option id="begrenzt">
      <name>Jetzt, aber begrenzt auf eine genannte Zahl Monate</name>
      <pros>Wirkung und Laufzeit lassen sich an einem kleinen Ausschnitt messen, bevor der Rest folgt</pros>
      <cons>Der Bestand steht danach zwischen altem und neuem Stand, bis der zweite Lauf folgt</cons>
    </option>
  </options>
  <files>.planning/phases/14-absicherung-und-auslieferung/14-BACKFILL-PRODUKTION.md</files>
  <action>
Lege `.planning/phases/14-absicherung-und-auslieferung/14-BACKFILL-PRODUKTION.md` an. Fahre den
Trockenlauf gegen die lokale Kopie
`DATABASE_PATH=./database/14-prod-nach-umstellung.db npm run backfill:overtime-journal`,
trage Befehl und Fundliste ein, lege dem Anwender die sechs Punkte aus dem
`<context>`-Block vor und halte seine Antwort samt Zeitpunkt fest. Kein Befehl gegen die
Produktionsdatenbank in diesem Task.
  </action>
  <verify>
    <automated>cd server &amp;&amp; DATABASE_PATH=./database/14-prod-nach-umstellung.db npm run backfill:overtime-journal</automated>
  </verify>
  <done>Der gemessene Umfang und die Freigabeentscheidung stehen in `14-BACKFILL-PRODUKTION.md`; bei `spaeter` ist kein Befehl gegen die Produktion gelaufen.</done>
  <acceptance_criteria>
    - Der Trockenlauf gegen `server/database/14-prod-nach-umstellung.db` ist vor dem Checkpoint gelaufen; Zahl der betroffenen Nutzer und Monate steht mit dem ausgeführten Befehl in `14-BACKFILL-PRODUKTION.md`.
    - Die Freigabeentscheidung samt Wortlaut und Zeitpunkt ist protokolliert.
    - Bei `begrenzt`: die genannte Monatszahl ist protokolliert und wird in Task 2 als `--maxMonths` übernommen.
    - Bei `spaeter`: der Plan endet, ohne dass ein Befehl gegen die Produktion gelaufen ist, und der ROADMAP-Status der Phase 9.1 wird in Task 3 entsprechend gesetzt.
  </acceptance_criteria>
  <resume-signal>Antworte mit `jetzt`, `begrenzt: <Anzahl Monate>` oder `spaeter: <Grund>`</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Zweite Sicherung ziehen, Trockenlauf gegen die Produktion, dann Schreiblauf</name>
  <files>.planning/phases/14-absicherung-und-auslieferung/14-BACKFILL-PRODUKTION.md</files>
  <read_first>
    - .planning/phases/14-absicherung-und-auslieferung/14-ROLLBACK-RUNBOOK.md (Abschnitt 1 — der Sicherungsbefehl, hier mit dem Namensteil `PRE-14-10`)
    - server/src/scripts/backfillOvertimeJournal.ts (Ausgabeform der Fundliste und die `model_change`-Prüfung)
    - .planning/phases/14-absicherung-und-auslieferung/14-URTEIL-PHASE-9.1.md (Abschnitt „Probelauf" — die dort gemessene Laufzeit als Erwartungswert)
    - .planning/phases/14-absicherung-und-auslieferung/14-BACKFILL-PRODUKTION.md (Stand nach Task 1)
  </read_first>
  <action>
**Schritt A — Zweite Sicherung.** Derselbe Befehl wie in Plan 14-08 Schritt A, mit dem
Namensmuster `/home/ubuntu/databases/backups/production.PRE-14-10_<YYYYMMDD>_<HHMMSS>.db`,
per `VACUUM INTO` über das serverseitige `better-sqlite3`. Prüfen wie dort: Dateigröße > 0,
`integrity_check` = `ok`, `foreign_key_check` leer, Rohkennzahlensatz erheben, per `scp` auf
den Arbeitsrechner holen. Die Sicherung aus Plan 14-08 wird **nicht** überschrieben und
**nicht** gelöscht — sie ist der Rückweg hinter den Modellwechsel, diese hier nur der Rückweg
hinter den Backfill.

**Schritt B — Skript auf dem Server prüfen.** Per SSH die Existenz von
`/home/ubuntu/TimeTracking-Clean/server/src/scripts/backfillOvertimeJournal.ts` belegen. Fehlt
sie, ist das Deployment aus Plan 14-08 unvollständig; der Plan endet hier.

**Schritt C — Trockenlauf gegen die echte Produktion (D3).**
`DATABASE_PATH=/home/ubuntu/databases/production.db NODE_ENV=production npx tsx src/scripts/backfillOvertimeJournal.ts --allow-production`
Erwartet: erste Zeile mit dem aufgelösten Pfad, Warnzeile wegen `--allow-production`,
Modusbanner `TROCKENLAUF`, vollständige Fundliste, Exit 0. Ausgabe wörtlich ins Protokoll.

Belege, dass nichts geschrieben wurde: `SELECT COUNT(*) FROM overtime_transactions` und
`SELECT ROUND(SUM(hours),6) FROM overtime_transactions` readonly vor und nach dem Trockenlauf,
beide Werte identisch.

Vergleiche die Fundliste mit der aus Task 1 (gegen die lokale Kopie erhobene). Weicht sie
deutlich ab, ist das zu erklären — die Kopie ist älter als die Produktion, kleine Abweichungen
durch zwischenzeitliche Zeiteinträge sind erwartbar, große nicht.

**Schritt D — Schreiblauf.** Derselbe Befehl mit `--apply`, bei der Option `begrenzt`
zusätzlich `--maxMonths=<vom Anwender genannte Zahl>`.
Erwartet: Modusbanner `MODUS: SCHREIBEN`, je Monat eine Fortschrittszeile, am Ende die
Differenz in `COUNT(*)` und `SUM(hours)`, unveränderte Zahl der `model_change`-Zeilen,
`integrity_check` = `ok`, Exit 0.

Bricht der Lauf ab: die Zeile „X von Y Monaten verarbeitet" wörtlich festhalten. Der Bestand
steht dann zwischen altem und neuem Stand — das ist bei diesem Werkzeug verkraftbar, weil ein
erneuter Lauf dieselben Monate neu aufbaut, aber es ist auszusprechen und dem Anwender zu
melden, bevor irgendetwas anderes passiert.

**Schritt E — Health-Check.** `curl -s http://129.159.8.19:3000/api/health` — `status: ok`.
Der Backfill läuft gegen dieselbe Datei wie der Serverprozess; ein Fehler hier wäre ein
Sperrkonflikt und gehört sofort erkannt.
  </action>
  <verify>
    <automated>curl -s http://129.159.8.19:3000/api/health</automated>
  </verify>
  <acceptance_criteria>
    - `/home/ubuntu/databases/backups/production.PRE-14-10_*.db` existiert mit Größe > 0 und `integrity_check` = `ok`; dieselbe Datei liegt zusätzlich unter `server/database/backups/`.
    - Die Sicherung `production.PRE-14-08_*.db` aus Plan 14-08 existiert unverändert weiter.
    - Die Existenz von `backfillOvertimeJournal.ts` auf dem Server ist per SSH belegt.
    - Der Trockenlauf endet mit Exit 0; `COUNT(*)` und `ROUND(SUM(hours),6)` von `overtime_transactions` sind davor und danach identisch, beide Werte im Protokoll.
    - Der Schreiblauf endet mit Exit 0, `integrity_check` = `ok`, und die Zahl der `model_change`-Zeilen ist vor und nach dem Lauf gleich.
    - `curl -s http://129.159.8.19:3000/api/health` liefert `status` = `ok`.
  </acceptance_criteria>
  <done>Der Backfill ist mit eigener Sicherung, gesichteter Fundliste und intakten Modellwechsel-Buchungen gefahren.</done>
</task>

<task type="auto">
  <name>Task 3: Wirkung des Backfills belegen und Phase 9.1 in der ROADMAP nachziehen</name>
  <files>.planning/phases/14-absicherung-und-auslieferung/14-BACKFILL-PRODUKTION.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/14-absicherung-und-auslieferung/14-BACKFILL-PRODUKTION.md (Stand nach Task 2)
    - .planning/phases/14-absicherung-und-auslieferung/14-URTEIL-PHASE-9.1.md (Teil 3 — was in Phase 9.1 offen bleibt)
    - .planning/ROADMAP.md (Abschnitt Phase 9.1 und ihre Zeile in der Fortschrittstabelle)
    - .planning/phases/14-absicherung-und-auslieferung/14-UMSTELLUNGSFALL.md (der `--asOf`-Wert, der auch hier gilt)
  </read_first>
  <action>
**Schritt A — Erneuter Trockenlauf als Wirkungsnachweis.** Denselben Befehl wie in Task 2
Schritt C erneut fahren. Erwartet: **leere Fundliste** — kein Nutzer, kein Monat mehr
unvollständig. War in Task 1 die Option `begrenzt` gewählt, ist die Restliste erwartungsgemäß
nicht leer; dann ist die verbleibende Zahl zu beziffern und der zweite Lauf ausdrücklich als
offener Punkt festzuhalten.

**Schritt B — Der D5-Vergleich bleibt gültig.** Ziehe eine frische Produktionskopie nach dem
Muster aus Plan 14-09 Schritt A (eigene Sicherung von `development.db`, `sync-dev-db`,
`VACUUM INTO './database/14-prod-nach-backfill.db'`, Wiederherstellung mit Nachweis) und
erhebe einen Snapshot mit **demselben** `--asOf`-Wert wie in Plan 14-08 und 14-09:
`--json=../.planning/phases/14-absicherung-und-auslieferung/14-SNAPSHOT-PROD-NACH-BACKFILL.json`

Diff gegen `14-SNAPSHOT-PROD-NACHHER.users.json` aus Plan 14-09. Verbindliche Erwartung:
**kein Unterschied**. Das ist der Produktionsbeleg für Teil 1 des Urteils aus Plan 14-07. Zeigt
sich ein Unterschied, ist das ein Befund, der das Urteil widerlegt: wörtlich festhalten, dem
Anwender melden, Abschnitt 2 des Runbooks prüfen.

**Schritt C — Gegenprobe, dass der Backfill gewirkt hat.** Für einen Nutzer aus der Fundliste
von Task 2 `getOvertimeBalanceAtDate(userId, <letzter Tag eines betroffenen Monats>)` vor und
nach dem Lauf gegenüberstellen — hier muss sich ein Unterschied zeigen. Den Vorherwert aus
`14-prod-nach-umstellung.db` (Plan 14-09) lesen, den Nachherwert aus
`14-prod-nach-backfill.db`. Ohne diese Gegenprobe wäre ein leerer Diff in Schritt B auch mit
einem wirkungslosen Lauf vereinbar.

**Schritt D — Kontoauszug ansehen.** Für denselben Nutzer den Kontoauszug am laufenden
Produktionsserver abrufen und belegen, dass die zuvor fehlenden Tage am Monatsende jetzt als
Zeilen erscheinen. Die betreffenden Zeilen (gekürzt) ins Protokoll.

**Schritt E — Phase 9.1 in der ROADMAP nachziehen.** Im Abschnitt `### Phase 9.1` einen
Absatz „Stand nach Plan 14-10" ergänzen: der Backfill ist auf der Produktion gelaufen (Datum,
Zahl der verarbeiteten Nutzer und Monate, Verweis auf `14-BACKFILL-PRODUKTION.md`), offen
bleibt die Code-Härtung WR-02 bis WR-05. Die Zeile in der Fortschrittstabelle entsprechend auf
`Backfill erledigt (Plan 14-10); offen: Code-Härtung WR-02–WR-05` setzen. Keine anderen Zeilen
der ROADMAP anfassen.

**Schritt F — UAT-Sammlung ergänzen.** Unter `## Phase 14 — Absicherung und Auslieferung`
einen Punkt `14-U5` aufnehmen: Kontoauszug eines vom Backfill betroffenen Mitarbeiters
ansehen — die Monatsenden sind vollständig, und es sind keine doppelten Tageszeilen
entstanden. Prüfweg A. Nur diesen Punkt ergänzen.
  </action>
  <verify>
    <automated>cd server &amp;&amp; DATABASE_PATH=./database/14-prod-nach-backfill.db npm run check:period-chains</automated>
  </verify>
  <acceptance_criteria>
    - Der erneute Trockenlauf liefert eine leere Fundliste — oder, bei der Option `begrenzt`, eine bezifferte Restliste mit ausdrücklich festgehaltenem offenem zweiten Lauf.
    - `14-SNAPSHOT-PROD-NACH-BACKFILL.json` und die zugehörige `*.users.json` existieren, erhoben mit demselben `--asOf`-Wert wie in Plan 14-08 und 14-09; der Wert steht dreifach nebeneinander im Protokoll.
    - Der `diff` gegen `14-SNAPSHOT-PROD-NACHHER.users.json` liefert **keine** Ausgabe; das Ergebnis steht wörtlich im Protokoll.
    - Die Gegenprobe aus Schritt C zeigt für mindestens einen Nutzer einen Unterschied in `getOvertimeBalanceAtDate`; Nutzer, Datum, Wert vorher und nachher stehen im Protokoll.
    - Der Kontoauszug-Auszug aus Schritt D belegt die nachgetragenen Monatsendtage.
    - `DATABASE_PATH=./database/14-prod-nach-backfill.db npm run check:period-chains` endet mit Exitcode 0.
    - `.planning/ROADMAP.md` trägt im Abschnitt und in der Tabellenzeile der Phase 9.1 den neuen Stand; `git diff --stat .planning/ROADMAP.md` zeigt Änderungen nur dort.
    - `14-UAT-SAMMLUNG.md` trägt den neuen Punkt `14-U5`; `git diff` zeigt für diese Datei nur Änderungen im Phase-14-Abschnitt.
    - `server/database/development.db` steht nach Schritt B wieder im Zustand von vorher; `cd server && npx vitest run` meldet weiterhin `3 failed` mit den bekannten Titeln.
  </acceptance_criteria>
  <done>Der Backfill hat gewirkt, den D5-Vergleich nicht verschoben, und Phase 9.1 ist auf ihren tatsächlichen Reststand gesetzt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backfill → `overtime_transactions` der Produktion | Massenschreibzugriff über alle Nutzer und alle vergangenen Monate echter Mitarbeiterdaten |
| Backfill-Prozess → Serverprozess | Beide halten gleichzeitig eine Verbindung auf dieselbe Datei; das Projekt kennt kein `busy_timeout`-Pragma (WR-03, offen in Phase 9.1) |
| Zweiter Datenlauf → Verifikation des ersten | Ein gleichzeitiger Lauf machte Ursachen nicht mehr unterscheidbar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-58 | Tampering | `model_change`-Zeilen | mitigate | Das Werkzeug zählt sie vor und nach dem Lauf und bricht bei Abweichung mit Exit 1 ab; die Gleichheit ist eigenes Akzeptanzkriterium. |
| T-14-59 | Tampering | Teilzustand bei Abbruch | mitigate | Transaktional ist der Monat; das Werkzeug gibt „X von Y Monaten verarbeitet" aus, `--maxMonths` begrenzt den ersten Lauf, ein erneuter Lauf baut dieselben Monate neu auf. Der erneute Trockenlauf in Task 3 macht einen Teilzustand sichtbar. |
| T-14-60 | Denial of Service | `SQLITE_BUSY` durch fehlendes `busy_timeout` | accept | Das Pragma fehlt projektweit (WR-03, bewusst in Phase 9.1 belassen, siehe Teil 3 des Urteils). Gegenmaßnahme hier: Health-Check unmittelbar nach dem Lauf, Empfehlung eines Fensters außerhalb der Arbeitszeit, und der Rückweg über die Sicherung `production.PRE-14-10_*`. Ein `busy_timeout` nachzurüsten wäre Codearbeit mitten im offenen Fenster — genau das, was Teil 3 des Urteils ausschließt. |
| T-14-61 | Denial of Service | unbrauchbare zweite Sicherung | mitigate | `VACUUM INTO` statt `cp`, Prüfung über `integrity_check`, `foreign_key_check` und Kennzahlensatz vor dem Lauf, zweite Kopie auf dem Arbeitsrechner; die Sicherung aus Plan 14-08 bleibt zusätzlich erhalten. |
| T-14-62 | Tampering | leere Datenbank durch fehlendes `DATABASE_PATH` | mitigate | Jeder Aufruf trägt `DATABASE_PATH=/home/ubuntu/databases/production.db` und `NODE_ENV=production`; das Werkzeug bricht ohne die Variable mit Exit 2 ab. |
| T-14-63 | Repudiation | „der Backfill hat nichts kaputtgemacht" ohne Beleg | mitigate | Drei getrennte Belege: leerer `diff` gegen den Snapshot aus Plan 14-09, Gegenprobe über `getOvertimeBalanceAtDate` mit nachgewiesener Wirkung, und ein erneuter Trockenlauf mit leerer Fundliste. |
| T-14-64 | Tampering | lokale Arbeitsdatenbank durch `sync-dev-db` | mitigate | Eigene `VACUUM INTO`-Sicherung vor dem Sync, Wiederherstellung als Pflichtschritt, belegt durch ein erneutes `vitest run`. |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | Dieser Plan installiert kein Paket. Entfällt mangels Installationsschritt. |
</threat_model>

<verification>
- Zweite Sicherung `production.PRE-14-10_*.db` geprüft und doppelt vorhanden; die aus 14-08 unverändert
- Trockenlauf ohne Schreibwirkung, Schreiblauf Exit 0, `model_change`-Zahl unverändert, `integrity_check` = ok
- Erneuter Trockenlauf: leere Fundliste (oder bezifferte Restliste bei `begrenzt`)
- `diff` gegen den Snapshot aus Plan 14-09: leer; Gegenprobe zeigt Wirkung
- ROADMAP-Status der Phase 9.1 auf den tatsächlichen Reststand gesetzt
</verification>

<success_criteria>
Der Journal-Backfill der Phase 9.1 ist auf der Produktion gelaufen, hat nachweislich gewirkt
und den Vorher/Nachher-Vergleich der Phase 14 nicht verschoben. Phase 9.1 trägt in der ROADMAP
nur noch ihre Code-Härtung als offenen Punkt.
</success_criteria>

<output>
Create `.planning/phases/14-absicherung-und-auslieferung/14-10-SUMMARY.md` when done
</output>
