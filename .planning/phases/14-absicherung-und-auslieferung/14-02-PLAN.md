---
phase: 14-absicherung-und-auslieferung
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/services/userService.ts
  - server/src/services/userWorkPeriodProvisioning.test.ts
  - PROJECT_STATUS.md
  - .planning/phases/14-absicherung-und-auslieferung/14-WR07-ENTSCHEIDUNG.md
autonomous: true
requirements: [REQ-33]

must_haves:
  truths:
    - "Ein Aufruf von PUT /api/users/:id mit geänderten weeklyHours oder geändertem workSchedule wird abgewiesen und schreibt nichts"
    - "Ein Aufruf von PUT /api/users/:id mit unveränderten weeklyHours (der Weg, den EditUserModal geht) funktioniert weiter"
    - "Der einzige Weg, Wochenstunden zu ändern, führt über POST /api/work-periods/change oder PUT /api/work-periods/:id — mit Token, Begründung, Rebuild und Journalzeile"
  artifacts:
    - path: "server/src/services/userService.ts"
      provides: "Abweisung statt Spiegelung im updateUser()-Zweig für weeklyHours/workSchedule"
      contains: "WorkPeriodBypassError"
    - path: "server/src/services/userWorkPeriodProvisioning.test.ts"
      provides: "Umgekehrte Erwartung: die früher gespiegelten Fälle werfen jetzt, die unveränderten Fälle laufen durch"
      contains: "WorkPeriodBypassError"
    - path: ".planning/phases/14-absicherung-und-auslieferung/14-WR07-ENTSCHEIDUNG.md"
      provides: "Begründete Entscheidung, warum WR-07 vor dem Produktionslauf geschlossen wird"
      contains: "vor dem Produktionslauf"
  key_links:
    - from: "server/src/services/userService.ts"
      to: "server/src/routes/users.ts"
      via: "Fehlertyp wird auf HTTP 400 abgebildet"
      pattern: "WorkPeriodBypassError"
    - from: "PROJECT_STATUS.md"
      to: "14-WR07-ENTSCHEIDUNG.md"
      via: "Restposten von OFFEN auf GESCHLOSSEN mit Verweis"
      pattern: "WR-07"
---

<objective>
WR-07 schließen: `PUT /api/users/:id` spiegelt geänderte `weeklyHours`/`workSchedule`
weiterhin ohne Vorschau-Token, ohne Pflichtbegründung, ohne Rebuild und ohne Journalzeile in
die offene Arbeitszeitperiode. Dieser zweite Schreibweg wird **vor** dem Produktionslauf
entfernt.

Purpose: Der Milestone verspricht, dass eine Stundenumstellung die Vergangenheit nicht still
verschiebt. Ein Schreibweg an der Perioden-Historie vorbei hebelt genau dieses Versprechen
aus — und zwar unbemerkt, weil er weder Journalzeile noch `audit_log`-Eintrag hinterlässt.
Output: `updateUser()` weist den Bypass ab, die 17 Tests der Provisioning-Datei bilden das
neue Verhalten ab, `PROJECT_STATUS.md` führt den Restposten als geschlossen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-absicherung-und-auslieferung/14-CONTEXT.md
@.planning/phases/14-absicherung-und-auslieferung/14-UAT-SAMMLUNG.md

<entscheidung_wr07>
## Entscheidung des Planners: WR-07 wird VOR dem Produktionslauf geschlossen

**Abgewogen wurde gegen die Alternative „geht als bekannter Restposten mit".**

Für das Schließen:
1. Es ist ein Schreibweg an der Perioden-Historie vorbei. Ändert ein Admin nach dem
   Produktionslauf über die normale Nutzerverwaltung die Wochenstunden, entsteht exakt der
   stille Zustand, den dieser Milestone beseitigt: `user_work_periods` trägt neue Werte, es
   gibt keine `model_change`-Buchung, keinen `audit_log`-Eintrag und keine Begründung. Der
   Kontoauszug zeigt eine Saldoänderung ohne Ursache.
2. Der gesamte Milestone geht in **einer** Auslieferung auf die Produktion (siehe
   Plan 14-08: `origin/main` steht auf `0f2a03e`, lokal liegen 337 unveröffentlichte
   Commits). Den Bypass jetzt zu schließen kostet keine zusätzliche Auslieferung. Ihn
   mitzunehmen bedeutet, ihn später in einem eigenen Deployment nachzuziehen.
3. Der Umfang ist begrenzt und vollständig bekannt: der Spiegelungszweig in
   `userService.updateUser()` und fünf der 17 Tests in `userWorkPeriodProvisioning.test.ts`.
4. Das Risiko der Änderung ist beherrschbar, weil die eigene Oberfläche den Weg nicht mehr
   nutzt: `EditUserModal.handleSubmit` schickt `weeklyHours` **unverändert** mit. Genau
   deshalb darf die Abweisung nur bei einer **Wertänderung** greifen, nicht bei bloßer
   Anwesenheit des Feldes — sonst bricht jedes Speichern von Stammdaten.

Gegen das Schließen sprach nur, dass es eine Vertragsänderung an `updateUser()` ist. Das ist
ein Umfangs-, kein Risikoargument, und der Umfang ist gemessen (siehe Punkt 3).

**Betriebliche Zwischenmaßnahme entfällt**, weil der Weg geschlossen wird und nicht mitgeht.
</entscheidung_wr07>

<interfaces>
Gemessen an HEAD 207dab9 — `server/src/services/userService.ts`:

- Zeile 589-594: `weeklyHoursChanged` / `workScheduleChanged` / `mustMirrorPeriod` — das ist
  die Bedingung, die künftig abweist statt spiegelt.
- Zeile 599-601: `mirroredWeeklyHours` / `mirroredWorkSchedule` — entfallen mit dem Zweig.
- Innerhalb von `applyUpdate = db.transaction(...)`: der Block `if (!mustMirrorPeriod) return;`
  gefolgt von `getCurrentWorkPeriod` / `ensureInitialWorkPeriod` /
  `updateWorkPeriodValues(currentPeriod.id, ...)` / `DELETE FROM overtime_balance WHERE userId = ?`
  — genau dieser Block entfällt.
- Der `hireDateChanged`-Zweig mit `syncStartPeriodToHireDate()` und seinem eigenen
  `DELETE FROM overtime_balance` bleibt **unverändert** — er ist nicht Teil von WR-07.
- Nach `applyUpdate()` steht ein weiterer Block „If weeklyHours changed, recalculate all
  overtime_balance entries" (ab Zeile ~695). Auch dieser wird unerreichbar und entfällt.
- Import Zeile 8: `updateWorkPeriodValues` wird von `userService.ts` nach dem Umbau nicht mehr
  gebraucht; die Funktion selbst bleibt in `workPeriodService.ts` (Seed- und Wartungsskripte
  nutzen sie).

`server/src/services/userWorkPeriodProvisioning.test.ts` — 17 `it`-Blöcke, davon betroffen:
- Zeile 170 `weeklyHours-Änderung spiegelt in users UND in die offene Periode, validFrom bleibt`
- Zeile ~188 `workSchedule`-Spiegelung (Analog)
- Zeile 210 `ein Update ohne weeklyHours/workSchedule rührt die Periode nicht an` — bleibt gültig
- Zeile 228 `mirror-noop` (unveränderter Wert) — bleibt gültig, prüft künftig zusätzlich, dass NICHT geworfen wird
- Zeile ~248 `updateUser(userId, { weeklyHours: 25 })` erwartet Spiegelung
- Zeile 261 `mirror-atomic` mit `weeklyHours: 55` — prüft heute die 0..80-Validierung; bleibt gültig
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entscheidung festhalten und den Spiegelungszweig in updateUser() durch eine Abweisung ersetzen</name>
  <files>server/src/services/userService.ts, .planning/phases/14-absicherung-und-auslieferung/14-WR07-ENTSCHEIDUNG.md</files>
  <read_first>
    - server/src/services/userService.ts (Zeilen 450-720 vollständig — `updateUser()` mit Validierung, `applyUpdate`-Transaktion, hireDate-Zweig, Spiegelungszweig und Nachlaufblock)
    - server/src/routes/users.ts (der `PUT /:id`-Handler: wie werden Service-Fehler auf HTTP-Status abgebildet?)
    - server/src/services/workPeriodChangeService.ts (Definition und Verwendung von `WorkTimeChangeValidationError` — das Muster für einen typisierten, auf 400 abbildbaren Servicefehler)
    - server/src/types/index.ts (Definition von `UpdateUserInput`)
    - PROJECT_STATUS.md (Abschnitt „Offene Restposten")
  </read_first>
  <action>
Schreibe zuerst `.planning/phases/14-absicherung-und-auslieferung/14-WR07-ENTSCHEIDUNG.md`
mit dem Entscheidungstext aus dem Kontextblock `entscheidung_wr07` dieses Plans, ergänzt um
die tatsächlich vorgefundenen Zeilennummern und den Wortlaut des entfernten Blocks vor der
Änderung. Diese Datei ist der Nachweis, dass die Entscheidung getroffen und nicht übergangen
wurde.

Baue dann `server/src/services/userService.ts` um:

1. Exportiere eine neue Fehlerklasse `WorkPeriodBypassError extends Error` in `userService.ts`,
   nach dem Muster von `WorkTimeChangeValidationError` in `workPeriodChangeService.ts` (Feld
   `name` explizit setzen, damit `instanceof` über Modulgrenzen zuverlässig bleibt). Meldung
   wörtlich: `Wochenstunden und Tagesplan werden nicht mehr ueber PUT /api/users/:id geaendert. Nutzen Sie POST /api/work-periods/change (Stundenwechsel ab Stichtag) oder PUT /api/work-periods/:id (Stammdaten korrigieren).`
2. Berechne `weeklyHoursChanged` und `workScheduleChanged` **unverändert** wie heute
   (Vergleich gegen `existingUser`, nicht bloße Anwesenheit des Feldes). Wirf
   `new WorkPeriodBypassError(...)`, sobald einer der beiden `true` ist — **vor** dem Aufbau
   des SQL-`UPDATE` und **vor** `applyUpdate()`, damit nachweislich nichts geschrieben wird.
3. Entferne aus dem `UPDATE users`-Aufbau die beiden Zweige `if (data.weeklyHours !== undefined)`
   und `if (data.workSchedule !== undefined)`, die `weeklyHours = ?` bzw. `workSchedule = ?`
   in `updates`/`values` schieben. Nach Schritt 2 sind sie nur noch für unveränderte Werte
   erreichbar und würden dann denselben Wert erneut schreiben — ersatzloser Wegfall.
4. Entferne aus `applyUpdate()` den kompletten Spiegelungsblock ab `if (!mustMirrorPeriod) return;`
   bis einschließlich des zugehörigen `DELETE FROM overtime_balance WHERE userId = ?`.
   Der `hireDateChanged`-Zweig mit `syncStartPeriodToHireDate()` und seinem eigenen
   `DELETE FROM overtime_balance` bleibt **unangetastet**.
5. Entferne den nachgelagerten Block „If weeklyHours changed, recalculate all overtime_balance
   entries" hinter `applyUpdate()` — nach Schritt 2 unerreichbar.
6. Entferne `updateWorkPeriodValues` aus der Importzeile von `userService.ts`, falls dort
   danach kein Aufrufer mehr steht. Die Funktion selbst bleibt in `workPeriodService.ts`.
7. Ersetze den langen WR-07-Kommentarblock (die „RICHTIGSTELLUNG EINER FALSCHEN ZUSAGE") durch
   eine kurze Begründung des neuen Verhaltens mit Verweis auf `14-WR07-ENTSCHEIDUNG.md`.
   Keine Behauptung stehen lassen, die der Code nicht mehr einlöst.
8. Bilde `WorkPeriodBypassError` im `PUT /:id`-Handler in `server/src/routes/users.ts` auf
   **HTTP 400** mit dem Meldungstext im Antwortkörper ab — nach dem dort vorhandenen Muster
   für typisierte Servicefehler. Kein 500.
  </action>
  <verify>
    <automated>cd server &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class WorkPeriodBypassError" server/src/services/userService.ts` liefert genau einen Treffer und die Klasse ist `export`.
    - `grep -n "updateWorkPeriodValues" server/src/services/userService.ts` liefert keinen Treffer mehr.
    - `grep -cv '^\s*//' server/src/services/userService.ts | true; grep -n "mustMirrorPeriod" server/src/services/userService.ts` liefert keinen Treffer mehr.
    - `grep -n "WorkPeriodBypassError" server/src/routes/users.ts` liefert mindestens einen Treffer, und in dessen Umgebung steht `400`.
    - `cd server && npx tsc --noEmit` endet mit Exit 0.
    - `.planning/phases/14-absicherung-und-auslieferung/14-WR07-ENTSCHEIDUNG.md` existiert, nennt beide abgewogenen Optionen und begründet die gewählte mit mindestens den vier Punkten aus dem Kontextblock.
  </acceptance_criteria>
  <done>`updateUser()` weist eine Wertänderung an `weeklyHours`/`workSchedule` mit HTTP 400 ab; kein Schreibweg an der Perioden-Historie vorbei bleibt übrig.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Die 17 Tests der Provisioning-Datei auf das neue Verhalten umstellen</name>
  <files>server/src/services/userWorkPeriodProvisioning.test.ts</files>
  <read_first>
    - server/src/services/userWorkPeriodProvisioning.test.ts (vollständig — alle 17 `it`-Blöcke, die Helfer `baseUserInput` und `createRawTestUser`, das Aufräummuster)
    - server/src/services/userService.ts (Stand nach Task 1)
    - server/src/services/workPeriodChangeService.test.ts (Zeilen 696-800 — Muster, wie ein erwarteter Servicefehler samt „keine Schreibwirkung" geprüft wird)
  </read_first>
  <behavior>
    - `updateUser(id, { weeklyHours: <anderer Wert> })` wirft `WorkPeriodBypassError`; danach sind `users.weeklyHours` und die offene Periode unverändert und es existiert keine neue `overtime_transactions`-Zeile.
    - `updateUser(id, { workSchedule: <anderer Tagesplan> })` wirft `WorkPeriodBypassError` mit derselben Nachweisführung.
    - `updateUser(id, { weeklyHours: <gleicher Wert wie gespeichert> })` wirft **nicht** und lässt die Periode unangetastet — das ist der Weg, den `EditUserModal.handleSubmit` geht.
    - `updateUser(id, { firstName: 'X' })` ohne `weeklyHours`/`workSchedule` wirft nicht und rührt die Periode nicht an.
    - `updateUser(id, { hireDate: <anderes Datum> })` zieht die Startperiode weiterhin nach (`syncStartPeriodToHireDate`) — durch WR-07 unberührt.
    - Die Fehlermeldung nennt beide Ersatzwege wörtlich: `POST /api/work-periods/change` und `PUT /api/work-periods/:id`.
  </behavior>
  <action>
Stelle die betroffenen `it`-Blöcke in `server/src/services/userWorkPeriodProvisioning.test.ts`
von „spiegelt" auf „weist ab" um. Betroffen sind die Blöcke, die heute eine erfolgreiche
Spiegelung erwarten (`mirror-hours`, der `workSchedule`-Analogfall und der Fall mit
`updateUser(userId, { weeklyHours: 25 })`). Ersetze die Erfolgs-Erwartungen durch
`await expect(updateUser(...)).rejects.toThrow(WorkPeriodBypassError)` plus drei
Nachweiszeilen: `users.weeklyHours` unverändert, `getCurrentWorkPeriod(...)?.weeklyHours`
unverändert, Zeilenzahl in `overtime_transactions` für den Nutzer unverändert.

Benenne die Blöcke um, damit der Titel das neue Verhalten trägt, zum Beispiel
`WR-07: eine weeklyHours-Aenderung ueber updateUser wird abgewiesen, Periode und Saldo bleiben unangetastet`.

Erhalte die Blöcke, die weiterhin gelten, unverändert im Verhalten und ergänze sie um eine
ausdrückliche Gegenprobe: `mirror-noop` (unveränderter Wert) und
`ein Update ohne weeklyHours/workSchedule rührt die Periode nicht an` bekommen je eine Zeile,
die belegt, dass **nicht** geworfen wird. Der `hireDate`-Block und der 0..80-Validierungsblock
(`weeklyHours: 55`) bleiben inhaltlich unverändert — beim Validierungsblock prüfen, welcher
Fehler jetzt zuerst greift, und die Erwartung entsprechend anpassen statt sie zu raten.

Ergänze einen neuen Block, der die Meldung prüft: sie muss die Zeichenketten
`POST /api/work-periods/change` und `PUT /api/work-periods/:id` wörtlich enthalten. Ein
Fehlertext, der den Ersatzweg nicht nennt, ist für einen Admin an der API wertlos.

Das Aufräummuster der Datei nicht verändern — dieselben Präfixe, dasselbe `try/finally`.
  </action>
  <verify>
    <automated>cd server &amp;&amp; npx vitest run src/services/userWorkPeriodProvisioning.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && npx vitest run src/services/userWorkPeriodProvisioning.test.ts` meldet `0 failed` bei mindestens 18 Tests.
    - `grep -c "WorkPeriodBypassError" server/src/services/userWorkPeriodProvisioning.test.ts` liefert mindestens 4.
    - `grep -n "POST /api/work-periods/change" server/src/services/userWorkPeriodProvisioning.test.ts` liefert mindestens einen Treffer.
    - Kein `it`-Block der Datei erwartet noch eine erfolgreiche Spiegelung: `grep -n "spiegelt" server/src/services/userWorkPeriodProvisioning.test.ts` liefert keinen Treffer in einem Testtitel, der ein Erfolgsergebnis erwartet.
  </acceptance_criteria>
  <done>Die Provisioning-Tests belegen das neue Verhalten, einschließlich der Gegenproben für unveränderte Werte und für `hireDate`.</done>
</task>

<task type="auto">
  <name>Task 3: Die drei Gates fahren und den Restposten in PROJECT_STATUS.md schließen</name>
  <files>PROJECT_STATUS.md, .planning/phases/14-absicherung-und-auslieferung/14-WR07-ENTSCHEIDUNG.md</files>
  <read_first>
    - PROJECT_STATUS.md (Zeilen 240-280 — Abschnitt „Offene Restposten" mit der Tabelle und den vier Absätzen)
    - .planning/phases/14-absicherung-und-auslieferung/14-WR07-ENTSCHEIDUNG.md (Stand nach Task 1)
    - .planning/phases/11-datumsabh-ngige-berechnung/11-AUSGANGSZUSTAND.md (die drei verbindlichen roten Testtitel)
  </read_first>
  <action>
Fahre die vier Pflichtprüfungen und protokolliere jede wörtlich in
`14-WR07-ENTSCHEIDUNG.md` unter einer neuen Überschrift „Gates nach Plan 14-02":

1. `cd server && npx tsc --noEmit` — Exit 0.
2. `cd desktop && npx tsc --noEmit` — Exit 0.
3. `cd server && npx vitest run` — genau 3 rote Tests mit den drei bekannten Titeln aus
   `11-AUSGANGSZUSTAND.md`. Ein vierter roter Titel oder ein geänderter Titel ist ein
   Abbruchgrund: Befund wörtlich festhalten, Plan als blockiert melden.
4. `cd desktop && npm run check:rules` — Exit 0.

Stelle danach in `PROJECT_STATUS.md` die Tabellenzeile des Restpostens
`PUT /api/users/:id umgeht den Perioden-Schreibweg` von `🔓 OFFEN` auf
`🔒 GESCHLOSSEN (Phase 14, Plan 14-02)` um und schreibe den Absatz „Noch zu tun" auf
„Erledigt" um: welcher Zweig entfernt wurde, welcher Fehler jetzt mit HTTP 400 antwortet,
welche Tests das belegen, und ein Verweis auf `14-WR07-ENTSCHEIDUNG.md`. Den Absatz
„Bereits behoben (Code-Review Phase 13, WR-07)" stehen lassen — er beschreibt einen anderen,
früheren Teilschritt.

Ergänze in `.planning/phases/14-absicherung-und-auslieferung/14-UAT-SAMMLUNG.md` **nicht** —
das erledigt Plan 14-03 gebündelt, damit zwei Pläne nicht dieselbe Datei anfassen.
  </action>
  <verify>
    <automated>cd server &amp;&amp; npx vitest run 2>&amp;1 | grep -E "Tests +[0-9]+ failed"</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && npx tsc --noEmit` → Exit 0, keine Ausgabe.
    - `cd desktop && npx tsc --noEmit` → Exit 0, keine Ausgabe.
    - `cd server && npx vitest run` → `3 failed`, Titel wörtlich identisch mit den drei aus `11-AUSGANGSZUSTAND.md`.
    - `cd desktop && npm run check:rules` → Exit 0.
    - `grep -n "GESCHLOSSEN (Phase 14, Plan 14-02)" PROJECT_STATUS.md` liefert genau einen Treffer.
    - `grep -n "🔓 OFFEN" PROJECT_STATUS.md` liefert keinen Treffer mehr in der Restposten-Tabelle.
    - `14-WR07-ENTSCHEIDUNG.md` enthält die vier wörtlichen Gate-Ausgaben.
  </acceptance_criteria>
  <done>WR-07 ist geschlossen, belegt und in `PROJECT_STATUS.md` als erledigt geführt; die drei Gates stehen unverändert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → `PUT /api/users/:id` | Untrusted Eingabe eines angemeldeten Admins; bis heute Schreibweg in `user_work_periods` ohne Nachweis |
| `updateUser()` → `user_work_periods` | Historisierte Tabelle; jede Änderung soll Begründung, Journalzeile und `audit_log` erzeugen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Repudiation | `PUT /api/users/:id` | mitigate | Der Schreibweg entfällt; jede Stundenänderung muss über `POST /api/work-periods/change` bzw. `PUT /api/work-periods/:id` und hinterlässt `model_change`-Buchung, Begründung und `audit_log`-Eintrag. |
| T-14-07 | Tampering | `user_work_periods` | mitigate | `WorkPeriodBypassError` wird vor dem Aufbau des SQL-`UPDATE` geworfen; die Tests belegen für jeden Abweisungsfall, dass `users`, Periode und `overtime_transactions` unverändert bleiben. |
| T-14-08 | Denial of Service | Nutzerverwaltung der eigenen Oberfläche | mitigate | Die Abweisung greift nur bei einer **Wertänderung**, nicht bei Anwesenheit des Feldes — `EditUserModal.handleSubmit` schickt `weeklyHours` unverändert und muss weiter funktionieren. Eigener Test (`mirror-noop`) als Gegenprobe. |
| T-14-09 | Information Disclosure | Fehlermeldung | accept | Die Meldung nennt zwei interne Endpunktpfade. Empfänger ist ein authentifizierter Admin; ohne die Pfade ist die Meldung unbrauchbar. |
| T-14-10 | Tampering | Produktionsdatenbank | mitigate | Kein Schritt dieses Plans setzt `DATABASE_PATH`, öffnet einen Produktionspfad oder baut eine SSH-Verbindung auf. Ausschließlich lokale Arbeit gegen `server/database/development.db`. |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | Dieser Plan installiert kein Paket. Entfällt mangels Installationsschritt. |
</threat_model>

<verification>
- `cd server && npx tsc --noEmit` → Exit 0
- `cd desktop && npx tsc --noEmit` → Exit 0
- `cd server && npx vitest run` → genau 3 rot, Titel unverändert
- `cd desktop && npm run check:rules` → Exit 0
- `grep -n "mustMirrorPeriod" server/src/services/userService.ts` → kein Treffer
</verification>

<success_criteria>
Es gibt keinen Weg mehr, `weeklyHours` oder `workSchedule` an der Perioden-Historie vorbei zu
ändern. Die Abweisung antwortet mit HTTP 400 und nennt beide Ersatzwege. Die Entscheidung ist
schriftlich begründet, der Restposten in `PROJECT_STATUS.md` geschlossen.
</success_criteria>

<output>
Create `.planning/phases/14-absicherung-und-auslieferung/14-02-SUMMARY.md` when done
</output>
