---
phase: 14-absicherung-und-auslieferung
plan: 07
type: execute
wave: 4
depends_on: ["14-04", "14-05"]
files_modified:
  - server/src/scripts/backfillOvertimeJournal.ts
  - server/src/scripts/backfillOvertimeJournal.test.ts
  - server/package.json
  - .planning/phases/14-absicherung-und-auslieferung/14-URTEIL-PHASE-9.1.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [REQ-33]

must_haves:
  truths:
    - "Es steht schriftlich und begründet fest, welcher Teil der Phase 9.1 im Produktionsfenster der Phase 14 mitläuft und welcher nicht"
    - "Das Backfill-Werkzeug findet die unvollständigen Monate, ohne zu schreiben, und schreibt erst mit --apply"
    - "Der Backfill ist auf einer Produktionskopie gefahren, und der D5-Saldenvergleich bleibt davon unberührt"
  artifacts:
    - path: ".planning/phases/14-absicherung-und-auslieferung/14-URTEIL-PHASE-9.1.md"
      provides: "Begründetes Urteil zur Phase 9.1 mit Codebelegen und der Einordnung in das Produktionsfenster"
      contains: "Urteil"
    - path: "server/src/scripts/backfillOvertimeJournal.ts"
      provides: "Zwei-Stufen-Backfill der unvollständigen Monatsenden im overtime_transactions-Journal"
      contains: "--apply"
    - path: "server/src/scripts/backfillOvertimeJournal.test.ts"
      provides: "Tests der Findelogik für unvollständige Monate, ohne Schreibzugriff"
  key_links:
    - from: "server/src/scripts/backfillOvertimeJournal.ts"
      to: "server/src/services/overtimeTransactionRebuildService.ts"
      via: "rebuildOvertimeTransactionsForMonth je Nutzer und Monat"
      pattern: "rebuildOvertimeTransactionsForMonth"
    - from: ".planning/ROADMAP.md"
      to: "14-URTEIL-PHASE-9.1.md"
      via: "Phase-9.1-Eintrag verweist auf das Urteil"
      pattern: "14-URTEIL-PHASE-9"
---

<objective>
Phase 9.1 hat bis heute keine Pläne und ist laut ROADMAP mit dem Produktionsfenster der
Phase 14 gekoppelt. Dieser Plan fällt das Urteil darüber, welcher Teil hier mitläuft, baut das
dafür nötige Werkzeug und probt es auf einer Produktionskopie.

Purpose: Ohne ausdrückliches Urteil bliebe offen, ob das Produktionsfenster einmal oder
zweimal geöffnet wird — zwei Fenster bedeuten zwei Sicherungen, zwei Risikofenster und zwei
Freigaben.
Output: `14-URTEIL-PHASE-9.1.md`, das Zwei-Stufen-Werkzeug `backfillOvertimeJournal.ts` samt
Tests und ein Probelauf gegen eine Produktionskopie.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-absicherung-und-auslieferung/14-CONTEXT.md
@.planning/phases/14-absicherung-und-auslieferung/14-MIGRATIONSSTAND.md
@.planning/phases/14-absicherung-und-auslieferung/14-VORHER-NACHHER.md

<urteil_phase_9_1 priority="hoch">
## Urteil des Planners zu Phase 9.1

**Aufgeteilt: der Datenlauf kommt mit, die Codearbeit bleibt in Phase 9.1.**

### Teil 1 — Der Journal-Backfill läuft im Produktionsfenster der Phase 14 mit

Begründung:
1. **Dieselbe Infrastruktur, dasselbe Fenster.** Sicherung, Generalprobe auf der
   Produktionskopie, Vorher/Nachher-Vergleich und Freigabe sind für Phase 14 und für den
   9.1-Backfill identisch. Ein zweites Fenster bedeutete eine zweite Sicherung, ein zweites
   Risikofenster und eine zweite Freigabe für denselben Datenbestand.
2. **Der Backfill wird durch das Deployment der Phase 14 überhaupt erst möglich.** Der
   Monatsend-Off-by-one ist in `overtimeTransactionRebuildService.ts` und `overtimeService.ts`
   behoben (Plan 09-05), aber dieser Code liegt bis Plan 14-08 nicht auf dem Server
   (`origin/main` steht auf `0f2a03e`, Stand vor Phase 9). Ein Rebuild vor dem Deployment
   würde den Fehler reproduzieren statt ihn zu beheben.
3. **Der Backfill verfälscht den D5-Vergleich nicht.** Am Code geprüft:
   `snapshotBalances.ts` liest ausschließlich über
   `unifiedOvertimeService.calculatePeriodOvertime()` (Zeile 265-292), das für jeden Tag des
   Zeitraums `calculateDailyOvertime()` (Zeile 120-155) aufruft und dort Ist- und Sollstunden
   **aus `time_entries`, Abwesenheiten und Korrekturen neu berechnet**. Kein Lesezugriff auf
   `overtime_transactions`. Der Backfill schreibt ausschließlich in `overtime_transactions`
   und kann die D5-Zahlen daher nicht bewegen.
   Ebenso unberührt: `getOvertimeBalance()` (Zeile 454-475) summiert
   `overtime_balance`, nicht das Journal.

### Teil 2 — Der Backfill läuft als ZWEITER, eigens freigegebener Schritt NACH der Verifikation der Phase 14

Begründung: Was der Backfill sehr wohl bewegt, ist das, worüber die Verifikation der Phase 14
urteilt — `getOvertimeBalanceAtDate()` (Zeile 549-562) summiert `overtime_transactions`, und
der Kontoauszug listet genau diese Zeilen. Läuft der Backfill vor oder während der
Verifikation des realen Umstellungsfalls, ist eine Überraschung im Kontoauszug nicht mehr
eindeutig einer Ursache zuzuordnen: Modellwechsel oder Backfill. Deshalb erst danach — Plan
14-10, nach Plan 14-09.

### Teil 3 — Die Code-Härtung WR-02 bis WR-05 bleibt in Phase 9.1

Betroffen: Fehlerisolierung je Nutzer in `server/scripts/fix-overtime.ts` und der fehlende
`db.close()` im Fehlerpfad (WR-05), das fehlende `busy_timeout`-Pragma (WR-03), die
Untypisiertheit von `server/scripts/**` durch den Ausschluss in `tsconfig.json:26` (WR-04),
und die fehlenden Tests weiterer Servicefunktionen (WR-02).

Begründung: Das ist Codearbeit ohne Bezug zum Produktionsfenster. Sie in dieses Fenster zu
ziehen hieße, während eines laufenden Datenlaufs ein zweites Deployment einzuschieben — genau
die Vermischung, die D1 mit „jeder Schritt ist ein eigener Plan-Abschnitt mit eigenem
Nachweis" verhindern soll. Die Härtung wird nicht dringender dadurch, dass gerade ein Fenster
offen ist.

**Eine Ausnahme mit Bezug zum Fenster** wird nicht in Phase 9.1 vertagt, sondern in
Plan 14-08 behandelt: die Aufrufreihenfolge von `fix-overtime.ts` im Deploy-Workflow. Das
Skript läuft dort **vor** dem PM2-Start, also bevor `runMigrations()` die Tabelle
`user_work_periods` angelegt hat, und greift über
`ensureOvertimeBalanceEntries` → `unifiedOvertimeService` → `getDailyTargetHours()` genau auf
diese Tabelle zu. Das ist kein 9.1-Restpunkt, sondern eine unmittelbare Folge dieses
Deployments.
</urteil_phase_9_1>

<der_defekt>
Aus dem ROADMAP-Eintrag der Phase 9.1, wörtlich: „jeder vollständig durchlaufene Monat endet
dort am vorletzten Kalendertag (u. a. userId 3/17 auf `2026-09-29`, userId 15 auf
`2026-03-30`, userId 21/22/29 auf `2026-07-30`)".

Diese Nutzer-IDs stammen aus der Erhebung der Phase 9 und sind **nicht** als Kandidatenliste
zu verwenden. Das Werkzeug findet die betroffenen Nutzer und Monate selbst am Bestand — die
Liste dient nur als Gegenprobe, ob das Werkzeug plausibel findet.
</der_defekt>

<interfaces>
`server/src/services/overtimeTransactionRebuildService.ts`
- `rebuildOvertimeTransactionsForMonth(userId: number, month: string): …` — `month` im Format
  `YYYY-MM`. Der kanonische Rebuild-Weg, in Phase 11/12/13 durchgängig verwendet.

`server/src/scripts/migrateOvertimeToTransactions.ts` (Kopfkommentar Zeile 14-23) — der
Fallstrick, der hier gilt: **transaktional ist die Einzeleinheit, nicht der Lauf.** Bricht der
Lauf ab, sind alle vorher verarbeiteten Einheiten bereits festgeschrieben. Das Werkzeug muss
das aussprechen und beim Abbruch ausgeben, wie viele Nutzer und Monate bereits verarbeitet
waren.

`server/src/scripts/productionGuard.ts` — `assertNotProduction(explicitPath?)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Das Urteil zu Phase 9.1 schriftlich fassen und in der ROADMAP verankern</name>
  <files>.planning/phases/14-absicherung-und-auslieferung/14-URTEIL-PHASE-9.1.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (Abschnitt `### Phase 9.1: Journal-Backfill und Betriebs-Härtung` vollständig, und die Zeile der Phase 9.1 in der Fortschrittstabelle)
    - server/src/services/unifiedOvertimeService.ts (Zeilen 110-160 und 260-300 — belegen, dass `calculateDailyOvertime` und `calculatePeriodOvertime` nicht aus `overtime_transactions` lesen)
    - server/src/services/overtimeTransactionService.ts (Zeilen 450-480 und 545-565 — `getOvertimeBalance` liest `overtime_balance`, `getOvertimeBalanceAtDate` summiert `overtime_transactions`)
    - server/src/scripts/snapshotBalances.ts (Zeilen 1-45 — der Kopfkommentar zu den nebenwirkungsfreien Lesepfaden)
    - .planning/phases/14-absicherung-und-auslieferung/14-CONTEXT.md (D5)
  </read_first>
  <action>
Schreibe `.planning/phases/14-absicherung-und-auslieferung/14-URTEIL-PHASE-9.1.md` mit dem
Inhalt des Kontextblocks `urteil_phase_9_1` dieses Plans — als eigenständiges Dokument mit der
Überschrift `# Urteil zu Phase 9.1 im Produktionsfenster der Phase 14`, nicht als Nebensatz in
einer Zusammenfassung.

Prüfe jeden der drei Codebelege selbst am Quelltext nach, bevor du ihn übernimmst, und trage
die tatsächlich vorgefundenen Zeilennummern ein (Zero-Hallucination-Policy,
`.claude/CLAUDE.md`). Weicht ein Befund vom Kontextblock ab, gilt der Befund — dann ist das
Urteil neu zu begründen und die Abweichung ausdrücklich zu vermerken, statt den Text
durchzuwinken.

Gliedere so:
1. Die Frage (was zur Entscheidung stand)
2. Was Phase 9.1 umfasst (Backfill + WR-02 bis WR-05, aus dem ROADMAP-Eintrag)
3. Der Codebefund zur Verfälschungsfrage — mit Datei, Funktion, Zeilennummer und dem Satz,
   was die jeweilige Funktion tatsächlich liest
4. Das Urteil in drei Teilen (Backfill kommt mit / als zweiter Schritt danach / Code-Härtung
   bleibt)
5. Was daraus für die Planung folgt: Plan 14-10 fährt den Backfill, Phase 9.1 behält die
   Code-Härtung
6. Die eine Ausnahme (Aufrufreihenfolge von `fix-overtime.ts`), die Plan 14-08 übernimmt

Verankere das Urteil danach in `.planning/ROADMAP.md`:
- Im Abschnitt `### Phase 9.1` einen Absatz „Einordnung nach Plan 14-07" mit zwei bis drei
  Sätzen und dem Verweis auf `14-URTEIL-PHASE-9.1.md`.
- In der Fortschrittstabelle den Status der Zeile `9.1 Journal-Backfill und Betriebs-Härtung`
  von `Nicht begonnen — Ausführung im Produktionsfenster der Phase 14` auf
  `Aufgeteilt (Plan 14-07): Backfill läuft in Plan 14-10 mit, Code-Härtung WR-02–WR-05 bleibt offen`
  ändern.
Keine anderen Zeilen der ROADMAP anfassen.
  </action>
  <verify>
    <automated>grep -c "14-URTEIL-PHASE-9.1.md" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/14-absicherung-und-auslieferung/14-URTEIL-PHASE-9.1.md` existiert mit der Überschrift `# Urteil zu Phase 9.1 im Produktionsfenster der Phase 14` und den sechs genannten Gliederungspunkten.
    - Für jede der drei Funktionen (`calculatePeriodOvertime`, `getOvertimeBalance`, `getOvertimeBalanceAtDate`) steht im Dokument Datei, Zeilennummer und die Aussage, aus welcher Tabelle sie liest — jede Zeilennummer am Quelltext bestätigt.
    - Das Urteil benennt ausdrücklich alle drei Teile (Backfill mit, zweiter Schritt danach, Code-Härtung bleibt) und für jeden mindestens eine Begründung.
    - `grep -n "14-URTEIL-PHASE-9.1.md" .planning/ROADMAP.md` liefert mindestens einen Treffer im Abschnitt der Phase 9.1.
    - Die Fortschrittstabelle der ROADMAP trägt für Phase 9.1 den neuen Status; `grep -n "Ausführung im Produktionsfenster der Phase 14" .planning/ROADMAP.md` liefert in der Tabellenzeile keinen Treffer mehr.
    - `git diff --stat .planning/ROADMAP.md` zeigt Änderungen ausschließlich im Abschnitt der Phase 9.1 und in ihrer Tabellenzeile.
  </acceptance_criteria>
  <done>Das Urteil zu Phase 9.1 steht begründet und belegt als eigenes Dokument und ist in der ROADMAP verankert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Das Zwei-Stufen-Backfill-Werkzeug bauen</name>
  <files>server/src/scripts/backfillOvertimeJournal.ts, server/src/scripts/backfillOvertimeJournal.test.ts, server/package.json</files>
  <read_first>
    - server/src/scripts/migrateOvertimeToTransactions.ts (vollständig — Kopfkommentar zur Nicht-Atomarität des Laufs, Produktionsschutz, Ausgabeform bei Abbruch)
    - server/src/scripts/applyModelChange.ts (Stand nach Plan 14-05 — die dort etablierte Importreihenfolge, `DATABASE_PATH`-Pflichtprüfung, `--allow-production`)
    - server/src/services/overtimeTransactionRebuildService.ts (Zeilen 78-200 — was `rebuildOvertimeTransactionsForMonth` löscht und neu schreibt, und welche `type`-Werte es dabei anfasst)
    - server/src/scripts/productionGuard.ts (vollständig)
    - server/package.json (Abschnitt `scripts`)
  </read_first>
  <behavior>
    - Ohne `--apply` gibt das Werkzeug nur eine Fundliste aus; `COUNT(*)` und `SUM(hours)` in `overtime_transactions` sind danach unverändert.
    - Fehlt `DATABASE_PATH`, endet das Werkzeug mit Exit 2 und der Meldung `FEHLER: DATABASE_PATH ist nicht gesetzt.`
    - Ein Monat gilt als unvollständig, wenn er vollständig in der Vergangenheit liegt (letzter Kalendertag < heute) und das Höchstdatum der `earned`-Zeilen des Nutzers in diesem Monat kleiner ist als der letzte Werktag, an dem der Nutzer nach seinem Arbeitszeitmodell Sollstunden hatte.
    - Monate vor dem `hireDate` des Nutzers und Monate nach seinem `endDate` werden nicht als unvollständig gemeldet.
    - Nutzer mit `deletedAt IS NOT NULL` werden übersprungen und ausdrücklich als übersprungen gezählt, nicht stillschweigend weggelassen.
    - Bei Abbruch mitten im Lauf gibt das Werkzeug aus, wie viele Nutzer und wie viele Monate bereits geschrieben wurden.
    - `--userId=<id>` beschränkt den Lauf auf einen Nutzer; ohne das Argument läuft er über alle.
  </behavior>
  <action>
Lege `server/src/scripts/backfillOvertimeJournal.ts` an — ein Zwei-Stufen-Werkzeug (D3), das
die unvollständigen Monatsenden im `overtime_transactions`-Journal findet und auf Anweisung
über `rebuildOvertimeTransactionsForMonth(userId, month)` neu aufbaut.

Übernimm aus `applyModelChange.ts` wörtlich: die Importreihenfolge (nur importsichere Module
am Kopf, Argumentauswertung, `DATABASE_PATH`-Pflichtprüfung, `assertNotProduction` sofern
`--allow-production` fehlt, danach erst die dynamischen Importe innerhalb von `main()`), die
erste Ausgabezeile mit dem aufgelösten Datenbankpfad und das Modusbanner.

Argumente: `--apply` (schreibt), `--allow-production` (umgeht den Guard, mit Warnzeile),
`--userId=<id>` (optional, beschränkt den Lauf), `--maxMonths=<n>` (optional, begrenzt die
Zahl der geschriebenen Monate — der Sicherheitsgurt für einen ersten Produktionslauf).

Ablauf:
1. Aufgelösten Pfad und Modus drucken.
2. **Finden**: je Nutzer (ungefiltert gelesen, soft-gelöschte ausdrücklich gezählt und
   übersprungen) die Menge der vollständig vergangenen Monate zwischen `hireDate` und heute
   bzw. `endDate` bestimmen. Je Monat das Höchstdatum der vorhandenen `earned`-Zeilen mit dem
   letzten Tag des Monats vergleichen, an dem der Nutzer nach `getDailyTargetHours` Sollstunden
   hatte. Fehlt mindestens ein solcher Tag, ist der Monat unvollständig.
3. **Fundliste drucken**: je Nutzer eine Zeile mit Id, Name, Zahl der unvollständigen Monate
   und den Monaten selbst; darunter eine Summenzeile mit der Gesamtzahl betroffener Nutzer und
   Monate. Zusätzlich `COUNT(*)` und `ROUND(SUM(hours),6)` von `overtime_transactions` vor dem
   Lauf.
4. Ohne `--apply`: `Trockenlauf beendet. Mit --apply ausfuehren.` und Exit 0.
5. Mit `--apply`: je Nutzer und Monat `rebuildOvertimeTransactionsForMonth` aufrufen, je Monat
   eine Fortschrittszeile drucken. Den Lauf **nicht** in eine einzige Transaktion klammern —
   die Einzeleinheit ist der Monat, wie bei `migrateOvertimeToTransactions.ts`. Den Zähler
   „X von Y Monaten verarbeitet" mitführen und im `catch`-Zweig ausgeben, bevor mit Exit 1
   abgebrochen wird.
6. Nach dem Lauf: `COUNT(*)` und `ROUND(SUM(hours),6)` von `overtime_transactions` erneut
   drucken, dazu die Differenz, und `PRAGMA integrity_check`.
7. Ausdrücklich prüfen und drucken, dass die Zahl der `model_change`-Zeilen vor und nach dem
   Lauf gleich ist — der Backfill darf keine Modellwechsel-Buchung anfassen. Bei Abweichung
   Exit 1.

Lege `server/src/scripts/backfillOvertimeJournal.test.ts` an. Getestet wird die **Findelogik**
als reine Funktion (exportiere `findIncompleteMonths(...)` mit den gelesenen Daten als
Parameter, nicht mit einem Datenbankzugriff im Inneren): Monat vollständig → nicht gemeldet;
Monat endet am vorletzten Werktag → gemeldet; Monat vor `hireDate` → nicht gemeldet; Monat
nach `endDate` → nicht gemeldet; laufender Monat → nicht gemeldet; soft-gelöschter Nutzer →
übersprungen und gezählt. Zusätzlich die Argumentauswertung nach dem Muster aus
`applyModelChange.test.ts`.

Trage in `server/package.json` unter `scripts` ein:
`"backfill:overtime-journal": "tsx src/scripts/backfillOvertimeJournal.ts"`.
  </action>
  <verify>
    <automated>cd server &amp;&amp; npx vitest run src/scripts/backfillOvertimeJournal.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && npx vitest run src/scripts/backfillOvertimeJournal.test.ts` meldet `0 failed` bei mindestens 8 Tests.
    - `cd server && npx tsx src/scripts/backfillOvertimeJournal.ts` (ohne `DATABASE_PATH`) endet mit Exitcode 2 und der Meldung `FEHLER: DATABASE_PATH ist nicht gesetzt.`
    - `grep -n "rebuildOvertimeTransactionsForMonth" server/src/scripts/backfillOvertimeJournal.ts` liefert mindestens einen Treffer; `grep -n "DELETE FROM overtime_transactions\|INSERT INTO overtime_transactions" server/src/scripts/backfillOvertimeJournal.ts` liefert keinen Treffer (keine eigene Schreiblogik).
    - `grep -n "model_change" server/src/scripts/backfillOvertimeJournal.ts` liefert mindestens einen Treffer (die Unversehrtheitsprüfung aus Schritt 7).
    - `grep -n '"backfill:overtime-journal"' server/package.json` liefert genau einen Treffer.
    - `cd server && npx tsc --noEmit` endet mit Exit 0.
  </acceptance_criteria>
  <done>Das Backfill-Werkzeug findet die unvollständigen Monate ohne zu schreiben und schützt die Modellwechsel-Buchungen.</done>
</task>

<task type="auto">
  <name>Task 3: Den Backfill auf einer Produktionskopie proben und seine Nichteinwirkung auf den D5-Vergleich belegen</name>
  <files>.planning/phases/14-absicherung-und-auslieferung/14-URTEIL-PHASE-9.1.md</files>
  <read_first>
    - server/src/scripts/backfillOvertimeJournal.ts (Stand nach Task 2)
    - .planning/phases/14-absicherung-und-auslieferung/14-MIGRATIONSSTAND.md (der `--asOf`-Wert und die Rohkennzahlen der Produktionskopie)
    - .planning/phases/14-absicherung-und-auslieferung/14-VORHER-NACHHER.md (die Aufrufform der Snapshot-Läufe)
    - .planning/ROADMAP.md (Abschnitt Phase 9.1 — die dort genannten Beispiel-Nutzer-IDs als Gegenprobe)
  </read_first>
  <action>
Lege eine eigene Probekopie an, damit `14-generalprobe.db` (mit dem Generalprobe-Modellwechsel
aus Plan 14-05) unberührt bleibt:
`VACUUM INTO './database/14-backfill-probe.db'` gegen `./database/14-produktionskopie.db`,
danach
`DATABASE_PATH=./database/14-backfill-probe.db npm run migrate:copy -- --db=./database/14-backfill-probe.db --expect-migration=015_unique_reversal_of_index`.

Fahre dann und protokolliere jeden Schritt wörtlich in einem neuen Abschnitt „Probelauf des
Backfills (Plan 14-07)" von `14-URTEIL-PHASE-9.1.md`:

1. **Snapshot VORHER** gegen die Probekopie:
   `DATABASE_PATH=./database/14-backfill-probe.db npm run snapshot:balances -- --all --asOf=<asOf> --json=../.planning/phases/14-absicherung-und-auslieferung/14-SNAPSHOT-BF-VORHER.json`
2. **Trockenlauf**:
   `DATABASE_PATH=./database/14-backfill-probe.db npm run backfill:overtime-journal`
   Fundliste vollständig übernehmen. Belegen, dass nichts geschrieben wurde: `COUNT(*)` und
   `ROUND(SUM(hours),6)` von `overtime_transactions` vor und nach dem Trockenlauf identisch.
   **Gegenprobe zur Plausibilität**: Prüfen, ob unter den gefundenen Nutzern die im
   ROADMAP-Eintrag der Phase 9.1 genannten Beispiele (userId 3, 15, 17, 21, 22, 29) auftauchen.
   Tauchen sie nicht auf, ist das kein Fehler — der Bestand kann sich seit Phase 9 geändert
   haben —, aber es ist ausdrücklich zu vermerken und zu erklären, statt es zu übergehen.
3. **Schreiblauf**:
   `DATABASE_PATH=./database/14-backfill-probe.db npm run backfill:overtime-journal -- --apply`
   Erwartet: Exit 0, Differenz in `COUNT(*)` von `overtime_transactions` **größer als 0**
   (sonst gab es nichts zu tun — dann ausdrücklich festhalten und den Rest des Tasks
   entsprechend als „nicht anwendbar" begründen statt zu behaupten), Zahl der
   `model_change`-Zeilen unverändert, `integrity_check` = `ok`.
4. **Snapshot NACHHER** mit demselben `--asOf`:
   `--json=../.planning/phases/14-absicherung-und-auslieferung/14-SNAPSHOT-BF-NACHHER.json`
5. **Der entscheidende Nachweis**: `diff` der beiden `*.users.json`-Dateien.
   Verbindliche Erwartung: **kein Unterschied**. Das ist der maschinelle Beleg für Teil 1 des
   Urteils — der Backfill bewegt die von D5 gemessene Größe nicht. Zeigt der Diff doch einen
   Unterschied, ist das Urteil widerlegt: Befund wörtlich festhalten, Teil 1 und Teil 2 des
   Urteils neu begründen und den Plan als blockiert melden.
6. **Gegenprobe, dass der Backfill überhaupt etwas bewegt hat**: für einen der betroffenen
   Nutzer `getOvertimeBalanceAtDate(userId, <letzter Tag eines betroffenen Monats>)` vor und
   nach dem Lauf gegenüberstellen — hier **muss** sich ein Unterschied zeigen. Ohne diese
   Gegenprobe wäre „kein Diff in Schritt 5" auch mit einem wirkungslosen Backfill vereinbar.
7. Kettenprüfung: `DATABASE_PATH=./database/14-backfill-probe.db npm run check:period-chains`
   — Exitcode 0.

Fahre abschließend die vier Gates und protokolliere sie wörtlich.
  </action>
  <verify>
    <automated>cd server &amp;&amp; DATABASE_PATH=./database/14-backfill-probe.db npm run check:period-chains</automated>
  </verify>
  <acceptance_criteria>
    - `server/database/14-backfill-probe.db` existiert, `migrate:copy` endete mit Exit 0 und `--expect-migration=015_unique_reversal_of_index` wurde erfüllt.
    - Der Trockenlauf hinterlässt `COUNT(*)` und `SUM(hours)` von `overtime_transactions` unverändert; beide Werte stehen vor und nach dem Lauf im Protokoll.
    - Der Schreiblauf endet mit Exit 0, `integrity_check` = `ok`, und die Zahl der `model_change`-Zeilen ist vor und nach dem Lauf gleich.
    - `diff` der beiden `*.users.json`-Dateien liefert **keine** Ausgabe; das Ergebnis steht wörtlich im Protokoll.
    - Die Gegenprobe aus Schritt 6 zeigt für mindestens einen Nutzer einen Unterschied in `getOvertimeBalanceAtDate`; Nutzer, Datum, Wert vorher und Wert nachher stehen im Protokoll.
    - Die Plausibilitäts-Gegenprobe zu den ROADMAP-Beispiel-IDs ist ausgeführt und ihr Ergebnis erklärt.
    - `DATABASE_PATH=./database/14-backfill-probe.db npm run check:period-chains` endet mit Exitcode 0.
    - `cd server && npx tsc --noEmit`, `cd desktop && npx tsc --noEmit`, `cd desktop && npm run check:rules` → je Exit 0; `cd server && npx vitest run` → `3 failed` mit den drei bekannten Titeln.
  </acceptance_criteria>
  <done>Der Backfill ist auf einer Produktionskopie gefahren, wirkt nachweislich, und seine Nichteinwirkung auf den D5-Vergleich ist maschinell belegt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backfill → `overtime_transactions` | Massenschreibzugriff über Bestandsdaten aller Nutzer und aller vergangenen Monate |
| Backfill → `model_change`-Buchungen | Ein Rebuild, der Modellwechsel-Buchungen mit löscht, zerstört den Nachweis dieses Milestones |
| Urteil → Produktionsfenster | Ein falsches Urteil öffnet das Fenster ein zweites Mal oder vermischt zwei Datenläufe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-34 | Tampering | `model_change`-Zeilen | mitigate | Das Werkzeug zählt die `model_change`-Zeilen vor und nach dem Lauf und bricht mit Exit 1 ab, wenn sich die Zahl ändert; der Probelauf prüft das als eigenes Akzeptanzkriterium. |
| T-14-35 | Tampering | Teilzustand bei Abbruch | mitigate | Die Nicht-Atomarität des Laufs wird im Kopfkommentar ausgesprochen; der Zähler „X von Y Monaten verarbeitet" wird im Fehlerpfad ausgegeben, damit der Bediener weiß, wo er steht. `--maxMonths` begrenzt den ersten Produktionslauf. |
| T-14-36 | Tampering | `production.db` | mitigate | `assertNotProduction()` vor jedem dynamischen Import; Umgehung nur über den ausdrücklichen `--allow-production`-Schalter mit Warnzeile; ohne `--apply` bleibt es auch dann ein Trockenlauf. |
| T-14-37 | Tampering | leere Datenbank durch fehlendes `DATABASE_PATH` | mitigate | Pflichtprüfung mit Exit 2 vor `assertNotProduction()`, geprüft als eigenes Akzeptanzkriterium. |
| T-14-38 | Repudiation | Behauptung „der Backfill stört den D5-Vergleich nicht" | mitigate | Maschineller Diff zweier `*.users.json`-Dateien **plus** Gegenprobe über `getOvertimeBalanceAtDate`, die belegt, dass der Backfill überhaupt gewirkt hat. Ohne die Gegenprobe wäre ein leerer Diff auch mit einem wirkungslosen Werkzeug vereinbar. |
| T-14-39 | Information Disclosure | `14-SNAPSHOT-BF-*.json` | accept | Stammdaten und Salden, keine Passwort-Hashes; `.planning/` führt bereits vergleichbare Nachweise, das Repository ist privat. |
| T-14-40 | Denial of Service | Laufzeit des Backfills auf Produktion | mitigate | Der Probelauf misst Zahl der Nutzer und Monate; `--maxMonths` erlaubt eine Begrenzung. Plan 14-10 übernimmt die gemessene Laufzeit als Erwartungswert. |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | Dieser Plan installiert kein Paket; `server/package.json` bekommt nur einen `scripts`-Eintrag. |
</threat_model>

<verification>
- `14-URTEIL-PHASE-9.1.md` existiert mit belegtem Urteil in drei Teilen und dem Probelaufprotokoll
- `.planning/ROADMAP.md` verweist im Abschnitt der Phase 9.1 auf das Urteil, Fortschrittstabelle angepasst
- `cd server && npx vitest run src/scripts/backfillOvertimeJournal.test.ts` → 0 failed
- Trockenlauf ohne Schreibwirkung, Schreiblauf mit Wirkung, `model_change`-Zahl unverändert
- `diff` der beiden `*.users.json` → leer; Gegenprobe über `getOvertimeBalanceAtDate` → Unterschied vorhanden
- Die vier Gates unverändert
</verification>

<success_criteria>
Es steht begründet und belegt fest, welcher Teil der Phase 9.1 im Produktionsfenster mitläuft.
Das Werkzeug dafür existiert, ist getestet und auf einer Produktionskopie gefahren, und sein
Einfluss auf den D5-Vergleich ist maschinell mit null belegt — bei nachgewiesener eigener
Wirkung.
</success_criteria>

<output>
Create `.planning/phases/14-absicherung-und-auslieferung/14-07-SUMMARY.md` when done
</output>
