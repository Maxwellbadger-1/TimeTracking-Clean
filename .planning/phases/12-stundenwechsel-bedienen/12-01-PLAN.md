---
phase: 12-stundenwechsel-bedienen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/database/migrations/011_add_model_change_transaction_type.ts
  - server/src/database/schema.ts
  - server/src/types/index.ts
  - server/src/services/overtimeTransactionManager.ts
  - server/src/routes/workPeriods.ts
  - server/src/server.ts
autonomous: true
requirements: [REQ-26, REQ-29]

must_haves:
  truths:
    - "D5: Eine Buchung vom Typ 'model_change' kann in overtime_transactions geschrieben werden und traegt ueber referenceType='work_period' + referenceId den Bezug auf die ausloesende Periode"
    - "D6: Ein Mitarbeiter kann ueber GET /api/work-periods ausschliesslich seine eigenen Perioden lesen; eine fremde userId beantwortet der Server mit 403"
    - "Eine frisch angelegte Datenbank (schema.ts) und eine migrierte Bestandsdatenbank (Migration 011) haben denselben CHECK-Constraint auf overtime_transactions.type"
    - "Der Vertrag zwischen Server und Desktop (Eingabe, Vorschauwerte, Ergebnis) steht als exportierter Typ in server/src/types/index.ts, bevor irgendein Aufrufer gebaut wird"
  artifacts:
    - path: "server/src/database/migrations/011_add_model_change_transaction_type.ts"
      provides: "Erweiterung des type- und referenceType-CHECK auf overtime_transactions"
      contains: "model_change"
    - path: "server/src/routes/workPeriods.ts"
      provides: "GET /api/work-periods — Perioden-Lese-API mit Rollenpruefung"
      exports: ["default"]
    - path: "server/src/types/index.ts"
      provides: "WorkTimeChangeInput, WorkTimeChangePreview, WorkTimeChangeOutcome"
      contains: "WorkTimeChangePreview"
  key_links:
    - from: "server/src/server.ts"
      to: "server/src/routes/workPeriods.ts"
      via: "app.use('/api/work-periods', workPeriodsRoutes)"
      pattern: "api/work-periods"
    - from: "server/src/routes/workPeriods.ts"
      to: "server/src/services/workPeriodService.ts"
      via: "getWorkPeriods(userId)"
      pattern: "getWorkPeriods"
---

<objective>
Das Fundament der Phase: der Buchungstyp `model_change` existiert in Schema und Migration, der
Datenvertrag zwischen Server und Desktop steht als Typ fest, und der Desktop bekommt zum ersten
Mal einen Lesezugang zu den Arbeitszeitperioden eines Nutzers.

Zweck: Ohne den erweiterten CHECK-Constraint kann die Buchung aus REQ-29 nicht geschrieben
werden; ohne die Perioden-Lese-API kann weder die Periodenliste im Dialog noch der
WR-12-Nachzug im Desktop gebaut werden (`11-DESKTOP-DISPOSITION.md` nennt diese API
ausdruecklich als fehlende Voraussetzung).
Ausgabe: Migration 011, angeglichenes `schema.ts`, Vertragstypen, `GET /api/work-periods`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/12-stundenwechsel-bedienen/12-CONTEXT.md
@.planning/phases/12-stundenwechsel-bedienen/12-PATTERNS.md

<interfaces>
Bereits vorhanden, hier NICHT neu zu erfinden (aus dem Bestand extrahiert):

server/src/types/index.ts:
```typescript
export interface UserWorkPeriod {
  id: number;
  userId: number;
  validFrom: string;      // inklusiv, YYYY-MM-DD
  validTo: string | null; // exklusiv, null = laufend
  weeklyHours: number;
  workSchedule: WorkSchedule | null;
  note: string | null;
  createdAt: string;
  createdBy: number | null;
}
```

server/src/services/workPeriodService.ts:
```typescript
export function getWorkPeriods(userId: number): UserWorkPeriod[];
export function resolveWorkPeriodIn(periods: UserWorkPeriod[], date: string): UserWorkPeriod | null;
export function createWorkPeriod(input: CreateWorkPeriodInput): UserWorkPeriod;
export function closeWorkPeriod(periodId: number, validTo: string): UserWorkPeriod;
export function checkPeriodChain(userId: number): PeriodChainCheckResult;
export function checkAllPeriodChains(): PeriodChainIssue[];
export class WorkPeriodConflictError extends Error {}
```

server/src/services/overtimeTransactionManager.ts (heutiger Stand, Zeile 29-40):
```typescript
export interface TransactionParams {
  userId: number;
  date: string;
  type: 'earned' | 'compensation' | 'correction' | 'carryover' | 'vacation_credit'
      | 'sick_credit' | 'overtime_comp_credit' | 'special_credit' | 'unpaid_adjustment';
  hours: number;
  description: string;
  referenceType?: 'time_entry' | 'absence' | 'manual' | 'system' | null;
  referenceId?: number | null;
  createdBy?: number | null;
  balanceBefore?: number;
  balanceAfter?: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 011 und schema.ts auf den Buchungstyp model_change bringen</name>
  <files>server/src/database/migrations/011_add_model_change_transaction_type.ts, server/src/database/schema.ts</files>
  <read_first>
    - `server/src/database/migrations/006_add_time_entry_transaction_type.ts` — vollstaendig. Das ist die woertliche Vorlage (Tabellen-Neubau, Zeilenzahlvergleich, DROP, RENAME, drei CREATE INDEX). 12-PATTERNS.md Abschnitt 5 zitiert Zeile 24-147 als Schritt-fuer-Schritt-Muster.
    - `server/src/database/schema.ts` Zeile 505-535 — der heutige CREATE TABLE overtime_transactions mit der CHECK-Liste.
    - `server/src/database/migrationRunner.ts` Zeile 110-215 — belegt: Migrationen werden alphabetisch geladen, synchron in einer db.transaction() ausgefuehrt; `up()` darf KEINE AsyncFunction sein.
    - `server/src/database/migrations/010_fix_user_work_periods_delete_guard.ts` — juengste Migration, zeigt den aktuellen Stil (logger statt console).
  </read_first>
  <action>
Lege `server/src/database/migrations/011_add_model_change_transaction_type.ts` an, exakt nach dem
Muster von Migration 006 (Tabellen-Neubau, weil SQLite kein `ALTER TABLE ... ADD CONSTRAINT`
kennt). `up(db: Database.Database): void` — synchron, kein `async`.

Zwei Erweiterungen an der neu gebauten Tabelle `overtime_transactions_new`:
1. Der `type`-CHECK bekommt zusaetzlich den Wert `'model_change'`. Die uebrigen 18 Werte werden
   woertlich aus dem heutigen Stand uebernommen: `'worked', 'time_entry', 'vacation_credit',
   'sick_credit', 'overtime_comp_credit', 'special_credit', 'unpaid_deduction', 'holiday_credit',
   'weekend_credit', 'carry_over', 'payout', 'correction', 'initial_balance', 'year_end_balance',
   'earned', 'compensation', 'carryover', 'unpaid_adjustment'`.
2. Der `referenceType`-CHECK bekommt zusaetzlich `'work_period'` (D5 verlangt den Bezug auf die
   ausloesende Periode; eine neue Spalte ist dafuer nicht noetig, `referenceType`/`referenceId`
   existieren bereits).

Die neu gebaute Tabelle uebernimmt die Spalten des heutigen Bestands **einschliesslich**
`balanceBefore REAL` und `balanceAfter REAL` (aus Migration 005) — sonst gehen diese Spalten
beim Tabellen-Neubau verloren. Vor dem Neubau die Zeilenzahl merken, nach dem `INSERT INTO ...
SELECT ...` gegen die gemerkte Zahl pruefen und bei Abweichung mit einer sprechenden Meldung
werfen (Muster 006). Danach `DROP TABLE overtime_transactions`, `ALTER TABLE
overtime_transactions_new RENAME TO overtime_transactions`, und die drei Indizes aus Migration
006 wortgleich neu anlegen. Protokollierung ausschliesslich ueber `logger` aus
`../../utils/logger.js`, keine `console.*`.

Danach in `server/src/database/schema.ts` (Zeile ~517-523) dieselben beiden CHECK-Listen
erweitern: `'model_change'` im `type`-CHECK, `'work_period'` im `referenceType`-CHECK.
Parity-Pflicht: eine frische Installation ueber `schema.ts` und eine migrierte Bestandsdatenbank
muessen denselben Constraint tragen.

VORBESTEHENDE ABWEICHUNG, NICHT BEHEBEN: `schema.ts` fuehrt in diesem CREATE TABLE keine
Spalten `balanceBefore`/`balanceAfter`, obwohl Migration 005 sie ergaenzt hat. Das ist eine
Altlast vor dieser Phase. Sie wird NICHT angefasst (kein Aufraeumfeldzug), aber im SUMMARY unter
"Beobachtete Altlasten" schriftlich vermerkt.
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit && npx tsx -e "import('./src/database/migrations/011_add_model_change_transaction_type.ts').then(m => { if (m.default.up.constructor.name === 'AsyncFunction') { throw new Error('up() darf nicht async sein'); } console.log('ok'); })"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "model_change" server/src/database/migrations/011_add_model_change_transaction_type.ts` liefert mindestens 1
    - `grep -c "work_period" server/src/database/migrations/011_add_model_change_transaction_type.ts` liefert mindestens 1
    - `grep -c "model_change" server/src/database/schema.ts` liefert genau 1
    - `grep -c "work_period" server/src/database/schema.ts` liefert genau 1
    - `grep -c "balanceBefore" server/src/database/migrations/011_add_model_change_transaction_type.ts` liefert mindestens 1 (Spalte geht beim Neubau nicht verloren)
    - `grep -c "async up" server/src/database/migrations/011_add_model_change_transaction_type.ts` liefert 0
    - `grep -c "console\." server/src/database/migrations/011_add_model_change_transaction_type.ts` liefert 0
    - `cd server && npx tsc --noEmit` endet mit Exitcode 0
    - Gegen eine Arbeitskopie der Entwicklungsdatenbank (`cp server/database.db /tmp/12-01-probe.db`, dann `DATABASE_PATH=/tmp/12-01-probe.db npm run migrate` bzw. der im Projekt uebliche Migrationsbefehl) gilt: `SELECT COUNT(*) FROM overtime_transactions` ist vor und nach dem Lauf identisch, und `INSERT INTO overtime_transactions (userId, date, type, hours, description, referenceType) VALUES (1,'2026-01-01','model_change',1.0,'probe','work_period')` laeuft ohne CHECK-Fehler durch (danach wieder loeschen, es ist eine Arbeitskopie)
  </acceptance_criteria>
  <done>Migration 011 existiert, ist synchron, erweitert beide CHECK-Listen; schema.ts traegt dieselben beiden Werte; ein Probe-INSERT mit type='model_change' und referenceType='work_period' wird von der Datenbank angenommen.</done>
</task>

<task type="auto">
  <name>Task 2: Vertragstypen fuer den Stundenwechsel festschreiben</name>
  <files>server/src/types/index.ts, server/src/services/overtimeTransactionManager.ts</files>
  <read_first>
    - `server/src/types/index.ts` Zeile 240-285 — `UserWorkPeriod`, `UserWorkPeriodRow`, `WorkSchedule`; der neue Vertrag wird direkt daneben abgelegt.
    - `server/src/services/overtimeTransactionManager.ts` Zeile 29-130 — `TransactionParams` und `createTransaction()`; die beiden Unions muessen erweitert werden, sonst ist die Buchung aus REQ-29 nicht typisierbar.
    - `.planning/phases/12-stundenwechsel-bedienen/12-UI-SPEC.md` Abschnitt "Vorschaupanel" (Textbuch) — er legt fest, welche Werte die Oberflaeche anzeigt: Zeitraumsatz mit Arbeitstagen, Sollstunden bisher/neu/Differenz, Saldo heute/nachher, hervorgehobene Saldoaenderung, Hinweis bei Stichtag mitten im Monat, Panel "nichts zu tun".
  </read_first>
  <action>
In `server/src/types/index.ts` diese Typen ergaenzen und exportieren (Namen woertlich, sie sind
der Vertrag fuer alle Folgeplaene):

`WorkTimeChangeInput` mit `userId: number`, `validFrom: string` (YYYY-MM-DD, inklusiv),
`weeklyHours: number`, `workSchedule: WorkSchedule | null`, `reason: string`.

`WorkTimeChangePreview` mit `userId: number`, `validFrom: string`, `isRetroactive: boolean`,
`rangeStart: string`, `rangeEnd: string`, `workingDaysInRange: number`, `targetHoursBefore:
number`, `targetHoursAfter: number`, `targetHoursDelta: number`, `balanceBefore: number`,
`balanceAfter: number`, `balanceDelta: number`, `currentPeriod: { validFrom: string;
weeklyHours: number; workSchedule: WorkSchedule | null } | null`, `isNoOp: boolean`,
`midMonthEffective: boolean`, `affectedMonths: string[]`.

`WorkTimeChangePreviewResponse` = `WorkTimeChangePreview & { previewToken: string }` — nur die
Vorschau-Route haengt das Token an; die Berechnung selbst kennt kein Token.

`WorkTimeChangeOutcome` mit `preview: WorkTimeChangePreview`, `period: UserWorkPeriod | null`
(im Trockenlauf `null`, weil die Periode zurueckgerollt wird), `transactionId: number | null`
(`null`, wenn die Saldodifferenz 0 ist — D4: bei einem Stichtag in der Zukunft entsteht keine
Buchung).

Jedes Feld bekommt einen kurzen deutschen Doc-Kommentar, der seine fachliche Bedeutung nennt
(Vorbild: die Kommentare an `UserWorkPeriod`). Kein `any`.

In `server/src/services/overtimeTransactionManager.ts` die Union `TransactionParams['type']` um
`'model_change'` und die Union `TransactionParams['referenceType']` um `'work_period'`
erweitern. Sonst nichts an dieser Datei aendern — insbesondere bleibt die Duplikatspruefung in
`createTransaction()` unangetastet.
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export interface WorkTimeChangeInput" server/src/types/index.ts` liefert 1
    - `grep -c "export interface WorkTimeChangePreview" server/src/types/index.ts` liefert 1
    - `grep -c "WorkTimeChangeOutcome" server/src/types/index.ts` liefert mindestens 1
    - `grep -c "previewToken" server/src/types/index.ts` liefert mindestens 1
    - `grep -c "model_change" server/src/services/overtimeTransactionManager.ts` liefert 1
    - `grep -c "work_period" server/src/services/overtimeTransactionManager.ts` liefert 1
    - `grep -vn "^\s*\*" server/src/types/index.ts | grep -c ": any" liefert 0
    - `cd server && npx tsc --noEmit` endet mit Exitcode 0
  </acceptance_criteria>
  <done>Alle vier Vertragstypen sind exportiert und dokumentiert; `TransactionParams` akzeptiert `type: 'model_change'` und `referenceType: 'work_period'`; der Server kompiliert fehlerfrei.</done>
</task>

<task type="auto">
  <name>Task 3: GET /api/work-periods — Perioden-Lese-API mit Rollenpruefung (D6)</name>
  <files>server/src/routes/workPeriods.ts, server/src/server.ts</files>
  <read_first>
    - `server/src/routes/workTimeAccounts.ts` Zeile 139-179 — das exakte Analog laut 12-PATTERNS.md Abschnitt 4: `userId`-Query-Parameter, Admin-vs-Eigenzugriff, 403 bei fremder userId, `ApiResponse`-Huelle.
    - `server/src/routes/vacationTransactions.ts` Zeile 23-59 — zweites Vorkommen desselben Rollenmusters, dort mit `logger.error({ err: error }, ...)` statt `console.error`; fuer neuen Code ist `logger` das zu verwendende Muster.
    - `server/src/services/workPeriodService.ts` Zeile 191-205 — `getWorkPeriods(userId)` existiert bereits; die Route legt nur die HTTP-Huelle und die Rollenpruefung drumherum, es entsteht KEINE neue Service-Funktion.
    - `server/src/server.ts` Zeile 170-200 — die Liste der `app.use('/api/...')`-Registrierungen.
    - `server/src/middleware/auth.ts` — `requireAuth`, `requireAdmin`.
  </read_first>
  <action>
Lege `server/src/routes/workPeriods.ts` an: ein Express-`Router` als Default-Export, vorerst mit
genau einem Endpunkt.

`GET /` (also `GET /api/work-periods`), Middleware `requireAuth`:
- `const isAdmin = req.session.user!.role === 'admin';`
- `const requestedUserId = req.query.userId ? parseInt(req.query.userId as string, 10) : req.session.user!.id;`
- `if (Number.isNaN(requestedUserId)) → 400 { success: false, error: 'Invalid userId' }`
- `if (!isAdmin && requestedUserId !== req.session.user!.id) → 403 { success: false, error: 'Forbidden' }`
  (D6 — ein Mitarbeiter darf keine fremden Perioden lesen)
- Antwort `res.json({ success: true, data: getWorkPeriods(requestedUserId) })` — die Reihenfolge
  liefert der Service; die absteigende Sortierung fuer die Anzeige macht der Desktop.
- `catch`: `logger.error({ err: error }, 'Failed to load work periods')` und
  `500 { success: false, error: 'Failed to load work periods' }`. Keine Personendaten ins Log —
  nur `userId` als Zahl, kein Name, keine E-Mail.

Die Datei traegt einen Kopfkommentar, der festhaelt: dieser Router bekommt in Plan 12-05 zwei
weitere Endpunkte (`POST /preview`, `POST /change`); der Ablageort ist bewusst ein gemeinsamer
Router, damit `/api/work-periods` eine Adresse hat.

In `server/src/server.ts` den Import ergaenzen und `app.use('/api/work-periods',
workPeriodsRoutes);` in die bestehende Registrierungsliste einfuegen — unmittelbar nach
`app.use('/api/work-time-accounts', workTimeAccountsRoutes);`, damit verwandte Adressen
beieinanderstehen.
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit && grep -c "api/work-periods" src/server.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "app.use('/api/work-periods'" server/src/server.ts` liefert 1
    - `grep -c "requireAuth" server/src/routes/workPeriods.ts` liefert mindestens 1
    - `grep -c "403" server/src/routes/workPeriods.ts` liefert mindestens 1
    - `grep -c "getWorkPeriods" server/src/routes/workPeriods.ts` liefert mindestens 1
    - `grep -c "console\." server/src/routes/workPeriods.ts` liefert 0
    - `cd server && npx tsc --noEmit` endet mit Exitcode 0
    - Gegen den lokalen Dev-Server: `GET /api/work-periods?userId=<fremde-id>` mit einer Session der Rolle `employee` antwortet mit HTTP 403; dieselbe Anfrage mit Admin-Session antwortet mit HTTP 200 und einem Array; `GET /api/work-periods` ohne Parameter mit Employee-Session liefert 200 und ausschliesslich Perioden mit der eigenen `userId`. Die drei Aufrufe und ihre Antwortcodes werden im SUMMARY woertlich protokolliert.
  </acceptance_criteria>
  <done>`/api/work-periods` ist registriert, liefert fuer Admins beliebige und fuer Mitarbeiter ausschliesslich eigene Perioden, und weist fremde Zugriffe mit 403 ab.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Desktop-Client → `/api/work-periods` | Ungeprueftes `userId` aus der Query eines beliebigen angemeldeten Nutzers |
| Migrationslauf → Produktionsdatenbank | Tabellen-Neubau mit DROP/RENAME auf einer Tabelle mit Buchungshistorie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | `GET /api/work-periods` | mitigate | Rollenpruefung in der Route: Nicht-Admin darf ausschliesslich die eigene `userId` lesen, sonst 403 (D6). Belegt durch den Drei-Aufruf-Nachweis in den Acceptance Criteria von Task 3. |
| T-12-02 | Tampering | `req.query.userId` | mitigate | `parseInt` + `Number.isNaN`-Pruefung vor jeder Verwendung; Weitergabe ausschliesslich als gebundener Parameter an das Prepared Statement in `getWorkPeriods()`. Kein String-Concat in SQL. |
| T-12-03 | Denial of Service | Migration 011 | mitigate | Zeilenzahlvergleich vor DROP; Migration laeuft synchron in der `db.transaction()` des `migrationRunner` — bei Abweichung Rollback statt Datenverlust. Erprobung ausschliesslich gegen eine Arbeitskopie (`/tmp/12-01-probe.db`), nie gegen Produktion. |
| T-12-04 | Information Disclosure | Fehlerpfad der Route | mitigate | `logger.error({ err })` mit `userId` als Zahl; keine Namen, E-Mail-Adressen oder Stammdaten im Log. |
| T-12-05 | Elevation of Privilege | Kuenftige Schreibendpunkte auf demselben Router | mitigate | Kopfkommentar der Datei haelt fest, dass die in 12-05 folgenden `POST`-Endpunkte zusaetzlich `requireAdmin` tragen muessen — `requireAuth` allein genuegt dort nicht. |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | Nicht anwendbar: dieser Plan installiert kein Paket. Die UI-SPEC haelt fest: "Neue Abhaengigkeiten: keine". Wird in einem Folgeschritt doch ein Paket noetig, gilt der Legitimitaets-Gate mit blockierender Pruefung. |
</threat_model>

<verification>
1. `cd server && npx tsc --noEmit` — Exitcode 0.
2. `cd server && npm test` — keine neuen roten Tests gegenueber dem Ausgangsstand; der Ausgangsstand ist vor Beginn einmal zu messen und im SUMMARY zu nennen.
3. Migration gegen eine Arbeitskopie der Entwicklungsdatenbank, Zeilenzahl vor/nach identisch, Probe-INSERT mit `model_change` akzeptiert.
4. Die drei HTTP-Aufrufe aus Task 3 (Employee fremd → 403, Admin fremd → 200, Employee eigen → 200) protokolliert.
</verification>

<success_criteria>
- Migration 011 und `schema.ts` tragen `'model_change'` und `'work_period'`; beide Wege erzeugen denselben Constraint.
- Die vier Vertragstypen sind exportiert; `TransactionParams` akzeptiert die neue Buchung.
- `GET /api/work-periods` ist erreichbar und setzt D6 durch.
- Kein `any`, kein `console.*` in neuem Code, `npx tsc --noEmit` gruen.
</success_criteria>

<output>
Erstelle `.planning/phases/12-stundenwechsel-bedienen/12-01-SUMMARY.md`.

Pflichtabschnitte:
- "Beobachtete Altlasten" — u. a. die fehlenden Spalten `balanceBefore`/`balanceAfter` im
  `CREATE TABLE overtime_transactions` von `schema.ts` (vorbestehend, bewusst nicht behoben).
- "UAT-Punkte fuer Phase 14" — nummerierte Liste, je Punkt: was zu pruefen ist, wo, mit welchem
  erwarteten Ergebnis. Mindestens:
  1. Migration 011 auf der Produktionskopie: Zeilenzahl `overtime_transactions` vor und nach dem
     Lauf identisch, `PRAGMA integrity_check` liefert `ok`.
  2. `GET /api/work-periods` mit einer echten Mitarbeiter-Session gegen eine fremde `userId`:
     erwartet HTTP 403, keine Perioden im Antwortkoerper.
</output>
