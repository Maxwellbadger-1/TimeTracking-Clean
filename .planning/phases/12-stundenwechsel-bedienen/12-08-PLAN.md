---
phase: 12-stundenwechsel-bedienen
plan: 08
type: execute
wave: 3
depends_on: ["12-04"]
files_modified:
  - desktop/src/utils/timeUtils.ts
  - desktop/src/utils/index.ts
  - desktop/src/components/absences/AbsenceRequestForm.tsx
  - desktop/src/components/worktime/WorkScheduleDisplay.tsx
autonomous: true
requirements: [REQ-26]

must_haves:
  truths:
    - "WR-12 ist geschlossen: die Desktop-Vorschau im Abwesenheitsantrag rechnet periodengetreu und ueberspringt Wochenenden und Feiertage — dieselben drei Regeln wie der Server"
    - "Die Intervalllogik im Client ist zeichengleich zu resolveWorkPeriodIn: validFrom <= date && (validTo === null || validTo > date) — es entsteht keine zweite Auslegung derselben Regel"
    - "WorkScheduleDisplay zeigt sichtbar, auf welchen Stichtag sich die angezeigten Werte beziehen, statt stillschweigend den heutigen Stammdatensatz zu behaupten"
    - "Die drei Messzahlen aus dem Wirksamkeitsfenster der 11-DESKTOP-DISPOSITION.md sind erneut erhoben; mindestens eine ist nach einem eingetragenen Wechsel nicht mehr null"
  artifacts:
    - path: "desktop/src/utils/timeUtils.ts"
      provides: "periodengetreue calculateAbsenceHoursWithWorkSchedule und countWorkingDaysForUser"
      contains: "validTo"
    - path: "desktop/src/components/absences/AbsenceRequestForm.tsx"
      provides: "Aufrufstelle mit Perioden und Feiertagen"
      contains: "useWorkPeriods"
  key_links:
    - from: "desktop/src/components/absences/AbsenceRequestForm.tsx"
      to: "desktop/src/hooks/useWorkTimeChange.ts"
      via: "useWorkPeriods(selectedUserId)"
      pattern: "useWorkPeriods"
    - from: "desktop/src/utils/timeUtils.ts"
      to: "server/src/services/workPeriodService.ts"
      via: "zeichengleiche Intervalllogik zu resolveWorkPeriodIn"
      pattern: "validTo"
---

<objective>
Der Nachzug des Desktops auf den periodengetreuen Massstab — der Punkt, den Phase 11 bewusst
ausgelassen und schriftlich an Phase 12 uebergeben hat.

Zweck: `11-REVIEW.md` Befund **WR-12**: `desktop/src/utils/timeUtils.ts:213` rechnet mit dem
heutigen Stammdatensatz und kennt keine Feiertage. Fuer eine Abwesenheit ueber einen Stichtag
zeigt das Formular 40 h an, waehrend der Server 28 h bucht. Der Nutzer sieht eine Zahl, die
nicht gilt. `11-DESKTOP-DISPOSITION.md` nennt als Voraussetzung eine Perioden-Lese-API — sie
entsteht in Plan 12-01 und traegt diesen Nachzug.

Ausgabe: periodengetreue Berechnungsfunktionen im Desktop, umgehaengte Aufrufstelle, ehrliche
Anzeige im `WorkScheduleDisplay`, und die erneut erhobenen drei Messzahlen.

**Gewaehlter Weg (Entscheidung dieser Planung).** WR-12 nennt zwei Moeglichkeiten: (a) die
Vorschau vom Server holen oder (b) die Client-Kopie periodengetreu machen. Gewaehlt ist **(b)**.
Begruendung: Fuer (a) gaebe es heute keinen Endpunkt — die Anreicherung `calculatedHours` in
`getAbsenceRequestsPaginated()` gilt fuer **bestehende** Antraege, nicht fuer einen noch nicht
angelegten; (a) hiesse also, in dieser Phase einen weiteren Endpunkt samt Entprellung zu bauen,
waehrend die Perioden-Lese-API bereits da ist. Der Preis von (b) ist das bekannte
Dual-Calculation-Risiko; er wird bezahlt, indem die Intervalllogik zeichengleich zu
`resolveWorkPeriodIn()` uebernommen und durch einen Test festgenagelt wird. Die vollstaendige
Aufloesung der Client-Kopie (WR-12 "mittelfristig") bleibt ausserhalb dieser Phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/11-datumsabh-ngige-berechnung/11-DESKTOP-DISPOSITION.md
@.planning/phases/12-stundenwechsel-bedienen/12-PATTERNS.md
@.planning/phases/12-stundenwechsel-bedienen/12-04-SUMMARY.md

<interfaces>
Server-Gegenstueck, an dem sich der Umbau ausrichtet:
```typescript
// server/src/utils/workingDays.ts:240
export function calculateAbsenceHoursWithWorkSchedule(
  user: UserPublic, startDate: string, endDate: string, periods: WorkPeriodContext
): number;
// Regeln je Tag, in dieser Reihenfolge:
//   1. Wochenende (Sonntag=0, Samstag=6) -> uebersprungen
//   2. Feiertag (Treffer in holidays) -> uebersprungen
//   3. Periode zum Datum aufloesen; keine Periode -> 0 Stunden, kein Fehler
//   4. period.workSchedule ? workSchedule[dayName] : Math.round((period.weeklyHours/5)*100)/100

// server/src/services/workPeriodService.ts:241-251 — die eine Auslegung des Intervalls
period.validFrom <= date && (period.validTo === null || period.validTo > date)
```

Heutige Desktop-Signatur (`desktop/src/utils/timeUtils.ts:213`):
```typescript
export function calculateAbsenceHoursWithWorkSchedule(
  startDate: string, endDate: string,
  workSchedule: WorkSchedule | null | undefined, weeklyHours: number
): number;
```

Aus Plan 12-04: `useWorkPeriods(userId)` liefert `WorkTimePeriod[]`.
Bereits vorhanden: `useHolidays(year)` und `useMultiYearHolidays()` in
`desktop/src/hooks/useHolidays.ts`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: timeUtils periodengetreu machen — Perioden, Wochenenden, Feiertage</name>
  <files>desktop/src/utils/timeUtils.ts, desktop/src/utils/index.ts, desktop/src/utils/timeUtils.periods.test.ts</files>
  <read_first>
    - `desktop/src/utils/timeUtils.ts` Zeile 195-300 — die heutige `calculateAbsenceHoursWithWorkSchedule` (Zeile 213-247) und `countWorkingDaysForUser`; beide iterieren mit `new Date(startDate)` (UTC-Mitternacht) und wenden fuer jeden Tag denselben, uebergebenen Stammdatensatz an.
    - `server/src/utils/workingDays.ts` Zeile 240-285 — das Gegenstueck, an dem sich der Umbau ausrichtet: Reihenfolge Wochenende → Feiertag → Periode → Stunden, Parsing ueber `+ 'T12:00:00'`, Rundung `Math.round((weeklyHours/5)*100)/100`.
    - `server/src/services/workPeriodService.ts` Zeile 241-251 — `resolveWorkPeriodIn`; die Intervallbedingung wird zeichengleich uebernommen.
    - `desktop/src/utils/index.ts` — der Re-Export, der mitgeaendert werden muss.
    - `.planning/phases/11-datumsabh-ngige-berechnung/11-DESKTOP-DISPOSITION.md`, Abschnitt "Die vier Desktop-Dateien einzeln", Punkt 1 und 2.
  </read_first>
  <behavior>
    - Ein Zeitraum vollstaendig innerhalb einer Periode mit 40 h/Woche und ohne Tagesplan liefert 8 h je Werktag.
    - Ein Zeitraum, der ueber einen Stichtag laeuft (40 h davor, 20 h danach), liefert fuer die Tage vor dem Stichtag 8 h und ab dem Stichtag 4 h — `validFrom` ist inklusiv.
    - `validTo` ist exklusiv: der Tag, der gleich `validTo` ist, gehoert bereits zur Folgeperiode.
    - Samstage und Sonntage liefern 0 h, auch wenn ein Tagesplan dort Stunden vorsieht — wie im Server-Gegenstueck.
    - Ein Datum, das in der Feiertagsliste steht, liefert 0 h, auch wenn es ein Werktag ist.
    - Ein Datum ohne passende Periode liefert 0 h und wirft nicht.
    - Eine leere Periodenliste liefert fuer den gesamten Zeitraum 0 h und wirft nicht.
    - `countWorkingDaysForUser` zaehlt genau die Tage, die nach denselben Regeln mehr als 0 h liefern.
    - Kein Ergebnis haengt von der Zeitzone des Rechners ab: derselbe Aufruf liefert bei gesetztem `TZ=UTC` und bei `TZ=Europe/Berlin` denselben Wert.
  </behavior>
  <action>
`desktop/src/utils/timeUtils.ts` umbauen.

Neue, exportierte Hilfsfunktion `resolveWorkTimePeriodIn(periods: WorkTimePeriod[], date:
string): WorkTimePeriod | null`. Ihre Bedingung ist **zeichengleich** zu
`resolveWorkPeriodIn()` im Server: `period.validFrom <= date && (period.validTo === null ||
period.validTo > date)`. Ein Kommentar direkt darueber nennt die Quelle
(`server/src/services/workPeriodService.ts:241-251`) und haelt fest, dass jede Abweichung ein
Dual-Calculation-Fehler waere.

`calculateAbsenceHoursWithWorkSchedule` bekommt die neue Signatur
`(startDate: string, endDate: string, periods: WorkTimePeriod[], holidayDates: ReadonlySet<string>)
: number`. Der Ablauf je Tag folgt dem Server in genau dieser Reihenfolge:
1. Wochenende (`getDay() === 0 || === 6`) → uebersprungen.
2. Datum in `holidayDates` → uebersprungen.
3. `resolveWorkTimePeriodIn(periods, dateStr)`; keine Periode → 0 Stunden fuer diesen Tag, kein
   Fehler.
4. `period.workSchedule ? (period.workSchedule[dayName] || 0) :
   Math.round((period.weeklyHours / 5) * 100) / 100`.
Ergebnis am Ende `Math.round(total * 100) / 100`.

Datumsbehandlung: Iteration ueber `new Date(startDate + 'T12:00:00')` bis
`new Date(endDate + 'T12:00:00')` mit `setDate(+1)`; die Tageszeichenkette wird aus
`getFullYear()`, `getMonth()+1`, `getDate()` mit fuehrenden Nullen gebildet.
`toISOString().split('T')[0]` ist verboten — es ist genau der Fehler, den `.claude/CLAUDE.md`
benennt, und die heutige Fassung nutzt mit `new Date(startDate)` bereits die unsichere Variante.

`countWorkingDaysForUser` bekommt dieselbe neue Signatur und zaehlt die Tage, die nach denselben
Regeln mehr als 0 Stunden liefern.

`desktop/src/utils/index.ts`: die beiden Namen bleiben im Re-Export, `resolveWorkTimePeriodIn`
kommt hinzu.

Der Kopfkommentar der beiden Funktionen haelt fest: sie sind die Client-Kopie der Server-Regel,
ihre vollstaendige Aufloesung ist WR-12 "mittelfristig" und ausserhalb dieser Phase; wer sie
aendert, muss das Server-Gegenstueck danebenlegen.

Lege `desktop/src/utils/timeUtils.periods.test.ts` an, die die neun Punkte aus `<behavior>` je
als eigenen Testfall prueft. Testlaeufer wie in Plan 12-02 Task 1 ermittelt; der Zeitzonentest
laeuft ueber zwei Laeufe mit gesetztem `process.env.TZ` bzw. ueber die vom Laeufer angebotene
Zeitzonensteuerung.
  </action>
  <verify>
    <automated>cd desktop && npx tsc --noEmit && (npm test -- timeUtils.periods 2>/dev/null || npx tsx src/utils/timeUtils.periods.test.ts)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function resolveWorkTimePeriodIn" desktop/src/utils/timeUtils.ts` liefert 1
    - Die Zeichenkette `period.validFrom <= date && (period.validTo === null || period.validTo > date)` kommt in `desktop/src/utils/timeUtils.ts` genau einmal vor — zeichengleich zum Server
    - `grep -c "workPeriodService.ts:241" desktop/src/utils/timeUtils.ts` liefert 1 (Quellenangabe im Kommentar)
    - `grep -c "holidayDates" desktop/src/utils/timeUtils.ts` liefert mindestens 3
    - `grep -c "T12:00:00" desktop/src/utils/timeUtils.ts` liefert mindestens 1
    - `grep -c "toISOString" desktop/src/utils/timeUtils.ts` liefert 0
    - `grep -c "resolveWorkTimePeriodIn" desktop/src/utils/index.ts` liefert 1
    - Der Testlauf deckt alle neun `<behavior>`-Punkte ab und endet mit Exitcode 0
    - `cd desktop && npx tsc --noEmit` endet mit Exitcode 0 — die Signaturaenderung macht jede nicht nachgezogene Aufrufstelle sichtbar; es darf keine uebrig bleiben
  </acceptance_criteria>
  <done>Die beiden Desktop-Funktionen rechnen periodengetreu und ueberspringen Wochenenden und Feiertage; ihre Intervalllogik ist zeichengleich zum Server und durch neun Testfaelle abgesichert.</done>
</task>

<task type="auto">
  <name>Task 2: Aufrufstellen umhaengen — AbsenceRequestForm und WorkScheduleDisplay</name>
  <files>desktop/src/components/absences/AbsenceRequestForm.tsx, desktop/src/components/worktime/WorkScheduleDisplay.tsx</files>
  <read_first>
    - `desktop/src/components/absences/AbsenceRequestForm.tsx` Zeile 50-95 — der `useEffect`, der beide Berechnungen mit `selectedUser.workSchedule`/`selectedUser.weeklyHours` aufruft, und Zeile 135-150, der Submit-Pfad, der `requiredHours` NICHT mitsendet.
    - `desktop/src/components/worktime/WorkScheduleDisplay.tsx` Zeile 13-70 — `WorkScheduleDisplayProps` ohne Datumsparameter, `hasIndividualSchedule = !!user.workSchedule`, `const dailyHours = user.weeklyHours / 5`.
    - `desktop/src/hooks/useHolidays.ts` Zeile 1-70 — `useHolidays(year)` und `useMultiYearHolidays()`; welche Form die Feiertagsdaten haben und wie daraus ein `Set<string>` mit `YYYY-MM-DD` entsteht.
    - `.planning/phases/11-datumsabh-ngige-berechnung/11-DESKTOP-DISPOSITION.md`, Punkte 2 und 3 — der Befund, dass der Wert im Antragsformular reine UI-Vorschau ohne Persistenzwirkung ist, und dass `WorkScheduleDisplay` heute ausschliesslich den heutigen Stand zeigt.
    - `desktop/src/hooks/useWorkTimeChange.ts` (Plan 12-04) — `useWorkPeriods(userId)`.
  </read_first>
  <action>
**`AbsenceRequestForm.tsx`:**
- `useWorkPeriods(selectedUserId)` einbinden und die Feiertage der betroffenen Jahre ueber den
  vorhandenen Feiertags-Hook laden; daraus ein `Set<string>` mit Datumsschluesseln
  `YYYY-MM-DD` bilden (per `useMemo`, damit es nicht bei jedem Render neu entsteht).
- Die beiden Aufrufe im `useEffect` (heute Zeile 62-79) auf die neue Signatur umstellen:
  `calculateAbsenceHoursWithWorkSchedule(startDate, endDate, periods, holidaySet)` und
  `countWorkingDaysForUser(startDate, endDate, periods, holidaySet)`.
- Solange Perioden oder Feiertage noch laden, wird **keine** Zahl behauptet: die Vorschauzeile
  zeigt stattdessen einen neutralen Ladehinweis. Schlaegt das Laden der Perioden fehl, wird
  ebenfalls keine Zahl gezeigt, sondern der Hinweis, dass die Vorschau gerade nicht berechnet
  werden kann — eine falsche Zahl ist schlechter als keine. Der Antrag selbst bleibt in beiden
  Faellen absendbar, weil der Server den Wert ohnehin selbst berechnet
  (`11-DESKTOP-DISPOSITION.md`, Punkt 2: `requiredHours` geht nicht in den Payload).
- Am Submit-Pfad aendert sich nichts.

**`WorkScheduleDisplay.tsx`:**
- Die Komponente hat keinen Datumsparameter und zeigt ausschliesslich den heutigen Stand
  (belegt in der Disposition, Punkt 3). Ab Phase 12 kann ein Nutzer mehrere Perioden haben; die
  Anzeige bleibt inhaltlich richtig, ist aber ohne Bezugsangabe missverstaendlich.
- Minimale, additive Aenderung: Unter der bestehenden Darstellung erscheint eine Zeile in
  `text-xs text-gray-500 dark:text-gray-400` mit `Aktuell gültiges Modell seit {TT.MM.JJJJ}`,
  wobei das Datum das `validFrom` der heute gueltigen Periode aus `useWorkPeriods(user.id)` ist.
  Sind die Perioden nicht geladen oder gibt es keine, entfaellt die Zeile ersatzlos — es wird
  nichts behauptet, was nicht belegt ist.
- Die bestehende Berechnung `user.weeklyHours / 5` und `!!user.workSchedule` bleibt
  unveraendert: sie beschreibt den heutigen Stand, und der heutige Stand ist laut Phase 11
  identisch mit der heute gueltigen Periode (`userService` spiegelt jede Stammdatenaenderung in
  die offene Periode). Ein Umbau auf Periodenwerte waere hier eine Aenderung ohne
  Wirkungsunterschied und damit unnoetiges Risiko.

Datumsanzeige ueber `new Date(iso + 'T12:00:00').toLocaleDateString('de-DE')`. Kein nacktes
`fetch()`, kein `any`, keine `console.*`, kein `toISOString`.
  </action>
  <verify>
    <automated>cd desktop && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useWorkPeriods" desktop/src/components/absences/AbsenceRequestForm.tsx` liefert mindestens 2 (Import und Verwendung)
    - `calculateAbsenceHoursWithWorkSchedule` und `countWorkingDaysForUser` haben in `AbsenceRequestForm.tsx` je genau einen Aufruf mit vier Argumenten, deren drittes die Periodenliste und deren viertes das Feiertags-Set ist; `selectedUser.workSchedule` kommt in keinem der beiden Aufrufe mehr vor
    - `grep -ci "holiday" desktop/src/components/absences/AbsenceRequestForm.tsx` liefert mindestens 2
    - `grep -c "useMemo" desktop/src/components/absences/AbsenceRequestForm.tsx` liefert mindestens 1
    - `grep -c "useWorkPeriods" desktop/src/components/worktime/WorkScheduleDisplay.tsx` liefert mindestens 2
    - `grep -c "Aktuell gültiges Modell seit" desktop/src/components/worktime/WorkScheduleDisplay.tsx` liefert 1
    - `grep -c "T12:00:00" desktop/src/components/worktime/WorkScheduleDisplay.tsx` liefert mindestens 1
    - `grep -c "toISOString" desktop/src/components/absences/AbsenceRequestForm.tsx desktop/src/components/worktime/WorkScheduleDisplay.tsx` liefert 0 fuer beide Dateien
    - `grep -cE "\bfetch\(" desktop/src/components/absences/AbsenceRequestForm.tsx desktop/src/components/worktime/WorkScheduleDisplay.tsx` liefert 0 fuer beide Dateien
    - `grep -c "console\." desktop/src/components/absences/AbsenceRequestForm.tsx desktop/src/components/worktime/WorkScheduleDisplay.tsx` liefert 0 fuer beide Dateien
    - `grep -rn "calculateAbsenceHoursWithWorkSchedule\|countWorkingDaysForUser" desktop/src --include=*.tsx --include=*.ts` zeigt ausschliesslich Aufrufe mit der neuen Signatur; kein Aufrufer bleibt auf der alten
    - `cd desktop && npx tsc --noEmit` endet mit Exitcode 0
  </acceptance_criteria>
  <done>Die Antragsvorschau rechnet periodengetreu oder behauptet gar nichts; die Modellanzeige nennt den Stichtag, auf den sie sich bezieht.</done>
</task>

<task type="auto">
  <name>Task 3: Wirksamkeitsfenster erneut messen — die drei Zahlen aus der Desktop-Disposition</name>
  <files>server/src/scripts/verifyDesktopEffectiveness.ts</files>
  <read_first>
    - `.planning/phases/11-datumsabh-ngige-berechnung/11-DESKTOP-DISPOSITION.md`, Abschnitt "Begruendung 2: Wirksamkeitsfenster" — dort steht der vollstaendige, woertliche Messbefehl (drei Abfragen: Nutzer mit mehr als einer Periode, Drift zwischen `users` und offener Periode, Nutzer ohne offene Periode) samt der damaligen Ausgabe (0 / 0 / 0) und dem Unversehrtheitsnachweis (Dateigroesse und Zeitstempel vor und nach der Messung).
    - `.planning/phases/11-datumsabh-ngige-berechnung/11-DESKTOP-DISPOSITION.md`, Abschnitte "Ab wann traegt Begruendung 2 nicht mehr" und "Uebergabe an Phase 12" — die Disposition verlangt die erneute Pruefung ausdruecklich und sagt voraus, dass mindestens eine Zahl nicht mehr null sein wird.
    - `server/src/scripts/verifyPeriodNullEffect.ts` — das im Projekt etablierte Muster fuer ein rein lesendes Pruefwerkzeug: `readonly: true`, `PRAGMA query_only = ON`, Produktionsschutz, Ausgabe als Klartextbericht.
    - `.claude/CLAUDE.md`, Abschnitt "Database Rules" — `DATABASE_PATH` ist bei jedem Skriptlauf ausdruecklich zu setzen; ohne die Variable entsteht eine leere Datenbank.
  </read_first>
  <action>
Lege `server/src/scripts/verifyDesktopEffectiveness.ts` an — ein rein lesendes Pruefwerkzeug,
das die drei Messzahlen aus `11-DESKTOP-DISPOSITION.md` erhebt und als Klartextbericht ausgibt:
1. Nutzer mit mehr als einer Periode (`SELECT userId, COUNT(*) c FROM user_work_periods GROUP BY
   userId HAVING COUNT(*) > 1`).
2. Drift zwischen `users.weeklyHours`/`users.workSchedule` und der offenen Periode.
3. Nutzer ohne offene Periode.
Zusaetzlich die Gesamtzahlen `users` und `user_work_periods`, wie in der Disposition.

Das Werkzeug oeffnet die Datenbank mit `readonly: true` und setzt zusaetzlich
`PRAGMA query_only = ON` (doppelte Absicherung, Muster aus `verifyPeriodNullEffect.ts`). Es
uebernimmt den Produktionsschutz-Guard aus `verifyPeriodNullEffect.ts` unveraendert: laeuft der
Pfad auf die Produktionsdatenbank, bricht es ab. Vor und nach der Messung gibt es Dateigroesse
und Zeitstempel der geoeffneten Datei aus (Unversehrtheitsnachweis). Kein Schreibzugriff, keine
`console.log`-Debugausgaben — die Berichtsausgabe laeuft ueber `process.stdout.write` oder
`logger.info`.

Ablauf der Messung selbst:
1. Arbeitskopie ziehen: `cp server/database.db server/database/12-08-wirksamkeit.db`. Niemals
   gegen die Produktionsdatenbank messen.
2. Erster Lauf: `DATABASE_PATH=./database/12-08-wirksamkeit.db npx tsx
   src/scripts/verifyDesktopEffectiveness.ts` aus `server/`. Ausgabe woertlich festhalten.
3. Ist Zahl 1 zu diesem Zeitpunkt noch 0 (weil in dieser Arbeitskopie noch kein Wechsel
   eingetragen wurde), wird gegen dieselbe Arbeitskopie ein Wechsel eingetragen — ueber
   `applyWorkTimeChange` mit `dryRun: false` und gesetztem
   `DATABASE_PATH=./database/12-08-wirksamkeit.db` — und der Lauf wiederholt. Danach MUSS
   Zahl 1 groesser als 0 sein.
4. Bleibt Zahl 1 auch nach einem eingetragenen Wechsel 0, ist das ein **blockierender Befund**:
   dann schreibt der Wechsel keine zweite Periode, und die Phase erfuellt REQ-26 nicht. In
   diesem Fall wird der Befund gemeldet statt weitergearbeitet.
5. Beide Laeufe, der Unversehrtheitsnachweis und die Bewertung ("Begruendung 2 der Disposition
   traegt nicht mehr — der Nachzug war faellig und ist erfolgt") kommen woertlich ins SUMMARY.
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit && cp database.db database/12-08-wirksamkeit.db && DATABASE_PATH=./database/12-08-wirksamkeit.db npx tsx src/scripts/verifyDesktopEffectiveness.ts</automated>
  </verify>
  <acceptance_criteria>
    - `server/src/scripts/verifyDesktopEffectiveness.ts` existiert und laeuft mit Exitcode 0 gegen die Arbeitskopie
    - `grep -c "query_only" server/src/scripts/verifyDesktopEffectiveness.ts` liefert mindestens 1
    - `grep -c "readonly: true" server/src/scripts/verifyDesktopEffectiveness.ts` liefert mindestens 1
    - `grep -c "console.log" server/src/scripts/verifyDesktopEffectiveness.ts` liefert 0
    - Die Ausgabe nennt alle drei Zahlen und die beiden Gesamtzahlen im selben Format wie `11-DESKTOP-DISPOSITION.md`
    - Nach dem eingetragenen Wechsel gegen die Arbeitskopie ist Zahl 1 ("Nutzer mit >1 Periode") groesser als 0; die beiden Ausgaben vor und nach dem Wechsel stehen woertlich im SUMMARY
    - Dateigroesse und Zeitstempel der Arbeitskopie sind vor und nach dem reinen Messlauf identisch (Unversehrtheitsnachweis im SUMMARY)
    - `cd server && npx tsc --noEmit` endet mit Exitcode 0
  </acceptance_criteria>
  <done>Die drei Messzahlen sind erneut erhoben, der Nachweis liegt schriftlich vor, und die Aussage der Disposition ist fortgeschrieben statt behauptet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Serverregel → Client-Kopie | Dieselbe fachliche Regel existiert an zwei Orten |
| Pruefwerkzeug → Datenbankdatei | Ein Skript oeffnet eine Datenbank; ohne Schutz koennte es die Produktion treffen |
| Vorschauzahl → Erwartung des Nutzers | Eine angezeigte Stundenzahl wird als verbindlich gelesen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-39 | Tampering | Zweite Auslegung der Intervallregel | mitigate | Die Bedingung wird zeichengleich aus `resolveWorkPeriodIn()` uebernommen, mit Quellenangabe im Kommentar und mit einem Testfall zu `validFrom` inklusiv / `validTo` exklusiv. Eine Abweichung ist damit sichtbar, nicht still. |
| T-12-40 | Repudiation | Falsche Vorschauzahl im Antragsformular | mitigate | Solange Perioden oder Feiertage nicht geladen sind, wird gar keine Zahl gezeigt. Der gebuchte Wert entsteht ohnehin serverseitig (`requiredHours` geht nicht in den Payload) — der Client kann keine falsche Buchung ausloesen, nur eine falsche Erwartung wecken, und genau die wird hier vermieden. |
| T-12-41 | Denial of Service | Messwerkzeug gegen die Produktionsdatenbank | mitigate | Produktionsschutz-Guard aus `verifyPeriodNullEffect.ts` unveraendert uebernommen, `readonly: true` plus `PRAGMA query_only = ON`, Messung ausschliesslich gegen eine Arbeitskopie, Unversehrtheitsnachweis ueber Groesse und Zeitstempel. |
| T-12-42 | Information Disclosure | Ausgabe des Messwerkzeugs | mitigate | Der Bericht nennt ausschliesslich Nutzer-Ids und Zahlen — keine Namen, keine E-Mail-Adressen, keine Stundenwerte einzelner Personen im Klartextprotokoll. |
| T-12-43 | Spoofing | Datenzugriffe der geaenderten Komponenten | mitigate | Ausschliesslich ueber die vorhandenen Hooks, die `apiClient`/`universalFetch` nutzen; `grep -cE "\bfetch\("` = 0 in beiden Dateien. |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | Nicht anwendbar: keine neue Abhaengigkeit. |
</threat_model>

<verification>
1. `cd desktop && npx tsc --noEmit` — Exitcode 0; die Signaturaenderung deckt jede nicht nachgezogene Aufrufstelle auf.
2. Der Testlauf zu `timeUtils.periods` deckt neun Verhaltensregeln ab und ist gruen.
3. `grep -rn "calculateAbsenceHoursWithWorkSchedule" desktop/src` zeigt genau die Definition, den Re-Export, den Test und die eine Aufrufstelle in `AbsenceRequestForm.tsx` — keinen weiteren Aufrufer auf der alten Signatur.
4. Die drei Messzahlen liegen in zwei Laeufen (vor und nach einem eingetragenen Wechsel) woertlich im SUMMARY, samt Unversehrtheitsnachweis.
5. Gegenprobe zum WR-12-Szenario, protokolliert im SUMMARY: Fuer einen Testnutzer mit einem Stichtag mitten in einem Abwesenheitszeitraum liefert die Desktop-Vorschau denselben Stundenwert wie die Server-Funktion `calculateAbsenceHoursWithWorkSchedule` fuer denselben Zeitraum. Beide Zahlen werden genannt.
</verification>

<success_criteria>
- WR-12 ist geschlossen: die Desktop-Vorschau rechnet periodengetreu, ueberspringt Wochenenden und Feiertage und stimmt im Gegenprobefall mit dem Server ueberein.
- Die Intervalllogik existiert im Client in genau einer, zeichengleich uebernommenen Fassung.
- `WorkScheduleDisplay` nennt den Stichtag, auf den sich seine Anzeige bezieht.
- Die drei Messzahlen der Desktop-Disposition sind erneut erhoben, mindestens eine ist nicht mehr null.
- Kein `any`, kein nacktes `fetch()`, kein `toISOString`, `npx tsc --noEmit` gruen.
</success_criteria>

<output>
Erstelle `.planning/phases/12-stundenwechsel-bedienen/12-08-SUMMARY.md`.

Pflichtabschnitte:
- "Gewaehlter Weg fuer WR-12" — Weg (b), mit der Begruendung und mit dem ausdruecklichen
  Vermerk, dass die vollstaendige Aufloesung der Client-Kopie ("mittelfristig" laut WR-12)
  weiterhin offen ist und als eigener Punkt gefuehrt wird.
- "Wirksamkeitsfenster nach Phase 12" — beide Messlaeufe woertlich, Unversehrtheitsnachweis,
  Bewertung.
- "Gegenprobe Desktop gegen Server" — die beiden Stundenwerte des WR-12-Szenarios.
- "UAT-Punkte fuer Phase 14" — nummerierte Liste, je Punkt: was zu pruefen ist, wo, mit welchem
  erwarteten Ergebnis. Mindestens:
  1. Abwesenheitsantrag ueber einen Stichtag hinweg im Desktop stellen: die angezeigte
     Stundenzahl entspricht der, die nach der Genehmigung tatsaechlich gebucht wird.
  2. Abwesenheitsantrag ueber einen Feiertag: der Feiertag wird in der Vorschau nicht als
     Arbeitstag gezaehlt.
  3. Server waehrend des Ausfuellens nicht erreichbar: es erscheint keine Zahl, sondern der
     Hinweis, dass die Vorschau nicht berechnet werden kann; der Antrag bleibt absendbar.
  4. `WorkScheduleDisplay` bei einem Nutzer mit mehreren Perioden: die Zeile
     "Aktuell gültiges Modell seit …" nennt das `validFrom` der heute gueltigen Periode.
</output>
