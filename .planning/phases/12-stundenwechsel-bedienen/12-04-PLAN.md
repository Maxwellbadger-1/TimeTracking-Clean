---
phase: 12-stundenwechsel-bedienen
plan: 04
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - desktop/src/types/index.ts
  - desktop/src/hooks/useWorkTimeChange.ts
  - desktop/src/components/worktime/WorkTimePeriodList.tsx
autonomous: true
requirements: [REQ-26, REQ-27]

must_haves:
  truths:
    - "Der Desktop kann die Arbeitszeitperioden eines Nutzers lesen — die Voraussetzung, die 11-DESKTOP-DISPOSITION.md als fehlend benannt hat"
    - "D2: Die Vorschauwerte kommen unveraendert vom Server; die Hook-Schicht rechnet keine Zwischensumme und keine Differenz selbst"
    - "Jeder Serverzugriff laeuft ueber apiClient (der intern universalFetch nutzt); nacktes fetch() kommt in keiner neuen Datei vor"
    - "Die Periodenliste ist reine Anzeige und blockiert das Speichern nie — bei einem Ladefehler bietet sie 'Perioden erneut laden' an (Zustand 2)"
  artifacts:
    - path: "desktop/src/hooks/useWorkTimeChange.ts"
      provides: "useWorkPeriods, usePreviewWorkTimeChange, useSaveWorkTimeChange"
      exports: ["useWorkPeriods", "usePreviewWorkTimeChange", "useSaveWorkTimeChange"]
    - path: "desktop/src/components/worktime/WorkTimePeriodList.tsx"
      provides: "Tabellarische Anzeige der Perioden mit Lade-, Fehler- und Leerzustand"
      exports: ["WorkTimePeriodList"]
    - path: "desktop/src/types/index.ts"
      provides: "WorkTimePeriod, WorkTimeChangeInput, WorkTimeChangePreview"
      contains: "WorkTimeChangePreview"
  key_links:
    - from: "desktop/src/hooks/useWorkTimeChange.ts"
      to: "/api/work-periods"
      via: "apiClient.get / apiClient.post"
      pattern: "work-periods"
    - from: "desktop/src/components/worktime/WorkTimePeriodList.tsx"
      to: "desktop/src/hooks/useWorkTimeChange.ts"
      via: "useWorkPeriods(userId)"
      pattern: "useWorkPeriods"
---

<objective>
Die Datenschicht des Desktops fuer den Stundenwechsel: Typen, Hooks und die Periodenliste.

Zweck: Ohne diese Schicht hat weder der Wechsel-Dialog (Plan 12-06) noch der WR-12-Nachzug
(Plan 12-08) einen Weg an die Perioden. Die Trennung in einen eigenen Plan haelt den Dialog
selbst klein genug, um in einem Zug sauber gebaut zu werden.
Ausgabe: Vertragstypen im Desktop, `useWorkTimeChange.ts`, `WorkTimePeriodList.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/12-stundenwechsel-bedienen/12-UI-SPEC.md
@.planning/phases/12-stundenwechsel-bedienen/12-PATTERNS.md
@.planning/phases/12-stundenwechsel-bedienen/12-01-SUMMARY.md

<interfaces>
Serververtrag aus Plan 12-01 (`server/src/types/index.ts`) — die Desktop-Typen sind die
spiegelbildliche Kopie derselben Felder:

```typescript
WorkTimeChangeInput   { userId, validFrom, weeklyHours, workSchedule, reason }
WorkTimeChangePreview { userId, validFrom, isRetroactive, rangeStart, rangeEnd,
                        workingDaysInRange, targetHoursBefore, targetHoursAfter,
                        targetHoursDelta, balanceBefore, balanceAfter, balanceDelta,
                        currentPeriod, isNoOp, midMonthEffective, affectedMonths }
WorkTimeChangePreviewResponse = WorkTimeChangePreview & { previewToken: string }
```

Endpunkte (GET existiert nach 12-01, die beiden POST kommen in 12-05):
```
GET  /api/work-periods?userId=<id>      -> ApiResponse<UserWorkPeriod[]>
POST /api/work-periods/preview          -> ApiResponse<WorkTimeChangePreviewResponse>
POST /api/work-periods/change           -> ApiResponse<{ period, preview, transactionId }>
```

Bestehende Bausteine im Desktop:
```typescript
// desktop/src/api/client.ts — nutzt intern universalFetch aus ../lib/tauriHttpClient
apiClient.get<T>(path): Promise<ApiResponse<T>>
apiClient.post<T>(path, body): Promise<ApiResponse<T>>

// desktop/src/utils/timeUtils.ts
export function formatHours(hours: number): string;   // Format H:MMh
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vertragstypen und Hook-Schicht fuer den Stundenwechsel</name>
  <files>desktop/src/types/index.ts, desktop/src/hooks/useWorkTimeChange.ts</files>
  <read_first>
    - `desktop/src/hooks/useVacationBalanceAdmin.ts` Zeile 40-135 — laut 12-PATTERNS.md Abschnitt 12 das Analog: `useQuery` mit `queryKey`, `apiClient.get(...)`, `retry: false`; `useMutation` mit `apiClient.post(...)` und Fehlerwurf bei `!response.success`. Das dortige optimistische Update wird ausdruecklich NICHT uebernommen — es naehme den Saldo vorweg, den der Server erst bestaetigen muss.
    - `desktop/src/hooks/useOvertimeCorrections.ts` Zeile 100-160 — zweites Vorbild fuer eine Schreib-Mutation samt Invalidierung.
    - `desktop/src/hooks/invalidationHelpers.ts` — die im Projekt uebliche Art, nach einer Schreiboperation Queries zu invalidieren.
    - `desktop/src/api/client.ts` Zeile 1-20 — belegt, dass `apiClient` intern `universalFetch` verwendet; ein Aufrufer darf deshalb `apiClient` nutzen und muss `universalFetch` nicht selbst importieren.
    - `desktop/src/types/index.ts` Zeile 1-40 — `WorkSchedule`, `DayName`, `User`; die neuen Typen kommen in dieselbe Datei.
    - `server/src/types/index.ts` (Stand nach Plan 12-01) — die Felder muessen zeichengleich gespiegelt werden.
  </read_first>
  <action>
In `desktop/src/types/index.ts` ergaenzen und exportieren:
`WorkTimePeriod` (Spiegel von `UserWorkPeriod`: `id`, `userId`, `validFrom`, `validTo`,
`weeklyHours`, `workSchedule`, `note`, `createdAt`, `createdBy`), `WorkTimeChangeInput`,
`WorkTimeChangePreview`, `WorkTimeChangePreviewResponse` und `WorkTimeChangeResult`
(`{ period: WorkTimePeriod; preview: WorkTimeChangePreview; transactionId: number | null }`).
Feldnamen und Typen zeichengleich zum Serververtrag. Kein `any`.

Lege `desktop/src/hooks/useWorkTimeChange.ts` an mit drei Exporten:

`useWorkPeriods(userId: number | null)` — `useQuery`, `queryKey: ['work-periods', userId]`,
`enabled: userId !== null`, `retry: false`, `queryFn` ruft
`apiClient.get<WorkTimePeriod[]>('/work-periods?userId=' + userId)` und wirft bei
`!response.success` einen `Error` mit `response.error`. Rueckgabe ist das rohe Array; das
Sortieren fuer die Anzeige macht die Liste.

`usePreviewWorkTimeChange()` — `useMutation`, `mutationFn` ruft
`apiClient.post<WorkTimeChangePreviewResponse>('/work-periods/preview', input)` mit
`input: Omit<WorkTimeChangeInput, 'reason'>` (die Begruendung ist fuer die Vorschau nicht
erforderlich und ist auch nicht an das Token gebunden). Wirft bei `!response.success`. **Kein**
`onSuccess`, das Werte umrechnet — die Antwort wird unveraendert durchgereicht (D2).

`useSaveWorkTimeChange()` — `useMutation`, `mutationFn` ruft
`apiClient.post<WorkTimeChangeResult>('/work-periods/change', { ...input, previewToken })`.
Der Parameter ist `WorkTimeChangeInput & { previewToken: string }`. Bei `!response.success`
wird ein `Error` geworfen, dessen `message` die Servermeldung traegt — das Fehlerbanner in
Zustand 13 zeigt sie woertlich an. Lehnt der Server das Token ab, liefert er laut Plan 12-05
den Fehlercode `PREVIEW_STALE`; der Hook reicht diesen Text unveraendert weiter, damit der
Dialog Zustand 14 von Zustand 13 unterscheiden kann.
`onSuccess` invalidiert `['work-periods', userId]`, `['users']` und die Ueberstunden-Queries
(`['overtime']`-Praefix bzw. die im Projekt dafuer etablierten Schluessel aus
`invalidationHelpers.ts`) — sonst zeigen die schreibgeschuetzten Felder im `EditUserModal` nach
dem Speichern noch den alten Wert (Zustand 15).

Kein nacktes `fetch()`, kein `any`, keine `console.*`.
  </action>
  <verify>
    <automated>cd desktop && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function useWorkPeriods" desktop/src/hooks/useWorkTimeChange.ts` liefert 1
    - `grep -c "export function usePreviewWorkTimeChange" desktop/src/hooks/useWorkTimeChange.ts` liefert 1
    - `grep -c "export function useSaveWorkTimeChange" desktop/src/hooks/useWorkTimeChange.ts` liefert 1
    - `grep -cE "\bfetch\(" desktop/src/hooks/useWorkTimeChange.ts` liefert 0
    - `grep -c "apiClient" desktop/src/hooks/useWorkTimeChange.ts` liefert mindestens 3
    - `grep -c "retry: false" desktop/src/hooks/useWorkTimeChange.ts` liefert mindestens 1
    - `grep -c "invalidateQueries" desktop/src/hooks/useWorkTimeChange.ts` liefert mindestens 1
    - `grep -c ": any" desktop/src/hooks/useWorkTimeChange.ts` liefert 0
    - `grep -c "console\." desktop/src/hooks/useWorkTimeChange.ts` liefert 0
    - `grep -c "export interface WorkTimeChangePreview" desktop/src/types/index.ts` liefert 1
    - `grep -c "export interface WorkTimePeriod" desktop/src/types/index.ts` liefert 1
    - `cd desktop && npx tsc --noEmit` endet mit Exitcode 0
  </acceptance_criteria>
  <done>Die drei Hooks existieren, gehen ausschliesslich ueber `apiClient`, rechnen nichts selbst und invalidieren nach dem Speichern die betroffenen Queries.</done>
</task>

<task type="auto">
  <name>Task 2: WorkTimePeriodList.tsx — die Periodenliste als reine Anzeige</name>
  <files>desktop/src/components/worktime/WorkTimePeriodList.tsx</files>
  <read_first>
    - `.planning/phases/12-stundenwechsel-bedienen/12-UI-SPEC.md`, Abschnitt "4. Die Periodenliste" — Spalten, Sortierung, Badges, Zustaende, `renderActions`-Schnitt, ausdrueckliches Verbot einer `Card` und der Accent-Farbe.
    - `.planning/phases/12-stundenwechsel-bedienen/12-UI-SPEC.md`, Zustand 2 und Zustand 10 — die Liste blockiert das Speichern nie; bei Ueberlappung hebt sie, sofern geladen, die betroffene Zeile mit `ring-2 ring-red-400` hervor.
    - `desktop/src/components/worktime/OvertimeTransactions.tsx` Zeile 29-76 (Lade-, Fehler-, Leerzustand) und Zeile 190-270 (Tabellenteil) — das wortgleich zu uebernehmende Muster: `<div className="overflow-x-auto">`, `divide-y divide-gray-200 dark:divide-gray-700`, Hover `hover:bg-gray-50 dark:hover:bg-gray-800/50 transition-colors`, Zellen `px-4 py-3 text-sm`, und Zeile 215 das Datumsmuster `new Date(iso + 'T12:00:00').toLocaleDateString('de-DE')`.
    - `desktop/src/components/ui/Button.tsx` Zeile 25-35 — belegt: es gibt KEIN Default-`type`; jeder Button in diesem Teilbaum traegt ausdruecklich `type="button"`.
    - `desktop/src/components/ui/LoadingSpinner.tsx` — Groessen und Verwendung.
  </read_first>
  <action>
Lege `desktop/src/components/worktime/WorkTimePeriodList.tsx` an. Props:
`{ userId: number; highlightPeriodId?: number | null; renderActions?: (period: WorkTimePeriod)
=> ReactNode }`. Keine `Card` — die Liste steht in einem Modal.

Datenquelle ist `useWorkPeriods(userId)` aus Plan 12-04 Task 1.

Spalten in dieser Reihenfolge: **Gueltig ab** (links, `TT.MM.JJJJ`), **Gueltig bis** (links,
`TT.MM.JJJJ` oder `—` bei offener Periode), **Wochenstunden** (rechtsbuendig, `{X,X} h`),
**Tagesplan** (links, `Standard (5 Tage)` wenn `workSchedule === null`, sonst kompakt
`Mo 8 · Di 8 · Mi 8 · Do 8 · Fr 2`; Tage mit 0 h entfallen). Ist `renderActions` nicht gesetzt,
wird die Aktionsspalte samt `<th>` gar nicht gerendert — keine leere Spalte, kein Platzhalter
(Schnitt fuer Phase 13).

Sortierung: `validFrom` absteigend, im Client, ohne die Serverantwort zu veraendern.

Badges in der Spalte "Gueltig ab": `Aktuell` fuer die heute gueltige Periode (heute liegt in
`[validFrom, validTo)`), `Geplant` fuer Perioden mit `validFrom` in der Zukunft, sonst kein
Badge. **Beide** Badges nutzen dieselbe neutrale Faerbung
`bg-gray-100 text-gray-700 dark:bg-gray-800 dark:text-gray-300` und unterscheiden sich allein
durch den Text — die Liste gehoert ausdruecklich nicht zu den vier freigegebenen
Accent-Einsaetzen.

Zustaende:
- Laden: `LoadingSpinner` zentriert mit `py-8`.
- Fehler: `AlertCircle` (lucide) + `Perioden konnten nicht geladen werden: {Meldung}` in
  red-600/400, darunter `<Button type="button" variant="secondary" size="sm">Perioden erneut
  laden</Button>`, der `refetch()` der Query ausloest.
- Leer: Ueberschrift `Noch kein Stichtag hinterlegt` und darunter der Textbuchsatz
  `Für diesen Mitarbeiter gibt es bisher keine Periode mit Stichtag. Es gelten die
  Stammdatenwerte seit dem Eintrittsdatum. Über „Stundenwechsel ab Datum …" tragen Sie die
  erste Umstellung ein.`

Ist `highlightPeriodId` gesetzt und die Liste geladen, traegt die betroffene Zeile zusaetzlich
`ring-2 ring-red-400` (Zustand 10).

Datumsanzeige ausschliesslich ueber `new Date(iso + 'T12:00:00').toLocaleDateString('de-DE')`.
`toISOString().split('T')[0]` ist verboten. Stundenwerte werden mit deutschem Dezimalkomma
ausgegeben. Dark Mode ist an jeder Flaeche und jedem Text mitzuschreiben. Umbruch ueber
`<div className="overflow-x-auto">`. Kein `any`, keine `console.*`.
  </action>
  <verify>
    <automated>cd desktop && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function WorkTimePeriodList" desktop/src/components/worktime/WorkTimePeriodList.tsx` liefert 1
    - `grep -c "renderActions" desktop/src/components/worktime/WorkTimePeriodList.tsx` liefert mindestens 2
    - `grep -c "overflow-x-auto" desktop/src/components/worktime/WorkTimePeriodList.tsx` liefert 1
    - `grep -c "divide-y divide-gray-200 dark:divide-gray-700" desktop/src/components/worktime/WorkTimePeriodList.tsx` liefert 1
    - `grep -c "px-4 py-3" desktop/src/components/worktime/WorkTimePeriodList.tsx` liefert mindestens 1
    - `grep -c "T12:00:00" desktop/src/components/worktime/WorkTimePeriodList.tsx` liefert mindestens 1
    - `grep -c "toISOString" desktop/src/components/worktime/WorkTimePeriodList.tsx` liefert 0
    - `grep -c "Perioden erneut laden" desktop/src/components/worktime/WorkTimePeriodList.tsx` liefert 1
    - `grep -c "Noch kein Stichtag hinterlegt" desktop/src/components/worktime/WorkTimePeriodList.tsx` liefert 1
    - `grep -c "ring-2 ring-red-400" desktop/src/components/worktime/WorkTimePeriodList.tsx` liefert 1
    - `grep -c "bg-gray-100 text-gray-700 dark:bg-gray-800 dark:text-gray-300" desktop/src/components/worktime/WorkTimePeriodList.tsx` liefert mindestens 1
    - `grep -cE "blue-(50|100|500|600|700)" desktop/src/components/worktime/WorkTimePeriodList.tsx` liefert 0 — die Liste verwendet keine Accent-Farbe
    - `grep -c "<Card" desktop/src/components/worktime/WorkTimePeriodList.tsx` liefert 0
    - `grep -c 'type="button"' desktop/src/components/worktime/WorkTimePeriodList.tsx` entspricht der Anzahl der `<Button`-Vorkommen in der Datei
    - `grep -c ": any" desktop/src/components/worktime/WorkTimePeriodList.tsx` liefert 0
    - `grep -c "console\." desktop/src/components/worktime/WorkTimePeriodList.tsx` liefert 0
    - `cd desktop && npx tsc --noEmit` endet mit Exitcode 0
  </acceptance_criteria>
  <done>Die Periodenliste zeigt vier Spalten, sortiert absteigend, kennt Lade-, Fehler- und Leerzustand mit Wiederholknopf, hebt bei Bedarf eine Zeile hervor und blockiert nichts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Desktop → Server | Jede Anfrage traegt die Session; in Tauri gehen Cookies bei nacktem `fetch()` verloren |
| Serverantwort → Anzeige | Freitextfelder (`note`, Fehlermeldungen) werden in der Oberflaeche dargestellt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-18 | Spoofing | Serverzugriffe der neuen Hooks | mitigate | Ausschliesslich ueber `apiClient`, der intern `universalFetch` aus `../lib/tauriHttpClient` verwendet. Belegt durch `grep -cE "\bfetch\("` = 0 in `useWorkTimeChange.ts`. Ohne das verliert die Anfrage in Tauri die Session und faellt auf einen anderen Nutzerkontext zurueck. |
| T-12-19 | Information Disclosure | Anzeige fremder Perioden | transfer | Die Durchsetzung liegt serverseitig in `GET /api/work-periods` (D6, Plan 12-01). Der Client filtert nicht selbst und darf sich darauf nicht verlassen; er zeigt genau das, was der Server ihm ausliefert. |
| T-12-20 | Tampering | Darstellung von Freitext (`note`, Servermeldung) | mitigate | Ausgabe ausschliesslich als React-Textknoten; kein `dangerouslySetInnerHTML` in den neuen Dateien. Pruefbar mit `grep -c "dangerouslySetInnerHTML"` = 0. |
| T-12-21 | Denial of Service | Wiederholtes Nachladen bei Fehler | mitigate | `retry: false` an der Query; der Wiederholversuch geschieht ausschliesslich auf ausdrueckliche Bedienung ueber den Knopf "Perioden erneut laden". |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | Nicht anwendbar: keine neue Abhaengigkeit. `@tanstack/react-query` und `lucide-react` sind bereits installiert (UI-SPEC, "Neue Abhaengigkeiten: keine"). |
</threat_model>

<verification>
1. `cd desktop && npx tsc --noEmit` — Exitcode 0.
2. `grep -rn "fetch(" desktop/src/hooks/useWorkTimeChange.ts desktop/src/components/worktime/WorkTimePeriodList.tsx` liefert keinen Treffer ausser innerhalb von `refetch`.
3. Gegen den lokalen Dev-Server mit Admin-Session: die Periodenliste eines Nutzers mit genau einer Periode zeigt eine Zeile mit dem Badge "Aktuell" und `—` in der Spalte "Gueltig bis"; die Beobachtung wird im SUMMARY protokolliert.
4. Server gestoppt, Liste erneut geoeffnet: Fehlerzustand mit dem Knopf "Perioden erneut laden" erscheint; nach Serverstart fuehrt ein Klick zum erfolgreichen Nachladen. Protokolliert im SUMMARY.
</verification>

<success_criteria>
- Drei Hooks vorhanden, alle ueber `apiClient`, keine Rechnung im Client (D2).
- Periodenliste mit vier Spalten, absteigender Sortierung, neutralen Badges, drei Zustaenden und `renderActions`-Schnitt.
- Kein `any`, kein nacktes `fetch()`, kein `toISOString`, `npx tsc --noEmit` gruen.
</success_criteria>

<output>
Erstelle `.planning/phases/12-stundenwechsel-bedienen/12-04-SUMMARY.md`.

Pflichtabschnitte:
- "UAT-Punkte fuer Phase 14" — nummerierte Liste, je Punkt: was zu pruefen ist, wo, mit welchem
  erwarteten Ergebnis. Mindestens:
  1. Periodenliste im hellen und im dunklen Modus: Kontrast der Badges und der Tabellentexte
     augenscheinlich mindestens so gut wie im bestehenden Kontoauszug.
  2. Schmal gezogenes Fenster (unter 640 px): die Liste ist horizontal scrollbar, es wird kein
     Inhalt abgeschnitten.
  3. Ein Nutzer mit mehreren Perioden: die juengste Umstellung steht oben, die heute gueltige
     traegt "Aktuell", eine kuenftige traegt "Geplant".
</output>
