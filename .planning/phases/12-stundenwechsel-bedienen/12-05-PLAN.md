---
phase: 12-stundenwechsel-bedienen
plan: 05
type: execute
wave: 3
depends_on: ["12-01", "12-03"]
files_modified:
  - server/src/routes/workPeriods.ts
  - server/src/services/workPeriodChangeService.test.ts
autonomous: true
requirements: [REQ-26, REQ-27, REQ-28, REQ-29]

must_haves:
  truths:
    - "D6: POST /api/work-periods/preview und POST /api/work-periods/change verlangen beide requireAuth UND requireAdmin; ein Mitarbeiter erhaelt 403 und kann keinen Wechsel eintragen"
    - "D2/REQ-27: Ein automatisierter Test vergleicht die Vorschauwerte mit den nach dem Speichern tatsaechlich gemessenen Werten und schlaegt bei jeder Abweichung fehl"
    - "REQ-26: Ein rueckwirkender Stichtag rechnet ab seinem Datum neu und laesst jede Buchung davor unveraendert"
    - "REQ-28: Der angesparte Saldo wird nicht umgerechnet — bei einem Stichtag in der Zukunft aendert sich keine Minute vor dem Stichtag und es entsteht keine Buchung"
    - "REQ-29: Nach einem rueckwirkenden Wechsel steht genau eine Zeile vom Typ model_change mit Begruendung und Periodenbezug im Journal"
    - "Ein Speicherversuch ohne gueltiges previewToken wird abgewiesen; die Vorschau ist damit kein Umgehungsweg fuer die Validierung"
  artifacts:
    - path: "server/src/routes/workPeriods.ts"
      provides: "POST /preview und POST /change mit Rollenpruefung und Tokenpruefung"
      contains: "requireAdmin"
    - path: "server/src/services/workPeriodChangeService.test.ts"
      provides: "Nachweis fuer REQ-26 bis REQ-29 und D2/D3/D4/D7"
      contains: "applyWorkTimeChange"
  key_links:
    - from: "server/src/routes/workPeriods.ts"
      to: "server/src/services/workPeriodChangeService.ts"
      via: "applyWorkTimeChange(input, { dryRun, createdBy })"
      pattern: "applyWorkTimeChange"
    - from: "server/src/routes/workPeriods.ts"
      to: "server/src/services/workTimeChangeToken.ts"
      via: "issuePreviewToken / verifyPreviewToken"
      pattern: "PreviewToken"
---

<objective>
Die beiden Schreibendpunkte und der automatisierte Nachweis, dass die Phase haelt, was die
ROADMAP verspricht.

Zweck: Die vier Erfolgskriterien der Phase sind Behauptungen, solange sie nicht gemessen sind.
Dieser Plan macht aus jedem Kriterium einen Test, der bei Verletzung rot wird — insbesondere
aus dem schwierigsten: Vorschau und Speichern liefern denselben Wert.
Ausgabe: `POST /api/work-periods/preview`, `POST /api/work-periods/change`,
`workPeriodChangeService.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/12-stundenwechsel-bedienen/12-CONTEXT.md
@.planning/phases/12-stundenwechsel-bedienen/12-PATTERNS.md
@.planning/phases/12-stundenwechsel-bedienen/12-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: POST /preview und POST /change mit Rollen- und Tokenpruefung</name>
  <files>server/src/routes/workPeriods.ts</files>
  <read_first>
    - `server/src/routes/workPeriods.ts` in der Fassung aus Plan 12-01 — der bestehende `GET /` und der Kopfkommentar, der diese beiden Endpunkte ankuendigt.
    - `server/src/routes/overtime.ts` Zeile 132-180 — laut 12-PATTERNS.md Abschnitt 2 das Analog fuer den Schreibendpunkt: `requireAuth, requireAdmin`, Pflichtfelderpruefung, Mindestlaenge der Begruendung, Delegation an den Service, Fehlerantwort.
    - `server/src/routes/yearEndRollover.ts` Zeile 26-57 — das Analog fuer den Vorschau-Endpunkt: Parametervalidierung, `logger.info` mit Kontext, `ApiResponse`-Huelle, `logger.error({ err: error }, ...)` im `catch`.
    - `server/src/services/workPeriodChangeService.ts` (Plan 12-03) — Signatur von `applyWorkTimeChange` und die Fehlerklasse `WorkTimeChangeValidationError`.
    - `server/src/services/workTimeChangeToken.ts` (Plan 12-03) — `issuePreviewToken`, `verifyPreviewToken` und die drei Ablehnungsgruende.
    - `server/src/middleware/auth.ts` — `requireAuth`, `requireAdmin`.
  </read_first>
  <action>
`server/src/routes/workPeriods.ts` um zwei Endpunkte erweitern. Der bestehende `GET /` bleibt
unveraendert.

**`POST /preview`** mit `requireAuth, requireAdmin` (D6 — auch die Vorschau ist eine
Admin-Funktion; sie legt Sollstunden und Saldo eines fremden Nutzers offen):
- Koerper lesen als `unknown` und mit einem Typwaechter in `{ userId: number; validFrom: string;
  weeklyHours: number; workSchedule: WorkSchedule | null }` ueberfuehren. Fehlt ein Feld oder hat
  es den falschen Typ, `400 { success: false, error: 'Missing or invalid fields' }`.
  Kein `as`-Cast auf den Anfragekoerper.
- `applyWorkTimeChange({ ...input, reason: '' }, { dryRun: true, createdBy:
  req.session.user!.id })` aufrufen — die Begruendung wird im Trockenlauf nicht geprueft.
- Aus dem Ergebnis `outcome.preview` nehmen, `issuePreviewToken({ userId, validFrom,
  weeklyHours, workSchedule })` anhaengen und
  `res.json({ success: true, data: { ...outcome.preview, previewToken } })` antworten.
- `WorkTimeChangeValidationError` → `400 { success: false, error: err.message }` (die Meldung
  ist bereits der deutsche Textbuchsatz und wird woertlich in der Oberflaeche angezeigt).
  `WorkPeriodConflictError` → `409` mit derselben Behandlung. Alles andere →
  `logger.error({ err: error }, 'Failed to preview work time change')` und `500` mit einer
  neutralen Meldung.

**`POST /change`** mit `requireAuth, requireAdmin`:
- Koerper wie oben mit Typwaechter lesen, zusaetzlich `reason: string` und
  `previewToken: string`.
- `verifyPreviewToken(previewToken, { userId, validFrom, weeklyHours, workSchedule })`.
  Ist das Ergebnis nicht `valid`, mit `409 { success: false, error: 'PREVIEW_STALE: Die
  Vorschau ist nicht mehr aktuell.' }` antworten und **nicht speichern**. Der Praefix
  `PREVIEW_STALE` ist Vertrag mit dem Desktop (Plan 12-04, Zustand 14) und muss woertlich am
  Anfang der Meldung stehen.
- Danach `applyWorkTimeChange({ ... }, { dryRun: false, createdBy: req.session.user!.id })`.
- Antwort `res.json({ success: true, data: { period, preview, transactionId } })`.
- Fehlerbehandlung wie bei `/preview`.
- `logger.info({ userId, validFrom, balanceDelta: outcome.preview.balanceDelta, adminId:
  req.session.user!.id }, 'Stundenwechsel gespeichert')` — keine Namen, keine Begruendung im
  Log.

Reihenfolge der Routen beachten: beide `POST`-Pfade sind statisch (`/preview`, `/change`) und
kollidieren nicht mit `GET /`. Kein `console.*`, kein `any`.
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "requireAdmin" server/src/routes/workPeriods.ts` liefert genau 2 (je einmal an `/preview` und `/change`)
    - `grep -c "'/preview'" server/src/routes/workPeriods.ts` liefert 1 und `grep -c "'/change'" server/src/routes/workPeriods.ts` liefert 1
    - `grep -c "verifyPreviewToken" server/src/routes/workPeriods.ts` liefert 1
    - `grep -c "issuePreviewToken" server/src/routes/workPeriods.ts` liefert 1
    - `grep -c "PREVIEW_STALE" server/src/routes/workPeriods.ts` liefert 1
    - `grep -c "dryRun: true" server/src/routes/workPeriods.ts` liefert 1 und `grep -c "dryRun: false" server/src/routes/workPeriods.ts` liefert 1
    - `grep -c "req.body as" server/src/routes/workPeriods.ts` liefert 0 — der Anfragekoerper wird ueber einen Typwaechter gefuehrt, nicht per Cast
    - `grep -c ": any" server/src/routes/workPeriods.ts` liefert 0
    - `grep -c "console\." server/src/routes/workPeriods.ts` liefert 0
    - Gegen den lokalen Dev-Server, protokolliert im SUMMARY: `POST /api/work-periods/change` mit einer Employee-Session antwortet 403; derselbe Aufruf mit Admin-Session und ohne `previewToken` antwortet 409 mit einer Meldung, die mit `PREVIEW_STALE` beginnt; ein Aufruf mit einem Token, das zu anderen Wochenstunden ausgestellt wurde, antwortet ebenfalls 409 — und in allen drei Faellen ist `SELECT COUNT(*) FROM user_work_periods` vor und nach dem Aufruf identisch
    - `cd server && npx tsc --noEmit` endet mit Exitcode 0
  </acceptance_criteria>
  <done>Beide Endpunkte existieren, sind admin-pflichtig, die Vorschau stellt ein Token aus, das Speichern verlangt es, und ein abgelehntes Token fuehrt nachweislich zu keinem Schreibvorgang.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Der Nachweis — Nullwirkung, Rueckwirkung, Vorschau gleich Speichern</name>
  <files>server/src/services/workPeriodChangeService.test.ts</files>
  <read_first>
    - `server/src/services/workPeriodService.test.ts` Zeile 1-60 — das Testmuster des Projekts: geteilte Verbindung aus `connection.js` statt `:memory:`, Fixtures je Test mit eindeutigem `username`, Aufraeumen ueber `DELETE FROM users WHERE id = ?` (CASCADE raeumt Perioden ab), direkter Aufruf der Servicefunktionen statt Nachbau der Logik.
    - `server/src/services/absencePeriodAwareness.test.ts` — zweites Vorbild, insbesondere wie dort Zeiteintraege und Perioden fuer einen Testnutzer aufgebaut werden.
    - `server/src/services/workPeriodChangeService.ts` (Plan 12-03) — die zehn `<behavior>`-Punkte aus dem dortigen Task 2 sind die Sollliste dieser Testdatei.
    - `server/src/services/overtimeTransactionService.ts` Zeile 400-427 — `getOvertimeBalance()`, der Lesepfad, gegen den die Vorschau geprueft wird.
    - `server/vitest.config.ts` — belegt, dass Tests gegen die Arbeitsdatenbank laufen; jeder Testnutzer wird deshalb am Ende wieder entfernt.
    - `.planning/phases/12-stundenwechsel-bedienen/12-03-SUMMARY.md`, Abschnitt "Gemessener Befund zum Saldo-Lesepfad".
  </read_first>
  <behavior>
    - **REQ-28 / Erfolgskriterium 1 (Nullwirkung):** Testnutzer mit 40 h/Woche und Zeiteintraegen von Juli bis heute; Wechsel auf 20 h zum 01.10. eines kuenftigen Datums. Nach dem Speichern ist jede Zeile in `overtime_transactions` mit `date < validFrom` in `hours` und `type` unveraendert (Vergleich einer vorher gezogenen Momentaufnahme), `balanceDelta === 0`, und `SELECT COUNT(*) FROM overtime_transactions WHERE type = 'model_change'` liefert 0.
    - **REQ-26 / Erfolgskriterium 2 (Rueckwirkung):** Testnutzer mit 40 h/Woche; Wechsel auf 20 h mit einem `validFrom`, das zwei Monate zurueckliegt. Nach dem Speichern gilt: jede Zeile mit `date < validFrom` unveraendert; `balanceDelta > 0`; genau eine Zeile mit `type = 'model_change'`, deren `date` dem Stichtag entspricht, deren `referenceType` `'work_period'` ist und deren `referenceId` der id der neuen Periode entspricht.
    - **REQ-27 / Erfolgskriterium 3 (Vorschau gleich Speichern):** derselbe Eingabesatz wird zuerst mit `dryRun: true` und danach mit `dryRun: false` durch `applyWorkTimeChange` geschickt. Die Felder `targetHoursBefore`, `targetHoursAfter`, `targetHoursDelta`, `balanceBefore`, `balanceAfter`, `balanceDelta` und `workingDaysInRange` sind paarweise exakt gleich. Zusaetzlich gilt nach dem Speichern `getOvertimeBalance(userId) === vorschau.balanceAfter`.
    - **Trockenlauf schreibt nichts:** nach einem `dryRun: true`-Lauf sind die Zeilenzahlen in `user_work_periods` und `overtime_transactions`, die Summe ueber `overtime_balance` und `getOvertimeBalance(userId)` unveraendert gegenueber davor.
    - **REQ-29 / Erfolgskriterium 4 (Sichtbarkeit):** die `model_change`-Zeile taucht in `getOvertimeHistory(userId)` auf, ihre `description` enthaelt die Begruendung woertlich, und `getOvertimeBalance(userId)` ist exakt gleich gross wie ohne diese Zeile — die Buchung zaehlt den Saldo nicht doppelt. Der Vergleich erfolgt, indem die Zeile testweise entfernt und `getOvertimeBalance` erneut gelesen wird.
    - **Stichtag mitten im Monat:** `midMonthEffective === true`; im Rebuild-Monat traegt ein Tag vor dem Stichtag das alte und ein Tag ab dem Stichtag das neue Tagessoll (Vergleich zweier `time_entry`-Zeilen desselben Monats).
    - **Wechsel ueber einen Jahreswechsel:** `affectedMonths` enthaelt Monate aus zwei Kalenderjahren, und der Lauf endet ohne Fehler.
    - **Validierung:** Stichtag vor `hireDate`, Stichtag gleich einer bestehenden Periode, Wochenstunden ueber 60, leere Begruendung im Speicherpfad und Werte identisch zur gueltigen Periode werfen je `WorkTimeChangeValidationError`; nach jedem dieser Versuche sind die Zeilenzahlen in `user_work_periods` und `overtime_transactions` unveraendert.
    - **D7 (Atomaritaet):** faellt der Vorgang nach dem Anlegen der Periode aus (erzwungen ueber eine Eingabe, die `checkPeriodChain` scheitern laesst, oder ueber einen Testnutzer, dessen Periodenkette absichtlich verletzt wird), ist danach weder die neue Periode noch eine `model_change`-Buchung vorhanden.
    - **Kettenpruefung im Ganzen:** nach allen Testfaellen liefert `checkAllPeriodChains()` fuer die angelegten Testnutzer keinen Befund.
  </behavior>
  <action>
Lege `server/src/services/workPeriodChangeService.test.ts` an. Jeder Punkt aus `<behavior>` wird
ein eigener, benannter Testfall; der Name nennt das jeweilige Requirement bzw. die Entscheidung
(z. B. `REQ-28: Stichtag in der Zukunft laesst jede Buchung davor unveraendert`).

Aufbau je Testfall nach dem Muster aus `workPeriodService.test.ts`: eigener Testnutzer mit
eindeutigem `username` (Praefix `test-12-05-`), Zeiteintraege ueber den benoetigten Zeitraum,
Periode aus `ensureInitialWorkPeriod` bzw. `createWorkPeriod`. Im `afterEach` wird der
Testnutzer per `DELETE FROM users WHERE id = ?` entfernt; zusaetzlich werden erzeugte
`overtime_balance`- und `overtime_transactions`-Zeilen dieses Nutzers geraeumt, soweit CASCADE
sie nicht abdeckt.

Fuer den Vergleich "jede Buchung davor unveraendert" wird vor dem Wechsel eine Momentaufnahme
gezogen (`SELECT id, date, type, hours FROM overtime_transactions WHERE userId = ? AND date < ?
ORDER BY date, id`) und danach mit `toEqual` gegen dieselbe Abfrage geprueft. Ein blosser
Zeilenzahlvergleich genuegt nicht — die ROADMAP verlangt "um keine Minute".

Kein Zugriff auf die Produktionsdatenbank; der Testlauf laeuft gegen die vom Projekt
konfigurierte Arbeitsdatenbank. Kein `any` (unbekannte Zeilen ueber `unknown` plus Typwaechter
oder ueber eine benannte Zeilen-Schnittstelle), keine `console.*`.
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit && npm test -- workPeriodChangeService</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && npm test -- workPeriodChangeService` endet mit Exitcode 0 und meldet mindestens 10 bestandene Testfaelle
    - `grep -c "REQ-26" server/src/services/workPeriodChangeService.test.ts` liefert mindestens 1
    - `grep -c "REQ-27" server/src/services/workPeriodChangeService.test.ts` liefert mindestens 1
    - `grep -c "REQ-28" server/src/services/workPeriodChangeService.test.ts` liefert mindestens 1
    - `grep -c "REQ-29" server/src/services/workPeriodChangeService.test.ts` liefert mindestens 1
    - `grep -c "dryRun: true" server/src/services/workPeriodChangeService.test.ts` liefert mindestens 2
    - `grep -c "checkAllPeriodChains" server/src/services/workPeriodChangeService.test.ts` liefert mindestens 1
    - `grep -c "getOvertimeBalance" server/src/services/workPeriodChangeService.test.ts` liefert mindestens 3
    - `grep -c "model_change" server/src/services/workPeriodChangeService.test.ts` liefert mindestens 3
    - `grep -c ": any" server/src/services/workPeriodChangeService.test.ts` liefert 0
    - Ein absichtlich eingefuehrter Fehler belegt die Schaerfe des Vorschau-Tests: waehrend des Laufs wird `targetHoursAfter` im Service testweise um 1 verfaelscht, der Test schlaegt fehl, die Verfaelschung wird zurueckgenommen. Der Beleg (Fehlermeldung des roten Laufs) steht im SUMMARY.
    - `cd server && npx tsc --noEmit` endet mit Exitcode 0
  </acceptance_criteria>
  <done>Alle vier Erfolgskriterien der ROADMAP sind als Testfall abgebildet und gruen; der Vorschau-Test ist nachweislich scharf.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Angemeldeter Nutzer → Schreibendpunkt | Jeder mit gueltiger Session kann die Endpunkte aufrufen |
| Vorschau → Speichern | Zwischen beiden Aufrufen kann der Client die Eingaben veraendert haben |
| Testlauf → Arbeitsdatenbank | Tests legen echte Nutzer an und raeumen sie wieder ab |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-22 | Elevation of Privilege | `POST /preview`, `POST /change` | mitigate | Beide tragen `requireAuth` UND `requireAdmin` (D6). Belegt durch `grep -c "requireAdmin"` = 2 und den 403-Nachweis mit Employee-Session. |
| T-12-23 | Information Disclosure | `POST /preview` | mitigate | Die Vorschau legt Sollstunden und Ueberstundensaldo eines fremden Nutzers offen und ist deshalb bewusst admin-pflichtig, nicht nur `requireAuth`. |
| T-12-24 | Tampering | Speichern ohne Vorschau | mitigate | `verifyPreviewToken` vor jedem Schreibvorgang; bei Ablehnung 409 und kein Schreiben. Nachgewiesen ueber den Zeilenzahlvergleich in den Acceptance Criteria von Task 1. |
| T-12-25 | Tampering | Anfragekoerper | mitigate | Typwaechter statt `req.body as ...`; jedes Feld wird auf Typ und Wertebereich geprueft, bevor es den Service erreicht. Die fachliche Validierung liegt zusaetzlich im Service (Verteidigung in der Tiefe). |
| T-12-26 | Information Disclosure | Protokollierung und Fehlerantworten | mitigate | `logger.info`/`logger.error` fuehren nur Zahlen und Datumsangaben; die 500er-Antwort traegt eine neutrale Meldung, nicht den Stacktrace. Die 400er-Antwort traegt ausschliesslich den deutschen Validierungssatz. |
| T-12-27 | Denial of Service | Testlauf gegen die Arbeitsdatenbank | mitigate | Testnutzer tragen den Praefix `test-12-05-` und werden im `afterEach` entfernt; kein Test greift auf die Produktionsdatenbank zu (kein gesetztes `DATABASE_PATH` auf den Produktionspfad). |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | Nicht anwendbar: keine neue Abhaengigkeit. |
</threat_model>

<verification>
1. `cd server && npx tsc --noEmit` — Exitcode 0.
2. `cd server && npm test -- workPeriodChangeService` — gruen, mindestens 10 Faelle.
3. `cd server && npm test` — keine neuen roten Tests gegenueber dem Ausgangsstand aus dem SUMMARY von Plan 12-01.
4. Die drei HTTP-Nachweise aus Task 1 (403 fuer Employee, 409 ohne Token, 409 bei fremdem Token — jeweils ohne Schreibwirkung) protokolliert.
5. Der Beleg des absichtlich roten Laufs aus Task 2 protokolliert.
</verification>

<success_criteria>
- Beide Endpunkte sind admin-pflichtig und tokengebunden.
- Alle vier Erfolgskriterien der ROADMAP sind automatisiert belegt.
- Der Vorschau-Test ist scharf, nachgewiesen durch einen absichtlich roten Lauf.
- `checkAllPeriodChains()` meldet nach den Testlaeufen keinen Befund.
- Kein `any`, kein `console.*`, `npx tsc --noEmit` gruen.
</success_criteria>

<output>
Erstelle `.planning/phases/12-stundenwechsel-bedienen/12-05-SUMMARY.md`.

Pflichtabschnitte:
- "Bestaetigung des Saldo-Lesepfads" — die gemessene Aussage, ob die `model_change`-Zeile den
  ueber `getOvertimeBalance()` gelesenen Saldo unveraendert laesst, mit den beiden Zahlen.
- "Beleg der Testschaerfe" — die Fehlermeldung des absichtlich verfaelschten Laufs.
- "UAT-Punkte fuer Phase 14" — nummerierte Liste, je Punkt: was zu pruefen ist, wo, mit welchem
  erwarteten Ergebnis. Mindestens:
  1. Auf einer Kopie der Produktionsdatenbank fuer einen realen Nutzer eine Vorschau abrufen und
     danach speichern: der in der Vorschau gezeigte neue Saldo steht danach im Kontoauszug.
  2. Fuer alle Nutzer OHNE Modellwechsel: `getOvertimeBalance` vor und nach dem Lauf identisch.
  3. `checkAllPeriodChains()` gegen die Produktionskopie nach dem Lauf: kein Befund.
</output>
