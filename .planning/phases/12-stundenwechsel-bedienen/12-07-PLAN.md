---
phase: 12-stundenwechsel-bedienen
plan: 07
type: execute
wave: 4
depends_on: ["12-02", "12-04", "12-06"]
files_modified:
  - desktop/src/components/users/WorkScheduleEditor.tsx
  - desktop/src/pages/UserManagementPage.tsx
  - desktop/src/components/users/EditUserModal.tsx
  - desktop/src/components/worktime/OvertimeTransactions.tsx
autonomous: true
requirements: [REQ-26, REQ-29]

must_haves:
  truths:
    - "D1: 'Stundenwechsel ab Datum …' ist eine eigene Aktion mit eigenem Dialog; die Stammdatenfelder fuer Stunden und Tagesplan sind im EditUserModal schreibgeschuetzt und verweisen auf diese Aktion"
    - "'Änderungen speichern' im EditUserModal kann das Arbeitszeitmodell nicht mehr still ueberschreiben — weeklyHours und workSchedule gehen unveraendert aus user in den Absende-Pfad"
    - "REQ-29: Eine model_change-Buchung erscheint im Ueberstunden-Kontoauszug als eigene Zeile mit dem Badge 'Modellwechsel', Betrag mit Vorzeichen und Trendpfeil, Begruendung und einer zweiten Zeile mit Periodenbezug"
    - "Nach dem Speichern zeigen Periodenliste und die schreibgeschuetzten Felder sofort den neuen Wert (Zustand 15) — moeglich, weil UserManagementPage nur noch die Id haelt"
    - "Die 14 console.log in EditUserModal.tsx (5) und UserManagementPage.tsx (9) sind entfernt; console.error und console.warn bleiben stehen"
  artifacts:
    - path: "desktop/src/components/users/EditUserModal.tsx"
      provides: "Schreibgeschuetzte Stundenfelder, Hinweispanel, Einstiegs-Button, Periodenliste, Dialogsteuerung, Erfolgsbanner"
      contains: "Stundenwechsel ab Datum"
    - path: "desktop/src/components/worktime/OvertimeTransactions.tsx"
      provides: "Darstellung des Buchungstyps model_change"
      contains: "model_change"
  key_links:
    - from: "desktop/src/components/users/EditUserModal.tsx"
      to: "desktop/src/components/worktime/WorkTimeChangeModal.tsx"
      via: "Rendern als Geschwister NACH </form>"
      pattern: "WorkTimeChangeModal"
    - from: "desktop/src/components/users/EditUserModal.tsx"
      to: "desktop/src/components/worktime/WorkTimePeriodList.tsx"
      via: "Periodenblock im Abschnitt Arbeitszeit"
      pattern: "WorkTimePeriodList"
    - from: "desktop/src/pages/UserManagementPage.tsx"
      to: "desktop/src/components/users/EditUserModal.tsx"
      via: "editingUserId + users?.find(...)"
      pattern: "editingUserId"
---

<objective>
Die Einbindung: Der Stundenwechsel wird dort bedienbar, wo der Admin ohnehin arbeitet, und die
entstandene Buchung wird dort sichtbar, wo Salden nachvollzogen werden.

Zweck: D1 (getrennte Aktion statt stillem Formularfeld) und REQ-29 (Sichtbarkeit im
Kontoauszug). Ohne diesen Plan existieren Dialog und Liste, sind aber nicht erreichbar, und die
`model_change`-Buchung erschiene ohne Beschriftung.
Ausgabe: vier geaenderte Bestandsdateien.

`12-UI-SPEC.md` Abschnitt 1 und 5 sowie die Aenderungstabellen A und B sind bindend und nennen
jede Aenderung mit Zeilennummer. Die Dateien werden ausschliesslich an den dort benannten
Stellen angefasst — kein Aufraeumfeldzug.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/12-stundenwechsel-bedienen/12-UI-SPEC.md
@.planning/phases/12-stundenwechsel-bedienen/12-PATTERNS.md
@.planning/phases/12-stundenwechsel-bedienen/12-06-SUMMARY.md
@.planning/phases/12-stundenwechsel-bedienen/12-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: WorkScheduleEditor readOnly-Prop und UserManagementPage auf die Id umstellen</name>
  <files>desktop/src/components/users/WorkScheduleEditor.tsx, desktop/src/pages/UserManagementPage.tsx</files>
  <read_first>
    - `desktop/src/components/users/WorkScheduleEditor.tsx` vollstaendig — insbesondere Zeile 4-8 (Props), Zeile 90-104 (Toggle als `<button type="button">`, kein `<select>`, keine Checkbox), Zeile 110 (`grid grid-cols-2 gap-3`), Zeile 127-138 (Tagesfelder als `<input type="number">`), Zeile 146-151 (Summenkarte), Zeile 159-162 (Abweichungswarnung).
    - `desktop/src/pages/UserManagementPage.tsx` — Zeile 38 (`useState<User | null>`), Zeile 127-156 (Loeschpfad mit den neun `console.log`, dem `console.warn` in Zeile 134 und den `console.error` in Zeile 155-156), Zeile 461-467 (Zeilenaktion "Bearbeiten", `setEditingUser(user)`), Zeile 523-527 (bedingtes Rendern des `EditUserModal`).
    - `.planning/phases/12-stundenwechsel-bedienen/12-UI-SPEC.md`, Aenderungstabelle A, Zeilen zu `WorkScheduleEditor.tsx` und `UserManagementPage.tsx` — dort steht die abschliessende Aufgabenliste samt Begruendung fuer den B4-Fix.
    - `.planning/phases/12-stundenwechsel-bedienen/12-UI-SPEC.md`, Abschnitt "Debugausgaben — abschliessende Regelung" — welche 16 Aufrufe entfernt werden und welche `console.error`/`console.warn` stehen bleiben.
  </read_first>
  <action>
**`WorkScheduleEditor.tsx`**, zwei Aenderungen:
1. Neue optionale Prop `readOnly?: boolean` mit Default `false`. Bei `true`:
   - Der Toggle (Zeile 90-104) bekommt `disabled` und zusaetzlich
     `disabled:cursor-not-allowed`. Die Akzentfarbe `bg-blue-600` im eingeschalteten Zustand
     bleibt — sie ist hier Zustandsanzeige, kein Bedienangebot.
   - Die Tagesfelder (Zeile 127-138) bekommen `disabled` und zusaetzlich
     `disabled:bg-gray-100 dark:disabled:bg-gray-900 disabled:text-gray-600
     dark:disabled:text-gray-400 disabled:cursor-not-allowed`. **Kein `opacity`** — sonst greift
     die Browser-Standardabblendung und der Wert wird unlesbar.
   - Summenkarte und Fallback-Info bleiben unveraendert sichtbar.
2. `grid grid-cols-2 gap-3` in Zeile 110 wird zu `grid grid-cols-1 sm:grid-cols-2 gap-3`.

Sonst nichts. Jeder bestehende Aufruf (`EditUserModal.tsx` Zeile 274-277) verhaelt sich ohne die
neue Prop unveraendert.

**`UserManagementPage.tsx`**, zwei Aenderungen:
1. Der lokale State haelt nur noch die Id:
   `const [editingUserId, setEditingUserId] = useState<number | null>(null)` (heute Zeile 38
   `useState<User | null>`). Das Objekt wird bei jedem Render aus der bestehenden Nutzerquery
   abgeleitet: `const editingUser = users?.find(u => u.id === editingUserId) ?? null;`.
   Zeile 464 `setEditingUser(user)` wird zu `setEditingUserId(user.id)`. Zeile 523-527 bleiben
   in ihrer Form (`{editingUser && <EditUserModal … user={editingUser} />}`), und `onClose`
   setzt `setEditingUserId(null)`.
   Grund: Ohne diese Umstellung zeigt das Modal nach dem Speichern eine Momentaufnahme und
   Zustand 15 ist nicht erreichbar.
2. Die neun `console.log` im Loeschpfad (Zeile 128-131, 139, 147, 148, 152, 153) werden
   **ersatzlos entfernt**. Der `console.warn` in Zeile 134 und die `console.error` in
   Zeile 155-156 bleiben unangetastet — Guard- und Fehlerpfade sind keine Debugausgaben.

Kein `any`, keine neue `console.*`. Keine weitere Aenderung an diesen beiden Dateien.
  </action>
  <verify>
    <automated>cd desktop && npx tsc --noEmit && test "$(grep -c 'console\.log' src/pages/UserManagementPage.tsx)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "readOnly" desktop/src/components/users/WorkScheduleEditor.tsx` liefert mindestens 3 (Prop-Deklaration, Toggle, Tagesfelder)
    - `grep -c "grid grid-cols-1 sm:grid-cols-2 gap-3" desktop/src/components/users/WorkScheduleEditor.tsx` liefert 1
    - `grep -c "grid grid-cols-2 gap-3" desktop/src/components/users/WorkScheduleEditor.tsx` liefert 0
    - `grep -c "disabled:cursor-not-allowed" desktop/src/components/users/WorkScheduleEditor.tsx` liefert mindestens 2
    - `grep -c "disabled:opacity" desktop/src/components/users/WorkScheduleEditor.tsx` liefert 0
    - `grep -c "console.log" desktop/src/pages/UserManagementPage.tsx` liefert 0
    - `grep -c "console.warn" desktop/src/pages/UserManagementPage.tsx` liefert 1
    - `grep -c "console.error" desktop/src/pages/UserManagementPage.tsx` liefert 2
    - `grep -c "editingUserId" desktop/src/pages/UserManagementPage.tsx` liefert mindestens 4
    - `grep -c "useState<User | null>(null)" desktop/src/pages/UserManagementPage.tsx` liefert 0
    - `grep -c "users?.find(u => u.id === editingUserId)" desktop/src/pages/UserManagementPage.tsx` liefert 1
    - `cd desktop && npx tsc --noEmit` endet mit Exitcode 0
  </acceptance_criteria>
  <done>Der `WorkScheduleEditor` kennt `readOnly` mit lesbaren Kontrastklassen und einen Breakpoint; `UserManagementPage` haelt nur noch die Id und ist frei von Debugausgaben.</done>
</task>

<task type="auto">
  <name>Task 2: EditUserModal — schreibgeschuetzte Felder, Einstiegs-Button, Periodenliste, Dialogsteuerung</name>
  <files>desktop/src/components/users/EditUserModal.tsx</files>
  <read_first>
    - `desktop/src/components/users/EditUserModal.tsx` vollstaendig — insbesondere Zeile 41-54 (Reset-Effekt mit Abhaengigkeit `[user]`), Zeile 103-106 und 123 (die fuenf `console.log`), Zeile 113-114 (Absende-Pfad mit `weeklyHours`/`workSchedule`), Zeile 134 (`console.error`, bleibt), Zeile 138-159 (`handleClose`-Muster), Zeile 168 und 336 (die Grenzen des einen `<form>`), Zeile 249-260 (Feld Wochenstunden), Zeile 274-278 (Aufruf des `WorkScheduleEditor`).
    - `.planning/phases/12-stundenwechsel-bedienen/12-UI-SPEC.md`, Abschnitt 1 vollstaendig — schreibgeschuetztes Feld mit den `!`-Hover-Overrides, kein `tabIndex={-1}`, kein `readOnly`-Zweig in `Input.tsx`, Hinweispanel mit Einstiegs-Button, Periodenblock, und der Unterabschnitt "Formulargrenzen im verschachtelten Baum" mit den drei verbindlichen Festlegungen.
    - `.planning/phases/12-stundenwechsel-bedienen/12-UI-SPEC.md`, Aenderungstabelle B, Zeile zu `EditUserModal.tsx` — Reset-Effekt auf `[user.id]`, schmaler Sync-Effekt mit Wertvergleich, Entfernung der fuenf `console.log`.
    - `.planning/phases/12-stundenwechsel-bedienen/12-UI-SPEC.md`, Textbuch → "Einstieg in `EditUserModal.tsx`" — die vier Saetze und die drei Toastvarianten woertlich.
    - `desktop/src/components/worktime/WorkTimeChangeModal.tsx` (Plan 12-06) — Props `isOpen`, `onClose`, `user`, `onSaved`.
    - `desktop/src/components/worktime/WorkTimePeriodList.tsx` (Plan 12-04) — Props `userId`, `highlightPeriodId`, `renderActions`.
  </read_first>
  <action>
Im Abschnitt "Arbeitszeit & Berechtigungen" des `EditUserModal`:

1. **Feld Wochenstunden schreibgeschuetzt** (Zeile 249-260): `readOnly`, `aria-readonly="true"`,
   zusaetzlich `className="bg-gray-100 dark:bg-gray-900 text-gray-600 dark:text-gray-400
   cursor-not-allowed hover:!border-gray-300 dark:hover:!border-gray-600 hover:!shadow-sm"`.
   Die Ausrufezeichen sind zwingend: `Input.tsx` Zeile 36-37 setzt `hover:border-gray-400` und
   `hover:shadow-md` mit derselben Spezifitaet. **Kein `disabled`** (Lesbarkeit) und **kein
   `tabIndex={-1}`** (Erreichbarkeit). `Input.tsx` wird nicht angefasst.
   Darunter der Hilfstext `Wird über „Stundenwechsel ab Datum …" geändert, damit vergangene
   Monate unberührt bleiben.`
2. **`WorkScheduleEditor` mit `readOnly`** (Zeile 274-278), darueber der Hilfstext
   `Der Tagesplan gehört zum Arbeitszeitmodell und wird zusammen mit dem Stichtag geändert.`
3. **Hinweispanel** unmittelbar darunter im bestehenden Blau-Muster
   (`bg-blue-50 dark:bg-blue-900/20 border border-blue-200 dark:border-blue-800 rounded-lg p-4`)
   mit rechtsbuendig `<Button type="button" variant="secondary" size="sm">Stundenwechsel ab
   Datum …</Button>`. `type="button"` ist zwingend, sonst loest er den Submit des umgebenden
   Formulars aus. Der Button traegt eine `ref` fuer die Fokusrueckgabe.
4. **Periodenblock** darunter, getrennt durch `pt-6 border-t border-gray-200
   dark:border-gray-700`, mit der Ueberschrift `Arbeitszeitmodell — Perioden` und darunter
   `<WorkTimePeriodList userId={user.id} />`.
5. **Renderort des Dialogs (verbindlich):** `<WorkTimeChangeModal>` wird **ausserhalb** des
   `<form>`-Elements gerendert — als Geschwister **nach** `</form>` (heute Zeile 336),
   weiterhin innerhalb der Kinder von `Modal`. `createPortal` allein genuegt nicht: Ereignisse
   propagieren entlang des React-Baums, nicht des DOM-Baums. Dasselbe gilt fuer jeden
   `ConfirmDialog`, der aus diesem Baum geoeffnet wird.
6. **Erfolg (Zustand 15):** `onSaved` des Dialogs setzt einen lokalen Zustand, der 8 Sekunden
   lang ein gruenes Banner im Abschnitt "Arbeitszeit" zeigt: `Stundenwechsel gespeichert: ab
   {TT.MM.JJJJ} gelten {X,X} h/Woche.` Zusaetzlich `toast.success('Stundenwechsel
   gespeichert')`; ist die Saldodifferenz ungleich 0, stattdessen `Stundenwechsel gespeichert —
   Saldoänderung {±H:MMh} steht im Kontoauszug`. Danach kehrt der Fokus auf den Button
   "Stundenwechsel ab Datum …" zurueck.
7. **Absende-Pfad** (Zeile 113-114): `weeklyHours` und `workSchedule` werden weiterhin
   mitgesendet, aber unveraendert aus `user` — nicht aus dem Formularzustand. So kann
   "Änderungen speichern" das Arbeitszeitmodell nicht mehr still ueberschreiben.
8. **Reset-Effekt** (Zeile 41-54): Abhaengigkeit wird `[user.id]` statt `[user]`. Ein zweiter,
   schmaler Effekt synchronisiert **nur** `weeklyHours` und `workSchedule`, wenn sich diese
   beiden Werte in `user` aendern; seine Abhaengigkeit ist `user.weeklyHours` zusammen mit einer
   stabilen Serialisierung von `user.workSchedule` (`JSON.stringify` ueber ein Objekt mit
   sortierten Schluesseln) — **Wertvergleich, nicht Objektidentitaet**, weil `user` nach der
   Umstellung auf die abgeleitete Query bei jedem Refetch ein neues Objekt ist.
9. **Die fuenf `console.log`** in Zeile 103-106 und 123 werden **ersatzlos entfernt**. Der
   `console.error` in Zeile 134 bleibt stehen.

**Nicht anfassen:** die starren `grid-cols-2`/`grid-cols-3` in Zeile 197, 216, 238 und 280 sind
eine vorbestehende Altlast und bleiben unveraendert (UI-SPEC, "Beobachtete, hier nicht behobene
Altlasten"). Neuer Code dieser Phase fuehrt das Muster nicht fort: jedes neue Raster bekommt
einen Breakpoint.

Kein `any`, keine neue `console.*`, kein `toISOString`.
  </action>
  <verify>
    <automated>cd desktop && npx tsc --noEmit && test "$(grep -c 'console\.log' src/components/users/EditUserModal.tsx)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "console.log" desktop/src/components/users/EditUserModal.tsx` liefert 0
    - `grep -c "console.error" desktop/src/components/users/EditUserModal.tsx` liefert 1
    - `grep -c "Stundenwechsel ab Datum" desktop/src/components/users/EditUserModal.tsx` liefert mindestens 2 (Button und Hilfstext)
    - `grep -c "aria-readonly=\"true\"" desktop/src/components/users/EditUserModal.tsx` liefert 1
    - `grep -c "hover:!border-gray-300" desktop/src/components/users/EditUserModal.tsx` liefert 1
    - `grep -c "tabIndex={-1}" desktop/src/components/users/EditUserModal.tsx` liefert 0
    - `grep -c "readOnly" desktop/src/components/users/EditUserModal.tsx` liefert mindestens 2 (Feld und `WorkScheduleEditor`)
    - `grep -c "Arbeitszeitmodell — Perioden" desktop/src/components/users/EditUserModal.tsx` liefert 1
    - `grep -c "WorkTimePeriodList" desktop/src/components/users/EditUserModal.tsx` liefert mindestens 2 (Import und Verwendung)
    - `grep -c "WorkTimeChangeModal" desktop/src/components/users/EditUserModal.tsx` liefert mindestens 2
    - Die Zeilennummer von `<WorkTimeChangeModal` ist **groesser** als die Zeilennummer von `</form>` — pruefbar mit `grep -n "</form>\|<WorkTimeChangeModal" desktop/src/components/users/EditUserModal.tsx`
    - `grep -c "\[user.id\]" desktop/src/components/users/EditUserModal.tsx` liefert mindestens 1
    - `grep -c "}, \[user\])" desktop/src/components/users/EditUserModal.tsx` liefert 0
    - `grep -c "toast.success" desktop/src/components/users/EditUserModal.tsx` liefert mindestens 1
    - `grep -c "Stundenwechsel gespeichert" desktop/src/components/users/EditUserModal.tsx` liefert mindestens 2
    - `grep -c ": any" desktop/src/components/users/EditUserModal.tsx` liefert 0
    - `cd desktop && npx tsc --noEmit` endet mit Exitcode 0
  </acceptance_criteria>
  <done>Der Einstieg in den Stundenwechsel steht im Stammdatendialog, die Stundenfelder sind lesbar schreibgeschuetzt, die Periodenliste ist eingebunden, der Wechsel-Dialog liegt ausserhalb des Formulars, und die fuenf Debugausgaben sind weg.</done>
</task>

<task type="auto">
  <name>Task 3: OvertimeTransactions — der Buchungstyp model_change im Kontoauszug (REQ-29)</name>
  <files>desktop/src/components/worktime/OvertimeTransactions.tsx</files>
  <read_first>
    - `desktop/src/components/worktime/OvertimeTransactions.tsx` Zeile 78-146 — die vier Zuordnungsfunktionen `getTypeLabel`, `getTypeDescription`, `getTypeBadgeColor`, `isAbsenceType`; dort kommt je ein neuer `case` hinzu.
    - `desktop/src/components/worktime/OvertimeTransactions.tsx` Zeile 190-270 — der Tabellenteil mit der Beschreibungszelle, der Betragsspalte (`font-bold`, Trendpfeil) und dem Datumsmuster in Zeile 215.
    - `.planning/phases/12-stundenwechsel-bedienen/12-UI-SPEC.md`, Abschnitt "5. Sichtbarkeit im Kontoauszug (REQ-29)" — Label, Beschreibung, Badge-Farbe, zweite Beschreibungszeile, und die bekannte, akzeptierte Einschraenkung beim Monatsfilter.
    - `.planning/phases/12-stundenwechsel-bedienen/12-UI-SPEC.md`, Textbuch → "Kontoauszug (Überstunden-Journal)" — die drei Saetze woertlich; die Begruendung wird unveraendert durchgereicht, kein Trimmen, kein Umformatieren.
  </read_first>
  <action>
Drei neue `case`-Zweige in den bestehenden Zuordnungsfunktionen ergaenzen:
- `getTypeLabel`: `case 'model_change': return 'Modellwechsel';`
- `getTypeDescription`: `case 'model_change': return 'Saldodifferenz aus einer rückwirkenden
  Umstellung des Arbeitszeitmodells';`
- `getTypeBadgeColor`: `case 'model_change': return 'bg-teal-100 text-teal-700 dark:bg-teal-900/30
  dark:text-teal-300';`

`isAbsenceType` bleibt unberuehrt — die Zeile zeigt einen echten Stundenwert mit Vorzeichen,
Trendpfeil und `font-bold`, genau wie `earned` und `correction`.

Die Spalte **Datum** traegt den Stichtag der Periode (er steht bereits als `date` an der
Buchung). Weil die Liste nach `createdAt DESC, id DESC` sortiert ist (Phase-8-Entscheidung),
bekommt die Beschreibungszelle fuer diesen Typ eine zweite Zeile in
`text-xs text-gray-500 dark:text-gray-400`:
`Periode ab {TT.MM.JJJJ} · eingetragen am {TT.MM.JJJJ} von {Admin-Name}`. Die Datumsangaben
kommen ueber `new Date(iso + 'T12:00:00').toLocaleDateString('de-DE')`. Steht kein Admin-Name
zur Verfuegung, entfaellt der Namensteil samt "von", statt "unbekannt" zu behaupten.

Die Beschreibung selbst wird **unveraendert** durchgereicht — kein Trimmen des Inhalts, kein
Umformatieren von Datumsmustern darin. Das ist die Regel, die `VacationTransactions.tsx` in
`formatDescription()` ausdruecklich begruendet.

Kein bestehender Typ aendert sein Aussehen. Der vorbestehende `any`-Typ der Transaktionen in
dieser Datei ist eine Altlast und wird **nicht** angefasst (UI-SPEC, "Beobachtete, hier nicht
behobene Altlasten"); die neuen Zweige fuehren selbst kein `any` ein. Keine `console.*`.
  </action>
  <verify>
    <automated>cd desktop && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "model_change" desktop/src/components/worktime/OvertimeTransactions.tsx` liefert mindestens 3
    - `grep -c "Modellwechsel" desktop/src/components/worktime/OvertimeTransactions.tsx` liefert 1
    - `grep -c "Saldodifferenz aus einer rückwirkenden Umstellung des Arbeitszeitmodells" desktop/src/components/worktime/OvertimeTransactions.tsx` liefert 1
    - `grep -c "bg-teal-100 text-teal-700 dark:bg-teal-900/30 dark:text-teal-300" desktop/src/components/worktime/OvertimeTransactions.tsx` liefert 1
    - `grep -c "Periode ab" desktop/src/components/worktime/OvertimeTransactions.tsx` liefert 1
    - `grep -c "eingetragen am" desktop/src/components/worktime/OvertimeTransactions.tsx` liefert 1
    - `grep -c "toISOString" desktop/src/components/worktime/OvertimeTransactions.tsx` liefert 0
    - `grep -c "console\." desktop/src/components/worktime/OvertimeTransactions.tsx` liefert 0
    - Die Anzahl der `case '`-Vorkommen in `getTypeLabel` ist gegenueber dem Ausgangsstand um genau 1 gewachsen; dasselbe gilt fuer `getTypeDescription` und `getTypeBadgeColor`. Die drei Ausgangszahlen sind vor der Aenderung zu messen und im SUMMARY zu nennen.
    - `cd desktop && npx tsc --noEmit` endet mit Exitcode 0
  </acceptance_criteria>
  <done>Eine `model_change`-Buchung erscheint im Kontoauszug mit dem Badge "Modellwechsel" in Teal, Betrag mit Vorzeichen und Trendpfeil, unveraenderter Begruendung und einer zweiten Zeile mit Periodenbezug.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stammdatenformular → Server | "Änderungen speichern" sendet weiterhin `weeklyHours` und `workSchedule` mit |
| Serverdaten → Kontoauszug | Begruendung und Admin-Name sind Freitext aus der Datenbank |
| Browser-Konsole | Debugausgaben in einer Personalverwaltung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-34 | Tampering | Stiller Ueberschreiber des Arbeitszeitmodells | mitigate | Der Absende-Pfad sendet `weeklyHours`/`workSchedule` unveraendert aus `user`, nicht aus dem Formularzustand; die Felder sind zusaetzlich schreibgeschuetzt. Ohne beides koennte "Änderungen speichern" die soeben angelegte Periode wieder ueberschreiben. |
| T-12-35 | Information Disclosure | 14 `console.log` in zwei Dateien | mitigate | Ersatzlose Entfernung. Die Ausgaben schreiben Nutzer-Id, Name, Eintritts- und Austrittsdatum, E-Mail, Abteilung und Position in die Browser-Konsole. Belegt durch `grep -c "console.log"` = 0 in beiden Dateien. |
| T-12-36 | Tampering | Darstellung der Begruendung im Kontoauszug | mitigate | Ausgabe ausschliesslich als React-Textknoten; kein `dangerouslySetInnerHTML` in den geaenderten Dateien. |
| T-12-37 | Elevation of Privilege | Sichtbarkeit des Einstiegs-Buttons | transfer | Der Button steht im `EditUserModal`, das nur Admins erreichen; die verbindliche Durchsetzung liegt serverseitig an den Endpunkten (D6, Plan 12-05). Sichtbarkeit im Client ist keine Berechtigung. |
| T-12-38 | Denial of Service | Absenden des aeusseren Formulars durch einen Button des inneren Dialogs | mitigate | Renderort ausserhalb `</form>` (verbindlich, per Zeilennummernvergleich geprueft), `stopPropagation()` im inneren `onSubmit`, `type="button"` an jedem Button des Teilbaums. Ohne das schliesst ein Klick im Dialog das aeussere Modal und verwirft alle ungespeicherten Stammdaten. |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | Nicht anwendbar: keine neue Abhaengigkeit. |
</threat_model>

<verification>
1. `cd desktop && npx tsc --noEmit` — Exitcode 0.
2. `grep -c "console.log"` liefert 0 fuer `EditUserModal.tsx` und `UserManagementPage.tsx`; die Summe der in dieser Phase entfernten `console.log` betraegt zusammen mit Plan 12-02 genau 16.
3. Zeilennummernvergleich: `<WorkTimeChangeModal` steht nach `</form>` in `EditUserModal.tsx`.
4. Gegen den lokalen Dev-Server mit Admin-Session, protokolliert im SUMMARY: Nutzer bearbeiten → Feld Wochenstunden ist nicht editierbar, behaelt aber seinen sichtbaren Wert und ist per Tabulator erreichbar; Klick auf "Stundenwechsel ab Datum …" oeffnet den Dialog ueber dem Stammdatendialog; ein rueckwirkender Wechsel wird gespeichert; danach zeigt die Periodenliste zwei Zeilen, das schreibgeschuetzte Feld den neuen Wert, und der Kontoauszug eine Zeile mit dem Badge "Modellwechsel".
</verification>

<success_criteria>
- D1 ist umgesetzt: eigene Aktion, schreibgeschuetzte Stammdatenfelder mit Verweis.
- Der Dialog liegt ausserhalb des Formulars und ueber dem Stammdatendialog.
- Zustand 15 ist erreichbar: nach dem Speichern zeigen Liste und Felder den neuen Wert.
- REQ-29 ist sichtbar: eigene Zeile mit Badge, Betrag, Begruendung und Periodenbezug.
- 14 `console.log` entfernt, `console.error`/`console.warn` unangetastet.
- Kein `any` in neuem Code, `npx tsc --noEmit` gruen.
</success_criteria>

<output>
Erstelle `.planning/phases/12-stundenwechsel-bedienen/12-07-SUMMARY.md`.

Pflichtabschnitte:
- "Gezaehlte Debugausgaben" — wie viele `console.log` je Datei entfernt wurden (5 + 9), und die
  Bestaetigung, dass zusammen mit Plan 12-02 genau 16 Aufrufe verschwunden sind.
- "Nicht angefasste Altlasten" — die starren `grid-cols` in `EditUserModal.tsx` Zeile 197, 216,
  238, 280 und der `any`-Typ in `OvertimeTransactions.tsx`.
- "UAT-Punkte fuer Phase 14" — nummerierte Liste, je Punkt: was zu pruefen ist, wo, mit welchem
  erwarteten Ergebnis. Mindestens:
  1. Kontrast des schreibgeschuetzten Wochenstundenfelds im hellen und dunklen Modus:
     mindestens 4,5:1, Wert klar lesbar.
  2. Vollstaendiger Bedienfluss im Desktop: Nutzer bearbeiten → Stundenwechsel eintragen →
     Erfolgsbanner und Toast erscheinen → Periodenliste und Feld zeigen sofort den neuen Wert.
  3. Kontoauszug desselben Nutzers: die Zeile "Modellwechsel" steht mit Betrag, Vorzeichen,
     Trendpfeil, Begruendung und der zweiten Zeile mit Periodenbezug im Auszug.
  4. Bestehende Bedienwege der Nutzerverwaltung (Anlegen, Loeschen, Deaktivieren) verhalten sich
     unveraendert zu v1.8.0.
</output>
