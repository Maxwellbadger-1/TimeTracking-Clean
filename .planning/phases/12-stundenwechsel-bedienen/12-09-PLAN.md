---
phase: 12-stundenwechsel-bedienen
plan: 09
type: execute
wave: 5
depends_on: ["12-05", "12-07", "12-08"]
files_modified:
  - desktop/tests/user-edit.spec.ts
autonomous: true
requirements: [REQ-26, REQ-27, REQ-28, REQ-29]

must_haves:
  truths:
    - "Der E2E-Test 'Change employee to 0 hours' fuehrt die Umstellung ueber den neuen Wechsel-Dialog aus — fachlich ist die Aktion genau dorthin gewandert"
    - "D8: Diese Phase liefert Server- und Desktop-Code, aber kein Release; die Abnahme laeuft ueber den lokalen Dev-Server plus npm run sync-dev-db"
    - "npx tsc --noEmit ist in server/ und in desktop/ gruen — das Pflicht-Gate des Projekts"
    - "Die UAT-Punkte aller Plaene dieser Phase sind zu einer einzigen, nummerierten Liste fuer Phase 14 zusammengefasst"
    - "Kein Aufraeumfeldzug: die sieben verbleibenden kaputten aria-label-Selektoren in user-edit.spec.ts bleiben stehen und sind als eigener, vorbestehender Defekt vermerkt"
  artifacts:
    - path: "desktop/tests/user-edit.spec.ts"
      provides: "E2E-Weg der Nullstunden-Umstellung ueber den Wechsel-Dialog"
      contains: "Stundenwechsel"
  key_links:
    - from: "desktop/tests/user-edit.spec.ts"
      to: "desktop/src/components/worktime/WorkTimeChangeModal.tsx"
      via: "Bedienung des neuen Dialogs im Test"
      pattern: "Stundenwechsel"
---

<objective>
Der Abschluss der Phase: der eine E2E-Test, den der Schreibschutz bricht, wird auf den neuen
Bedienweg umgeschrieben; die Pflicht-Gates laufen ueber Server und Desktop; und die ueber die
Plaene verstreuten UAT-Punkte werden zu einer Liste zusammengezogen, mit der Phase 14 arbeiten
kann.

Zweck: Ohne diesen Plan bliebe die Phase mit einem rot gewordenen Bestandstest und mit acht
einzelnen SUMMARY-Listen zurueck, die niemand zusammensucht.
Ausgabe: umgeschriebener E2E-Test, Gate-Protokoll, gebuendelte UAT-Liste.

Diese Phase veroeffentlicht **kein** Release (D8). Die Desktop-Aenderungen erreichen die
Anwender erst mit dem Release in Phase 14.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/12-stundenwechsel-bedienen/12-UI-SPEC.md
@.planning/phases/12-stundenwechsel-bedienen/12-05-SUMMARY.md
@.planning/phases/12-stundenwechsel-bedienen/12-07-SUMMARY.md
@.planning/phases/12-stundenwechsel-bedienen/12-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Den einen E2E-Test auf den neuen Bedienweg umschreiben</name>
  <files>desktop/tests/user-edit.spec.ts</files>
  <read_first>
    - `desktop/tests/user-edit.spec.ts` Zeile 85-115 — der Test "Change employee to 0 hours (critical bug fix test)": Zeile 101 nutzt den Selektor `button[aria-label="Bearbeiten"]`, Zeile 102 wartet auf `text=Benutzer bearbeiten:`, Zeile 104 fuellt `[name="weeklyHours"]` mit `'0'`.
    - `.planning/phases/12-stundenwechsel-bedienen/12-UI-SPEC.md`, Aenderungstabelle D — die abschliessende Festlegung: genau dieser eine Test wird umgeschrieben, und nur in ihm wird der kaputte Selektor repariert.
    - `.planning/phases/12-stundenwechsel-bedienen/12-UI-SPEC.md`, Abschnitt "Geprueftier Umfang (21.08.2026)" unmittelbar danach — der Nachweis, dass die sieben weiteren `weeklyHours`-Fuellungen im `CreateUserModal` liegen und unveraendert bleiben, und dass der Checkbox-Zweig ein toter Zweig ist.
    - `.planning/phases/12-stundenwechsel-bedienen/12-UI-SPEC.md`, Textbuch → "Wechsel-Dialog" — die Beschriftungen, ueber die der Test den Dialog bedient (`Stundenwechsel ab Datum …`, `Stichtag`, `Neue Wochenstunden`, `Begründung`, `Stundenwechsel speichern` bzw. `Rückwirkend speichern`).
    - `desktop/src/components/users/EditUserModal.tsx` in der Fassung aus Plan 12-07 — welche Beschriftungen tatsaechlich im DOM stehen.
  </read_first>
  <action>
Genau **einen** Test in `desktop/tests/user-edit.spec.ts` umschreiben: "Change employee to 0
hours (critical bug fix test)" (Zeile 85-115).

Zwei Aenderungen in diesem einen Test:
1. Der Selektor `button[aria-label="Bearbeiten"]` (Zeile 101) wird durch
   `button:has-text("Bearbeiten")` ersetzt. Das Attribut `aria-label` existiert nicht —
   `UserManagementPage.tsx` Zeile 461-467 rendert `<Button size="sm"
   variant="ghost">Bearbeiten</Button>` ohne `aria-label`. Ohne diesen Ersatz scheitert der
   Test bereits vor der eigentlichen Zusicherung.
2. Die Umstellung auf 0 h laeuft nicht mehr ueber `page.fill('[name="weeklyHours"]', '0')` im
   Bearbeiten-Dialog — das Feld ist dort seit Plan 12-07 `readOnly` und `page.fill()` schlaegt
   hart fehl. Stattdessen bedient der Test den neuen Weg:
   - Klick auf `button:has-text("Stundenwechsel ab Datum")`.
   - Warten auf den Dialogtitel `Stundenwechsel:`.
   - Stichtag setzen (ein Datum in der Zukunft, damit kein Bestaetigungsschritt noetig ist und
     der Test schnell und deterministisch bleibt).
   - `Neue Wochenstunden` auf `0` setzen.
   - Eine Begruendung mit mindestens 10 Zeichen eintragen.
   - Warten, bis der Primaerbutton `Stundenwechsel speichern` bedienbar ist (er wird erst durch
     die eingetroffene Vorschau freigeschaltet — der Test wartet auf den freigeschalteten
     Zustand, nicht auf eine feste Zeitspanne).
   - Speichern und pruefen, dass die Periodenliste im `EditUserModal` danach eine Zeile mit
     `0,0 h` bzw. dem im Projekt verwendeten Format enthaelt.

Die fachliche Absicht des Tests bleibt unveraendert: ein Mitarbeiter kann auf 0 Stunden gesetzt
werden. Nur der Bedienweg ist ein anderer.

**Kein Aufraeumfeldzug.** Die sieben weiteren Vorkommen von `button[aria-label="Bearbeiten"]`
in derselben Datei bleiben unangetastet und werden im SUMMARY als eigener, vorbestehender
Defekt vermerkt. Die sieben `weeklyHours`-Fuellungen im `CreateUserModal` (Zeile 32, 64, 94,
125, 172, 211, 243) bleiben ebenfalls unveraendert — dort ist das Feld weiterhin bedienbar.
  </action>
  <verify>
    <automated>cd desktop && npx tsc --noEmit && grep -c "Stundenwechsel" tests/user-edit.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Stundenwechsel" desktop/tests/user-edit.spec.ts` liefert mindestens 3
    - `grep -c "button\[aria-label=\"Bearbeiten\"\]" desktop/tests/user-edit.spec.ts` liefert genau 7 (eines von acht ist ersetzt, die uebrigen bleiben stehen)
    - `grep -c "button:has-text(\"Bearbeiten\")" desktop/tests/user-edit.spec.ts` liefert 1
    - `grep -c "\[name=\"weeklyHours\"\]" desktop/tests/user-edit.spec.ts` liefert genau 7 (die Fuellung im Bearbeiten-Dialog ist entfernt, die sieben im Anlege-Dialog bleiben)
    - `grep -c "waitForTimeout" desktop/tests/user-edit.spec.ts` ist gegenueber dem Ausgangsstand nicht gewachsen — der Test wartet auf Zustaende, nicht auf Zeit. Die Ausgangszahl ist vor der Aenderung zu messen und im SUMMARY zu nennen.
    - Der umgeschriebene Test laeuft gegen den lokalen Dev-Server durch, oder — falls die E2E-Umgebung lokal nicht startet — ist das im SUMMARY mit der genauen Fehlermeldung dokumentiert und als UAT-Punkt fuer Phase 14 eingetragen. Ein stillschweigendes Ueberspringen ist nicht zulaessig.
    - `cd desktop && npx tsc --noEmit` endet mit Exitcode 0
  </acceptance_criteria>
  <done>Der eine betroffene E2E-Test bedient den neuen Wechsel-Dialog, sein kaputter Selektor ist repariert, und die uebrigen sieben Vorkommen sind unangetastet und vermerkt.</done>
</task>

<task type="auto">
  <name>Task 2: Pflicht-Gates fahren und die UAT-Liste fuer Phase 14 buendeln</name>
  <files>desktop/tests/user-edit.spec.ts</files>
  <read_first>
    - `.claude/CLAUDE.md`, Abschnitt "QUALITY GATES → Pre-Commit Checklist" — `npx tsc --noEmit`, kein `any`, keine Debug-`console.log`, Prepared Statements, Dark Mode, Loading/Error States.
    - `.planning/phases/12-stundenwechsel-bedienen/12-UI-SPEC.md`, Abschnitt "Debugausgaben — abschliessende Regelung" — die Zielzahl 16 entfernter `console.log` in genau drei Dateien.
    - Die SUMMARY-Dateien aller bisherigen Plaene dieser Phase (`12-01` bis `12-08`), jeweils der Abschnitt "UAT-Punkte fuer Phase 14" — sie sind die Quelle der zu buendelnden Liste.
    - `.planning/ROADMAP.md`, Abschnitt "Phase 12" — die vier Erfolgskriterien, gegen die abschliessend abgeglichen wird.
  </read_first>
  <action>
Diese Task aendert keinen Produktivcode. Sie fuehrt die Gates aus und schreibt das Ergebnis
fort.

1. **TypeScript-Gate:** `cd server && npx tsc --noEmit` und `cd desktop && npx tsc --noEmit`.
   Beide muessen Exitcode 0 liefern. Die Ausgaben kommen woertlich ins SUMMARY.
2. **Testgate Server:** `cd server && npm test`. Das Ergebnis wird gegen den im SUMMARY von
   Plan 12-01 festgehaltenen Ausgangsstand gestellt: es darf kein Test rot werden, der vorher
   gruen war. Neue rote Tests sind ein blockierender Befund.
3. **Testgate Desktop:** die in dieser Phase angelegten Testdateien
   (`modalStack`, `timeUtils.periods`, ggf. weitere) laufen und sind gruen.
4. **Debugausgaben-Gate:** `grep -rn "console.log" desktop/src/components/ui/ConfirmDialog.tsx
   desktop/src/components/users/EditUserModal.tsx desktop/src/pages/UserManagementPage.tsx`
   liefert keinen Treffer. Zusaetzlich wird belegt, dass in `Modal.tsx`,
   `WorkScheduleEditor.tsx` und `OvertimeTransactions.tsx` weiterhin keine `console.log`
   stehen, und dass `console.error`/`console.warn` an ihren drei bekannten Stellen erhalten
   geblieben sind.
5. **`any`-Gate fuer neuen Code:** fuer jede in dieser Phase neu angelegte Datei liefert
   `grep -c ": any"` den Wert 0. Die Liste der geprueften Dateien kommt ins SUMMARY.
6. **Abgleich gegen die vier Erfolgskriterien der ROADMAP:** je Kriterium eine Zeile mit dem
   Beleg (Testname aus `workPeriodChangeService.test.ts` bzw. die protokollierte Beobachtung
   aus dem jeweiligen Plan). Ein Kriterium ohne Beleg ist ein blockierender Befund.
7. **UAT-Liste buendeln:** Alle Abschnitte "UAT-Punkte fuer Phase 14" aus den SUMMARYs der
   Plaene 12-01 bis 12-08 werden zu einer einzigen, durchnummerierten Liste im SUMMARY dieses
   Plans zusammengezogen. Je Punkt: was zu pruefen ist, wo (Bildschirm, Endpunkt, Datei), mit
   welchem erwarteten Ergebnis, und aus welchem Plan er stammt. Doppelungen werden
   zusammengefasst, nichts wird weggelassen.
8. **D8 festhalten:** Ein eigener Abschnitt haelt fest, dass diese Phase kein Release
   veroeffentlicht, dass die Abnahme ueber den lokalen Dev-Server plus `npm run sync-dev-db`
   laeuft, und dass die Desktop-Aenderungen die Anwender erst mit dem Release in Phase 14
   erreichen.
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit && cd ../desktop && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && npx tsc --noEmit` endet mit Exitcode 0
    - `cd desktop && npx tsc --noEmit` endet mit Exitcode 0
    - `cd server && npm test` bringt keinen zuvor gruenen Test zu Fall; der Vergleich zum Ausgangsstand steht im SUMMARY
    - `grep -rn "console.log" desktop/src/components/ui/ConfirmDialog.tsx desktop/src/components/users/EditUserModal.tsx desktop/src/pages/UserManagementPage.tsx` liefert 0 Treffer
    - Die Summe der in dieser Phase entfernten `console.log` betraegt 16 und ist im SUMMARY nach Datei aufgeschluesselt (2 + 5 + 9)
    - Fuer jede in dieser Phase neu angelegte Datei liefert `grep -c ": any"` den Wert 0; die geprueften Dateien sind im SUMMARY namentlich aufgefuehrt
    - Die vier Erfolgskriterien der ROADMAP sind je mit einem konkreten Beleg (Testname oder protokollierte Beobachtung) hinterlegt
    - Die gebuendelte UAT-Liste enthaelt mindestens einen Punkt aus jedem der Plaene 12-01 bis 12-08 und ist durchnummeriert
  </acceptance_criteria>
  <done>Beide TypeScript-Gates sind gruen, kein Test ist neu rot, die 16 Debugausgaben sind belegt entfernt, jedes Erfolgskriterium hat einen Beleg, und Phase 14 hat eine einzige, vollstaendige UAT-Liste.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E-Test → laufende Anwendung | Der Test legt echte Nutzer an und veraendert deren Arbeitszeitmodell |
| Gate-Protokoll → Freigabeentscheidung | Auf diesen Zahlen beruht die Aussage, dass die Phase fertig ist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-44 | Tampering | E2E-Test gegen eine Datenbank mit echten Daten | mitigate | Der Test laeuft ausschliesslich gegen den lokalen Dev-Server und seine Arbeitsdatenbank; er legt seinen Nutzer selbst an, statt einen bestehenden zu veraendern. Kein Lauf gegen die Produktionsdatenbank, kein gesetztes `DATABASE_PATH` auf den Produktionspfad. |
| T-12-45 | Repudiation | Unbelegtes "fertig" | mitigate | Jedes der vier ROADMAP-Erfolgskriterien bekommt einen benannten Beleg; ein Kriterium ohne Beleg ist ein blockierender Befund. Ausgangs- und Endstand der Testlaeufe werden gegenuebergestellt statt behauptet. |
| T-12-46 | Information Disclosure | Verbliebene Debugausgaben | mitigate | Abschliessendes Gate ueber die drei betroffenen Dateien; die Zielzahl 16 wird nach Datei aufgeschluesselt belegt. |
| T-12-47 | Denial of Service | Zeitbasierte Wartezeiten im E2E-Test | mitigate | Der Test wartet auf Zustaende (freigeschalteter Primaerbutton, sichtbarer Dialogtitel), nicht auf feste Zeitspannen; `waitForTimeout` waechst gegenueber dem Ausgangsstand nicht. |
| T-12-48 | Elevation of Privilege | Release ohne Freigabe | mitigate | D8: Diese Phase veroeffentlicht ausdruecklich kein Release, setzt keinen Tag und bumpt keine Version. Das Gate dafuer liegt in Phase 14. |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | Nicht anwendbar: keine neue Abhaengigkeit in der gesamten Phase. Sollte beim Ausfuehren dennoch ein Paket noetig werden, ist der Legitimitaets-Gate mit blockierender Pruefung faellig und die Phase bis dahin anzuhalten. |
</threat_model>

<verification>
1. `cd server && npx tsc --noEmit` und `cd desktop && npx tsc --noEmit` — beide Exitcode 0.
2. `cd server && npm test` — kein zuvor gruener Test ist rot.
3. Die in dieser Phase angelegten Desktop-Tests laufen gruen.
4. `grep -rn "console.log"` ueber die drei betroffenen Dateien liefert keinen Treffer.
5. Der umgeschriebene E2E-Test laeuft durch oder sein Fehlschlag ist mit Meldung dokumentiert und als UAT-Punkt eingetragen.
6. Die gebuendelte UAT-Liste ist vollstaendig und durchnummeriert.
</verification>

<success_criteria>
- Der eine betroffene E2E-Test bedient den neuen Weg; die uebrigen sieben Selektorfehler bleiben als eigener Defekt vermerkt.
- Beide TypeScript-Gates gruen, keine neuen roten Tests.
- 16 `console.log` belegt entfernt, `console.error`/`console.warn` erhalten.
- Jedes der vier ROADMAP-Erfolgskriterien hat einen benannten Beleg.
- Eine einzige, vollstaendige UAT-Liste fuer Phase 14 liegt vor.
- Kein Release, kein Versionssprung, kein Tag (D8).
</success_criteria>

<output>
Erstelle `.planning/phases/12-stundenwechsel-bedienen/12-09-SUMMARY.md`.

Pflichtabschnitte:
- "Gate-Protokoll" — die Ausgaben der beiden `tsc`-Laeufe, der Testlaeufe und der Grep-Gates.
- "Abgleich gegen die ROADMAP-Erfolgskriterien" — vier Zeilen, je mit Beleg.
- "Vorbestehende, nicht behobene Defekte" — die sieben `button[aria-label="Bearbeiten"]`-Selektoren
  in `user-edit.spec.ts`, die starren `grid-cols` in `EditUserModal.tsx`, der `any`-Typ in
  `OvertimeTransactions.tsx`, die fehlenden `balanceBefore`/`balanceAfter` im `CREATE TABLE`
  von `schema.ts`, und die weiterhin offene vollstaendige Aufloesung der Client-Kopie aus WR-12.
- "D8 — kein Release in dieser Phase" — Abnahmeweg ueber lokalen Dev-Server plus
  `npm run sync-dev-db`; Release erfolgt in Phase 14.
- "UAT-Punkte fuer Phase 14" — die gebuendelte, durchnummerierte Gesamtliste aus allen Plaenen
  dieser Phase. Je Punkt: was zu pruefen ist, wo, erwartetes Ergebnis, Herkunftsplan.
</output>
