---
phase: 06-buchungen-bei-jedem-vorgang
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/services/vacationBalanceService.ts
  - server/src/services/absenceService.ts
  - server/src/services/vacationCarryoverCalculation.test.ts
  - desktop/src/components/vacation/VacationBalanceEditModal.tsx
autonomous: true
gap_closure: true
requirements:
  - REQ-07 Anspruch und Übertrag bei Nutzeranlage und Jahreswechsel werden gebucht
must_haves:
  truths:
    - "Alle vier Stellen, die einen Übertrag berechnen (upsertVacationBalance, updateVacationBalance, bulkInitializeVacationBalances, absenceService.initializeVacationBalance), liefern für denselben Vorjahresrest denselben Wert — auch wenn der Rest mehr als 5 Tage beträgt"
    - "Ein Admin kann per upsertVacationBalance/updateVacationBalance einen Übertrag von mehr als 10 Tagen setzen, solange er den tatsächlichen Vorjahresrest nicht überschreitet — die bisherige feste 10-Tage-Grenze existiert nicht mehr"
    - "Kein Code-Kommentar oder Fehlertext behauptet mehr eine gesetzliche 5-Tage-Grenze — diese Regel existierte nur im Code, nie als tatsächliche fachliche Vorgabe"
    - "Das Admin-Korrekturformular (VacationBalanceEditModal.tsx) lehnt einen vom Server akzeptierten Übertrag (z. B. 20 oder 30 Tage) nicht mehr client-seitig ab — Oberfläche und Server vertreten dieselbe Regel"
  artifacts:
    - path: "server/src/services/vacationBalanceService.ts"
      provides: "calculateCarryover(previousBalance) als einzige Berechnungsquelle der Übertragsregel; genutzt von upsertVacationBalance, updateVacationBalance und bulkInitializeVacationBalances"
    - path: "server/src/services/absenceService.ts"
      provides: "initializeVacationBalance() importiert und nutzt calculateCarryover() aus vacationBalanceService.ts statt einer eigenen Math.min(...)-Berechnung"
    - path: "server/src/services/vacationCarryoverCalculation.test.ts"
      provides: "Regressionstests: alle vier Pfade liefern für denselben Vorjahresrest denselben Übertrag, insbesondere über 5 Tage hinaus; Unit-Tests für calculateCarryover()"
    - path: "desktop/src/components/vacation/VacationBalanceEditModal.tsx"
      provides: "Client-seitige Übertrags-Validierung ohne feste Obergrenze — nur noch nicht-negativ, konsistent mit der Server-Regel aus calculateCarryover()"
  key_links:
    - from: "absenceService.ts initializeVacationBalance"
      to: "vacationBalanceService.ts calculateCarryover"
      via: "import { calculateCarryover } from './vacationBalanceService.js'"
      pattern: "calculateCarryover\\("
    - from: "vacationBalanceService.ts upsertVacationBalance / updateVacationBalance / bulkInitializeVacationBalances"
      to: "vacationBalanceService.ts calculateCarryover"
      via: "direkter Funktionsaufruf innerhalb derselben Datei"
      pattern: "calculateCarryover\\("
    - from: "desktop VacationBalanceEditModal.tsx validateForm()"
      to: "Server-Regel (kein fester Deckel, nur der tatsächliche Vorjahresrest als Obergrenze)"
      via: "keine feste `> 10`-Bedingung mehr im Frontend"
      pattern: "carryoverNum < 0"
---

<objective>
Schließt einen Blocker, den der Plan-Checker beim Review von 06-04/06-05 gefunden hat, und
löst zugleich **WR-03** aus `06-REVIEW.md` (bereits vor diesem Gap-Closure-Zyklus dokumentiert,
aber noch nicht behoben).

**Der Blocker:** Der Code enthält vier Stellen, die einen Übertrag ins Folgejahr berechnen — mit
zwei widersprüchlichen Regeln, unabhängig gegen den Code verifiziert:

| Funktion | Datei:Zeile | Regel |
|---|---|---|
| `upsertVacationBalance()` (Validierungs-Obergrenze) | `vacationBalanceService.ts:220` | `Math.min(remaining, 5)` |
| `updateVacationBalance()` (Validierungs-Obergrenze) | `vacationBalanceService.ts:485` | `Math.min(remaining, 5)` |
| `absenceService.initializeVacationBalance()` | `absenceService.ts:1402` | `Math.min(remaining, 5)` |
| `bulkInitializeVacationBalances()` (Jahreswechsel-Pfad) | `vacationBalanceService.ts:713` | `previousBalance.remaining` — **unbegrenzt** |

Zusätzlich lehnen `upsertVacationBalance()` und `updateVacationBalance()` jeden Übertrag über 10
Tagen bereits vor dieser Prüfung hart ab (`vacationBalanceService.ts:210`, `:475`:
`data.carryover > 10` → Error).

**Warum das 06-04 und 06-05 blockiert hätte:** 06-04 lässt `initializeVacationBalance()` an
`upsertVacationBalance()` delegieren — die deckelt auf 5. 06-05 sichert die Nachbuchung beim
Jahreswechsel mit einer reinen Existenzprüfung ab (`COUNT(*) ... type = 'carryover'`). Beantragt
jemand im Dezember Urlaub für Januar, bucht der Auto-Init-Pfad 5 Tage Übertrag; der spätere echte
Jahreswechsel findet eine vorhandene `carryover`-Buchung und tut nichts mehr. Hatte der
Mitarbeiter 20 Resttage, blieben ihm dauerhaft 5 — exakt der Schaden, den Gap 2 (06-05)
verhindern soll, nur zementiert statt offensichtlich unbehoben.

**Anwenderentscheidung (bindend für diesen Plan):** Übertrag ist **unbegrenzt**. Resturlaub geht
vollständig ins Folgejahr, kein Verfall, keine Deckelung. `bulkInitializeVacationBalances()`
(bereits unbegrenzt) ist damit die fachlich korrekte Referenz — die drei gedeckelten Stellen
werden darauf angeglichen. Die Kommentare „Max 5 days per German law" bzw. „Max 5 days
carryover" sind falsch und werden entfernt.

**Nachtrag des Koordinators — dieselbe Grenze steht auch im Frontend:** Dieselbe feste
10-Tage-Grenze, die im Server entfernt wird, ist in
`desktop/src/components/vacation/VacationBalanceEditModal.tsx` dupliziert
(`carryoverNum > 10` in `validateForm()`, plus `max="10"` auf dem HTML-Eingabefeld). Ohne diesen
Nachtrag würde der Server nach diesem Plan einen unbegrenzten Übertrag akzeptieren, während das
Admin-Korrekturformular in der Oberfläche weiterhin alles über 10 Tagen ablehnt — genau der
Widerspruch zwischen zwei unabhängigen Implementierungen derselben Regel, den dieser Plan
eigentlich beseitigen soll, nur diesmal zwischen Server und Client statt zwischen vier
Server-Funktionen. Task 3 gleicht das an.

**Ausdrücklich NICHT Teil dieses Plans:** Der bestehende, gedeckelte 5-Tage-Übertrag des
System-Administrators (userId 1, Jahr 2026, erzeugt vom Phase-7-Backfill, siehe
`06-05-PLAN.md`) bleibt unverändert. Ob dieser Bestandswert auf den tatsächlichen Rest (30 Tage)
korrigiert wird, entscheidet der Anwender separat — dieser Plan ändert keine bestehenden
Datensätze und enthält kein Korrekturskript.

Purpose: Die Übertragsregel dieser Phase darf nicht mehr an vier Server-Funktionen und einem
Frontend-Formular unabhängig voneinander implementiert sein — ein Widerspruch an dieser Stelle
kann sonst jederzeit wieder entstehen und den mühsam behobenen Gap 2 (06-05) im Alltag
unterlaufen, ohne dass ein Test es bemerkt.

Output: `calculateCarryover(previousBalance)` in `vacationBalanceService.ts` als einzige
Berechnungsquelle; alle vier Server-Aufrufstellen nutzen sie; das Admin-Korrekturformular im
Frontend validiert dieselbe Regel (keine feste Obergrenze mehr); neuer Regressionstest, der alle
vier Server-Pfade gegeneinander prüft.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-buchungen-bei-jedem-vorgang/06-VERIFICATION.md
@.planning/phases/06-buchungen-bei-jedem-vorgang/06-REVIEW.md

Dieser Plan läuft in **Welle 1**, vor 06-04 und 06-05 (beide hängen jetzt von diesem Plan ab,
`depends_on: ["06-06"]`), weil er dieselben zwei Server-Dateien anfasst, die 06-04 und 06-05
ebenfalls ändern. Nach diesem Plan liegen die Zeilennummern in `vacationBalanceService.ts` und
`absenceService.ts` anders als in den ursprünglich verifizierten Fundstellen — 06-04 und 06-05
verweisen deshalb nicht mehr auf feste Zeilennummern, sondern auf Funktionsnamen/Grep-Muster.
Die Frontend-Datei (`VacationBalanceEditModal.tsx`) fasst kein anderer Plan dieser Phase an —
Task 3 hat keine Wellen-Abhängigkeit innerhalb dieses Plans.

**Verifiziert gegen die aktuelle Entwicklungsdatenbank** (`server/database/development.db`,
`node -e "new (require('better-sqlite3'))(...)"`, 2026-08-21):
- `vacation_balance` für `userId=1, year=2025`: `remaining=30`.
- `vacation_balance` für `userId=1, year=2026`: `carryover=5` (gedeckelt aus den 30 Resttagen
  von 2025 — exakt das Muster, das dieser Plan korrigiert, aber an bestehenden Daten NICHT
  anfasst).
- Genau eine `carryover`-Buchung existiert systemweit: `userId=1`, `days=5`,
  `description="Übertrag aus 2025 (rückwirkend erzeugt)"`.

**Verifiziert gegen `desktop/src/components/vacation/VacationBalanceEditModal.tsx`:**
`validateForm()` enthält `if (isNaN(carryoverNum) || carryoverNum < 0 || carryoverNum > 10) {
setCarryoverError('Übertrag muss zwischen 0 und 10 Tagen liegen'); }`. Das zugehörige `<Input
type="number" ... min="0" max="10" step="1" helperText="Übertrag aus Vorjahr (max. 5 Tage
automatisch)" />` trägt zusätzlich ein `max="10"`-Attribut (begrenzt die Browser-Spinner-Pfeile)
und einen `helperText`, der eine falsche, nicht mehr geltende Zahl (5) nennt und fälschlich
"automatisch" behauptet, obwohl dieses Feld ein manuelles Admin-Korrekturfeld ist.

**Zur Einordnung geprüft, nicht betroffen:** `desktop/src/pages/VacationBalanceManagementPage.tsx`
enthält `balance.remaining > 10` (Zeile ~187) — das ist ausschließlich eine Farbschwelle für die
Übersichtstabelle (grün/gelb/rot je nach verbleibenden Resttagen), keine Validierung eines
Eingabewerts und keine Kopie der Carryover-Regel. Diese Datei ist NICHT Teil von
`files_modified` dieses Plans und wird nicht angefasst.

<interfaces>
Aus `server/src/services/vacationBalanceService.ts` (bereits exportiert, für Task 1 relevant):

```typescript
export interface VacationBalance {
  id: number; userId: number; year: number;
  entitlement: number; carryover: number; taken: number; remaining: number;
}
export function getVacationBalance(userId: number, year: number): VacationBalance | null;
export function upsertVacationBalance(data: VacationBalanceCreateInput): VacationBalance;
export function updateVacationBalance(id: number, data: VacationBalanceUpdateInput): VacationBalance;
export function bulkInitializeVacationBalances(year: number, createdBy?: number | null): number;
```

Aus `server/src/services/absenceService.ts` (lokale, strukturell kompatible `VacationBalance`
mit denselben Feldern plus zusätzlichem `pending: number` — TypeScript prüft Variablen
strukturell, ein zusätzliches Feld verhindert die Zuweisung an einen enger typisierten Parameter
nicht):

```typescript
export function initializeVacationBalance(userId: number, year: number): VacationBalance; // lokaler Typ
export function getVacationBalance(userId: number, year: number): VacationBalance | null; // lokale Funktion, unverändert
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: calculateCarryover() als einzige Berechnungsquelle einführen und alle vier Aufrufstellen umstellen</name>
  <files>server/src/services/vacationBalanceService.ts, server/src/services/absenceService.ts</files>

  <read_first>
```bash
sed -n '1,12p' server/src/services/absenceService.ts
grep -n "export interface VacationBalanceUpdateInput" -A 10 server/src/services/vacationBalanceService.ts
grep -n "export function upsertVacationBalance" -A 40 server/src/services/vacationBalanceService.ts
grep -n "export function updateVacationBalance" -A 45 server/src/services/vacationBalanceService.ts
grep -n "export function bulkInitializeVacationBalances" -A 30 server/src/services/vacationBalanceService.ts
grep -n "export function initializeVacationBalance" -A 25 server/src/services/absenceService.ts
```
  </read_first>

  <behavior>
    `calculateCarryover(previousBalance: { remaining: number } | null): number` — neue,
    exportierte Funktion in `vacationBalanceService.ts`:
    - `calculateCarryover(null)` → `0`
    - `calculateCarryover({ remaining: 0 })` → `0`
    - `calculateCarryover({ remaining: -3 })` → `0` (defensiv — ein negativer Rest darf nie
      einen negativen Übertrag erzeugen, auch wenn dieser Fall in der Praxis nicht vorkommen
      sollte)
    - `calculateCarryover({ remaining: 3 })` → `3`
    - `calculateCarryover({ remaining: 20 })` → `20` — **nicht** gekappt auf 5, das ist der Kern
      dieses Fixes
  </behavior>

  <action>
In `server/src/services/vacationBalanceService.ts` direkt nach der schließenden Klammer des
`VacationBalanceUpdateInput`-Interface und vor dem Kommentar „Get all vacation balances" die neue
Funktion `calculateCarryover` einfügen (siehe `<behavior>` für die exakte Logik: `previousBalance
&& previousBalance.remaining > 0 ? previousBalance.remaining : 0`). JSDoc ergänzen, die
festhält: (1) dies ist die EINZIGE Stelle, die die Übertragsregel kennt — alle vier Aufrufer
unten nutzen ausschließlich diese Funktion, (2) Anwenderentscheidung: Übertrag ist unbegrenzt,
kein Verfall, (3) Verweis auf `06-REVIEW.md` WR-03 und `06-VERIFICATION.md` (Interaktionsanalyse
zwischen Gap 1/Gap 2), damit der Widerspruch nachvollziehbar bleibt, falls er je wieder
auftaucht.

**`upsertVacationBalance()` umstellen:** Die Zeile `if (data.carryover < 0 || data.carryover >
10) { throw new Error('Carryover must be between 0 and 10 days'); }` durch `if (data.carryover <
0) { throw new Error('Carryover must be 0 or greater'); }` ersetzen — die feste 10-Tage-Grenze
entfällt, negative Werte bleiben abgelehnt. In der nachfolgenden
Vorjahresrest-Prüfung `const maxCarryover = Math.min(previousBalance.remaining, 5); // Max 5 days
per German law` durch `const maxCarryover = calculateCarryover(previousBalance);` ersetzen (ohne
den Kommentar — der ist falsch). In der zugehörigen Fehlermeldung den Teilstring `` `German law
limit: 5 days)` `` entfernen (Meldung endet dann nach der `remaining`-Angabe mit `` `)` ``).

**`updateVacationBalance()` umstellen:** identische Änderung — dieselbe `> 10`-Grenze, dieselbe
`Math.min(previousBalance.remaining, 5)`-Zeile mit demselben falschen Kommentar, dieselbe
Fehlermeldung mit demselben `German law limit`-Textbaustein existieren dort ein zweites Mal
(Duplikat von `upsertVacationBalance`, nicht per Funktion geteilt) — beide Stellen exakt
analog anpassen.

**`bulkInitializeVacationBalances()` umstellen:** Den Block
```
const previousYear = year - 1;
const previousBalance = getVacationBalance(user.id, previousYear);
const carryover =
  previousBalance && previousBalance.remaining > 0
    ? previousBalance.remaining // Transfer ALL days
    : 0;
```
durch
```
const previousYear = year - 1;
const previousBalance = getVacationBalance(user.id, previousYear);
const carryover = calculateCarryover(previousBalance);
```
ersetzen (Kommentarzeile „PROFESSIONAL STANDARD: Transfer ALL remaining vacation days
(unlimited!)" oberhalb bleibt stehen — sie beschreibt die jetzt überall geltende Regel korrekt).

**`absenceService.ts` `initializeVacationBalance()` umstellen:** Neue Import-Zeile am Dateikopf
ergänzen: `import { calculateCarryover } from './vacationBalanceService.js';` (neben dem
bestehenden Import von `recordVacationTransaction`). Den Block
```
const previousYear = year - 1;
const previousBalance = getVacationBalance(userId, previousYear);
const carryover =
  previousBalance && previousBalance.remaining > 0
    ? Math.min(previousBalance.remaining, 5) // Max 5 days carryover
    : 0;
```
durch
```
const previousYear = year - 1;
const previousBalance = getVacationBalance(userId, previousYear);
const carryover = calculateCarryover(previousBalance);
```
ersetzen. Die rohe `INSERT ... ON CONFLICT ... DO UPDATE`-Query direkt darunter bleibt in diesem
Plan unverändert — sie an `upsertVacationBalance()` delegieren zu lassen ist Aufgabe von 06-04
(Welle 2, hängt von diesem Plan ab), nicht dieses Plans. Dieser Plan korrigiert ausschließlich
die Zahl, nicht den Buchungsweg.
  </action>

  <verify>
    <automated>cd server && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
- `grep -c "calculateCarryover(" server/src/services/vacationBalanceService.ts` liefert
  mindestens 4 (Definition + Aufruf in `upsertVacationBalance`, `updateVacationBalance`,
  `bulkInitializeVacationBalances`).
- `grep -c "calculateCarryover" server/src/services/absenceService.ts` liefert genau 2 (Import
  + Aufruf in `initializeVacationBalance`).
- `grep -c "Math.min(previousBalance.remaining, 5)" server/src/services/vacationBalanceService.ts
  server/src/services/absenceService.ts` liefert in Summe 0 — keine der drei gekappten Stellen
  existiert mehr wörtlich.
- `grep -c "German law limit: 5 days" server/src/services/vacationBalanceService.ts` liefert 0.
- `grep -c "carryover > 10" server/src/services/vacationBalanceService.ts` liefert 0 — die feste
  Obergrenze ist entfernt.
- `npx tsc --noEmit` läuft ohne Fehler (keine Typkollision zwischen der lokalen `VacationBalance`
  in `absenceService.ts` und dem Parametertyp von `calculateCarryover`).
  </acceptance_criteria>

  <done>calculateCarryover() ist die einzige Stelle, die die Übertragsregel berechnet; alle vier Aufrufstellen (upsertVacationBalance, updateVacationBalance, bulkInitializeVacationBalances, absenceService.initializeVacationBalance) nutzen sie; kein Kommentar oder Fehlertext behauptet mehr eine 5- oder 10-Tage-Grenze; npx tsc --noEmit fehlerfrei.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regressionstest — alle vier Pfade liefern denselben Übertrag über 5 Tage hinaus</name>
  <files>server/src/services/vacationCarryoverCalculation.test.ts</files>

  <read_first>
```bash
sed -n '1,70p' server/src/services/vacationEntitlementBooking.test.ts
grep -n "2031\|2032\|2033\|2034\|2035" server/src/services/*.test.ts
```
Signaturen (alle bereits exportiert, keine Quelländerung nötig): `calculateCarryover(previousBalance)`,
`upsertVacationBalance(data)`, `updateVacationBalance(id, data)`,
`bulkInitializeVacationBalances(year, createdBy?)`, `getVacationBalance(userId, year)` — alle
aus `vacationBalanceService.ts`; `initializeVacationBalance(userId, year)` aus
`absenceService.ts`.
  </read_first>

  <behavior>
    **Unit-Tests (ohne DB), direkt gegen `calculateCarryover`:**
    - `calculateCarryover(null)` → `0`
    - `calculateCarryover({ remaining: 0 } as VacationBalance)` → `0`
    - `calculateCarryover({ remaining: -3 } as VacationBalance)` → `0`
    - `calculateCarryover({ remaining: 3 } as VacationBalance)` → `3`
    - `calculateCarryover({ remaining: 20 } as VacationBalance)` → `20`

    **Ein Konsistenz-Test (ein einzelner `it(...)`-Block), der intern alle vier Pfade prüft,
    mit vier vollständig isolierten Nutzer/Jahrespaaren (kein gemeinsames Zieljahr —
    verhindert, dass `bulkInitializeVacationBalances` beim Durchlauf durch ALLE aktiven Nutzer
    versehentlich einen der anderen drei Testnutzer mit-verarbeitet):**
    - Nutzer A: Vorjahreskonto `PREV_A = 2050` mit `entitlement: 25, carryover: 0, taken: 5` (→
      `remaining = 20`). Aufruf `upsertVacationBalance({ userId: userA, year: 2051,
      entitlement: 25, carryover: 20, actorId: null })`. Erwartung: wirft NICHT (vor diesem Fix
      hätte bereits die `> 10`-Prüfung geworfen); `result.carryover === 20`.
    - Nutzer B: Vorjahreskonto `PREV_B = 2052` mit `remaining = 20` (gleiche Werte wie A).
      Zielkonto `2053` per direktem `db.prepare(INSERT ...)` mit `carryover: 0` anlegen, dann
      `updateVacationBalance(id, { carryover: 20 })` aufrufen. Erwartung: wirft nicht;
      `getVacationBalance(userB, 2053).carryover === 20`.
    - Nutzer C: Vorjahreskonto `PREV_C = 2054` mit `remaining = 20`. Aufruf
      `bulkInitializeVacationBalances(2055, adminId)`. Erwartung:
      `getVacationBalance(userC, 2055).carryover === 20`.
    - Nutzer D: Vorjahreskonto `PREV_D = 2056` mit `remaining = 20`. Aufruf
      `initializeVacationBalance(userD, 2057)` (aus `absenceService.ts`). Erwartung:
      `result.carryover === 20`.
    - Gemeinsame Assertion am Testende: alle vier resultierenden `carryover`-Werte sind gleich
      (`20`) — das ist die eigentliche Regressionsaussage dieses Tests, nicht die vier
      Einzelwerte für sich.
  </behavior>

  <action>
Neue Testdatei anlegen, Struktur/Cleanup-Muster aus `vacationEntitlementBooking.test.ts`
übernehmen (`createTestUser`-Helfer mit `Date.now()`-eindeutigem Username,
`testUserIds`-Array, globales `afterEach`-Cleanup für `vacation_transactions`/`vacation_balance`/
`users`). Jahre `2050`-`2057` sind exklusiv für diese Datei (kollidieren nicht mit `2031-2035`,
die in `vacationEntitlementBooking.test.ts` bereits belegt sind) — `afterEach` räumt
`vacation_transactions`/`vacation_balance` global für `year IN (2050, 2051, 2052, 2053, 2054,
2055, 2056, 2057)` auf (nicht nur nach `userId` gefiltert), weil `bulkInitializeVacationBalances`
alle aktiven Nutzer der Datenbank verarbeitet — exakt das bestehende Muster aus Test 4/5 in
`vacationEntitlementBooking.test.ts`.

Import-Block: `calculateCarryover`, `upsertVacationBalance`, `updateVacationBalance`,
`bulkInitializeVacationBalances`, `getVacationBalance`, `VacationBalance` (Typ, für die
Unit-Test-Objektliterale) aus `./vacationBalanceService.js`; `initializeVacationBalance` aus
`./absenceService.js`; `db` aus `../database/connection.js`.

Zwei `describe`-Blöcke: einen für die fünf Unit-Tests von `calculateCarryover` (kein DB-Zugriff,
kein `beforeEach`/`afterEach` nötig), einen für den Konsistenz-Test mit den vier Nutzern
(`beforeEach` legt `adminId` an, jeder der vier Fälle legt seinen eigenen Testnutzer per
`createTestUser` an und fügt ihn `testUserIds` hinzu).

Kommentar am Dateikopf ergänzen, der auf den behobenen Blocker verweist: vier Stellen mit zwei
widersprüchlichen Regeln (`06-REVIEW.md` WR-03), warum das den Nachbuchungs-Fix aus 06-05
unterlaufen hätte (`06-VERIFICATION.md` Gap 1/Gap 2 Interaktion), und dass dieser Test genau die
Interaktion absichert, die keiner der 27 bestehenden Tests aus 06-01/06-02/06-03 abdeckt.
  </action>

  <verify>
    <automated>cd server && npx vitest run vacationCarryoverCalculation</automated>
  </verify>

  <acceptance_criteria>
- `npx vitest run vacationCarryoverCalculation` meldet 6 bestandene Tests (5 Unit-Tests +
  1 Konsistenz-Test, der intern alle vier Pfade prüft), 0 Fehlschläge.
- Der Konsistenz-Test schlägt fehl, wenn Task 1 rückgängig gemacht wird: vor dem Fix würde
  Nutzer A bereits an der `> 10`-Validierung von `upsertVacationBalance` scheitern (Exception
  statt `carryover === 20`) — der Test ist damit ein echter Regressionswächter für den Blocker,
  nicht nur ein grüner Selbstzweck.
- Kein bestehender Test aus `vacationEntitlementBooking.test.ts`,
  `vacationCorrectionBooking.test.ts` oder `absenceVacationBooking.test.ts` wird durch die
  Umstellung in Task 1 rot (alle dort verwendeten Carryover-Werte liegen bei ≤5 Tagen und
  bleiben unter der neuen wie der alten Regel identisch).
  </acceptance_criteria>

  <done>6 Tests in vacationCarryoverCalculation.test.ts gruen — 5 Unit-Tests fuer calculateCarryover() und ein Konsistenztest (ein it()-Block), der alle vier Buchungspfade gegeneinander prueft, insbesondere bei mehr als 5 Resttagen.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend-Validierungsgrenze im Admin-Korrekturformular an die Server-Regel angleichen</name>
  <files>desktop/src/components/vacation/VacationBalanceEditModal.tsx</files>

  <read_first>
```bash
grep -n "carryover\|Carryover\|Übertrag" desktop/src/components/vacation/VacationBalanceEditModal.tsx
sed -n '60,100p' desktop/src/components/vacation/VacationBalanceEditModal.tsx
sed -n '165,185p' desktop/src/components/vacation/VacationBalanceEditModal.tsx
```
  </read_first>

  <action>
In `validateForm()` die Zeile `if (isNaN(carryoverNum) || carryoverNum < 0 || carryoverNum >
10) { setCarryoverError('Übertrag muss zwischen 0 und 10 Tagen liegen'); isValid = false; }`
ersetzen durch `if (isNaN(carryoverNum) || carryoverNum < 0) { setCarryoverError('Übertrag darf
nicht negativ sein'); isValid = false; }` — die feste Obergrenze entfällt, die `isNaN`- und
`< 0`-Prüfung (negativer Übertrag ist weiterhin unsinnig) bleibt unverändert erhalten, nur der
Fehlertext wird an die verbleibende Regel angepasst (er darf nicht mehr "zwischen 0 und 10"
behaupten, wenn nur noch die Untergrenze geprüft wird).

Am `<Input type="number" label="Übertrag (Tage)" ... />`-Element für den Übertrag (nicht das
Anspruchs-Feld darüber, das behält `max="50"`) das Attribut `max="10"` vollständig entfernen —
ein `max`-Attribut auf einem nativen `<input type="number">` begrenzt die Browser-Spinner-Pfeile
und kann Validierungshinweise des Browsers auslösen; würde es auf `10` stehen bleiben, entstünde
exakt derselbe Widerspruch zwischen Oberfläche und Server, den dieser Plan gerade beseitigt.
`min="0"` und `step="1"` bleiben unverändert.

Den `helperText` des Übertrag-Feldes `"Übertrag aus Vorjahr (max. 5 Tage automatisch)"` ersetzen
durch `"Übertrag aus Vorjahr"` — der bisherige Text nennt sowohl eine falsche, nicht mehr
geltende Zahl (5, dieselbe alte Regel wie in Task 1) als auch fälschlich "automatisch", obwohl
dieses Feld eine manuelle Admin-Korrektur ist, keine automatische Berechnung.

Sonst keine Änderung an dieser Datei — insbesondere das Anspruchs-Feld (`entitlement`, `min="0"
max="50"`) bleibt unangetastet, ebenso `desktop/src/pages/VacationBalanceManagementPage.tsx`
(dort ist `balance.remaining > 10` eine reine Farbschwelle der Übersichtstabelle, keine
Validierung — siehe `<context>` — und nicht Teil dieses Plans).
  </action>

  <verify>
    <automated>cd desktop && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
- `grep -c "carryoverNum > 10" desktop/src/components/vacation/VacationBalanceEditModal.tsx`
  liefert 0.
- `grep -c "zwischen 0 und 10" desktop/src/components/vacation/VacationBalanceEditModal.tsx`
  liefert 0.
- `grep -c "max=\"10\"" desktop/src/components/vacation/VacationBalanceEditModal.tsx` liefert 0
  (das Anspruchs-Feld behält `max="50"`, das ist nicht betroffen).
- `grep -c "max. 5 Tage" desktop/src/components/vacation/VacationBalanceEditModal.tsx` liefert 0.
- `grep -c "carryoverNum < 0" desktop/src/components/vacation/VacationBalanceEditModal.tsx`
  liefert mindestens 1 — die Untergrenze bleibt geprüft.
- `cd desktop && npx tsc --noEmit` läuft ohne Fehler.
- `git diff --stat` (oder Vergleich der geänderten Dateien) zeigt KEINE Änderung an
  `desktop/src/pages/VacationBalanceManagementPage.tsx`.
  </acceptance_criteria>

  <done>Das Admin-Korrekturformular akzeptiert denselben Wertebereich wie der Server (Übertrag ≥ 0, keine feste Obergrenze mehr) — Oberfläche und Server widersprechen sich nicht mehr; npx tsc --noEmit im Desktop-Projekt fehlerfrei.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin-Route → `upsertVacationBalance`/`updateVacationBalance` | Authentifizierter Admin-Request — unverändert durch diesen Plan, nur die Validierungsgrenze ändert sich |
| Interner Service-Aufruf → `calculateCarryover` | Kein neuer Grenzübertritt — reine, deterministische Berechnungsfunktion ohne DB-Zugriff |
| Admin-Oberfläche (`VacationBalanceEditModal.tsx`) → `upsertVacationBalance` (via API) | Client-seitige Validierung ist reine UX-Vorabprüfung, keine Sicherheitsgrenze — die tatsächliche Durchsetzung bleibt serverseitig in `upsertVacationBalance`/`updateVacationBalance` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Tampering | Divergenz zwischen vier unabhängigen Übertragsberechnungen | mitigate | `calculateCarryover()` als einzige Berechnungsquelle, alle vier Aufrufstellen delegieren an sie — ein erneuter Widerspruch kann nur noch entstehen, wenn eine fünfte Stelle die Funktion umgeht, was der Konsistenz-Test in Task 2 sofort aufdeckt |
| T-06-06-02 | Repudiation | Entfernte Validierungsobergrenze (10 Tage) könnte theoretisch unplausibel hohe Admin-Korrekturen zulassen | accept | Die verbleibende Prüfung (`data.carryover` darf `calculateCarryover(previousBalance)` nicht überschreiten) bindet den Wert weiterhin an den tatsächlichen Vorjahresrest — eine erfundene, durch nichts gedeckte Zahl bleibt weiterhin unmöglich |
| T-06-06-03 | Tampering | Frontend-Validierung (`VacationBalanceEditModal.tsx`) dupliziert die Server-Regel unabhängig — kann erneut divergieren, wenn nur eine Seite künftig geändert wird | accept | Client-seitige Validierung ist ausschließlich UX (sofortiges Feedback vor dem API-Call); die eigentliche Durchsetzung bleibt serverseitig in `upsertVacationBalance`. Eine erneute Divergenz würde bestenfalls zu einer irreführenden Client-Fehlermeldung führen, nicht zu einem Sicherheits- oder Datenintegritätsproblem — der Server bleibt in jedem Fall die Quelle der Wahrheit |
| T-06-06-SC | Tampering | npm/pip/cargo installs | n/a | Dieser Plan installiert keine neuen Pakete — kein Package-Legitimacy-Gate nötig |
</threat_model>

<verification>
- `cd server && npx tsc --noEmit` — keine TypeScript-Fehler
- `cd server && npx vitest run vacationCarryoverCalculation` — alle neuen Tests grün
- `cd server && npx vitest run` — Gesamtstatus unverändert gegenüber dem in `06-VERIFICATION.md`
  dokumentierten Referenzwert (135 passed / 2 vorbestehend rote, datumsabhängige Fehlschläge in
  `unifiedOvertimeService.test.ts`), zzgl. der neuen Tests aus diesem Plan
- `cd desktop && npx tsc --noEmit` — keine TypeScript-Fehler im Admin-Korrekturformular
</verification>

<success_criteria>
- Genau eine Funktion (`calculateCarryover`) berechnet den Übertrag; alle vier produktiven
  Server-Aufrufstellen nutzen sie
- Ein Vorjahresrest über 5 Tage wird an keiner der vier Server-Stellen mehr gekappt
- Kein Kommentar und keine Fehlermeldung behauptet mehr eine gesetzliche 5- oder feste
  10-Tage-Grenze — weder im Server noch im Admin-Korrekturformular
- Das Admin-Korrekturformular akzeptiert denselben Wertebereich wie der Server (≥ 0, keine feste
  Obergrenze) — kein Widerspruch zwischen Oberfläche und Server mehr
- Bestehende, bereits produktive Datensätze (insbesondere der gedeckelte 5-Tage-Übertrag von
  `userId=1`) werden von diesem Plan nicht verändert — die Korrektur dieses Bestandswerts ist
  eine separate, offene Entscheidung beim Anwender
</success_criteria>

<output>
Create `.planning/phases/06-buchungen-bei-jedem-vorgang/06-06-SUMMARY.md` when done
</output>
