---
phase: 14.1-rechenwerk-blocker-aus-dem-produktionslauf-schliessen
plan: 06
type: execute
wave: 6
depends_on: ["14.1-05", "14.1-01"]
files_modified:
  - server/src/scripts/purgeFutureOvertimeRows.ts
  - server/package.json
  - .planning/phases/14.1-rechenwerk-blocker-aus-dem-produktionslauf-schliessen/14.1-NACHWEIS-BEREINIGUNG.md
autonomous: true
requirements: []
decisions: [D-06, D-07, D-08, D-09, D-11, D-12, D-13]

must_haves:
  truths:
    - "D-06: Der Ablauf ist zwingend Sicherung → Trockenlauf ohne Schreibzugriff → Pruefung der Trockenlauf-Ausgabe → --apply → Wiederherstellungsnachweis; keine Stufe wird uebersprungen"
    - "D-06: Die Bereinigung laeuft nach dem Code-Fix von BL-01 (Plan 14.1-01) — vorher legte der Code die Zeilen sofort wieder an"
    - "D-06: Das Auswahlpraedikat ist im Plan und im Skript ausdruecklich festgelegt und begruendet"
    - "D-06: Die im Trockenlauf tatsaechlich gefundene Menge ist ausgewiesen, und die Abweichung zur Roadmap-Zahl 59 ist benannt statt stillschweigend verrechnet — der Trockenlauf ist die massgebliche Zaehlung"
    - "D-06: Der Testnutzer 15015 ist vom Praedikat ausgeschlossen und bleibt unangetastet"
    - "D-06: Die geloeschten Zeilen sind aus der Sicherung nachweislich wiederherstellbar — tatsaechlich in eine Kopie zurueckgespielt und Zeile fuer Zeile verglichen, nicht behauptet"
    - "Kein Journaleintrag und keine Monatszeile traegt danach ein Datum in der Zukunft"
    - "Die Salden aller Nutzer sind durch die Bereinigung allein unveraendert — Vorher/Nachher-Vergleich auf einer Produktionsarbeitskopie, Differenz 0,00 h je Nutzer"
    - "D-08: Keine Zeile in time_entries, absence_requests, overtime_corrections, vacation_balance, vacation_transactions wurde angefasst (Zeilenzahl und Pruefsumme vorher/nachher belegt)"
    - "D-07: Die Bereinigung ist ein eigener, einzeln rueckgaengig machbarer Vorgang mit eigenem Commit und benannter Sicherung"
    - "D-09: Keine WR-Warnung wurde behoben"
    - "D-12: npx vitest run endet mit hoechstens 3 roten Tests; tsc --noEmit Exit 0 fuer server und desktop; desktop check:rules gruen"
    - "D-13: Es wurde ausschliesslich gegen server/database/development.db und selbst angelegte Arbeitskopien geschrieben; die Beweisdatenbanken blieben readonly; kein Push, kein Deployment, kein Produktionszugriff"
  artifacts:
    - path: "server/src/scripts/purgeFutureOvertimeRows.ts"
      provides: "Zweistufiges Loeschwerkzeug mit Produktionsschutz, DATABASE_PATH-Pflicht und Trockenlauf als Vorgabe"
      contains: "--apply"
    - path: ".planning/phases/14.1-rechenwerk-blocker-aus-dem-produktionslauf-schliessen/14.1-NACHWEIS-BEREINIGUNG.md"
      provides: "Praedikat, Fundmenge, Abweichung zur Zahl 59, Sicherungspfad, Wiederherstellungsnachweis, D-08-Kennzahlen"
  key_links:
    - from: "server/src/scripts/purgeFutureOvertimeRows.ts"
      to: "server/src/scripts/productionGuard.ts"
      via: "assertNotProduction vor jedem Datenbankzugriff"
      pattern: "assertNotProduction"
    - from: "server/src/scripts/purgeFutureOvertimeRows.ts"
      to: "overtime_transactions"
      via: "DELETE mit gebundenen Parametern, nur mit --apply"
      pattern: "DELETE FROM overtime_transactions"
---

<objective>
Die Zukunftsmonats-Datenreste aus der Arbeitsdatenbank entfernen: fiktive Journalbuchungen fuer
Tage, die noch nicht stattgefunden haben, sowie die zugehoerigen Monatszeilen in
`overtime_balance` — Nutzer 3 und 17 fuer 2026-09, Nutzer 30 fuer 2026-10.

Zweck: Erfolgskriterium der Phase — „Kein Journaleintrag und keine Monatszeile traegt ein Datum
in der Zukunft." Das ist der **einzige** Schritt der Phase, der Daten loescht.

**Reihenfolge (D-06, zwingend):** Sicherung → Trockenlauf ohne Schreibzugriff → Pruefung der
Trockenlauf-Ausgabe → `--apply` → Nachweis, dass die geloeschten Zeilen aus der Sicherung
wiederherstellbar sind. Keine Stufe wird uebersprungen, keine wird zusammengezogen.

**Warum zuletzt:** Die Bereinigung laeuft nach dem Code-Fix von BL-01 (Plan 14.1-01). Vorher
legt der Code die Zeilen bei der naechsten Berechnung sofort wieder an, und die Bereinigung
waere wertlos. Sie laeuft zudem nach allen uebrigen Plaenen, damit deren Testlaeufe — die in
dieselbe `development.db` schreiben — die Fundmenge nicht mehr veraendern koennen.

Ergebnis: Ein zweistufiges Loeschwerkzeug mit Produktionsschutz, ein ausgewiesenes und
begruendetes Auswahlpraedikat, die tatsaechlich gefundene Menge im Verhaeltnis zur Roadmap-Zahl
59, ein gefuehrter Wiederherstellungsnachweis und der Beleg, dass sich durch die Bereinigung
allein kein Saldo bewegt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/phases/14.1-rechenwerk-blocker-aus-dem-produktionslauf-schliessen/14.1-CONTEXT.md
@.planning/phases/14.1-rechenwerk-blocker-aus-dem-produktionslauf-schliessen/14.1-PATTERNS.md
@.planning/phases/14-absicherung-und-auslieferung/14-ROLLBACK-RUNBOOK.md
@.planning/phases/14-absicherung-und-auslieferung/deferred-items.md

**Nicht suchen:** Keine RESEARCH.md, keine VALIDATION.md fuer diese Phase — `workflow.research`
ist abgeschaltet.

**Verbindliche Umgebung (D-13):** Arbeitsdatenbank `server/database/development.db`.
`server/database.db` ist unmigrierter Altbestand und wird nicht geoeffnet. Die
Beweisdatenbanken `14-produktionskopie.db`, `14-prod-nach-migration.db` und
`14-generalprobe.db` werden ausschliesslich `readonly` geoeffnet; Arbeitskopien entstehen per
`VACUUM INTO` in **neue** Dateien. **Kein Push, kein Deployment, kein Zugriff auf die
Produktionsdatenbank.**

<interfaces>
Der Bestand, am 25.08.2026 readonly gegen `server/database/development.db` gemessen — als
Erwartungswert, nicht als Ersatz fuer den Trockenlauf:

  overtime_transactions mit date > '2026-08-25', je Typ:
      time_entry            118 Zeilen   2026-09-01 .. 2026-10-30   Summe hours -352
      vacation_credit        11 Zeilen   2026-09-01 .. 2026-09-28   Summe hours  +20
      overtime_comp_credit    1 Zeile    2026-09-29                 Summe hours   +4
      → 130 Zeilen insgesamt, **alle** von abgeleiteten (rebuildbaren) Typen;
        **keine** Zeile eines book-once-Typs, insbesondere kein `model_change`

  je Nutzer:  userId 3 → 38, userId 17 → 32, userId 30 → 30, userId 15015 → 30
  ohne den Testnutzer 15015: **100 Zeilen**, davon **50** mit `hours != 0`

  Zeilen mit date zwischen 2026-08-26 und 2026-08-31: **0**
      → `date > heute` und `date >= '2026-09-01'` liefern heute dieselbe Menge

  overtime_balance mit month > '2026-08':
      userId 3 / 2026-09     targetHours 36   actualHours 16   → -20 h
      userId 17 / 2026-09    targetHours 52   actualHours  8   → -44 h
      userId 30 / 2026-10    targetHours 176  actualHours  0   → -176 h
      userId 15015 / 2026-09 targetHours 88   actualHours  0   → -88 h  (Testnutzer, ausgenommen)
      → ohne 15015: **3 Zeilen**, deckungsgleich mit den in D-06 bestaetigten Werten

Die book-once-Typen, die **niemals** geloescht werden duerfen, stehen woertlich in
`overtimeTransactionRebuildService.ts:137-166`: der Rebuild nimmt sie seit dem 18.08.2026
ausdruecklich von seinem Loeschbereich aus (`compensation`, `correction`, `carry_over`,
`payout`, `initial_balance`, `year_end_balance`; dazu `model_change` aus Phase 12/13). Eine
`model_change`-Zeile **darf** ein Datum in der Zukunft tragen — die Korrektur oder der Storno
einer geplanten Periode (WR-10, Phase 13, belegt in `overtimeLiveCalculationService.ts` Block
6b, der solche Zeilen ausdruecklich sichtbar haelt). Heute gibt es keine solche Zeile in der
Zukunft; das Praedikat muss sie trotzdem ausschliessen, weil bis zur Ausfuehrung eine
entstehen kann.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Loeschwerkzeug mit Produktionsschutz und Trockenlauf als Vorgabe (D-06)</name>
  <files>server/src/scripts/purgeFutureOvertimeRows.ts, server/package.json</files>
  <read_first>
    - `.planning/phases/14.1-rechenwerk-blocker-aus-dem-produktionslauf-schliessen/14.1-PATTERNS.md`, Abschnitt „Muster fuer datenveraendernde Skripte mit Trockenlauf (D-06)" — Kopfform, Reihenfolge-Vorschrift, `main()`-Skelett, Fundliste, Abbruch ohne `--apply`, `integrity_check`
    - `server/src/scripts/backfillOvertimeJournal.ts:1-70` und `:290-350` und `:430-540` — das Vorbild vollstaendig: Kopfkommentar, `parseArgs`, DATABASE_PATH-Pflicht, Guard-Reihenfolge, Fundliste, Abbruch, Abschlusspruefung
    - `server/src/scripts/productionGuard.ts` — `assertNotProduction` vollstaendig
    - `server/src/services/overtimeTransactionRebuildService.ts:137-180` — die Liste der rebuildbaren Typen und die Begruendung, warum die uebrigen book-once sind
    - `.planning/phases/14.1-rechenwerk-blocker-aus-dem-produktionslauf-schliessen/14.1-CONTEXT.md`, Abschnitt D-06 — die Vorgabe zum Praedikat und zur Zahl 59
  </read_first>
  <action>
Neues Skript `server/src/scripts/purgeFutureOvertimeRows.ts`, eingetragen in
`server/package.json` unter `"purge:future-overtime"` neben
`"backfill:overtime-journal"`. Struktur woertlich nach `backfillOvertimeJournal.ts`.

**A — Kopfkommentar.** Aufrufform (Trockenlauf ohne, Schreiblauf mit `--apply`), die
Reihenfolge-Vorschrift des Produktionsschutzes wie im Vorbild (`:51-65`), und der Satz, dass
`--allow-production` die einzige Stelle ist, an der der Guard umgangen werden kann, und dass
ohne `--apply` auch mit `--allow-production` der Trockenlauf bleibt. Zusaetzlich: der Verweis
auf D-06 und darauf, dass dieses Werkzeug **das erste im Projekt ist, das tatsaechlich
`DELETE` ausfuehrt** — deshalb die Sicherung vor dem Lauf Pflicht ist.

**B — Reihenfolge in `main()`, zwingend wie im Vorbild:**
  1. Nur importsichere Module am Kopf: `path`, `url`, `./productionGuard.js`,
     `../config/database.js`.
  2. `DATABASE_PATH`-Pflichtpruefung **vor** `parseArgs()`, mit `process.exit(2)` und der
     Meldung, dass es keinen stillen Rueckfall auf `database/development.db` gibt.
  3. `parseArgs()` — reine Funktion, oeffnet nichts. Schalter: `--apply`,
     `--allow-production`. Kein weiterer Schalter; insbesondere kein `--force`, das den
     Trockenlauf ueberspringt.
  4. Aufgeloester Datenbankpfad als allererste Ausgabezeile.
  5. `assertNotProduction(resolvedPath)`, ausser bei `--allow-production` (dann die
     Warnzeile des Vorbilds).
  6. Modus-Zeile: `### TROCKENLAUF — es wird nichts geschrieben ###` bzw.
     `### MODUS: SCHREIBEN ###`.
  7. Erst danach `await import('../database/connection.js')` und alles Weitere.

**C — Das Auswahlpraedikat, ausdruecklich festgelegt und im Skriptkopf begruendet:**

Fuer `overtime_transactions`: Zeilen mit einem Datum **nach heute**, die von einem
abgeleiteten (rebuildbaren) Typ sind und nicht dem ausgenommenen Testnutzer gehoeren. Also
drei Bedingungen, alle als gebundene Parameter bzw. Platzhalter:
  - `date > ?` mit `formatDate(getCurrentDate(), 'yyyy-MM-dd')` — **nicht** ein fest
    verdrahtetes `'2026-09-01'`. Begruendung: D-06 spricht von „Tagen, die noch nicht
    stattgefunden haben"; das ist ein bewegliches Datum. Heute liefern beide Fassungen
    dieselbe Menge (0 Zeilen zwischen dem 26. und 31.08.), aber ein fest verdrahtetes Datum
    waere ab dem 01.09. falsch.
  - `type IN (?, ?, …)` mit exakt den elf rebuildbaren Typen aus
    `overtimeTransactionRebuildService.ts:153-166` (`worked`, `time_entry`, `earned`,
    `vacation_credit`, `sick_credit`, `overtime_comp_credit`, `special_credit`,
    `unpaid_deduction`, `unpaid_adjustment`, `holiday_credit`, `weekend_credit`). Begruendung
    im Kommentar: Die uebrigen Typen sind book-once und werden seit dem 18.08.2026 auch vom
    Rebuild bewusst nicht angefasst; eine `model_change`-Zeile darf ein Datum in der Zukunft
    tragen (Korrektur oder Storno einer geplanten Periode, WR-10 Phase 13). Heute gibt es
    keine solche Zeile in der Zukunft — der Filter ist ein Sicherheitsnetz, kein Fuellwerk,
    und bleibt trotzdem drin.
  - `userId NOT IN (?)` mit 15015. Begruendung im Kommentar: Der Testnutzer aus
    `development.db` ist laut D-06 ausdruecklich **kein** Gegenstand der Bereinigung.

Fuer `overtime_balance`: `month > ?` mit `formatDate(getCurrentDate(), 'yyyy-MM')` und
`userId NOT IN (?)` mit 15015. Kein Typfilter — die Tabelle hat keinen.

**Alle Werte werden als Prepared-Statement-Parameter gebunden.** Es wird keine Zahl und keine
Zeichenkette in SQL hineinformatiert, auch nicht die Typliste — die Platzhalter werden nach dem
Muster von `overtimeTransactionRebuildService.ts:167-173` erzeugt
(`TYPES.map(() => '?').join(', ')`). `formatDate` und `getCurrentDate` kommen aus
`../utils/timezone.js`; `toISOString()` ist projektweit verboten.

**D — Trockenlauf.** Ohne `--apply` wird ausschliesslich `SELECT` ausgefuehrt. Ausgegeben wird:
  - eine Fundliste je `userId` und Monat mit Zeilenzahl, Zahl der Zeilen mit `hours != 0` und
    Summe der `hours`,
  - die Fundliste der betroffenen `overtime_balance`-Zeilen mit `userId`, `month`,
    `targetHours`, `actualHours` und der Differenz,
  - die Gesamtzahlen fuer beide Tabellen,
  - eine ausdrueckliche Gegenueberstellung zur Roadmap-Zahl **59**: die gefundene Zeilenzahl,
    die Zahl der Zeilen mit `hours != 0`, und die Feststellung, dass 59 mit keiner der beiden
    Abgrenzungen deckungsgleich ist und dass **der Trockenlauf die massgebliche Zaehlung ist**.
    Dieser Satz wird vom Skript selbst gedruckt, nicht nur ins Nachweisdokument geschrieben.
  - eine Kontrollausgabe der ausgenommenen Zeilen: wie viele Zeilen das Datums- und
    Typkriterium erfuellen, aber durch den Nutzerausschluss (15015) oder den Typfilter
    **nicht** geloescht werden. Wer nichts ausnimmt, hat nichts geprueft.
  - Abschluss: `Trockenlauf beendet. Mit --apply ausfuehren.` und `process.exit(0)`.

**E — Schreiblauf.** Mit `--apply`: beide `DELETE`-Anweisungen in **einer** `db.transaction()`,
zuerst `overtime_transactions`, dann `overtime_balance`. Danach:
  - die Zahl der tatsaechlich geloeschten Zeilen je Tabelle (`.changes`),
  - eine Nachkontrolle: dieselben `SELECT`s wie im Trockenlauf muessen jetzt 0 Zeilen liefern,
  - `console.log('integrity_check:', JSON.stringify(db.pragma('integrity_check')))` und
    `foreign_key_check`,
  - die Zeilenzahl und Pruefsumme der fuenf nach D-08 geschuetzten Tabellen, damit der
    Nachweis nicht nachtraeglich zusammengesucht werden muss.

**F — Das Skript legt selbst keine Sicherung an.** Die Sicherung ist ein eigener,
nachpruefbarer Schritt (Task 2). Ein Werkzeug, das seine eigene Sicherung zieht, verleitet
dazu, sie nicht zu pruefen.

Commit: `feat(14.1-06): Loeschwerkzeug fuer Zukunftszeilen mit Trockenlauf und Produktionsschutz`.
Der Commit enthaelt Skript und `package.json`-Eintrag, aber **keinen** Lauf und keine
Datenaenderung (D-07).
  </action>
  <acceptance_criteria>
    - `cd server && npm run purge:future-overtime` **ohne** `DATABASE_PATH` endet mit Exit 2 und der Meldung, dass `DATABASE_PATH` nicht gesetzt ist; es entsteht keine neue Datenbankdatei (`ls server/database/` vorher und nachher identisch)
    - `cd server && DATABASE_PATH=./database/development.db npm run purge:future-overtime` gibt als allererste Zeile den aufgeloesten Datenbankpfad aus, druckt `### TROCKENLAUF — es wird nichts geschrieben ###` und endet mit Exit 0
    - Der Trockenlauf veraendert die Datenbank nicht: Hash von `server/database/development.db` vor und nach dem Trockenlauf identisch
    - `grep -c "DELETE FROM" server/src/scripts/purgeFutureOvertimeRows.ts` liefert genau **2** — `overtime_transactions` und `overtime_balance`, kein weiteres Loeschziel
    - `grep -n "DELETE FROM" server/src/scripts/purgeFutureOvertimeRows.ts` zeigt keine Zeile mit einer eingebetteten Zeichenkette oder Zahl im `WHERE`-Teil; alle Werte sind `?`-Platzhalter
    - `grep -c "'2026-" server/src/scripts/purgeFutureOvertimeRows.ts` liefert **0** — kein fest verdrahtetes Datum
    - `grep -c "toISOString" server/src/scripts/purgeFutureOvertimeRows.ts` liefert **0**
    - `grep -c "15015" server/src/scripts/purgeFutureOvertimeRows.ts` liefert mindestens **1**, und der Wert wird als Parameter gebunden, nicht in SQL eingebettet
    - `grep -n "assertNotProduction" server/src/scripts/purgeFutureOvertimeRows.ts` liefert einen Treffer, der vor dem ersten `await import('../database/connection.js')` steht (Zeilenreihenfolge pruefbar)
    - Die Trockenlauf-Ausgabe enthaelt die Zeichenkette `59` in der Gegenueberstellung und den Satz, dass der Trockenlauf die massgebliche Zaehlung ist
    - Die Trockenlauf-Ausgabe weist die ausgenommenen Zeilen aus (Nutzer 15015 und, falls vorhanden, book-once-Typen)
    - `grep -n "purge:future-overtime" server/package.json` liefert genau einen Treffer
    - `cd server && npx tsc --noEmit` Exit 0
    - `git status --porcelain server/database/` ist leer — der Commit dieser Task enthaelt keine Datenaenderung
  </acceptance_criteria>
  <verify>
    <automated>cd server && npx tsc --noEmit && test "$(grep -c 'DELETE FROM' src/scripts/purgeFutureOvertimeRows.ts)" = "2" && test "$(grep -c "'2026-" src/scripts/purgeFutureOvertimeRows.ts)" = "0" && DATABASE_PATH=./database/development.db npm run purge:future-overtime</automated>
  </verify>
  <done>Das Werkzeug existiert, verweigert den Lauf ohne `DATABASE_PATH`, schreibt ohne `--apply` nichts, bindet jeden Wert als Parameter, nennt die Abweichung zur Zahl 59 selbst und weist die ausgenommenen Zeilen aus.</done>
</task>

<task type="auto">
  <name>Task 2: Probelauf auf Produktionsarbeitskopie, Sicherung, Trockenlauf, Pruefung, --apply (D-06)</name>
  <files>.planning/phases/14.1-rechenwerk-blocker-aus-dem-produktionslauf-schliessen/14.1-NACHWEIS-BEREINIGUNG.md</files>
  <read_first>
    - `.planning/phases/14-absicherung-und-auslieferung/14-ROLLBACK-RUNBOOK.md`, Schritte 1 bis 3 — Kennzahlen erheben, `VACUUM INTO` statt `cp`, Sicherung pruefen **bevor** auf sie gebaut wird
    - `.planning/phases/14.1-rechenwerk-blocker-aus-dem-produktionslauf-schliessen/14.1-CONTEXT.md`, Abschnitt D-06 — der zwingende Ablauf und die bestaetigten drei `overtime_balance`-Werte
    - `server/src/scripts/snapshotBalances.ts` — das bestehende Werkzeug fuer den Saldenvergleich ueber alle Nutzer (`npm run snapshot:balances`), eingesetzt in den Plaenen 14-05 und 14-06
    - `server/src/scripts/purgeFutureOvertimeRows.ts` — das in Task 1 gebaute Werkzeug
  </read_first>
  <action>
**Der Ablauf ist zwingend und wird in dieser Reihenfolge protokolliert. Keine Stufe wird
uebersprungen.**

**Stufe 0 — Vorbedingungen pruefen.**
  - Alle vorangegangenen Plaene der Phase sind abgeschlossen; insbesondere ist der BL-01-Fix
    (Plan 14.1-01) im Arbeitsbaum. Ohne ihn legt der Code die Zeilen sofort wieder an (D-06).
    Beleg: `grep -q "effectiveEndDate" server/src/services/unifiedOvertimeService.ts` und
    `grep -q "deletedAt IS NULL" server/src/services/overtimeLiveCalculationService.ts`.
  - Es steht kein Testnutzer aus den Plaenen 14.1-01 bis 14.1-05 mehr in `users`
    (`SELECT COUNT(*) FROM users WHERE username LIKE 'test-141-%'` ergibt 0). Ist die Zahl
    groesser 0, wird der Aufraeumpfad des betreffenden Plans nachgeholt — der Restbestand wird
    **nicht** vom Loeschpraedikat mitgenommen.
  - Der naechtliche Lauf um 03:00 Uhr ist angehalten (seit dem 23.08., D-11); das wird im
    Nachweis festgehalten.

**Stufe 1 — Vollstaendiger Probelauf auf einer Produktionsarbeitskopie.** Die Roadmap verlangt
fuer die Bereinigung ausdruecklich, sie „zuerst vollstaendig auf einer Produktionskopie" zu
fahren.
  - `14-prod-nach-migration.db` **readonly** oeffnen und per `VACUUM INTO` nach
    `server/database/14.1-bereinigung-probe.db` schreiben. Die Beweisdatenbank wird nicht
    beschrieben (D-13).
  - Kennzahlen der Arbeitskopie nach dem Muster von Runbook Schritt 1 erheben
    (`integrity_check`, `foreign_key_check`, Migrationsanzahl, Zeilenzahlen der beruehrten und
    der fuenf geschuetzten Tabellen).
  - Salden **vorher** ueber alle Nutzer:
    `DATABASE_PATH=./database/14.1-bereinigung-probe.db npm run snapshot:balances -- --asOf=<heute> --json=<pfad-vorher.json>`.
  - Trockenlauf gegen die Arbeitskopie, Ausgabe vollstaendig protokollieren.
  - `--apply` gegen die Arbeitskopie.
  - Salden **nachher** ueber alle Nutzer, in eine zweite JSON-Datei.
  - Vergleich: Die Salden aller Nutzer muessen sich durch die Bereinigung allein **nicht**
    bewegen — `getOvertimeBalance()` deckelt ohnehin auf den laufenden Monat
    (`overtimeTransactionService.ts:454-479`), und `calculatePeriodOvertime()` deckelt seit
    Plan 14.1-01 auf heute. Erwartung: Differenz 0,00 h je Nutzer. Bewegt sich ein Saldo, ist
    das ein **Abbruchgrund** — dann wird nicht gegen `development.db` gefahren, sondern der
    Befund im Nachweisdokument festgehalten und der Plan angehalten.

**Stufe 2 — Sicherung von `server/database/development.db`, vor jedem Schreibzugriff.**
  - Kennzahlen des Ausgangsstands erheben (Runbook Schritt 1), inklusive der Zeilenzahl und
    Pruefsumme der fuenf nach D-08 geschuetzten Tabellen.
  - `mkdir -p server/database/backups`, dann per `VACUUM INTO` — **nicht** `cp` — nach
    `server/database/backups/development.PRE-14.1-06_<YYYYMMDD>_<HHMMSS>.db`. Begruendung
    (Runbook Schritt 2): Ein `cp` auf eine WAL-aktive Datei kann die juengsten, noch nicht in
    die Hauptdatei geschriebenen Transaktionen verlieren.
  - **Sicherung pruefen, bevor auf sie gebaut wird** (Runbook Schritt 3): Dateigroesse groesser
    0, `integrity_check` = `ok` **gegen die Sicherungsdatei**, `foreign_key_check` leer, und
    alle in Stufe 2 erhobenen Kennzahlen gegen die Sicherung erneut erheben und mit dem
    Ausgangsstand vergleichen — Differenz 0. Eine ungeprueft Sicherung ist keine Sicherung.
  - Der vollstaendige Sicherungspfad wird im Nachweisdokument genannt.

**Stufe 3 — Trockenlauf gegen `server/database/development.db`.**
  `DATABASE_PATH=./database/development.db npm run purge:future-overtime`, Ausgabe
  vollstaendig protokollieren. Hash der Datenbank vor und nach dem Trockenlauf erheben — er
  muss gleich sein.

**Stufe 4 — Pruefung der Trockenlauf-Ausgabe, vor `--apply`.** Ausdruecklich abgehakt:
  - Betrifft die Fundliste genau die erwarteten Nutzer und Monate (3 und 17 fuer 2026-09,
    30 fuer 2026-10)? Jeder zusaetzliche Nutzer ist ein Abbruchgrund und wird geklaert, bevor
    geschrieben wird.
  - Ist Nutzer 15015 in der Ausnahmeliste und **nicht** in der Fundliste?
  - Enthaelt die Fundliste eine Zeile eines book-once-Typs? (Erwartung: nein. Wenn doch, ist
    das Praedikat falsch und wird korrigiert, nicht der Lauf gestartet.)
  - Wie verhaelt sich die gefundene Menge zur Roadmap-Zahl 59?

**Stufe 5 — `--apply` gegen `server/database/development.db`.**
  `DATABASE_PATH=./database/development.db npm run purge:future-overtime -- --apply`.
  Ausgabe vollstaendig protokollieren, inklusive `.changes` je Tabelle, Nachkontrolle
  (0 verbleibende Zeilen), `integrity_check` und `foreign_key_check`.

**Nachweisdokument** `14.1-NACHWEIS-BEREINIGUNG.md` im Phasenverzeichnis mit allen Stufen in
dieser Reihenfolge, dazu:
  - Dem **Praedikat im Wortlaut** (beide `WHERE`-Bedingungen) und seiner Begruendung.
  - Der **Fundmenge** aus dem Trockenlauf gegen `development.db`, je Nutzer und Monat.
  - Einem eigenen Abschnitt **„Abweichung zur Zahl 59"**: die Roadmap nennt 59 fiktive
    Journalbuchungen; gemessen sind es unter dem festgelegten Praedikat N Zeilen, davon M mit
    `hours != 0`. Die Zahl 59 ist mit keiner der beiden Abgrenzungen deckungsgleich. Der
    Trockenlauf ist die massgebliche Zaehlung. Diese Abweichung wird **benannt**, nicht
    verrechnet.
  - Den drei bestaetigten `overtime_balance`-Werten (Nutzer 30 / 2026-10 = −176 h,
    Nutzer 17 / 2026-09 = −44 h, Nutzer 3 / 2026-09 = −20 h) im Abgleich mit dem, was der
    Trockenlauf findet.
  - Dem Saldenvergleich aus Stufe 1 (Zahl der Nutzer mit Differenz ungleich 0,00 h — Erwartung
    0) und dem Hashvergleich von `14-prod-nach-migration.db` vor und nach dem Plan.
  - Den **D-08-Kennzahlen** der fuenf geschuetzten Tabellen in `development.db` vor und nach
    dem `--apply`-Lauf, Differenz 0.

Commit: `chore(14.1-06): Zukunftszeilen aus der Arbeitsdatenbank entfernt — Sicherung, Trockenlauf, Apply`.
Der Commit enthaelt das Nachweisdokument. Datenbankdateien und Sicherungen werden **nicht**
eingecheckt; `git status` wird darauf geprueft.
  </action>
  <acceptance_criteria>
    - `14.1-NACHWEIS-BEREINIGUNG.md` enthaelt alle sechs Stufen (0 bis 5) in dieser Reihenfolge, jede mit ihrer Ausgabe
    - Der Trockenlauf gegen `development.db` ist protokolliert, und der Hash der Datenbank vor und nach dem Trockenlauf ist identisch (beide Werte im Dokument)
    - Der Sicherungspfad `server/database/backups/development.PRE-14.1-06_<YYYYMMDD>_<HHMMSS>.db` ist mit vollem Namen genannt, seine Dateigroesse in Bytes ist ausgewiesen und groesser 0
    - `integrity_check` **gegen die Sicherungsdatei** ist `ok` und `foreign_key_check` leer; beide Ausgaben stehen im Dokument
    - Die Kennzahlentabelle „Ausgangsstand gegen Sicherungsdatei" weist fuer jede erhobene Kennzahl die Differenz **0** aus (Runbook Schritt 3)
    - `14.1-NACHWEIS-BEREINIGUNG.md` enthaelt einen Abschnitt „Abweichung zur Zahl 59", der die Roadmap-Zahl, die gefundene Zeilenzahl und die Zahl der Zeilen mit `hours != 0` nebeneinanderstellt und ausdruecklich feststellt, dass der Trockenlauf die massgebliche Zaehlung ist
    - Die Fundliste betrifft genau die Nutzer 3, 17 und 30; Nutzer 15015 steht in der Ausnahmeliste und nicht in der Fundliste — beide Listen stehen im Dokument
    - Die Fundliste enthaelt keine Zeile eines book-once-Typs (`compensation`, `correction`, `carry_over`, `payout`, `initial_balance`, `year_end_balance`, `model_change`); die Typaufschluesselung der Fundliste steht im Dokument
    - Nach `--apply` liefert `SELECT COUNT(*) FROM overtime_transactions WHERE date > <heute> AND userId != 15015` den Wert **0**, und `SELECT COUNT(*) FROM overtime_balance WHERE month > <laufender Monat> AND userId != 15015` den Wert **0** — beide Abfragen und ihre Ergebnisse stehen im Dokument
    - Die Zeilen des Testnutzers 15015 sind unveraendert: `SELECT COUNT(*) FROM overtime_transactions WHERE userId = 15015` und `SELECT COUNT(*) FROM overtime_balance WHERE userId = 15015` liefern vor und nach dem Lauf denselben Wert (im Dokument: 30 bzw. 1, oder der tatsaechlich gemessene Wert)
    - `integrity_check` nach `--apply` ist `ok`, `foreign_key_check` leer
    - Der Saldenvergleich aus Stufe 1 weist **0** Nutzer mit einer Differenz ungleich 0,00 h aus; die Zahl steht im Dokument
    - Der Hash von `server/database/14-prod-nach-migration.db` vor und nach dem Plan ist identisch (D-13)
    - Fuer alle fuenf geschuetzten Tabellen gilt: Zeilenzahl vorher = nachher und Pruefsumme vorher = nachher, alle fuenf Paare mit Zahlen im Dokument (D-08)
    - `git status --porcelain` zeigt keine `.db`-Datei als hinzugefuegt oder geaendert
  </acceptance_criteria>
  <verify>
    <automated>cd server && node -e "const D=require('better-sqlite3');const db=new D('./database/development.db',{readonly:true});const t=db.prepare(\"SELECT COUNT(*) c FROM overtime_transactions WHERE date > date('now','localtime') AND userId != 15015\").get();const b=db.prepare(\"SELECT COUNT(*) c FROM overtime_balance WHERE month > strftime('%Y-%m','now','localtime') AND userId != 15015\").get();console.log('ot_zukunft',t.c,'ob_zukunft',b.c);if(t.c!==0||b.c!==0)process.exit(1);console.log('integrity',JSON.stringify(db.pragma('integrity_check')));db.close();"</automated>
  </verify>
  <done>Sicherung gezogen und geprueft, Trockenlauf gefahren und geprueft, `--apply` ausgefuehrt, keine Zukunftszeile mehr ausserhalb des Testnutzers, kein Saldo bewegt, und alle Zahlen stehen im Nachweisdokument.</done>
</task>

<task type="auto">
  <name>Task 3: Wiederherstellungsnachweis aus der Sicherung und Abschlussgate der Phase (D-06)</name>
  <files>.planning/phases/14.1-rechenwerk-blocker-aus-dem-produktionslauf-schliessen/14.1-NACHWEIS-BEREINIGUNG.md, .planning/phases/14-absicherung-und-auslieferung/deferred-items.md</files>
  <read_first>
    - `.planning/phases/14-absicherung-und-auslieferung/14-ROLLBACK-RUNBOOK.md`, Schritte 5 und 6 — Zurueckspielen auf eine **neue** Datei per `VACUUM INTO`, danach Kennzahlenvergleich Zeile fuer Zeile
    - `.planning/phases/14.1-rechenwerk-blocker-aus-dem-produktionslauf-schliessen/14.1-NACHWEIS-BEREINIGUNG.md` — die Zahlen aus Task 2, gegen die verglichen wird
  </read_first>
  <action>
**A — Wiederherstellungsnachweis (D-06, der Kern dieses Plans).** D-06 verlangt einen
„Nachweis, dass die geloeschten Zeilen aus der Sicherung wiederherstellbar sind" — und der
Auftrag praezisiert: nicht behauptet, sondern tatsaechlich in eine Kopie zurueckgespielt und
verglichen.

Ablauf nach Runbook Schritt 5 und 6:
  1. Die Sicherungsdatei aus Task 2 Stufe 2 **readonly** oeffnen und per `VACUUM INTO` nach
     `server/database/14.1-restore-probe.db` schreiben — in eine **neue** Datei, niemals ueber
     die laufende `development.db` (dasselbe Muster, das im Ernstfall auf dem Server gilt).
  2. Gegen `14.1-restore-probe.db` dieselben Kennzahlen erheben wie fuer den Ausgangsstand in
     Task 2 Stufe 2: `integrity_check`, `foreign_key_check`, Migrationsanzahl, Zeilenzahlen
     und Summen der beruehrten Tabellen, Zeilenzahlen der fuenf geschuetzten Tabellen.
  3. **Zeile fuer Zeile** vergleichen: Die in Task 2 Stufe 5 geloeschten Zeilen muessen in
     `14.1-restore-probe.db` vollstaendig vorhanden sein. Nachgewiesen wird das nicht ueber die
     Gesamtzahl allein, sondern ueber die Fundlisten-Abfrage aus dem Trockenlauf: Sie liefert
     gegen die zurueckgespielte Kopie exakt dieselbe Liste (Nutzer, Monat, Zeilenzahl, Zahl der
     Zeilen mit `hours != 0`, Summe `hours`) wie der Trockenlauf vor dem `--apply`.
  4. Ergaenzend die drei `overtime_balance`-Zeilen mit ihren Werten (`targetHours`,
     `actualHours`) im Vergleich zur Fundliste vor dem Loeschen.
  5. Die Probekopie wird nach dem Vergleich **nicht** an die Stelle von `development.db`
     gesetzt. Sie bleibt als Nachweis liegen oder wird geloescht — was von beidem geschah,
     steht im Dokument.

**B — Abschlussgate der Phase** (ohne weitere Codeaenderung):
  1. `cd server && npx vitest run` — hoechstens 3 rote Tests, und zwar genau
     `unifiedOvertimeService.test.ts:285`, `:340`, `vacationBackfillService.test.ts:138`
     (D-12). Dieser Lauf ist besonders aussagekraeftig: Er laeuft gegen die **bereinigte**
     `development.db`. Wird ein Test rot, der sich bisher auf eine Zukunftszeile gestuetzt hat,
     ist das der Befund, den D-10 fuer den Phase-12-Test bereits vorweggenommen hat — er wird
     im Nachweisdokument benannt und nicht stillschweigend „angepasst".
  2. `cd server && npx tsc --noEmit` und `cd desktop && npx tsc --noEmit` — beide Exit 0.
  3. `cd desktop && npm run check:rules` — gruen (D-13: `vitest` ist im `desktop/` nicht
     lauffaehig).
  4. `cd server && DATABASE_PATH=./database/development.db npm run verify:balance-vs-journal`
     (das Werkzeug aus Plan 14.1-01) — 0 Nutzer mit einer Differenz ungleich 0,00 h. Damit ist
     das erste Erfolgskriterium der Phase noch einmal gegen den bereinigten Bestand belegt.
  5. D-09: Eintrag in `.planning/phases/14-absicherung-und-auslieferung/deferred-items.md`,
     falls in dieser Phase eine WR-Stelle beruehrt wurde; sonst der entsprechende Satz im
     Nachweisdokument. Zusaetzlich eine Zusammenfassung ueber die ganze Phase: welche
     WR-Warnungen beruehrt und liegen gelassen wurden.
  6. **D-13-Abschlusspruefung:** `git log origin/main..HEAD --oneline` listet alle Commits der
     Phase, und **keiner** wurde gepusht. Es fand kein Deployment statt und kein Zugriff auf
     die Produktionsdatenbank. `grep -rn "129.159.8.19\|production.db\|git push"` ueber die in
     dieser Phase geaenderten Dateien liefert keinen Treffer, der einen solchen Zugriff
     ausfuehrt.
  7. **D-07-Abschlusspruefung:** Die Phase hat je Befund mindestens einen eigenen Commit, und
     die sechs Fix-/Bereinigungs-Commits sind einzeln revertierbar. Die Commit-Liste steht im
     Nachweisdokument.

Commit: `docs(14.1-06): Wiederherstellungsnachweis und Abschlussgate der Phase 14.1`.
  </action>
  <acceptance_criteria>
    - `server/database/14.1-restore-probe.db` wurde per `VACUUM INTO` aus der Sicherungsdatei erzeugt; der Weg (Quelle readonly, Ziel neue Datei) ist im Dokument beschrieben, und die laufende `development.db` wurde dabei nicht ueberschrieben
    - `integrity_check` gegen `14.1-restore-probe.db` ist `ok`, `foreign_key_check` leer; beide Ausgaben stehen im Dokument
    - Die Fundlisten-Abfrage gegen `14.1-restore-probe.db` liefert **exakt dieselbe** Liste wie der Trockenlauf vor dem `--apply`: gleiche Nutzer, gleiche Monate, gleiche Zeilenzahlen, gleiche Zahl der Zeilen mit `hours != 0`, gleiche Summen. Beide Listen stehen nebeneinander im Dokument, mit einer Differenzspalte, die durchgehend **0** zeigt
    - Die drei `overtime_balance`-Zeilen sind mit `userId`, `month`, `targetHours` und `actualHours` in der zurueckgespielten Kopie nachgewiesen und stimmen mit der Fundliste vor dem Loeschen ueberein
    - Das Dokument haelt fest, ob `14.1-restore-probe.db` liegen blieb oder geloescht wurde
    - `cd server && npx vitest run` endet mit hoechstens 3 roten Tests; Zahl gruen / Zahl rot und die Namen der roten stehen im Dokument. Ist ein weiterer Test rot, ist im Dokument benannt, welcher und warum
    - `cd server && npx tsc --noEmit` Exit 0, `cd desktop && npx tsc --noEmit` Exit 0, `cd desktop && npm run check:rules` Exit 0
    - `cd server && DATABASE_PATH=./database/development.db npm run verify:balance-vs-journal` weist **0** Nutzer mit Differenz ungleich 0,00 h aus; die Zahl steht im Dokument
    - Das Dokument enthaelt die Commit-Liste der gesamten Phase (`git log origin/main..HEAD --oneline`) mit mindestens einem Commit je Befund BL-01 bis BL-05 und einem fuer die Bereinigung (D-07)
    - Das Dokument enthaelt die Feststellung, dass kein Push, kein Deployment und kein Produktionszugriff stattgefunden hat (D-13)
    - Das Dokument enthaelt die phasenweite WR-Zusammenfassung (D-09)
  </acceptance_criteria>
  <verify>
    <automated>cd server && npx vitest run 2>&1 | tail -20 && npx tsc --noEmit && DATABASE_PATH=./database/development.db npm run verify:balance-vs-journal && cd ../desktop && npx tsc --noEmit && npm run check:rules</automated>
  </verify>
  <done>Die geloeschten Zeilen sind aus der Sicherung nachweislich wiederhergestellt und Zeile fuer Zeile mit dem Stand vor der Loeschung verglichen, und alle Abschlussgates der Phase sind mit Zahlen belegt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Loeschwerkzeug → Arbeitsdatenbank | Das erste Werkzeug im Projekt, das tatsaechlich `DELETE` ausfuehrt; ein unvollstaendiges oder zu weites Praedikat vernichtet echte Buchungen |
| Sicherung → Wiederherstellbarkeit | Eine Sicherung, auf die niemand gebaut hat, ist keine; erst der gefuehrte Rueckweg macht sie zu einer |
| Beweisdatenbanken → Arbeitskopien | `14-prod-nach-migration.db` traegt echte Personendaten und ist der Beleg des Produktionsstands |
| Skriptparameter → SQL | Datum, Typliste und ausgenommene Nutzer-Id fliessen in `WHERE`-Bedingungen |
| Arbeitsdatenbank → Produktionsdatenbank | Ein falsch gesetztes `DATABASE_PATH` wuerde ein Loeschwerkzeug auf die Produktion richten |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.1-06-01 | Denial of Service | Datenverlust durch ein zu weites Praedikat — book-once-Zeilen oder Zeilen des Testnutzers 15015 | mitigate | Dreiteiliges Praedikat mit Typfilter aus `overtimeTransactionRebuildService.ts:153-166` und Nutzerausschluss; der Trockenlauf gibt die **ausgenommenen** Zeilen aus; Stufe 4 prueft die Typaufschluesselung der Fundliste ausdruecklich vor dem `--apply` |
| T-14.1-06-02 | Denial of Service | `--apply` ohne vorherigen Trockenlauf oder ohne Sicherung | mitigate | Trockenlauf ist die Vorgabe, `--apply` der Ausnahmeschalter; kein `--force`; Stufen 2 bis 4 sind Abnahmekriterien mit eigenen Zahlen, nicht Empfehlungen |
| T-14.1-06-03 | Repudiation | Sicherung, auf die niemand gebaut hat, verspricht eine Wiederherstellbarkeit, die nicht besteht | mitigate | `VACUUM INTO` statt `cp` (WAL-Verlust); Pruefung der Sicherung **vor** dem Schreiblauf; Task 3 spielt sie tatsaechlich zurueck und vergleicht Zeile fuer Zeile mit einer Differenzspalte |
| T-14.1-06-04 | Tampering | SQL-Injection ueber Datum, Typliste oder Nutzer-Id | mitigate | Alle Werte als Prepared-Statement-Parameter gebunden, Typplatzhalter nach dem `map(() => '?')`-Muster; Abnahmekriterien schliessen eingebettete Zeichenketten und fest verdrahtete Daten aus |
| T-14.1-06-05 | Elevation of Privilege | Das Werkzeug wird gegen die Produktionsdatenbank gerichtet | mitigate | `DATABASE_PATH`-Pflicht mit `process.exit(2)` und ohne stillen Rueckfall; `assertNotProduction()` vor jedem `await import` der DB-beruehrenden Module; `--allow-production` als einzige, laut protokollierte Umgehung, die ohne `--apply` trotzdem nur einen Trockenlauf faehrt; D-13 verbietet den Produktionszugriff in dieser Phase vollstaendig |
| T-14.1-06-06 | Tampering | Schreibzugriff auf eine Beweisdatenbank zerstoert ihre Beweiskraft | mitigate | `14-prod-nach-migration.db` nur `readonly` geoeffnet, Arbeitskopie per `VACUUM INTO` in eine neue Datei; Hashvergleich vor und nach dem Plan als Abnahmekriterium |
| T-14.1-06-07 | Denial of Service | Sichtbare Zahlen der Mitarbeiter aendern sich durch die Bereinigung | mitigate | Vollstaendiger Probelauf auf der Produktionsarbeitskopie mit Saldenvergleich ueber **alle** Nutzer vor dem Lauf gegen `development.db`; eine Differenz ungleich 0,00 h ist ein ausdruecklicher Abbruchgrund |
| T-14.1-06-08 | Information Disclosure | — | accept | Das Werkzeug gibt Nutzer-Ids und Stundenwerte auf der Konsole aus; es laeuft ausschliesslich lokal gegen die Arbeitsdatenbank und schreibt keine Datei ausserhalb des Projekts |

**Keine Paketinstallation in diesem Plan** — `better-sqlite3` und `tsx` sind bereits
installiert; kein `npm install`, damit kein Legitimitaets-Gate und kein `T-{phase}-SC`-Eintrag
erforderlich.
</threat_model>

<verification>
1. `npm run purge:future-overtime` ohne `DATABASE_PATH` — Exit 2, keine neue Datenbankdatei
2. Trockenlauf gegen `development.db` — Hash vorher = nachher
3. Sicherung per `VACUUM INTO` — Groesse groesser 0, `integrity_check` = ok gegen die Sicherung,
   alle Kennzahlen deckungsgleich mit dem Ausgangsstand
4. Probelauf auf `14.1-bereinigung-probe.db` — 0 Nutzer mit Saldendifferenz ungleich 0,00 h
5. `--apply` gegen `development.db` — danach 0 Zukunftszeilen ausserhalb des Testnutzers in
   beiden Tabellen, `integrity_check` = ok, `foreign_key_check` leer
6. Nutzer 15015 unveraendert (Zeilenzahlen vorher = nachher)
7. Wiederherstellungsnachweis — Fundliste gegen `14.1-restore-probe.db` deckungsgleich mit dem
   Trockenlauf, Differenzspalte durchgehend 0
8. `cd server && npx vitest run` — hoechstens 3 rot, genau die drei vorbestehenden
9. `cd server && npx tsc --noEmit` und `cd desktop && npx tsc --noEmit` — Exit 0
10. `cd desktop && npm run check:rules` — Exit 0
11. `npm run verify:balance-vs-journal` gegen `development.db` — 0 Nutzer mit Differenz
12. Hash von `14-prod-nach-migration.db` vorher = nachher
13. D-08-Kennzahlen der fuenf geschuetzten Tabellen: Differenz 0
14. `git log origin/main..HEAD` — kein Push, `.db`-Dateien nicht eingecheckt
</verification>

<success_criteria>
- Kein Journaleintrag und keine Monatszeile traegt ein Datum in der Zukunft (Erfolgskriterium
  der Phase, in Zahlen belegt, Testnutzer 15015 ausgenommen)
- Der zwingende Ablauf aus D-06 ist vollstaendig und in dieser Reihenfolge gefuehrt:
  Sicherung → Trockenlauf → Pruefung → `--apply` → Wiederherstellungsnachweis
- Das Auswahlpraedikat ist festgelegt, begruendet und im Skript nachlesbar; die Abweichung zur
  Roadmap-Zahl 59 ist benannt statt verrechnet
- Die geloeschten Zeilen sind aus der Sicherung nachweislich wiederherstellbar — zurueckgespielt
  und Zeile fuer Zeile verglichen
- Durch die Bereinigung allein bewegt sich kein Saldo (Differenz 0,00 h je Nutzer)
- Die fuenf geschuetzten Tabellen sind unberuehrt (D-08)
- Der Testausgangszustand ist nicht schlechter geworden (D-12)
- Kein Push, kein Deployment, kein Produktionszugriff (D-13); keine WR-Warnung behoben (D-09)
- Ein eigener Commit fuer die Bereinigung, benannter Sicherungspfad, einzeln rueckgaengig
  machbar (D-07)
</success_criteria>

<output>
Create `.planning/phases/14.1-rechenwerk-blocker-aus-dem-produktionslauf-schliessen/14.1-06-SUMMARY.md` when done
</output>
