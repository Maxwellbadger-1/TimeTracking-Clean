---
phase: 09-ein-ma-stab-ein-weg
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md
  - .planning/phases/09-ein-ma-stab-ein-weg/09-DEBUG-SICHTUNG.md
autonomous: true
requirements: [REQ-17, REQ-19]

must_haves:
  truths:
    - "Jede Stelle im Repository, die Soll-Arbeitszeit für einen Tag ermittelt, ist mit Datei und Zeilennummer erfasst"
    - "Für jede Fundstelle steht fest, ob sie im Produktivpfad liegt und ob sie über getDailyTargetHours() läuft"
    - "Die vier offenen Überstunden-Debug-Sessions sind gesichtet und je einem Requirement, einer Phase oder dem Status 'bereits behoben' zugeordnet"
    - "Das Inventar liegt als dauerhaftes Artefakt vor und ist von den Phasen 11 und 14 wiederverwendbar"
  artifacts:
    - path: ".planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md"
      provides: "Vollständiges Inventar der Sollstunden-Ermittlung mit Klassifikation"
      contains: "getDailyTargetHours"
      min_lines: 60
    - path: ".planning/phases/09-ein-ma-stab-ein-weg/09-DEBUG-SICHTUNG.md"
      provides: "Zuordnung der vier offenen Überstunden-Debug-Sessions"
      contains: "carmen-rothemund-overtime-analysis"
      min_lines: 40
  key_links:
    - from: ".planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md"
      to: "server/src/utils/workingDays.ts"
      via: "Referenz auf die kanonische Funktion getDailyTargetHours()"
      pattern: "workingDays\\.ts"
    - from: ".planning/phases/09-ein-ma-stab-ein-weg/09-DEBUG-SICHTUNG.md"
      to: "REQ-19"
      via: "Zuordnung des overtime_comp-Defekts"
      pattern: "REQ-19"
---

<objective>
Bestandsaufnahme vor jedem Eingriff. Dieser Plan schreibt keinen Produktivcode. Er stellt
fest, wo im System Soll-Arbeitszeit ermittelt wird, welche dieser Stellen an
`getDailyTargetHours()` (`server/src/utils/workingDays.ts:63`) vorbeirechnen, und was die vier
offenen Überstunden-Debug-Sessions in `.planning/debug/` heute noch wert sind.

Zweck: REQ-17 verlangt genau einen Weg zur Sollstundenermittlung. Ohne belastbares Inventar
ist weder feststellbar, ob dieser Zustand erreicht ist, noch was dafür geändert werden muss.
Die Pläne 09-02 bis 09-04 setzen auf diesem Inventar auf.

Ergebnis: Zwei dauerhafte Artefakte im Phasenverzeichnis — das Sollstunden-Inventar und die
Debug-Sichtung.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ein-ma-stab-ein-weg/09-CONTEXT.md
@.claude/CLAUDE.md

**Zero-Hallucination-Policy (`.claude/CLAUDE.md`, Abschnitt 0) gilt für diesen Plan absolut.**
Jede Aussage im Inventar über das Verhalten einer Codestelle muss durch ein Zitat mit
`datei:zeile` belegt sein. Formulierungen wie „sieht korrekt aus", „nutzt vermutlich" oder
„der Rest ist analog" sind unzulässig und machen die Abnahme dieses Plans ungültig.

**Antwortsprache: Deutsch.** Beide Artefakte werden auf Deutsch verfasst. Bezeichner,
Dateipfade und Kommandos bleiben unverändert.
</context>

<interfaces>
<!-- Die kanonische Funktion, gegen die alles gemessen wird. Aus dem Quellcode extrahiert. -->

Aus `server/src/utils/workingDays.ts`:
- Zeile 63: `export function getDailyTargetHours(user: UserPublic, date: Date | string): number`
  - Zeile 66-70: Feiertagsprüfung ZUERST (`SELECT 1 FROM holidays WHERE date = ?`) → 0
  - Zeile 73-76: `if (user.workSchedule)` → `user.workSchedule[dayName] || 0`
  - Zeile 80-82: `weeklyHours === 0` → 0
  - Zeile 85-91: Wochenendprüfung `dayOfWeek === 0 || dayOfWeek === 6` → 0
  - Zeile 93: `Math.round((user.weeklyHours / 5) * 100) / 100`
- Zeile 336: `countTargetHoursForUser`-Schleife summiert `getDailyTargetHours(user, d)`

Aus `server/src/config/database.ts`:
- Zeile 20: `export function getDatabasePath(): string` — `DATABASE_PATH` hat höchste Priorität,
  sonst `server/database/development.db` im Development-Modus.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Inventar aller Sollstunden-Fundstellen erstellen</name>
  <files>.planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md</files>
  <read_first>
    - server/src/utils/workingDays.ts (vollständig — die kanonische Funktion)
    - server/src/services/overtimeService.ts (Zeilen 90-210 und 280-530)
    - server/src/services/unifiedOvertimeService.ts (vollständig)
    - server/src/services/overtimeLiveCalculationService.ts (Zeilen 30-110 und 120-410)
    - server/src/services/reportService.ts (Zeilen 94-200)
    - server/src/services/overtimeTransactionRebuildService.ts (Zeilen 80-270)
    - server/src/services/absenceService.ts (Zeilen 1140-1210)
    - server/src/services/exportService.ts (Zeilen 85-110)
    - .claude/CLAUDE.md (Abschnitt „Überstunden-Berechnung")
  </read_first>
  <action>
    Erstelle `.planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md` als
    dauerhaftes Artefakt (nicht nur als SUMMARY-Inhalt — die Phasen 11 und 14 verwenden die
    Aufrufer-Liste wieder, siehe 09-CONTEXT.md Abschnitt „Specific Ideas").

    Erhebe die Fundstellen vollständig über diese vier Suchläufe und halte die
    Rohtreffer-Anzahl je Suchlauf im Dokument fest:
    `grep -rn "getDailyTargetHours" --include=*.ts --include=*.tsx server desktop`,
    `grep -rn "weeklyHours" --include=*.ts --include=*.tsx server/src desktop/src server/scripts`,
    `grep -rn "workSchedule" --include=*.ts --include=*.tsx server/src desktop/src`,
    `grep -rn "targetHours" --include=*.ts server/src`.
    Schließe `node_modules` aus. Der Suchlauf ist der Ausgangspunkt, nicht das Ergebnis —
    jeder Treffer wird gelesen und beurteilt, bevor er ins Inventar oder in die
    Ausschlussliste wandert.

    Das Dokument enthält eine Tabelle mit den Spalten: `Fundstelle` (Format `pfad/datei.ts:zeile`),
    `Funktion/Kontext`, `Was wird ermittelt`, `Weg` (Werte: `getDailyTargetHours` |
    `eigene Rechnung` | `Vorfilter vor getDailyTargetHours`), `Beleg` (wörtliches Codezitat,
    einzeilig).

    Mindestens diese Fundstellen MÜSSEN als Zeilen in der Tabelle stehen, weil sie bereits
    verifiziert vorliegen — fehlt eine, ist das Inventar unvollständig:
    `server/src/services/overtimeLiveCalculationService.ts:66`,
    `server/src/services/overtimeService.ts:309`,
    `server/src/services/overtimeService.ts:347`,
    `server/src/services/overtimeService.ts:320`,
    `server/src/services/overtimeService.ts:360`,
    `server/src/services/unifiedOvertimeService.ts:115`,
    `server/src/services/overtimeTransactionRebuildService.ts:250`,
    `server/src/services/absenceService.ts:1195`,
    `server/src/services/exportService.ts:98`,
    `server/src/utils/workingDays.ts:336`,
    `server/scripts/fix-overtime.ts:78`,
    `server/src/scripts/validateOvertimeCalculation.ts:211`,
    `server/src/scripts/verifyTestData.ts:92`,
    `desktop/src/components/users/WorkScheduleEditor.tsx:44`,
    `desktop/src/components/worktime/WorkScheduleDisplay.tsx:60`.

    Ergänze einen Abschnitt „Ausgeschlossene Treffer" für Fundstellen, die nur Stammdaten
    anzeigen oder speichern (etwa Eingabemasken) — mit Begründung je Eintrag, nicht pauschal.

    Ergänze einen Abschnitt „Referenz: getDailyTargetHours()" mit der Ableitungsreihenfolge
    aus `server/src/utils/workingDays.ts:63-94` (Feiertag → workSchedule → weeklyHours===0 →
    Wochenende → weeklyHours/5), jeweils mit Zeilennummer. Das ist der Maßstab, gegen den
    Task 2 misst.
  </action>
  <acceptance_criteria>
    - Datei `.planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md` existiert
      und hat mindestens 60 Zeilen.
    - Alle 15 oben aufgezählten Pflicht-Fundstellen erscheinen wörtlich im Dokument:
      `for f in "overtimeLiveCalculationService.ts:66" "overtimeService.ts:309" "overtimeService.ts:347" "overtimeService.ts:320" "overtimeService.ts:360" "unifiedOvertimeService.ts:115" "overtimeTransactionRebuildService.ts:250" "absenceService.ts:1195" "exportService.ts:98" "workingDays.ts:336" "fix-overtime.ts:78" "validateOvertimeCalculation.ts:211" "verifyTestData.ts:92" "WorkScheduleEditor.tsx:44" "WorkScheduleDisplay.tsx:60"; do grep -q "$f" .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md || echo "FEHLT: $f"; done`
      gibt keine Zeile aus.
    - Das Dokument enthält die Abschnittsüberschriften „Ausgeschlossene Treffer" und
      „Referenz: getDailyTargetHours()".
    - Die Ableitungsreihenfolge nennt alle fünf Stufen: `grep -c "Feiertag\|workSchedule\|weeklyHours\|Wochenende" .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md` ist ≥ 5.
    - Keine Tabellenzeile enthält die Wörter „vermutlich", „vermutet", „sollte", „scheint"
      oder „analog": `grep -n "vermutlich\|vermutet\|scheint\|analog" .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md` gibt nichts aus.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md && test $(wc -l < .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md) -ge 60 && ! grep -q "vermutlich\|vermutet\|scheint\|analog" .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md && echo OK</automated>
  </verify>
  <done>Das Inventar listet jede Sollstunden-Fundstelle mit Datei, Zeile und wörtlichem Codebeleg; die 15 verifizierten Pflicht-Fundstellen sind enthalten.</done>
</task>

<task type="auto">
  <name>Task 2: Fundstellen klassifizieren und Abweichungsliste ableiten</name>
  <files>.planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md</files>
  <read_first>
    - .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md (Ergebnis aus Task 1)
    - server/src/routes/overtime.ts (Zeilen 30-140, 460-560, 640-730, 860-1000)
    - server/src/routes/reports.ts (Zeilen 1-210)
    - server/src/services/overtimeLiveCalculationService.ts (Zeilen 30-110)
    - server/src/services/overtimeService.ts (Zeilen 280-400)
  </read_first>
  <action>
    Ergänze in `09-INVENTAR-SOLLSTUNDEN.md` je Fundstelle zwei weitere Spalten:
    `Produktivpfad` (Werte `ja` | `nein`) und `Erreichbar über` (konkreter Express-Endpunkt,
    Cronjob oder npm-Skript, das die Stelle ausführt).

    „Produktivpfad = ja" bedeutet: Die Stelle ist von einem registrierten Express-Handler in
    `server/src/routes/` aus erreichbar oder läuft in einem geplanten Job. Weise das je Eintrag
    durch eine Aufrufkette nach (Route → Service → Funktion), jedes Glied mit `datei:zeile`.
    Dateien unter `server/scripts/`, `server/src/scripts/`, `*.test.ts` sowie die
    `debug_*.ts` / `test_*.ts` im Serverwurzelverzeichnis sind `nein` — belege das dadurch,
    dass kein Import aus `server/src/routes/` oder `server/src/server.ts` auf sie zeigt.

    Ergänze anschließend den Abschnitt „Abweichungen (REQ-17)" mit einer nummerierten Liste
    `A-1`, `A-2`, … Aufgenommen wird jede Fundstelle mit `Produktivpfad = ja` UND
    `Weg != getDailyTargetHours`. Je Abweichung: Fundstelle, welche der fünf Stufen von
    `getDailyTargetHours()` sie überspringt oder vorwegnimmt, und welcher fachliche Fall
    dadurch falsch gerechnet wird. Konkretes Beispiel, das erhoben werden muss: Ein Nutzer mit
    `workSchedule.saturday > 0` — was liefert `server/src/services/overtimeService.ts:309-312`
    (`isWeekend → continue`) für diesen Samstag, und was liefert `getDailyTargetHours()`?
    Ergebnis beider Wege im Dokument nennen.

    Ergänze den Abschnitt „Nicht-Abweichungen mit Begründung" für Fundstellen mit
    `Produktivpfad = nein`, die trotzdem eigenständig rechnen — sie fallen nicht unter REQ-17,
    aber die Begründung gehört dokumentiert, damit Phase 14 sie nicht erneut aufrollt.

    Schließe mit einem Abschnitt „Urteil REQ-17": Entweder „erfüllt" (Abweichungsliste leer)
    oder „nicht erfüllt — n Abweichungen, siehe A-1 … A-n". Kein drittes Urteil.
  </action>
  <acceptance_criteria>
    - Das Dokument enthält die Abschnittsüberschriften „Abweichungen (REQ-17)",
      „Nicht-Abweichungen mit Begründung" und „Urteil REQ-17".
    - Jede Zeile der Fundstellen-Tabelle hat einen Wert in `Produktivpfad`:
      `grep -c "| ja |\|| nein |" .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md` ist ≥ 15.
    - Der Abschnitt „Urteil REQ-17" enthält entweder das Wort „erfüllt" oder „nicht erfüllt":
      `grep -A 5 "Urteil REQ-17" .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md | grep -q "erfüllt"` liefert Exit 0.
    - Der Samstags-Fall ist erhoben: `grep -qi "samstag\|saturday" .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md` liefert Exit 0.
    - Mindestens eine Abweichung `A-1` ist vergeben oder die Abweichungsliste ist ausdrücklich
      als leer gekennzeichnet: `grep -q "A-1\|Abweichungsliste ist leer" .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Abweichungen (REQ-17)" .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md && grep -q "Urteil REQ-17" .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md && grep -qi "samstag\|saturday" .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md && grep -q "A-1\|Abweichungsliste ist leer" .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md && echo OK</automated>
  </verify>
  <done>Jede Fundstelle ist als Produktivpfad ja/nein klassifiziert, die REQ-17-Abweichungen sind nummeriert und begründet, und das Urteil zu REQ-17 steht fest.</done>
</task>

<task type="auto">
  <name>Task 3: Die vier Überstunden-Debug-Sessions sichten und zuordnen</name>
  <files>.planning/phases/09-ein-ma-stab-ein-weg/09-DEBUG-SICHTUNG.md</files>
  <read_first>
    - .planning/debug/carmen-rothemund-overtime-analysis.md
    - .planning/debug/overtime-compensation-workschedule-bug.md
    - .planning/debug/overtime-validation-backend-mismatch.md
    - .planning/debug/OVERTIME-FIX-PLAN.md
    - .planning/debug/urlaubstage-bei-ablehnung-verloren.md (Abschnitt „Bug 3" und „Bug 4")
    - server/src/services/overtimeTransactionService.ts (Zeilen 100-140 und 400-430)
    - server/src/services/overtimeTransactionRebuildService.ts (Zeilen 94-140)
    - server/src/services/overtimeService.ts (Zeilen 595-680)
  </read_first>
  <action>
    Erstelle `.planning/phases/09-ein-ma-stab-ein-weg/09-DEBUG-SICHTUNG.md`.

    Für jede der vier Sessions — `carmen-rothemund-overtime-analysis`,
    `overtime-compensation-workschedule-bug`, `overtime-validation-backend-mismatch`,
    `OVERTIME-FIX-PLAN` — einen Abschnitt mit: behauptetem Defekt, betroffener Codestelle laut
    Session, IST-Zustand dieser Codestelle heute (wörtliches Zitat mit `datei:zeile` aus dem
    aktuellen Arbeitsverzeichnis) und daraus abgeleitetem Status.

    Erlaubte Statuswerte, genau einer je Session: `behoben — Beleg im Code`,
    `offen — gehört zu REQ-17`, `offen — gehört zu REQ-18`, `offen — gehört zu REQ-19`,
    `offen — außerhalb Milestone v3.0, verbleibt in .planning/debug/`.

    Prüfe insbesondere diese drei bereits bekannten Angelpunkte und halte das Ergebnis fest:
    (a) `getOvertimeBalance()` in `server/src/services/overtimeTransactionService.ts:406` —
    enthält die SQL heute den Filter `AND month <= strftime('%Y-%m', 'now')`?
    (b) `getOvertimeSummary()` in `server/src/services/overtimeService.ts` — enthält die
    Monatsabfrage heute `AND month <= ?`?
    (c) `overtimeTransactionRebuildService.ts` — löscht der Rebuild heute noch alle
    Transaktionen eines Monats ohne Typfilter, oder gibt es die Liste `REBUILDABLE_TYPES`?
    Zitiere jeweils die tatsächliche Zeile, nicht die Behauptung aus der Session.

    Halte gesondert fest, was der Kommentarblock in
    `server/src/services/overtimeTransactionRebuildService.ts` über den verbleibenden Defekt
    aussagt („That the overtime_comp debit never reaches the balance at all is a separate
    defect") — diese Selbstauskunft des Codes ist der direkte Anknüpfungspunkt für REQ-19 und
    muss mit Zeilennummer im Dokument stehen.

    Schließe mit einer Übersichtstabelle `Session | Status | Zuordnung | Nachfolgeplan`.
    `Nachfolgeplan` verweist auf `09-03` (REQ-17/REQ-18) oder `09-04` (REQ-19) oder trägt
    „keiner". Die beiden Sessions `unpaid-leave-logic-issues` und
    `notifications-position-column-missing` sind laut 09-CONTEXT.md ausdrücklich zurückgestellt
    und werden NICHT bearbeitet — vermerke das in einer Fußzeile.
  </action>
  <acceptance_criteria>
    - Datei `.planning/phases/09-ein-ma-stab-ein-weg/09-DEBUG-SICHTUNG.md` existiert mit
      mindestens 40 Zeilen.
    - Alle vier Session-Namen kommen vor: `for s in carmen-rothemund-overtime-analysis overtime-compensation-workschedule-bug overtime-validation-backend-mismatch OVERTIME-FIX-PLAN; do grep -q "$s" .planning/phases/09-ein-ma-stab-ein-weg/09-DEBUG-SICHTUNG.md || echo "FEHLT: $s"; done` gibt nichts aus.
    - Jeder der drei Angelpunkte ist mit Zeilennummer belegt:
      `grep -c "overtimeTransactionService.ts:4\|overtimeService.ts:6\|REBUILDABLE_TYPES" .planning/phases/09-ein-ma-stab-ein-weg/09-DEBUG-SICHTUNG.md` ist ≥ 3.
    - Die Zuordnung zu REQ-19 ist vorhanden: `grep -q "REQ-19" .planning/phases/09-ein-ma-stab-ein-weg/09-DEBUG-SICHTUNG.md`.
    - Die Übersichtstabelle verweist auf mindestens einen Nachfolgeplan:
      `grep -q "09-03\|09-04" .planning/phases/09-ein-ma-stab-ein-weg/09-DEBUG-SICHTUNG.md`.
    - Die Fußzeile nennt die zurückgestellten Sessions: `grep -q "unpaid-leave-logic-issues" .planning/phases/09-ein-ma-stab-ein-weg/09-DEBUG-SICHTUNG.md`.
  </acceptance_criteria>
  <verify>
    <automated>test $(wc -l < .planning/phases/09-ein-ma-stab-ein-weg/09-DEBUG-SICHTUNG.md) -ge 40 && grep -q "REQ-19" .planning/phases/09-ein-ma-stab-ein-weg/09-DEBUG-SICHTUNG.md && grep -q "REBUILDABLE_TYPES" .planning/phases/09-ein-ma-stab-ein-weg/09-DEBUG-SICHTUNG.md && grep -q "unpaid-leave-logic-issues" .planning/phases/09-ein-ma-stab-ein-weg/09-DEBUG-SICHTUNG.md && echo OK</automated>
  </verify>
  <done>Alle vier Debug-Sessions haben genau einen Status, sind belegt und einem Nachfolgeplan oder „keiner" zugeordnet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| keine neue | Dieser Plan erzeugt keinen ausführbaren Code und keine neue Ein-/Ausgabe. Es entstehen ausschließlich Markdown-Artefakte im Repository. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-I | Information Disclosure | Die beiden Artefakte im Repository | mitigate | Im Inventar und in der Debug-Sichtung nur Nutzer-IDs, Vor-/Nachname und Arbeitszeitmodell führen, keine E-Mail-Adressen, keine Passwort-Hashes, keine Session-Daten. Es wird keine Datenbank abgefragt — die Erhebung erfolgt allein aus Quellcode. |
| T-09-01-R | Repudiation | Aussagen ohne Beleg | mitigate | Zero-Hallucination-Policy: jede Verhaltensaussage mit `datei:zeile`-Zitat. Die Abnahmekriterien schließen Vermutungsvokabular per grep aus. |
| T-09-01-SC | Tampering | npm/pip/cargo-Installationen | mitigate | Dieser Plan installiert keine Pakete. Kein `npm install`, keine neue Abhängigkeit. Sollte während der Ausführung eine Installation nötig erscheinen, ist der Plan zu stoppen statt zu installieren. |
</threat_model>

<verification>
- Beide Artefakte existieren im Phasenverzeichnis und sind auf Deutsch verfasst.
- `git status` zeigt ausschließlich Änderungen unterhalb von
  `.planning/phases/09-ein-ma-stab-ein-weg/` — kein Produktivcode wurde angefasst:
  `git status --porcelain | grep -v "^?? .planning/\|^ M .planning/\|^A  .planning/"` gibt nichts aus.
- Kein Schreibzugriff auf eine Datenbank erfolgt (D5): Der Plan enthält keinen
  Datenbankzugriff, weder lesend noch schreibend.
</verification>

<success_criteria>
- REQ-17 hat ein belastbares Urteil: entweder „erfüllt" oder eine nummerierte Abweichungsliste
  `A-1 … A-n`, die Plan 09-03 abarbeiten kann.
- Die vier Überstunden-Debug-Sessions sind gesichtet; jede hat genau einen Status und eine
  Zuordnung.
- Die Aufrufer-Liste im Inventar ist so aufgebaut, dass die Phasen 11 und 14 sie ohne erneute
  Erhebung wiederverwenden können (09-CONTEXT.md, „Specific Ideas").
</success_criteria>

<output>
Create `.planning/phases/09-ein-ma-stab-ein-weg/09-01-SUMMARY.md` when done.
Die SUMMARY nennt die Anzahl der Fundstellen, die Anzahl der Abweichungen `A-n` und den Status
je Debug-Session — sie wiederholt die Artefakte nicht, sondern verweist auf sie.
</output>
