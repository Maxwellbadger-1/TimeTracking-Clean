---
phase: 09-ein-ma-stab-ein-weg
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - server/src/services/overtimeLiveCalculationService.ts
  - server/src/services/overtimeLiveCalculationService.test.ts
  - server/src/services/overtimeService.ts
  - .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md
  - .planning/phases/09-ein-ma-stab-ein-weg/09-ANGLEICHUNG-NACHWEIS.md
  - .planning/phases/09-ein-ma-stab-ein-weg/09-ANGLEICHUNG-BASELINE.json
  - server/scripts/fix-overtime.ts
  - .planning/phases/09-ein-ma-stab-ein-weg/09-A1-NACHWEIS.md
  - .planning/phases/09-ein-ma-stab-ein-weg/09-A1-VORHER.json
  - .planning/phases/09-ein-ma-stab-ein-weg/09-A1-NACHHER.json
  - .planning/STATE.md
autonomous: true
requirements: [REQ-17, REQ-18]

must_haves:
  truths:
    - "Kein Produktivpfad entscheidet mehr eigenständig, ob ein Tag ein Arbeitstag ist — die Entscheidung fällt über getDailyTargetHours() (REQ-17)"
    - "Ein Nutzer mit workSchedule.saturday > 0 bekommt seinen Samstag in allen Produktivpfaden als Arbeitstag gezählt"
    - "Der Legacy-Pfad overtimeService.ts bleibt bestehen und schreibt weiter overtime_balance, löst seine Sollstunden aber nachweislich über getDailyTargetHours() auf (D1, REQ-18)"
    - "Dashboard und Berichte liefern für die drei Prüfnutzer denselben Überstundenwert — belegt durch Exit-Code 0 des Vergleichswerkzeugs"
    - "Das bei jedem Deployment und täglich um 3 Uhr laufende Skript server/scripts/fix-overtime.ts berechnet keine Sollstunden mehr selbst, sondern schreibt overtime_balance über ensureOvertimeBalanceEntries() -> unifiedOvertimeService -> getDailyTargetHours() (Abweichung A-1, REQ-17)"
    - "Der Zusammenhang zwischen fix-overtime.ts und .github/workflows/deploy-server.yml ist schriftlich festgehalten, damit Phase 14 ihn vorfindet statt ihn neu zu entdecken"
    - "STATE.md behauptet nicht länger, der tägliche Cron sei deaktiviert"
  artifacts:
    - path: "server/src/services/overtimeLiveCalculationService.test.ts"
      provides: "Regressionsnetz gegen den Rückfall in eine eigene Arbeitstags-Entscheidung"
      contains: "saturday"
    - path: ".planning/phases/09-ein-ma-stab-ein-weg/09-ANGLEICHUNG-NACHWEIS.md"
      provides: "Vorher/Nachher-Beleg der Angleichung mit Exit-Codes und Belegkette für REQ-18"
      min_lines: 40
    - path: ".planning/phases/09-ein-ma-stab-ein-weg/09-A1-NACHWEIS.md"
      provides: "Vorher/Nachher-Messung zu A-1 und die Auslieferungslogik von fix-overtime.ts als Übergabe an Phase 14"
      min_lines: 50
      contains: "deploy-server.yml:118"
  key_links:
    - from: "server/src/services/overtimeLiveCalculationService.ts"
      to: "server/src/utils/workingDays.ts"
      via: "getDailyTargetHours entscheidet über den Arbeitstag"
      pattern: "getDailyTargetHours\\(user"
    - from: "server/src/services/overtimeService.ts"
      to: "server/src/services/unifiedOvertimeService.ts"
      via: "updateMonthlyOvertime delegiert die Monatsrechnung"
      pattern: "unifiedOvertimeService\\.calculateMonthlyOvertime"
    - from: "server/scripts/fix-overtime.ts"
      to: "server/src/services/overtimeService.ts"
      via: "das Deploy-/Cron-Skript schreibt overtime_balance über die kanonische Funktion statt über eine eigene Kopie"
      pattern: "ensureOvertimeBalanceEntries"
---

<objective>
Die in Plan 09-01 gefundenen Abweichungen im Produktivpfad beseitigen und den Legacy-Pfad
nachweislich auf dieselbe Sollstunden-Auflösung ziehen wie `unifiedOvertimeService.ts`.

**D1 gilt: angleichen, nicht stilllegen.** `overtimeService.ts` bleibt bestehen, bleibt
aufrufbar und schreibt weiter `overtime_balance`. Es wird kein Aufrufer umgebogen, kein Export
entfernt, keine Route umgehängt. Der vollständige Rückbau des Legacy-Systems ist in
REQUIREMENTS.md ausdrücklich out of scope.

Zweck: REQ-17 verlangt genau einen Weg zur Sollstundenermittlung, REQ-18 übereinstimmende
Werte in Dashboard und Berichten. Nach diesem Plan ist beides nicht behauptet, sondern durch
den Exit-Code des Werkzeugs aus Plan 09-02 belegt.

Ergebnis: Zwei bereinigte Services, ein Regressionstest und der Vorher/Nachher-Nachweis.

**Nachtrag nach Welle 1 und 2 — Abweichung A-1.** Das Inventar aus Plan 09-01 hat eine
Fundstelle ergeben, die beim ersten Entwurf dieses Plans noch nicht bekannt war:
`server/scripts/fix-overtime.ts` ermittelt seine Sollstunden selbst und läuft bei jedem
Deployment sowie täglich um 3 Uhr gegen die Produktionsdatenbank. Sie fällt unter REQ-17 und
wird in Task 3 angeglichen, in Task 4 belegt. Ohne diese Angleichung würde das Skript jedes
Ergebnis dieses Milestones binnen höchstens 24 Stunden wieder überschreiben — und zwar erst
nach dem Deployment, wenn niemand mehr hinsieht.

D1 gilt auch hier: Das Skript wird angeglichen, nicht stillgelegt. Es bleibt bestehen, läuft
weiter und schreibt weiter `overtime_balance` — nur eben über denselben Maßstab wie alle
anderen. Der Deploy-Workflow selbst wird in Phase 9 **nicht** geändert; das ist
Auslieferungslogik und gehört nach Phase 14. Task 4 hält den Zusammenhang schriftlich fest,
damit Phase 14 ihn vorfindet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ein-ma-stab-ein-weg/09-CONTEXT.md
@.planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md
@.planning/phases/09-ein-ma-stab-ein-weg/09-VERGLEICH-BEFUND.md
@.planning/phases/09-ein-ma-stab-ein-weg/09-PRUEFNUTZER.csv
@.claude/CLAUDE.md

**NO REGRESSION (`.claude/CLAUDE.md`, Kernprinzip 1).** Die geänderten Funktionen erzeugen
`overtime_transactions`-Zeilen, die im Kontoauszug aus Milestone v2.0 sichtbar sind. Die
Serversuite muss nach der Änderung mindestens so viele Tests bestehen wie vorher — die
Ausgangszahl wird in Task 1 gemessen, bevor irgendetwas geändert wird.

**D5 gilt weiter:** Läufe ausschließlich gegen `server/database/development.db`. Änderungen
gehen den normalen Weg über `git push origin main`; dieser Plan deployt nicht.

**Antwortsprache: Deutsch** für Dokumente und Kommentare. Bezeichner und Pfade unverändert.
</context>

<interfaces>
<!-- Die betroffenen Stellen, aus dem Quellcode extrahiert. -->

Aus `server/src/services/overtimeLiveCalculationService.ts`:
- Zeile 20-25: importiert bereits `getDailyTargetHours` aus `../utils/workingDays.js` und den
  Typ `UserPublic` aus `../types/index.js`
- Zeile 36-40: `function getAllWorkingDaysBetween(startDate: string, endDate: string, workSchedule: Record<string, number> | null, _weeklyHours: number): string[]`
  — nicht exportiert
- Zeile 50-53: eigene Feiertagsabfrage `SELECT 1 FROM holidays WHERE date = ?`
- Zeile 57-68: eigene Arbeitstags-Entscheidung. Mit `workSchedule`: `hoursForDay > 0`.
  Ohne `workSchedule`: `dayOfWeek >= 1 && dayOfWeek <= 5` — Kommentar Zeile 66:
  „Default: Monday-Friday are working days (weeklyHours/5)"
- Zeile 242: einziger Aufrufer,
  `const allWorkingDays = getAllWorkingDaysBetween(startDate, endDate, workSchedule, user.weeklyHours);`
  — an dieser Stelle liegt `user` bereits vor
- Zeile 501 in `server/src/routes/overtime.ts`: dynamischer Import des Services für
  `GET /api/overtime/transactions/live` — damit ist die Stelle Produktivpfad

Aus `server/src/services/overtimeService.ts` — fünfmal dasselbe Muster
`const dayOfWeek = d.getDay(); const isWeekend = dayOfWeek === 0 || dayOfWeek === 6; if (isWeekend) continue;`
gefolgt von einer eigenen `holidays`-Abfrage, direkt vor einem Aufruf von `getDailyTargetHours`:
- Zeile 99-106 in `_calculateAbsenceCreditsForMonth` (Zeile 46) — im Repository ohne Aufrufer
- Zeile 188-195 in `_calculateUnpaidLeaveForMonth` (Zeile 145) — im Repository ohne Aufrufer
- Zeile 309-317 in `ensureAbsenceTransactionsForMonth` (Zeile 229), PHASE 1
- Zeile 350-357 in `ensureAbsenceTransactionsForMonth` (Zeile 229), PHASE 2
- Zeile 1312-1320 in `ensureAbsenceTransactions` (Zeile 1251)

Aus `server/src/services/overtimeService.ts` — die Belegkette für REQ-18:
- Zeile 421: `export function updateMonthlyOvertime(userId: number, month: string): void`
- Zeile 454: `const monthlyResult = unifiedOvertimeService.calculateMonthlyOvertime(userId, month);`
  (korrigiert gegenüber dem Erstentwurf, der 453 nannte — `grep -n` gegen die Datei liefert 454)
- Zeile 475-486: schreibt `monthlyResult.targetHours` und `monthlyResult.actualHours` nach
  `overtime_balance`
- `server/src/services/unifiedOvertimeService.ts:115`:
  `const rawTargetHours = getDailyTargetHours(user, date);`

Aus `server/src/utils/workingDays.ts:63-94` — der Maßstab: Feiertag → `workSchedule[dayName]`
→ `weeklyHours === 0` → Wochenende → `weeklyHours / 5`.

## Abweichung A-1 — `server/scripts/fix-overtime.ts` (188 Zeilen)

Jede Zeilenangabe unten wurde bei der Planüberarbeitung einzeln am Quellcode nachgeprüft:

- Zeile 16: `const db = new Database(dbPath);` — eine zweite, eigene Datenbankverbindung
- Zeile 22: `import { countWorkingDaysBetween } from '../dist/utils/workingDays.js';` — die Datei
  importiert aus `dist/`, nicht aus `src/`
- Zeile 27: Kommentar `// Same logic as in overtimeService.ts` — die lokale Kopie ist im Code
  selbst als Dublette ausgewiesen
- Zeile 28: `function ensureOvertimeBalanceEntries(userId: number, targetMonth: string)` (Kopie)
- Zeile 30: `.prepare('SELECT hireDate, weeklyHours FROM users WHERE id = ?')` — `workSchedule`
  wird gar nicht erst geladen
- Zeile 73-76: Kommentar `// IMPORTANT: Use countWorkingDaysBetween() for consistency - it
  excludes holidays!` und `const workingDays = countWorkingDaysBetween(startDate, effectiveEnd, db);`
  — die Feiertagslogik ist korrekt eingebunden
- **Zeile 78: `const targetHoursPerDay = user.weeklyHours / 5;`** — der Bypass
- Zeile 79: `const targetHours = workingDays * targetHoursPerDay;`
- Zeile 101: `const absenceCredits = (absenceResult?.total || 0) * targetHoursPerDay;` — derselbe
  flache Tageswert ein zweites Mal
- Zeile 117: `const unpaidLeaveReduction = (unpaidLeaveResult?.total || 0) * targetHoursPerDay;`
- Zeile 125-130: UPSERT nach `overtime_balance` mit
  `ON CONFLICT(userId, month) DO UPDATE SET targetHours = ?, actualHours = ?`
- Zeile 151-183: Nutzerschleife, Zeile 159: `ensureOvertimeBalanceEntries(user.id, currentMonth);`
- Zeile 188: `db.close();`

Der kanonische Ersatz existiert bereits als Export:

- `server/src/services/overtimeService.ts:684`:
  `export async function ensureOvertimeBalanceEntries(userId: number, upToMonth: string): Promise<void>`
- Zeile 745: `const monthlyResult = unifiedOvertimeService.calculateMonthlyOvertime(userId, month);`
- Zeile 767: `INSERT INTO overtime_balance (userId, month, targetHours, actualHours)` mit
  demselben `ON CONFLICT(userId, month) DO UPDATE SET targetHours = ?, actualHours = ?`
  → **identisches Schreibverhalten**, dieselbe Tabelle, dieselben zwei Spalten. Nur die Rechnung
  ist die kanonische: `unifiedOvertimeService.ts:115` löst je Tag über
  `getDailyTargetHours(user, date)` auf.

## `.github/workflows/deploy-server.yml` — Auslieferungslogik, in Phase 9 NICHT zu ändern

- Zeile 116-121: Ausführung bei **jedem** Deployment. Zeile 118:
  `DATABASE_PATH=$DATABASE_PATH NODE_ENV=production npx tsx scripts/fix-overtime.ts || {`.
  Zeile 119-120 fängt einen Fehlschlag mit einer Warnung ab und setzt das Deployment fort.
- Zeile 123-130: Installation eines täglichen Cronjobs bei **jedem** Deployment. Zeile 125 setzt
  `CRON_COMMAND="0 3 * * * cd /home/ubuntu/TimeTracking-Clean/server && DATABASE_PATH=/home/ubuntu/databases/production.db NODE_ENV=production npx tsx scripts/fix-overtime.ts >> /home/ubuntu/logs/overtime-fix.log 2>&1"`,
  Zeile 127 schreibt ihn mit
  `(crontab -l 2>/dev/null | grep -v "fix-overtime.ts"; echo "$CRON_COMMAND") | crontab -`.
  Der Cron trägt `DATABASE_PATH` also bereits gesetzt.
  Korrektur gegenüber der Vorgabe an die Planung („:124-127"): Der Cron-Block umfasst die
  Zeilen 123-130, die Installation selbst 125-129.

## Randbedingungen für den Umbau — geprüft, nicht angenommen

- `server/tsconfig.json` hat `"include": ["src/**/*"]`. `server/scripts/fix-overtime.ts` liegt
  außerhalb: `npx tsc --noEmit` prüft diese Datei **nicht**. Der Beleg, dass die Änderung trägt,
  ist der tatsächliche Lauf gegen eine lokale Kopie — nicht der Typcheck.
- `server/dist/` existiert im Arbeitsverzeichnis derzeit nicht
  (`ls server/dist/utils/workingDays.js` → nicht vorhanden). Vor jedem lokalen Lauf des Skripts
  ist `cd server && npm run build` nötig.
- `server/src/utils/workingDays.ts:1` importiert `db` aus `../database/connection.js`.
  `getDailyTargetHours` (Zeile 63) nimmt **keine** Datenbankinstanz entgegen — anders als
  `countWorkingDaysBetween(fromDate, toDate, dbInstance?)` (Zeile 264). Beide Wege lösen den Pfad
  über `databaseConfig.path` auf (`server/src/config/database.ts`), also über dieselbe Datei;
  `connection.ts` exportiert die Verbindung als Proxy (`export { dbProxy as db }`, Zeile 122).
  Weil `fix-overtime.ts` über `workingDays.js` ohnehin schon `connection.js` lädt, ist diese
  Verbindung im Prozess bereits offen — der Umbau eröffnet keine zusätzliche, er entfernt eine.
- **Das Vergleichswerkzeug repariert, was es messen soll.**
  `server/src/services/overtimeService.ts:618` ruft in `getOvertimeSummary()`
  `await ensureOvertimeBalanceEntries(userId, endMonth)` auf.
  `server/src/scripts/compareOvertimePaths.ts:239` erhebt den `dashboard`-Weg genau über
  `getOvertimeSummary`; erst danach liest Zeile 265 `SELECT overtime FROM overtime_balance`.
  Folge: Eine von `fix-overtime.ts` falsch geschriebene Zeile wird im selben Lauf überschrieben,
  bevor sie gelesen wird. Der Exit-Code von `npm run validate:overtime:paths` kann A-1 deshalb
  weder beweisen noch widerlegen. Er ist der Regressionswächter. Der A-1-Beleg ist die direkte
  SQL-Lesung von `overtime_balance.targetHours` unmittelbar nach dem Skriptlauf — und genau
  dieses Verhalten macht die Messung in Task 3/4 auswertbar: derselbe Kopiedatenbestand liefert
  vor dem Werkzeuglauf den Skriptwert und nach dem Werkzeuglauf den kanonischen Wert.
- `overtime_balance.overtime` ist eine generierte Spalte (`server/src/database/schema.ts:266`:
  `overtime REAL GENERATED ALWAYS AS (actualHours - targetHours) VIRTUAL`) — ein falscher
  `targetHours`-Wert schlägt unmittelbar auf den angezeigten Saldo durch.
- Die aktive lokale Datenbank ist `server/database/development.db`. `.claude/CLAUDE.md`
  dokumentiert fälschlich `./database.db`; `server/database.db` (02.04.2026) ist eine tote
  Altlast. Siehe `.planning/notes/db-pfad-diskrepanz-20260821.md`.
  `.gitignore:27-29` deckt `*.db`, `*.db-shm`, `*.db-wal` ab — Arbeitskopien landen nicht im Commit.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Arbeitstags-Entscheidung im Live-Service auf getDailyTargetHours() ziehen</name>
  <files>server/src/services/overtimeLiveCalculationService.ts, server/src/services/overtimeLiveCalculationService.test.ts</files>
  <read_first>
    - server/src/services/overtimeLiveCalculationService.ts (Zeilen 1-120 und 230-270)
    - server/src/utils/workingDays.ts (Zeilen 40-100)
    - server/src/utils/workingDays.test.ts (Zeilen 460-500 — bestehendes Testmuster für getDailyTargetHours)
    - server/src/routes/overtime.ts (Zeilen 467-556 — der Endpunkt /transactions/live)
    - server/vitest.config.ts
    - .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md (Abschnitt „Abweichungen (REQ-17)")
  </read_first>
  <behavior>
    Erwartetes Verhalten von `getAllWorkingDaysBetween(startDate, endDate, user)` nach der
    Umstellung. Diese Tests entstehen VOR der Änderung und müssen zuerst rot sein:
    - Nutzer mit `workSchedule = { monday: 8, ..., saturday: 4, sunday: 0 }`: Ein Samstag im
      Zeitraum ERSCHEINT in der Rückgabe. (Vor der Umstellung fehlt er, weil Zeile 66-67 nur
      Mo–Fr zulässt, sobald kein `workSchedule` greift, bzw. weil der Aufrufer nur
      `workSchedule` weiterreicht.)
    - Nutzer mit `workSchedule.wednesday = 0`: Der Mittwoch ERSCHEINT NICHT.
    - Nutzer ohne `workSchedule` und `weeklyHours = 40`: Mo–Fr erscheinen, Sa und So nicht.
    - Nutzer ohne `workSchedule` und `weeklyHours = 0` (Aushilfe): kein Tag erscheint.
    - Ein Datum, das in `holidays` steht, erscheint für keinen der obigen Nutzer — auch nicht,
      wenn `workSchedule` für diesen Wochentag Stunden vorsieht.
    - Der Rückgabewert ist chronologisch aufsteigend und enthält keinen Tag doppelt.

    Der Feiertagsfall wird gegen die vorhandene `holidays`-Tabelle der Entwicklungsdatenbank
    geprüft: Wähle im Test ein Datum, das per Abfrage nachweislich in `holidays` steht, statt
    ein Datum zu raten. Der Test schreibt nichts in die Datenbank.
  </behavior>
  <action>
    Miss zuerst die Ausgangslage der Serversuite und halte die Zahl für Task 3 fest:
    `cd server && npx vitest run 2>&1 | tail -20`. Notiere bestandene und fehlgeschlagene Tests.
    Laut `server/vitest.config.ts` sind zum Zeitpunkt der Planung 179 von 182 bekannt und drei
    Fehlschläge begründet zurückgestellt — weiche Abweichungen davon gehören dokumentiert.

    Ändere `getAllWorkingDaysBetween` in
    `server/src/services/overtimeLiveCalculationService.ts`:
    - Neue Signatur: `(startDate: string, endDate: string, user: UserPublic): string[]`.
      Die Parameter `workSchedule` und `_weeklyHours` entfallen; beide Angaben stecken bereits
      im `user`-Objekt.
    - Exportiere die Funktion (`export function`), damit der Test sie ansprechen kann.
    - Entferne die eigene `holidays`-Abfrage (Zeile 50-53) und die eigene Arbeitstags-Logik
      (Zeile 57-68) ersatzlos. Ein Tag gehört genau dann in die Rückgabe, wenn
      `getDailyTargetHours(user, dateStr) > 0` gilt. Die Feiertagsprüfung erledigt
      `getDailyTargetHours` selbst (`server/src/utils/workingDays.ts:66-70`) — die doppelte
      Abfrage entfällt damit ersatzlos und spart zugleich einen Datenbankzugriff pro Tag.
    - Passe den einzigen Aufrufer in Zeile 242 an: `getAllWorkingDaysBetween(startDate, endDate, user)`.
      Prüfe, ob die lokale Variable `workSchedule` danach noch verwendet wird; ist sie
      unbenutzt, entferne sie, damit `npx tsc --noEmit` sauber bleibt.
    - Setze über die Funktion einen deutschen Kommentar, der REQ-17 nennt und festhält, warum
      hier nicht mehr selbst über Wochentage entschieden wird. Verweise auf
      `server/src/utils/workingDays.ts:63` als kanonische Stelle.

    Datumsarithmetik: Die vorhandene Schleife nutzt `new Date(startDate + 'T12:00:00')` und
    `formatDate(d, 'yyyy-MM-dd')`. Behalte beides bei. Verwende an keiner Stelle
    `toISOString().split('T')[0]` — laut `.claude/CLAUDE.md` → Verbote führt das zu
    Zeitzonenfehlern.

    Lege `server/src/services/overtimeLiveCalculationService.test.ts` mit den Fällen aus dem
    `<behavior>`-Block an.
  </action>
  <acceptance_criteria>
    - `cd server && npx vitest run src/services/overtimeLiveCalculationService.test.ts` läuft
      grün mit mindestens 6 Testfällen.
    - Die eigene Arbeitstags-Entscheidung ist verschwunden:
      `grep -n "dayOfWeek >= 1 && dayOfWeek <= 5" server/src/services/overtimeLiveCalculationService.ts` gibt nichts aus.
    - Die doppelte Feiertagsabfrage in dieser Funktion ist verschwunden: Zwischen der
      Funktionsdeklaration `export function getAllWorkingDaysBetween` und ihrer schließenden
      Klammer kommt `FROM holidays` nicht mehr vor.
    - Die Funktion ist exportiert:
      `grep -q "export function getAllWorkingDaysBetween" server/src/services/overtimeLiveCalculationService.ts`.
    - Die Entscheidung läuft über den Maßstab:
      `grep -q "getDailyTargetHours(user" server/src/services/overtimeLiveCalculationService.ts`.
    - REQ-17 ist im Kommentar genannt:
      `grep -q "REQ-17" server/src/services/overtimeLiveCalculationService.ts`.
    - Kein Zeitzonenfehler eingeschleppt:
      `grep -n "toISOString" server/src/services/overtimeLiveCalculationService.ts` gibt nichts aus.
    - `cd server && npx tsc --noEmit` läuft ohne Fehler.
  </acceptance_criteria>
  <verify>
    <automated>cd server && npx vitest run src/services/overtimeLiveCalculationService.test.ts && npx tsc --noEmit && ! grep -q "dayOfWeek >= 1 && dayOfWeek <= 5" src/services/overtimeLiveCalculationService.ts && grep -q "export function getAllWorkingDaysBetween" src/services/overtimeLiveCalculationService.ts && ! grep -q "toISOString" src/services/overtimeLiveCalculationService.ts && echo OK</automated>
  </verify>
  <done>`getAllWorkingDaysBetween` entscheidet ausschließlich über `getDailyTargetHours()`, ist exportiert und durch sechs Testfälle abgesichert; der Samstag eines `workSchedule`-Nutzers geht nicht mehr verloren.</done>
</task>

<task type="auto">
  <name>Task 2: Wochenend- und Feiertags-Vorfilter im Legacy-Pfad beseitigen</name>
  <files>server/src/services/overtimeService.ts</files>
  <read_first>
    - server/src/services/overtimeService.ts (Zeilen 40-215, 229-400, 1240-1400)
    - server/src/utils/workingDays.ts (Zeilen 63-94)
    - server/src/services/unifiedOvertimeService.ts (Zeilen 100-150)
    - .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md (Abschnitt „Abweichungen (REQ-17)")
    - .claude/CLAUDE.md (Abschnitt „Überstunden-Berechnung", insbesondere „Feiertag ÜBERSCHREIBT workSchedule")
  </read_first>
  <action>
    Beseitige an allen fünf Stellen in `server/src/services/overtimeService.ts` den
    vorgeschalteten Wochenend- und Feiertagsfilter. Es sind:
    Zeilen 99-106 (`_calculateAbsenceCreditsForMonth`),
    Zeilen 188-195 (`_calculateUnpaidLeaveForMonth`),
    Zeilen 309-317 (`ensureAbsenceTransactionsForMonth`, PHASE 1),
    Zeilen 350-357 (`ensureAbsenceTransactionsForMonth`, PHASE 2),
    Zeilen 1312-1320 (`ensureAbsenceTransactions`).
    Die Zeilennummern stammen aus dem Stand bei Planerstellung — arbeite nach dem Muster, nicht
    blind nach der Zahl, und prüfe mit
    `grep -n "isWeekend" server/src/services/overtimeService.ts`, dass genau diese fünf
    Vorkommen erfasst sind. Findest du mehr oder weniger, halte die Abweichung fest und
    behandle jedes Vorkommen einzeln.

    Ersetze an jeder Stelle das Dreiergespann `dayOfWeek`/`isWeekend`/`if (isWeekend) continue`
    zusammen mit der nachfolgenden eigenen `holidays`-Abfrage durch die eine Bedingung, die
    ohnehin schon folgt: `getDailyTargetHours(user, dateStr)` ermitteln, und bei `=== 0`
    überspringen. Wo dieser Aufruf bereits existiert (Zeilen 320, 360, 1323), bleibt er stehen
    und übernimmt allein die Entscheidung. Wo bisher nur summiert wurde (Zeilen 110, 198),
    ergibt sich die Null-Addition von selbst — dort darf der Vorfilter ersatzlos entfallen.

    Fachliche Wirkung, die dadurch entsteht und beabsichtigt ist: Ein Nutzer mit
    `workSchedule.saturday > 0` erhält seine Samstage nun als Abwesenheitstage gutgeschrieben
    beziehungsweise als Sollstunden gezählt. Vorher fielen sie durch `if (isWeekend) continue`
    heraus, obwohl `getDailyTargetHours()` für diesen Tag Stunden liefert. Das ist die
    Auflösung der Abweichung aus `09-INVENTAR-SOLLSTUNDEN.md`.

    Setze über jede geänderte Schleife einen deutschen Kommentar: REQ-17, warum der Vorfilter
    weg ist, und dass `getDailyTargetHours()` Feiertag, Wochenende und `workSchedule` in dieser
    Reihenfolge selbst behandelt (`server/src/utils/workingDays.ts:66-91`).

    `_calculateAbsenceCreditsForMonth` und `_calculateUnpaidLeaveForMonth` haben im Repository
    keinen Aufrufer — belege das mit
    `grep -rn "_calculateAbsenceCreditsForMonth\|_calculateUnpaidLeaveForMonth" --include=*.ts server/src`
    und halte das Ergebnis im Kommentar fest. Sie werden trotzdem angeglichen und NICHT
    gelöscht: D1 erlaubt Angleichen, nicht Rückbau, und ein späterer Aufrufer soll nicht auf
    die alte Logik treffen.

    Nicht Teil dieser Aufgabe: Exporte entfernen, Aufrufer umhängen, `overtime_balance`
    abschalten, Routen ändern. `updateMonthlyOvertime` (Zeile 421) bleibt unverändert — die
    Delegation an `unifiedOvertimeService.calculateMonthlyOvertime` (Zeile 454) ist bereits der
    gewünschte Zustand und wird in Task 3 nur belegt, nicht angefasst.
  </action>
  <acceptance_criteria>
    - Kein `isWeekend` mehr in der Datei:
      `grep -n "isWeekend" server/src/services/overtimeService.ts` gibt nichts aus.
    - Kein eigener Wochentags-Vergleich mehr:
      `grep -n "dayOfWeek === 0 || dayOfWeek === 6" server/src/services/overtimeService.ts` gibt nichts aus.
    - Die Anzahl eigener Feiertagsabfragen ist gesunken: `grep -c "FROM holidays" server/src/services/overtimeService.ts`
      liefert einen kleineren Wert als vor der Änderung (Ausgangswert vor dem Eingriff messen
      und in der SUMMARY nennen).
    - `getDailyTargetHours` wird weiterhin an mindestens fünf Stellen aufgerufen:
      `grep -v '^\s*[/*]' server/src/services/overtimeService.ts | grep -c "getDailyTargetHours(user" ` ist ≥ 5.
    - REQ-17 ist in den Kommentaren belegt:
      `grep -c "REQ-17" server/src/services/overtimeService.ts` ist ≥ 3.
    - `updateMonthlyOvertime` delegiert unverändert:
      `grep -q "unifiedOvertimeService.calculateMonthlyOvertime" server/src/services/overtimeService.ts`.
    - Kein Export wurde entfernt (D1): `grep -c "^export \(async \)\?function" server/src/services/overtimeService.ts`
      liefert denselben Wert wie vor der Änderung (16 zum Zeitpunkt der Planung).
    - `cd server && npx tsc --noEmit` läuft ohne Fehler.
  </acceptance_criteria>
  <verify>
    <automated>cd server && npx tsc --noEmit && ! grep -q "isWeekend" src/services/overtimeService.ts && grep -q "unifiedOvertimeService.calculateMonthlyOvertime" src/services/overtimeService.ts && test $(grep -c "^export \(async \)\?function" src/services/overtimeService.ts) -eq 16 && test $(grep -c "REQ-17" src/services/overtimeService.ts) -ge 3 && echo OK</automated>
  </verify>
  <done>Alle fünf Vorfilter sind entfernt, die Arbeitstags-Entscheidung liegt allein bei `getDailyTargetHours()`, und der Legacy-Pfad hat unverändert 16 Exporte — angeglichen, nicht stillgelegt.</done>
</task>

<task type="auto">
  <name>Task 3: fix-overtime.ts auf den kanonischen Weg ziehen (Abweichung A-1)</name>
  <files>server/scripts/fix-overtime.ts, .planning/phases/09-ein-ma-stab-ein-weg/09-A1-VORHER.json, .planning/phases/09-ein-ma-stab-ein-weg/09-A1-NACHWEIS.md</files>
  <read_first>
    - server/scripts/fix-overtime.ts (vollständig, 188 Zeilen)
    - server/src/services/overtimeService.ts (Zeilen 684-782 — die kanonische `ensureOvertimeBalanceEntries`)
    - server/src/services/unifiedOvertimeService.ts (Zeilen 100-150)
    - server/src/config/database.ts (vollständig — Pfadauflösung über `DATABASE_PATH`)
    - server/src/database/connection.ts (Zeilen 1-60 und 115-123 — Export als Proxy)
    - server/src/scripts/compareOvertimePaths.ts (Zeilen 55-100 und 191-270 — Guard und Messreihenfolge)
    - .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md (Abschnitt „A-1")
    - .planning/phases/09-ein-ma-stab-ein-weg/09-PRUEFNUTZER.csv
    - .github/workflows/deploy-server.yml (Zeilen 110-132 — **nur lesen, nicht ändern**)
    - .planning/notes/db-pfad-diskrepanz-20260821.md
  </read_first>
  <action>
    **Schritt 1 — Vorher-Messung, bevor eine einzige Zeile geändert wird.**
    Ohne diesen Schritt gibt es später keinen Vergleichspunkt, weil die alte Fassung des Skripts
    nach der Änderung nicht mehr vorliegt.

    Baue zuerst `dist/`: `cd server && npm run build`. Das Skript importiert aus `../dist/`,
    und `server/dist/` existiert im Arbeitsverzeichnis derzeit nicht.

    Lege eine Arbeitskopie an: `cp server/database/development.db server/database/a1-vorher.db`,
    zusammen mit den Dateien `development.db-wal` und `development.db-shm`, falls vorhanden
    (Zielnamen entsprechend `a1-vorher.db-wal`/`a1-vorher.db-shm`). `development.db` selbst wird
    weder beschrieben noch verschoben — der lokale Dev-Server kann sie offen halten.

    Führe das **unveränderte** Skript gegen die Kopie aus:
    `cd server && DATABASE_PATH=./database/a1-vorher.db npx tsx scripts/fix-overtime.ts`.
    `NODE_ENV` bleibt dabei auf `development` — kein Produktionslauf, D5 gilt.

    Lies unmittelbar danach, ohne vorher irgendeinen Dienst anzusprechen, die geschriebenen Werte
    für die drei Prüfnutzer aus `09-PRUEFNUTZER.csv` (userId 2 / 2026-07, userId 16 / 2026-07,
    userId 17 / 2026-04) direkt aus der Kopie: `targetHours` und `actualHours` aus
    `overtime_balance`. Das sind die Skriptwerte.

    Lass anschließend das Werkzeug aus Welle 2 gegen dieselbe Kopie laufen:
    `cd server && DATABASE_PATH=./database/a1-vorher.db npm run validate:overtime:paths -- --from=../.planning/phases/09-ein-ma-stab-ein-weg/09-PRUEFNUTZER.csv --json=../.planning/phases/09-ein-ma-stab-ein-weg/09-A1-VORHER.json`
    und lies dieselben drei Zeilen ein zweites Mal aus. Weil `getOvertimeSummary`
    (`overtimeService.ts:618`) im Werkzeuglauf `ensureOvertimeBalanceEntries` auslöst, stehen
    danach die kanonischen Werte in der Kopie. Die Differenz zwischen erster und zweiter Lesung
    **ist** die Abweichung A-1, gemessen statt behauptet.

    Erwartung, die vor dem Lauf schriftlich festzuhalten ist: Für userId 16 (Benedikt Jochem,
    kein `workSchedule`, `weeklyHours = 30`) darf sich `targetHours` nicht ändern — für ihn
    stimmt `weeklyHours / 5` mit `getDailyTargetHours()` überein. Für userId 2 (Karin Jochem,
    `workSchedule` mit vier 0-Stunden-Werktagen) und userId 17 (Carmen Rothemund,
    `workSchedule` mit Mittwoch und Freitag auf 0) ist eine Änderung der erwartete Beleg, dass
    der Bypass real ist. Ändert sich der Wert bei userId 16 doch, ist das ein **Blocker**, kein
    Hinweis: Halte die Arbeit an, ermittle die Ursache und dokumentiere sie, bevor du das Skript
    änderst.

    Lege die Zahlen sofort in
    `.planning/phases/09-ein-ma-stab-ein-weg/09-A1-NACHWEIS.md` unter der Überschrift
    „Vorher (Skriptwert) gegen kanonischen Wert" ab: je Prüfnutzer `userId`, Monat,
    `targetHours` Skript, `targetHours` kanonisch, Delta.

    **Schritt 2 — Angleichen.**
    Ersetze in `server/scripts/fix-overtime.ts` die lokale Kopie der Funktion
    `ensureOvertimeBalanceEntries` (Zeile 28-133) vollständig durch einen Import der bereits
    exportierten Funktion gleichen Namens aus `../dist/services/overtimeService.js`. Zeile 27
    weist die Kopie im Code selbst als Dublette aus („Same logic as in overtimeService.ts") —
    dieser Anspruch wird hier eingelöst. Das Schreibverhalten bleibt identisch: derselbe UPSERT
    auf dieselben zwei Spalten von `overtime_balance` (`overtimeService.ts:767`).

    Ersetze außerdem die eigene Verbindung `new Database(dbPath)` (Zeile 16) durch die geteilte
    Verbindung aus `../dist/database/connection.js`. Behalte die Startausgabe des aufgelösten
    Datenbankpfades (Zeile 18) bei — sie ist der Schutz gegen den Fehlgriff auf die tote
    `server/database.db`. `databaseConfig` (Zeile 14) bleibt dafür importiert.

    Entferne die dadurch unbenutzten Importe: `Database` aus `better-sqlite3`,
    `countWorkingDaysBetween`, `getTodayString` und `parseDate`. `getCurrentMonth` bleibt
    (Zeile 139). Prüfe `__filename`/`__dirname` (Zeile 10-11) mit `grep` und entferne sie samt
    `fileURLToPath`/`dirname`, falls sie unbenutzt sind.

    Die kanonische Funktion ist `async` (`overtimeService.ts:684`). Der Skriptrumpf ab Zeile 137
    muss das aufnehmen: entweder in eine `async function main()` mit abschließendem Aufruf oder
    über Top-Level-`await` — die Datei ist ESM (`import.meta.url`) und läuft über `tsx`, beides
    ist zulässig. Ein unbehandeltes Promise darf nicht entstehen: ein Fehlschlag muss den Prozess
    mit einem Exit-Code ungleich 0 beenden.

    `db.close()` (Zeile 188): Prüfe, ob der Proxy aus `connection.ts` den Aufruf durchreicht.
    Reicht er ihn nicht durch, entferne die Zeile — der Prozess endet ohnehin.

    Setze über den Import einen deutschen Kommentar, der REQ-17 und die Abweichung A-1 nennt,
    `server/src/utils/workingDays.ts:63` als kanonische Stelle und
    `server/src/services/overtimeService.ts:684` als jetzt genutzte Funktion angibt, und
    festhält, dass das Skript weiterhin bei jedem Deployment und täglich um 3 Uhr läuft
    (`.github/workflows/deploy-server.yml:118` und `:125`).

    Nicht Teil dieser Aufgabe: `.github/workflows/deploy-server.yml` ändern, den Cron abschalten,
    das Skript löschen, gegen Produktion laufen lassen. D1 verlangt Angleichen; die
    Auslieferungslogik gehört nach Phase 14 und wird in Task 4 nur dokumentiert.
  </action>
  <acceptance_criteria>
    - Der Bypass ist verschwunden:
      `grep -v '^[[:space:]]*[/*]' server/scripts/fix-overtime.ts | grep -c "weeklyHours / 5"` liefert 0.
    - Der abgeleitete Tageswert ist verschwunden:
      `grep -v '^[[:space:]]*[/*]' server/scripts/fix-overtime.ts | grep -c "targetHoursPerDay"` liefert 0.
    - Die kanonische Funktion wird importiert:
      `grep -q "from '../dist/services/overtimeService.js'" server/scripts/fix-overtime.ts` und
      `grep -q "ensureOvertimeBalanceEntries" server/scripts/fix-overtime.ts`.
    - Keine eigene Datenbankverbindung mehr:
      `grep -v '^[[:space:]]*[/*]' server/scripts/fix-overtime.ts | grep -c "new Database("` liefert 0.
    - Kein eigener Arbeitstage-Zähler mehr:
      `grep -v '^[[:space:]]*[/*]' server/scripts/fix-overtime.ts | grep -c "countWorkingDaysBetween"` liefert 0.
    - Kein Zeitzonenfehler eingeschleppt:
      `grep -v '^[[:space:]]*[/*]' server/scripts/fix-overtime.ts | grep -c "toISOString"` liefert 0.
    - REQ-17 und A-1 sind im Kommentar belegt:
      `grep -c "REQ-17" server/scripts/fix-overtime.ts` ist ≥ 1 und
      `grep -c "A-1" server/scripts/fix-overtime.ts` ist ≥ 1.
    - Der Deploy-Workflow ist unangetastet:
      `git diff --name-only` enthält `.github/workflows/deploy-server.yml` nicht.
    - Die Vorher-Messung liegt vor:
      `.planning/phases/09-ein-ma-stab-ein-weg/09-A1-VORHER.json` existiert, und
      `09-A1-NACHWEIS.md` enthält den Abschnitt „Vorher (Skriptwert) gegen kanonischen Wert"
      mit allen drei `userId` (2, 16, 17) und je einem Delta-Wert.
    - Das Delta für userId 16 ist 0,00 — steht ein anderer Wert dort, ist der Plan blockiert und
      die Ursache muss im selben Abschnitt dokumentiert sein.
    - `cd server && npx tsc --noEmit` läuft ohne Fehler. Dieser Check deckt
      `server/scripts/fix-overtime.ts` laut `tsconfig.json` (`"include": ["src/**/*"]`) **nicht**
      ab; er belegt nur, dass `src/` unberührt blieb.
  </acceptance_criteria>
  <verify>
    <automated>cd server && npm run build >/dev/null 2>&1 && ! grep -v '^[[:space:]]*[/*]' scripts/fix-overtime.ts | grep -q "weeklyHours / 5" && ! grep -v '^[[:space:]]*[/*]' scripts/fix-overtime.ts | grep -q "new Database(" && ! grep -v '^[[:space:]]*[/*]' scripts/fix-overtime.ts | grep -q "countWorkingDaysBetween" && grep -q "ensureOvertimeBalanceEntries" scripts/fix-overtime.ts && grep -q "REQ-17" scripts/fix-overtime.ts && test -f ../.planning/phases/09-ein-ma-stab-ein-weg/09-A1-VORHER.json && npx tsc --noEmit && echo OK</automated>
  </verify>
  <done>`fix-overtime.ts` schreibt `overtime_balance` über `ensureOvertimeBalanceEntries()` und damit über `getDailyTargetHours()`; die Vorher-Werte der drei Prüfnutzer sind gemessen und abgelegt, bevor die Änderung erfolgte.</done>
</task>

<task type="auto">
  <name>Task 4: A-1 belegen, Auslieferungslogik dokumentieren, STATE.md richtigstellen</name>
  <files>.planning/phases/09-ein-ma-stab-ein-weg/09-A1-NACHWEIS.md, .planning/phases/09-ein-ma-stab-ein-weg/09-A1-NACHHER.json, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/09-ein-ma-stab-ein-weg/09-A1-NACHWEIS.md (der in Task 3 geschriebene Vorher-Abschnitt)
    - .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md (Abschnitt „A-1" und die Skript-Tabelle)
    - .planning/phases/09-ein-ma-stab-ein-weg/09-PRUEFNUTZER.md (die drei Nutzer und ihre Prüfmonate)
    - .github/workflows/deploy-server.yml (Zeilen 110-132)
    - .planning/STATE.md (Abschnitt „Zurückgestellte Punkte", Zeilen 192-210)
    - .planning/debug/db-stabilisierung-20260818.md
    - .claude/CLAUDE.md (Abschnitt „Database Rules" — die `DATABASE_PATH`-Pflicht und der WAL-Vorfall)
  </read_first>
  <action>
    **Schritt 1 — Nachher-Messung, gleiche Reihenfolge wie in Task 3.**
    Lege eine frische Kopie an: `cp server/database/development.db server/database/a1-nachher.db`
    (samt `-wal`/`-shm`, falls vorhanden). Nutze **nicht** `a1-vorher.db` weiter — die enthält
    bereits kanonisch überschriebene Zeilen und wäre kein sauberer Ausgangspunkt.

    Führe das geänderte Skript aus:
    `cd server && DATABASE_PATH=./database/a1-nachher.db npx tsx scripts/fix-overtime.ts`,
    `NODE_ENV` bleibt `development` (D5). Lies unmittelbar danach `targetHours` und
    `actualHours` für die drei Prüfnutzer direkt aus der Kopie.

    Lass danach das Werkzeug gegen dieselbe Kopie laufen:
    `cd server && DATABASE_PATH=./database/a1-nachher.db npm run validate:overtime:paths -- --from=../.planning/phases/09-ein-ma-stab-ein-weg/09-PRUEFNUTZER.csv --json=../.planning/phases/09-ein-ma-stab-ein-weg/09-A1-NACHHER.json`,
    halte den Exit-Code fest und lies dieselben drei Zeilen erneut aus.

    Erwartung: Nach der Angleichung ist das Delta zwischen Skriptwert und kanonischem Wert für
    **alle drei** Prüfnutzer 0,00. Bleibt ein Delta bestehen, ist das kein Erfolg — benenne die
    verbleibende Stelle mit `datei:zeile` und ordne sie zu, statt sie zu glätten.

    **Schritt 2 — `09-A1-NACHWEIS.md` vervollständigen.**
    Ergänze das in Task 3 begonnene Dokument um:
    - Abschnitt „Nachher (Skriptwert) gegen kanonischen Wert": dieselbe Tabelle wie im
      Vorher-Abschnitt, je Prüfnutzer mit `targetHours` Skript, `targetHours` kanonisch, Delta.
    - Abschnitt „Wirkung je Prüfnutzertyp": ausdrücklich benannt, dass sich für den Nutzer **ohne**
      `workSchedule` (userId 16) nichts geändert hat und dass die Änderung bei den Nutzern **mit**
      `workSchedule` (userId 2 und 17) der Beleg dafür ist, dass der Bypass real war. Trage die
      gemessenen Zahlen ein, keine Schätzungen.
    - Abschnitt „Warum der Exit-Code allein nichts beweist": `getOvertimeSummary`
      (`overtimeService.ts:618`) ruft `ensureOvertimeBalanceEntries` auf, bevor
      `compareOvertimePaths.ts:265` `balance_row` liest (Erhebung des `dashboard`-Wegs in
      `compareOvertimePaths.ts:239`). Das Werkzeug repariert die Zeile also im selben Lauf. Nenne
      beide Exit-Codes trotzdem — sie sind der Regressionswächter dieser Änderung.
    - Abschnitt „Auslieferungslogik (Übergabe an Phase 14)". Er hält fest, dass
      `.github/workflows/deploy-server.yml:118` das Skript bei **jedem** Deployment gegen die
      Produktionsdatenbank ausführt und Zeile 119-120 einen Fehlschlag mit einer Warnung abfängt,
      und dass `deploy-server.yml:123-130` bei **jedem** Deployment den täglichen Cronjob um
      3 Uhr neu installiert (`CRON_COMMAND` in Zeile 125, `crontab -` in Zeile 127). Zitiere
      beide Zeilen wörtlich. Halte ausdrücklich fest, dass Phase 9 den Workflow **nicht** ändert,
      und notiere die offene Frage für Phase 14: Ob der tägliche Cron überhaupt noch gebraucht
      wird, nachdem `ensureOvertimeBalanceEntries()` ohnehin bei jedem Dashboard- und
      Berichtsaufruf läuft (`overtimeService.ts:618`, `routes/overtime.ts:806` und `:1031`).
    - Abschnitt „Korrektur an STATE.md": der bisherige Wortlaut, der neue Wortlaut und die
      Begründung mit Zeilenbeleg.

    **Schritt 3 — `.planning/STATE.md` richtigstellen.**
    In der Tabelle unter „Zurückgestellte Punkte" steht in der Zeile `db-stabilisierung-20260818`
    als Weiterbehandlung „Restpunkt: Cron mit `DATABASE_PATH` reaktivieren". Diese Annahme ist
    falsch: Der Cron ist nicht deaktiviert. `deploy-server.yml:123-130` installiert ihn bei jedem
    Deployment neu, und `CRON_COMMAND` in Zeile 125 enthält
    `DATABASE_PATH=/home/ubuntu/databases/production.db` bereits gesetzt — das WAL-Problem vom
    18.08. ist damit entschärft. Ersetze die Zelle durch die zutreffende Aussage mit
    Zeilenbeleg und verweise auf `09-A1-NACHWEIS.md`.

    Ändere in `STATE.md` **ausschließlich** diese eine Tabellenzelle. Es können weitere Agenten
    parallel in diesem Arbeitsverzeichnis arbeiten: Fremde, nicht committete Änderungen bleiben
    unangetastet, und es wird weder `git checkout --`, `git restore`, `git stash`, `git reset`
    noch `git clean` ausgeführt. Committe die Dateien einzeln und namentlich.

    **Schritt 4 — Aufräumen.**
    Lösche `server/database/a1-vorher.db` und `server/database/a1-nachher.db` samt zugehörigen
    `-wal`/`-shm`-Dateien. `server/database/development.db` bleibt unberührt.
  </action>
  <acceptance_criteria>
    - `.planning/phases/09-ein-ma-stab-ein-weg/09-A1-NACHHER.json` existiert.
    - `09-A1-NACHWEIS.md` hat mindestens 50 Zeilen und enthält die Überschriften
      „Vorher (Skriptwert) gegen kanonischen Wert", „Nachher (Skriptwert) gegen kanonischen Wert",
      „Wirkung je Prüfnutzertyp", „Warum der Exit-Code allein nichts beweist",
      „Auslieferungslogik (Übergabe an Phase 14)" und „Korrektur an STATE.md".
    - Alle drei Prüfnutzer-IDs erscheinen:
      `for u in $(tail -n +2 .planning/phases/09-ein-ma-stab-ein-weg/09-PRUEFNUTZER.csv | cut -d, -f1); do grep -q "$u" .planning/phases/09-ein-ma-stab-ein-weg/09-A1-NACHWEIS.md || echo "FEHLT: $u"; done` gibt nichts aus.
    - Die Auslieferungsbelege sind wörtlich zitiert:
      `for l in "deploy-server.yml:118" "deploy-server.yml:123-130" "deploy-server.yml:125" "overtimeService.ts:618" "overtimeService.ts:684" "compareOvertimePaths.ts:265"; do grep -q "$l" .planning/phases/09-ein-ma-stab-ein-weg/09-A1-NACHWEIS.md || echo "FEHLT: $l"; done` gibt nichts aus.
    - Nach der Angleichung ist kein Delta mehr offen: Der Abschnitt „Nachher" weist für alle drei
      Prüfnutzer ein Delta von 0,00 aus, oder jede Ausnahme ist mit `datei:zeile` benannt und
      einem Nachfolgeplan zugeordnet.
    - `.planning/STATE.md` behauptet nicht länger, der Cron sei zu reaktivieren:
      `grep -c "Restpunkt: Cron mit" .planning/STATE.md` liefert 0,
      und `grep -q "deploy-server.yml" .planning/STATE.md` trifft zu.
    - An `STATE.md` wurde genau eine Zeile geändert:
      `git diff --numstat .planning/STATE.md` weist 1 hinzugefügte und 1 entfernte Zeile aus.
    - `.github/workflows/deploy-server.yml` ist unverändert:
      `git diff --name-only` enthält die Datei nicht.
    - Keine Arbeitskopie bleibt liegen: `ls server/database/a1-*.db` gibt nichts aus.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/09-ein-ma-stab-ein-weg/09-A1-NACHHER.json && test $(wc -l < .planning/phases/09-ein-ma-stab-ein-weg/09-A1-NACHWEIS.md) -ge 50 && for l in "deploy-server.yml:118" "deploy-server.yml:123-130" "deploy-server.yml:125" "overtimeService.ts:618" "overtimeService.ts:684" "compareOvertimePaths.ts:265"; do grep -q "$l" .planning/phases/09-ein-ma-stab-ein-weg/09-A1-NACHWEIS.md || exit 1; done && grep -q "deploy-server.yml" .planning/STATE.md && ! git diff --name-only | grep -q "deploy-server.yml" && ! ls server/database/a1-*.db >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <done>Die Abweichung A-1 ist mit gemessenen Vorher/Nachher-Werten belegt, die Auslieferungslogik von `fix-overtime.ts` ist für Phase 14 schriftlich übergeben, und `STATE.md` beschreibt den Cron-Zustand zutreffend.</done>
</task>

<task type="auto">
  <name>Task 5: Nachweis führen und Inventar abschließen</name>
  <files>.planning/phases/09-ein-ma-stab-ein-weg/09-ANGLEICHUNG-NACHWEIS.md, .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md</files>
  <read_first>
    - .planning/phases/09-ein-ma-stab-ein-weg/09-VERGLEICH-BEFUND.md (der Vorher-Stand)
    - .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md
    - .planning/phases/09-ein-ma-stab-ein-weg/09-PRUEFNUTZER.csv
    - server/src/services/overtimeService.ts (Zeilen 421-500 — die Belegkette für REQ-18)
    - server/src/services/unifiedOvertimeService.ts (Zeilen 100-150)
    - server/src/scripts/compareOvertimePaths.ts
  </read_first>
  <action>
    Führe den Nachweislauf gegen dieselben drei Prüfnutzer aus, mit denen Plan 09-02 den
    Vorher-Stand erhoben hat:
    `cd server && npm run validate:overtime:paths -- --from=../.planning/phases/09-ein-ma-stab-ein-weg/09-PRUEFNUTZER.csv --json=../.planning/phases/09-ein-ma-stab-ein-weg/09-ANGLEICHUNG-BASELINE.json`
    und je Nutzer
    `npm run validate:overtime:detailed -- --userId=<ID> --month=<YYYY-MM>`.
    Führe außerdem `cd server && npx vitest run` und `cd server && npx tsc --noEmit` aus.

    Schreibe `.planning/phases/09-ein-ma-stab-ein-weg/09-ANGLEICHUNG-NACHWEIS.md` mit:
    - Abschnitt „Vorher/Nachher": je Prüfnutzer eine Tabelle mit den fünf Wegnamen
      (`unified`, `dashboard`, `report_summary`, `report_daily`, `balance_row`), Spalte
      „vorher" aus `09-VERGLEICH-BEFUND.md`, Spalte „nachher" aus diesem Lauf, Spalte „Delta"
    - Abschnitt „Exit-Codes": Exit-Code des Erstlaufs aus 09-02 und Exit-Code dieses Laufs
    - Abschnitt „Belegkette REQ-18 (D1)": die Kette
      `GET /api/overtime/:userId` (`server/src/routes/overtime.ts:642`) →
      `getOvertimeSummary` (`overtimeService.ts:595`) → `overtime_balance` ←
      `updateMonthlyOvertime` (`overtimeService.ts:421`) →
      `unifiedOvertimeService.calculateMonthlyOvertime` (`overtimeService.ts:454`) →
      `getDailyTargetHours` (`unifiedOvertimeService.ts:115`) → `workingDays.ts:63`.
      Jedes Glied mit wörtlichem Codezitat. Halte ausdrücklich fest, dass `overtimeService.ts`
      weiterhin existiert, weiterhin `overtime_balance` schreibt und weiterhin 16 Exporte hat —
      D1 verlangt Angleichen statt Stilllegen.
    - Abschnitt „Regressionsnetz": Testzahl vor der Änderung (aus Task 1) gegen Testzahl nach
      der Änderung. Ein Rückgang ist ein Fehlschlag und muss behoben werden, bevor der Plan als
      erledigt gilt.
    - Abschnitt „Restabweichungen": Bleibt der Exit-Code bei 1, benenne jede verbleibende
      Paarung, ihre Ursache und ob sie in REQ-19 (Plan 09-04) fällt. Erfinde keine Erklärung —
      wo die Ursache offen ist, steht „Ursache offen".

    Aktualisiere anschließend `09-INVENTAR-SOLLSTUNDEN.md`: Ergänze je Abweichung `A-n` eine
    Spalte „Status" mit `beseitigt in 09-03 Task <n>` oder `offen — Begründung`. Ersetze den
    Abschnitt „Urteil REQ-17" durch das Urteil nach der Angleichung und lasse das
    ursprüngliche Urteil als „Urteil REQ-17 (Stand 09-01)" stehen, damit die Entwicklung
    ablesbar bleibt.
  </action>
  <acceptance_criteria>
    - `.planning/phases/09-ein-ma-stab-ein-weg/09-ANGLEICHUNG-NACHWEIS.md` existiert mit
      mindestens 40 Zeilen und enthält die Abschnitte „Vorher/Nachher", „Exit-Codes",
      „Belegkette REQ-18 (D1)", „Regressionsnetz" und „Restabweichungen".
    - Die Grundlinie nach der Angleichung wurde geschrieben:
      `test -f .planning/phases/09-ein-ma-stab-ein-weg/09-ANGLEICHUNG-BASELINE.json`.
    - Alle drei Prüfnutzer-IDs erscheinen im Nachweis:
      `for u in $(tail -n +2 .planning/phases/09-ein-ma-stab-ein-weg/09-PRUEFNUTZER.csv | cut -d, -f1); do grep -q "$u" .planning/phases/09-ein-ma-stab-ein-weg/09-ANGLEICHUNG-NACHWEIS.md || echo "FEHLT: $u"; done` gibt nichts aus.
    - Die Belegkette nennt alle Glieder:
      `for l in "routes/overtime.ts:642" "overtimeService.ts:595" "overtimeService.ts:421" "overtimeService.ts:454" "unifiedOvertimeService.ts:115" "workingDays.ts:63"; do grep -q "$l" .planning/phases/09-ein-ma-stab-ein-weg/09-ANGLEICHUNG-NACHWEIS.md || echo "FEHLT: $l"; done` gibt nichts aus.
    - `09-INVENTAR-SOLLSTUNDEN.md` enthält beide Urteile:
      `grep -q "Urteil REQ-17 (Stand 09-01)" .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md`.
    - `cd server && npx vitest run` besteht mindestens so viele Tests wie der in Task 1
      gemessene Ausgangswert; die beiden Zahlen stehen wörtlich im Abschnitt „Regressionsnetz".
    - `cd server && npx tsc --noEmit` läuft ohne Fehler.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/09-ein-ma-stab-ein-weg/09-ANGLEICHUNG-BASELINE.json && grep -q "Belegkette REQ-18" .planning/phases/09-ein-ma-stab-ein-weg/09-ANGLEICHUNG-NACHWEIS.md && grep -q "Regressionsnetz" .planning/phases/09-ein-ma-stab-ein-weg/09-ANGLEICHUNG-NACHWEIS.md && grep -q "workingDays.ts:63" .planning/phases/09-ein-ma-stab-ein-weg/09-ANGLEICHUNG-NACHWEIS.md && grep -q "Urteil REQ-17 (Stand 09-01)" .planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md && cd server && npx tsc --noEmit && echo OK</automated>
  </verify>
  <done>Vorher/Nachher-Werte, Exit-Codes, Belegkette für REQ-18 und Testzahlen sind dokumentiert; jede Abweichung `A-n` aus dem Inventar hat einen Status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| Client → `GET /api/overtime/transactions/live` | `server/src/routes/overtime.ts:467-556` reicht `userId` und Zeitraum an den geänderten Live-Service weiter. Der Endpunkt ist authentifiziert (`requireAuth`). |
| Service → SQLite | Die geänderten Schleifen erzeugen `overtime_transactions`-Zeilen. Fehlerhafte Tagesmengen erzeugen falsche Buchungen im Kontoauszug. |
| GitHub Actions Runner → Produktionsdatenbank | `.github/workflows/deploy-server.yml:118` führt `scripts/fix-overtime.ts` bei jedem Deployment gegen `/home/ubuntu/databases/production.db` aus; `deploy-server.yml:123-130` installiert dazu einen täglichen Cronjob um 3 Uhr. Ein Schreibzugriff auf Produktion ohne menschliche Freigabe, ausgelöst durch `git push origin main`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-T | Tampering | `overtime_transactions` — mehr Tage als vorher werden als Arbeitstage gezählt | mitigate | Die Verhaltensänderung ist beabsichtigt und begrenzt auf Nutzer mit `workSchedule` an Samstagen/Sonntagen. Sie wird durch den Vorher/Nachher-Vergleich in Task 3 mit Zahlen belegt, nicht behauptet. Die Serversuite muss ihre Ausgangszahl halten (NO REGRESSION). |
| T-09-03-E | Elevation of Privilege | Berechtigungsprüfung der betroffenen Endpunkte | accept | Dieser Plan ändert keine Route, keine Middleware und keinen Export. `requireAuth` und die Eigentümerprüfung in `routes/overtime.ts` bleiben unverändert; die Angriffsfläche wächst nicht. |
| T-09-03-D | Denial of Service | Datenbanklast pro Tag | mitigate | Die Änderung entfernt je Tag eine zusätzliche `SELECT ... FROM holidays`-Abfrage, weil `getDailyTargetHours()` sie ohnehin ausführt. Die Last sinkt, sie steigt nicht. |
| T-09-03-SC | Tampering | npm/pip/cargo-Installationen | mitigate | Dieser Plan installiert kein Paket. Alle verwendeten Werkzeuge (`tsx`, `vitest`, `better-sqlite3`) stehen bereits in `server/package.json`. Bei Installationsbedarf ist der Plan zu stoppen und der Bedarf zu melden. |
| T-09-03-A1-T | Tampering | `overtime_balance` über `server/scripts/fix-overtime.ts`, ausgeführt bei jedem Deployment (`deploy-server.yml:118`) und täglich um 3 Uhr (`deploy-server.yml:125`) | mitigate | Höchster Einzelposten dieses Plans: Das Skript überschreibt per UPSERT (Zeile 125-130) jede zuvor korrekt berechnete `targetHours`-Spalte mit `weeklyHours / 5` (Zeile 78) — ohne Angleichung wäre der gesamte Milestone binnen 24 Stunden still wirkungslos. Task 3 ersetzt die Rechnung durch `ensureOvertimeBalanceEntries()` (`overtimeService.ts:684`), die denselben UPSERT (`overtimeService.ts:767`) auf dieselben zwei Spalten ausführt — identisches Schreibverhalten, kanonische Rechnung. Task 4 belegt die Wirkung mit gemessenen Vorher/Nachher-Werten gegen eine lokale Kopie. |
| T-09-03-A1-D | Denial of Service | Versehentlicher Lauf gegen die Produktionsdatenbank aus der Entwicklungsumgebung | mitigate | Alle Läufe in Task 3 und 4 setzen `DATABASE_PATH` auf eine Kopie unterhalb `server/database/` und lassen `NODE_ENV` auf `development` (D5). `server/database/development.db` wird nur kopiert, nie beschrieben. `compareOvertimePaths.ts:59-78` bricht mit Exit 2 ab, sobald der aufgelöste Pfad `production` enthält oder `NODE_ENV=production` gesetzt ist. Der Deploy-Workflow wird nicht angefasst, also entsteht auch kein neuer Produktionslauf. |
| T-09-03-A1-R | Repudiation | Fehlschlag des Skripts im Deployment bleibt unbemerkt | accept | `deploy-server.yml:118-120` fängt einen Fehlschlag mit `|| { echo "⚠️ ... continuing deployment..." }` ab; der Cron protokolliert ausschließlich nach `/home/ubuntu/logs/overtime-fix.log`. Eine fehlerhafte Salden-Neuberechnung fällt damit nicht auf. Bewusst hingenommen: Eine Änderung an der Fehlerbehandlung des Workflows ist Auslieferungslogik und liegt außerhalb von Phase 9. Task 4 übergibt den Befund schriftlich an Phase 14. |
</threat_model>

<verification>
- `cd server && npx tsc --noEmit` ohne Fehler (nicht verhandelbare Randbedingung der ROADMAP).
- `cd server && npx vitest run` erreicht mindestens den in Task 1 gemessenen Ausgangswert.
- `grep -n "isWeekend" server/src/services/overtimeService.ts` gibt nichts aus.
- `grep -n "dayOfWeek >= 1 && dayOfWeek <= 5" server/src/services/overtimeLiveCalculationService.ts` gibt nichts aus.
- `grep -v '^[[:space:]]*[/*]' server/scripts/fix-overtime.ts | grep -c "weeklyHours / 5"` liefert 0.
- `.planning/phases/09-ein-ma-stab-ein-weg/09-A1-NACHWEIS.md` existiert und nennt
  `deploy-server.yml:118` sowie `deploy-server.yml:123-130`.
- `ls server/database/a1-*.db` gibt nichts aus — keine Arbeitskopie bleibt liegen.
- `git diff --stat` zeigt Änderungen ausschließlich in
  `server/src/services/overtimeLiveCalculationService.ts`,
  `server/src/services/overtimeLiveCalculationService.test.ts`,
  `server/src/services/overtimeService.ts`, `server/scripts/fix-overtime.ts` und unterhalb von
  `.planning/`. `.github/workflows/deploy-server.yml` erscheint **nicht**.
- Fremde, nicht committete Änderungen anderer Agenten bleiben unangetastet. Es wird weder
  `git checkout --`, `git restore`, `git stash`, `git reset` noch `git clean` ausgeführt;
  committet wird ausschließlich Datei für Datei, namentlich.
- Kein Deployment aus diesem Plan heraus. Der Push nach `main` erfolgt erst, wenn der
  Anwender ihn auslöst.
</verification>

<success_criteria>
- REQ-17: Kein Produktivpfad entscheidet mehr selbst über Arbeitstage; das Inventar weist für
  jede Abweichung `A-n` einen Status aus.
- REQ-18 / D1: Die Belegkette vom Dashboard-Endpunkt bis `getDailyTargetHours()` ist mit
  Zeilennummern und Zitaten dokumentiert; `overtimeService.ts` besteht unverändert mit
  16 Exporten weiter.
- Erfolgskriterium der ROADMAP: Für die drei Prüfnutzer stimmen Dashboard- und Berichtswert
  überein — Exit-Code 0 von `npm run validate:overtime:paths` — oder jede Restabweichung ist
  im Nachweis benannt und einem Nachfolgeplan zugeordnet.
- `npm run validate:overtime:detailed` läuft für diese Nutzer ohne Abweichungsmeldung.
- REQ-17 / A-1: `server/scripts/fix-overtime.ts` ermittelt keine Sollstunden mehr selbst. Die
  Messung weist für die beiden `workSchedule`-Prüfnutzer (userId 2, 17) eine Änderung aus — den
  Beleg, dass der Bypass real war — und für den `weeklyHours`-Prüfnutzer (userId 16) keine.
- Die Auslieferungslogik ist übergeben: `09-A1-NACHWEIS.md` hält fest, dass
  `deploy-server.yml:118` das Skript bei jedem Deployment ausführt und `:123-130` den täglichen
  Cron neu installiert. Der Workflow selbst bleibt in Phase 9 unverändert (Phase 14).
- `.planning/STATE.md` beschreibt den Cron-Zustand zutreffend: nicht deaktiviert, sondern bei
  jedem Deployment neu installiert, inzwischen mit gesetztem `DATABASE_PATH`.
</success_criteria>

<output>
Create `.planning/phases/09-ein-ma-stab-ein-weg/09-03-SUMMARY.md` when done.
Die SUMMARY nennt: Anzahl beseitigter Abweichungen, Exit-Code vorher und nachher, Testzahl
vorher und nachher, sowie jede Restabweichung mit Zuordnung.
Für A-1 zusätzlich: die gemessenen `targetHours`-Deltas je Prüfnutzer vor und nach der
Angleichung, und der ausdrückliche Hinweis, dass `.github/workflows/deploy-server.yml` in
Phase 9 nicht geändert wurde und als Aufgabe an Phase 14 übergeben ist.
</output>
