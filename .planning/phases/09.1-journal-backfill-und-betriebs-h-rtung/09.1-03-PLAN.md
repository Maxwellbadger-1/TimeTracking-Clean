---
phase: 09.1-journal-backfill-und-betriebs-h-rtung
plan: 03
type: execute
wave: 2
depends_on: ["09.1-01"]
files_modified:
  - server/src/scripts/migrate.ts
  - server/src/scripts/seed.ts
  - server/src/scripts/sync-db.ts
  - server/src/scripts/validateSchema.ts
  - server/scripts/migrate.ts
  - server/scripts/seed.ts
  - server/scripts/sync-db.ts
  - server/scripts/validateSchema.ts
  - server/scripts/analyze-dev-db.js
  - server/scripts/debug-december.js
  - server/scripts/test-current-month.js
  - server/scripts/test-getcurrentdate.js
  - server/scripts/test-month-generation.js
  - server/scripts/verify-db.js
  - server/scripts/verify-dev-db.js
  - server/scripts/verify-production-overtime.js
  - server/scripts/cleanup-invalid-entries.js
  - server/scripts/cleanup-invalid-time-entries.ts
  - server/scripts/fix-timezone-bugs.ts
  - server/scripts/check-db-sync.ts
  - server/scripts/create-admin.ts
  - server/package.json
autonomous: true
requirements: [WR-04]

must_haves:
  truths:
    - "D-09: Die vier ungetypten Bestandsskripte migrate.ts, seed.ts, sync-db.ts und validateSchema.ts liegen unter server/src/scripts/ und werden von tsconfig.json (include: src/**/*) ohne Sonderkonfiguration erfasst."
    - "D-09: npx tsc --noEmit erfasst die verschobenen Dateien und endet mit Exitcode 0."
    - "D-10: Die 13 unreferenzierten Legacy-Skripte sind entfernt; für jede Datei ist vor dem Löschen nachweislich geprüft, dass keine Referenz existiert."
    - "D-11: server/package.json ruft alle vier Skripte unter src/scripts/ auf; npm run migrate findet weiterhin genau die SQL-Migrationen aus server/database/migrations."
    - "D-16: Kein Schritt öffnet production.db; die Prüfläufe laufen gegen die lokale Entwicklungsdatenbank."
  artifacts:
    - path: "server/src/scripts/migrate.ts"
      provides: "Migrationswerkzeug unter tsc-Abdeckung, mit korrigiertem MIGRATIONS_DIR"
      contains: "../../database/migrations"
    - path: "server/src/scripts/seed.ts"
      provides: "Seed-Werkzeug unter tsc-Abdeckung, mit korrigiertem SEEDS_DIR"
      contains: "../../database/seeds"
    - path: "server/src/scripts/sync-db.ts"
      provides: "Sync-Werkzeug unter tsc-Abdeckung, mit korrigiertem Schlüsselpfad"
    - path: "server/src/scripts/validateSchema.ts"
      provides: "Schema-Validierung unter tsc-Abdeckung"
    - path: "server/package.json"
      provides: "Aufrufpfade auf src/scripts/ umgestellt"
      contains: "tsx src/scripts/migrate.ts"
  key_links:
    - from: "server/src/scripts/migrate.ts"
      to: "server/database/migrations"
      via: "path.join(__dirname, '../../database/migrations')"
      pattern: "\\.\\./\\.\\./database/migrations"
    - from: "server/package.json"
      to: "server/src/scripts/migrate.ts"
      via: "npm-Skript migrate:prod, das deploy-server.yml und deploy-staging.yml aufrufen"
      pattern: "tsx src/scripts/migrate\\.ts prod"
---

<objective>
WR-04 schließen. `server/scripts/**` liegt heute außerhalb der `include: ["src/**/*"]`-Regel
von `server/tsconfig.json` und ist damit ungetypt. Nach D-09 wird das Verzeichnis
**aufgelöst**, nicht per Sonderkonfiguration typisiert: die aktiv referenzierten Skripte
wandern nach `server/src/scripts/`, wo bereits 47 typisierte Skripte liegen.

Purpose: Eine eigene `tsconfig.scripts.json` brächte die Dateien zwar unter `tsc`, ließe aber
den Kern des Befunds ungelöst — die `../dist/`-Importe ohne `.d.ts`, die faktisch `any` sind.
Nach dem Verschieben gehen die Importe auf `../config/`, `../services/`, `../database/` und
sind echt typisiert.

Output: Vier verschobene und pfadkorrigierte Skripte, 13 gelöschte Legacy-Skripte,
angepasste Aufrufpfade in `server/package.json`.

**Zuschnitt:** `server/scripts/fix-overtime.ts` gehört NICHT in diesen Plan. Sie ist die
einzige Datei, die echtes Umschreiben braucht (Importe aus `../dist/` statt `../src/`) und
wird zusammen mit ihrer Härtung in Plan 09.1-05 behandelt. Nach diesem Plan ist sie die
letzte verbliebene `.ts`-Datei in `server/scripts/`.

**Am Quelltext geprüfte Korrektur zur Musterkarte (2026-08-29):** `09.1-PATTERNS.md` behauptet,
die `__dirname`-basierten Pfade blieben unverändert, weil `server/scripts/` und
`server/src/scripts/` „dieselbe Tiefe zu `server/database/`" hätten. **Das ist falsch und
gefährlich.** `server/src/database/migrations/` existiert und enthält 21 TypeScript-Dateien.
Bliebe `MIGRATIONS_DIR` auf `'../database/migrations'`, zeigte es nach dem Verschieben genau
dorthin, `migrate.ts` filterte auf `.sql`, fände **null** Dateien und meldete „keine
ausstehenden Migrationen" — die beiden echten SQL-Migrationen in `server/database/migrations`
liefen im Deployment nie wieder, **ohne Fehlermeldung**. Die Pfadkorrektur ist deshalb der
Kern von Task 2, nicht eine Nebensache.

**Warum dieser Plan NACH 09.1-01 laufen muss (harte Voraussetzung, nicht Reihenfolgeempfehlung):**
Alle drei Aufgaben dieses Plans tragen `cd server && npx vitest run` als Abnahmekriterium —
der volle Testlauf ist hier der Regressionsschutz dafür, dass Löschen, Verschieben und
Pfadänderung nichts gebrochen haben. Dieser Lauf trifft dieselbe Datei wie jeder andere
Testlauf des Projekts: `server/database/development.db` aus `getDatabasePath()`
(`server/vitest.config.ts` hält das ausdrücklich fest). Liefe dieser Plan parallel zu
09.1-01, träfen zwei volle vitest-Läufe ohne gesetztes `busy_timeout` gleichzeitig auf diese
Datei — genau die Fehlerklasse (`SQLITE_BUSY` ohne Wartezeit), die WR-03 (D-07) beseitigt.
Diese Ordnung in der eigenen Ausführung stehen zu lassen, während die Phase sie im Betrieb
abstellt, wäre widersprüchlich. `depends_on: ["09.1-01"]` ist deshalb inhaltlich begründet
und darf beim Ausführen nicht aufgeweicht werden.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-CONTEXT.md
@.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-PATTERNS.md
@.claude/CLAUDE.md

<interfaces>
<!-- Am Quelltext geprüft am 2026-08-29. -->

`server/tsconfig.json` — `"include": ["src/**/*"]`, `"exclude": ["node_modules", "dist"]`,
`"rootDir": "./src"`, `"outDir": "./dist"`, `"strict": true`, `"noImplicitAny": true`,
`"noUnusedParameters": true`, `"noUnusedLocals": false`. `server/scripts/` ist NICHT
ausgeschlossen, sondern schlicht nicht eingeschlossen — nach dem Verschieben erledigt sich
der Punkt ohne Änderung an dieser Datei.

Ist-Stand der vier zu verschiebenden Dateien:
- `server/scripts/migrate.ts:18` `import { databaseConfig } from '../src/config/database.js';`
  `:20-21` `__filename`/`__dirname` über `fileURLToPath(import.meta.url)`
  `:23` `const MIGRATIONS_DIR = path.join(__dirname, '../database/migrations');`
  `:193-216` CLI: `create` | `prod`/`production` | (sonst) `runMigrations(false)`
- `server/scripts/seed.ts:17` derselbe `databaseConfig`-Import
  `:22` `const SEEDS_DIR = path.join(__dirname, '../database/seeds');`
- `server/scripts/sync-db.ts:17` derselbe Import
  `:24` `const ORACLE_KEY_PATH = path.join(__dirname, '../../.ssh/oracle_key');`
- `server/scripts/validateSchema.ts:14` derselbe Import; keine `__dirname`-Nutzung

Tatsächliche Verzeichnisse (gemessen):
- `server/database/migrations/` — 2 Dateien: `20260208_add_position_column.sql`,
  `20260208_add_time_entry_type.sql`
- `server/src/database/migrations/` — 21 TypeScript-Dateien (`001_…` bis `015_…`)
- `server/database/seeds/` — 1 Datei: `001_test_user_data.sql`
- `server/src/database/seeds/` — **existiert nicht**

Referenzmuster in `server/src/scripts/` (47 Dateien): `import … from '../config/database.js'`,
`'../database/connection.js'`, `'../services/…'`, `'../utils/…'`.

Namenskollisionen geprüft: keine der vier Zieldateinamen existiert bereits in
`server/src/scripts/`.

`server/package.json` — die acht anzupassenden Zeilen (11-15, 26, 29-30):
`db:sync`, `db:sync:remote`, `migrate`, `migrate:prod`, `migrate:create`, `validate:schema`,
`seed`, `seed:reset`. Vorbild für den Zielzustand in derselben Datei:
`"migrate:copy": "tsx src/scripts/applyMigrationsToCopy.ts"`.

Aufrufer der npm-Skripte (ändern sich NICHT, weil sie über `npm run` gehen):
`.github/workflows/deploy-server.yml:103` `npm run migrate:prod`, `:113` `npm run validate:schema`;
`.github/workflows/deploy-staging.yml:97` `npm run migrate:prod`, `:106` `npm run validate:schema`.

Referenzlage der 13 Legacy-Skripte (gemessen mit `grep -rIl` über das gesamte Repository
ohne `node_modules` und `.git`, 2026-08-29): **keine ausführbare Referenz.** Alle Treffer
liegen in `.planning/`-Dokumenten; `fix-timezone-bugs.ts` wird zusätzlich in den historischen
Berichten `MIGRATION_GUIDE.md` und `SYSTEM_INCONSISTENCIES_REPORT.md` erwähnt, ebenfalls nur
beschreibend. `create-admin.ts` steht NICHT in `server/package.json`.

**Dateien in `server/scripts/`, die BLEIBEN müssen:**
- `sync-prod-to-staging.sh` — **wird von einem crontab-Eintrag auf dem Server mit absolutem
  Pfad aufgerufen**: `0 2 * * 0 /home/ubuntu/TimeTracking-Staging/server/scripts/sync-prod-to-staging.sh`
  (belegt in `14-AUSLIEFERUNG.md:585` und im Kopfkommentar der Datei selbst, `:8`).
  Verschieben oder Löschen bräche den wöchentlichen Staging-Sync stillschweigend.
- `14-ist-stand-report.mjs`, `14-ist-stand-vergleich.mjs` — Auswertungswerkzeuge der Phase 14,
  in `PROJECT_STATUS.md` benannt. `.mjs` wird von `tsc` ohnehin nie erfasst; sie berühren
  WR-04 nicht.
- `fix-overtime.ts` — Gegenstand von Plan 09.1-05.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Referenzprüfung je Datei und Entfernen der 13 Legacy-Skripte (D-10)</name>
  <files>server/scripts/analyze-dev-db.js, server/scripts/debug-december.js, server/scripts/test-current-month.js, server/scripts/test-getcurrentdate.js, server/scripts/test-month-generation.js, server/scripts/verify-db.js, server/scripts/verify-dev-db.js, server/scripts/verify-production-overtime.js, server/scripts/cleanup-invalid-entries.js, server/scripts/cleanup-invalid-time-entries.ts, server/scripts/fix-timezone-bugs.ts, server/scripts/check-db-sync.ts, server/scripts/create-admin.ts (alle 13 per git rm entfernt)</files>

  <read_first>
    - `server/package.json` (die vollständige `scripts`-Sektion — nur so ist belegbar, dass
      keine der 13 Dateien dort steht)
    - `.github/workflows/deploy-server.yml` und `.github/workflows/deploy-staging.yml`
      (alle Zeilen mit `scripts/`)
    - `server/scripts/sync-prod-to-staging.sh` Zeilen 1-15 (der Kopfkommentar mit dem
      crontab-Eintrag — Beleg dafür, warum diese Datei bleibt)
    - `.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-CONTEXT.md`, D-10
  </read_first>

  <action>
Führe für **jede** der 13 in D-10 benannten Dateien einzeln und nachweislich eine
Referenzprüfung durch, bevor du sie löschst. Die 13 Dateien sind:
`analyze-dev-db.js`, `debug-december.js`, `test-current-month.js`, `test-getcurrentdate.js`,
`test-month-generation.js`, `verify-db.js`, `verify-dev-db.js`, `verify-production-overtime.js`,
`cleanup-invalid-entries.js`, `cleanup-invalid-time-entries.ts`, `fix-timezone-bugs.ts`,
`check-db-sync.ts`, `create-admin.ts`.

Prüfmethode je Datei: `grep -rIn --exclude-dir=node_modules --exclude-dir=.git "<dateiname>" .`
Werte das Ergebnis in drei Klassen aus und halte je Datei fest, welche Klasse zutrifft:
- **ausführbare Referenz** (`server/package.json`, `.github/workflows/**`, ein `import`/`require`
  in Quellcode, ein crontab-Eintrag in einem Dokument, ein Shell-Skript) → **nicht löschen**,
  stattdessen nach `server/src/scripts/` verschieben und die Referenz mitziehen, wie D-10 es
  vorschreibt.
- **nur beschreibende Erwähnung** (`.planning/**`, `MIGRATION_GUIDE.md`,
  `SYSTEM_INCONSISTENCIES_REPORT.md`, `CHANGELOG.md`) → löschen.
- **kein Treffer** → löschen.

Lösche mit `git rm`, nicht mit `rm` — die Historie bleibt so über git erhalten, wie D-10 es
festhält, und der Löschvorgang steht sauber im Commit.

Schreibe das Ergebnis als Tabelle mit 13 Zeilen (Datei · Trefferzahl · Klasse · Entscheidung)
in die SUMMARY. Eine pauschale Aussage „keine Referenzen gefunden" genügt nicht.

**Nicht anfassen** (mit Begründung in der SUMMARY vermerken):
`server/scripts/sync-prod-to-staging.sh` — der Server ruft sie wöchentlich über einen
crontab-Eintrag mit absolutem Pfad auf; `server/scripts/14-ist-stand-report.mjs` und
`server/scripts/14-ist-stand-vergleich.mjs` — in `PROJECT_STATUS.md` benannte
Auswertungswerkzeuge, von `tsc` ohnehin nie erfasst; `server/scripts/fix-overtime.ts` —
Gegenstand von Plan 09.1-05.
  </action>

  <acceptance_criteria>
    - `ls server/scripts/` listet danach genau vier Einträge: `fix-overtime.ts`,
      `sync-prod-to-staging.sh`, `14-ist-stand-report.mjs`, `14-ist-stand-vergleich.mjs`.
    - `git status --porcelain server/scripts/` zeigt für jede der 13 Dateien den Status `D`
      (gelöscht, im Index).
    - Die SUMMARY enthält eine Tabelle mit genau 13 Zeilen, je Datei mit Trefferzahl und
      Entscheidung.
    - `grep -rIn --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning
      "create-admin.ts\|check-db-sync.ts\|verify-production-overtime.js" server/ .github/`
      liefert keinen Treffer.
    - `cd server && npx tsc --noEmit` endet mit Exitcode 0 (die Löschungen dürfen nichts
      brechen).
    - `cd server && npx vitest run` zeigt keine neue rote Testdatei.
  </acceptance_criteria>

  <verify>
    <automated>ls server/scripts/ && cd server && npx tsc --noEmit && npx vitest run</automated>
  </verify>

  <done>
    13 Legacy-Skripte per `git rm` entfernt, je Datei mit dokumentierter Referenzprüfung;
    `server/scripts/` enthält nur noch die vier bewusst belassenen Einträge; `tsc` und
    Testlauf unverändert grün.
  </done>
</task>

<task type="auto">
  <name>Task 2: Vier Skripte verschieben und alle Pfade korrigieren (D-09)</name>
  <files>server/src/scripts/migrate.ts, server/src/scripts/seed.ts, server/src/scripts/sync-db.ts, server/src/scripts/validateSchema.ts (aus server/scripts/ per git mv)</files>

  <read_first>
    - `server/scripts/migrate.ts` (vollständig — Importe, `__dirname`-Konstruktion,
      `MIGRATIONS_DIR`, die CLI-Verzweigung am Dateiende)
    - `server/scripts/seed.ts` (vollständig — `SEEDS_DIR`, `checkEnvironment()`,
      `databaseConfig.developmentPath`)
    - `server/scripts/sync-db.ts` (vollständig — `ORACLE_KEY_PATH`, die dynamischen
      `import('better-sqlite3')`-Stellen)
    - `server/scripts/validateSchema.ts` (vollständig)
    - `server/src/scripts/applyMigrationsToCopy.ts` (ein bereits dort liegendes Skript als
      Referenz für Importtiefe und Stil)
    - `server/tsconfig.json` (die `strict`-Schalter, gegen die die Dateien nun geprüft werden)
    - `server/src/config/database.ts` (wie `databaseConfig.path`, `.developmentPath` und
      `.productionPath` aufgelöst werden)
  </read_first>

  <action>
Verschiebe die vier Dateien mit `git mv`, damit git die Umbenennung erkennt und die Historie
folgt:
- `server/scripts/migrate.ts` → `server/src/scripts/migrate.ts`
- `server/scripts/seed.ts` → `server/src/scripts/seed.ts`
- `server/scripts/sync-db.ts` → `server/src/scripts/sync-db.ts`
- `server/scripts/validateSchema.ts` → `server/src/scripts/validateSchema.ts`

Korrigiere danach in jeder Datei **beide** Pfadarten. Sie verhalten sich unterschiedlich und
dürfen nicht verwechselt werden:

**(a) Modulimporte werden eine Ebene flacher.** In allen vier Dateien:
`import { databaseConfig } from '../src/config/database.js';`
→ `import { databaseConfig } from '../config/database.js';`

**(b) `__dirname`-basierte Dateisystempfade werden eine Ebene TIEFER.** `server/src/scripts/`
liegt eine Ebene unter `server/scripts/`; jeder relative Pfad, der aus dem Skriptverzeichnis
heraus auf `server/…` zeigt, braucht ein zusätzliches `../`:
- `migrate.ts`: `path.join(__dirname, '../database/migrations')`
  → `path.join(__dirname, '../../database/migrations')`
- `seed.ts`: `path.join(__dirname, '../database/seeds')`
  → `path.join(__dirname, '../../database/seeds')`
- `sync-db.ts`: `path.join(__dirname, '../../.ssh/oracle_key')`
  → `path.join(__dirname, '../../../.ssh/oracle_key')`
- `validateSchema.ts`: keine `__dirname`-Nutzung — nichts zu tun. Prüfe das am Quelltext,
  statt es anzunehmen.

Suche in allen vier Dateien nach weiteren `__dirname`-, `path.join`-, `path.resolve`- und
`process.cwd()`-Stellen und behandle jede einzeln; die obige Liste ist am Quelltext erhoben,
aber die Datei ist maßgeblich, nicht die Liste.

**Zusätzliche Absicherung in `migrate.ts` — begründet, nicht Umbau:** Ergänze unmittelbar
nach der Definition von `MIGRATIONS_DIR` eine Prüfung, die abbricht, wenn das Verzeichnis
nicht existiert **oder** keine einzige `.sql`-Datei enthält: Fehlermeldung auf `stderr`, die
den aufgelösten Pfad nennt, und `process.exit(1)`. Grund (der eigentliche Anlass dieser
Aufgabe): `server/src/database/migrations/` existiert und enthält 21 `.ts`-Dateien. Zeigte
`MIGRATIONS_DIR` versehentlich dorthin, fände der `.sql`-Filter null Dateien und das Werkzeug
meldete Erfolg ohne eine einzige Migration — im Deployment gegen die Produktionsdatenbank ein
stiller Ausfall. Ein leerer Migrationsordner ist im Zustand dieses Projekts immer ein Fehler,
kein gültiger Zustand.

Behebe alle `tsc`-Fehler, die durch die neue Abdeckung auftreten, **im engsten Sinn**: kein
`any` (nach `.claude/CLAUDE.md` `unknown` plus Type Guard), keine Umgestaltung der Logik,
keine Umbenennung von Funktionen, keine Verhaltensänderung. Ein durch `noUnusedParameters`
gemeldeter Parameter wird mit `_` präfixiert, nicht entfernt. Halte jeden Fix mit Datei,
Zeile und Fehlercode in der SUMMARY fest.
  </action>

  <acceptance_criteria>
    - `ls server/src/scripts/migrate.ts server/src/scripts/seed.ts server/src/scripts/sync-db.ts server/src/scripts/validateSchema.ts`
      listet alle vier Dateien.
    - `ls server/scripts/*.ts` listet ausschließlich `server/scripts/fix-overtime.ts`.
    - `git status --porcelain` zeigt die vier Dateien als Umbenennung (`R`), nicht als
      Löschung plus Neuanlage.
    - `grep -c "'\.\./src/config/database.js'" server/src/scripts/migrate.ts server/src/scripts/seed.ts server/src/scripts/sync-db.ts server/src/scripts/validateSchema.ts`
      liefert für jede Datei `0`.
    - `grep -c "'\.\./config/database.js'" server/src/scripts/migrate.ts server/src/scripts/seed.ts server/src/scripts/sync-db.ts server/src/scripts/validateSchema.ts`
      liefert für jede Datei `1`.
    - `grep -n "database/migrations" server/src/scripts/migrate.ts` zeigt
      `'../../database/migrations'`.
    - `grep -n "database/seeds" server/src/scripts/seed.ts` zeigt `'../../database/seeds'`.
    - `grep -n "oracle_key" server/src/scripts/sync-db.ts` zeigt `'../../../.ssh/oracle_key'`.
    - `cd server && npx tsc --noEmit` endet mit Exitcode 0 — damit ist das
      ROADMAP-Erfolgskriterium „`server/scripts/**` läuft unter `tsc --noEmit`" erfüllt,
      weil es kein `server/scripts/**` mit TypeScript mehr gibt außer der in Plan 09.1-05
      behandelten Datei.
    - `grep -rn ": any\b" server/src/scripts/migrate.ts server/src/scripts/seed.ts server/src/scripts/sync-db.ts server/src/scripts/validateSchema.ts`
      liefert keinen Treffer.
    - Gegenprobe zur Pfadkorrektur: Setze `MIGRATIONS_DIR` probeweise wieder auf
      `'../database/migrations'` und rufe `cd server && npm run migrate` auf — der Lauf muss
      jetzt mit der neuen Prüfung und Exitcode 1 abbrechen und den falschen Pfad nennen.
      Beide Ausgaben (falscher und richtiger Pfad) gehören in die SUMMARY; danach den
      richtigen Wert wiederherstellen und `git diff` prüfen.
  </acceptance_criteria>

  <verify>
    <automated>cd server && npx tsc --noEmit && ls src/scripts/migrate.ts src/scripts/seed.ts src/scripts/sync-db.ts src/scripts/validateSchema.ts</automated>
  </verify>

  <done>
    Vier Dateien liegen unter `server/src/scripts/`, alle Importe auf `../config/database.js`
    umgestellt, alle `__dirname`-Pfade um eine Ebene vertieft, `migrate.ts` bricht bei leerem
    Migrationsordner laut ab; `tsc --noEmit` Exitcode 0; die Gegenprobe zur Pfadkorrektur ist
    protokolliert.
  </done>
</task>

<task type="auto">
  <name>Task 3: Aufrufpfade in server/package.json nachziehen und die Werkzeuge nachweislich laufen lassen (D-11)</name>
  <files>server/package.json</files>

  <read_first>
    - `server/package.json` (die vollständige `scripts`-Sektion — die acht anzupassenden
      Zeilen und die bereits richtigen Einträge wie `migrate:copy` als Vorbild)
    - `.github/workflows/deploy-server.yml` Zeilen 95-115 (die Aufrufe `npm run migrate:prod`
      und `npm run validate:schema` — sie gehen über `npm run` und ändern sich NICHT)
    - `.github/workflows/deploy-staging.yml` Zeilen 90-110 (dieselben zwei Aufrufe)
    - `server/src/scripts/migrate.ts` und `server/src/scripts/validateSchema.ts` nach Task 2
  </read_first>

  <action>
Ändere in `server/package.json` genau diese acht Werte von `scripts/…` auf `src/scripts/…`,
sonst nichts:
- `db:sync` → `tsx src/scripts/sync-db.ts`
- `db:sync:remote` → `tsx src/scripts/sync-db.ts --remote`
- `migrate` → `tsx src/scripts/migrate.ts`
- `migrate:prod` → `tsx src/scripts/migrate.ts prod`
- `migrate:create` → `tsx src/scripts/migrate.ts create`
- `validate:schema` → `tsx src/scripts/validateSchema.ts`
- `seed` → `tsx src/scripts/seed.ts`
- `seed:reset` → `tsx src/scripts/seed.ts --reset`

Die beiden Deploy-Workflows werden in diesem Plan NICHT angefasst: sie rufen
`npm run migrate:prod` und `npm run validate:schema` auf, also über `npm run`, und erben die
Korrektur. Der einzige Workflow-Aufruf mit direktem Pfad ist `npx tsx scripts/fix-overtime.ts`
— der gehört zu Plan 09.1-05.

Weise danach nach, dass die verschobenen Werkzeuge tatsächlich funktionieren, und zwar
ausschließlich gegen die lokale Entwicklungsdatenbank (D-16 — kein `DATABASE_PATH` auf eine
Produktionsdatei, kein `NODE_ENV=production`, kein `migrate:prod`):

1. `cd server && npm run migrate` — muss durchlaufen und die beiden SQL-Migrationen
   `20260208_add_position_column.sql` und `20260208_add_time_entry_type.sql` entweder anwenden
   oder als bereits angewandt melden. Sichere `server/database/development.db` vorher per
   Dateikopie (der Lauf ist idempotent, aber ein Beleg schadet nicht) und halte in der SUMMARY
   fest, ob sich die Datei verändert hat.
2. `cd server && npm run validate:schema` — muss durchlaufen und einen Schemabericht
   ausgeben, keinen Modulauflösungsfehler.
3. `cd server && npx tsx src/scripts/sync-db.ts --help` oder der Aufruf ohne Argumente —
   belegt, dass die Datei geladen wird. **Führe keinen echten Sync gegen den Produktionsserver
   aus** (D-16). Wird die Datei ohne Argumente sofort aktiv, brich mit Strg-C ab und halte das
   in der SUMMARY fest, statt den Lauf zu Ende zu bringen.
4. `npm run seed` NICHT ausführen — es verändert die Entwicklungsdatenbank. Belege stattdessen
   per `grep -n "database/seeds" server/src/scripts/seed.ts`, dass der Pfad auf
   `'../../database/seeds'` steht, und per `ls server/database/seeds`, dass dort
   `001_test_user_data.sql` liegt.
  </action>

  <acceptance_criteria>
    - `grep -c '"tsx scripts/' server/package.json` liefert `0`.
    - `grep -c 'tsx src/scripts/migrate.ts' server/package.json` liefert `3`
      (`migrate`, `migrate:prod`, `migrate:create`).
    - `grep -c 'tsx src/scripts/sync-db.ts' server/package.json` liefert `2`.
    - `grep -c 'tsx src/scripts/seed.ts' server/package.json` liefert `2`.
    - `grep -c 'tsx src/scripts/validateSchema.ts' server/package.json` liefert `1`.
    - `cd server && npm run migrate` endet mit Exitcode 0; die Ausgabe nennt beide
      SQL-Migrationsdateien namentlich (angewandt oder bereits angewandt).
    - `cd server && npm run validate:schema` endet ohne Modulauflösungsfehler; die Ausgabe
      enthält einen Schemabericht.
    - `git diff .github/workflows/` ist leer.
    - `cd server && npx tsc --noEmit` endet mit Exitcode 0.
    - `cd server && npx vitest run`: die Menge der roten Tests ist gegenüber dem
      Ausgangsstand nicht gewachsen.
    - In keinem Lauf dieses Plans war `DATABASE_PATH` auf eine Datei mit `production` im
      Namen gesetzt und `NODE_ENV` nicht auf `production` — mit der jeweils verwendeten
      Kommandozeile in der SUMMARY belegt (D-16).
  </acceptance_criteria>

  <verify>
    <automated>cd server && npm run migrate && npm run validate:schema && npx tsc --noEmit</automated>
  </verify>

  <done>
    Alle acht npm-Skripte zeigen auf `src/scripts/`; `npm run migrate` und
    `npm run validate:schema` laufen nachweislich durch; die Deploy-Workflows sind
    unverändert; kein Schritt hat die Produktionsdatenbank berührt.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `server/package.json` → Deploy-Workflow → Produktionsserver | `npm run migrate:prod` läuft im Deployment gegen `/home/ubuntu/databases/production.db`. Ein falscher Pfad hier wirkt unmittelbar auf die Produktion. |
| Skriptverzeichnis → Dateisystem | Die `__dirname`-Pfade entscheiden, welchen Migrations-, Seed- und Schlüsselordner die Werkzeuge lesen. |
| Entwicklerrechner → `.ssh/oracle_key` | `sync-db.ts` liest einen privaten SSH-Schlüssel über einen relativen Pfad. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-11 | Tampering | `migrate.ts` zeigt nach dem Verschieben auf `server/src/database/migrations`, findet null `.sql`-Dateien und meldet stillen Erfolg — die Produktionsmigrationen laufen nie | mitigate | Pfadkorrektur auf `'../../database/migrations'` (Task 2) PLUS ausdrückliche Abbruchprüfung mit Exitcode 1, wenn der aufgelöste Ordner nicht existiert oder keine `.sql`-Datei enthält; Gegenprobe mit dem falschen Pfad ist Abnahmekriterium. |
| T-09.1-12 | Denial of Service | Eine der acht `package.json`-Zeilen bleibt auf `scripts/…` stehen; `npm run migrate:prod` bricht im Deployment ab | mitigate | Abnahmekriterium `grep -c '"tsx scripts/' server/package.json` = 0 plus je Skript eine Zählprüfung; `npm run migrate` und `npm run validate:schema` werden lokal tatsächlich ausgeführt, nicht nur gelesen. |
| T-09.1-13 | Denial of Service | Eine gelöschte Legacy-Datei war doch referenziert; ein Werkzeug oder Deployment bricht später ab | mitigate | D-10-Referenzprüfung je Datei mit dokumentierter Trefferzahl und Klasse; `sync-prod-to-staging.sh` ist wegen des crontab-Eintrags ausdrücklich von der Löschung ausgenommen; `tsc` und voller Testlauf nach dem Löschen. |
| T-09.1-14 | Information Disclosure | `sync-db.ts` liest `.ssh/oracle_key` über einen relativen Pfad; ein falsch korrigierter Pfad könnte auf eine andere Datei zeigen | mitigate | Pfad in Task 2 ausdrücklich auf `'../../../.ssh/oracle_key'` festgelegt und als Abnahmekriterium per `grep` geprüft; der Schlüssel selbst wird nicht bewegt, nicht kopiert und nicht protokolliert. Kein echter Sync-Lauf in diesem Plan. |
| T-09.1-15 | Tampering | Ein `tsc`-Fehler wird durch `any` oder einen `@ts-ignore` stillgelegt statt behoben | mitigate | Abnahmekriterium: `grep -rn ": any\b"` über die vier Dateien liefert keinen Treffer; jeder Fix mit Datei, Zeile und Fehlercode in der SUMMARY. |
| T-09.1-16 | Tampering | Ein Prüflauf richtet sich versehentlich auf die Produktionsdatenbank | mitigate | D-16: `npm run migrate:prod` wird in diesem Plan ausdrücklich NICHT ausgeführt; jede verwendete Kommandozeile wird in der SUMMARY zitiert; `npm run seed` bleibt ungefahren. |
| T-09.1-SC | Tampering | npm/pip/cargo-Installationen | mitigate | Dieser Plan installiert KEIN Paket; er verschiebt und löscht ausschließlich vorhandene Dateien und ändert Skriptpfade. Wird `npm install` doch nötig, ist es vorher mit einem blockierenden Checkpoint dem Anwender vorzulegen. |
</threat_model>

<verification>
- `cd server && npx tsc --noEmit` → Exitcode 0
- `ls server/scripts/*.ts` → nur `fix-overtime.ts`
- `ls server/scripts/` → genau vier Einträge
- `cd server && npm run migrate` → Exitcode 0, beide SQL-Migrationen namentlich in der Ausgabe
- `cd server && npm run validate:schema` → Exitcode 0
- `cd server && npx vitest run` → keine neue rote Testdatei
- `git diff .github/workflows/` → leer
</verification>

<success_criteria>
- Die vier aktiv referenzierten Skripte liegen unter `server/src/scripts/` und sind von
  `tsconfig.json` ohne Sonderkonfiguration erfasst (D-09)
- Die 13 Legacy-Skripte sind mit dokumentierter Einzelprüfung entfernt (D-10)
- `server/package.json` zieht alle acht Aufrufpfade nach; die Deploy-Workflows brauchen keine
  Änderung und bekommen keine (D-11)
- `migrate.ts` findet weiterhin genau die SQL-Migrationen aus `server/database/migrations` und
  bricht laut ab, wenn nicht
- Kein Schritt hat die Produktionsdatenbank berührt (D-16)
</success_criteria>

<output>
Erstelle `.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-03-SUMMARY.md`
</output>
