---
phase: 09.1-journal-backfill-und-betriebs-h-rtung
plan: 06
type: execute
wave: 3
depends_on: ["09.1-01", "09.1-02", "09.1-03", "09.1-04", "09.1-05"]
files_modified:
  - .planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-NACHWEIS-STAGING.md
autonomous: false
requirements: [WR-02, WR-03, WR-04, WR-05]

must_haves:
  truths:
    - "D-14: Die Änderungen laufen zuerst auf Green (Port 3001) — der Wirkungsnachweis ist dort ohne Produktionsrisiko geführt, bevor Blue angefasst wird."
    - "D-15: Der Stand der drei Commits vom 27.08.2026 ist gemessen statt angenommen; die SUMMARY nennt für jeden Hash, ob er bereits in origin/main steht."
    - "D-16: Kein Schritt öffnet production.db — weder lesend noch schreibend, weder per sqlite3 noch per VACUUM INTO noch per Skript mit DATABASE_PATH."
    - "D-01: Auf Green belegt das Protokoll den In-Prozess-Scheduler (03:15 Europe/Berlin) und den einmaligen Anlauf nach Serverstart."
    - "D-03: Auf dem Server enthält crontab -l keinen fix-overtime-Eintrag mehr, der wöchentliche sync-prod-to-staging-Eintrag steht unverändert."
    - "D-05: Die Bilanzzeile verarbeitet/fehlgeschlagen steht im Green-Protokoll und ist der Erfolgsnachweis des Laufs."
  artifacts:
    - path: ".planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-NACHWEIS-STAGING.md"
      provides: "Messprotokoll der Green-Auslieferung: Gates, D-15-Messung, Workflow-Lauf, Protokollausschnitte, crontab, Dateizeiger"
      min_lines: 60
  key_links:
    - from: "origin/staging"
      to: "Green-Server /home/ubuntu/TimeTracking-Green"
      via: "deploy-staging.yml, git reset --hard origin/staging, PM2-Prozess timetracking-staging auf Port 3001"
      pattern: "timetracking-staging"
    - from: "Green-Protokoll"
      to: "server/src/services/overtimeRecalcRunner.ts"
      via: "Bilanzzeile mit anlass: serverstart"
      pattern: "serverstart"
---

<objective>
Die Code-Härtung dieser Phase auf Staging ausliefern und ihre Wirkung dort belegen, bevor die
Produktion angefasst wird (D-14). `.claude/CLAUDE.md` §Deployment führt Green als „optional
für isolierte Tests, nicht Teil des Standardwegs" — genau dieser Fall liegt hier vor. Der
Umweg ist bewusst gewählt und kein Abweichen von der Leitlinie.

Purpose: Die Änderungen dieser Phase berühren den Migrationsaufruf des Deployments
(`npm run migrate:prod` zeigt nach Plan 09.1-03 auf einen neuen Pfad), die crontab des Servers
und die Startsequenz des Serverprozesses. Jede dieser drei Stellen kann ein Deployment
stillschweigend beschädigen. Green ist der Ort, an dem sich das ohne Produktionsrisiko zeigt.

Output: `09.1-NACHWEIS-STAGING.md` mit gemessenen Zahlen und wörtlichen Protokollausschnitten;
die Produktionsauslieferung folgt in Plan 09.1-07.

**Vorbefund zu D-15, am 29.08.2026 lokal gemessen und in diesem Plan zu überprüfen, nicht zu
übernehmen:** `git merge-base --is-ancestor` meldet für alle drei Commits `ad48068`,
`ded14d3` und `c0acb59`, dass sie **bereits in `origin/main` stehen**;
`git log --oneline origin/main..HEAD` listet nur die zwei Dokumentations-Commits der
Kontextsitzung. `.claude/CLAUDE.md` nennt Desktop `v1.9.1` als ausgeliefert (28.08.2026) und
den Backup-Download-Defekt als behoben — das ist genau der Punkt, für den D-15 ein
Desktop-Release fordert. Die Prämisse von D-15 ist damit vermutlich überholt. **Das ist zu
messen, nicht anzunehmen**, und das Messergebnis gehört in die SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-CONTEXT.md
@.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-01-SUMMARY.md
@.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-03-SUMMARY.md
@.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-04-SUMMARY.md
@.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-05-SUMMARY.md
@.planning/debug/wal-abgehaengt-20260827.md
@.claude/CLAUDE.md
@ENV.md

<interfaces>
<!-- Am Quelltext und am Repository geprüft am 2026-08-29. -->

`.github/workflows/deploy-staging.yml`
- Auslöser: `push` auf Branch `staging` mit `paths: ['server/**']`, zusätzlich
  `workflow_dispatch`.
- Der Server holt den Stand über `git fetch origin staging` und `git reset --hard origin/staging`
  in `/home/ubuntu/TimeTracking-Green`.
- PM2-Prozessname: `timetracking-staging`, Port `3001`, `NODE_ENV=staging`.
- Der Green-Server setzt **kein** `DATABASE_PATH`. `server/src/config/database.ts:27-35`
  liefert für jedes `NODE_ENV` ungleich `production` den Entwicklungspfad — Green arbeitet
  also auf `TimeTracking-Green/server/database/development.db` und **nicht** auf
  `production.db`. Das ist die Grundlage dafür, dass D-16 in diesem Plan eingehalten werden
  kann.

`.github/workflows/deploy-server.yml`
- Auslöser: `push` auf `main` mit `paths: ['server/**']`. **Ein Push nach `main` löst
  unmittelbar ein Produktions-Deployment aus.** Deshalb geht in diesem Plan nichts nach `main`;
  das ist Plan 09.1-07.

Lokaler Git-Stand am 29.08.2026 (vor der Ausführung erneut messen):
- Branch `main`, `git log --oneline origin/main..HEAD` = 2 Commits (`5991da2`, `5e49cc2`,
  beide reine `.planning/`-Dokumentation)
- `git status --porcelain` = eine unverfolgte Datei (`09.1-PATTERNS.md`)
- `ad48068`, `ded14d3`, `c0acb59` sind Vorfahren von `origin/main`

Lesende Serverprüfungen, die `.claude/CLAUDE.md` §Datenbank-Regeln 3 ausdrücklich vorschreibt
und die **keinen** Datenbankzugriff darstellen:
```
PID=$(pm2 jlist | jq -r '.[]|select(.name=="timetracking-staging").pid')
ls -l /proc/$PID/fd | grep '\.db'     # steht "(deleted)" dabei → STOPP
```
SSH-Zugang: `ssh -i .ssh/oracle_server.key ubuntu@129.159.8.19` (siehe `ENV.md`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vollständiges lokales Gate, D-15-Messung und Commit-Ordnung</name>
  <files>keine Quelldatei geändert — Ergebnis sind Gate-Messwerte und die Commit-Ordnung auf main (ungepusht)</files>

  <read_first>
    - Alle fünf SUMMARY-Dateien der Pläne 09.1-01 bis 09.1-05 (die dort festgehaltenen
      Ausgangs- und Endstände der Gates — nur gegen sie ist ein Zuwachs beurteilbar)
    - `.claude/CLAUDE.md` Abschnitt „Prüfliste vor dem Commit" und §Deployment
    - `.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-CONTEXT.md`, D-15
    - `git log --oneline -15` und `git status` (der tatsächliche Ist-Stand des Arbeitsbaums)
  </read_first>

  <action>
**(a) Alle vier Gates fahren und die Zahlen festhalten**, jeweils mit Kommandozeile und
Exitcode:
1. `cd server && npx tsc --noEmit` → muss Exitcode 0 liefern
2. `cd desktop && npx tsc --noEmit` → muss Exitcode 0 liefern (diese Phase ändert den Desktop
   nicht; das Gate belegt, dass sie ihn auch nicht versehentlich gebrochen hat)
3. `cd server && npx vitest run` → die Menge der **roten** Tests darf gegenüber dem in den
   SUMMARYs festgehaltenen Ausgangsstand nicht gewachsen sein. Nenne die roten Tests einzeln
   mit Datei und Zeile und ordne jeden zu: vorbestehend oder neu. Ein neuer roter Test
   blockiert die Auslieferung.
4. `cd server && npm run check:rules`, falls das Skript in `server/package.json` existiert —
   prüfe das am Quelltext; existiert es nicht, halte das fest, statt einen Lauf zu erfinden.

**(b) D-15 messen, nicht annehmen.** Führe aus und protokolliere die Ausgabe wörtlich:
- `git fetch origin` (nur holen, nichts verändern)
- `git log --oneline origin/main..HEAD`
- `git rev-list --left-right --count origin/main...HEAD`
- für jeden der drei Hashes `ad48068`, `ded14d3`, `c0acb59`:
  `git merge-base --is-ancestor <hash> origin/main; echo $?` — `0` bedeutet „steht bereits in
  origin/main"
- `gh release list --limit 5` — belegt, ob ein Desktop-Release nach dem 27.08.2026 existiert
  (`ded14d3` braucht nach D-15 ein Desktop-Release)

Werte aus und halte in der SUMMARY je Hash eine Zeile fest:
- Steht ein Hash bereits in `origin/main`: D-15 ist für diesen Commit **erfüllt**; nenne das
  Release oder Deployment, mit dem er ausgeliefert wurde.
- Steht ein Hash **nicht** in `origin/main`: er geht mit dieser Phase mit; halte fest, welcher
  Auslieferungsweg nötig ist (`ad48068` löst ein Server-Deployment aus, `ded14d3` braucht ein
  Desktop-Release).
Eine pauschale Aussage genügt nicht; D-15 verlangt eine Aussage je Commit.

**(c) Commits ordnen.** Fasse die Arbeit dieser Phase in unvermischte, einzeln revertierbare
Commits — je Plan mindestens einen, je Befund höchstens ein Commit-Satz. Verwende die
Commit-Konvention des Projekts (`fix(09.1): …`, `test(09.1): …`, `refactor(09.1): …`,
`ci(09.1): …`, `docs(09.1): …`). Jede Commit-Nachricht erklärt das WARUM, nicht nur das WAS.
**Noch nicht pushen** — der Push ist Gegenstand des Checkpoints in Task 2.

**(d)** `git status --porcelain` muss danach leer sein.
  </action>

  <acceptance_criteria>
    - `cd server && npx tsc --noEmit` Exitcode 0; `cd desktop && npx tsc --noEmit` Exitcode 0
      — beide Kommandozeilen und Exitcodes wörtlich in der SUMMARY.
    - `cd server && npx vitest run`: Zahl grün/rot genannt; jeder rote Test einzeln mit Datei
      und Zeile aufgeführt und als vorbestehend belegt (Abgleich gegen die SUMMARYs der Pläne
      01-05). Kein neuer roter Test.
    - Die SUMMARY enthält für jeden der drei Hashes `ad48068`, `ded14d3`, `c0acb59` eine Zeile
      mit dem Ergebnis von `git merge-base --is-ancestor` (`0` oder `1`) und der daraus
      abgeleiteten Aussage zu D-15.
    - Die SUMMARY enthält die Ausgabe von `gh release list --limit 5`.
    - `git status --porcelain` ist leer.
    - `git log --oneline origin/main..HEAD` listet die Commits dieser Phase; jeder Commit
      berührt genau einen Befund oder genau einen Plan.
    - Es wurde **nicht** gepusht: `git rev-list --count origin/main..HEAD` ist größer als 0.
    - In keinem Schritt wurde `production.db` geöffnet, kopiert oder als `DATABASE_PATH`
      gesetzt — die verwendeten Kommandozeilen stehen vollständig in der SUMMARY (D-16).
  </acceptance_criteria>

  <verify>
    <automated>cd server && npx tsc --noEmit && npx vitest run; cd ../desktop && npx tsc --noEmit</automated>
  </verify>

  <done>
    Alle Gates gefahren und mit Zahlen belegt; D-15 je Commit gemessen statt angenommen; die
    Arbeit steht in unvermischten Commits auf `main`, ungepusht.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Freigabe für die Staging-Auslieferung (D-14)</name>
  <files>keine Datei geändert — Entscheidungsvorlage an den Anwender</files>

  <action>
Lege dem Anwender die unter <context> genannten Messwerte vor, hole seine Entscheidung ein und halte sie wörtlich mit Datum fest. Pushe nichts, bevor die Entscheidung vorliegt.
  </action>

  <decision>
Soll der Stand dieser Phase nach `origin/staging` gebracht werden, um das Green-Deployment
(Port 3001) auszulösen — und wenn ja, auf welchem Weg?
  </decision>

  <context>
D-14 legt fest: erst Staging (Green, Port 3001), dann Produktion (Blue, Port 3000). Der Weg
über Green ist Teil des Plans, nicht optional.

`deploy-staging.yml` löst bei einem Push auf den Branch `staging` mit Änderungen unter
`server/**` aus und setzt den Green-Server per `git reset --hard origin/staging` auf diesen
Stand. Der Branch `staging` wird laut Kopfkommentar des Workflows „im 2-Tier-Ablauf nicht
gepflegt" — sein aktueller Inhalt ist vor der Entscheidung zu messen
(`git fetch origin && git log --oneline -3 origin/staging` sowie
`git rev-list --left-right --count origin/staging...HEAD`) und dem Anwender zusammen mit
dieser Entscheidung vorzulegen.

Ein Push auf `main` kommt hier **nicht** in Frage: `deploy-server.yml` löst bei
`server/**`-Änderungen unmittelbar ein Produktions-Deployment aus und würde D-14 verletzen.

Zu klären ist außerdem, was mit dem Messergebnis aus Task 1 (b) zu D-15 geschieht.
  </context>

  <options>
    <option id="option-a">
      <name>Zwangsüberschreiben von origin/staging mit dem aktuellen Stand</name>
      <pros>Green trägt exakt den Stand, der später nach Produktion geht — der Nachweis ist aussagekräftig. Der Branch wird ohnehin nicht gepflegt.</pros>
      <cons>`git push origin HEAD:staging --force-with-lease` verwirft den bisherigen Inhalt von `staging`. Der bisherige Inhalt ist vorher zu messen und in der SUMMARY zu nennen.</cons>
    </option>
    <option id="option-b">
      <name>Green über workflow_dispatch mit dem bestehenden staging-Stand auslösen</name>
      <pros>Kein Branch wird überschrieben.</pros>
      <cons>Green liefe dann NICHT auf dem Stand dieser Phase — der Nachweis wäre wertlos. Nur sinnvoll, wenn `origin/staging` bereits auf dem Stand dieser Phase steht.</cons>
    </option>
    <option id="option-c">
      <name>Green auslassen und direkt nach Produktion</name>
      <pros>Ein Auslieferungsvorgang statt zwei.</pros>
      <cons>Verletzt D-14 ausdrücklich. Die drei riskanten Stellen dieser Phase (Migrationspfad, crontab, Startsequenz) würden zuerst an der Produktion geprüft. Nur bei ausdrücklicher, begründeter Anweisung des Anwenders.</cons>
    </option>
  </options>

  <read_first>
    - `.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-CONTEXT.md`, D-14/D-15
    - `.github/workflows/deploy-staging.yml` Zeilen 1-70 (Auslöser und Pull-Verhalten)
    - `.claude/CLAUDE.md` §Deployment (Green als „optional für isolierte Tests")
    - `.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-06-SUMMARY.md`-Entwurf
      bzw. die Ergebnisse aus Task 1 (die Gate-Zahlen und die D-15-Messung, die dem Anwender
      vorzulegen sind)
  </read_first>

  <acceptance_criteria>
    - Dem Anwender wurden vor der Entscheidung vorgelegt: die vier Gate-Ergebnisse aus Task 1,
      die D-15-Messung je Commit, der aktuelle Inhalt von `origin/staging`
      (`git log --oneline -3 origin/staging`) und die Zahl der Commits, um die `HEAD` von
      `origin/staging` abweicht.
    - Die Entscheidung des Anwenders ist wörtlich in der SUMMARY festgehalten, mit Datum.
    - Ohne Freigabe wird nichts gepusht.
  </acceptance_criteria>

  <resume-signal>Wähle: option-a, option-b oder option-c — oder beschreibe einen anderen Weg.</resume-signal>

  <verify>
    <human-check>Die vier Gate-Ergebnisse, die D-15-Messung je Commit und der aktuelle Inhalt von origin/staging wurden dem Anwender vorgelegt; seine Entscheidung liegt wörtlich vor.</human-check>
  </verify>

  <done>
    Die Entscheidung des Anwenders ist wörtlich und mit Datum festgehalten; vor der Freigabe wurde nichts gepusht.
  </done>
</task>

<task type="auto">
  <name>Task 3: Green ausliefern und die Wirkung dort messen (D-01, D-03, D-05, D-14, D-16)</name>
  <files>.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-NACHWEIS-STAGING.md (neu)</files>

  <read_first>
    - `.github/workflows/deploy-staging.yml` nach Plan 09.1-05 (der Ablauf, dessen Schritte
      gleich im Protokoll wiederzufinden sind — insbesondere „Running database migrations"
      und der Bereinigungsschritt der crontab)
    - `server/src/services/cronService.ts` und `server/src/server.ts` nach Plan 09.1-04 (die
      Log-Meldungen, nach denen im Green-Protokoll zu suchen ist — der Ausführende muss die
      exakten Zeichenketten kennen, statt zu raten)
    - `server/src/services/overtimeRecalcRunner.ts` (Feldnamen der Bilanzzeile)
    - `.claude/CLAUDE.md` §Datenbank-Regeln 3 (die lesende Prüfung der Dateizeiger) und
      §Deployment (die Pflichtprüfungen nach jedem Deployment)
    - `ENV.md` (SSH-Zugang, Ports, Verzeichnisse)
  </read_first>

  <action>
Führe die in Task 2 freigegebene Auslieferung nach Green durch und weise ihre Wirkung nach.
Alles, was du misst, gehört wörtlich in
`.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-NACHWEIS-STAGING.md`.

**(a) Auslieferung.** Nach der Freigabe pushen; danach den Lauf beobachten:
`gh run list --workflow="deploy-staging.yml" --limit 1` bis `completed` und `success`. Bei
Fehlschlag `gh run view <run-id> --log-failed` und den Grund benennen, statt zu wiederholen.

**(b) Der kritische Schritt: die Migrationen.** Hole aus dem Workflow-Protokoll den Abschnitt
„Running database migrations" wörtlich. Er muss zeigen, dass `npm run migrate:prod` über den
neuen Pfad `src/scripts/migrate.ts` läuft, **keinen** Modulauflösungsfehler wirft und die
SQL-Migrationen aus `server/database/migrations` findet. Das ist der Nachweis gegen die in
Plan 09.1-03 benannte Gefahr, dass ein falscher `MIGRATIONS_DIR` still null Dateien findet.
Ebenso den Abschnitt „Validating database schema".

**(c) Gesundheit.** `curl -s http://129.159.8.19:3001/api/health` — HTTP 200, Antwortkörper
wörtlich protokollieren.

**(d) Der In-Prozess-Scheduler (D-01, D-02).** Über SSH
`pm2 logs timetracking-staging --lines 300 --nostream` lesen und drei Belege heraussuchen:
1. die Zeile, die den Start des Überstunden-Schedulers mit `03:15` und `Europe/Berlin` meldet
2. die Startzeile des einmaligen Anlaufs mit `anlass` = `serverstart`
3. die Bilanzzeile mit `verarbeitet` und `fehlgeschlagen` (D-05) — sie IST der Erfolgsnachweis,
   da der Lauf keinen Exit-Code mehr hat
Alle drei Zeilen wörtlich zitieren. Fehlt eine, ist das ein Befund und keine Nebensache.

**(e) crontab (D-03).** Über SSH `crontab -l` ausführen und die vollständige Ausgabe
protokollieren. Zusicherungen:
- kein Eintrag mehr, der `fix-overtime.ts` enthält
- der wöchentliche Eintrag
  `0 2 * * 0 /home/ubuntu/TimeTracking-Staging/server/scripts/sync-prod-to-staging.sh …`
  steht **unverändert** da
Trifft eine der beiden Aussagen nicht zu, brich ab und melde den Befund, statt nachzubessern.

**(f) Dateizeiger (Vorstufe zu D-17).** Rein lesend, wie `.claude/CLAUDE.md` es vorschreibt:
`PID=$(pm2 jlist | jq -r '.[]|select(.name=="timetracking-staging").pid')` und
`ls -l /proc/$PID/fd | grep '\.db'`. Kein Dateizeiger darf `(deleted)` tragen. Ausgabe wörtlich
protokollieren, samt PID und Zeitstempel — dieser Wert ist der Ausgangspunkt der Beobachtung
aus Plan 09.1-08.

**(g) Fachlicher Funktionstest**, wie ihn `.claude/CLAUDE.md` nach jedem Deployment
verlangt: über die Anwendung gegen Port 3001 anmelden und eine Überstundenansicht aufrufen.
Halte fest, welche Ansicht geprüft wurde und was sie zeigte.

**VERBOTEN in jedem Schritt dieses Plans (D-16 und stehende Projektregel):** `production.db`
öffnen, kopieren, mit `sqlite3` oder `better-sqlite3` ansprechen, per `VACUUM INTO` sichern
oder ein Skript mit `DATABASE_PATH=/home/ubuntu/databases/production.db` starten — auch nicht
lesend. Das hat in diesem Projekt bereits zweimal Schaden verursacht
(`.planning/debug/db-stabilisierung-20260818.md`,
`.planning/debug/wal-abgehaengt-20260827.md`). `ls -l /proc/<pid>/fd` und `pm2 logs` sind
keine Datenbankzugriffe und ausdrücklich erlaubt.
  </action>

  <acceptance_criteria>
    - `gh run list --workflow="deploy-staging.yml" --limit 1` zeigt `completed` und `success`;
      die Lauf-ID steht in der SUMMARY.
    - Der Protokollabschnitt „Running database migrations" ist wörtlich zitiert und enthält
      keinen `Cannot find module`- oder `ENOENT`-Fehler.
    - `curl -s http://129.159.8.19:3001/api/health` liefert HTTP 200; der Antwortkörper steht
      wörtlich in der SUMMARY.
    - Drei Protokollzeilen aus `pm2 logs timetracking-staging` sind wörtlich zitiert:
      Scheduler-Start mit `03:15` und `Europe/Berlin`, Anlaufstart mit `serverstart`,
      Bilanzzeile mit `verarbeitet` und `fehlgeschlagen`.
    - Die vollständige Ausgabe von `crontab -l` steht in der SUMMARY;
      `crontab -l | grep -c fix-overtime` liefert `0` und
      `crontab -l | grep -c sync-prod-to-staging` liefert `1`.
    - Die Ausgabe von `ls -l /proc/$PID/fd | grep '\.db'` steht mit PID und Zeitstempel in der
      SUMMARY und enthält kein `(deleted)`.
    - Der fachliche Funktionstest gegen Port 3001 ist mit Ansicht und Ergebnis beschrieben.
    - Die SUMMARY enthält eine ausdrückliche Zeile: „Kein Zugriff auf production.db in diesem
      Plan", belegt durch die vollständige Liste der ausgeführten SSH-Kommandos.
    - `.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-NACHWEIS-STAGING.md`
      existiert und enthält alle oben genannten Messwerte.
  </acceptance_criteria>

  <verify>
    <automated>gh run list --workflow="deploy-staging.yml" --limit 1 && curl -s -o /dev/null -w "%{http_code}" http://129.159.8.19:3001/api/health</automated>
    <human-check>Anmeldung gegen Port 3001 und Aufruf einer Überstundenansicht — die Zahlen sind plausibel und die Ansicht lädt fehlerfrei.</human-check>
  </verify>

  <done>
    Green läuft auf dem Stand dieser Phase; Migrationsschritt, Health-Check, Scheduler-Start,
    Anlauf-Bilanzzeile, crontab und Dateizeiger sind gemessen und wörtlich protokolliert; kein
    Schritt hat die Produktionsdatenbank berührt.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lokaler Arbeitsbaum → `origin/staging` → Green-Server | Ein Push setzt einen fremden Rechner per `git reset --hard` auf diesen Stand. |
| Lokaler Arbeitsbaum → `origin/main` → **Produktionsserver** | Ein versehentlicher Push nach `main` mit `server/**`-Änderungen deployt sofort auf Produktion. |
| Entwicklerrechner → Produktionsdatenbank über SSH | Jeder SSH-Befehl kann eine Datei öffnen. Die stehende Projektregel verbietet das für `production.db` absolut. |
| Deployment → crontab des Servers | Der Bereinigungsschritt schreibt die crontab neu. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-32 | Tampering | Ein Schritt öffnet `production.db` lesend („nur kurz nachsehen") und hängt die WAL des Servers ab | mitigate | D-16 als absolutes Verbot in `<action>`; erlaubte Ersatzwege ausdrücklich benannt (`pm2 logs`, `ls -l /proc/<pid>/fd`, HTTP-API); Abnahmekriterium verlangt die vollständige Liste der ausgeführten SSH-Kommandos in der SUMMARY. Zwei Vorfallsberichte sind als Kontext eingebunden. |
| T-09.1-33 | Tampering | Versehentlicher Push nach `main` statt `staging` löst ein ungeprüftes Produktions-Deployment aus | mitigate | D-14 als Wellenordnung: dieser Plan pusht ausschließlich nach `staging`, und erst nach dem blockierenden Entscheidungs-Checkpoint in Task 2. Der Produktions-Push ist Gegenstand von Plan 09.1-07 mit eigenem Checkpoint. |
| T-09.1-34 | Denial of Service | `git push --force` auf `staging` verwirft einen Stand, den jemand noch braucht | mitigate | `--force-with-lease` statt `--force`; der bisherige Inhalt von `origin/staging` wird vor der Entscheidung gemessen und dem Anwender vorgelegt (Task 2). |
| T-09.1-35 | Denial of Service | Der Migrationsschritt findet nach dem Pfadwechsel null `.sql`-Dateien und meldet still Erfolg | mitigate | In Plan 09.1-03 durch eine Abbruchprüfung in `migrate.ts` abgesichert; hier zusätzlich durch die wörtliche Auswertung des Protokollabschnitts „Running database migrations" auf Green — vor jedem Produktionskontakt. |
| T-09.1-36 | Denial of Service | Der Bereinigungsschritt löscht den wöchentlichen `sync-prod-to-staging`-Eintrag mit | mitigate | Abnahmekriterium: `crontab -l | grep -c sync-prod-to-staging` = 1 nach dem Deployment; bei Abweichung Abbruch und Befundmeldung statt Nachbesserung. |
| T-09.1-37 | Repudiation | Der Nachweis behauptet Wirkung, statt sie zu zeigen | mitigate | Alle Belege sind wörtliche Ausgaben mit Lauf-ID, PID und Zeitstempel; fehlt eine der drei Protokollzeilen, ist das als Befund zu melden. |
| T-09.1-38 | Information Disclosure | `pm2 logs` und Workflow-Protokolle können Nutzerdaten enthalten | mitigate | In die SUMMARY wandern ausschließlich die benannten Zeilen (Scheduler-Start, Anlauf, Bilanz, crontab, Dateizeiger). Die Bilanzzeile trägt nach Plan 09.1-04 nur `userId`, keine Namen. |
| T-09.1-39 | Elevation of Privilege | Der SSH-Schlüssel `.ssh/oracle_server.key` wird kopiert, protokolliert oder in eine Datei geschrieben | mitigate | Der Schlüssel wird ausschließlich per `-i` an `ssh` übergeben; er erscheint in keiner SUMMARY, in keinem Commit und in keiner Protokollzeile. |
| T-09.1-SC | Tampering | npm/pip/cargo-Installationen | mitigate | Dieser Plan installiert lokal KEIN Paket. Der Green-Server führt im Rahmen des bestehenden, unveränderten Workflows `npm install --legacy-peer-deps` aus — dieser Schritt ist Bestandsverhalten und wird von dieser Phase nicht angefasst; `server/package.json` hat außer den Skriptpfaden und dem Eintrag `fix:overtime` keine Änderung erfahren (`git diff` auf den `dependencies`-Block muss leer sein). |
</threat_model>

<verification>
- Vier lokale Gates gefahren, Zahlen und Exitcodes in der SUMMARY
- D-15 je Commit gemessen (drei Zeilen `git merge-base --is-ancestor`)
- Freigabe des Anwenders wörtlich festgehalten
- `gh run list --workflow="deploy-staging.yml"` → `completed`/`success`
- `curl http://129.159.8.19:3001/api/health` → 200
- Drei Protokollzeilen aus Green wörtlich zitiert
- `crontab -l` → 0 Treffer `fix-overtime`, 1 Treffer `sync-prod-to-staging`
- `ls -l /proc/$PID/fd | grep '\.db'` → kein `(deleted)`
- Kein Zugriff auf `production.db` (D-16)
</verification>

<success_criteria>
- Der Stand dieser Phase läuft auf Green (Port 3001) und ist dort in seiner Wirkung belegt
  (D-14)
- Der In-Prozess-Scheduler und der einmalige Anlauf sind am Protokoll nachgewiesen (D-01,
  D-02, D-04, D-05)
- Der crontab-Eintrag für `fix-overtime.ts` ist verschwunden, der fremde Sync-Eintrag steht
  unverändert (D-03)
- D-15 ist je Commit gemessen und die Auslieferungslage benannt
- Kein Schritt hat die Produktionsdatenbank berührt (D-16)
</success_criteria>

<output>
Erstelle `.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-06-SUMMARY.md`
und `.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-NACHWEIS-STAGING.md`
</output>
