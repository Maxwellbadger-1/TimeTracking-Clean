---
phase: 09.1-journal-backfill-und-betriebs-h-rtung
plan: 07
type: execute
wave: 5
depends_on: ["09.1-06"]
files_modified:
  - .planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-NACHWEIS-PRODUKTION.md
  - CHANGELOG.md
  - PROJECT_STATUS.md
  - .claude/CLAUDE.md
autonomous: false
requirements: [WR-02, WR-03, WR-04, WR-05]

must_haves:
  truths:
    - "D-14: Die Produktion (Blue, Port 3000) wird erst nach dem geführten Green-Nachweis und erst nach ausdrücklicher Freigabe des Anwenders angefasst."
    - "D-03: Auf dem Produktionsserver enthält crontab -l keinen fix-overtime-Eintrag mehr; kein manueller Eingriff auf dem Server war nötig."
    - "D-01: Das Produktionsprotokoll belegt den In-Prozess-Scheduler und den einmaligen Anlauf nach Serverstart."
    - "D-15: Der in Plan 09.1-06 gemessene Stand der drei Commits vom 27.08.2026 ist abschließend behandelt — entweder mit ausgeliefert oder als bereits ausgeliefert belegt."
    - "D-16: Kein Schritt öffnet production.db — die Nachweise laufen über den laufenden Serverprozess, pm2 logs und lesende /proc-Prüfungen."
    - "D-17: Die Ausgangswerte der Beobachtung sind aufgenommen: die Dateizeiger des Serverprozesses und die Verzeichnis-mtime von ~/databases/."
  artifacts:
    - path: ".planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-NACHWEIS-PRODUKTION.md"
      provides: "Messprotokoll der Produktionsauslieferung samt Ausgangswerten für die Beobachtung aus D-17"
      min_lines: 60
    - path: "CHANGELOG.md"
      provides: "Eintrag zu Phase 9.1 unter [Unreleased] bzw. der ausgelieferten Version"
    - path: "PROJECT_STATUS.md"
      provides: "ausgelieferter Stand aktualisiert"
    - path: ".claude/CLAUDE.md"
      provides: "Datenbank-Regel 3 auf den ausgelieferten Stand gebracht, Beobachtung als laufend gekennzeichnet"
  key_links:
    - from: "origin/main"
      to: "Produktionsserver /home/ubuntu/TimeTracking-Clean"
      via: "deploy-server.yml, PM2-Prozess timetracking-server auf Port 3000"
      pattern: "timetracking-server"
    - from: "Produktionsprotokoll"
      to: "server/src/services/overtimeRecalcRunner.ts"
      via: "Bilanzzeile mit anlass: serverstart"
      pattern: "serverstart"
---

<objective>
Die Code-Härtung auf die Produktion ausliefern (D-14, zweiter Schritt) und ihre Wirkung dort
belegen, ohne die Produktionsdatenbank von außen anzufassen (D-16).

Purpose: Nach dem Green-Nachweis aus Plan 09.1-06 sind die drei riskanten Stellen dieser Phase
— Migrationspfad, crontab, Startsequenz — an einem laufenden System geprüft. Erst danach ist
die Produktion an der Reihe. `ad48068` (WAL-Prüfpunkt bei SIGTERM/SIGINT) löst nach D-15
ohnehin ein Server-Deployment aus; dieser Vorgang ist der dafür vorgesehene.

Output: `09.1-NACHWEIS-PRODUKTION.md` mit gemessenen Werten, die Ausgangswerte für die
Beobachtung aus D-17, sowie nachgezogene Kerndokumentation.

**Diese Phase enthält keinen Datenlauf.** Der Journal-Backfill über Bestandsdaten ist am
26.08.2026 im Produktionsfenster der Phase 14 gelaufen
(`14-BACKFILL-PRODUKTION.md`, 12 Nutzer / 39 Monate) und gehört nicht mehr zum Umfang. Es gibt
in diesem Plan keinen Schreibzugriff auf `production.db` von außen und keinen zu genehmigenden
Datenlauf — nur ein Deployment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-CONTEXT.md
@.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-06-SUMMARY.md
@.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-NACHWEIS-STAGING.md
@.planning/debug/wal-abgehaengt-20260827.md
@.planning/debug/db-stabilisierung-20260818.md
@.claude/CLAUDE.md
@ENV.md

<interfaces>
<!-- Am Quelltext und am Repository geprüft am 2026-08-29. -->

`.github/workflows/deploy-server.yml`
- Auslöser: `push` auf `main` mit `paths: ['server/**']`, zusätzlich `workflow_dispatch`.
- `script_stop: true`, `set -e` — der Lauf bricht beim ersten Fehler ab.
- `export DATABASE_PATH=/home/ubuntu/databases/production.db` gilt für alle Schritte des
  Deployments; die Sicherung erfolgt per
  `cp /home/ubuntu/databases/production.db /home/ubuntu/databases/backups/production.predeploy_$(date +%Y%m%d_%H%M%S).db`
  **durch den Workflow selbst**, nicht durch den Ausführenden.
- `git fetch origin main` + `git reset --hard origin/main` in `/home/ubuntu/TimeTracking-Clean`.
- PM2-Prozessname `timetracking-server`, Port `3000`.
- Health-Check `curl … http://localhost:3000/api/health`, bei ≠ 200 `pm2 logs` und `exit 1`.
- Danach der Schritt `- name: Verify DB path post-deploy`.

Pflichtprüfungen nach jedem Deployment (`.claude/CLAUDE.md` §Deployment, Anweisung des
Anwenders vom 2026-02-08):
```
gh run list --workflow="deploy-server.yml" --limit 1   # completed + success?
curl -s http://129.159.8.19:3000/api/health | jq       # nach 2-3 Min
# dann Funktionstest: Login, Zeitbuchung, Überstunden
```

Lesende Prüfung der Dateizeiger (`.claude/CLAUDE.md` §Datenbank-Regeln 3) — **kein**
Datenbankzugriff und ausdrücklich vorgeschrieben:
```
PID=$(pm2 jlist | jq -r '.[]|select(.name=="timetracking-server").pid')
ls -l /proc/$PID/fd | grep production.db     # steht "(deleted)" dabei -> STOPP
```

Die beiden Messgrößen aus D-17 (Herkunft: Befund vom 28.08.2026 im ROADMAP-Nachtrag):
1. die Dateizeiger des Serverprozesses tragen kein `(deleted)`
2. die Verzeichnis-mtime von `~/databases/` trägt keinen 03:00-Stempel mehr
   (`stat -c '%y %n' /home/ubuntu/databases`)

`.claude/CLAUDE.md` §Datenbank-Regeln 3 enthält heute den Satz „**Das passiert jede Nacht.**
Behebung ist Umfang der Phase 9.1 (WR-05/WR-03)." — dieser Satz ist nach der Auslieferung
nicht mehr der Stand und wird in Task 3 nachgezogen.

SSH-Zugang: `ssh -i .ssh/oracle_server.key ubuntu@129.159.8.19` (`ENV.md`).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Freigabe für die Produktionsauslieferung (D-14)</name>
  <files>keine Datei geändert — Freigabevorlage an den Anwender</files>

  <action>
Lege dem Anwender die sieben unter <how-to-verify> genannten Belege wörtlich vor, benenne ausdrücklich, was der Push nach main auslöst und was NICHT geschieht, und halte die Freigabe mit Datum und Wortlaut fest. Ohne Freigabe wird weder gepusht noch ein workflow_dispatch ausgelöst.
  </action>

  <what-built>
Die gesamte Code-Härtung der Phase 9.1 ist umgesetzt und auf Green (Port 3001) ausgeliefert
und gemessen:
- WR-05: Der Nachtlauf läuft im Serverprozess über die geteilte Verbindung, geplant auf 03:15
  Europe/Berlin, mit Fehlerisolierung je Nutzer und Bilanzzeile (D-01, D-02, D-04, D-05)
- WR-05: Beide externen crontab-Einträge und beide Post-Deploy-Aufrufe sind aus den Workflows
  entfernt; das Skript bleibt als gehärtetes Handbetriebs-Werkzeug erhalten (D-03, D-06)
- WR-03: `PRAGMA busy_timeout = 5000` zentral gesetzt, mit echtem Nebenläufigkeitstest und
  Gegenprobe belegt (D-07, D-08)
- WR-04: `server/scripts/` enthält keine TypeScript-Datei mehr; die fünf aktiven Skripte liegen
  typisiert unter `server/src/scripts/`, 13 Legacy-Skripte sind entfernt (D-09, D-10, D-11)
- WR-02: Tests für `handleAbsenceDay()`/`updateOvertimeBalanceForMonth()` mit isoliertem
  `overtime_comp`-Tag und für `ensureOvertimeBalanceEntries()` (D-12, D-13)

Der Green-Nachweis liegt in `09.1-NACHWEIS-STAGING.md`.
  </what-built>

  <how-to-verify>
Lege dem Anwender vor der Freigabe **wörtlich** vor — nicht zusammengefasst, sondern als
Zitat aus `09.1-NACHWEIS-STAGING.md`:

1. Die vier lokalen Gate-Ergebnisse mit Zahlen (`tsc` Server, `tsc` Desktop, `vitest run`
   grün/rot, ggf. `check:rules`) und die Zuordnung jedes roten Tests als vorbestehend.
2. Die Lauf-ID des Green-Deployments und den Status `success`.
3. Den Protokollabschnitt „Running database migrations" von Green — der Beleg, dass
   `npm run migrate:prod` über den neuen Pfad läuft und die SQL-Migrationen findet. **Das ist
   der Punkt, an dem ein Produktions-Deployment scheitern würde.**
4. Die drei Protokollzeilen von Green: Scheduler-Start mit `03:15`/`Europe/Berlin`,
   Anlaufstart mit `anlass: serverstart`, Bilanzzeile mit `verarbeitet`/`fehlgeschlagen`.
5. Die Ausgabe von `crontab -l`: kein `fix-overtime`, aber der wöchentliche
   `sync-prod-to-staging`-Eintrag steht noch.
6. Das Ergebnis des fachlichen Funktionstests gegen Port 3001.
7. Die D-15-Messung je Commit (`ad48068`, `ded14d3`, `c0acb59`).

Nenne ausdrücklich, was der Push nach `main` auslöst: `deploy-server.yml` startet, sichert die
Produktionsdatenbank nach `~/databases/backups/production.predeploy_<zeitstempel>.db`, setzt
den Server per `git reset --hard origin/main` auf diesen Stand, baut, migriert, startet PM2 neu
und prüft die Gesundheit. Ein PM2-Neustart ist dabei unbedenklich, **solange kein Dateizeiger
`(deleted)` trägt** — nenne den auf Green gemessenen Ausgangswert und weise darauf hin, dass
derselbe Wert auf Blue **vor** dem Push zu prüfen ist (siehe Task 2 (a)).

Nenne auch, was NICHT geschieht: kein Datenlauf, kein Backfill, kein Schreibzugriff von außen
auf `production.db` (D-16).
  </how-to-verify>

  <read_first>
    - `.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-NACHWEIS-STAGING.md`
      (die Grundlage der Vorlage — vollständig)
    - `.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-06-SUMMARY.md`
    - `.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-CONTEXT.md`, D-14/D-16
    - `.claude/CLAUDE.md` §Deployment und §Datenbank-Regeln 3
  </read_first>

  <acceptance_criteria>
    - Alle sieben Punkte sind dem Anwender wörtlich vorgelegt worden, nicht zusammengefasst.
    - Die Freigabe des Anwenders ist mit Datum und Wortlaut in der SUMMARY festgehalten.
    - Ohne Freigabe wird nicht nach `main` gepusht und kein `workflow_dispatch` ausgelöst.
  </acceptance_criteria>

  <resume-signal>Antworte „freigegeben", um die Produktionsauslieferung zu starten — oder beschreibe, was vorher zu klären ist.</resume-signal>

  <verify>
    <human-check>Alle sieben Belege aus dem Green-Nachweis wurden wörtlich vorgelegt; der Anwender hat die Produktionsauslieferung ausdrücklich freigegeben.</human-check>
  </verify>

  <done>
    Die Freigabe liegt mit Datum und Wortlaut vor; ohne sie wurde weder nach main gepusht noch ein workflow_dispatch ausgelöst.
  </done>
</task>

<task type="auto">
  <name>Task 2: Produktion ausliefern und die Wirkung messen (D-01, D-03, D-14, D-16, D-17)</name>
  <files>.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-NACHWEIS-PRODUKTION.md (neu)</files>

  <read_first>
    - `.github/workflows/deploy-server.yml` nach Plan 09.1-05 (der Ablauf, dessen Schritte im
      Protokoll wiederzufinden sind)
    - `server/src/services/cronService.ts` und `server/src/server.ts` nach Plan 09.1-04 (die
      exakten Log-Zeichenketten, nach denen zu suchen ist)
    - `.claude/CLAUDE.md` §Deployment (die drei Pflichtprüfungen) und §Datenbank-Regeln 3 (die
      lesende Dateizeigerprüfung und das absolute Öffnungsverbot)
    - `.planning/debug/wal-abgehaengt-20260827.md` (was zu tun ist, falls doch ein Dateizeiger
      `(deleted)` trägt — Prüfpunkt über die App erzwingen, NICHT neu starten)
    - `ENV.md` (SSH, Ports, Verzeichnisse)
  </read_first>

  <action>
**(a) Vorprüfung VOR dem Push — sie entscheidet, ob überhaupt deployt werden darf.** Über SSH,
rein lesend:
vier Abfragen in dieser Reihenfolge: die PID des PM2-Prozesses `timetracking-server` über
`pm2 jlist` und `jq -r '.[]|select(.name=="timetracking-server").pid'` ermitteln; damit
`ls -l /proc/$PID/fd | grep production.db` ausführen; danach
`stat -c '%y %n' /home/ubuntu/databases`; zuletzt `crontab -l`.
Trägt ein Dateizeiger `(deleted)`: **STOPP, nicht deployen.** Das Deployment startet PM2 neu
und würde den seit dem letzten Prüfpunkt geschriebenen Stand vernichten. Der einzige Weg, der
eine abgehängte WAL noch rettet, ist ein Backup über die App
(`createBackup()` setzt `wal_checkpoint(TRUNCATE)` auf der eigenen Verbindung des Servers).
Bitte in diesem Fall den Anwender darum und prüfe danach erneut; erst wenn kein `(deleted)`
mehr dasteht, geht es weiter. Protokolliere alle vier Ausgaben mit Zeitstempel — sie sind
zugleich der **Ausgangswert für D-17**.

**(b) Auslieferung.** Nach der Freigabe aus Task 1: `git push origin main`. Der Push löst
`deploy-server.yml` aus. Beobachte:
`gh run list --workflow="deploy-server.yml" --limit 1` bis `completed` und `success`. Bei
Fehlschlag `gh run view <run-id> --log-failed`, den Grund benennen und ihn nach
`CHANGELOG.md` unter `[Unreleased]` schreiben, wie es `.claude/CLAUDE.md` §Deployment
vorschreibt — nicht blind wiederholen.

**(c) Der kritische Schritt.** Hole den Protokollabschnitt „Running database migrations"
wörtlich aus dem Lauf. Er muss zeigen, dass `npm run migrate:prod` über
`src/scripts/migrate.ts` läuft, keinen Modulauflösungsfehler wirft und die SQL-Migrationen
findet. Ebenso „Validating database schema" und den Schritt „Verify DB path post-deploy".
Halte außerdem den Dateinamen der vom Workflow selbst angelegten Vorab-Sicherung fest
(`production.predeploy_<zeitstempel>.db`) — sie entsteht durch den Workflow, nicht durch dich.

**(d) Pflichtprüfungen nach dem Deployment** (`.claude/CLAUDE.md` §Deployment):
- `gh run list --workflow="deploy-server.yml" --limit 1` → `completed` + `success`
- nach 2-3 Minuten `curl -s http://129.159.8.19:3000/api/health | jq` → HTTP 200, Körper
  wörtlich protokollieren
- Funktionstest über die Anwendung: Anmeldung, eine Zeitbuchung ansehen, die
  Überstundenansicht aufrufen. Halte fest, was geprüft wurde und was zu sehen war.

**(e) Der In-Prozess-Scheduler (D-01, D-02, D-04, D-05).** `pm2 logs timetracking-server
--lines 300 --nostream` und drei Zeilen wörtlich zitieren: Scheduler-Start mit `03:15` und
`Europe/Berlin`, Anlaufstart mit `anlass` = `serverstart`, Bilanzzeile mit `verarbeitet` und
`fehlgeschlagen`. Ist `fehlgeschlagen > 0`, nenne jede Fehlerzeile mit `userId` — das ist ein
Befund, kein Nebengeräusch.

**(f) crontab (D-03).** `crontab -l` nach dem Deployment vollständig protokollieren.
Zusicherungen: kein Eintrag mit `fix-overtime.ts`; der wöchentliche
`sync-prod-to-staging.sh`-Eintrag steht unverändert. D-03 verlangt ausdrücklich, dass dafür
**kein manueller Eingriff auf dem Server** nötig ist — der Bereinigungsfilter des Deployments
erledigt es. Führe deshalb selbst **keinen** `crontab`-Schreibbefehl aus. Steht der Eintrag
danach noch da, ist das ein Befund für die SUMMARY.

**(g) Ausgangswerte für D-17 nach dem Deployment.** Dieselben Messungen wie in (a), nun am
frisch gestarteten Prozess:
die PID des neu gestarteten Prozesses erneut über `pm2 jlist` und `jq` ermitteln, dann
`ls -l /proc/$PID/fd | grep production.db`, dann `stat -c '%y %n' /home/ubuntu/databases`,
zuletzt `date -u` und `date` für den Zeitstempel in beiden Zeitzonen.
Protokolliere PID, Zeitstempel und beide Ausgaben in
`09.1-NACHWEIS-PRODUKTION.md` unter einer eigenen Überschrift „Ausgangswerte der Beobachtung
(D-17)". Plan 09.1-08 misst gegen genau diese Werte.

**VERBOTEN in jedem Schritt (D-16 und stehende Projektregel):** `production.db` öffnen,
kopieren, mit `sqlite3` oder `better-sqlite3` ansprechen, per `VACUUM INTO` sichern oder ein
Skript mit `DATABASE_PATH=/home/ubuntu/databases/production.db` starten — auch nicht lesend,
auch nicht „nur kurz". Das hat in diesem Projekt zweimal Schaden verursacht. `pm2 logs`,
`pm2 jlist`, `ls -l /proc/<pid>/fd`, `stat` auf ein Verzeichnis, `crontab -l` und die HTTP-API
sind keine Datenbankzugriffe und ausdrücklich erlaubt.
  </action>

  <acceptance_criteria>
    - Die Vorprüfung aus (a) ist mit allen vier Ausgaben und Zeitstempel protokolliert; kein
      Dateizeiger trug `(deleted)`, oder das Vorgehen bei `(deleted)` ist dokumentiert.
    - `gh run list --workflow="deploy-server.yml" --limit 1` zeigt `completed` und `success`;
      die Lauf-ID steht in der SUMMARY.
    - Der Protokollabschnitt „Running database migrations" ist wörtlich zitiert und enthält
      keinen `Cannot find module`- oder `ENOENT`-Fehler.
    - Der Dateiname der vom Workflow angelegten Vorab-Sicherung
      `production.predeploy_<zeitstempel>.db` steht in der SUMMARY.
    - `curl -s http://129.159.8.19:3000/api/health` liefert HTTP 200; der Körper steht wörtlich
      in der SUMMARY.
    - Drei Protokollzeilen aus `pm2 logs timetracking-server` sind wörtlich zitiert
      (Scheduler-Start `03:15`/`Europe/Berlin`, Anlauf `serverstart`, Bilanzzeile).
    - `crontab -l | grep -c fix-overtime` liefert `0`;
      `crontab -l | grep -c sync-prod-to-staging` liefert `1`; die vollständige Ausgabe steht
      in der SUMMARY.
    - Es wurde kein `crontab`-Schreibbefehl ausgeführt — die Liste aller SSH-Kommandos in der
      SUMMARY belegt das (D-03: kein manueller Eingriff vorgesehen).
    - `09.1-NACHWEIS-PRODUKTION.md` enthält den Abschnitt „Ausgangswerte der Beobachtung
      (D-17)" mit PID, `ls -l /proc/$PID/fd`-Ausgabe und
      `stat -c '%y %n' /home/ubuntu/databases`.
    - Die SUMMARY enthält die vollständige Liste der ausgeführten SSH-Kommandos und die Zeile
      „Kein Zugriff auf production.db in diesem Plan" (D-16).
    - Der fachliche Funktionstest (Anmeldung, Zeitbuchung, Überstunden) ist mit Ergebnis
      beschrieben.
  </acceptance_criteria>

  <verify>
    <automated>gh run list --workflow="deploy-server.yml" --limit 1 && curl -s -o /dev/null -w "%{http_code}" http://129.159.8.19:3000/api/health</automated>
    <human-check>Anmeldung an der Produktion, Zeitbuchung und Überstundenansicht geprüft — die Zahlen sind unverändert plausibel und die Ansichten laden fehlerfrei.</human-check>
  </verify>

  <done>
    Die Produktion läuft auf dem Stand dieser Phase; Deployment, Migrationsschritt,
    Health-Check, Scheduler, Anlauf-Bilanz, crontab und Dateizeiger sind gemessen und wörtlich
    protokolliert; die Ausgangswerte für die Beobachtung aus D-17 sind aufgenommen; kein
    Schritt hat die Produktionsdatenbank geöffnet.
  </done>
</task>

<task type="auto">
  <name>Task 3: D-15 abschließen und die Kerndokumentation nachziehen</name>
  <files>CHANGELOG.md, PROJECT_STATUS.md, .claude/CLAUDE.md</files>

  <read_first>
    - `.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-06-SUMMARY.md`
      (die D-15-Messung je Commit — sie bestimmt, was hier noch zu tun ist)
    - `CHANGELOG.md` (die Struktur, insbesondere der Abschnitt `[Unreleased]`)
    - `PROJECT_STATUS.md` (die Abschnitte zum ausgelieferten Stand und zu bekannten Defekten)
    - `.claude/CLAUDE.md` §Datenbank-Regeln 3 (der Absatz „Die Ursache ist der Nachtlauf …
      Behebung ist Umfang der Phase 9.1 (WR-05/WR-03)" — genau dieser Text ist nachzuziehen)
    - `.claude/CLAUDE.md` §Release (die Schrittfolge, falls ein Desktop-Release nötig ist)
  </read_first>

  <action>
**(a) D-15 abschließen.** Werte die Messung aus Plan 09.1-06 aus und handle je Commit:
- `ad48068` (WAL-Prüfpunkt bei SIGTERM/SIGINT): steht er bereits in `origin/main`, ist er mit
  dem Deployment aus Task 2 ohnehin auf dem Server. Belege das mit
  `git merge-base --is-ancestor ad48068 origin/main; echo $?` = `0` und mit dem Vorhandensein
  der Log-Zeile `🛡️  Signalbehandlung aktiv (SIGTERM/SIGINT)` im Produktionsprotokoll aus
  Task 2 (e). Steht er noch nicht drin: er ist mit dem Push aus Task 2 mitgegangen — belege es
  ebenso.
- `ded14d3` (tauriHttpClient binärsicher): dieser Commit braucht nach D-15 ein
  **Desktop-Release**, kein Server-Deployment. Prüfe mit `gh release list --limit 5` und
  `git merge-base --is-ancestor ded14d3 <tag des jüngsten Releases>; echo $?`, ob er in einem
  bereits veröffentlichten Release enthalten ist. **Ist er enthalten** (nach dem Stand vom
  29.08.2026 ist `v1.9.1` vom 28.08.2026 der Kandidat): halte das mit Tag und
  Veröffentlichungsdatum fest — D-15 ist für diesen Commit erfüllt, ein weiteres Release ist
  **nicht** vorzunehmen. **Ist er nicht enthalten:** halte fest, dass ein Desktop-Release
  aussteht, lege es dem Anwender als eigenständige Entscheidung vor und führe es **nicht**
  eigenmächtig durch — ein Versionssprung berührt drei Dateien und einen Tag und ist ein
  Auslieferungsvorgang eigener Art (so benannt in `09.1-CONTEXT.md` unter „Deferred Ideas").
- `c0acb59` (Doku): genügt der Nachweis, dass er in `origin/main` steht.

Schreibe für jeden der drei Commits eine Zeile mit Hash, Messergebnis und Schlussfolgerung in
die SUMMARY.

**(b) `CHANGELOG.md`.** Ergänze unter `[Unreleased]` (oder dem passenden Versionsabschnitt,
je nach vorhandener Struktur — lies sie, statt sie anzunehmen) einen Eintrag zu Phase 9.1 mit
vier Punkten: Nachtlauf in den Serverprozess verlagert und Fehlerisolierung je Nutzer (WR-05),
`busy_timeout = 5000` zentral gesetzt (WR-03), `server/scripts/` aufgelöst und typisiert
(WR-04), Tests für den Rebuild-Weg und `ensureOvertimeBalanceEntries()` (WR-02). Nenne die
Ursache der abgehängten WAL ausdrücklich als behoben-mit-laufender-Beobachtung, nicht als
abschließend belegt — der Nachweis ist Gegenstand von Plan 09.1-08.

**(c) `PROJECT_STATUS.md`.** Ziehe den ausgelieferten Stand nach (Server-Commit, Datum) und
entferne oder korrigiere Einträge, die den täglichen externen Cron als bestehend beschreiben.
Lies den Abschnitt „bekannte Defekte", bevor du etwas änderst.

**(d) `.claude/CLAUDE.md` §Datenbank-Regeln 3 — Zwischenstand, noch nicht Endfassung.**
Ersetze die beiden Sätze „**Das passiert jede Nacht.** Behebung ist Umfang der Phase 9.1
(WR-05/WR-03)." durch eine Formulierung, die den ausgelieferten Stand wiedergibt: Der externe
Nachtlauf ist mit Phase 9.1 entfallen; die Neuberechnung läuft seit `<Datum>` im
Serverprozess um 03:15 Europe/Berlin über dieselbe Verbindung; **der Nachweis über zwei Nächte
läuft noch** und wird in `09.1-NACHWEIS-BEOBACHTUNG.md` (Plan 09.1-08) geführt.
Die Prüfanweisung mit `ls -l /proc/$PID/fd` und der Hinweis auf `createBackup()` bleiben
unverändert stehen — sie gelten unabhängig von der Ursache weiter. Der Abschnitt
„🛑 Produktionsdatenbank: niemals aus einem eigenen Prozess öffnen" bleibt **unangetastet**.

Ändere in allen drei Dokumenten nichts, was nicht durch diese Phase belegt ist. Wo eine
Aussage noch offen ist, schreibe sie als offen — nicht als erledigt.
  </action>

  <acceptance_criteria>
    - Die SUMMARY enthält für `ad48068`, `ded14d3` und `c0acb59` je eine Zeile mit Hash,
      Messergebnis und Schlussfolgerung zu D-15.
    - Ist `ded14d3` nicht in einem veröffentlichten Release enthalten, ist dem Anwender ein
      Desktop-Release als eigene Entscheidung vorgelegt worden und **kein** Versionssprung
      eigenmächtig durchgeführt (`git diff desktop/package.json desktop/src-tauri/Cargo.toml
      desktop/src-tauri/tauri.conf.json` ist leer).
    - `git diff CHANGELOG.md` zeigt ausschließlich Einfügungen, keine gelöschte Zeile in
      bestehenden Versionsabschnitten.
    - `grep -c "Behebung ist Umfang der Phase 9.1" .claude/CLAUDE.md` liefert `0`.
    - `grep -c "niemals aus einem eigenen Prozess öffnen" .claude/CLAUDE.md` liefert weiterhin
      ≥ 1, und `git diff .claude/CLAUDE.md` zeigt in diesem Abschnitt keine Änderung.
    - `grep -c "09.1-NACHWEIS-BEOBACHTUNG" .claude/CLAUDE.md` liefert ≥ 1 (die laufende
      Beobachtung ist als offen gekennzeichnet).
    - `git status --porcelain` ist nach dem Dokumentations-Commit leer.
    - Die Dokumentationsänderungen liegen in einem eigenen Commit, getrennt von den
      Codeänderungen.
  </acceptance_criteria>

  <verify>
    <automated>grep -c "Behebung ist Umfang der Phase 9.1" .claude/CLAUDE.md; grep -c "09.1-NACHWEIS-BEOBACHTUNG" .claude/CLAUDE.md; git status --porcelain</automated>
  </verify>

  <done>
    D-15 ist je Commit abschließend beurteilt und ein etwaiges Desktop-Release dem Anwender
    vorgelegt statt eigenmächtig ausgeführt; CHANGELOG, PROJECT_STATUS und die
    Datenbank-Regel 3 in CLAUDE.md geben den ausgelieferten Stand wieder und weisen die
    Beobachtung aus D-17 ausdrücklich als noch laufend aus.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `origin/main` → Produktionsserver | Ein Push setzt den Produktionsserver per `git reset --hard` auf diesen Stand und startet PM2 neu. |
| Entwicklerrechner → Produktionsdatenbank über SSH | Jeder SSH-Befehl kann eine Datei öffnen. Für `production.db` ist das absolut verboten. |
| Deployment → crontab des Produktionsservers | Der Bereinigungsschritt schreibt die crontab neu. |
| Dokumentation → künftige Sitzungen | `.claude/CLAUDE.md` steuert das Verhalten aller künftigen Arbeiten am Projekt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-40 | Tampering | PM2-Neustart bei bereits abgehängter WAL vernichtet den Schreibstand seit dem letzten Prüfpunkt | mitigate | Vorprüfung in Task 2 (a) VOR dem Push: `ls -l /proc/$PID/fd \| grep production.db`; bei `(deleted)` STOPP, Prüfpunkt über die App erzwingen lassen, erneut prüfen. Genau dieser Ablauf steht in `.claude/CLAUDE.md` §Datenbank-Regeln 3 und in `wal-abgehaengt-20260827.md`. |
| T-09.1-41 | Tampering | Ein Schritt öffnet `production.db` „nur lesend" und hängt die WAL ab | mitigate | D-16 als absolutes Verbot; erlaubte Ersatzwege benannt; Abnahmekriterium verlangt die vollständige Liste aller SSH-Kommandos in der SUMMARY. |
| T-09.1-42 | Denial of Service | Der Migrationsschritt scheitert am neuen Pfad und bricht das Deployment ab (`script_stop: true`, `set -e`) | mitigate | Derselbe Schritt ist auf Green bereits erfolgreich gelaufen und wörtlich belegt (Plan 09.1-06); zusätzlich die Abbruchprüfung in `migrate.ts` (Plan 09.1-03). Bei Fehlschlag: `gh run view --log-failed`, Grund benennen, `CHANGELOG.md` unter `[Unreleased]` eintragen — kein blindes Wiederholen. |
| T-09.1-43 | Denial of Service | Der Bereinigungsschritt löscht den fremden crontab-Eintrag mit | mitigate | Abnahmekriterium `crontab -l \| grep -c sync-prod-to-staging` = 1; Filter ist eng auf `fix-overtime.ts` gefasst (Plan 09.1-05). |
| T-09.1-44 | Denial of Service | Nach dem Deployment läuft weder der externe Cron noch der In-Prozess-Scheduler; die Überstunden werden nicht mehr neu berechnet | mitigate | Task 2 (e) verlangt drei wörtliche Protokollzeilen als Beleg; fehlt eine, ist das ein Befund und die Phase gilt nicht als ausgeliefert. |
| T-09.1-45 | Elevation of Privilege | Ein manueller `crontab`-Schreibbefehl auf dem Produktionsserver ersetzt den vorgesehenen automatischen Weg | mitigate | D-03: kein manueller Eingriff vorgesehen. Abnahmekriterium: die Liste der SSH-Kommandos enthält keinen `crontab -`-Schreibaufruf. |
| T-09.1-46 | Repudiation | Die Dokumentation meldet die WAL-Ursache als abschließend behoben, bevor der Nachweis geführt ist | mitigate | D-17: Task 3 (d) verlangt ausdrücklich eine Zwischenstandsformulierung mit Verweis auf die laufende Beobachtung; Abnahmekriterium prüft per `grep`, dass `09.1-NACHWEIS-BEOBACHTUNG` in `.claude/CLAUDE.md` genannt ist. |
| T-09.1-47 | Information Disclosure | Der SSH-Schlüssel oder `SESSION_SECRET` gelangen in eine SUMMARY oder einen Commit | mitigate | Der Schlüssel wird ausschließlich per `-i` übergeben; aus dem Workflow-Protokoll werden nur die benannten Abschnitte zitiert, nicht der Abschnitt „Setting up environment"; `.env`-Inhalte werden nicht gelesen. |
| T-09.1-SC | Tampering | npm/pip/cargo-Installationen | mitigate | Dieser Plan installiert lokal KEIN Paket. Das Deployment führt im Rahmen des bestehenden, in dieser Phase nicht geänderten Workflows `npm install --legacy-peer-deps` auf dem Server aus; `git diff` auf den `dependencies`- und `devDependencies`-Block von `server/package.json` muss über die gesamte Phase leer sein — als Abnahmekriterium in Plan 09.1-06 Task 1 bereits erhoben. |
</threat_model>

<verification>
- Freigabe des Anwenders wörtlich festgehalten (D-14)
- Vorprüfung der Dateizeiger VOR dem Push protokolliert
- `gh run list --workflow="deploy-server.yml"` → `completed`/`success`
- Protokollabschnitt „Running database migrations" wörtlich, fehlerfrei
- `curl http://129.159.8.19:3000/api/health` → 200
- Drei Protokollzeilen aus der Produktion wörtlich zitiert
- `crontab -l` → 0 Treffer `fix-overtime`, 1 Treffer `sync-prod-to-staging`, kein
  Schreibbefehl ausgeführt
- Ausgangswerte für D-17 aufgenommen (Dateizeiger, Verzeichnis-mtime, PID, Zeitstempel)
- D-15 je Commit beurteilt
- CHANGELOG, PROJECT_STATUS und CLAUDE.md nachgezogen, Beobachtung als offen gekennzeichnet
- Kein Zugriff auf `production.db` (D-16)
</verification>

<success_criteria>
- Die Produktion läuft auf dem Stand dieser Phase, nach Green und nach ausdrücklicher
  Freigabe (D-14)
- Der externe crontab-Eintrag ist ohne manuellen Servereingriff verschwunden (D-03)
- Der In-Prozess-Scheduler und der einmalige Anlauf sind am Produktionsprotokoll belegt
  (D-01, D-02, D-04, D-05)
- D-15 ist je Commit abschließend beurteilt; ein etwaiges Desktop-Release ist dem Anwender
  vorgelegt statt eigenmächtig ausgeführt
- Die Ausgangswerte für die Beobachtung aus D-17 sind aufgenommen
- Kein Schritt hat die Produktionsdatenbank geöffnet (D-16)
</success_criteria>

<output>
Erstelle `.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-07-SUMMARY.md`
und `.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-NACHWEIS-PRODUKTION.md`
</output>
