---
phase: 09.1-journal-backfill-und-betriebs-h-rtung
plan: 05
type: execute
wave: 3
depends_on: ["09.1-03", "09.1-04"]
files_modified:
  - server/src/scripts/fixOvertime.ts
  - server/scripts/fix-overtime.ts
  - server/package.json
  - .github/workflows/deploy-server.yml
  - .github/workflows/deploy-staging.yml
autonomous: true
requirements: [WR-04, WR-05]

must_haves:
  truths:
    - "D-06: Das Werkzeug bleibt für den Handbetrieb erhalten, verlässt aber jeden automatischen Pfad — es wird von keinem Workflow und von keinem crontab-Eintrag mehr aufgerufen."
    - "D-06: Ohne --allow-production verweigert das Werkzeug die Arbeit gegen die Produktionsdatenbank; der Guard läuft VOR jedem Import eines datenbankberührenden Moduls."
    - "D-06: db.close() steht in einem finally-Block und läuft damit auch im Fehlerpfad."
    - "D-03: Beide crontab-Einträge sind entfernt — Blue in deploy-server.yml und Green in deploy-staging.yml; der Bereinigungsfilter bleibt stehen, damit sich der Server beim nächsten Deployment selbst aufräumt."
    - "D-04: Die beiden Post-Deploy-Aufrufe in deploy-server.yml und deploy-staging.yml sind entfernt; der In-Prozess-Anlauf aus Plan 09.1-04 tritt an ihre Stelle."
    - "D-11: server/package.json ruft das Werkzeug unter src/scripts/ auf; nach diesem Plan enthält server/scripts/ keine TypeScript-Datei mehr."
    - "D-16: Kein Schritt dieses Plans öffnet production.db."
  artifacts:
    - path: "server/src/scripts/fixOvertime.ts"
      provides: "gehärtetes Handbetriebs-Werkzeug mit Produktionsschutz, --allow-production, --userId und db.close() im finally"
      min_lines: 70
    - path: ".github/workflows/deploy-server.yml"
      provides: "Deployment ohne crontab-Neueintrag und ohne Post-Deploy-Lauf, mit Bereinigungsfilter"
    - path: ".github/workflows/deploy-staging.yml"
      provides: "dasselbe für Green"
    - path: "server/package.json"
      provides: "npm-Skript fix:overtime auf src/scripts/fixOvertime.ts"
      contains: "tsx src/scripts/fixOvertime.ts"
  key_links:
    - from: "server/src/scripts/fixOvertime.ts"
      to: "server/src/services/overtimeRecalcRunner.ts"
      via: "await import('../services/overtimeRecalcRunner.js') NACH dem Produktionsschutz"
      pattern: "overtimeRecalcRunner"
    - from: "server/src/scripts/fixOvertime.ts"
      to: "server/src/scripts/productionGuard.ts"
      via: "assertNotProduction(resolvedPath) vor jedem datenbankberührenden Import"
      pattern: "assertNotProduction"
---

<objective>
WR-05 abschließen und WR-04 vollständig machen: Das Skript verlässt jeden automatischen Pfad,
bleibt aber als gehärtetes Werkzeug für den Handbetrieb erhalten (D-06). Beide externen
crontab-Einträge und beide Post-Deploy-Aufrufe entfallen (D-03, D-04). Damit ist der
Serverprozess der einzige Schreiber auf `production.db`.

Purpose: Der In-Prozess-Scheduler aus Plan 09.1-04 existiert bereits. Erst jetzt darf der
externe Lauf verschwinden — die umgekehrte Reihenfolge hinterließe ein Fenster ohne
Neuberechnung.

Output: `server/src/scripts/fixOvertime.ts` als gehärtetes Werkzeug,
`server/scripts/fix-overtime.ts` gelöscht, ein npm-Skript, zwei bereinigte Deploy-Workflows.

**Am Quelltext geprüfte Korrektur zur Musterkarte (2026-08-29):** `09.1-PATTERNS.md` verlangt,
den crontab-Block „komplett inklusive `CRON_COMMAND` und dem erneuten `crontab -`-Aufruf"
zu entfernen. Das widerspricht D-06 des Kontexts nicht, wohl aber **D-03**: dort steht
ausdrücklich, dass die vorhandene Bereinigung `crontab -l | grep -v "fix-overtime.ts"` weiter
wirken soll, damit „sich der Server beim nächsten Deployment selbst aufräumt" und „ein
manueller Eingriff auf dem Server nicht nötig ist und nicht vorgesehen". Entfiele auch der
`crontab -`-Aufruf, bliebe der alte Eintrag auf beiden Servern stehen und liefe täglich um
03:00 ins Leere. **Maßgeblich ist D-03:** Der Block wird auf einen reinen Bereinigungsschritt
reduziert, nicht ersatzlos gestrichen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-CONTEXT.md
@.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-PATTERNS.md
@.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-04-SUMMARY.md
@.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-03-SUMMARY.md
@.claude/CLAUDE.md

<interfaces>
<!-- Am Quelltext geprüft am 2026-08-29. -->

`server/scripts/fix-overtime.ts` (99 Zeilen, Ist-Zustand):
- `:7` `import { databaseConfig } from '../dist/config/database.js';` — Import aus `dist/`,
  faktisch `any`; das ist der Kern von WR-04 für diese Datei
- `:13` `import { getCurrentMonth } from '../dist/utils/timezone.js';`
- `:36-37` `import { ensureOvertimeBalanceEntries } from '../dist/services/overtimeService.js';`
  und `import { db } from '../dist/database/connection.js';` — **statische** Importe; sie
  lösen beim bloßen Modulladen `initializeConnection()` und damit die vollständige
  Schema-Initialisierung aus (`09-REVIEW.md:211-222`)
- `:45-49` Nutzerabfrage auf **Modulebene**, nicht in `main()`
- `:55-87` die Schleife ohne Absicherung
- `:93` `db.close()` nur im Erfolgspfad
- `:96-99` `main().catch(...)` → `process.exit(1)`, ohne `db.close()`
- `:15-35` ein langer Kommentarblock zu REQ-17/A-1 — sein fachlicher Kern (kanonische Funktion
  statt eigener Kopie) gilt weiter und ist zu übernehmen

`server/src/scripts/productionGuard.ts:42-62`
`export function assertNotProduction(explicitPath?: string): void` — vergleicht
`path.resolve(explicitPath ?? getDatabasePath())` mit `path.resolve(getProductionDatabasePath())`,
prüft zusätzlich `NODE_ENV === 'production'` und einen Substring-Rückfall auf `production`,
und beendet mit `process.exit(2)`. Der Kopfkommentar `:6-15` erklärt, warum der Aufruf
**synchron und VOR jedem `import`/`await import()` eines schreibenden Moduls** stehen muss:
`connection.ts` öffnet die Datei bereits beim Modulimport und legt das Zielverzeichnis vorher
per `mkdirSync` an.

`server/src/scripts/backfillOvertimeJournal.ts:300-345` — die verbindliche Guard-Reihenfolge:
1. `DATABASE_PATH` fehlt oder leer → `console.error` + `process.exit(2)`
2. `parseArgs(process.argv.slice(2))`
3. `const resolvedPath = path.resolve(getDatabasePath());`
4. erste Ausgabezeile: `console.log('Aufgelöster Datenbankpfad:', resolvedPath);`
5. `if (args.allowProduction) { console.log('### WARNUNG: --allow-production gesetzt — Produktionsschutz UMGANGEN. ###'); } else { assertNotProduction(resolvedPath); }`
6. **erst danach** `const { db } = await import('../database/connection.js');` usw.

`server/src/services/overtimeRecalcRunner.ts` (aus Plan 09.1-04):
`runOvertimeRecalcForAllUsers(options: { anlass: 'nachtlauf' | 'serverstart' | 'handbetrieb'; userIds?: number[] }): Promise<OvertimeRecalcBilanz>`
mit `OvertimeRecalcBilanz = { anlass, gesamt, verarbeitet, fehlgeschlagen, fehler, dauerMs }`.

`.github/workflows/deploy-server.yml` (Zeilennummern des Ist-Stands, vor Ort erneut prüfen):
- `:116-118` HINWEIS-Kommentar, dass der Schritt „Fix overtime calculations" nach hinten
  verschoben wurde
- `:120-127` crontab-Block Blue: `CRON_COMMAND="0 3 * * * cd /home/ubuntu/TimeTracking-Clean/server && DATABASE_PATH=/home/ubuntu/databases/production.db NODE_ENV=production npx tsx scripts/fix-overtime.ts >> /home/ubuntu/logs/overtime-fix.log 2>&1"`,
  `mkdir -p /home/ubuntu/logs`,
  `(crontab -l 2>/dev/null | grep -v "fix-overtime.ts"; echo "$CRON_COMMAND") | crontab - || { … }`
- `:182-203` Begründungskommentar zur Position nach PM2-Start und Health-Check
- `:204-209` der Post-Deploy-Lauf `npx tsx scripts/fix-overtime.ts || { … }`
- `:211 ff.` der Folgeschritt `- name: Verify DB path post-deploy` bleibt unverändert

`.github/workflows/deploy-staging.yml` (Ist-Stand):
- `:109-114` Post-Deploy-Lauf Green: `NODE_ENV=staging npx tsx scripts/fix-overtime.ts || { … }`
- `:116-123` crontab-Block Green: `CRON_COMMAND="0 3 * * * cd /home/ubuntu/TimeTracking-Green/server && NODE_ENV=staging npx tsx scripts/fix-overtime.ts …"`, gefolgt von
  `mkdir -p /home/ubuntu/logs` und demselben `crontab -`-Aufruf
- `:125-134` der Block „Create .env" schließt direkt an und bleibt **unverändert**

**Auf beiden Servern steht ein zweiter, fremder crontab-Eintrag, der erhalten bleiben MUSS:**
`0 2 * * 0 /home/ubuntu/TimeTracking-Staging/server/scripts/sync-prod-to-staging.sh >> /home/ubuntu/logs/db-sync.log 2>&1`
(belegt in `14-AUSLIEFERUNG.md:585`). Der Filter darf deshalb ausschließlich auf
`fix-overtime.ts` lauten und niemals breiter gefasst werden.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: fixOvertime.ts als gehärtetes Handbetriebs-Werkzeug (D-06, D-11)</name>
  <files>server/src/scripts/fixOvertime.ts (neu), server/scripts/fix-overtime.ts (per git rm entfernt), server/package.json</files>

  <read_first>
    - `server/scripts/fix-overtime.ts` (vollständig, 99 Zeilen — die abzulösende Datei; der
      Kommentarblock `:15-35` ist fachlich zu übernehmen)
    - `server/src/scripts/backfillOvertimeJournal.ts` Zeilen 290-350 (die verbindliche
      Guard-Reihenfolge und der Meldungsstil, wörtlich zu spiegeln)
    - `server/src/scripts/productionGuard.ts` (vollständig — `assertNotProduction()` und der
      Kopfkommentar, der die Aufrufreihenfolge begründet)
    - `server/src/services/overtimeRecalcRunner.ts` (aus Plan 09.1-04 — die aufzurufende
      Signatur und der Rückgabetyp)
    - `server/package.json` nach Plan 09.1-03 (die `scripts`-Sektion, in die der neue Eintrag
      kommt)
    - `.claude/CLAUDE.md` Abschnitt „Produktionsdatenbank: niemals aus einem eigenen Prozess
      öffnen" und §Datenbank-Regeln 1 und 3
  </read_first>

  <action>
Lege `server/src/scripts/fixOvertime.ts` neu an und entferne `server/scripts/fix-overtime.ts`
per `git rm`. Es ist kein reines Verschieben: die `../dist/`-Importe müssen echt umgeschrieben
werden, und die Guard-Reihenfolge verlangt einen anderen Aufbau als heute.

**Aufbau, in genau dieser Reihenfolge** (Muster aus `backfillOvertimeJournal.ts:300-345`):

1. Am Dateikopf **nur** importsichere Module statisch importieren: `path`,
   `import { getDatabasePath } from '../config/database.js';` und
   `import { assertNotProduction } from './productionGuard.js';`. **Kein** statischer Import
   von `../database/connection.js`, `../services/overtimeService.js` oder
   `../services/overtimeRecalcRunner.js` — genau der statische Import ist heute die Ursache
   der täglichen Schema-Initialisierung gegen `production.db` (`09-REVIEW.md:211-222`).
2. `parseArgs(argv: string[])` erkennt `--allow-production` und `--userId=<zahl>` (mehrfach
   angebbar). Unbekannte Argumente führen zu einer Nutzungsmeldung und `process.exit(2)`.
3. In `main()`:
   a. `DATABASE_PATH` fehlt oder ist leer → deutsche Fehlermeldung auf `stderr`, die
      ausdrücklich sagt, dass es keinen stillen Rückfall auf `database/development.db` gibt,
      und `process.exit(2)`.
   b. `parseArgs(process.argv.slice(2))`
   c. `const resolvedPath = path.resolve(getDatabasePath());`
   d. Erste Ausgabezeile: `Aufgelöster Datenbankpfad: <pfad>`
   e. Ist `--allow-production` gesetzt: eine mehrzeilige Warnung ausgeben, die (i) sagt, dass
      der Produktionsschutz umgangen wird, (ii) die Zieldatenbank nennt und (iii) ausdrücklich
      auf die Gefahr hinweist — ein eigener Prozess auf `production.db` räumt beim Schließen
      WAL und SHM auf und hängt die WAL des laufenden Serverprozesses ab (Vorfälle
      `.planning/debug/db-stabilisierung-20260818.md` und
      `.planning/debug/wal-abgehaengt-20260827.md`); der reguläre Weg ist der
      In-Prozess-Scheduler. Ist der Schalter nicht gesetzt: `assertNotProduction(resolvedPath)`.
   f. **Erst jetzt** die datenbankberührenden Module laden:
      `const { db } = await import('../database/connection.js');` und
      `const { runOvertimeRecalcForAllUsers } = await import('../services/overtimeRecalcRunner.js');`
   g. `try { const bilanz = await runOvertimeRecalcForAllUsers({ anlass: 'handbetrieb', userIds: args.userIds }); … Bilanz ausgeben … } finally { db.close(); }`
      Der `finally`-Block ist der Kern von D-06: heute fehlt `db.close()` im Fehlerpfad
      (`:96-99`).
   h. Nach dem `finally`: Ist `bilanz.fehlgeschlagen > 0`, mit `process.exit(1)` enden, sonst
      mit `0`. Als CLI-Werkzeug behält es einen Exit-Code — anders als der Scheduler, der nach
      D-05 nur die Bilanzzeile hat.
4. `main().catch((error) => { console.error('❌ FATAL:', error); process.exit(1); });` am
   Dateiende bleibt als äußerste Sicherung.

Die Nutzerschleife wird **nicht** neu geschrieben. Sie lebt seit Plan 09.1-04 in
`overtimeRecalcRunner.ts`; eine zweite Kopie wäre genau der Befund A-1 aus Phase 9 („eine
eigene, zweite Kopie der Logik").

Übernimm den fachlichen Kern des Kommentarblocks `:15-35` (REQ-17 / A-1: kanonische Funktion
statt eigener Sollstundenrechnung) und ergänze ihn um: WR-05, D-06, und den ausdrücklichen
Satz, dass dieses Werkzeug ab Phase 9.1 **keinen** automatischen Aufrufer mehr hat — weder
crontab noch Deploy-Workflow — und ausschließlich für den Handbetrieb existiert.

Ergänze in `server/package.json` einen Eintrag
`"fix:overtime": "tsx src/scripts/fixOvertime.ts"` bei den übrigen Werkzeugen. Ändere sonst
nichts an der Datei — die acht Pfadanpassungen aus Plan 09.1-03 sind bereits erledigt.
  </action>

  <acceptance_criteria>
    - `server/src/scripts/fixOvertime.ts` existiert; `ls server/scripts/*.ts` liefert keinen
      Treffer mehr (Exitcode 2 bzw. leere Ausgabe) — damit ist `server/scripts/**` frei von
      TypeScript und WR-04 vollständig geschlossen.
    - `grep -c "dist/" server/src/scripts/fixOvertime.ts` liefert `0`.
    - Die Zeilennummer von `assertNotProduction` in `server/src/scripts/fixOvertime.ts` liegt
      VOR jeder Zeile mit `await import('../database/connection.js')` oder
      `await import('../services/` — prüfbar über
      `grep -n "assertNotProduction\|await import(" server/src/scripts/fixOvertime.ts`.
    - `grep -c "^import .*connection.js\|^import .*overtimeService.js\|^import .*overtimeRecalcRunner.js" server/src/scripts/fixOvertime.ts`
      liefert `0` (keine statischen Importe datenbankberührender Module).
    - `grep -c "finally" server/src/scripts/fixOvertime.ts` liefert ≥ 1, und `db.close()`
      steht innerhalb dieses Blocks.
    - `grep -c "tsx src/scripts/fixOvertime.ts" server/package.json` liefert `1`.
    - **Kanarienprobe A (Guard greift):**
      `cd server && DATABASE_PATH=/home/ubuntu/databases/production.db npx tsx src/scripts/fixOvertime.ts`
      endet mit Exitcode `2` und gibt die Verweigerungsmeldung aus. Der Pfad existiert auf dem
      Entwicklungsrechner nicht — es wird nichts geöffnet und nichts angelegt. Prüfe nach dem
      Lauf mit `ls`, dass unter dem Pfadpräfix keine Datei entstanden ist (D-16).
    - **Kanarienprobe B (fehlendes DATABASE_PATH):**
      `cd server && npx tsx src/scripts/fixOvertime.ts` ohne gesetztes `DATABASE_PATH` endet
      mit Exitcode `2` und nennt die fehlende Variable.
    - **Kanarienprobe C (NODE_ENV):**
      `cd server && DATABASE_PATH=<lokaler Wegwerfpfad> NODE_ENV=production npx tsx src/scripts/fixOvertime.ts`
      endet mit Exitcode `2` — der Guard prüft auch `NODE_ENV`.
    - **Funktionsnachweis:** `cd server && DATABASE_PATH=server/database/development.db npx tsx src/scripts/fixOvertime.ts --userId=<eine reale id aus development.db>`
      läuft durch, gibt „Aufgelöster Datenbankpfad" als erste Zeile aus und endet mit
      Exitcode `0`; die Bilanzzeile nennt `verarbeitet: 1`, `fehlgeschlagen: 0`. Sichere
      `development.db` vorher per Dateikopie und halte in der SUMMARY fest, ob und wie sich
      die Datei verändert hat.
    - **Fehlerpfad-Nachweis (D-06):** derselbe Aufruf mit einer nicht vergebenen id endet mit
      Exitcode `1`, nennt `fehlgeschlagen: 1` und hinterlässt **keine** `-wal`- oder
      `-shm`-Datei neben der Wegwerfdatenbank — Beleg dafür, dass `db.close()` auch im
      Fehlerpfad lief. Prüfe das mit `ls` vor und nach dem Lauf.
    - `cd server && npx tsc --noEmit` endet mit Exitcode 0.
    - `grep -rn ": any\b" server/src/scripts/fixOvertime.ts` liefert keinen Treffer.
  </acceptance_criteria>

  <verify>
    <automated>cd server && npx tsc --noEmit && grep -n "assertNotProduction\|await import(" src/scripts/fixOvertime.ts && ls scripts/*.ts 2>&1 | head -1</automated>
  </verify>

  <done>
    Das Werkzeug liegt typisiert unter `server/src/scripts/`, lädt datenbankberührende Module
    erst nach dem Produktionsschutz, schließt die Verbindung im `finally` und nutzt dieselbe
    Nutzerschleife wie der Scheduler. Drei Kanarienproben und zwei Funktionsläufe sind mit
    Kommandozeile und Exitcode protokolliert. `server/scripts/` enthält keine TypeScript-Datei
    mehr.
  </done>
</task>

<task type="auto">
  <name>Task 2: Beide crontab-Einträge und beide Post-Deploy-Läufe aus den Workflows entfernen (D-03, D-04)</name>
  <files>.github/workflows/deploy-server.yml, .github/workflows/deploy-staging.yml</files>

  <read_first>
    - `.github/workflows/deploy-server.yml` (vollständig — mindestens Zeilen 95-215; die
      Zeilennummern des Ist-Stands sind vor der Änderung erneut am Quelltext zu prüfen, sie
      können sich durch Plan 09.1-03 nicht verschoben haben, aber die Regel gilt)
    - `.github/workflows/deploy-staging.yml` (vollständig — mindestens Zeilen 85-155; Green hat
      zwischen den beiden Blöcken einen dritten Block „Create .env", der stehen bleibt)
    - `.planning/phases/14-absicherung-und-auslieferung/14-AUSLIEFERUNG.md` Abschnitt mit dem
      wöchentlichen crontab-Eintrag `sync-prod-to-staging.sh` (Zeile 585) — der Eintrag, der
      den Filter eng halten muss
    - `server/src/server.ts` nach Plan 09.1-04 (der Kommentarblock, der die Begründung jetzt
      trägt — auf ihn wird im Workflow verwiesen)
    - `.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-CONTEXT.md`, D-03/D-04
  </read_first>

  <action>
**In `.github/workflows/deploy-server.yml`:**

(a) Ersetze den crontab-Block (`:120-127`) durch einen reinen **Bereinigungsschritt**. Die
Zeilen `CRON_COMMAND=…`, `mkdir -p /home/ubuntu/logs` und der `echo "$CRON_COMMAND"`-Teil
entfallen; der Aufruf
`(crontab -l 2>/dev/null | grep -v "fix-overtime.ts") | crontab - || { echo "…"; }` **bleibt**
— ohne den angehängten Neueintrag. Damit räumt sich der Server beim nächsten Deployment selbst
auf, wie D-03 es vorsieht, und ein manueller Eingriff auf dem Server ist nicht nötig.
Der Filterausdruck bleibt **exakt** `fix-overtime.ts` — auf demselben Server steht ein
wöchentlicher Eintrag für `sync-prod-to-staging.sh`, den ein breiterer Filter mitlöschen würde.
Setze einen deutschen Kommentar darüber, der Phase 9.1, D-03 und den Grund nennt (der Nachtlauf
läuft seit Phase 9.1 im Serverprozess; ein externer Cron ist die Ursache der abgehängten WAL).

(b) Entferne den Post-Deploy-Lauf (`:204-209`) vollständig, einschließlich der
`echo`-Zeilen davor und danach und des `cd`-Wechsels, sofern er ausschließlich diesem Schritt
dient — prüfe am Quelltext, ob der `cd` von einem Folgeschritt gebraucht wird, bevor du ihn
entfernst.

(c) Kürze den Begründungskommentar (`:182-203`) auf drei bis fünf Zeilen ein, die festhalten:
Der Schritt ist mit Phase 9.1 (D-04) entfallen; der Neuberechnungslauf läuft jetzt einmalig
im Serverprozess nach `runMigrations(db)`; die vollständige Begründung für diese Position
steht im Kommentarblock in `server/src/server.ts`. Die Begründung darf nicht ersatzlos
verschwinden — sie erklärt, warum die Position im Ablauf nicht beliebig ist, und dieser Grund
gilt unverändert weiter.

(d) Passe den HINWEIS-Kommentar (`:116-118`) an: der Schritt ist nicht mehr „nach hinten
verschoben", sondern entfallen.

**In `.github/workflows/deploy-staging.yml`:**

(e) Entferne den Post-Deploy-Lauf (`:109-114`) vollständig.

(f) Ersetze den crontab-Block (`:116-123`) durch denselben reinen Bereinigungsschritt wie unter
(a), ohne `DATABASE_PATH`-Präfix (Green setzt es nicht) und mit demselben Filter
`fix-overtime.ts`.

(g) Der Block „Create .env" (`:125-134`) und alles danach bleiben **unverändert**.

Ändere in beiden Dateien nichts anderes: keine Umformatierung, keine Neusortierung von
Schritten, keine Änderung an Migrations-, Build-, PM2- oder Health-Check-Schritten. YAML ist
einrückungsempfindlich — prüfe nach der Änderung die Einrückung der verbliebenen Zeilen im
`script: |`-Block.
  </action>

  <acceptance_criteria>
    - `grep -c "fix-overtime.ts" .github/workflows/deploy-server.yml` liefert genau `1` — nur
      noch der Filterausdruck im Bereinigungsschritt.
    - `grep -c "fix-overtime.ts" .github/workflows/deploy-staging.yml` liefert genau `1`.
    - `grep -c "CRON_COMMAND" .github/workflows/deploy-server.yml .github/workflows/deploy-staging.yml`
      liefert für beide Dateien `0`.
    - `grep -c "npx tsx scripts/" .github/workflows/deploy-server.yml .github/workflows/deploy-staging.yml`
      liefert für beide Dateien `0`.
    - `grep -n "grep -v \"fix-overtime.ts\"" .github/workflows/deploy-server.yml .github/workflows/deploy-staging.yml`
      liefert in jeder Datei genau einen Treffer (der Bereinigungsfilter steht noch, D-03).
    - `grep -c "sync-prod-to-staging" .github/workflows/deploy-server.yml .github/workflows/deploy-staging.yml`
      liefert `0` — der Filter fasst diesen Eintrag nicht an.
    - Beide Dateien sind gültiges YAML: `python -c "import yaml,sys; yaml.safe_load(open(sys.argv[1]))" .github/workflows/deploy-server.yml`
      und dasselbe für `deploy-staging.yml` enden mit Exitcode 0. Steht `python` nicht zur
      Verfügung, `npx --no-install js-yaml <datei>` oder ein gleichwertiges Werkzeug verwenden
      und das verwendete Kommando in der SUMMARY nennen.
    - `git diff .github/workflows/deploy-server.yml` zeigt **keine** Änderung an den Schritten
      „Running database migrations", „Validating database schema", „Restarting PM2",
      „Running health check" und `- name: Verify DB path post-deploy`.
    - `git diff .github/workflows/deploy-staging.yml` zeigt **keine** Änderung am Block
      „Create .env" und an allem danach.
    - `git diff --stat` dieses Plans listet ausschließlich die fünf in `files_modified`
      genannten Dateien.
  </acceptance_criteria>

  <verify>
    <automated>grep -c "fix-overtime.ts" .github/workflows/deploy-server.yml .github/workflows/deploy-staging.yml && grep -c "CRON_COMMAND" .github/workflows/deploy-server.yml .github/workflows/deploy-staging.yml; true</automated>
  </verify>

  <done>
    Beide Workflows enthalten keinen crontab-Neueintrag und keinen Post-Deploy-Lauf mehr; der
    Bereinigungsfilter auf `fix-overtime.ts` steht in beiden Dateien genau einmal; beide
    Dateien sind gültiges YAML; kein anderer Schritt ist berührt.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI-Werkzeug → Datenbankdatei aus `DATABASE_PATH` | Ein Bediener kann das Werkzeug mit beliebigem Pfad starten; `--allow-production` hebt den Schutz auf. |
| Deploy-Workflow → crontab des Servers | Der Workflow schreibt die crontab des Nutzers `ubuntu` neu. Ein zu breiter Filter löscht fremde Einträge. |
| Deploy-Workflow → Verfügbarkeit des Nachtlaufs | Wird der externe Cron entfernt, ohne dass der In-Prozess-Lauf greift, findet keine Neuberechnung mehr statt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-25 | Tampering | `--allow-production` wird versehentlich oder gewohnheitsmäßig gesetzt; das Werkzeug öffnet und schließt `production.db` aus einem eigenen Prozess und hängt die WAL des Servers ab | mitigate | Der Schalter ist nach D-06 vorgeschrieben, wird aber mit einer mehrzeiligen Warnung versehen, die beide Vorfallsberichte namentlich nennt und auf den In-Prozess-Scheduler als regulären Weg verweist. Ohne den Schalter greift `assertNotProduction()` mit `process.exit(2)` — als Kanarienprobe A ausdrücklich nachgewiesen. Kein automatischer Aufrufer setzt den Schalter; nach Task 2 gibt es überhaupt keinen automatischen Aufrufer mehr. |
| T-09.1-26 | Tampering | Der Produktionsschutz läuft zu spät — ein statischer Import hat die Datei bereits geöffnet und per `mkdirSync` ein Verzeichnis angelegt | mitigate | Verbindliche Guard-Reihenfolge aus `backfillOvertimeJournal.ts`; datenbankberührende Module ausschließlich über `await import()` NACH dem Guard. Abnahmekriterium prüft die Zeilenreihenfolge per `grep -n` und verbietet statische Importe per Zählprüfung. |
| T-09.1-27 | Denial of Service | Der Bereinigungsfilter ist zu breit gefasst und löscht den wöchentlichen crontab-Eintrag `sync-prod-to-staging.sh` | mitigate | Filterausdruck bleibt exakt `fix-overtime.ts`; Abnahmekriterium: `grep -c "sync-prod-to-staging"` in beiden Workflows = 0, und der Nachweis am laufenden Server (`crontab -l` enthält die Sync-Zeile noch) ist Teil von Plan 09.1-06. |
| T-09.1-28 | Denial of Service | Der externe Cron ist entfernt, der In-Prozess-Scheduler läuft aber nicht — es findet gar keine Neuberechnung mehr statt | mitigate | Wellenordnung: dieser Plan hängt an `09.1-04`, der Scheduler existiert also bereits. Zusätzlich wird in Plan 09.1-06 auf Green und auf Blue am Protokoll belegt, dass Scheduler-Start und Anlauf-Bilanzzeile erscheinen, bevor die Phase als abgeschlossen gilt. |
| T-09.1-29 | Denial of Service | Ein Einrückungsfehler im `script: |`-Block macht den Workflow ungültig; das nächste Deployment schlägt fehl | mitigate | YAML-Gültigkeitsprüfung beider Dateien als Abnahmekriterium; `git diff` gegen die unberührten Schritte; das erste Deployment geht nach D-14 ohnehin auf Green (Port 3001), nicht auf Produktion. |
| T-09.1-30 | Repudiation | Der Begründungskommentar zur Position im Ablauf verschwindet; eine spätere Änderung schiebt den Lauf wieder vor die Migrationen | mitigate | Die Begründung wandert nach `server/src/server.ts` (Plan 09.1-04, Task 3) und wird im Workflow als Kurzverweis behalten — ersatzloses Streichen ist ausdrücklich untersagt. |
| T-09.1-31 | Information Disclosure | Die Warnung bei `--allow-production` nennt den vollen Datenbankpfad in einem CI-Protokoll | accept | Der Pfad steht bereits heute im Workflow und in `.claude/CLAUDE.md`; kein Geheimnis. Das Werkzeug wird nach diesem Plan von keinem Workflow mehr aufgerufen. |
| T-09.1-SC | Tampering | npm/pip/cargo-Installationen | mitigate | Dieser Plan installiert KEIN Paket. Wird für die YAML-Prüfung ein Werkzeug benötigt, ist `python -c "import yaml…"` (Bordmittel) der erste Weg; ein `npx --no-install`-Aufruf lädt nichts nach. Jede echte Installation ist vorher mit einem blockierenden Checkpoint dem Anwender vorzulegen. |
</threat_model>

<verification>
- `cd server && npx tsc --noEmit` → Exitcode 0
- `ls server/scripts/*.ts` → kein Treffer (WR-04 vollständig)
- `grep -c "fix-overtime.ts"` in beiden Workflows → je genau 1 (nur der Filter)
- `grep -c "CRON_COMMAND"` in beiden Workflows → je 0
- YAML-Gültigkeit beider Workflows → Exitcode 0
- Drei Kanarienproben des Werkzeugs (fehlendes `DATABASE_PATH`, Produktionspfad,
  `NODE_ENV=production`) → je Exitcode 2
- Funktionslauf gegen `development.db` → Exitcode 0; Fehlerpfadlauf → Exitcode 1, keine
  zurückgelassene `-wal`/`-shm`-Datei
- `cd server && npx vitest run` → keine neue rote Testdatei
- Kein Zugriff auf `production.db` (D-16)
</verification>

<success_criteria>
- Das Werkzeug existiert typisiert unter `server/src/scripts/fixOvertime.ts`, ist gehärtet und
  hat keinen automatischen Aufrufer mehr (D-06)
- Beide crontab-Einträge sind entfernt, der Bereinigungsfilter bleibt stehen (D-03)
- Beide Post-Deploy-Läufe sind entfernt (D-04)
- `server/scripts/` enthält keine TypeScript-Datei mehr — WR-04 ist damit vollständig
  geschlossen (D-09, D-11)
- Der wöchentliche `sync-prod-to-staging.sh`-Eintrag ist von keinem Filter betroffen
</success_criteria>

<output>
Erstelle `.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-05-SUMMARY.md`
</output>
