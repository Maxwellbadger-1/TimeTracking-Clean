---
phase: 09.1-journal-backfill-und-betriebs-h-rtung
plan: 07
subsystem: infra
tags: [deployment, production, github-actions, pm2, ssh, cron, scheduler, wal]

requires:
  - phase: 09.1-06
    provides: Gemessener Nachweis der Betriebs-Härtung auf Green (Port 3001), Freigabe-Grundlage
provides:
  - Produktionsserver (Blue, Port 3000) trägt den vollständigen Stand der Phase 9.1
  - Die abgehängte Produktions-WAL ist durch dieses Deployment aufgelöst — Ausgangswert für D-17
  - D-15 für alle drei Commits (ad48068, ded14d3, c0acb59) abschließend beurteilt
affects: [09.1-08]

tech-stack:
  added: []
  patterns: ["App-Backup als Sicherheitsnetz vor risikobehaftetem Neustart", "Vier-Punkt-Vorprüfung der Dateizeiger vor jedem Produktions-Neustart"]

key-files:
  created:
    - .planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-NACHWEIS-PRODUKTION.md
  modified:
    - CHANGELOG.md
    - PROJECT_STATUS.md
    - .claude/CLAUDE.md
    - .planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/deferred-items.md

key-decisions:
  - "Anwender gab am 29.08.2026 die Produktionsauslieferung mit der Bedingung „Es darf nichts verloren gehen\" frei und wählte den Weg „Backup in der App, dann ausliefern\"."
  - "Trotz GitHub-Actions-Lauf `completed`/`failure` wurde die Auslieferung als inhaltlich erfolgreich behandelt (Option A des Zwischen-Checkpoints), weil Migrations-, Cron- und Neustartschritte nachweislich vollständig und fehlerfrei liefen und nur der Health-Check-Schritt an einer zu kurzen Wartezeit scheiterte — Workflow wurde bewusst NICHT erneut ausgelöst."
  - "ded14d3 (tauriHttpClient binärsicher) ist bereits über Release v1.9.1 (28.08.2026) ausgeliefert; für D-15 ist kein weiteres Desktop-Release nötig."

patterns-established: []

requirements-completed: [WR-02, WR-03, WR-04, WR-05]

duration: ca. 70min
completed: 2026-08-29
---

# Phase 09.1 Plan 07: Produktionsauslieferung und Wirkungsnachweis Summary

**Die Betriebs-Härtung der Phase 9.1 auf Produktion (Blue, Port 3000) ausgeliefert — die seit dem 27.08.2026 abgehängte Produktions-WAL ist mit diesem Deployment aufgelöst, D-15 für alle drei Commits abschließend beurteilt, D-03/D-01/D-02/D-04/D-05 auf echten Produktionsdaten (15 Nutzer) bestätigt.**

## Performance

- **Duration:** ca. 70 Min (Task 2 + Task 3; Task 1 war bereits vor dieser Fortsetzung entschieden)
- **Started:** 2026-08-29T19:20:00Z
- **Completed:** 2026-08-29T~20:30:00Z
- **Tasks:** 3 (Task 1 bereits entschieden, Task 2 und 3 in dieser Fortsetzung ausgeführt)
- **Files modified:** 5 (1 neu, 4 geändert)

## Accomplishments

- **Freigabe (D-14):** Der Anwender hat am 29.08.2026 die sieben Belege aus
  `09.1-NACHWEIS-STAGING.md` wörtlich vorgelegt bekommen und die Produktionsauslieferung
  freigegeben. Bedingung, wörtlich: **„Es darf nichts verloren gehen."** Gewählter Weg:
  „Backup in der App, dann ausliefern".
- **Sicherheitsnetz vor dem Push:** Der App-Backup-Prüfpunkt (`createBackup()`,
  `wal_checkpoint(TRUNCATE)`) leerte die abgehängte WAL vor dem Push auf 0 Bytes. Drei
  unabhängige Sicherungen lagen vor dem Deployment bereit: `database-backup-2026-08-29T19-19-42-699Z.db`
  (App, im Arbeitsbaum), `pre-phase9.1_20260829_191942.db` (Zweitkopie außerhalb des
  Arbeitsbaums, md5 `9162c73f7351837d685a26dd223b9d2d` identisch mit der ersten), sowie die
  vom Deploy-Workflow selbst angelegte `production.predeploy_20260829_192300.db`.
- **Push nach `main`, Workflow-Lauf `33270724872`:** Migrationsschritt lief korrekt gegen
  `/home/***/databases/production.db` (kein `Cannot find module`, kein `ENOENT`). crontab
  bereinigt (`fix-overtime`-Eintrag entfernt, `sync-prod-to-staging` unverändert) — ohne
  manuellen Servereingriff (D-03). PM2-Neustart erfolgreich (neue PID 3751867). **Der
  GitHub-Actions-Lauf endete formal `completed`/`failure`**, weil der Health-Check-Schritt nur
  5 Sekunden nach dem PM2-Neustart wartete und bei einem ungewöhnlich langen `npm install`
  (~4 Min) auf einen noch initialisierenden Server traf (`curl`-Exitcode 7). Kein Codedefekt
  dieser Phase — als Befund in `deferred-items.md` (Plan 09.1-07, Befund 4) und in
  `CHANGELOG.md` unter „Known Issues" dokumentiert, Workflow bewusst **nicht** erneut
  ausgelöst.
- **In-Prozess-Scheduler und Anlauf (D-01, D-02, D-04, D-05) auf echten Produktionsdaten
  bestätigt:** Scheduler „03:15 AM Europe/Berlin" gestartet, Anlauf `anlass: serverstart` mit
  `gesamt: 15`, Bilanz `verarbeitet: 15, fehlgeschlagen: 0` — alle 15 aktiven
  Produktionsnutzer fehlerfrei verarbeitet.
- **Der stärkste Wirkungsnachweis dieser Phase — die abgehängte WAL ist aufgelöst:**
  Vorzustand (27.08.–29.08.2026): `production.db-wal`/`-shm` trugen `(deleted)` am
  Dateizeiger des Serverprozesses, existierten nicht mehr im Dateisystem. Nachzustand (ab
  19:28:27 UTC, PID 3751867): beide Dateien wieder regulär verknüpft, kein `(deleted)` mehr,
  Verzeichnis-mtime von `~/databases/` auf `19:28:22` gesprungen (nicht mehr der alte
  `03:00:11`-Stempel des Nachtlaufs). Diese Werte sind der D-17-Ausgangswert für Plan
  09.1-08 und wurden vom Orchestrator um 19:31:46 UTC unabhängig gegengeprüft.
- **D-15 je Commit abschließend beurteilt** (siehe „Decisions Made"): alle drei Commits sind
  ausgeliefert, kein weiteres Desktop-Release nötig.
- **Kerndokumentation nachgezogen:** `CHANGELOG.md` (Phase-9.1-Eintrag unter `[Unreleased]`
  plus Known-Issue-Eintrag zum CI-Fehlschlag), `PROJECT_STATUS.md` (ausgelieferter Stand,
  Nebenbefund B-1 als behoben markiert), `.claude/CLAUDE.md` §Datenbank-Regeln 3 (Zwischenstand
  formuliert, Beobachtung aus Plan 09.1-08 als offen gekennzeichnet, Abschnitt „niemals aus
  einem eigenen Prozess öffnen" unangetastet).
- **Kein Zugriff auf `production.db`** in diesem gesamten Plan (D-16) — vollständige
  Kommandoliste in `09.1-NACHWEIS-PRODUKTION.md`, Abschnitt 9.

## Task Commits

1. **Task 1: Freigabe für die Produktionsauslieferung (D-14)** — Checkpoint, keine Datei
   geändert; Freigabe des Anwenders wörtlich festgehalten (siehe „Decisions Made").
2. **Task 2: Produktion ausliefern und die Wirkung messen** — `98ec14f` (docs)
3. **Task 3: D-15 abschließen und die Kerndokumentation nachziehen** — `18d0aec` (docs)

**Plan metadata:** wird mit dieser SUMMARY committet (`docs(09.1-07): complete plan`).

## Files Created/Modified

- `.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-NACHWEIS-PRODUKTION.md` -
  Messprotokoll der Produktionsauslieferung: Vorprüfung, Push, Workflow-Lauf (inkl.
  CI-Fehlschlag-Befund), Migration, Health-Check, Scheduler/Anlauf/Bilanz, crontab,
  D-17-Ausgangswerte, D-16-Nachweis
- `CHANGELOG.md` - Phase-9.1-Eintrag unter `[Unreleased]`, Known-Issue-Eintrag zum
  CI-Fehlschlag des Deployments
- `PROJECT_STATUS.md` - ausgelieferter Stand (Server `e4866a4`), Deployment-Zeile ergänzt,
  Nebenbefund B-1 als behoben markiert
- `.claude/CLAUDE.md` - Kopfzeile und §Datenbank-Regeln 3 auf Zwischenstand gebracht,
  Beobachtung aus Plan 09.1-08 referenziert
- `.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/deferred-items.md` - Befund 4
  (Health-Check-Wettlauf im Deploy-Workflow, kein Codedefekt dieser Phase)

## Decisions Made

**Freigabe des Anwenders, wörtlich, 29.08.2026:** Nach wörtlicher Vorlage aller sieben Belege
aus `09.1-NACHWEIS-STAGING.md` gab der Anwender die Produktionsauslieferung frei. Seine
Bedingung, wörtlich: **„Es darf nichts verloren gehen."** Er wählte den Weg **„Backup in der
App, dann ausliefern"** und löste das Backup selbst über „Datenbank Backups" → „Backup
erstellen" aus. Diese Bedingung wurde eingelöst: drei unabhängige Sicherungen lagen vor dem
Push bereit (siehe „Accomplishments"), und die Vorprüfung in Task 2(a) bestätigte, dass die
WAL zum Zeitpunkt des Pushs bereits vollständig (0 Bytes) in `production.db` stand — kein
Datenverlust durch den nachfolgenden Neustart möglich.

**Zwischen-Checkpoint-Entscheidung des Orchestrators, wörtlich, 29.08.2026:** Nach dem
CI-Fehlschlag (Lauf `33270724872`, `completed`/`failure`) legte dieser Ausführer laut den
harten Grenzen des Auftrags einen Checkpoint mit drei Optionen vor. Der Orchestrator
entschied: **„Fahre mit Option A fort und schließe Plan 09.1-07 ab. Löse den Workflow NICHT
erneut aus."** Begründung: Option A ist das, was Task 2(b) für den Fehlschlagsfall selbst
vorschreibt — Grund benennen, nach `CHANGELOG.md` unter `[Unreleased]` schreiben, nicht blind
wiederholen. Der Orchestrator prüfte die Messwerte unabhängig gegen (Stand 19:31:46 UTC) und
bestätigte: PM2 online, Dateizeiger regulär (kein `(deleted)`), Health 200,
`origin/main` == lokaler HEAD. Diese unabhängig bestätigten Werte sind in
`09.1-NACHWEIS-PRODUKTION.md` Abschnitt 8 aufgenommen.

**D-15 je Commit, abschließend:**

| Commit | Messung | Schlussfolgerung |
|--------|---------|-------------------|
| `ad48068` (WAL-Prüfpunkt bei SIGTERM/SIGINT) | `git merge-base --is-ancestor ad48068 origin/main` → Exitcode 0; Log-Zeile `🛡️ Signalbehandlung aktiv (SIGTERM/SIGINT) - WAL-Pruefpunkt beim Beenden` im Produktionsprotokoll nach diesem Deployment vorhanden | D-15 erfüllt — mit diesem Deployment (`e4866a4`) real auf Produktion aktiv |
| `ded14d3` (tauriHttpClient binärsicher) | `git merge-base --is-ancestor ded14d3 v1.9.1` → Exitcode 0; `gh release list` zeigt `v1.9.1` vom 2026-08-28 als aktuellstes Release | D-15 erfüllt — bereits über Release v1.9.1 ausgeliefert, **kein** weiteres Desktop-Release nötig; `git diff` auf die drei Versionsdateien blieb leer |
| `c0acb59` (Doku) | `git merge-base --is-ancestor c0acb59 origin/main` → Exitcode 0 | D-15 erfüllt — steht in `origin/main` |

## Deviations from Plan

### Auto-fixed Issues

Keine Rule-1/2/3-Auto-Fixes an Code — dieser Plan ändert keine Quelldateien.

### Dokumentierte, nicht behobene Befunde (außerhalb des Plan-Umfangs)

**1. [Scope Boundary — nicht diese Phase] Health-Check-Wettlauf im Deploy-Workflow**
- **Gefunden während:** Task 2, Abschnitt (b)/(d) — Auswertung des Workflow-Protokolls
- **Befund:** `deploy-server.yml` wartet nach dem PM2-Neustart nur `sleep 5`, bevor der
  Health-Check-Schritt gegen `localhost:3000/api/health` prüft. Bei diesem Lauf brauchte
  `npm install` ungewöhnlich lange (~4 Min, `🔧 Fixing npm corruption`), wodurch der Server
  zum Prüfzeitpunkt (`19:28:25`) noch initialisierte (`Listening` erst `19:28:27`) — `curl`
  scheiterte mit Exitcode 7, der Workflow brach mit `set -e`/`script_stop: true` ab.
- **Auswirkung eingeordnet:** Kein Datenverlust, keine Beeinträchtigung — alle inhaltlichen
  Schritte (Migration, crontab-Bereinigung, PM2-Neustart, Scheduler/Anlauf) liefen vorher
  bereits vollständig und fehlerfrei durch. Server unabhängig als gesund bestätigt (eigene
  Prüfung 19:29:55 UTC, Orchestrator-Prüfung 19:31:48 UTC, beide HTTP 200).
- **Nicht behoben:** Eine Änderung der Wartezeit/eines Retry-Loops im Workflow ist außerhalb
  des Umfangs dieses Plans (Auslieferung + Messung, keine Workflow-Änderung) und verdient
  einen eigenen kleinen Plan.
- **Dokumentiert in:** `deferred-items.md`, Plan 09.1-07, Befund 4; `CHANGELOG.md` unter
  „Known Issues".

---

**Total deviations:** 0 auto-fixed, 1 dokumentierter Befund außerhalb des Scopes (Workflow,
nicht Code dieser Phase).
**Impact on plan:** Keine Auswirkung auf die fachlichen Abnahmekriterien — Deployment-Inhalt,
Scheduler, crontab, D-17-Ausgangswerte und D-15 sind alle unabhängig vom CI-Laufstatus erfüllt
und belegt.

## Issues Encountered

- GitHub-Actions-Lauf `33270724872` zeigt `completed`/`failure`, obwohl der ausgelieferte
  Zustand nachweislich korrekt ist (siehe Deviations). Für künftige „letzter Lauf =
  success"-Prüfungen (`gh run list --workflow=... --limit 1`) ist das irreführend, solange
  Befund 4 nicht behoben ist — vermerkt in `deferred-items.md`.

## SSH-/API-Kommandos (vollständige Liste, D-16-Nachweis)

Siehe `09.1-NACHWEIS-PRODUKTION.md`, Abschnitt 9, für die vollständige nummerierte Liste aller
20 in diesem Plan ausgeführten SSH-, `gh`-, `curl`- und lokalen Git-Kommandos.
**Kein Zugriff auf `production.db` in diesem Plan.** Der SSH-Schlüssel wurde ausschließlich
per `-i .ssh/oracle_server.key` übergeben und erscheint an keiner anderen Stelle.

## User Setup Required

None - keine externe Dienstkonfiguration nötig.

## Next Phase Readiness

- Produktion (Blue, Port 3000) trägt den vollständigen Stand der Phase 9.1, Server-Commit
  `e4866a4`.
- D-17-Ausgangswert für Plan 09.1-08 aufgenommen: PID 3751867, Dateizeiger regulär
  (`production.db`/`-wal`/`-shm`, kein `(deleted)`), Zeitstempel 2026-08-29T19:29:56Z,
  Verzeichnis-mtime `~/databases/` = `2026-08-29T19:28:22`.
- Plan 09.1-08 beobachtet ab hier, ob dieser reguläre Zustand über mindestens zwei Nächte
  (inklusive des 03:15-Nachtlaufs im Serverprozess) bestehen bleibt, und schreibt das Ergebnis
  in `09.1-NACHWEIS-BEOBACHTUNG.md`.
- Offener, nicht blockierender Befund für eine künftige, kleine Änderung: Health-Check-Timing
  in `deploy-server.yml` (`deferred-items.md`, Plan 09.1-07, Befund 4).

## Self-Check: PASSED

- `.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-NACHWEIS-PRODUKTION.md` — FOUND
- Commit `98ec14f` — FOUND in `git log --oneline --all`
- Commit `18d0aec` — FOUND in `git log --oneline --all`
- `CHANGELOG.md`, `PROJECT_STATUS.md`, `.claude/CLAUDE.md`, `deferred-items.md` — geändert, `git status --short` nach Task 3 leer
- Server-Deployment `e4866a4` — FOUND (`git ls-remote --heads origin` zeigt `main` auf `e4866a46f34c6f7505850acf354f76c121d273ed`)
- D-17-Ausgangswert (kein `(deleted)`) — FOUND, unabhängig durch Orchestrator gegengeprüft (19:31:46 UTC)

---
*Phase: 09.1-journal-backfill-und-betriebs-h-rtung*
*Completed: 2026-08-29*
