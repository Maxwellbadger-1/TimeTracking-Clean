---
phase: 09.1-journal-backfill-und-betriebs-h-rtung
plan: 02
type: execute
wave: 2
depends_on: ["09.1-01"]
files_modified:
  - server/src/services/overtimeTransactionRebuildService.test.ts
  - server/src/services/overtimeService.test.ts
autonomous: true
requirements: [WR-02, REQ-19]

must_haves:
  truths:
    - "D-12: handleAbsenceDay() ist mit einem isolierten overtime_comp-Tag abgedeckt — der Tag erzeugt genau eine Journalzeile und KEINE overtime_comp_credit-Gutschrift."
    - "D-12: updateOvertimeBalanceForMonth() ist abgedeckt — overtime_comp reduziert das Soll nicht und schreibt keine Ist-Gutschrift, im Gegensatz zu vacation und unpaid."
    - "D-12: ensureOvertimeBalanceEntries() aus overtimeService.ts ist abgedeckt — der kanonische Aufrufer, den der Nachtlauf verwendet."
    - "D-13: Jeder neue Test legt Nutzer, Periode und Abwesenheit in beforeEach selbst an und räumt sie in afterEach per DELETE weg — keine Abhängigkeit von Bestandsdaten in development.db."
    - "D-16: Kein Test öffnet production.db oder setzt DATABASE_PATH auf eine Produktionsdatei."
  artifacts:
    - path: "server/src/services/overtimeTransactionRebuildService.test.ts"
      provides: "overtime_comp-Isolation für handleAbsenceDay() und updateOvertimeBalanceForMonth() über den exportierten Einstieg"
      contains: "overtime_comp"
    - path: "server/src/services/overtimeService.test.ts"
      provides: "Abdeckung von ensureOvertimeBalanceEntries() inklusive Idempotenz"
      contains: "ensureOvertimeBalanceEntries"
  key_links:
    - from: "server/src/services/overtimeTransactionRebuildService.test.ts"
      to: "rebuildOvertimeTransactionsForMonth()"
      via: "einziger exportierter Einstieg — handleAbsenceDay und updateOvertimeBalanceForMonth sind modulprivat"
      pattern: "rebuildOvertimeTransactionsForMonth"
    - from: "server/src/services/overtimeService.test.ts"
      to: "ensureOvertimeBalanceEntries()"
      via: "direkter Import aus ./overtimeService.js"
      pattern: "ensureOvertimeBalanceEntries"
---

<objective>
WR-02 im von D-12 gesetzten Restumfang schließen. `09-REVIEW.md:197-209` benennt den Punkt
konkret und begrenzt ihn damit: mindestens ein Test für `handleAbsenceDay()` und
`updateOvertimeBalanceForMonth()` in `overtimeTransactionRebuildService.ts`, der einen
`overtime_comp`-Tag isoliert prüft — analog zum Block in `unifiedOvertimeService.test.ts` —
sowie Testabdeckung für `overtimeService.ts`. „Weitere Servicefunktionen" wird nach D-12
NICHT darüber hinaus ausgelegt.

Purpose: Beide Dateien tragen die REQ-19-Kreditlogik, die in Phase 9 fachlich verändert
wurde. Genau die Art von stiller Abweichung, die dort auffiel, fängt ein Unit-Test ab. Der
Nachtlauf aus Plan 09.1-04 wird `ensureOvertimeBalanceEntries()` künftig im Serverprozess
aufrufen — die Funktion braucht ein Netz, bevor sie dort landet.

Output: Zwei erweiterte Testdateien, keine Änderung an Produktivcode.

**Warum dieser Plan NACH 09.1-01 laufen muss (harte Voraussetzung, nicht Reihenfolgeempfehlung):**
Alle Tests dieses Projekts arbeiten auf derselben Datei aus `getDatabasePath()` — lokal
`server/database/development.db` (`server/vitest.config.ts` dokumentiert das ausdrücklich).
Die hier ergänzten Tests legen über `beforeEach`/`afterEach` echte Zeilen in `users`,
`absence_requests`, `overtime_balance` und `overtime_transactions` an und löschen sie wieder.
Liefe dieser Plan parallel zu 09.1-01, schrieben zwei getrennte vitest-Prozesse ohne
`busy_timeout` gleichzeitig auf dieselbe Datei — genau die Fehlerklasse (`SQLITE_BUSY` ohne
Wartezeit), die WR-03 beheben soll. `depends_on: ["09.1-01"]` ist deshalb inhaltlich
begründet und darf beim Ausführen nicht aufgeweicht werden.

**Wichtige, am Quelltext geprüfte Randbedingung:** `handleAbsenceDay()` (`:406`) und
`updateOvertimeBalanceForMonth()` (`:597`) sind **modulprivat** — die Datei exportiert nur
`rebuildOvertimeTransactionsForMonth()` (`:78`) und `getCurrentOvertimeBalance()` (`:660`).
Die Tests werden deshalb über `rebuildOvertimeTransactionsForMonth()` geführt. Die Sichtbarkeit
der beiden Funktionen wird **nicht** erweitert — das wäre eine ungetestete Änderung an
Produktivcode und ist nicht Umfang dieses Plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-CONTEXT.md
@.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-PATTERNS.md
@.claude/CLAUDE.md

<interfaces>
<!-- Am Quelltext geprüft am 2026-08-29. -->

`server/src/services/overtimeTransactionRebuildService.ts`
- `:78` `export function rebuildOvertimeTransactionsForMonth(...)` — einziger exportierter
  Einstieg in den Rebuild.
- `:219` ruft `handleAbsenceDay(...)`, `:256` ruft
  `updateOvertimeBalanceForMonth(userId, month, dailyCalculations, runningBalance)`.
- `:406` `function handleAbsenceDay(...)` (privat). Verhalten für einen genehmigten
  Abwesenheitstag ohne Zeitbuchungen:
  * Transaktion 1 immer: `type = 'time_entry'`, `hours = day.timeEntriesHours - day.targetHours`,
    Beschreibung `Abwesenheit (<typ>): Soll/Ist-Differenz`, `referenceType = 'absence'`.
  * Transaktion 2 nur wenn `type` weder `'unpaid'` noch `'overtime_comp'`:
    `getCreditType()` → `vacation` → `vacation_credit`, `sick` → `sick_credit`,
    `special` → `special_credit`; `hours = day.targetHours`; Beschreibung
    `<Gutschriftstext> <datum>` mit `vacation` → `Urlaubs-Gutschrift`.
  * Für `'unpaid'` stattdessen `type = 'unpaid_deduction'`, `hours = day.targetHours`,
    Beschreibung `Unbezahlter Urlaub Anpassung <datum>`.
  * Für `'overtime_comp'`: **keine** zweite Transaktion (REQ-19).
- `:597` `function updateOvertimeBalanceForMonth(...)` (privat):
  * `targetHours` = Summe über alle Tage, wobei Tage mit `absence.type === 'unpaid'` mit 0
    eingehen.
  * `actualHours` = Summe aus `timeEntriesHours` + `corrections`, plus `targetHours` als
    Gutschrift **nur** wenn ein Abwesenheitstyp gesetzt ist und dieser weder `'unpaid'` noch
    `'overtime_comp'` ist.
  * Schreibt per `INSERT … ON CONFLICT(userId, month) DO UPDATE` in `overtime_balance`,
    beide Werte auf zwei Nachkommastellen gerundet.

`server/src/services/overtimeService.ts:300`
`export async function ensureOvertimeBalanceEntries(userId: number, upToMonth: string): Promise<void>`
- Holt den Nutzer über `getUserById(userId)`, wirft `User not found: <id>` wenn es ihn nicht
  gibt.
- Baut einmalig `createWorkPeriodContext()`.
- Ruft `ensureYearCoverage(year)` für jedes Jahr von `hireDate` bis `upToMonth`.
- Erzeugt die Monatsliste vom Monatsersten des `hireDate` bis einschließlich `upToMonth` und
  stellt je Monat einen `overtime_balance`-Eintrag sicher.

`server/src/services/overtimeService.test.ts` — existiert bereits (120 Zeilen), enthält
`describe('ensureAbsenceTransactions — REQ-19 Schreibpfad', …)` mit eigenem Testnutzer
(`testuser_overtimesvc`, `weeklyHours: 20`, `hireDate '2026-01-01'`), `insertTestWorkPeriod`
und expliziten `DELETE`-Anweisungen in `afterEach`.

`server/src/services/overtimeTransactionRebuildService.test.ts` — existiert bereits
(377 Zeilen), `:22-55` zeigen das Setup/Teardown-Muster: `INSERT INTO users` mit
`username, email, firstName, lastName, password, role, weeklyHours, hireDate`, danach
`insertTestWorkPeriod(testUserId, { validFrom: …, weeklyHours: … })`; `afterEach` löscht
`users`, `overtime_transactions`, `overtime_balance`, `absence_requests`, `time_entries`.

`server/src/test-support/workPeriodFixtures.ts:88` `export function insertTestWorkPeriod(...)`

`server/vitest.config.ts` — `fileParallelism: false`; alle Tests laufen gegen die Datenbank
aus `getDatabasePath()` (lokal `server/database/development.db`).
`server/vitest.setup.ts` — ruft `assertNotProduction()` vor jeder Testdatei.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: overtime_comp isoliert gegen den Rebuild-Weg absichern (D-12, D-13)</name>
  <files>server/src/services/overtimeTransactionRebuildService.test.ts</files>

  <read_first>
    - `server/src/services/overtimeTransactionRebuildService.test.ts` (die zu erweiternde
      Datei — bestehende `describe`-Blöcke, Setup/Teardown-Muster in `:22-55`, damit die
      Erweiterung rein additiv bleibt)
    - `server/src/services/overtimeTransactionRebuildService.ts` Zeilen 78-270
      (`rebuildOvertimeTransactionsForMonth`, die Aufrufstellen `:219` und `:256`) sowie
      `:395-540` (`handleAbsenceDay`, `getCreditType`, `getCreditDescription`) und
      `:590-655` (`updateOvertimeBalanceForMonth`) — der Ist-Zustand, gegen den zugesichert
      wird
    - `server/src/services/unifiedOvertimeService.test.ts` Zeilen 380-415 (der zu spiegelnde
      fachliche `overtime_comp`-Block)
    - `server/src/services/overtimeLiveCalculationService.test.ts` Zeilen 1-40 (der
      WR-07-Kopfkommentar, dessen Wortlaut nach D-13 wiederverwendet wird)
    - `server/src/test-support/workPeriodFixtures.ts` (Signatur von `insertTestWorkPeriod`)
  </read_first>

  <action>
Ergänze `server/src/services/overtimeTransactionRebuildService.test.ts` rein additiv um
einen neuen `describe`-Block. Bestehende Blöcke NICHT verändern — `git diff` darf keine
Löschung in den vorhandenen Testfällen zeigen.

Benenne den Block so, dass Herkunft und Auftrag ablesbar sind, z. B.
`describe('handleAbsenceDay / updateOvertimeBalanceForMonth — overtime_comp isoliert (WR-02, D-12)', …)`.
Setze darüber einen deutschen Kommentar, der drei Dinge festhält: (a) `handleAbsenceDay()`
und `updateOvertimeBalanceForMonth()` sind modulprivat, der Test führt deshalb über
`rebuildOvertimeTransactionsForMonth()`; (b) die Fixtures werden pro Test frisch angelegt
und nicht von `development.db`-Bestandsdaten abgeleitet (Wortlaut aus
`overtimeLiveCalculationService.test.ts`, WR-07-Vermeidung, D-13); (c) die fachliche Quelle
ist REQ-19 / `09-REVIEW.md:197-209`.

**Setup (`beforeEach`)** nach dem Muster aus `:22-55` derselben Datei:
- `INSERT INTO users` mit `username = 'testuser_rebuild_otcomp'`,
  `email = 'test@rebuildotcomp.com'`, `weeklyHours = 40`, `hireDate = '2026-01-01'`,
  `role = 'employee'`.
- `insertTestWorkPeriod(testUserId, { validFrom: '2026-01-01', weeklyHours: 40 })` — damit
  liegt das Tagessoll bei `8` Stunden (40 / 5), ohne `workSchedule`.
- Keine `time_entries` anlegen. Der gesamte Monat bleibt ohne Ist-Stunden; das isoliert die
  Wirkung des einen Abwesenheitstags.

**Teardown (`afterEach`)**: explizite `DELETE`-Anweisungen für `users`,
`overtime_transactions`, `overtime_balance`, `absence_requests`, `time_entries`, je gefiltert
auf `userId = testUserId` — wörtlich wie `:47-54`.

**Prüfmonat und Prüftage:** Monat `'2026-05'`, Abwesenheitstag `'2026-05-12'` (Dienstag),
Vergleichstag `'2026-05-13'` (Mittwoch). Beide liegen vollständig in der Vergangenheit — die
Deckelung des Berechnungsendes auf heute (Plan 14.2-05) greift dort nicht. Prüfe zu Beginn
des Blocks per `getDailyTargetHours` oder per Abfrage auf `holidays`, dass beide Tage
Arbeitstage ohne Feiertag sind; ist einer belegt, weiche auf den nächsten freien Werktag
desselben Monats aus und halte die Abweichung in der SUMMARY fest — rate nicht.

**Hilfsfunktion im Block:** eine lokale Funktion, die einen genehmigten Abwesenheitsantrag
eines übergebenen Typs für einen Tag anlegt
(`INSERT INTO absence_requests (userId, type, startDate, endDate, status, days) VALUES (?, ?, ?, ?, 'approved', 1)`),
danach `rebuildOvertimeTransactionsForMonth(testUserId, '2026-05')` aufruft und die Zeilen aus
`overtime_transactions` sowie die Zeile aus `overtime_balance` zurückgibt.

**Drei Testfälle:**

1. *`overtime_comp` erzeugt genau eine Journalzeile und keine Gutschrift.* Für
   `type = 'overtime_comp'` am `'2026-05-12'`:
   - Genau `1` Zeile in `overtime_transactions` mit `date = '2026-05-12'`.
   - Diese Zeile: `type === 'time_entry'`, `hours === -8`, `referenceType === 'absence'`,
     Beschreibung genau `Abwesenheit (overtime_comp): Soll/Ist-Differenz`.
   - `0` Zeilen mit `type = 'overtime_comp_credit'` für den Nutzer im gesamten Monat.

2. *Gegenprobe `vacation` — der Test erreicht `handleAbsenceDay()` tatsächlich.* Für
   `type = 'vacation'` am selben Tag:
   - Genau `2` Zeilen mit `date = '2026-05-12'`.
   - Eine mit `type === 'time_entry'`, `hours === -8`; eine mit `type === 'vacation_credit'`,
     `hours === 8`, Beschreibung beginnt mit `Urlaubs-Gutschrift`.
   Ohne diesen Fall wäre Fall 1 nicht von einem Test zu unterscheiden, der die Funktion gar
   nicht erreicht.

3. *`updateOvertimeBalanceForMonth` behandelt die drei Typen unterschiedlich.* Führe denselben
   Tag `'2026-05-12'` nacheinander als `overtime_comp`, als `vacation` und als `unpaid` durch
   die Hilfsfunktion (dazwischen den Antrag und die Journalzeilen löschen) und vergleiche die
   Zeile aus `overtime_balance` für `'2026-05'`:
   - `targetHours` bei `overtime_comp` === `targetHours` bei `vacation` (overtime_comp
     reduziert das Soll NICHT).
   - `targetHours` bei `unpaid` === `targetHours` bei `overtime_comp` minus `8` (unbezahlter
     Urlaub reduziert das Soll).
   - `actualHours` bei `vacation` === `actualHours` bei `overtime_comp` plus `8` (nur
     `vacation` schreibt eine Ist-Gutschrift).
   - `actualHours` bei `unpaid` === `actualHours` bei `overtime_comp` (keine Gutschrift).
   Formuliere die Zusicherungen als Differenzen zwischen den drei Läufen, nicht als absolute
   Monatssummen — die absolute Zahl hängt an der Zahl der Werktage und Feiertage im Mai 2026
   und wäre eine unnötig spröde Zusicherung.

Ändere KEINEN Produktivcode. Erweitere insbesondere NICHT die Sichtbarkeit von
`handleAbsenceDay` oder `updateOvertimeBalanceForMonth`.
  </action>

  <acceptance_criteria>
    - `cd server && npx vitest run src/services/overtimeTransactionRebuildService.test.ts`
      endet mit Exitcode 0; die Zahl der bestandenen Tests ist gegenüber dem in der SUMMARY
      festgehaltenen Ausgangsstand um genau `3` gewachsen.
    - `git diff --stat server/src/services/overtimeTransactionRebuildService.ts` ist leer
      (kein Produktivcode geändert).
    - `git diff server/src/services/overtimeTransactionRebuildService.test.ts` zeigt keine
      gelöschte Zeile in den bereits vorhandenen `describe`-Blöcken (rein additiv).
    - `grep -c "overtime_comp_credit" src/services/overtimeTransactionRebuildService.test.ts`
      ≥ 1.
    - Gegenversuch geführt: Wird die Ausnahme für `'overtime_comp'` in
      `overtimeTransactionRebuildService.ts:433` probeweise entfernt (sodass auch
      `overtime_comp` eine Gutschrift erhielte), fallen Fall 1 und Fall 3 rot aus. Beide
      Testausgaben gehören in die SUMMARY; danach `git diff` auf die Produktivdatei prüfen —
      er muss leer sein.
    - `grep -rn "development.db\|production.db" src/services/overtimeTransactionRebuildService.test.ts`
      liefert keinen Treffer (D-13, D-16).
    - `cd server && npx tsc --noEmit` endet mit Exitcode 0.
  </acceptance_criteria>

  <verify>
    <automated>cd server && npx vitest run src/services/overtimeTransactionRebuildService.test.ts && npx tsc --noEmit</automated>
  </verify>

  <done>
    Drei neue Tests grün; `overtime_comp` ist gegen den Rebuild-Weg isoliert zugesichert
    (eine Zeile, keine Gutschrift, kein Soll-Abzug), `vacation` und `unpaid` stehen als
    Gegenproben daneben; Produktivcode unverändert; Gegenversuch protokolliert.
  </done>
</task>

<task type="auto">
  <name>Task 2: ensureOvertimeBalanceEntries() abdecken — der Aufrufer des Nachtlaufs (D-12, D-13)</name>
  <files>server/src/services/overtimeService.test.ts</files>

  <read_first>
    - `server/src/services/overtimeService.test.ts` (die zu erweiternde Datei — vollständig,
      120 Zeilen; das bestehende `describe('ensureAbsenceTransactions — REQ-19 Schreibpfad', …)`
      liefert Setup, Teardown und Kopfkommentar-Stil)
    - `server/src/services/overtimeService.ts` Zeilen 295-400
      (`ensureOvertimeBalanceEntries` — Monatsliste, `ensureYearCoverage`,
      `createWorkPeriodContext`, das Verhalten bei unbekanntem Nutzer)
    - `server/src/test-support/workPeriodFixtures.ts` Zeilen 80-110
      (Signatur `insertTestWorkPeriod`)
    - `.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-CONTEXT.md`, D-12/D-13
  </read_first>

  <action>
Ergänze `server/src/services/overtimeService.test.ts` rein additiv um einen zweiten
`describe`-Block, z. B.
`describe('ensureOvertimeBalanceEntries — kanonischer Aufrufer des Nachtlaufs (WR-02, D-12)', …)`.
Der bestehende Block bleibt unverändert.

Setze darüber einen deutschen Kommentar, der festhält: Diese Funktion ist der Einstieg, den
der bisherige `fix-overtime.ts`-Nachtlauf aufruft und den der In-Prozess-Scheduler aus
Plan 09.1-04 übernehmen wird; Fixtures werden je Test frisch angelegt (D-13).

**Setup (`beforeEach`)**: eigener Nutzer `username = 'testuser_ensurebalance'`,
`email = 'test@ensurebalance.com'`, `weeklyHours = 40`, `hireDate = '2026-03-10'`,
`role = 'employee'`; dazu `insertTestWorkPeriod(testUserId, { validFrom: '2026-03-10', weeklyHours: 40 })`.
Keine `time_entries`, keine `absence_requests`.

**Teardown (`afterEach`)**: explizite `DELETE`-Anweisungen auf `users`, `overtime_balance`,
`overtime_transactions`, `time_entries`, `absence_requests`, `work_time_accounts` — je auf
`userId = testUserId` gefiltert. Prüfe nach dem Löschen mit einer `COUNT(*)`-Abfrage auf
`overtime_balance`, dass `0` Zeilen zurückbleiben, und lasse den Test scheitern, wenn nicht;
`fileParallelism: false` in `vitest.config.ts` sichert, dass keine andere Datei dazwischenkommt.

**Drei Testfälle:**

1. *Lückenlose Monatskette ab dem Einstellungsmonat.* `await ensureOvertimeBalanceEntries(testUserId, '2026-05')`.
   Danach enthält `overtime_balance` für den Nutzer genau die Monate `'2026-03'`, `'2026-04'`
   und `'2026-05'` — Zeilenzahl `3`, und die sortierte Liste der `month`-Werte entspricht
   genau dieser Reihenfolge. Das ist der eigentliche Zweck der Funktion: ohne Eintrag für
   Monate ohne Stunden sammeln sich negative Überstunden nicht an.

2. *Idempotenz.* Denselben Aufruf ein zweites Mal ausführen. Danach weiterhin genau `3`
   Zeilen, und `targetHours`/`actualHours` je Monat sind gegenüber dem ersten Lauf
   unverändert (Werte vor dem zweiten Aufruf einlesen und paarweise vergleichen). Das ist die
   Zusicherung, die der tägliche Lauf braucht: er darf nichts verschieben, wenn sich nichts
   geändert hat.

3. *Unbekannter Nutzer bricht klar ab.* `ensureOvertimeBalanceEntries(<nicht vergebene id>, '2026-05')`
   lehnt mit einem Fehler ab, dessen Meldung `User not found:` enthält. Diese Zusicherung ist
   die Grundlage für die Fehlerisolierung je Nutzer aus D-05 (Plan 09.1-04): der Scheduler
   muss sich darauf verlassen können, dass ein defekter Datensatz wirft statt still nichts zu
   tun. Verwende eine id, die per Abfrage nachweislich nicht in `users` steht (z. B.
   `MAX(id) + 100000`), nicht eine geratene Zahl.

Falls `ensureYearCoverage(2026)` im Testlauf einen Netzzugriff auslöst und ohne Netz
scheitert: NICHT stillschweigend abfangen. Halte den Befund in der SUMMARY fest und wähle
die Monate so, dass nur ein Jahr betroffen ist (das ist mit `hireDate '2026-03-10'` und
`upToMonth '2026-05'` bereits der Fall).

Ändere KEINEN Produktivcode.
  </action>

  <acceptance_criteria>
    - `cd server && npx vitest run src/services/overtimeService.test.ts` endet mit Exitcode 0;
      die Zahl der bestandenen Tests ist gegenüber dem Ausgangsstand um genau `3` gewachsen.
    - `git diff --stat server/src/services/overtimeService.ts` ist leer.
    - `git diff server/src/services/overtimeService.test.ts` zeigt keine gelöschte Zeile im
      bereits vorhandenen `describe`-Block.
    - `grep -c "ensureOvertimeBalanceEntries" src/services/overtimeService.test.ts` ≥ 3.
    - `grep -rn "development.db\|production.db" src/services/overtimeService.test.ts` liefert
      keinen Treffer (D-13, D-16).
    - Nach dem vollen Lauf ist die Arbeitsdatenbank sauber:
      `sqlite3`-freie Prüfung über ein kurzes `npx tsx`-Einzeiler-Skript oder eine Abfrage im
      Test selbst zeigt `0` verbleibende Zeilen in `users` mit
      `username LIKE 'testuser_ensurebalance%'` und `username LIKE 'testuser_rebuild_otcomp%'`.
    - `cd server && npx tsc --noEmit` endet mit Exitcode 0.
    - Voller Lauf `cd server && npx vitest run`: die Menge der roten Tests ist gegenüber dem
      in der SUMMARY festgehaltenen Ausgangsstand nicht gewachsen; der Zuwachs an grünen
      Tests ist vollständig den `3 + 3` neuen Fällen zugeordnet.
  </acceptance_criteria>

  <verify>
    <automated>cd server && npx vitest run src/services/overtimeService.test.ts && npx vitest run && npx tsc --noEmit</automated>
  </verify>

  <done>
    Drei neue Tests grün: lückenlose Monatskette ab Einstellungsmonat, Idempotenz beim
    zweiten Lauf, klarer Fehler bei unbekanntem Nutzer. Produktivcode unverändert, die
    Arbeitsdatenbank nach dem Lauf ohne Testrückstände.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Testprozess → Arbeitsdatenbank (`getDatabasePath()`) | Die Tests legen echte Zeilen in `users`, `absence_requests`, `overtime_balance` und `overtime_transactions` an und löschen sie wieder. |
| `DATABASE_PATH` aus der Umgebung → Testlauf | Ein gesetztes `DATABASE_PATH` bestimmt, in welcher Datei die Tests schreiben und löschen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-06 | Tampering | `npm test` mit `DATABASE_PATH` auf die Produktionsdatenbank würde dort `DELETE FROM users` ausführen | mitigate | Bereits vorhandener Riegel: `server/vitest.setup.ts` ruft `assertNotProduction()` vor jeder Testdatei und beendet mit Exitcode 2. Dieser Plan darf ihn nicht umgehen — Abnahmekriterium: kein `vi.stubEnv('DATABASE_PATH', …)` und kein `production` in den neuen Blöcken (D-16). |
| T-09.1-07 | Tampering | Ein neuer Test lässt Zeilen in der geteilten Arbeitsdatenbank zurück und verfälscht spätere Läufe | mitigate | Explizite `DELETE`-Anweisungen in `afterEach` über alle fünf berührten Tabellen; zusätzliche `COUNT(*)`-Nachkontrolle im Teardown; Abnahmekriterium mit `username LIKE`-Prüfung nach dem vollen Lauf. |
| T-09.1-08 | Tampering | Der Test greift auf Bestandsdaten in `development.db` zurück und wird dadurch scheinbar grün (WR-07-Muster) | mitigate | D-13: eigene Nutzer, eigene Periode über `insertTestWorkPeriod`, keine `time_entries` aus dem Bestand; die Zusicherungen in Task 1 Fall 3 sind Differenzen zwischen drei selbst erzeugten Läufen, nicht absolute Bestandszahlen. |
| T-09.1-09 | Repudiation | Ein Test ist grün, ohne die geprüfte Funktion überhaupt zu erreichen | mitigate | Task 1 Fall 2 (`vacation`-Gegenprobe) und der geforderte Gegenversuch (Ausnahme probeweise entfernen → rot) belegen, dass die Zusicherung an der Funktion hängt. |
| T-09.1-10 | Information Disclosure | Testnutzer mit erratbarem Passwortfeld verbleiben in der Datenbank | mitigate | Das `password`-Feld erhält den Platzhalter `'hash'` wie in den bestehenden Tests — kein gültiger bcrypt-Hash, damit kein anmeldefähiges Konto; zusätzlich Löschung im Teardown. |
| T-09.1-SC | Tampering | npm/pip/cargo-Installationen | mitigate | Dieser Plan installiert KEIN Paket. Sollte wider Erwarten eines nötig werden, ist es vor der Installation mit einem blockierenden Checkpoint dem Anwender vorzulegen. |
</threat_model>

<verification>
- `cd server && npx vitest run` → keine neue rote Testdatei; Zuwachs genau +6 grün
- `cd server && npx tsc --noEmit` → Exitcode 0
- `git diff --stat` zeigt ausschließlich die zwei Testdateien
- Kein Zugriff auf `production.db` (D-16)
</verification>

<success_criteria>
- `handleAbsenceDay()` und `updateOvertimeBalanceForMonth()` sind über den exportierten
  Einstieg mit einem isolierten `overtime_comp`-Tag abgedeckt (D-12)
- `ensureOvertimeBalanceEntries()` ist mit Monatskette, Idempotenz und Fehlerfall abgedeckt
  (D-12)
- Alle neuen Tests legen ihre Fixtures selbst an und räumen sie weg (D-13)
- Produktivcode ist unverändert; beide Gegenversuche sind protokolliert
</success_criteria>

<output>
Erstelle `.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/09.1-02-SUMMARY.md`
</output>
