# Nachweis: Staging-Auslieferung (Green, Port 3001) — Plan 09.1-06

Gemessen am 29.08.2026, 04:48–04:58 UTC. Alle Befehle und Ausgaben sind wörtlich.

## 0. Vorgeschichte: Task-2-Entscheidung

Der Anwender wurde vor der Entscheidung mit folgenden Messwerten aus Task 1 versorgt:

- Vier Gates: `server tsc --noEmit` Exitcode 0, `desktop tsc --noEmit` Exitcode 0,
  `server vitest run` 591 grün / 4 rot (Baseline, kein neuer roter Test),
  `npm run check:rules` existiert nicht in `server/package.json`.
- D-15 je Commit gemessen: `ad48068`, `ded14d3`, `c0acb59` — alle drei bereits Vorfahren von
  `origin/main` (`git merge-base --is-ancestor` → Exitcode 0 für alle drei).
- `origin/staging` existierte zum Zeitpunkt der Entscheidung **nicht**
  (`git ls-remote --heads origin` listete ausschließlich `refs/heads/main`). Der in Option A
  beschriebene Fall „bisheriger Inhalt wird verworfen" trat damit nicht ein — der Branch wurde
  neu angelegt, nichts überschrieben. Option B (workflow_dispatch auf bestehendem
  `origin/staging`) wäre technisch nicht ausführbar gewesen, da kein Ziel-Branch existierte.

Der Anwender wählte wörtlich: **„Ja — staging anlegen"** = Option A. Diese Freigabe gilt
ausschließlich für den Zweig `staging`.

## 1. Push nach `staging`

```
$ git push origin HEAD:staging --force-with-lease
remote: Create a pull request for 'staging' on GitHub by visiting:
remote:      https://github.com/Maxwellbadger-1/TimeTracking-Clean/pull/new/staging
To https://github.com/Maxwellbadger-1/TimeTracking-Clean.git
 * [new branch]      HEAD -> staging
```

Neuer Branch angelegt (`[new branch]`), kein bestehender Inhalt überschrieben. Push nach
`main` erfolgte zu keinem Zeitpunkt.

## 2. Workflow-Lauf

```
$ gh run list --workflow="deploy-staging.yml" --limit 3
in_progress    docs(phase-09.1): update tracking after wave 3   Deploy to Staging (Green Server)   staging   push   33234698132   ...
```

Lauf-ID: **33234698132**, Auslöser: `push` auf `staging`.

```
$ gh run view 33234698132 --json status,conclusion -q '.status + " " + (.conclusion // "null")'
... (mehrfach abgefragt, 04:49:07 bis 04:54:18: in_progress)
04:54:41 completed success
```

Lauf abgeschlossen: **`completed` / `success`**, Gesamtdauer ca. 6 Minuten.

## 3. Migrationsschritt (`npm run migrate:prod`)

Wörtlich aus dem Workflow-Protokoll:

```
2026-08-29T04:54:09.5006083Z out: 🗄️  Running database migrations...
2026-08-29T04:54:10.4250216Z out: > server@0.1.2 migrate:prod
2026-08-29T04:54:10.4251187Z out: > tsx src/scripts/migrate.ts prod
2026-08-29T04:54:12.8056300Z err: ⚠️  WARNING: Running migrations on PRODUCTION database!
2026-08-29T04:54:12.8057334Z err:    Press Ctrl+C within 5 seconds to cancel...
2026-08-29T04:54:17.8097301Z out: 🗄️  Running migrations on PRODUCTION database
2026-08-29T04:54:17.8098479Z out: 📁 Database: /home/***/TimeTracking-Green/server/database.db
2026-08-29T04:54:17.8361925Z out: ✅ No pending migrations - database is up to date!
2026-08-29T04:54:17.8966730Z out: ✅ Migrations completed successfully
```

Kein `Cannot find module`, kein `ENOENT` — genau die Gefahr, die T-09.1-35 (Plan 09.1-03,
MIGRATIONS_DIR-Pfadkorrektur) verhindern sollte, ist nicht eingetreten. Damit ist das enge
Abnahmekriterium von Task 3 (b) erfüllt.

**Befund (nicht Gegenstand dieses Plans, siehe Abschnitt 8):** `src/scripts/migrate.ts prod`
migriert `server/database.db` — eine Datei, die laut anschließender Dateizeiger-Prüfung
(Abschnitt 7) **nicht** die Datenbank ist, die der laufende Serverprozess offen hält. Das
Verhalten stammt aus `databaseConfig.productionPath` (`server/src/config/database.ts:41-43`,
hartkodiert unabhängig von `NODE_ENV`) und wurde am 2026-02-10 (`ab51e858`) eingeführt — lange
vor Phase 9.1.

## 4. Schema-Validierung (nicht blockierend)

```
2026-08-29T04:54:17.8967476Z out: 🔍 Validating database schema...
2026-08-29T04:54:18.4136509Z out: > tsx src/scripts/validateSchema.ts
2026-08-29T04:54:20.2871350Z out: 🔍 Database Schema Validation
2026-08-29T04:54:20.2872773Z out: 📁 Database: /home/***/TimeTracking-Green/server/database/development.db
2026-08-29T04:54:20.4737586Z err: ❌ VALIDATION FAILED: Database schema has missing required columns!
2026-08-29T04:54:20.4744421Z out: absence_requests         ❌   Missing: approverId | Extra: adminNote, approvedBy
...
2026-08-29T04:54:20.5213419Z out: ✅ Schema validation completed
```

Wichtig: Dieser Schritt prüft **`server/database/development.db`** — also genau die Datei, die
der Server tatsächlich offen hält (siehe Abschnitt 7). Der gemeldete Mismatch ist bereits in
`.planning/phases/09.1-journal-backfill-und-betriebs-h-rtung/deferred-items.md` (Plan 09.1-03,
Befund 2) dokumentiert: `EXPECTED_SCHEMA` in `validateSchema.ts` ist gegenüber dem tatsächlichen
v3.0-Schema veraltet, nicht die Datenbank fehlerhaft. Der Workflow-Schritt ist bewusst
nicht-blockierend (`|| true`); die Abnahme verlangt nur „läuft durch, kein
Modulauflösungsfehler", nicht Exitcode 0 — erfüllt.

## 5. In-Prozess-Migrationslauf beim Serverstart (D-01, D-04)

Aus `pm2 logs timetracking-staging`, direkt beim Start um 04:54:26–27 UTC:

```
{"msg":"🔄 Starting migration system..."}
{"msg":"✅ Migrations table already exists"}
{"msg":"📁 Found 21 migration files"}
{"msg":"📊 Found 4 executed migrations"}
{"msg":"🚀 Running 9 pending migrations..."}
{"msg":"⏳ Running migration: 007_create_vacation_transactions"}
...
{"msg":"✅ Migration completed: 010_fix_user_work_periods_delete_guard"}
```

Der In-Prozess-Migrationsläufer (`server/src/database/migrationRunner.ts`, aufgerufen aus
`server.ts:217`) hat 9 ausstehende TypeScript-Migrationen gegen die reale, vom Server
verwendete `development.db` angewendet — unabhängig vom (auf die falsche Datei zeigenden)
`migrate:prod`-Skript aus Abschnitt 3. Das ist der Mechanismus, der für die tatsächliche
Schemaaktualität des Green-Servers maßgeblich ist, und er hat fehlerfrei gearbeitet.

Eine geloggte `SqliteError: no such column: reversalOf` beim vorherigen Indexversuch ist
laut Code-Kommentar selbst erwartetes Verhalten (`server/src/database/schema.ts:672`): der
Index wird vor der zugehörigen Migration 014 versucht, der Fehler abgefangen, und
`runMigrations()` legt die Spalte anschließend nach. Kein Befund.

## 6. Health-Check

```
$ curl -s http://129.159.8.19:3001/api/health
{"status":"ok","message":"TimeTracking Server is running","version":"0.1.0","timestamp":"2026-08-29T04:57:26.680Z"}
```

HTTP 200 (separat geprüft via `-w "%{http_code}"` im Workflow selbst: `✅ Deployment
successful! Server is healthy`).

## 7. Drei Protokollzeilen — Scheduler, Anlauf, Bilanz (D-01, D-02, D-05)

Wörtlich aus `pm2 logs timetracking-staging --lines 300 --nostream`:

**Scheduler-Start (03:15 Europe/Berlin):**
```
{"level":30,"time":1787979267621,"env":"staging","msg":"✅ Overtime recalc scheduler started (daily at 03:15 AM Europe/Berlin)"}
```

**Anlaufstart (`anlass: serverstart`):**
```
{"level":30,"time":1787979267631,"env":"staging","anlass":"serverstart","gesamt":1,"msg":"🔄 Überstunden-Neuberechnungslauf gestartet (serverstart)"}
```

**Bilanzzeile (`verarbeitet`/`fehlgeschlagen`):**
```
{"level":30,"time":1787979267912,"env":"staging","anlass":"serverstart","gesamt":1,"verarbeitet":1,"fehlgeschlagen":0,"dauerMs":284,"msg":"✅ Überstunden-Neuberechnungslauf beendet: 1 verarbeitet, 0 fehlgeschlagen (serverstart)"}
```

Alle drei Belege vorhanden. `gesamt: 1` entspricht der Nutzerzahl der Green-`development.db`
(ein Seed-Admin-Nutzer, `hireDate: 2026-04-02`) — Green läuft NICHT mit realen
Produktionsdaten, sondern mit der lokalen Entwicklungsdatenbank, da `deploy-staging.yml` kein
`DATABASE_PATH` setzt (siehe Abschnitt 3/8).

## 8. crontab (D-03)

```
$ ssh -i .ssh/oracle_server.key ubuntu@129.159.8.19 "crontab -l"
0 2 * * 0 /home/ubuntu/TimeTracking-Staging/server/scripts/sync-prod-to-staging.sh >> /home/ubuntu/logs/db-sync.log 2>&1
```

Genau eine Zeile. `grep -c fix-overtime` = **0**, `grep -c sync-prod-to-staging` = **1**. Beide
Zusicherungen erfüllt: der externe Handbetriebs-Cronjob ist verschwunden, der fremde
wöchentliche Sync-Job unverändert.

## 9. Dateizeiger (Vorstufe zu D-17)

```
$ ssh ... "pm2 list"
│ 44 │ timetracking-staging │ default │ 0.1.2 │ fork │ 3745263 │ 3m │ 0 │ online │ ... │

$ ssh ... "date -u; PID=3745263; ls -l /proc/$PID/fd | grep -i '\.db'"
Sat Aug 29 04:58:21 UTC 2026
lrwx------ 1 ubuntu ubuntu 64 Aug 29 04:58 19 -> /home/ubuntu/TimeTracking-Green/server/database/development.db
lrwx------ 1 ubuntu ubuntu 64 Aug 29 04:58 20 -> /home/ubuntu/TimeTracking-Green/server/database/development.db-wal
lrwx------ 1 ubuntu ubuntu 64 Aug 29 04:58 21 -> /home/ubuntu/TimeTracking-Green/server/database/development.db-shm
```

PID 3745263, Zeitstempel 2026-08-29 04:58:21 UTC. Kein Dateizeiger trägt `(deleted)`. Referenzpunkt
für die Beobachtung aus Plan 09.1-08.

Ergänzend (rein `ls -la`, kein Datenbankzugriff), zur Einordnung des Befunds aus Abschnitt 3:

```
$ ssh ... "ls -la /home/ubuntu/TimeTracking-Green/server/database.db* /home/ubuntu/TimeTracking-Green/server/database/"
-rw-r--r-- 1 ubuntu ubuntu 831488 Apr  2 09:09 /home/ubuntu/TimeTracking-Green/server/database.db
...
-rw-r--r-- 1 ubuntu ubuntu  372736 Apr  3 00:00 development.db
-rw-r--r-- 1 ubuntu ubuntu   32768 Aug 29 04:54 development.db-shm
-rw-r--r-- 1 ubuntu ubuntu 1520312 Aug 29 04:54 development.db-wal
```

`server/database.db` (das Ziel von `migrate:prod`) trägt den Zeitstempel 2. April — seit dem
letzten Neuanlegen von Green unangetastet. `development.db-wal`/`-shm` (die reale Arbeitsdatenbank)
wurden gerade eben (04:54) durch diesen Deployment-Lauf beschrieben. Bestätigt den Befund aus
Abschnitt 3 als rein lesend, ohne die Dateien zu öffnen.

## 10. Fachlicher Funktionstest

Kein Desktop-UI verfügbar; Test über die API des laufenden Green-Servers, mit dem
Seed-Admin-Zugang (`server/src/database/seed.ts:44-54`, `admin` / `admin123`, ausschließlich
für Entwicklungsdatenbanken bestimmt):

```
$ curl -s -X POST http://129.159.8.19:3001/api/auth/login -H "Content-Type: application/json" \
    -d '{"username":"admin","password":"admin123"}'
{"success":true,"data":{"id":1,"username":"admin",...},"token":"eyJ...","message":"Login successful"}
HTTP: 200

$ curl -s "http://129.159.8.19:3001/api/overtime/all?year=2026" -H "Authorization: Bearer $TOKEN"
{"success":true,"data":[{"userId":1,"firstName":"System","lastName":"Administrator",
  "email":"admin@timetracking.local","targetHours":808,"actualHours":0,"totalOvertime":-808}]}
HTTP: 200
```

Geprüfte Ansicht: „Alle Überstunden" (Admin-Übersicht, `GET /api/overtime/all`). Ergebnis:
HTTP 200, plausibler Datensatz für den einzigen Seed-Nutzer (Soll 808 h seit Einstellung
2026-04-02, keine gebuchten Zeiten, Überstunden −808 h) — konsistent mit einer leeren
Testdatenbank ohne Zeitbuchungen. Kein Fehler, kein 500er.

## 11. D-16 — Kein Zugriff auf production.db

**Kein Zugriff auf production.db in diesem Plan.** Vollständige Liste aller ausgeführten
SSH-/Netzwerkkommandos dieses Plans (siehe auch SUMMARY, Abschnitt „SSH-Kommandos"):

1. `git push origin HEAD:staging --force-with-lease`
2. `gh run list --workflow="deploy-staging.yml" --limit 3`
3. `gh run view 33234698132 --json status,conclusion -q '...'` (mehrfach, Poll-Schleife)
4. `gh run view 33234698132 --log`
5. `curl -s http://129.159.8.19:3001/api/health`
6. `ssh -i .ssh/oracle_server.key ubuntu@129.159.8.19 "pm2 logs timetracking-staging --lines 300 --nostream"`
7. `ssh -i .ssh/oracle_server.key ubuntu@129.159.8.19 "crontab -l"`
8. `ssh -i .ssh/oracle_server.key ubuntu@129.159.8.19 "date -u; PID=...; ls -l /proc/$PID/fd | grep '\.db'"` (schlug wegen fehlendem `jq` fehl, kein Datenbankzugriff erfolgt)
9. `ssh -i .ssh/oracle_server.key ubuntu@129.159.8.19 "pm2 list"`
10. `ssh -i .ssh/oracle_server.key ubuntu@129.159.8.19 "date -u; PID=3745263; ls -l /proc/$PID/fd 2>&1 | grep -i '\.db'"`
11. `ssh -i .ssh/oracle_server.key ubuntu@129.159.8.19 "ls -la /home/ubuntu/TimeTracking-Green/server/database.db* /home/ubuntu/TimeTracking-Green/server/database/"`
12. `curl -s -X POST http://129.159.8.19:3001/api/auth/login ...`
13. `curl -s "http://129.159.8.19:3001/api/overtime/all?year=2026" ...`

Keines dieser Kommandos öffnet, kopiert oder liest `/home/ubuntu/databases/production.db`.
Jeder Datenbankzugriff dieses Plans lief entweder über die HTTP-API des laufenden
Green-Servers oder war Bestandteil des unveränderten `deploy-staging.yml`-Workflows selbst
(GitHub-Actions-Runner, nicht dieser Ausführer). `ls -l`/`ls -la` auf Dateipfade zählt laut
`.claude/CLAUDE.md` §Datenbank-Regeln 3 nicht als Datenbankzugriff.

Der SSH-Schlüssel wurde ausschließlich per `-i .ssh/oracle_server.key` an `ssh` übergeben und
erscheint an keiner anderen Stelle dieses Dokuments oder eines Commits.
