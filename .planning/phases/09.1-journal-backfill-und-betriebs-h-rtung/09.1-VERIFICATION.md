---
phase: 09.1-journal-backfill-und-betriebs-h-rtung
verified: 2026-09-02T21:44:25Z
status: passed
score: 3/3 must-haves verified
overrides_applied: 0
deferred:
  - truth: "Nach dem Backfill zeigt validate:overtime:detailed für eine Stichprobe realer Nutzer/Monate keine Transaktions-Abweichung mehr"
    addressed_in: "Phase 14 (Plan 14-10)"
    evidence: "ROADMAP.md, Abschnitt 'Einordnung nach Plan 14-07': Backfill lief 26.08.2026 im Produktionsfenster der Phase 14, Beleg 14-BACKFILL-PRODUKTION.md. ROADMAP.md schreibt ausdrücklich: 'nicht Umfang dieser Phase (09.1-CONTEXT.md <domain>)'."
---

# Phase 9.1: Journal-Backfill und Betriebs-Härtung — Verification Report

**Phase Goal (Ist-Umfang nach Aufteilung, ROADMAP.md „Einordnung nach Plan 14-07"):** Die
Betriebsskripte, die täglich unbeobachtet gegen die Produktion laufen, sind gegen die in
`09-REVIEW.md` gefundenen Restrisiken WR-02 bis WR-05 abgesichert. Der Journal-Backfill selbst
ist per Anwenderentscheidung vom 22.08.2026 aus dieser Phase herausgelöst und lief am
26.08.2026 im Produktionsfenster der Phase 14 (Beleg dort).

**Verified:** 2026-09-02T21:44:25Z
**Status:** passed
**Re-verification:** No — initial verification

## Wichtiger Hinweis zum Geltungsbereich

`.planning/ROADMAP.md` legt für Phase 9.1 ausdrücklich vier ursprüngliche Erfolgskriterien
fest, weist aber im Abschnitt „Einordnung nach Plan 14-07" (Zeile 179) an, dass der
Journal-Backfill (erstes Kriterium) **nicht** Umfang dieser Phase ist, sondern im
Produktionsfenster der Phase 14 lief. Dieser Bericht prüft deshalb nur die **drei** laut
Anweisung tatsächlich zu dieser Phase gehörenden Kriterien. Das Backfill-Kriterium ist unter
`deferred` in der Frontmatter geführt, nicht als Lücke.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `fix-overtime.ts` verarbeitet einen einzelnen defekten Datensatz mit Fehlerprotokoll, ohne die übrige Belegschaft in diesem Lauf zu blockieren | ✓ VERIFIED | `server/src/services/overtimeRecalcRunner.ts` läuft eine Nutzerschleife mit `try/catch` je Nutzer (kein `throw`/`break`), protokolliert jeden Fehler strukturiert mit `userId` (`logger.error`) und liefert am Ende eine Bilanz `{gesamt, verarbeitet, fehlgeschlagen, fehler[]}`. `server/src/services/overtimeRecalcRunner.test.ts` — 2/2 Tests grün (selbst ausgeführt): ein garantiert nicht vergebener Nutzer und ein gültiger Testnutzer laufen im selben Aufruf, defekte id zuerst; `verarbeitet=1, fehlgeschlagen=1`, Fehlermeldung „User not found" korrekt zugeordnet, der gültige Nutzer wird trotzdem verarbeitet. `server/src/scripts/fixOvertime.ts` (das Handbetriebs-Werkzeug) ruft ausschließlich diese Funktion auf, keine zweite Kopie der Schleife. Beide Dateien sind Bestandteil des produktiv laufenden Commits `e4866a4` (Ahnenverifikation: `git merge-base --is-ancestor e4866a4 130d252` → true; `130d252` ist der zuletzt gepushte/deploybare Produktionsstand). |
| 2 | Ein `PRAGMA busy_timeout` ist gesetzt und mit einem echten Nebenläufigkeitstest belegt | ✓ VERIFIED | `server/src/database/connection.ts:17,48-61` — `BUSY_TIMEOUT_MS = 5000`, gesetzt mit `database.pragma('busy_timeout = ' + BUSY_TIMEOUT_MS)` VOR `initializeDatabase(database)`, gegengelesen, wirft bei Abweichung. `server/src/database/connection.busyTimeout.test.ts` — 3/3 Tests grün (selbst ausgeführt: `npx vitest run src/database/connection.busyTimeout.test.ts`): (1) Konfigurationswert 5000, (2) echter Wartenachweis über einen Kindprozess als Sperrhalter (`spawn`, `better-sqlite3` synchron blockierend) — die zweite Verbindung wartet gemessen ≥300 ms und schreibt danach erfolgreich, (3) Gegenprobe mit `busy_timeout=0` auf einer rohen Verbindung wirft sofort mit `code==='SQLITE_BUSY'`. Der Kontrast aus (2) und (3) belegt kausal, dass das Pragma die Ursache des Wartens ist, nicht ein Konfigurations-Check allein. Ebenfalls im produktiven Commit `e4866a4` enthalten. |
| 3 | `server/scripts/**` läuft unter `tsc --noEmit` | ✓ VERIFIED | `server/scripts/` enthält nach `ls` nur noch `14-ist-stand-report.mjs`, `14-ist-stand-vergleich.mjs`, `sync-prod-to-staging.sh` — keine `.ts`-Datei mehr (13 unreferenzierte Legacy-Skripte gelöscht, vier aktiv genutzte Skripte plus `fixOvertime.ts` nach `server/src/scripts/` verschoben, dort unter `include: ["src/**/*"]` von `server/tsconfig.json`). Selbst ausgeführt: `cd server && npx tsc --noEmit` → Exitcode 0. Damit liegt kein TypeScript mehr außerhalb der `tsc`-Abdeckung — das Kriterium ist im wörtlichen wie im praktischen Sinn erfüllt (kein verbleibendes `server/scripts/**`-TS-File, das geprüft werden müsste). Ebenfalls im produktiven Commit `e4866a4` enthalten (`git ls-tree -r e4866a4 --name-only | grep '^server/scripts/'` liefert dieselben drei Nicht-TS-Dateien). |

**Score:** 3/3 truths verified

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | „Nach dem Backfill zeigt `validate:overtime:detailed` … keine Transaktions-Abweichung mehr" | Phase 14 (Plan 14-10) | ROADMAP.md, Abschnitt „Einordnung nach Plan 14-07": Backfill lief 26.08.2026 im Fenster der Phase 14 (`14-BACKFILL-PRODUKTION.md`), ausdrücklich als „nicht Umfang dieser Phase" markiert. WR-07 (`overtimeLiveCalculationService.test.ts`-Fixture-Abhängigkeit, ROADMAP-Umfangstext) ist laut derselben ROADMAP-Passage bereits durch Plan 14-02 geschlossen — ebenfalls kein Gegenstand dieser Verifikation. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `server/src/database/connection.ts` | `busy_timeout`-Pragma zentral, gesetzt-verifiziert-protokolliert | ✓ VERIFIED | Gelesen; Zeilen 17, 48-61 exakt wie geplant, Muster *setzen → gegenlesen → werfen → protokollieren* aus `schema.ts` übernommen |
| `server/src/database/connection.busyTimeout.test.ts` | Echter Nebenläufigkeitsnachweis mit Gegenprobe | ✓ VERIFIED | 221 Zeilen, 3/3 Tests grün, Kindprozess-basierter Sperrhalter, `SQLITE_BUSY`-Gegenprobe |
| `server/src/services/overtimeRecalcRunner.ts` | Fehlerisolierte Nutzerschleife, gemeinsam genutzt | ✓ VERIFIED | `try/catch` je Nutzer, Bilanzobjekt, kein `new Database`, keine `db.close()`-Berührung |
| `server/src/services/overtimeRecalcRunner.test.ts` | Nachweis der Fehlerisolierung | ✓ VERIFIED | 2/2 Tests grün, defekter + gültiger Nutzer im selben Lauf |
| `server/src/services/cronService.ts` | Scheduler 03:15 Europe/Berlin | ✓ VERIFIED | `startOvertimeRecalcScheduler()`, `'15 3 * * *'`, `timezone: 'Europe/Berlin'`, ruft `runOvertimeRecalcForAllUsers` |
| `server/src/server.ts` | Registrierung + einmaliger Anlauf nach Serverstart | ✓ VERIFIED | `startOvertimeRecalcScheduler()` und `runOvertimeRecalcForAllUsers({anlass:'serverstart'})` importiert und aufgerufen |
| `server/src/scripts/fixOvertime.ts` | Gehärtetes Handbetriebs-Werkzeug | ✓ VERIFIED (mit Anmerkung) | Produktionsschutz vor jedem DB-Import, `--allow-production`, `--userId`; ruft ausschließlich `runOvertimeRecalcForAllUsers`. **Anmerkung:** Der ursprüngliche Plan-Vorgabesatz „`db.close()` steht in einem finally-Block" trifft auf den aktuellen Code NICHT mehr zu — das Review (`09.1-REVIEW.md`, CR-02) hat diesen ursprünglichen Ansatz als Fehler identifiziert (`db.close()` löst dieselbe WAL-Abhäng-Ursache aus, die die Phase gerade beseitigen soll) und lokal korrigiert (`db.close()` entfernt, Kommentar erklärt Grund). Der lokale Fix ist im Arbeitsverzeichnis vorhanden, aber laut `git status` 14 Commits vor `origin/main` — **noch nicht in Produktion ausgeliefert** (Produktion läuft auf `130d252`, der Fix liegt im lokalen, nicht gepushten Commit `80da0b7`). Siehe Abschnitt „Anti-Patterns" unten. |
| `server/scripts/**` (Restbestand) | Keine `.ts`-Dateien mehr | ✓ VERIFIED | Nur `.mjs`/`.sh` übrig |
| `server/package.json` | Aufrufpfade auf `src/scripts/` | ✓ VERIFIED | `grep` bestätigt `tsx src/scripts/…` für migrate/seed/sync-db/validateSchema/fixOvertime |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `cronService.ts` | `overtimeRecalcRunner.ts` | `runOvertimeRecalcForAllUsers()` im Cron-Callback | ✓ WIRED | `grep` bestätigt Import und Aufruf in `startOvertimeRecalcScheduler()` |
| `server.ts` | `overtimeRecalcRunner.ts` | einmaliger, nicht blockierender Anlauf nach Serverstart | ✓ WIRED | `grep` bestätigt Import und `.catch()`-behafteten Aufruf |
| `overtimeRecalcRunner.ts` | `overtimeService.ts` | `ensureOvertimeBalanceEntries(userId, monat)` | ✓ WIRED | Import und Aufruf im Code bestätigt, Test deckt beide Pfade (Erfolg/Fehler) ab |
| `fixOvertime.ts` | `overtimeRecalcRunner.ts` | `await import('../services/overtimeRecalcRunner.js')` NACH Produktionsschutz | ✓ WIRED | Reihenfolge im Code bestätigt: `assertNotProduction()` synchron vor dem dynamischen Import |
| `connection.ts` | better-sqlite3-Verbindungsobjekt | `database.pragma('busy_timeout = 5000')` vor `initializeDatabase()` | ✓ WIRED | Zeilenreihenfolge per `grep -n` bestätigt (51 vor 64) |

### Data-Flow Trace (Level 4)

Nicht anwendbar im klassischen Sinn (kein UI-Rendering). Der relevante Datenfluss —
`runOvertimeRecalcForAllUsers()` liest echte Nutzer aus `db.prepare('SELECT id FROM users
WHERE deletedAt IS NULL')` (kein hartkodiertes Array) und schreibt über
`ensureOvertimeBalanceEntries()` echte Zeilen in `overtime_balance`/`overtime_transactions` —
wurde im Test durch die Assertion `overtime_balance`-Zeilenzahl `≥1` nach dem Lauf sowie durch
das reale Produktionsprotokoll bestätigt (`15/15 verarbeitet, 0 fehlgeschlagen`,
`09.1-NACHWEIS-BEOBACHTUNG.md`, zwei Nächte).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| busy_timeout real waiting + counter-proof | `cd server && npx vitest run src/database/connection.busyTimeout.test.ts` | 3/3 Tests grün, Wartedauer ≥300ms, Gegenprobe `SQLITE_BUSY` sofort | ✓ PASS |
| Fehlerisolierung je Nutzer | `cd server && npx vitest run src/services/overtimeRecalcRunner.test.ts` | 2/2 Tests grün, `verarbeitet=1, fehlgeschlagen=1` bei defektem + gültigem Nutzer im selben Lauf | ✓ PASS |
| `server/scripts/**` unter `tsc` | `cd server && npx tsc --noEmit` | Exitcode 0 | ✓ PASS |
| Regressionsschutz (Plan-02/05-Tests) | `npx vitest run src/services/overtimeService.test.ts src/services/overtimeTransactionRebuildService.test.ts src/scripts/productionGuard.test.ts` | 3 Dateien / 30 Tests grün | ✓ PASS |
| Voller Serverseitiger Testlauf | `cd server && npx vitest run` | 46/50 Dateien, 590/595 Tests grün, 5 rot | ⚠️ Siehe Anmerkung unten |

**Anmerkung zum vollen Testlauf:** Der volle Lauf zeigt aktuell **5** rote Tests, nicht die in
den SUMMARYs dokumentierten 3-4. Geprüft: Alle 5 betroffenen Dateien
(`overtimeCompFutureCommitment.test.ts`, `unifiedOvertimeService.test.ts` [2 Fälle],
`vacationBackfillService.test.ts`, `workPeriodChangeService.test.ts`) stehen in **keinem**
`files_modified`-Abschnitt eines der acht Phase-9.1-Pläne — die Phase hat sie nicht berührt.
Alle fünf Fälle referenzieren wörtlich `HEUTE`/„today"/`getTodayString()`/kalenderrelative
Fixdaten (z. B. „Feb 6 (today)"), sind also wanduhr-abhängig und driften mit dem
Systemdatum — zum Zeitpunkt der Phasenausführung (29.08.2026) war es „Feb 6" o. ä. noch
nicht, heute (02.09.2026, 4 Tage später) sind es andere Werte. Das ist dieselbe Klasse von
Testfragilität, die `09.1-REVIEW.md` unter WR-09 bereits unabhängig dokumentiert
(„feste Benutzernamen … im selben Repo bereits als Flakiness-Ursache dokumentiert" —
hier zusätzlich Datumsfragilität). Kein Bezug zu `connection.ts`, `overtimeRecalcRunner.ts`,
`cronService.ts`, `server.ts`, `fixOvertime.ts` oder den verschobenen Skripten. Nicht als
Phasenregression gewertet, aber als bekanntes, unbehandeltes Betriebsrisiko festgehalten.

### Probe Execution

Keine dedizierten `scripts/*/tests/probe-*.sh`-Dateien für diese Phase gefunden (`find
scripts -path '*/tests/probe-*.sh'` liefert nichts Phasenrelevantes). Die faktischen Probes
dieser Phase sind die Vitest-Suiten oben, alle selbst ausgeführt.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| WR-03 | 09.1-01 | `busy_timeout` zentral + Nebenläufigkeitstest | ✓ SATISFIED | Truth 2 oben |
| WR-02 (Restumfang) | 09.1-02 | `overtime_comp`/`ensureOvertimeBalanceEntries()` Tests | ✓ SATISFIED | `overtimeService.test.ts`, `overtimeTransactionRebuildService.test.ts` — 7 Tests grün, selbst ausgeführt |
| WR-04 | 09.1-03, 09.1-05 | `server/scripts/**` unter `tsc` | ✓ SATISFIED | Truth 3 oben |
| WR-05 | 09.1-04, 09.1-05 | Nachtlauf in Serverprozess, Fehlerisolierung | ✓ SATISFIED | Truth 1 oben |
| REQ-19 (Restumfang Phase 9.1) | — | Journal-Backfill | ⏭ DEFERRED | Siehe „Deferred Items" — läuft in Phase 14, dort per `14-BACKFILL-PRODUKTION.md` belegt |

Keine verwaisten (orphaned) Requirements gefunden — `grep -E "Phase 9.1" .planning/REQUIREMENTS.md`
liefert keinen zusätzlichen, in keinem Plan referenzierten Eintrag.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | Keine `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` in den neun geprüften Phase-9.1-Kerndateien (`connection.ts`, `overtimeRecalcRunner.ts`, `fixOvertime.ts`, `migrate.ts`, `seed.ts`, `sync-db.ts`, `validateSchema.ts`, `cronService.ts`, `server.ts`) | — | ℹ️ INFO | Sauber |
| `server/src/scripts/fixOvertime.ts` (Produktionsstand, Commit `e4866a4`/`130d252`) | 163-167 (Alt-Zustand) | `db.close()` in `finally` — von `09.1-REVIEW.md` (CR-02) als der Mechanismus identifiziert, der die WAL abhängt, wenn das Werkzeug mit `--allow-production` manuell gegen `production.db` läuft | ⚠️ WARNING | Lokal bereits behoben (Commit `80da0b7`, nicht gepusht) — die **produktiv laufende** Fassung des Handbetriebs-Werkzeugs trägt den Bug noch. Betrifft nur den manuellen `--allow-production`-Pfad, nicht den automatischen Nachtlauf (der läuft bereits fehlerfrei im Serverprozess, D-17 belegt). Betrifft keine der drei geprüften Erfolgskriterien dieser Phase direkt, ist aber ein bekanntes Betriebsrisiko, das der Anwender kennen sollte, bevor er das Werkzeug manuell gegen Produktion einsetzt. |
| 5 Testdateien außerhalb des Phase-Umfangs | diverse | Kalenderrelative Testfixtures (`HEUTE`/`getTodayString()`) | ⚠️ WARNING (vorbestehend, nicht Phase-9.1-verursacht) | Testsuite-Fragilität, bereits in `09.1-REVIEW.md` WR-09 als Muster benannt; nicht Gegenstand dieser Phase, aber relevant für künftige Regressionsläufe |

### Human Verification Required

Keine. Alle drei anwendbaren Erfolgskriterien sind code- und testbar verifizierbar und wurden
gegen den tatsächlichen Quelltext sowie gegen selbst ausgeführte Tests geprüft. Die
betriebliche WAL-Beobachtung (D-17) ist bereits als eigenständiger, mit Rohdaten belegter
Nachweis (`09.1-NACHWEIS-BEOBACHTUNG.md`) durchgeführt und ehrlich mit eigenen Grenzen
versehen (Abschnitt 6 dort) — sie war nicht erneut zu wiederholen, da dieser Verifier laut
Auftrag nicht mit der Produktionsdatenbank oder dem Server interagieren darf.

### Gaps Summary

Keine Lücken innerhalb des für diese Phase gültigen, per ROADMAP-Aufteilung (Plan 14-07)
verbindlich verengten Umfangs. Alle drei anwendbaren Erfolgskriterien sind im aktuellen
Arbeitsverzeichnis **und** im produktiv ausgelieferten Commit `e4866a4` (Vorfahre des
zuletzt gepushten Stands `130d252`) nachweisbar erfüllt:

1. Fehlerisolierung je Nutzer im Neuberechnungslauf — verifiziert per Quelltext und Test.
2. `busy_timeout` mit echtem Nebenläufigkeitsnachweis — verifiziert per Quelltext und Test.
3. `server/scripts/**` unter `tsc --noEmit` — verifiziert per Verzeichnisinhalt und Testlauf.

Zwei Punkte außerhalb der formalen Kriterien verdienen dennoch Aufmerksamkeit des Anwenders,
ohne den Phasenabschluss zu blockieren:

- Der lokale, 14 Commits umfassende, **nicht gepushte** Korrekturstand aus Quick Task
  `260831-t5j` (u. a. CR-02: `db.close()`-Entfernung aus `fixOvertime.ts`, WR-05:
  Signalbehandler-Reihenfolge im Serverstart, CR-04, WR-14, WR-02, WR-03, WR-07) ist im
  Arbeitsverzeichnis vorhanden, aber noch nicht in Produktion ausgeliefert. Der Push wurde
  laut `.planning/STATE.md` bewusst zurückgestellt, um die laufende D-17-Beobachtung nicht ein
  drittes Mal durch einen PID-Wechsel zurückzusetzen — eine nachvollziehbare, dokumentierte
  Entscheidung, aber ein offener operativer Schritt.
- 12 der ursprünglich 28 Review-Befunde aus `09.1-REVIEW.md` (WR-01, WR-04, WR-06, WR-08 bis
  WR-13, WR-15, sowie alle 8 INFO-Punkte) sind laut `.planning/STATE.md` weiterhin
  unbearbeitet. Keiner davon widerspricht einem der drei geprüften Erfolgskriterien dieser
  Phase; sie sind als Nachfolgearbeit dokumentiert, nicht verschwiegen.

---

*Verified: 2026-09-02T21:44:25Z*
*Verifier: Claude (gsd-verifier)*
