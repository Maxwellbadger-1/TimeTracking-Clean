---
phase: 09.1-journal-backfill-und-betriebs-h-rtung
plan: 05
subsystem: infra
tags: [typescript, cli-tooling, github-actions, cron, production-guard, overtime]

# Dependency graph
requires:
  - phase: 09.1-03
    provides: "server/scripts/ aufgeloest — migrate.ts/seed.ts/sync-db.ts/validateSchema.ts liegen unter tsc-Abdeckung; fix-overtime.ts blieb bewusst als letzte TS-Datei fuer diesen Plan liegen"
  - phase: 09.1-04
    provides: "runOvertimeRecalcForAllUsers() in overtimeRecalcRunner.ts — die eine kanonische Nutzerschleife, die Scheduler, Serverstart-Anlauf und jetzt auch das Handbetriebs-Werkzeug aufrufen"
provides:
  - "server/src/scripts/fixOvertime.ts — gehaertetes Handbetriebs-Werkzeug mit Produktionsschutz VOR jedem datenbankberuehrenden Import, --allow-production, --userId (mehrfach), db.close() im finally"
  - "server/scripts/ enthaelt keine TypeScript-Datei mehr — WR-04 vollstaendig geschlossen"
  - "Beide Deploy-Workflows (Blue/Green) ohne crontab-Neueintrag und ohne Post-Deploy-Lauf — der Serverprozess ist der einzige Schreiber auf production.db (WR-05 abgeschlossen)"
  - "Bereinigungsfilter fix-overtime.ts bleibt in beiden Workflows stehen, damit sich vorhandene Server selbst aufraeumen (D-03)"
affects: ["09.1-06 (Nachweis am laufenden Server, dass Scheduler und Bereinigung wie erwartet greifen)"]

tech-stack:
  added: []
  patterns:
    - "CLI-Werkzeuge auf production.db: nur importsichere Module (path, config, productionGuard) statisch, alles Datenbankberuehrende per await import() NACH assertNotProduction() — wiederholtes Muster aus backfillOvertimeJournal.ts"
    - "Deploy-Workflow-Bereinigungsschritte behalten den lesenden/entfernenden Teil (crontab -l | grep -v ... | crontab -), streichen aber den anhaengenden Neueintrag — ein Workflow raeumt sich selbst auf, statt manuellen Servereingriff zu verlangen"

key-files:
  created:
    - server/src/scripts/fixOvertime.ts
  modified:
    - server/package.json
    - .github/workflows/deploy-server.yml
    - .github/workflows/deploy-staging.yml
  deleted:
    - server/scripts/fix-overtime.ts

key-decisions:
  - "Kommentare in fixOvertime.ts vermeiden das woertliche Substring 'dist/' (Abnahmekriterium grep -c \"dist/\" = 0) und beschreiben den abgeloesten Import stattdessen als 'kompiliertes Ausgabeverzeichnis' — gleiches Prinzip wie die Verbots-Substring-Vermeidung in overtimeRecalcRunner.ts (Plan 09.1-04)"
  - "In den Workflow-Kommentaren wird der Dateiname 'fix-overtime.ts' nur einmal je Datei erwaehnt (im Filterausdruck selbst) — Prosa-Erwaehnungen wurden umformuliert ('das ehemalige Handbetriebs-Skript'), damit die Zaehlpruefung grep -c \"fix-overtime.ts\" = 1 nicht durch beschreibenden Text verfaelscht wird; ebenso wurde 'sync-prod-to-staging.sh' durch eine Umschreibung ersetzt, damit grep -c \"sync-prod-to-staging\" = 0 bleibt"
  - "development.db existierte in diesem frischen Worktree nicht (gitignored, jeder Worktree hat seinen eigenen Zustand) — vor dem Funktionsnachweis per fixOvertime.ts-Lauf (ohne --userId) frisch ueber initializeDatabase() aufgebaut, danach ein einzelner Testnutzer ueber die reguläre createUser()-Serviceroute angelegt (inkl. Startperiode), nicht per Roh-SQL — der mitgelieferte Seed 001_test_user_data.sql scheiterte an einem FOREIGN-KEY-Konflikt (vorbestehend, nicht Gegenstand dieses Plans, nicht behoben)"

patterns-established:
  - "fixOvertime.ts ist das einzige verbleibende Skript, das runOvertimeRecalcForAllUsers() manuell mit expliziten --userId-Werten aufrufen kann — Scheduler und Serverstart-Anlauf laufen immer ueber alle Nutzer"

requirements-completed: [WR-04, WR-05]

duration: ~50min
completed: 2026-08-29
---

# Phase 9.1 Plan 05: Handbetriebs-Werkzeug gehärtet, beide automatischen Aufrufer entfernt Summary

**`fixOvertime.ts` ersetzt `fix-overtime.ts` als gehärtetes, produktionssicheres Handbetriebs-Werkzeug ohne automatischen Aufrufer; beide Deploy-Workflows verlieren ihren crontab-Neueintrag und ihren Post-Deploy-Lauf, der Bereinigungsfilter bleibt stehen — der Serverprozess ist jetzt der einzige Schreiber auf `production.db`.**

## Performance

- **Duration:** ca. 50 min
- **Tasks:** 2/2
- **Files modified:** 1 neu, 1 gelöscht, 3 geändert

## Accomplishments

- `server/src/scripts/fixOvertime.ts`: Produktionsschutz (`assertNotProduction()`) läuft synchron vor jedem `await import()` eines datenbankberührenden Moduls; `db.close()` steht im `finally`-Block und läuft nachweislich auch im Fehlerpfad; ruft ausschließlich die kanonische `runOvertimeRecalcForAllUsers()` aus Plan 09.1-04 auf — keine zweite Kopie der Nutzerschleife.
- `server/scripts/fix-overtime.ts` per `git rm` entfernt — `server/scripts/` enthält damit keine TypeScript-Datei mehr; WR-04 ist über beide Pläne (09.1-03, 09.1-05) vollständig geschlossen.
- `server/package.json`: neues Skript `fix:overtime` ruft `tsx src/scripts/fixOvertime.ts`.
- `deploy-server.yml` und `deploy-staging.yml`: crontab-Neueintrag (`CRON_COMMAND`) und Post-Deploy-Lauf (`npx tsx scripts/fix-overtime.ts`) vollständig entfernt; der lesende/entfernende Teil des Bereinigungsfilters (`crontab -l | grep -v "fix-overtime.ts" | crontab -`) bleibt in beiden Dateien stehen, damit sich ein noch nicht bereinigter Server beim nächsten Deployment selbst aufräumt (D-03).
- Drei Kanarienproben und zwei Funktionsläufe des Werkzeugs manuell durchgeführt und protokolliert (siehe unten).

## Task Commits

Each task was committed atomically:

1. **Task 1: fixOvertime.ts als gehärtetes Handbetriebs-Werkzeug (D-06, D-11)** - `fefa890` (feat)
2. **Task 2: Beide crontab-Einträge und beide Post-Deploy-Läufe aus den Workflows entfernen (D-03, D-04)** - `19ae831` (chore)

_Kein separater Plan-Metadaten-Commit — SUMMARY.md wird vom Orchestrator nach dem Merge der Worktree behandelt; STATE.md/ROADMAP.md werden laut Auftrag NICHT von diesem Ausführer aktualisiert._

## Files Created/Modified

- `server/src/scripts/fixOvertime.ts` — neu, ~180 Zeilen; `parseArgs()` erkennt `--allow-production` und mehrfach angebbares `--userId=<id>`; `main()` folgt der Guard-Reihenfolge aus `backfillOvertimeJournal.ts` (DATABASE_PATH-Pflicht → parseArgs → resolvedPath ausgeben → Guard/Warnung → erst dann `await import()` von `connection.js`/`overtimeRecalcRunner.js`)
- `server/scripts/fix-overtime.ts` — gelöscht (`git rm`)
- `server/package.json` — ein neuer Eintrag `"fix:overtime": "tsx src/scripts/fixOvertime.ts"`
- `.github/workflows/deploy-server.yml` — crontab-Block auf reinen Bereinigungsschritt reduziert, Post-Deploy-Lauf samt Begründungskommentar entfernt (Kurzverweis auf `server/src/server.ts` bleibt), HINWEIS-Kommentar angepasst
- `.github/workflows/deploy-staging.yml` — dieselben zwei Blöcke entfernt/reduziert; Block „Create .env" und alles danach unverändert

## Decisions Made

- Siehe `key-decisions` im Frontmatter: (1) Vermeidung des Substrings `dist/` in Kommentaren, (2) Vermeidung mehrfacher Erwähnung von `fix-overtime.ts`/`sync-prod-to-staging` in Workflow-Kommentaren wegen wörtlicher Zählprüfungen, (3) `development.db` in diesem Worktree über `initializeDatabase()` und `createUser()` frisch aufgebaut statt über den vorhandenen (aber defekten) Seed.

## Deviations from Plan

### Auto-fixed Issues

Keine — beide Tasks liefen wie im Plan beschrieben. Die einzige Anpassung betraf Formulierungen in Kommentaren, um die eigenen `grep -c`-Abnahmekriterien nicht durch Prosa-Erwähnungen zu verfälschen (kein Rule-1/2/3-Fund, sondern Selbstkorrektur während der Verifikation vor dem Commit).

---

**Total deviations:** 0
**Impact on plan:** Keiner — Plan exakt wie geschrieben umgesetzt.

## Issues Encountered

- **`development.db` existierte in diesem Worktree nicht** (gitignored, jeder Git-Worktree hat einen eigenen Dateisystemzustand). Für den geforderten Funktionsnachweis wurde die Datei zunächst über einen Lauf von `fixOvertime.ts` ohne `--userId` (0 Nutzer, legt über `initializeConnection()`/`initializeDatabase()` das vollständige 19-Tabellen-Schema an) neu aufgebaut. Der mitgelieferte Seed `database/seeds/001_test_user_data.sql` scheiterte danach mit `SqliteError: FOREIGN KEY constraint failed` — ein vorbestehender Zustand des Seedfiles gegenüber dem aktuellen Schema, nicht durch diesen Plan verursacht und nicht Gegenstand des Auftrags. Stattdessen wurde ein einzelner Testnutzer über die reguläre Serviceroute `createUser()` angelegt (erzeugt Nutzer und Startperiode in einer Transaktion, exakt der reguläre Weg), um den Funktionsnachweis mit einer echten `userId` zu erbringen. Kein Codepfad dieses Plans wurde dafür geändert; die temporäre Erzeugungsdatei wurde nach der Verifikation wieder gelöscht und war nie Teil eines Commits.
- **Testsuite:** `npx vitest run` gegen die so aufgebaute `development.db` ergab 592 bestanden / 3 fehlgeschlagen (595 gesamt) — dieselben drei Assertion-Meldungen wie die dokumentierte Baseline (`overtimeCompFutureCommitment.test.ts` Test 5, zwei Fälle in `unifiedOvertimeService.test.ts` „REGRESSION TESTS: Corrections and Hire Date"). Der vierte Baseline-Fall (`vacationBackfillService.test.ts`, „erkennt einen bereits gelaufenen Backfill") bestand hier, vermutlich weil die frisch aufgebaute Datenbank keinen vorherigen Backfill-Lauf als Restzustand enthielt — eine Verbesserung durch den frischen DB-Zustand, keine Regression. Keine neue rote Testdatei durch diesen Plan.

## Kanarienproben und Funktionsnachweis (Task 1)

Alle Läufe manuell ausgeführt, Kommandozeile und Exitcode protokolliert:

**Kanarienprobe A (Produktionspfad):**
```
$ DATABASE_PATH=/home/ubuntu/databases/production.db npx tsx src/scripts/fixOvertime.ts
Aufgelöster Datenbankpfad: C:\...\home\ubuntu\databases\production.db
FEHLER: Produktionsschreibzugriff verweigert (T-10-04, 10-02-PLAN.md; D5, 09-CONTEXT.md).
EXIT=2
```
Vor und nach dem Lauf mit `ls` geprüft: unter dem Pfadpräfix ist keine Datei entstanden (D-16).

**Kanarienprobe B (fehlendes DATABASE_PATH):**
```
$ npx tsx src/scripts/fixOvertime.ts
FEHLER: DATABASE_PATH ist nicht gesetzt.
EXIT=2
```

**Kanarienprobe C (NODE_ENV=production):**
```
$ DATABASE_PATH=./scratch-canary-c.db NODE_ENV=production npx tsx src/scripts/fixOvertime.ts
FEHLER: Produktionsschreibzugriff verweigert ...
EXIT=2
```
`ls scratch-canary-c.db` danach: Datei existiert nicht — kein Zugriff hat stattgefunden.

**Funktionsnachweis (Erfolg):**
```
$ DATABASE_PATH=database/development.db NODE_ENV=development npx tsx src/scripts/fixOvertime.ts --userId=1
Aufgelöster Datenbankpfad: ...\server\database\development.db
✅ Überstunden-Neuberechnungslauf beendet: 1 verarbeitet, 0 fehlgeschlagen (handbetrieb)
EXIT=0
```
Keine `-wal`/`-shm`-Datei nach dem Lauf zurückgelassen (`ls` vor/nach geprüft) — Beleg für `db.close()` im Erfolgspfad.

**Fehlerpfad-Nachweis (D-06):**
```
$ DATABASE_PATH=database/development.db NODE_ENV=development npx tsx src/scripts/fixOvertime.ts --userId=999999
⚠️ Überstunden-Neuberechnungslauf beendet: 0 verarbeitet, 1 fehlgeschlagen (handbetrieb)
EXIT=1
```
Ebenfalls keine `-wal`/`-shm`-Datei zurückgelassen — `db.close()` lief auch im Fehlerpfad über den `finally`-Block.

## Verifikation Task 2

- `grep -c "fix-overtime.ts"` in beiden Workflows → je `1` (nur der Filterausdruck)
- `grep -c "CRON_COMMAND"` in beiden Workflows → je `0`
- `grep -c "npx tsx scripts/"` in beiden Workflows → je `0`
- `grep -n "grep -v \"fix-overtime.ts\""` → in jeder Datei genau ein Treffer
- `grep -c "sync-prod-to-staging"` in beiden Workflows → je `0`
- YAML-Gültigkeit: `python3` war auf diesem Rechner nicht installiert; stattdessen `node -e "require('js-yaml').load(...)"` gegen die im Repo-Root vorhandene `js-yaml`-Installation verwendet — beide Dateien laden fehlerfrei
- `git diff` je Workflow-Datei zeigt ausschließlich die beiden betroffenen Blöcke; Migrations-, Build-, PM2- und Health-Check-Schritte sowie der Block „Create .env" in `deploy-staging.yml` sind unverändert

## User Setup Required

None - keine externe Dienstkonfiguration nötig.

## Next Phase Readiness

- `server/scripts/` ist frei von TypeScript-Dateien — WR-04 ist über die Pläne 09.1-03 und 09.1-05 vollständig geschlossen.
- Beide Deploy-Workflows haben keinen automatischen Aufrufer des ehemaligen Cronjob-Skripts mehr; der Serverprozess ist der einzige Schreiber auf `production.db` (WR-05 vollständig, zusammen mit Plan 09.1-04).
- Plan 09.1-06 kann jetzt am laufenden Server (Blue und Green) nachweisen, dass der In-Prozess-Scheduler startet, die Anlauf-Bilanzzeile erscheint und der crontab-Bereinigungsschritt beim nächsten Deployment tatsächlich greift.
- Kein Blocker für 09.1-06.

---
*Phase: 09.1-journal-backfill-und-betriebs-h-rtung*
*Completed: 2026-08-29*

## Self-Check: PASSED

- FOUND: server/src/scripts/fixOvertime.ts
- CONFIRMED DELETED: server/scripts/fix-overtime.ts
- FOUND: .github/workflows/deploy-server.yml
- FOUND: .github/workflows/deploy-staging.yml
- FOUND commit: fefa890 (Task 1)
- FOUND commit: 19ae831 (Task 2)
