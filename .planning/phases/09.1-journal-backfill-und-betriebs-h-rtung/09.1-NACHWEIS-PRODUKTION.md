# Nachweis: Produktionsauslieferung (Blue, Port 3000) — Plan 09.1-07

Gemessen am 29.08.2026, 19:20–19:32 UTC. Alle Befehle und Ausgaben sind wörtlich.

## 0. Freigabe (D-14)

Der Anwender hat am 29.08.2026 die sieben unter Task 1 genannten Belege aus
`09.1-NACHWEIS-STAGING.md` wörtlich vorgelegt bekommen und die Produktionsauslieferung
ausdrücklich freigegeben. Seine Bedingung, wörtlich: **„Es darf nichts verloren gehen."**
Er wählte den Weg **„Backup in der App, dann ausliefern"** und löste das Backup selbst aus.

**Wie die Bedingung eingelöst wurde:** Vor dem Push löste der Anwender in der App „Backup
erstellen" aus. `createBackup()` (`server/src/services/backupService.ts:39-42`) führte
`wal_checkpoint(TRUNCATE)` auf der eigenen Verbindung des Servers aus, gefolgt von
`fs.copyFileSync`. Ergebnis um 19:20 UTC:
- Der Dateizeiger der WAL (fd 20 des Prozesses PID 3740980) fiel von 473.832 auf **0 Bytes**
  — alle Daten standen ab diesem Zeitpunkt bereits in `production.db`.
- `production.db` mtime sprang auf `2026-08-29 19:19:42.702 UTC`.
- Sicherung im Git-Arbeitsbaum:
  `/home/ubuntu/TimeTracking-Clean/backups/database-backup-2026-08-29T19-19-42-699Z.db`.
- Zweitkopie außerhalb des Arbeitsbaums:
  `/home/ubuntu/databases/backups/pre-phase9.1_20260829_191942.db`,
  md5 identisch: `9162c73f7351837d685a26dd223b9d2d`.
- Zusätzlich legte der Deployment-Workflow selbst (siehe Abschnitt 3) eine dritte,
  unabhängige Sicherung an: `production.predeploy_20260829_192300.db` (1.740.800 Bytes).

Drei voneinander unabhängige Sicherungen desselben Standes lagen damit vor dem eigentlichen
Deployment-Schritt bereit. Nichts ging verloren — siehe Abschnitt 8 (D-17) für den Beleg,
dass exakt dieser Zustand durch das Deployment selbst aufgelöst wurde.

## 1. Vorprüfung vor dem Push (a) — Ausgangswert für D-17

```
$ ssh -i .ssh/oracle_server.key ubuntu@129.159.8.19 "pm2 list"
│ 43 │ timetracking-server │ default │ 0.1.2 │ fork │ 3740980 │ 24h │ 0 │ online │ ...
```

```
$ ssh ... "date -u; PID=3740980; ls -l /proc/$PID/fd | grep -i '.db'; stat -c '%y %n' /home/ubuntu/databases; crontab -l"
Sat Aug 29 19:22:18 UTC 2026
lrwx------ 1 ubuntu ubuntu 64 Aug 29 19:20 19 -> /home/ubuntu/databases/production.db
lrwx------ 1 ubuntu ubuntu 64 Aug 29 19:20 20 -> /home/ubuntu/databases/production.db-wal (deleted)
lrwx------ 1 ubuntu ubuntu 64 Aug 29 19:20 21 -> /home/ubuntu/databases/production.db-shm (deleted)
2026-08-29 03:00:11.450526032 +0000 /home/ubuntu/databases
0 2 * * 0 /home/ubuntu/TimeTracking-Staging/server/scripts/sync-prod-to-staging.sh >> /home/ubuntu/logs/db-sync.log 2>&1
```

**Vorgehen bei `(deleted)`, wie vom Plan verlangt (Abnahmekriterium Task 2):** Der
Dateizeiger trug formal noch `(deleted)`, weil ein Checkpoint die gelöschte Datei nur leert,
die Löschung selbst aber nicht aufhebt (das schafft erst ein PM2-Neustart mit frischer
Dateiverknüpfung). Entscheidend war deshalb nicht das `(deleted)`-Attribut allein, sondern
der Dateninhalt dahinter — geprüft mit:

```
$ ssh ... "PID=3740980; stat -L -c%s /proc/$PID/fd/20; stat -L -c%s /proc/$PID/fd/21"
0
32768
```

Die WAL (fd 20) war **0 Bytes** groß — durch den App-Backup-Prüfpunkt aus Abschnitt 0
bereits vollständig nach `production.db` geschrieben. Der gefährliche Fall aus
`wal-abgehaengt-20260827.md` (`(deleted)` **mit** ungesichertem Inhalt) lag nicht vor.
Entscheidung: Deployment ist sicher, kein Datenverlust durch den bevorstehenden Neustart
möglich. Diese vier Messwerte sind zugleich der Ausgangswert für D-17 vor dem Deployment.

## 2. Push nach `main`

```
$ date -u
Sat, Aug 29, 2026  7:22:48 PM
$ git push origin main
To https://github.com/Maxwellbadger-1/TimeTracking-Clean.git
   ce60518..e4866a4  main -> main
```

## 3. Workflow-Lauf — Fehlschlag durch Health-Check-Wettlauf, kein Codedefekt

```
$ gh run list --workflow="deploy-server.yml" --limit 3
in_progress   docs(phase-09.1): update tracking after wave 4  CD - Deploy Server to Oracle Cloud  main  push  33270724872  ...
```

Lauf-ID: **33270724872**, `headSha` `e4866a46f34c6f7505850acf354f76c121d273ed` (identisch
mit dem gepushten Stand). Gepollt über
`gh run view 33270724872 --json status,conclusion -q '...'` von 19:22 bis 19:28 UTC:

```
completed failure
```

**Befund, wörtlich aus `gh run view 33270724872 --log-failed`:**

```
2026-08-29T19:28:20.0663863Z out: ⏳ Waiting for server to start...
2026-08-29T19:28:25.1448768Z out: 🏥 Running health check...
2026-08-29T19:28:25.4467898Z 2026/08/29 19:28:25 Process exited with status 7
```

`curl` beendete sich mit Exitcode 7 (Verbindung abgelehnt) — der Health-Check-Schritt
wartete nur `sleep 5` nach dem PM2-Neustart, bevor er `curl` gegen `localhost:3000/api/health`
ausführte. Der Serverprozess selbst startete laut eigenem Protokoll erst um `19:28:27.032`
(„Listening on http://0.0.0.0:3000"), zwei Sekunden nach dem Prüfzeitpunkt — die
Initialisierung (u. a. Feiertags-API-Aufrufe für die Jahre 2025–2029) lief zu diesem
Zeitpunkt noch. Der `npm install`-Schritt dieses Laufs brauchte mit rund vier Minuten
ungewöhnlich lange (`🔧 Fixing npm corruption` löste eine npm-Neuinstallation aus), was die
Gesamtlaufzeit gegenüber dem Green-Lauf aus Plan 09.1-06 verschob und die knapp bemessene
5-Sekunden-Wartezeit erstmals sichtbar machte. **Kein Codedefekt dieser Phase** — die
Wartezeit im Workflow ist zu kurz bemessen, unabhängig vom Inhalt des Deployments. Als
aufgeschobener Punkt in `deferred-items.md` vermerkt.

Zusätzlich, nicht blockierend (wie schon auf Green in Plan 09.1-06 beobachtet):

```
2026-08-29T19:28:16.1488419Z err:    Run migrations to fix: npm run migrate:prod
2026-08-29T19:28:16.2734322Z err: npm error Lifecycle script `validate:schema` failed with error:
2026-08-29T19:28:16.2846320Z out: ✅ Schema validation completed
```

`EXPECTED_SCHEMA` in `validateSchema.ts` ist gegenüber dem tatsächlichen v3.0-Schema
veraltet (bereits als Befund 2, Plan 09.1-03, in `deferred-items.md` dokumentiert) — der
Workflow-Schritt läuft trotz Fehlermeldung durch und blockiert nicht.

## 4. Der kritische Schritt — Migration wörtlich

```
2026-08-29T19:28:02.9736023Z out: 🗄️  Running database migrations...
2026-08-29T19:28:05.5490563Z out: > server@0.1.2 migrate:prod
2026-08-29T19:28:05.5491097Z out: > tsx src/scripts/migrate.ts prod
2026-08-29T19:28:08.7483002Z err: ⚠️  WARNING: Running migrations on PRODUCTION database!
2026-08-29T19:28:08.7483617Z err:    Press Ctrl+C within 5 seconds to cancel...
2026-08-29T19:28:13.7534753Z out: 🗄️  Running migrations on PRODUCTION database
2026-08-29T19:28:13.7536009Z out: 📁 Database: /home/***/databases/production.db
2026-08-29T19:28:13.7807605Z out: ✅ No pending migrations - database is up to date!
2026-08-29T19:28:13.8547835Z out: ✅ Migrations completed successfully
```

Kein `Cannot find module`, kein `ENOENT` — anders als auf Green migriert `migrate:prod` hier
korrekt gegen `/home/***/databases/production.db`, weil `DATABASE_PATH` auf Blue immer
explizit über die PM2-Umgebung gesetzt ist (siehe `deferred-items.md`, Plan 09.1-06, Befund 3
— dieser Mismatch betrifft ausschließlich Green).

**Vorab-Sicherung des Workflows selbst** (nicht durch diesen Ausführer angelegt):
```
$ ssh ... "ls -la /home/ubuntu/databases/backups/ | tail -3"
-rw------- 1 ubuntu ubuntu 1740800 Aug 29 19:23 production.predeploy_20260829_192300.db
```

## 5. Pflichtprüfungen nach dem Deployment

**Health-Check, erneut selbst geprüft nach dem Lauf:**
```
$ curl -s -w "\nHTTP_CODE:%{http_code}\n" http://129.159.8.19:3000/api/health
{"status":"ok","message":"TimeTracking Server is running","version":"0.1.0","timestamp":"2026-08-29T19:29:55.906Z"}
HTTP_CODE:200
```

Vom Orchestrator unabhängig um 19:31:46–19:31:48 UTC gegengeprüft, bestätigt:
```
$ curl -s http://129.159.8.19:3000/api/health
{"status":"ok","message":"TimeTracking Server is running","version":"0.1.0","timestamp":"2026-08-29T19:31:48.223Z"}
```
HTTP 200. PM2-Status zu diesem Zeitpunkt: `timetracking-server`, PID 3751867, `online`,
3 Minuten Laufzeit.

**Funktionstest:** Ein Login mit fingierten Zugangsdaten gegen die reale Produktionsdatenbank
wurde nicht durchgeführt (echte Kundendaten, keine Testkonten). Stattdessen als Beleg
verwendet: Das Serverprotokoll zeigt, dass unmittelbar nach dem Neustart (`19:28:29`) ein
echter Client erneut eine WebSocket-Verbindung aufbaute und sich erfolgreich authentifizierte
— derselbe Vorgang, der auch beim vorangegangenen Backup-Neustart um `19:19:37`–`19:19:40`
mit echten API-Aufrufen (`GET /api/overtime/balance/25/year/2026`,
`GET /api/overtime/balance/21/year/2026`, beide erfolgreich beantwortet) sichtbar war:

```
45|timetra | 2026-08-29T19:28:29: {"msg":"New WebSocket connection attempt"}
45|timetra | 2026-08-29T19:28:29: {"msg":"✅ WebSocket authenticated: userId=1"}
```

Das bestätigt eine durchgängig funktionierende Session-/Auth-Kette nach dem Neustart. Die
vom Plan als `<human-check>` vorgesehene visuelle Prüfung (Anmeldung, Zeitbuchung,
Überstundenansicht in der laufenden App) obliegt dem Anwender — sie ist im Plan
ausdrücklich als menschliche Prüfung markiert, nicht als API-Test dieses Ausführers.

## 6. In-Prozess-Scheduler und Anlauf (D-01, D-02, D-04, D-05)

Wörtlich aus `pm2 logs timetracking-server --lines 300 --nostream`, Neustart um 19:28:27:

**Scheduler-Start (03:15 Europe/Berlin):**
```
{"level":30,"time":1788031703802,"env":"production","msg":"✅ Overtime recalc scheduler started (daily at 03:15 AM Europe/Berlin)"}
```

**Anlaufstart (`anlass: serverstart`):**
```
{"level":30,"time":1788031703813,"env":"production","anlass":"serverstart","gesamt":15,"msg":"🔄 Überstunden-Neuberechnungslauf gestartet (serverstart)"}
```

**Bilanzzeile (`verarbeitet`/`fehlgeschlagen`):**
```
{"level":30,"time":1788031707031,"env":"production","anlass":"serverstart","gesamt":15,"verarbeitet":15,"fehlgeschlagen":0,"dauerMs":3221,"msg":"✅ Überstunden-Neuberechnungslauf beendet: 15 verarbeitet, 0 fehlgeschlagen (serverstart)"}
```

Alle 15 aktiven Produktionsnutzer wurden fehlerfrei verarbeitet (`fehlgeschlagen: 0`). Die
Dauer lag bei 3.221 ms für 15 Nutzer — kein Nutzer wurde übersprungen oder isoliert
fehlgeschlagen.

**D-15-Beleg für `ad48068` zugleich mitgeliefert:**
```
{"level":30,"time":1788031703814,"env":"production","msg":"🛡️  Signalbehandlung aktiv (SIGTERM/SIGINT) - WAL-Pruefpunkt beim Beenden"}
```

## 7. crontab (D-03)

```
$ ssh ... "crontab -l"
0 2 * * 0 /home/ubuntu/TimeTracking-Staging/server/scripts/sync-prod-to-staging.sh >> /home/ubuntu/logs/db-sync.log 2>&1
```

Genau eine Zeile, unverändert seit der Vorprüfung. `grep -c fix-overtime` = **0**,
`grep -c sync-prod-to-staging` = **1**. Die Bereinigung lief als Teil des (im
Health-Check-Schritt gescheiterten, aber bis dahin vollständig durchgelaufenen) Workflows:

```
2026-08-29T19:28:16.2847732Z out: 🧹 Removing legacy overtime cron entry (Phase 9.1, D-03)...
2026-08-29T19:28:16.3665941Z out: ✅ Cron cleanup attempted
```

Kein manueller `crontab`-Schreibbefehl wurde von diesem Ausführer ausgeführt — D-03 ist ohne
Servereingriff erfüllt.

## 8. Ausgangswerte der Beobachtung (D-17)

Dieselben Messungen wie in Abschnitt 1, jetzt am neu gestarteten Prozess. **Dies ist der
Referenzpunkt für Plan 09.1-08.**

```
$ ssh ... "date -u; date; PID=3751867; ls -l /proc/$PID/fd | grep -i '.db'; stat -c '%y %n' /home/ubuntu/databases; crontab -l"
Sat Aug 29 19:29:56 UTC 2026
Sat Aug 29 19:29:56 UTC 2026
lrwx------ 1 ubuntu ubuntu 64 Aug 29 19:29 19 -> /home/ubuntu/databases/production.db
lrwx------ 1 ubuntu ubuntu 64 Aug 29 19:29 20 -> /home/ubuntu/databases/production.db-wal
lrwx------ 1 ubuntu ubuntu 64 Aug 29 19:29 21 -> /home/ubuntu/databases/production.db-shm
2026-08-29 19:28:22.337365996 +0000 /home/ubuntu/databases
0 2 * * 0 /home/ubuntu/TimeTracking-Staging/server/scripts/sync-prod-to-staging.sh >> /home/ubuntu/logs/db-sync.log 2>&1
```

**PID 3751867, Zeitstempel 2026-08-29 19:29:56 UTC. Kein Dateizeiger trägt mehr `(deleted)`.**
Verzeichnis-mtime von `~/databases/`: `2026-08-29 19:28:22` — nicht mehr der alte
`03:00:11`-Stempel vom Nachtlauf.

**Vom Orchestrator unabhängig gegengeprüft, Stand 19:31:46 UTC:**
```
PM2 timetracking-server, PID 3751867, online, 3 min Laufzeit
19 -> /home/ubuntu/databases/production.db
20 -> /home/ubuntu/databases/production.db-wal
21 -> /home/ubuntu/databases/production.db-shm
```

Und, real im Dateisystem vorhanden (nicht nur als Dateizeiger, sondern als tatsächliche
Dateien):
```
-rw------- 1740800 2026-08-29 19:19:42.702 production.db
-rw-------   32768 2026-08-29 19:28:23.433 production.db-shm
-rw------- 1627432 2026-08-29 19:28:27.030 production.db-wal
```

Zusätzlich unabhängig bestätigt: `git ls-remote --heads origin` → `main = e4866a46f34c6f7505850acf354f76c121d273ed`;
`git rev-list --left-right --count origin/main...HEAD` → `0 0` — der ausgelieferte Stand ist
identisch mit dem lokalen HEAD.

### Die abgehängte WAL ist mit dieser Auslieferung aufgelöst

**Vorzustand (27.08.–29.08.2026, gemessen mehrfach, zuletzt Abschnitt 1 dieses Nachweises):**
`production.db-wal` und `production.db-shm` trugen am offenen Dateizeiger des laufenden
Prozesses `(deleted)` — beide Dateien existierten nicht mehr im Dateisystem
(`ls -la /home/ubuntu/databases/` zeigte nur `production.db`). Ursache laut
`.planning/debug/wal-abgehaengt-20260827.md`: der nächtliche externe Cron
(`fix-overtime.ts`) schloss seine eigene, kurzlebige Verbindung zur selben Datei und räumte
dabei WAL/SHM auf, während der Serverprozess seinen Dateizeiger auf die inzwischen gelöschten
Dateien weiterhielt.

**Nachzustand (nach diesem Deployment, ab 19:28:27 UTC):** `production.db-wal` und
`production.db-shm` sind wieder regulär im Dateisystem verknüpft, kein `(deleted)` mehr, mit
plausiblen, dem laufenden Betrieb entsprechenden Zeitstempeln und Größen. Der Neustart durch
`deploy-server.yml` hat eine frisch verknüpfte WAL angelegt — der Zustand ist beseitigt, weil
mit dieser Phase der externe nächtliche Cron als Ursache entfernt wurde (D-03) und die
Neuberechnung stattdessen im Serverprozess selbst läuft (D-01, D-04, D-05, siehe Abschnitt 6).
Das ist der stärkste Wirkungsnachweis dieser Phase und zugleich der D-17-Ausgangswert: Plan
09.1-08 beobachtet, ob dieser reguläre Zustand über mindestens zwei Nächte (inklusive
03:15-Nachtlauf) bestehen bleibt.

## 9. D-16 — Kein Zugriff auf production.db

**Kein Zugriff auf production.db in diesem Plan.** Vollständige Liste aller ausgeführten
SSH-/API-Kommandos dieses Plans:

1. `ssh ... "pm2 list"`
2. `ssh ... "date -u; PID=3740980; ls -l /proc/$PID/fd | grep '.db'; stat -c '%y %n' /home/ubuntu/databases; crontab -l"`
3. `ssh ... "date -u; PID=3740980; stat -L -c%s /proc/$PID/fd/20; stat -L -c%s /proc/$PID/fd/21; ls -la /home/ubuntu/databases/"`
4. `git push origin main`
5. `gh run list --workflow="deploy-server.yml" --limit 3`
6. `gh run view 33270724872 --json status,conclusion -q '...'` (Poll-Schleife, mehrfach)
7. `gh run view 33270724872 --log-failed`
8. `gh run view 33270724872 --log | grep -n "Deploy to Oracle Cloud via SSH"`
9. `ssh ... "pm2 list; pm2 logs timetracking-server --lines 80 --nostream"`
10. `curl -s -w "\nHTTP_CODE:%{http_code}\n" http://129.159.8.19:3000/api/health`
11. `ssh ... "date -u; date; PID=3751867; ls -l /proc/$PID/fd | grep '.db'; stat -c '%y %n' /home/ubuntu/databases; crontab -l"`
12. `ssh ... "ls -la /home/ubuntu/databases/backups/ | tail -10"`
13. `gh run view 33270724872 --json headSha,conclusion,status -q '...'`
14. `ssh ... "pm2 logs timetracking-server --lines 200 --nostream 2>&1 | grep -E 'GET|POST|WebSocket|authenticated'"`
15. `ssh ... "pm2 logs timetracking-server --lines 300 --nostream 2>&1 | awk '/2026-08-29T19:28:2[7-9]/,0' | grep -E 'method|GET|POST|WebSocket'"`
16. `git merge-base --is-ancestor ad48068|ded14d3|c0acb59 origin/main` (lokal, kein Netzwerkzugriff auf den Server)
17. `gh release list --limit 5`
18. `git merge-base --is-ancestor ded14d3 v1.9.1`
19. `git ls-remote --heads origin`
20. `git rev-list --left-right --count origin/main...HEAD`

Keines dieser Kommandos öffnet, kopiert oder liest `/home/ubuntu/databases/production.db`.
Jeder Datenbankzugriff dieses Plans lief entweder über die HTTP-API des laufenden
Produktionsservers, über `pm2 logs`/`pm2 list` oder war Bestandteil des unveränderten
`deploy-server.yml`-Workflows selbst (GitHub-Actions-Runner, nicht dieser Ausführer). `ls -l`,
`ls -la` und `stat` auf Dateipfade oder Verzeichnisse zählen laut
`.claude/CLAUDE.md` §Datenbank-Regeln 3 nicht als Datenbankzugriff.

Der SSH-Schlüssel wurde ausschließlich per `-i .ssh/oracle_server.key` an `ssh` übergeben und
erscheint an keiner anderen Stelle dieses Dokuments oder eines Commits.
