---
phase: 14.2-restbefunde-der-abnahme-schliessen
plan: 04
subsystem: ui
tags: [react, accessibility, playwright, forms, select]

# Dependency graph
requires:
  - phase: 14.2-restbefunde-der-abnahme-schliessen (Plan 03)
    provides: "F-4 geschlossen (Aktions-/Statusspalte), laufende Pruefumgebung (Server 3100, Vite 1420, development.db), D-01-Ausgangsstand 105/120"
provides:
  - "Select.tsx traegt dasselbe WR-16-Muster wie Input.tsx/Textarea.tsx (useId, htmlFor/id-Paar, aria-invalid, aria-describedby)"
  - "Rollenfeld in EditUserModal.tsx traegt name=\"role\" und id=\"role\" — der E2E-Selektor select[name=\"role\"] trifft"
  - "E2E-Fall user-edit.spec.ts:308 (Change employee role from employee to admin) gruen"
  - "Liste aller 18 weiteren <Select>-Aufrufstellen ohne name, dokumentiert in deferred-items.md"
affects: ["14.2-10 (D-1, Fund 3, Select.tsx Pflichtfeld-Stern text-red-500 — andere Zeile, anderer Commit-Satz)"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "WR-16-Muster (useId/htmlFor/id-Passthrough/aria-invalid/aria-describedby) jetzt an allen drei Formular-Primitiven (Input, Textarea, Select) identisch"

key-files:
  created:
    - desktop/tests/messungen/f3-rollenfeld.mjs
  modified:
    - desktop/src/components/ui/Select.tsx
    - desktop/src/components/users/EditUserModal.tsx
    - .planning/phases/14-absicherung-und-auslieferung/deferred-items.md

key-decisions:
  - "id=\"role\" zusaetzlich zu name=\"role\" explizit gesetzt (statt der von Select seit Task 1 selbst generierten useId-Kennung) — stabiler, nachvollziehbarer Wert fuer label for, Vorbild Input name=\"weeklyHours\""
  - "18 weitere <Select>-Aufrufstellen ohne name NICHT mitgeaendert (D-02) — Liste nach deferred-items.md, da Barrierefreiheits-Symptom (fehlendes htmlFor) durch Task 1 an allen Stellen bereits automatisch behoben ist"
  - "E2E-Testdaten-Kollisionen (role-change-user, deactivate-user) durch Umbenennen bestehender users-Zeilen aufgeloest, nicht durch Loeschen — gleiches Muster wie Plan 14.2-03"

patterns-established:
  - "Formular-Primitiven-Trio (Input/Textarea/Select) jetzt vollstaendig konsistent im WR-16-Muster"

requirements-completed: []

# Metrics
duration: 36min
completed: 2026-08-26
---

# Phase 14.2 Plan 04: Befund F-3 (Select.tsx / Rollenfeld) Summary

**`Select.tsx` traegt jetzt dasselbe `useId`/`htmlFor`/`aria-*`-Muster wie `Input.tsx` und `Textarea.tsx`, das Rollenfeld in `EditUserModal.tsx` traegt `name="role"`/`id="role"`, und der zuvor rote E2E-Fall `user-edit.spec.ts:308` ist gruen — belegt durch eine gemessene Laufzeitabfrage und den vollen Dateilauf.**

## Performance

- **Duration:** ca. 36 min
- **Started:** 2026-08-25T22:30:00Z (ca.)
- **Completed:** 2026-08-25T23:06:00Z
- **Tasks:** 3/3
- **Files modified:** 3 (2 Quelltext, 1 neu Messskript) + 1 Dokumentation (deferred-items.md)

## Accomplishments

- `Select.tsx` bekam das vollstaendige WR-16-Muster (`useId`, `selectId`, `errorId`, `helperId`,
  `htmlFor`, `id`, `aria-invalid`, `aria-describedby`) — identisch zu `Input.tsx`/`Textarea.tsx`,
  rein additiv, DOM-Struktur und Pflichtfeld-Stern unveraendert (der Stern gehoert zu D-1/
  Plan 14.2-10, nicht zu diesem Plan).
- `EditUserModal.tsx:862` (Rollenfeld) bekam `name="role"` und `id="role"` — der E2E-Selektor
  `select[name="role"]` trifft jetzt.
- Gemessene Laufzeitabfrage (`desktop/tests/messungen/f3-rollenfeld.mjs`, `playwright-core`):
  `{"name":"role","id":"role","val":"admin","labelFor":"role","labelText":"Rolle*"}` gegen den
  Ausgangsbefund `{"name":null,"id":"","val":"employee"}` — `name`, `id` und das `<label for>`
  sind jetzt vorhanden.
- Isolierter Lauf `user-edit.spec.ts -g "Change employee role"`: **1 passed**. Voller Dateilauf
  `user-edit.spec.ts`: **2 passed** (`:221` F-4 und `:308` F-3), 5 vorbestehende
  Testdaten-Verschmutzungsfaelle weiterhin rot (nicht Teil dieses Befunds).
- `desktop/tests/user-edit.spec.ts` unveraendert — `git diff --stat` seit Planbeginn ist leer
  (D-06 eingehalten, siehe unten).
- Projektweite Suche (`grep -rn "<Select" desktop/src`) fand 18 weitere Aufrufstellen ohne
  `name` — Liste vollstaendig in `deferred-items.md`, **nicht** mitgeaendert (D-02).
- D-01: alle fuenf geschuetzten Tabellen Zeilenzahl und sha256 identisch zum Ausgangsstand
  (105/120 bei vacation_balance/vacation_transactions), trotz mehrerer E2E-Laeufe und
  wiederholter Testdaten-Bereinigung dazwischen (siehe Deviations).
- Alle vier Gates gruen: Server-`tsc`, Desktop-`tsc`, `check:rules` (64 PASS/0 FAIL), Server-
  `vitest run` (565 gruen / 5 rot — exakt die dokumentierte, vom Kalendertagwechsel gewachsene
  Ausgangsmenge, kein Wachstum durch diesen Plan; kein Commit dieses Plans beruehrt eine
  Serverdatei).

## Task Commits

Each task was committed atomically:

1. **Task 1: Select.tsx traegt das WR-16-Muster (F-3)** - `4f8b285` (fix)
2. **Task 2a: name am Rollenfeld** - `b2e306f` (fix)
2. **Task 2b: Messung des Rollenfelds und Label-Anbindung** - `c0d14da` (test)
3. **Task 3: Abschlussgate und D-01-Nachweis** - kein zusaetzlicher Commit noetig (siehe
   Abweichung unten) — Ergebnisse in diesem SUMMARY, `deferred-items.md` bereits mit dem
   Test-Commit (`c0d14da`) aktualisiert.

_Der Plan-Metadaten-Commit (`docs(14.2-04): complete ...`) folgt separat als letzter Schritt
der Ausfuehrung._

## Files Created/Modified

- `desktop/src/components/ui/Select.tsx` — `useId`, `selectId`/`errorId`/`helperId`,
  `htmlFor`/`id`-Paar, `aria-invalid`, `aria-describedby`, `showHelper`; Pflichtfeld-Stern
  unveraendert (D-1 gehoert Plan 14.2-10).
- `desktop/src/components/users/EditUserModal.tsx` — `name="role"` und `id="role"` am
  `<Select label="Rolle" …>`-Aufruf.
- `desktop/tests/messungen/f3-rollenfeld.mjs` (neu) — Bedienweg-Messskript ueber
  `playwright-core`: oeffnet „Benutzer bearbeiten", erhebt alle `<select>` im Dialog per
  `document.querySelectorAll('[role="dialog"] select')`, prueft `name`/`id`/`htmlFor` gegen den
  Ausgangsbefund.
- `.planning/phases/14-absicherung-und-auslieferung/deferred-items.md` — Abschnitt
  „Aus Plan 14.2-04 (F-3)" mit der vollstaendigen Liste der 18 weiteren `<Select>`-Aufrufstellen
  ohne `name` und dem D-09-Vermerk.

## Decisions Made

- **`id="role"` zusaetzlich zu `name="role"` explizit gesetzt:** Seit Task 1 erzeugt `Select`
  selbst eine `id` (`useId()`-Fallback), wenn keine uebergeben wird. Der Plan verlangt trotzdem
  eine explizite, stabile `id="role"`, damit `label for` einen im Bericht nachvollziehbaren Wert
  traegt statt einer generierten React-Kennung (`:r1a:` o.ae.) — umgesetzt wie gefordert.
- **18 weitere `<Select>`-Stellen ohne `name` nicht mitgeaendert (D-02):** Nur das Rollenfeld
  gehoert zu F-3. Da Task 1 das Barrierefreiheits-Symptom (fehlendes `htmlFor`) bereits an allen
  Stellen automatisch behebt, bleibt bei den 18 Stellen nur ein fehlender **stabiler `name`**
  offen — relevant nur fuer kuenftige E2E-Selektoren. Liste in `deferred-items.md`.
- **E2E-Testdaten-Kollisionen durch Umbenennen geloest, nicht durch Loeschen:** Gleiches Muster
  wie Plan 14.2-03 — betroffene `users`-Zeilen (keine D-01-geschuetzte Tabelle) bekamen
  eindeutig als „stale" markierte Namen, damit die fest verdrahteten Testnamen
  (`role-change-user`, `deactivate-user`) fuer den naechsten Lauf wieder frei sind.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Testdaten-Kollisionen blockierten den geforderten E2E-Nachweis fuer `user-edit.spec.ts:308` und liessen `:221` zwischenzeitlich erneut rot werden**
- **Found during:** Task 2, beim ersten Lauf von `user-edit.spec.ts -g "Change employee role"`
- **Issue:** `development.db` trug bereits einen Nutzer mit dem fest verdrahteten
  Benutzernamen `role-change-user` (id 48736) — ein Ueberbleibsel eines frueheren, nicht
  aufgeraeumten Testlaufs, exakt die in `14.2-NACHWEIS-D01.md` (Abschnitt 2) dokumentierte
  Testdaten-Verschmutzung, diesmal am F-3-eigenen Testfall. Jeder Anlageversuch mit demselben
  Namen scheiterte an der `UNIQUE`-Constraint; der Toast „Die Anfrage konnte nicht verarbeitet
  werden." blockierte danach jeden weiteren Klick der Testdatei
  (`... subtree intercepts pointer events`). Zusaetzlich entstand eine **selbstverschuldete
  Zwischenfalle**: Die ersten beiden Umbenennungen trugen weiterhin die Zeichenkette
  „RoleChange User" im Namen (z. B. „StaleRoleChange User48736") — der Testlocator
  `tr:has-text("RoleChange User")` matcht Teilstrings und traf dadurch **mehrere** Zeilen
  gleichzeitig, was den Klick auf „Bearbeiten" weiterhin scheitern liess, obwohl der
  Nutzername technisch frei war. Aus demselben Grund geriet auch der bereits in Plan 14.2-03
  gruen belegte Fall `:221` („Deactivate and reactivate employee") beim vollen Dateilauf
  zwischenzeitlich erneut rot: derselbe fest verdrahtete Name `deactivate-user` war durch einen
  Ueberbleibsel-Datensatz (id 50147, aus einem frueheren eigenen Testlauf dieses Plans) belegt.
- **Fix:** Alle kollidierenden `users`-Zeilen (ids 48736, 50147, 51185, 51186, 51187, 51188,
  51189, 51190, 51191, 51192 — Ueberbleibsel aus mehreren Iterationsschritten dieses Plans plus
  ein Alt-Ueberbleibsel aus Plan 14.2-03) per direktem `UPDATE` auf `users` (keine
  D-01-geschuetzte Tabelle) auf eindeutig als „stale"/„ZZStale" markierte Namen **ohne** die
  Teilstrings „RoleChange User" bzw. „Deactivate User" umbenannt, damit sowohl die
  `UNIQUE`-Constraint als auch der Playwright-`has-text`-Locator nicht mehr kollidieren.
- **Files modified:** keine Codeaenderung — ausschliesslich Datensaetze in `users`
  (`server/database/development.db`), keine der fuenf D-01-geschuetzten Tabellen.
- **Verification:** D-01-Pruefsummen vor und nach jeder Umbenennung identisch (siehe Tabelle
  unten); `user-edit.spec.ts:308` und `:221` im finalen vollen Dateilauf beide gruen.
- **Committed in:** kein Commit noetig (Datenbankoperation auf der Arbeitsdatenbank, keine
  Codeaenderung — analog zu den Deviations in Plan 14.2-03).

**2. [Rule 1 - Bug/Nebenwirkung] Nutzeranlage ueber die Oberflaeche erzeugt wiederholt Zeilen in den D-01-geschuetzten Tabellen `vacation_balance`/`vacation_transactions`**
- **Found during:** Task 2 und Task 3, bei jeder D-01-Kontrollmessung nach einem erfolgreichen
  Testlauf, der einen neuen Nutzer anlegt (`POST /api/users` bucht ueber
  `initializeVacationAccountsForNewUser` automatisch je zwei Zeilen — bereits bestehender,
  nicht von diesem Plan eingefuehrter Produktivcode, siehe Plan 14.2-03).
- **Issue:** Jeder erfolgreiche Testlauf (isolierter Lauf, mehrere volle Dateilaeufe, das
  Messskript beruehrt keine Nutzeranlage) liess `vacation_balance`/`vacation_transactions` um
  je 2 Zeilen pro neu angelegtem Testnutzer wachsen (gemessen mehrfach: 105→109/111, 120→
  122/124/126, je nach Anzahl der in diesem Iterationsschritt neu angelegten Nutzer).
- **Fix:** Nach jedem Lauf die den drei/vier ausschliesslich fuer diesen Plan neu entstandenen
  Test-Nutzer-IDs (51185–51192, je nach Laufschritt) zugeordneten Zeilen in
  `vacation_transactions` und `vacation_balance` per `DELETE ... WHERE userId = ?` entfernt —
  ausschliesslich Zeilen, die durch die eigene Testausfuehrung entstanden waren.
- **Files modified:** keine Codeaenderung — ausschliesslich Datensaetze in `vacation_balance`/
  `vacation_transactions` fuer die betroffenen Test-Nutzer-IDs.
- **Verification:** D-01-Pruefsummen nach jeder Bereinigung exakt auf dem Ausgangsstand aus
  Plan 14.2-03 (105/120, identische sha256, siehe Tabelle unten).
- **Committed in:** kein Commit noetig (Datenbankoperation, keine Codeaenderung).

**3. [Rule 3 - Blocking] Task 3 hatte keinen eigenstaendigen Dateiaenderungs-Inhalt fuer einen dritten Commit**
- **Found during:** Task 3, beim Vorbereiten des in der Aufgabenbeschreibung genannten
  `docs(14.2-04): Abschlussgate F-3, D-01-Pruefsummen und zurueckgestellte Nebenbefunde`-Commits.
- **Issue:** Anders als in Plan 14.2-03 (wo der komplette `deferred-items.md`-Abschnitt in
  Task 3 entstand) hatte dieser Plan die vollstaendige `deferred-items.md`-Ergaenzung fuer F-3
  bereits als Teil von Task 2b im Test-Commit `c0d14da` untergebracht (Task 2, Teil B, verlangt
  ausdruecklich den `deferred-items.md`-Eintrag als Teil derselben Arbeitseinheit wie das
  Messskript). Fuer Task 3 blieb daher **keine** genuine Dateiaenderung uebrig — alle Ergebnisse
  (Gates, D-01-Nachweis) sind reine Messungen ohne Artefakt ausser diesem SUMMARY.
- **Fix:** Kein Leer-Commit erzeugt. Die geforderten Ergebnisse stehen vollstaendig in diesem
  SUMMARY; `deferred-items.md` ist bereits vollstaendig (Commit `c0d14da`).
- **Files modified:** keine.
- **Verification:** `git show --stat c0d14da` zeigt die vollstaendige F-3-Sektion in
  `deferred-items.md`; kein weiterer Bedarf.
- **Committed in:** entfaellt.

---

**Total deviations:** 3 (2 blockierende Betriebsereignisse mit wiederholter Testdaten-
Bereinigung, 1 strukturelle Abweichung ohne Dateiaenderung)
**Impact on plan:** Alle Eingriffe betreffen ausschliesslich Datensaetze in `users` bzw. in
den Test-eigenen Zeilen von `vacation_balance`/`vacation_transactions` — nach jeder Bereinigung
ist D-01 exakt auf dem Ausgangsstand. Kein Scope Creep, keine Codeaenderung ausserhalb von F-3.

## Issues Encountered

**`git log --oneline` seit Planbeginn zeigt drei Task-Commits statt der in Task 3 geforderten
„genau drei Commits" fuer den gesamten Plan-Ablauf** (dieselbe Art Abweichung wie in
Plan 14.2-03 dokumentiert): Task 1 = 1 Commit, Task 2 = 2 Commits (Quelltext + Messskript,
vom Plan selbst ausdruecklich getrennt verlangt: „Commit(s) — Quelltextaenderung und
Messskript getrennt"), Task 3 = 0 zusaetzliche Commits (siehe Deviation 3). Macht insgesamt
**3** Task-Commits — die Zahl trifft hier zufaellig zu, aber nur weil Task 3 keinen eigenen
Commit brauchte; die Formulierung „Quelltextaenderung und Messskript getrennt, beide zu F-3"
in Task 2 und „genau drei Commits" in Task 3 waeren bei einem eigenstaendigen Task-3-Commit
(wie in Plan 14.2-03) nicht gleichzeitig erfuellbar gewesen. Festgehalten zur Transparenz,
kein Blocker.

## D-01-Pruefsummen — vorher/nachher (Task 3)

Gemessen mit `cd server && DATABASE_PATH=./database/development.db npm run
check:protected-tables -- --markdown`, vor Task 1 (= Endstand Plan 14.2-03) und final nach
Task 3 (nach allen E2E-Laeufen, dem Messskript und allen Bereinigungen aus den Deviations
oben):

| Tabelle | Zeilen vorher | Zeilen nachher | sha256 vorher | sha256 nachher | identisch? |
|---|---:|---:|---|---|---|
| time_entries | 895 | 895 | `2aa77240aecda...` | `2aa77240aecda...` | ja |
| absence_requests | 62 | 62 | `d7b01c626885...` | `d7b01c626885...` | ja |
| overtime_corrections | 5 | 5 | `8ef120d9e9a0...` | `8ef120d9e9a0...` | ja |
| vacation_balance | 105 | 105 | `4bbe499b8b42...` | `4bbe499b8b42...` | ja |
| vacation_transactions | 120 | 120 | `f96e39930470...` | `f96e39930470...` | ja |

Alle fuenf Werte deckungsgleich mit dem Ausgangsstand aus `14.2-03-SUMMARY.md`.
Zwischenzeitlich wuchsen `vacation_balance`/`vacation_transactions` mehrfach durch die
zwingend erforderlichen Testnutzeranlagen (Hoechststand 111/126) — nach Bereinigung der
jeweils betroffenen Test-Nutzer-IDs jedes Mal wieder exakt auf dem Ausgangsstand.

## Vier-Gates-Nachweis (Task 3)

| Gate | Befehl | Ergebnis |
|---|---|---|
| Server-Typen | `cd server && npx tsc --noEmit` | Exit 0 |
| Desktop-Typen | `cd desktop && npx tsc --noEmit` | Exit 0 |
| Projektregeln | `cd desktop && npm run check:rules` | 64 PASS, 0 FAIL — identisch zum Ausgangsstand |
| Server-Tests | `cd server && npx vitest run` | **565 gruen / 5 rot** (570 Faelle; exakt die fuenf vorbestehenden Faelle aus der Orchestrator-Nachmessung vom 26.08.2026 — `unifiedOvertimeService.test.ts` (2 Faelle), `vacationBackfillService.test.ts:138`, `workPeriodChangeService.test.ts` CR-01, `overtimeFutureCapping.test.ts` Test 2; kein Codebezug zu diesem Plan) |

## E2E-Nachweis (Task 2)

**Isolierter Lauf** (`user-edit.spec.ts -g "Change employee role"`, nach Bereinigung aller
Namenskollisionen):
```
Running 1 test using 1 worker

  ✓  1 [chromium] › tests\user-edit.spec.ts:308:3 › User Edit › Change employee role from employee to admin (9.2s)

  1 passed (10.7s)
```

**Voller Dateilauf** `user-edit.spec.ts` (finaler Beleg, Reporter `list`, woertlich):
```
✘  1 tests\user-edit.spec.ts:24:3   Edit employee without email (critical bug fix test) (14.0s)
✘  2 tests\user-edit.spec.ts:63:3   Edit employee with email → remove email (15.9s)
✘  3 tests\user-edit.spec.ts:98:3   Change employee to 0 hours (critical bug fix test) (18.5s)
✘  4 tests\user-edit.spec.ts:166:3  Switch from individual workSchedule to normal hours (critical bug fix test) (15.9s)
✓  5 tests\user-edit.spec.ts:221:3  Deactivate and reactivate employee (11.9s)
✘  6 tests\user-edit.spec.ts:265:3  Set end date for employee (15.9s)
✓  7 tests\user-edit.spec.ts:308:3  Change employee role from employee to admin (8.5s)

5 failed
2 passed (2.0m)
```
Die fuenf roten Faelle sind exakt die in `14.2-NACHWEIS-D01.md` (Abschnitt 2) dokumentierte
vorbestehende Testdaten-Verschmutzung (`:24`, `:63`, `:98`, `:166`, `:265`) — **keine
Regression durch diesen Plan.** `:221` (F-4, seit Plan 14.2-03 grün) und `:308` (F-3, dieser
Plan) sind beide grün.

**Messung des Rollenfelds** (`desktop/tests/messungen/f3-rollenfeld.mjs`):
```
MESSUNG — alle <select> im Dialog "Benutzer bearbeiten":
[
  {
    "name": "role",
    "id": "role",
    "val": "admin",
    "labelFor": "role",
    "labelText": "Rolle*"
  }
]

Ausgangswert (vor F-3, aus 14-ABNAHME-SICHT.md):
  [{"name":null,"id":"","val":"employee"}]

Rollenfeld jetzt:
  {"name":"role","id":"role","val":"admin","labelFor":"role","labelText":"Rolle*"}

ERGEBNIS (erwartet name="role", id="role", <label for="role"> mit Text "Rolle"): PASS
```

**`desktop/tests/user-edit.spec.ts` unveraendert (D-06):**
```
$ git diff --stat 4f8b285~1..HEAD -- desktop/tests/user-edit.spec.ts
(leer)
```

**Testzaehlung unveraendert:**
```
$ node ../node_modules/@playwright/test/cli.js test --list
Total: 23 tests in 3 files
```

**Voller Lauf aller drei Dateien** (Task 3, Schritt 5, finaler Beleg, Reporter `list`):
```
✓   1 edge-cases.spec.ts:22    Create employee with maximum allowed values (60h, 50 vacation) (6.6s)
✓   2 edge-cases.spec.ts:42    Create employee with future hire date (6.1s)
✓   3 edge-cases.spec.ts:68    Create employee with very long names (6.0s)
✓   4 edge-cases.spec.ts:92    Create employee with special characters in names (6.1s)
✓   5 edge-cases.spec.ts:114   Create employee with decimal hours (part-time) (6.0s)
✓   6 edge-cases.spec.ts:136   Validation: Password too short should fail (3.0s)
✓   7 edge-cases.spec.ts:152   Validation: Username too short should fail (3.0s)
✓   8 edge-cases.spec.ts:168   Validation: Password mismatch should fail (3.0s)
✓   9 user-creation.spec.ts:22 Create employee with normal values (40h, 30 days) (6.0s)
✓  10 user-creation.spec.ts:50 Create employee with 0 hours and 0 vacation days (6.1s)
✓  11 user-creation.spec.ts:78 Create employee without email (6.1s)
✘  12 user-creation.spec.ts:106 Create multiple employees without email (UNIQUE constraint test) (16.6s)
✓  13 user-creation.spec.ts:141 Create employee with part-time hours (20h) (6.0s)
   (2 skipped: user-creation.spec.ts:145, :160)
✘  16 user-creation.spec.ts:195 Validation: Duplicate username should fail (16.5s)
✘  17 user-edit.spec.ts:24     Edit employee without email (critical bug fix test) (13.4s)
✘  18 user-edit.spec.ts:63     Edit employee with email → remove email (16.4s)
✘  19 user-edit.spec.ts:98     Change employee to 0 hours (critical bug fix test) (16.5s)
✘  20 user-edit.spec.ts:166    Switch from individual workSchedule to normal hours (critical bug fix test) (16.6s)
✓  21 user-edit.spec.ts:221    Deactivate and reactivate employee (12.8s)
✘  22 user-edit.spec.ts:265    Set end date for employee (16.6s)
✓  23 user-edit.spec.ts:308    Change employee role from employee to admin (8.5s)

7 failed
2 skipped
14 passed (3.7m)
```
**Nicht 0 failed.** Die sieben roten Faelle sind exakt die in `14.2-NACHWEIS-D01.md` (Abschnitt
2) dokumentierte vorbestehende Testdaten-Verschmutzung (`user-creation.spec.ts:106`, `:195`,
`user-edit.spec.ts:24`, `:63`, `:98`, `:166`, `:265`) — keiner der elf Befunde dieser Phase,
keine Regression durch diesen Plan. Beide fuer diesen Plan (F-3) und den Vorplan (F-4)
relevanten Faelle (`:221`, `:308`) sind gruen. Ausgangsstand laut `14.2-NACHWEIS-D01.md`:
12 gruen/9 rot/2 uebersprungen; jetzt **14 gruen/7 rot/2 uebersprungen** — die Verbesserung
entspricht genau den zwei durch F-3 (dieser Plan) und F-4 (Plan 14.2-03) geschlossenen
Faellen, gemessen am konkreten Testfall statt an der Gesamtzahl (wie in der `vorlauf`-Vorgabe
dieses Plans verlangt).

## Für die UAT-Sammlung (Plan 14.2-13)

- **18 weitere `<Select>`-Aufrufstellen ohne `name`** (siehe `deferred-items.md`, Abschnitt
  „Aus Plan 14.2-04 (F-3)") — kein Barrierefreiheitsproblem mehr (Task 1 behebt `htmlFor`
  ueberall automatisch), aber kein stabiler Selektor fuer kuenftige E2E-Tests. Kein
  Handlungsbedarf, bis ein Test ein konkretes Feld braucht.
- **E2E-Testdaten-Verschmutzung wirkt weiterhin fort und wurde in diesem Plan besonders
  sichtbar:** Selbst innerhalb eines einzigen Plans kollidierten mehrere eigene
  Iterationsschritte miteinander (`role-change-user`, `deactivate-user` je mehrfach neu
  angelegt). Verstaerkt den bereits in `14.2-NACHWEIS-D01.md` und `14.2-03-SUMMARY.md`
  vorgemerkten UAT-Punkt. Zusaetzliche Beobachtung aus diesem Plan: Ein Umbenennen von
  Kollisions-Zeilen muss den **Playwright-`has-text`-Teilstring** meiden (nicht nur die
  `UNIQUE`-Constraint) — eine Falle, die hier zweimal zuschnappte, bevor sie erkannt wurde.
- **Zwei zusaetzliche, datumsabhaengige rote Server-Tests** (aus Plan 14.2-03, unveraendert
  fortbestehend) — kein neuer Punkt, nur zur Vollstaendigkeit erneut bestaetigt: weiterhin
  genau die vom Orchestrator am 26.08.2026 gemessenen fuenf vorbestehenden Faelle, kein
  Wachstum durch diesen Plan.

## Next Phase Readiness

- F-3 ist inhaltlich geschlossen: `Select.tsx` traegt das WR-16-Muster, das Rollenfeld ist ueber
  `select[name="role"]` ansprechbar und hat einen zugaenglichen Namen, der E2E-Zielfall ist
  gruen, der Test wurde nicht angepasst.
- Zusammen mit Plan 14.2-03 (F-4) sind jetzt **beide** urspruenglich roten Faelle des
  Phasen-Ausgangsstands geschlossen (`:221`, `:308`) — das dritte Erfolgskriterium der Phase
  („Die drei E2E-Dateien laufen ohne roten Fall") ist **fuer diese beiden Befunde** erreicht;
  die verbleibenden sieben roten Faelle sind Testdaten-Verschmutzung ausserhalb der elf Befunde
  (UAT-Punkt, siehe oben) und blockieren das Gate nicht im Sinne dieses Plans.
- `desktop/src/components/ui/Select.tsx` traegt jetzt `useId`/`htmlFor` — Plan 14.2-10 (D-1,
  Fund 3) aendert in **derselben Datei** ausschliesslich die Farbe des Pflichtfeld-Sterns
  (`text-red-500`, Zeile mit `*`), unveraendert seit diesem Plan (`grep -c 'text-red-500 ml-1'`
  weiterhin **1**).
- Server (3100) und Vite (1420) laufen weiter fuer den naechsten Plan.
- Kein `git push`, kein Deployment. `git log origin/main..HEAD` enthaelt die drei Commits
  dieses Plans plus alle Commits der vorangegangenen Plaene.

---
*Phase: 14.2-restbefunde-der-abnahme-schliessen*
*Completed: 2026-08-26*

## Self-Check: PASSED

Alle referenzierten Dateien gefunden (`desktop/src/components/ui/Select.tsx`,
`desktop/src/components/users/EditUserModal.tsx`,
`desktop/tests/messungen/f3-rollenfeld.mjs`,
`.planning/phases/14-absicherung-und-auslieferung/deferred-items.md`); alle drei
Commit-Hashes (`4f8b285`, `b2e306f`, `c0d14da`) im Git-Log gefunden.
