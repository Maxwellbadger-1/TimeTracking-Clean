---
phase: 14.2-restbefunde-der-abnahme-schliessen
plan: 03
subsystem: ui
tags: [react, tailwind, playwright, user-management, soft-delete]

# Dependency graph
requires:
  - phase: 14.2-restbefunde-der-abnahme-schliessen (Plan 02)
    provides: "reactivateUser() deckt beide Wiederherstellungszustaende ab (F-1, Serverhaelfte), laufende Pruefumgebung (Server 3100, development.db)"
provides:
  - "Statusfilter der Nutzerliste zeigt standardmaessig alle Zustaende (Vorgabewert 'all' statt 'active')"
  - "Statusspalte unterscheidet aktiv / deaktiviert / archiviert (getUserStatus, eine Ableitungsstelle)"
  - "Aktionsspalte in drei Zweige: aktiv (Bearbeiten/Passwort/Loeschen), deaktiviert (Bearbeiten/Reaktivieren), archiviert (nur Reaktivieren)"
  - "E2E-Fall user-edit.spec.ts:221 (Deactivate and reactivate employee) gruen"
affects: ["14.2-08 (F-2, dieselbe Datei UserManagementPage.tsx, Spalte Stunden/Woche — nicht angefasst)"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Status-Ableitung als benannte Hilfsfunktion + Lookup-Tabelle (getUserStatus/USER_STATUS_BADGE) statt verschachtelter Ternaerketten, wiederverwendet in Statusspalte und Aktionsspalte"
    - "Messskript ausserhalb der *.spec.ts-Suite unter tests/messungen/, mit playwright-core statt @playwright/test — fuer Bedienweg-Nachweise, die keinen dauerhaften Regressionstest darstellen sollen"

key-files:
  created:
    - desktop/tests/messungen/f4-aktionsspalte.mjs
  modified:
    - desktop/src/pages/UserManagementPage.tsx
    - .planning/phases/14-absicherung-und-auslieferung/deferred-items.md

key-decisions:
  - "Vorgabewert des Statusfilters 'active' -> 'all' (F-4), mit allen drei mitziehenden Stellen (clearFilters, zwei Sichtbarkeitsbedingungen)"
  - "Mittlerer Aktionsspalten-Zweig (deaktiviert) traegt bewusst kein Loeschen/Passwort-Zuruecksetzen — Ausweitung ueber F-4 hinaus, als UAT-Kandidat vorgemerkt statt mitgenommen"
  - "Zwei server-seitige, datumsabhaengige Testfehlschlaege (overtimeFutureCapping.test.ts, workPeriodChangeService.test.ts) nicht gefixt — ausserhalb des Aenderungsumfangs jedes Commits dieses Plans, dokumentiert statt vermischt (D-02)"

patterns-established:
  - "getUserStatus()/USER_STATUS_BADGE als einzige Ableitungsstelle fuer den Drei-Zustand (aktiv/deaktiviert/archiviert), von zwei Tabellenspalten gemeinsam genutzt"

requirements-completed: []

# Metrics
duration: 52min
completed: 2026-08-26
---

# Phase 14.2 Plan 03: Befund F-4 (Aktions- und Statusspalte) Summary

**Deaktivierte Nutzer stehen jetzt ohne Filteraenderung in der Nutzerliste, sind bearbeitbar und reaktivierbar; die Statusspalte trennt aktiv/deaktiviert/archiviert ueber eine einzige Ableitungsstelle — belegt durch den gruenen E2E-Fall `user-edit.spec.ts:221` und ein eigenes Bedienweg-Messskript.**

## Performance

- **Duration:** ca. 52 min
- **Started:** 2026-08-25T21:59:00Z (ca.)
- **Completed:** 2026-08-25T22:23:00Z
- **Tasks:** 3/3
- **Files modified:** 3 (2 Quelltext/Test, 1 Dokumentation)

## Accomplishments

- Statusfilter-Vorgabewert von `'active'` auf `'all'` gezogen — ein deaktivierter Nutzer ist
  jetzt ohne Filteraenderung auffindbar (F-4, erste Haelfte des NB-1-Befunds).
- Statusspalte unterscheidet jetzt drei Zustaende (`Aktiv` gruen, `Deaktiviert` amber,
  `Archiviert` grau) statt der bisherigen zweiwertigen Badge — ueber eine einzige
  Ableitungsstelle (`getUserStatus`/`USER_STATUS_BADGE`), hell und dunkel.
- Aktionsspalte in drei Zweige gezogen: aktiv (Bearbeiten/Passwort/Loeschen unveraendert),
  deaktiviert (neu: Bearbeiten + Reaktivieren), archiviert (weiterhin nur Reaktivieren) — die
  Soft-Delete-Semantik bleibt sichtbar getrennt.
- Der E2E-Fall `user-edit.spec.ts:221` („Deactivate and reactivate employee") ist gruen — einer
  der beiden roten Faelle des Phasen-Ausgangsstands.
- Bedienweg mit eigenem Messskript (`playwright-core`, kein `*.spec.ts`) belegt: Schaltflaechenliste
  eines deaktivierten Nutzers ist exakt `["Bearbeiten","Reaktivieren"]`, nach Reaktivieren
  Statusbadge `Aktiv` mit `Bearbeiten`/`Löschen`.
- Alle vier Gates gruen (server tsc, desktop tsc, `check:rules`); die rote Server-Testmenge
  enthaelt zwei zusaetzliche, nachweislich domainfremde Faelle (siehe Deviations) — dokumentiert,
  nicht gefixt.
- D-01: alle fuenf geschuetzten Tabellen Zeilenzahl und sha256 identisch zum Ausgangsstand aus
  Plan 14.2-02, trotz mehrerer E2E-Laeufe und einer notwendigen Testdaten-Bereinigung dazwischen.

## Task Commits

Each task was committed atomically:

1. **Task 1: Standardfilter zeigt deaktivierte Nutzer, Statusspalte benennt drei Zustaende (F-4)** - `9a417bd` (fix)
2. **Task 2a: Aktionsspalte in drei Zweige (F-4)** - `7aafc08` (fix)
2. **Task 2b: Bedienweg-Messung der Aktionsspalte** - `5de0f12` (test)
3. **Task 3: Abschlussgate und D-01-Nachweis** - `b844b96` (docs)

_Kein zusaetzlicher Plan-Metadaten-Commit ueber die vier Task-Commits hinaus noetig; dieser
SUMMARY-Commit folgt separat._

## Files Created/Modified

- `desktop/src/pages/UserManagementPage.tsx` — Statusfilter-Vorgabewert `'all'`, drei
  richtiggestellte Filter-Beschriftungen, `getUserStatus()`/`USER_STATUS_BADGE` als einzige
  Ableitungsstelle, Statusspalte und Aktionsspalte beide auf drei Zustaende gezogen, `.map()`
  auf Block-Body umgestellt, damit `status` einmal berechnet und zweimal verwendet wird.
- `desktop/tests/messungen/f4-aktionsspalte.mjs` (neu) — Bedienweg-Messskript ueber
  `playwright-core`: legt einen frischen Testnutzer an, deaktiviert ihn ueber `EditUserModal`,
  misst die Schaltflaechenliste ohne Filteraenderung, reaktiviert, misst erneut.
- `.planning/phases/14-absicherung-und-auslieferung/deferred-items.md` — Abschnitt
  „Aus Plan 14.2-03 (F-4)" mit den zwei server-seitigen Testbefunden und der
  Passwort/Loeschen-Frage fuer deaktivierte Konten als UAT-Kandidaten.

## Decisions Made

- **Statusableitung als eine Stelle statt vier Kopien:** `getUserStatus(user)` liefert
  `'active' | 'deactivated' | 'archived'`, eine Lookup-Tabelle `USER_STATUS_BADGE` traegt
  Beschriftung und Farbklassen (inkl. `dark:`) — von Statusspalte **und** Aktionsspalte
  gemeinsam genutzt, wie vom Plan verlangt (Task 1 legt sie an, Task 2 nutzt sie erneut).
- **Kein Loeschen/Passwort-Zuruecksetzen fuer den deaktivierten Zwischenzustand:** F-4 verlangt
  „auffindbar und bearbeitbar", keinen vollen Funktionsumfang. Als UAT-Kandidat vorgemerkt statt
  in diesen Plan gezogen.
- **Zwei zusaetzliche rote Server-Tests nicht gefixt:** `overtimeFutureCapping.test.ts` und
  `workPeriodChangeService.test.ts` liegen ausserhalb jedes Commit-Diffs dieses Plans (siehe
  Deviations) — Scope Fence (D-02), dokumentiert statt vermischt.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Testdaten-Kollision blockierte den geforderten E2E-Nachweis fuer `user-edit.spec.ts:221`**
- **Found during:** Task 2, beim ersten Lauf von `user-edit.spec.ts -g "Deactivate and reactivate employee"`
- **Issue:** `development.db` trug bereits einen Nutzer mit Benutzername `deactivate-user` (id
  48734, Vorname „Deactivate", Nachname „User", `status='inactive'`, `deletedAt IS NULL`) — ein
  Ueberbleibsel eines frueheren, nicht aufgeraeumten Testlaufs (dieselbe dokumentierte
  Testdaten-Verschmutzung wie in `14.2-NACHWEIS-D01.md`, Abschnitt 2, fuer sieben andere Faelle
  beschrieben). Der Test versucht, genau diesen Benutzernamen frisch anzulegen; die Anlage
  scheiterte an der `UNIQUE`-Constraint, der offene Anlage-Dialog samt Fehler-Toast blockierte
  danach jeden Klick der Testdatei (`... subtree intercepts pointer events`).
- **Fix:** Die veralteten Zeilen (`username`, `email`, `firstName`, `lastName`) der kollidierenden
  Nutzer per direktem `UPDATE` auf `users` (nicht geschuetzte Tabelle) umbenannt, damit der
  Testname wieder frei ist. Betroffen: id 48734 (Alt-Ueberbleibsel vor diesem Plan) sowie id
  50146 (aus einem eigenen Zwischenlauf dieses Plans entstanden, ebenfalls umbenannt, um den
  naechsten vollen Dateilauf nicht erneut zu blockieren).
- **Files modified:** keine Codeaenderung — ausschliesslich Datensaetze in `users`
  (`server/database/development.db`), keine der fuenf D-01-geschuetzten Tabellen.
- **Verification:** D-01-Pruefsummen vor und nach jeder Umbenennung identisch (siehe Tabelle
  unten); `user-edit.spec.ts:221` danach gruen (siehe E2E-Nachweis unten).
- **Committed in:** kein Commit noetig (Datenbankoperation auf der Arbeitsdatenbank, keine
  Codeaenderung — analog zum Server-Neustart in Plan 14.2-02).

**2. [Rule 1 - Bug/Nebenwirkung] Nutzeranlage ueber die Oberflaeche erzeugt Zeilen in den D-01-geschuetzten Tabellen `vacation_balance`/`vacation_transactions`**
- **Found during:** Task 2, bei der D-01-Kontrollmessung nach dem ersten erfolgreichen
  Testlauf (isolierter Lauf von `user-edit.spec.ts:221`)
- **Issue:** `POST /api/users` initialisiert bei jeder Nutzeranlage ueber
  `initializeVacationAccountsForNewUser` (bereits bestehender, nicht von diesem Plan
  eingefuehrter Produktivcode, `server/src/routes/users.ts:303-327`) je zwei Zeilen in
  `vacation_balance` und `vacation_transactions` (laufendes und Folgejahr). Da der geforderte
  E2E-/Bedienweg-Nachweis zwingend einen neuen Nutzer ueber die Oberflaeche anlegt, wuchsen
  beide Tabellen bei jedem erfolgreichen Testlauf um je 2 Zeilen — gemessen 105→107 bzw.
  120→122 nach dem ersten erfolgreichen isolierten Testlauf.
- **Fix:** Nach jedem erfolgreichen Lauf (isolierter Testlauf, voller Dateilauf,
  Bedienweg-Messskript) die genau diesem Testnutzer zugeordneten Zeilen in
  `vacation_transactions` und `vacation_balance` per `DELETE ... WHERE userId = ?` entfernt —
  ausschliesslich Zeilen, die durch die eigene Testausfuehrung entstanden waren, keine
  Bestandsdaten.
- **Files modified:** keine Codeaenderung — ausschliesslich Datensaetze in `vacation_balance`/
  `vacation_transactions` fuer die drei betroffenen Test-Nutzer-IDs (50146, 50147, 50148).
- **Verification:** D-01-Pruefsummen nach jeder Bereinigung wieder exakt auf dem Ausgangsstand
  aus Plan 14.2-02 (105/120, identische sha256, siehe Tabelle unten).
- **Committed in:** kein Commit noetig (Datenbankoperation, keine Codeaenderung).

---

**Total deviations:** 2 auto-fixed (1 blockierendes Betriebsereignis, 1 Nebenwirkung
bestehenden Produktivcodes bei zwingend erforderlicher Testnutzeranlage)
**Impact on plan:** Beide Eingriffe betreffen ausschliesslich Datensaetze in `users` bzw. in den
Test-eigenen Zeilen von `vacation_balance`/`vacation_transactions` — nach jeder Bereinigung
ist D-01 exakt auf dem Ausgangsstand. Kein Scope Creep, keine Codeaenderung ausserhalb von F-4.

## Issues Encountered

**Zwei zusaetzliche rote Server-Tests beim Abschlussgate (Task 3), nicht gefixt:**

`cd server && npx vitest run` zeigte **565 gruen / 5 rot** (570 Faelle gesamt) statt der
erwarteten 567/3. Die drei vorbestehenden roten Faelle (`unifiedOvertimeService.test.ts:285`,
`:340`, `vacationBackfillService.test.ts:138`) sind weiterhin rot. Zwei zusaetzliche:

- `overtimeFutureCapping.test.ts` — „Test 2: Der Saldo stimmt mit der Summe der darunter
  gezeigten Buchungen ueberein" — `expected 1 to be +0`
- `workPeriodChangeService.test.ts` — „CR-01: Die model_change-Zeile wird in KEINEM
  transaktionssummierenden Lesepfad mitgezaehlt" — `expected 4 to be less than 0.011`

**Warum nicht gefixt (Scope Fence, D-02):** `git diff <Plan-02-Endcommit>..HEAD --stat` zeigt
fuer die vier Commits dieses Plans ausschliesslich `desktop/src/pages/UserManagementPage.tsx`,
`desktop/tests/messungen/f4-aktionsspalte.mjs` und `deferred-items.md` — keine Datei aus
`server/src/services/`. Beide Testdateien legen „heute" bei Modul-Ladezeit einmalig in einer
Konstante fest und vergleichen damit spaeter zwei Aufrufe derselben Ueberstunden-Berechnungskette,
die intern selbst je einmal frisch „heute" ermitteln (14.1-BL-01-Deckelungscode). Die
Abweichungsgroesse (1 h bzw. 4 h) entspricht in beiden Faellen genau dem Tagessoll eines
einzelnen Werktags — ein Datumsgrenzfall zwischen eingefrorenem Modul-Zeitpunkt und intern neu
ermittelter Zeit, reproduzierbar bei zwei unabhaengigen frischen `npx vitest run`-Aufrufen. Kein
Importpfad verbindet `UserManagementPage.tsx` mit diesen beiden Server-Dateien. Details und
Vorschlag in `deferred-items.md`, Abschnitt „Aus Plan 14.2-03 (F-4)", Punkt 1.

**Diese Abweichung erfuellt die im Plan formulierte Zahlengrenze („hoechstens 3 rot, genau die
drei vorbestehenden") nicht woertlich.** Sie wird hier bewusst nicht verschwiegen: 565/5 statt
567/3 grün/rot, mit vollstaendiger Ursachenanalyse und Diff-Beleg, dass keiner der vier Commits
dieses Plans eine Serverdatei beruehrt.

## D-01-Pruefsummen — vorher/nachher (Task 3)

Gemessen mit `cd server && DATABASE_PATH=./database/development.db npm run
check:protected-tables -- --markdown`, vor Task 1 (= Endstand Plan 14.2-02) und nach Task 3
(nach allen E2E-Laeufen, dem Messskript und den beiden Bereinigungen aus den Deviations oben):

| Tabelle | Zeilen vorher | Zeilen nachher | sha256 vorher | sha256 nachher | identisch? |
|---|---:|---:|---|---|---|
| time_entries | 895 | 895 | `2aa77240aecda...` | `2aa77240aecda...` | ja |
| absence_requests | 62 | 62 | `d7b01c626885...` | `d7b01c626885...` | ja |
| overtime_corrections | 5 | 5 | `8ef120d9e9a0...` | `8ef120d9e9a0...` | ja |
| vacation_balance | 105 | 105 | `4bbe499b8b42...` | `4bbe499b8b42...` | ja |
| vacation_transactions | 120 | 120 | `f96e39930470...` | `f96e39930470...` | ja |

Alle fuenf Werte deckungsgleich mit dem Ausgangsstand aus `14.2-02-SUMMARY.md`. Zwischenzeitlich
wuchsen `vacation_balance`/`vacation_transactions` durch die zwingend erforderliche
Testnutzeranlage auf 107/122 (siehe Deviation 2) — nach Bereinigung der drei betroffenen
Test-Nutzer-IDs wieder exakt auf dem Ausgangsstand.

## Vier-Gates-Nachweis (Task 3)

| Gate | Befehl | Ergebnis |
|---|---|---|
| Server-Typen | `cd server && npx tsc --noEmit` | Exit 0 |
| Desktop-Typen | `cd desktop && npx tsc --noEmit` | Exit 0 |
| Projektregeln | `cd desktop && npm run check:rules` | 64 PASS, 0 FAIL — identisch zum Ausgangsstand |
| Server-Tests | `cd server && npx vitest run` | **565 gruen / 5 rot** (570 Faelle; 3 vorbestehende + 2 zusaetzliche, dokumentiert unter „Issues Encountered", kein Codebezug zu diesem Plan) |

## E2E-Nachweis (Task 2)

**Isolierter Lauf** (`user-edit.spec.ts -g "Deactivate and reactivate employee"`, nach
Bereinigung der Namenskollision):
```
✓  1 [chromium] › tests\user-edit.spec.ts:221:3 › User Edit › Deactivate and reactivate employee (12.0s)
1 passed (13.5s)
```

**Voller Dateilauf** `user-edit.spec.ts` (finaler Beleg, Reporter `list`, woertlich):
```
✘  1 tests\user-edit.spec.ts:24:3   Edit employee without email (critical bug fix test)
✘  2 tests\user-edit.spec.ts:63:3   Edit employee with email → remove email
✘  3 tests\user-edit.spec.ts:98:3   Change employee to 0 hours (critical bug fix test)
✘  4 tests\user-edit.spec.ts:166:3  Switch from individual workSchedule to normal hours (critical bug fix test)
✓  5 tests\user-edit.spec.ts:221:3  Deactivate and reactivate employee (12.4s)
✘  6 tests\user-edit.spec.ts:265:3  Set end date for employee
✘  7 tests\user-edit.spec.ts:308:3  Change employee role from employee to admin

6 failed
1 passed (2.1m)
```
Die sechs roten Faelle sind exakt die in `14.2-NACHWEIS-D01.md` (Abschnitt 2) dokumentierte
vorbestehende Testdaten-Verschmutzung (fuenf Faelle: `:24`, `:63`, `:98`, `:166`, `:265`) plus der
F-3-Fall `:308` („Change employee role from employee to admin", Ursache `Select.tsx`/
`EditUserModal.tsx`, Plan 14.2-04) — **keine Regression durch diesen Plan.** `:221` ist gruen.

`cd desktop && node ../node_modules/@playwright/test/cli.js test --list`:
```
Total: 23 tests in 3 files
```
Unveraendert 23 Faelle in drei Dateien — das Messskript ist kein zusaetzlicher `*.spec.ts`-Fall.

## Bedienweg-Messung (Task 2, `desktop/tests/messungen/f4-aktionsspalte.mjs`)

```
MESSUNG 1 — nach Deaktivieren, ohne Filteraenderung gefunden:
  Statusbadge: Deaktiviert
  Schaltflaechen: ["Bearbeiten","Reaktivieren"]
MESSUNG 2 — nach Reaktivieren:
  Statusbadge: Aktiv
  Schaltflaechen: ["Bearbeiten","Löschen"]

ERGEBNIS Messung 1 (erwartet ["Bearbeiten","Reaktivieren"]): PASS
ERGEBNIS Messung 2 (erwartet Aktiv + Bearbeiten + Löschen): PASS
```
Gegenprobe gegen den urspruenglichen Befund `[{"t":"Reaktivieren","a":null}]` (nur „Reaktivieren",
kein „Bearbeiten"): Die gemessene Liste traegt jetzt beide Schaltflaechen. Das Passwort-Symbol
(`Key`, icon-only ohne Text) erscheint erwartungsgemaess nicht in der Textliste; seine Anwesenheit
gehoert nicht zum mittleren (deaktivierten) Zweig — siehe Aktionsspalten-Tabelle im Plan.

## Für die UAT-Sammlung (Plan 14.2-13)

- **Zwei zusaetzliche, datumsabhaengige rote Server-Tests** (`overtimeFutureCapping.test.ts`
  Test 2, `workPeriodChangeService.test.ts` CR-01) — Testinfrastruktur-Fragilitaet durch
  einmalig eingefrorenes „heute" bei Modul-Ladezeit statt einer durchgehenden Zeitquelle. Kein
  Datenverlust, keine Sicherheitswirkung, kein Codebezug zu F-4. Siehe
  `deferred-items.md`, Abschnitt „Aus Plan 14.2-03 (F-4)", Punkt 1.
- **Passwort-Zuruecksetzen/Loeschen fuer bloss deaktivierte (nicht archivierte) Konten:**
  F-4 hat bewusst nur Bearbeiten+Reaktivieren fuer den mittleren Zustand umgesetzt. Ob das
  fachlich reicht oder erweitert werden soll, ist eine Entscheidung des Anwenders. Siehe
  `deferred-items.md`, Punkt 2.
- **Plan-interne Unstimmigkeit (informativ, kein Blocker):** Die Aufgabenbeschreibung von Task 2
  erlaubt ausdruecklich zwei Commits für Quelltext und Messskript; die Abnahmekriterien von
  Task 3 sprechen dagegen von „genau drei Commits" fuer den ganzen Plan. Dieser Plan hat vier
  Commits (Task 1, Task 2a, Task 2b, Task 3) — jeder einzeln inhaltlich revertierbar und
  unvermischt (D-02 eingehalten), aber die Zahl "drei" aus Task 3 trifft nicht woertlich zu.
- **E2E-Testdaten-Verschmutzung wirkt weiterhin fort** (bereits in `14.2-NACHWEIS-D01.md`
  vorgemerkt) — betrifft auch die Folgeplaene. Dieser Plan zeigt zusaetzlich: Sogar der eigene
  Zielfall (`:221`) kann bei wiederholten Laeufen ohne Aufraeumschritt an derselben Kollision
  scheitern (`deactivate-user` durch einen fruehen eigenen Testlauf erneut belegt) — verstaerkt
  den bestehenden UAT-Punkt.

## Next Phase Readiness

- F-4 ist inhaltlich geschlossen: deaktivierte Nutzer sind auffindbar, bearbeitbar und
  reaktivierbar; die Statusspalte trennt drei Zustaende; der E2E-Zielfall ist gruen.
- `desktop/src/pages/UserManagementPage.tsx` traegt jetzt `getUserStatus()`/
  `USER_STATUS_BADGE` — Plan 14.2-08 (F-2, Spalte „Stunden/Woche", `:389`/`:448` in der
  Zeilennummerierung vor diesem Plan) fasst dieselbe Datei an; die hier ergaenzten Stellen
  (Statusfilter, Statusspalte, Aktionsspalte) liegen ausserhalb der von 14.2-08 zu aendernden
  Spalte, aber der `.map()`-Block-Body ist jetzt Teil der Struktur, in die Plan 14.2-08 seine
  eigene Aenderung einfuegen wird.
- Server (3100) und Vite (1420, ueber `localhost`, nicht `127.0.0.1` — siehe Hinweis unten)
  laufen weiter fuer den naechsten Plan.
- **Hinweis fuer den naechsten Plan:** Vite antwortete waehrend dieses Plans auf
  `http://127.0.0.1:1420/` nicht (Exit-Code 7), auf `http://localhost:1420/` dagegen mit 200 —
  vermutlich eine reine IPv6/IPv4-Bindungseigenschaft dieses Vite-Prozesses, ohne Auswirkung auf
  Playwright (dessen `baseURL` ohnehin `localhost` ist). Kein Neustart noetig gewesen.
- Kein `git push`, kein Deployment. `git log origin/main..HEAD` enthaelt die vier Commits dieses
  Plans plus die Commits der vorangegangenen Plaene.

---
*Phase: 14.2-restbefunde-der-abnahme-schliessen*
*Completed: 2026-08-26*

## Self-Check: PASSED

Alle referenzierten Dateien gefunden (`desktop/src/pages/UserManagementPage.tsx`,
`desktop/tests/messungen/f4-aktionsspalte.mjs`,
`.planning/phases/14-absicherung-und-auslieferung/deferred-items.md`); alle vier
Commit-Hashes (`9a417bd`, `7aafc08`, `5de0f12`, `b844b96`) im Git-Log gefunden.
