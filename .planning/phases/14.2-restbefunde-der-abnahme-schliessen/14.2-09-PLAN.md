---
phase: 14.2-restbefunde-der-abnahme-schliessen
plan: 09
type: execute
wave: 9
depends_on: ["14.2-08"]
files_modified:
  - desktop/src/components/worktime/WorkTimeChangeModal.tsx
  - desktop/src/components/users/EditUserModal.tsx
  - .planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-NACHWEIS-F8.md
autonomous: true
requirements: []
decisions: [D-01, D-02, D-03, D-10, D-14, D-15]

must_haves:
  truths:
    - "F-8 (a): Solange der Wechsel-Dialog offen ist, erreicht die Kollisionsinformation den Anwender — document.elementFromPoint() trifft das Element, nicht ein darueberliegendes"
    - "F-8 (b): Nach dem Schliessen des Wechsel-Dialogs bleibt die Zeilenmarkierung in der Periodenliste stehen und ist sichtbar"
    - "F-8: Nach erfolgreichem Speichern wird die Markierung zurueckgenommen — sie bleibt nicht dauerhaft haengen"
    - "F-8: Wird ein kollisionsfreier Stichtag eingegeben, verschwindet die Markierung wieder"
    - "F-8: Der Feldfehler im Dialog und die Sperre des Speichern-Knopfes sind unveraendert (D-10)"
    - "Der Nachweis ist ausschliesslich mit document.elementFromPoint() gefuehrt, nicht mit getBoundingClientRect()"
    - "D-01: Keine Zeile in den fuenf geschuetzten Tabellen wurde angefasst (Zeilenzahl und SHA-256 vorher = nachher)"
  artifacts:
    - path: "desktop/src/components/worktime/WorkTimeChangeModal.tsx"
      provides: "Kollisionsinfo im Dialog; resetForm nimmt die Markierung nicht mehr unbedingt zurueck"
      contains: "onConflict"
    - path: "desktop/src/components/users/EditUserModal.tsx"
      provides: "conflictPeriodId ueberlebt das Schliessen des Wechsel-Dialogs"
      contains: "setConflictPeriodId"
    - path: ".planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-NACHWEIS-F8.md"
      provides: "elementFromPoint-Messungen in allen vier Zustaenden"
  key_links:
    - from: "desktop/src/components/worktime/WorkTimeChangeModal.tsx"
      to: "desktop/src/components/users/EditUserModal.tsx"
      via: "onConflict meldet die kollidierende Periode an conflictPeriodId"
      pattern: "onConflict"
    - from: "desktop/src/components/users/EditUserModal.tsx"
      to: "desktop/src/components/worktime/WorkTimePeriodList.tsx"
      via: "highlightPeriodId={conflictPeriodId} — Zellhintergrund und 4 px linker Rand"
      pattern: "highlightPeriodId"
---

<objective>
**F-8 schliessen:** Die Kollisionsmarkierung wird korrekt gesetzt (Zellhintergrund
`rgb(254,242,242)` + 4 px linker Rand `rgb(239,68,68)` — der V-2-Fix traegt), erreicht den
Anwender aber **nie**. Zwei Teilprobleme, **ein** Befund, **ein** Commit-Satz (D-10):

**(a) Sie liegt unter dem Dialog.** `document.elementFromPoint()` auf der markierten Zelle
trifft `DIV.rounded-lg border p-4 space-y-3 …` — das Vorschaupanel des Wechsel-Dialogs. Der
Dialog rendert mit `zIndexClass="z-[60]"` (`WorkTimeChangeModal.tsx:599`) ueber dem
`EditUserModal` (Vorgabe `z-50`, `Modal.tsx:22`), in dem die Periodenliste eingebettet ist.
Die Liste liegt damit **strukturell** darunter, solange der Dialog offen ist.

**(b) Sie wird beim Schliessen geloescht.** `handleClose → resetForm → onConflict(null)`
(`:454-472`), und zusaetzlich setzt `EditUserModal.tsx:1075` im `onClose`-Handler
`setConflictPeriodId(null)`. Nach dem Schliessen ist die Markierung weg
(gemessen: `bg rgba(0,0,0,0)`, `borderLeft 0px`).

**Unveraendert bleibt (D-10):** Der Feldfehler „Zum 17.08.2026 existiert bereits eine Periode.
Waehlen Sie ein anderes Datum." und die Sperre des Speichern-Knopfes. Beide sind in Ordnung.

**Nachweis ausschliesslich mit `document.elementFromPoint()`.**
`getBoundingClientRect()` kann ein vom Container beschnittenes oder ueberdecktes Element
**nicht** feststellen — diese Falle ist in diesem Projekt bereits **dreimal** aufgetreten.

Zweck: Der Pfad ist seit WR-11 faktisch tot, nur an einer anderen Stelle als vorher (NB-3,
14-U6 Punkt 3).

**Warum dieser Plan allein in Welle 9 laeuft:** siehe `14.2-01-PLAN.md`, `<objective>`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-CONTEXT.md
@.planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-PATTERNS.md
@.planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-NACHWEIS-D01.md
@.planning/phases/13-korrigieren-und-r-ckg-ngig-machen/13-UI-SPEC.md
@.planning/phases/14-absicherung-und-auslieferung/deferred-items.md

**Umgebung:** Server auf `127.0.0.1:3100`, Vite auf `1420` (Plan 14.2-01). Anmeldung
`admin` / `admin123`. Testnutzer mit mehreren Perioden: `abnahme.vollstaendig` (48717,
Periode `id 22956` ab 17.08.2026). Kein `git push`, kein Deployment (D-03).

<interfaces>
Die Stellen, gegen die dieser Plan arbeitet — aus dem Quelltext entnommen:

  // desktop/src/components/worktime/WorkTimeChangeModal.tsx
  :302      onConflict?.(findCollidingPeriod(vFrom)?.id ?? null);   // Serverfehler in requestPreview.onError
  :387-389  function findCollidingPeriod(candidate) {
              return periodsQuery.data?.find((p) => p.validFrom === candidate) ?? null; }
  :408-412  const collision = findCollidingPeriod(validFrom);
            if (collision) { errors.validFrom = `Zum ${…} existiert bereits eine Periode. …`;
                             onConflict?.(collision.id); }
  :454-466  function resetForm() { … onConflict?.(null); /* WR-11 */ }
  :468-472  function handleClose() { if (isSaving) return; resetForm(); onClose(); }
  :489-491  performSave-Erfolgspfad: resetForm(); … onSaved?.(result);
  :599      zIndexClass="z-[60]"
  :290-293  requestPreview.onSuccess: setPreview(data); setFormError('');   // hier fehlt die Ruecknahme

  // desktop/src/components/ui/Modal.tsx:13,22 — zIndexClass?: string, Vorgabe 'z-50'

  // desktop/src/components/users/EditUserModal.tsx
  :149       const [conflictPeriodId, setConflictPeriodId] = useState<number | null>(null);
  :938-940   <WorkTimePeriodList userId={user.id} highlightPeriodId={conflictPeriodId} … />
  :1072-1077 WorkTimeChangeModal onClose: setIsChangeModalOpen(false); setConflictPeriodId(null); …
  :1079      onConflict={setConflictPeriodId}
  :1080-1082 onSaved: setIsChangeModalOpen(false); setConflictPeriodId(null); …
  :1119      WorkTimePeriodEditModal onConflict={setConflictPeriodId}

  // desktop/src/components/worktime/WorkTimePeriodList.tsx:183,200-203 — die Markierung selbst
  //   isHighlighted -> 'bg-red-50 dark:bg-red-900/20'
  //                  + 'border-l-4 border-red-500 dark:border-red-400'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Die Kollisionsinformation erreicht den Anwender (F-8, Teil a)</name>
  <files>desktop/src/components/worktime/WorkTimeChangeModal.tsx</files>
  <read_first>
    - `.planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-PATTERNS.md`, Abschnitt „F-8" — alle drei Fundstellen woertlich, die z-Index-Kette und der Hinweis auf die zwei Teilfixes
    - `.planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-CONTEXT.md`, Abschnitt D-10 — die Auflage, dass Feldfehler und Sperre unveraendert bleiben, und die Vorschrift `document.elementFromPoint()`
    - `desktop/src/components/worktime/WorkTimeChangeModal.tsx:280-320` — `requestPreview` mit `onSuccess` und `onError`
    - `desktop/src/components/worktime/WorkTimeChangeModal.tsx:380-475` — `findCollidingPeriod`, `validateForm`, `resetForm`, `handleClose`
    - `desktop/src/components/worktime/WorkTimeChangeModal.tsx:590-620` und `:800-860` — die Dialoghuelle mit `zIndexClass="z-[60]"` und das Vorschaupanel, unter dem die Liste verschwindet
    - `desktop/src/components/worktime/WorkTimePeriodList.tsx:175-215` — wie die Markierung gezeichnet wird, samt V-2-Kommentar zur `elementFromPoint`-Falle
    - `desktop/src/types/index.ts:155-183` — `WorkTimePeriod` mit `validFrom`, `validTo`, `weeklyHours`, `isCurrent`, `isFirst`
    - `.planning/phases/13-korrigieren-und-r-ckg-ngig-machen/13-UI-SPEC.md` — der geltende Designvertrag, damit das neue Element sich einfuegt statt ihn zu brechen
  </read_first>
  <action>
**Der Weg ist nicht, den z-Index zu drehen.** `z-[60]` ueber `z-50` ist genau die Stapelung,
die P12-31 und P12-5 gemessen und als richtig abgenommen haben (der obere Dialog liegt oben,
ESC schliesst nur den obersten). Wer sie umkehrt, bricht eine bestandene Pruefung. Die
Kollisionsinformation muss **im Dialog selbst** ankommen, solange er offen ist.

**A — Ein Kollisionspanel im Wechsel-Dialog.** Wenn `findCollidingPeriod(validFrom)` einen
Treffer liefert, rendert der Dialog **zusaetzlich** zum unveraenderten Feldfehler ein kurzes
Panel mit den konkreten Daten der kollidierenden Periode:

- Ueberschrift, die den Vorgang benennt (z. B. „Diese Periode belegt den Stichtag bereits").
- Die Periode in Zahlen: Geltungszeitraum von `validFrom` bis `validTo` (bei `validTo === null`
  in Worten „laufend"), dazu `weeklyHours`. Datumsangaben ueber die im Dialog bereits benutzte
  Hilfsfunktion `formatGermanDate`, **nicht** ueber `toISOString()`.
- Ein Satz, der sagt, dass dieselbe Zeile in der Periodenliste hinter dem Dialog markiert ist
  und dort nach dem Schliessen sichtbar bleibt.

Das Panel steht **oberhalb** des Vorschaupanels und in derselben Spalte, damit es nicht
seinerseits von etwas ueberdeckt wird — genau das ist in Task 2 mit `elementFromPoint()` zu
messen. Farbgebung nach dem im Projekt bereits verwendeten Fehlerband (rot), mit
`dark:`-Entsprechung fuer jede Klasse (**Dark Mode ist Pflicht**). Der Kontrast jedes Textknotens
muss **≥ 4,5:1** in hell **und** dunkel liegen; die Messung gehoert zu Task 2 und nicht zu
Plan 14.2-10 — ein in dieser Phase neu eingefuegtes Element darf die D-1-Bilanz nicht
verschlechtern.

Das Panel traegt `role="status"` (nicht `role="alert"` — der Feldfehler ist bereits die
Warnung; zwei gleichzeitige `alert`-Ausgaben ueberlagern sich fuer Screenreader) und ist
keine Interaktionsflaeche.

**B — Die Markierung ueberlebt das Schliessen (Teil b, Clientseite des Dialogs).**
`resetForm()` (`:454-466`) verliert seinen unbedingten Aufruf `onConflict?.(null)`. Statt
dessen wird die Markierung genau dort zurueckgenommen, wo sie ihren Zweck erfuellt hat:

- in `requestPreview`s `onSuccess` (`:290-293`), also sobald ein **kollisionsfreier** Stichtag
  eine gueltige Vorschau liefert — dort steht bereits `setFormError('')`;
- im Erfolgspfad von `performSave` (`:489-491`) — dort ruft `EditUserModal` ueber `onSaved`
  ohnehin bereits `setConflictPeriodId(null)` (`:1082`); der Ausfuehrende prueft, ob ein
  zusaetzlicher Aufruf noetig ist oder ob der bestehende genuegt, und haelt das Ergebnis im
  SUMMARY fest.

`handleClose()` ruft weiterhin `resetForm()` und `onClose()` — nur nimmt `resetForm()` die
Markierung nicht mehr mit. Die uebrigen zehn Ruecksetzungen in `resetForm()` bleiben
zeichengleich.

Ueber beiden Aenderungen ein Kommentar, der **F-8** nennt, den gemessenen Ausgangsbefund
(`elementFromPoint` trifft das Vorschaupanel; nach dem Schliessen `bg rgba(0,0,0,0)`,
`borderLeft 0px`) und den Grund, warum die Ruecknahme wandert statt zu verschwinden.

**Ausdruecklich nicht anfassen (Scope Fence, D-02, D-10):**
- Der Feldfehlertext bei `:410` und die Sperre des Speichern-Knopfes — beide sind in Ordnung.
- `zIndexClass="z-[60]"` (`:599`) und die `Modal`/`ConfirmDialog`-Vorgabe `z-50` — die
  Stapelung ist unter P12-31 und P12-5 abgenommen.
- `WorkTimePeriodList.tsx` — die Markierung selbst ist seit dem V-2-Fix korrekt.
- `WorkTimePeriodEditModal.tsx` — der Korrektur-Dialog meldet ebenfalls ueber `onConflict`
  (`EditUserModal.tsx:1119`). Faellt dort dasselbe Muster auf, geht es nach
  `deferred-items.md`; es ist ein eigener Befund.

Commit: `fix(14.2-F-8): Kollisionsinfo im Dialog, Markierung ueberlebt das Schliessen`
  </action>
  <acceptance_criteria>
    - `cd desktop && sed -n '/function resetForm/,/^  }/p' src/components/worktime/WorkTimeChangeModal.tsx | grep -c "onConflict"` liefert **0**
    - `cd desktop && sed -n '/onSuccess: (data)/,/},/p' src/components/worktime/WorkTimeChangeModal.tsx | grep -c "onConflict"` liefert mindestens **1** — die Ruecknahme steht jetzt am kollisionsfreien Vorschauerfolg
    - `cd desktop && grep -c "existiert bereits eine Periode. Wählen Sie ein anderes Datum." src/components/worktime/WorkTimeChangeModal.tsx` liefert weiterhin **1** — der Feldfehlertext ist unveraendert (D-10)
    - `cd desktop && grep -c 'zIndexClass="z-\[60\]"' src/components/worktime/WorkTimeChangeModal.tsx` liefert weiterhin **1** — die Stapelung ist unveraendert
    - `cd desktop && grep -q 'role="status"' src/components/worktime/WorkTimeChangeModal.tsx` — das Kollisionspanel ist kein zweites `alert`
    - `cd desktop && grep -c "toISOString" src/components/worktime/WorkTimeChangeModal.tsx` liefert **0**
    - Jede im Kollisionspanel neu hinzugekommene Tailwind-Farbklasse hat eine `dark:`-Entsprechung in derselben `className`; per Diff belegt und im SUMMARY bestaetigt
    - Der Kommentar an den geaenderten Stellen nennt woertlich `F-8` und den gemessenen Ausgangsbefund
    - `cd desktop && npx tsc --noEmit` Exit 0 und `npm run check:rules` Exit 0
    - `git show --stat HEAD` zeigt genau `desktop/src/components/worktime/WorkTimeChangeModal.tsx`
  </acceptance_criteria>
  <verify>
    <automated>cd desktop && npx tsc --noEmit && npm run check:rules && test "$(sed -n '/function resetForm/,/^  }/p' src/components/worktime/WorkTimeChangeModal.tsx | grep -c onConflict)" = "0" && grep -q 'zIndexClass="z-\[60\]"' src/components/worktime/WorkTimeChangeModal.tsx && echo GATE_OK</automated>
  </verify>
  <done>Der Dialog zeigt die kollidierende Periode in Zahlen, der Feldfehler und die Stapelung sind unveraendert, und `resetForm` nimmt die Markierung nicht mehr mit.</done>
</task>

<task type="auto">
  <name>Task 2: EditUserModal haelt die Markierung, und alles wird mit elementFromPoint gemessen</name>
  <files>desktop/src/components/users/EditUserModal.tsx, desktop/tests/messungen/f8-kollisionsmarkierung.mjs, .planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-NACHWEIS-F8.md</files>
  <read_first>
    - `desktop/src/components/users/EditUserModal.tsx:140-160` — `conflictPeriodId` in seiner Deklaration samt Kommentar
    - `desktop/src/components/users/EditUserModal.tsx:930-965` — die Verdrahtung `highlightPeriodId={conflictPeriodId}` an `WorkTimePeriodList`
    - `desktop/src/components/users/EditUserModal.tsx:1065-1125` — beide Dialoge und ihre `onClose`/`onSaved`/`onConflict`-Handler vollstaendig
    - `desktop/src/components/worktime/WorkTimePeriodList.tsx:175-215` — die Markierungsklassen und der V-2-Kommentar zur `elementFromPoint`-Falle
    - `.planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-CONTEXT.md`, Abschnitt D-10 und der Abschnitt „Messen heisst messen"
    - `.planning/phases/14-absicherung-und-auslieferung/14-ABNAHME-SICHT.md`, Abschnitt „5. …", **14-U6 Punkt 3** — die woertlichen Ausgangsmesswerte
  </read_first>
  <action>
**A — `EditUserModal.tsx`: die zweite Ruecknahme entfernen.** Im `onClose`-Handler des
`WorkTimeChangeModal` (`:1072-1077`) faellt `setConflictPeriodId(null)` weg — sonst haelt die
Aenderung aus Task 1 nicht. `setIsChangeModalOpen(false)` und die Fokusrueckgabe
`changeButtonRef.current?.focus()` bleiben.

Im `onSaved`-Handler (`:1080-1082`) bleibt `setConflictPeriodId(null)` **erhalten** — nach
erfolgreichem Speichern ist die Kollision aufgeloest und die Markierung waere irrefuehrend.

Zusaetzlich braucht die Markierung eine **Endstelle**, damit sie nicht ueber die ganze Sitzung
haengen bleibt: Beim Schliessen des `EditUserModal` selbst wird `conflictPeriodId`
zurueckgesetzt. Der Ausfuehrende findet die dafuer zustaendige Stelle am Quelltext (der
`onClose`-Weg des `EditUserModal` bzw. sein Ruecksetzeffekt beim Wechsel von `isOpen`) und
setzt sie dort — nicht an einer geratenen Stelle. Welche Stelle es geworden ist, gehoert ins
SUMMARY.

Der Kommentar an `conflictPeriodId` (`:146-149`) wird um einen Satz ergaenzt, der **F-8**
nennt und festhaelt, welche Ereignisse die Markierung setzen und welche sie zuruecknehmen —
damit die Lebensdauer an einer Stelle nachlesbar ist.

**B — Messung, ausschliesslich mit `document.elementFromPoint()`.** Ein Skript
`desktop/tests/messungen/f8-kollisionsmarkierung.mjs` (kein `*.spec.ts`), ueber
`playwright-core`, gegen den laufenden Server. Es misst **vier** Zustaende. In jedem Zustand
wird der Mittelpunkt des jeweils gepruefften Elements bestimmt und
`document.elementFromPoint(x, y)` aufgerufen; das Ergebnis wird als `tagName` + `className`
protokolliert. `getBoundingClientRect()` darf **ausschliesslich** zur Bestimmung des
Mittelpunkts dienen, **nie** als Beleg fuer Sichtbarkeit.

- **Zustand 1 — Dialog offen, Kollision eingetragen.** Als Admin `abnahme.vollstaendig`
  bearbeiten, Wechsel-Dialog oeffnen, als Stichtag den `validFrom` einer bestehenden Periode
  eintragen (`17.08.2026`, Periode `id 22956`). Gemessen wird das **Kollisionspanel** aus
  Task 1: `elementFromPoint` muss das Panel selbst oder einen seiner Nachfahren treffen —
  **nicht** ein darueberliegendes Element. Zusaetzlich der Feldfehlertext (muss unveraendert
  dastehen) und der Zustand des Speichern-Knopfes (muss gesperrt sein).
- **Zustand 2 — Dialog geschlossen.** Den Wechsel-Dialog ueber „Abbrechen" schliessen. Gemessen
  wird die markierte Zeile in der Periodenliste: `elementFromPoint` auf ihrem Mittelpunkt muss
  die Zeile oder eine ihrer Zellen treffen, und die berechneten Stile muessen den
  Zellhintergrund `rgb(254,242,242)` (hell) und den 4 px linken Rand `rgb(239,68,68)` tragen.
  **Ausgangsbefund zum Vergleich:** `bg rgba(0, 0, 0, 0)`, `borderLeft 0px`.
- **Zustand 3 — kollisionsfreier Stichtag.** Dialog erneut oeffnen, einen freien Stichtag
  eintragen, die Vorschau abwarten, Dialog schliessen. Erwartung: **keine** Zeile traegt die
  Markierung mehr (`borderLeft` auf allen Zeilen `0px`).
- **Zustand 4 — Dunkelmodus.** Zustand 2 im Dunkelmodus wiederholen. Umschalter klicken und
  `document.documentElement.classList.contains('dark') === true` **bestaetigen**, bevor
  gemessen wird. Erwartung: `bg` entspricht `dark:bg-red-900/20`, Rand `dark:border-red-400`;
  `elementFromPoint` trifft weiterhin die Zeile.

**Zusaetzlich, weil dieser Plan ein neues sichtbares Element einfuehrt:** Fuer **jeden**
Textknoten des Kollisionspanels wird der Kontrast gemessen — hell **und** dunkel, mit
aufgeloesten Alpha- und Elternhintergruenden. Soll: **≥ 4,5:1**. Ein in dieser Phase neu
eingefuegtes Element darf die D-1-Bilanz (Plan 14.2-10) nicht verschlechtern.

**C — `14.2-NACHWEIS-F8.md`** mit je einem Abschnitt pro Zustand. Jeder Abschnitt enthaelt:
den Aufruf, das `elementFromPoint`-Ergebnis als `tagName` + `className`, die gemessenen Stile
als `rgb(...)`-Werte, und ein **Ja/Nein**. Dazu eine Tabelle der Kontrastwerte des neuen
Panels (Element | hell | dunkel | Soll | Ja/Nein). Und ein Abschnitt „Ausgangsbefund" mit den
woertlichen Messwerten aus `14-ABNAHME-SICHT.md`, 14-U6 Punkt 3.

Commits:
`fix(14.2-F-8): conflictPeriodId ueberlebt das Schliessen des Wechsel-Dialogs`
`test(14.2-F-8): elementFromPoint-Messung in vier Zustaenden`
  </action>
  <acceptance_criteria>
    - `cd desktop && sed -n '/<WorkTimeChangeModal/,/^      \/>/p' src/components/users/EditUserModal.tsx | sed -n '/onClose={() => {/,/}}/p' | grep -c "setConflictPeriodId(null)"` liefert **0**
    - `cd desktop && sed -n '/<WorkTimeChangeModal/,/^      \/>/p' src/components/users/EditUserModal.tsx | sed -n '/onSaved=/,/}}/p' | grep -c "setConflictPeriodId(null)"` liefert mindestens **1** — nach erfolgreichem Speichern wird zurueckgenommen
    - `cd desktop && grep -c "setConflictPeriodId(null)" src/components/users/EditUserModal.tsx` liefert mindestens **2** — die Endstelle beim Schliessen des `EditUserModal` existiert; welche Stelle es ist, steht im SUMMARY
    - Der Kommentar an `conflictPeriodId` nennt woertlich `F-8` und listet setzende und zuruecknehmende Ereignisse
    - `desktop/tests/messungen/f8-kollisionsmarkierung.mjs` existiert; `cd desktop && node ../node_modules/@playwright/test/cli.js test --list` zaehlt weiterhin genau **23** Faelle in genau **drei** Dateien
    - Das Messskript ruft `document.elementFromPoint` auf; `grep -c "elementFromPoint" desktop/tests/messungen/f8-kollisionsmarkierung.mjs` liefert mindestens **4**
    - `14.2-NACHWEIS-F8.md`: Zustand 1 — `elementFromPoint` trifft das Kollisionspanel oder einen Nachfahren; `tagName` und `className` stehen woertlich da; Ergebnis **Ja**
    - `14.2-NACHWEIS-F8.md`: Zustand 2 — `elementFromPoint` trifft die markierte Zeile, gemessener Hintergrund `rgb(254, 242, 242)` und `border-left-width: 4px` mit `rgb(239, 68, 68)`; daneben der Ausgangsbefund `rgba(0, 0, 0, 0)` / `0px`; Ergebnis **Ja**
    - `14.2-NACHWEIS-F8.md`: Zustand 3 — keine Zeile traegt mehr eine Markierung; Ergebnis **Ja**
    - `14.2-NACHWEIS-F8.md`: Zustand 4 — vor der Messung ist `classList.contains('dark') === true` protokolliert; `elementFromPoint` trifft die Zeile; Ergebnis **Ja**
    - `14.2-NACHWEIS-F8.md` enthaelt eine Kontrasttabelle des neuen Kollisionspanels mit je einem Wert hell und dunkel; **jeder** Wert ist ≥ 4,5
    - Der Feldfehlertext und der gesperrte Speichern-Knopf sind in Zustand 1 mitgemessen und unveraendert (D-10)
    - Das Messskript endet mit Exit 0 nur, wenn alle Erwartungen halten
    - `cd desktop && npx tsc --noEmit` Exit 0 und `npm run check:rules` Exit 0
  </acceptance_criteria>
  <verify>
    <automated>cd desktop && npx tsc --noEmit && npm run check:rules && node tests/messungen/f8-kollisionsmarkierung.mjs && echo MESSUNG_OK</automated>
  </verify>
  <done>Vier Zustaende sind mit `document.elementFromPoint()` gemessen: Die Kollisionsinfo erreicht den Anwender bei offenem Dialog, die Markierung ueberlebt das Schliessen, verschwindet bei einem freien Stichtag und traegt auch im Dunkelmodus.</done>
</task>

<task type="auto">
  <name>Task 3: Abschlussgate und D-01-Nachweis</name>
  <files>.planning/phases/14-absicherung-und-auslieferung/deferred-items.md</files>
  <read_first>
    - `.planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-NACHWEIS-D01.md` — die Ausgangswerte, gegen die gemessen wird
    - `.planning/phases/14-absicherung-und-auslieferung/deferred-items.md`, letzter Abschnitt — Format der bestehenden Eintraege
  </read_first>
  <action>
Ohne weitere Codeaenderung:

1. `cd server && npx tsc --noEmit` — Exit 0.
2. `cd desktop && npx tsc --noEmit` — Exit 0.
3. `cd desktop && npm run check:rules` — Exit 0.
4. `cd server && npx vitest run` — rote Menge nicht gewachsen; Zahl gruen/rot ins SUMMARY.
5. `cd desktop && node ../node_modules/@playwright/test/cli.js test --reporter=list` — **0 rot**.
   Dieser Plan aendert `EditUserModal.tsx`, das die E2E-Faelle benutzen.
6. **D-01-Nachweis:** `check:protected-tables --markdown` vor und nach dem Plan; alle fuenf
   Zeilenzahlen und SHA-256 identisch. Die Messung in Task 2 legt **keine** Periode an (sie
   traegt nur einen Stichtag ein und bricht ab); wird dennoch gespeichert, betrifft das
   `user_work_periods` und nicht die fuenf geschuetzten. Beide Tabellen ins SUMMARY.
7. `deferred-items.md`, neuer Abschnitt `## Aus Plan 14.2-09 (F-8)`: insbesondere, ob
   `WorkTimePeriodEditModal.tsx` (der Korrektur-Dialog, `EditUserModal.tsx:1119`) dasselbe
   Ruecknahmemuster traegt — geprueft und mit Ergebnis vermerkt, **nicht** mitrepariert
   (eigener Befund, D-02). Dazu alles Weitere, was in keinen der elf faellt.
8. Kein `git push`, kein Deployment (D-03).

Commit: `docs(14.2-09): Abschlussgate F-8, D-01-Pruefsummen und zurueckgestellte Nebenbefunde`
  </action>
  <acceptance_criteria>
    - `cd server && npx tsc --noEmit` Exit 0 und `cd desktop && npx tsc --noEmit` Exit 0
    - `cd desktop && npm run check:rules` Exit 0
    - `cd server && npx vitest run` endet mit hoechstens 3 roten Tests, und die roten sind genau die drei vorbestehenden
    - `cd desktop && node ../node_modules/@playwright/test/cli.js test --reporter=list` endet mit **0 failed**
    - Fuer alle fuenf geschuetzten Tabellen: Zeilenzahl vorher = nachher **und** SHA-256 vorher = nachher, alle fuenf Paare als Tabelle im SUMMARY (D-01)
    - `deferred-items.md` enthaelt einen neuen Abschnitt `## Aus Plan 14.2-09 (F-8)` mit dem gepruefften Ergebnis zu `WorkTimePeriodEditModal.tsx`
    - `git log --oneline` seit Planbeginn zeigt genau drei bis vier Commits — jeder einzeln revertierbar (D-02)
    - `git log origin/main..HEAD` ist nicht leer und wurde nicht gepusht (D-03)
  </acceptance_criteria>
  <verify>
    <automated>cd server && npx tsc --noEmit && npx vitest run 2>&1 | tail -20 && DATABASE_PATH=./database/development.db npm run check:protected-tables && cd ../desktop && npx tsc --noEmit && npm run check:rules && node ../node_modules/@playwright/test/cli.js test --reporter=list 2>&1 | tail -10</automated>
  </verify>
  <done>Alle vier Gates sind gruen, die drei E2E-Dateien laufen ohne roten Fall, und die fuenf geschuetzten Tabellen sind mit Zahlen als unberuehrt belegt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Periodendaten → neues Anzeigeelement | Das Kollisionspanel zeigt Daten einer bestehenden Periode eines fremden Nutzers |
| Dialogstapelung → Sichtbarkeit | Die z-Index-Kette entscheidet, welche Information den Anwender erreicht |
| Markierungslebensdauer → Bedienentscheidung | Eine Markierung, die haengen bleibt, benennt eine Kollision, die es nicht mehr gibt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.2-09-01 | Information Disclosure | Das Kollisionspanel zeigt Periodendaten eines fremden Nutzers | accept | Der Dialog ist nur aus `EditUserModal` erreichbar, das seinerseits nur Admins offensteht (`isAdmin ? user.id : null` bei `EditUserModal.tsx:176`, serverseitig `requireAdmin` auf `PUT /api/users/:id` und 403 auf `GET /api/work-periods` fuer fremde `userId`, gemessen unter P12-2/13-U7a). Das Panel zeigt genau die Daten, die die Periodenliste im selben Dialog bereits zeigt — kein neues Feld |
| T-14.2-09-02 | Denial of Service | Ein haengengebliebener `conflictPeriodId` markiert dauerhaft eine falsche Zeile | mitigate | Drei Ruecknahmestellen: kollisionsfreier Vorschauerfolg, erfolgreiches Speichern, Schliessen des `EditUserModal`. Zustand 3 der Messung belegt die erste, das Abnahmekriterium `grep -c "setConflictPeriodId(null)" >= 2` die uebrigen |
| T-14.2-09-03 | Tampering | Der Fix dreht die z-Index-Stapelung und bricht die unter P12-31/P12-5 abgenommene Reihenfolge | mitigate | `zIndexClass="z-[60]"` bleibt unveraendert — Abnahmekriterium prueft es per `grep`. Die Information wird stattdessen im Dialog gezeigt |
| T-14.2-09-04 | Repudiation | Sichtbarkeit wird mit `getBoundingClientRect()` behauptet statt mit `elementFromPoint()` gemessen | mitigate | Vorschrift aus D-10; das Abnahmekriterium verlangt mindestens vier `elementFromPoint`-Aufrufe im Messskript und je Zustand ein protokolliertes `tagName`+`className`. `getBoundingClientRect()` ist ausschliesslich zur Mittelpunktbestimmung zugelassen |
| T-14.2-09-05 | Information Disclosure | Zwei gleichzeitige `role="alert"`-Ausgaben ueberlagern sich fuer Screenreader | mitigate | Das neue Panel traegt `role="status"`; der bestehende Feldfehler bleibt die Warnung |
| T-14.2-09-06 | Tampering | Das neue Element verschlechtert die D-1-Kontrastbilanz | mitigate | Jeder Textknoten des Panels wird hell und dunkel gemessen, Soll ≥ 4,5:1; die Tabelle ist Abnahmekriterium |

**Keine Paketinstallation in diesem Plan** — kein `npm install`. Damit ist kein
Legitimitaets-Gate und kein `T-14.2-SC`-Eintrag erforderlich.
</threat_model>

<verification>
1. `cd desktop && node tests/messungen/f8-kollisionsmarkierung.mjs` — Exit 0 ueber alle vier Zustaende
2. `14.2-NACHWEIS-F8.md`: je Zustand `elementFromPoint`-Ergebnis, `rgb(...)`-Werte und ein Ja/Nein
3. Kontrasttabelle des neuen Panels: jeder Wert ≥ 4,5 in hell **und** dunkel
4. Feldfehlertext und gesperrter Speichern-Knopf in Zustand 1 unveraendert (D-10)
5. `cd desktop && node ../node_modules/@playwright/test/cli.js test --reporter=list` — 0 failed
6. `cd desktop && npx tsc --noEmit && npm run check:rules` — beide Exit 0
7. `cd server && npx tsc --noEmit` — Exit 0
8. `cd server && npx vitest run` — hoechstens 3 rot, und zwar genau die drei vorbestehenden
9. D-01-Pruefsummen der fuenf Tabellen: Differenz 0
10. `git log --oneline` — drei bis vier Commits, kein Push
</verification>

<success_criteria>
- Die Kollisionsinformation erreicht den Anwender, solange der Dialog offen ist — mit
  `document.elementFromPoint()` belegt
- Die Zeilenmarkierung ueberlebt das Schliessen des Wechsel-Dialogs und ist sichtbar
- Sie verschwindet bei einem kollisionsfreien Stichtag und nach erfolgreichem Speichern
- Feldfehler, Sperre des Speichern-Knopfes und z-Index-Stapelung sind unveraendert (D-10)
- Das neue Element haelt in hell und dunkel mindestens 4,5:1
- Die fuenf geschuetzten Tabellen sind unberuehrt (D-01)
- Ein unvermischter Commit-Satz fuer F-8 (D-02)
</success_criteria>

<output>
Create `.planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-09-SUMMARY.md` when done
</output>
