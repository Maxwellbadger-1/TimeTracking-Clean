---
phase: 14.2-restbefunde-der-abnahme-schliessen
plan: 04
type: execute
wave: 4
depends_on: ["14.2-03"]
files_modified:
  - desktop/src/components/ui/Select.tsx
  - desktop/src/components/users/EditUserModal.tsx
  - desktop/tests/messungen/f3-rollenfeld.mjs
  - .planning/phases/14-absicherung-und-auslieferung/deferred-items.md
autonomous: true
requirements: []
decisions: [D-01, D-02, D-03, D-06, D-14, D-15]

must_haves:
  truths:
    - "F-3: Das Rollenfeld im Dialog 'Benutzer bearbeiten' traegt name='role' — der Selektor select[name=\"role\"] trifft"
    - "F-3: Select.tsx erzeugt wie Input.tsx und Textarea.tsx ein htmlFor/id-Paar, auch wenn der Aufrufer keine id uebergibt"
    - "F-3: Select.tsx bindet Fehler und Hilfetext ueber aria-describedby an und setzt aria-invalid — dasselbe WR-16-Muster wie Input.tsx"
    - "F-3: Der E2E-Fall 'Change employee role from employee to admin' (user-edit.spec.ts:308) ist gruen"
    - "F-3: Der Test wurde nicht umgeschrieben — der Selektor select[name=\"role\"] ist unveraendert geblieben"
    - "D-01: Keine Zeile in den fuenf geschuetzten Tabellen wurde angefasst (Zeilenzahl und SHA-256 vorher = nachher)"
  artifacts:
    - path: "desktop/src/components/ui/Select.tsx"
      provides: "useId, htmlFor, id-Passthrough, aria-invalid, aria-describedby"
      contains: "useId"
    - path: "desktop/src/components/users/EditUserModal.tsx"
      provides: "name/id am Rollenfeld"
      contains: "name=\"role\""
  key_links:
    - from: "desktop/src/components/users/EditUserModal.tsx"
      to: "desktop/src/components/ui/Select.tsx"
      via: "<Select label=\"Rolle\" name=\"role\" …> — die Prop wandert ueber {...props} auf das <select>"
      pattern: "name=\"role\""
    - from: "desktop/src/components/ui/Select.tsx"
      to: "desktop/src/components/ui/Input.tsx"
      via: "identisches WR-16-Muster (useId/htmlFor/aria-describedby)"
      pattern: "useId"
---

<objective>
**F-3 schliessen:** `EditUserModal.tsx:862` reicht an `<Select label="Rolle" …>` weder `name`
noch `id` durch. Die Laufzeitabfrage liefert `{"name":null,"id":""}`. Der E2E-Selektor
`select[name="role"]` kann damit nie treffen — der zweite rote Fall des Ausgangsstands
(`user-edit.spec.ts:308`, `TimeoutError: page.selectOption`). Zum selben Befund gehoert, dass
`Select.tsx` — anders als `Input.tsx` und `Textarea.tsx` — kein `htmlFor`/`id`-Paar erzeugt:
gleiche Ursache, gleiche Datei, ein Commit-Satz (D-06).

**Der Test wird nicht angepasst.** D-06 ist eindeutig: Der Selektor ist korrekt, der Code ist
falsch. Wer `user-edit.spec.ts` umschreibt, hat den Befund nicht behoben, sondern verdeckt.

Zweck: Drittes Erfolgskriterium der Phase — „Die drei E2E-Dateien laufen ohne roten Fall".
Zusammen mit Plan 14.2-03 (F-4) sind danach beide roten Faelle geschlossen.

Ergebnis: `Select.tsx` traegt dasselbe WR-16-Muster wie seine beiden Schwesterprimitiven, das
Rollenfeld ist ueber einen stabilen Selektor ansprechbar und hat einen zugaenglichen Namen,
und der E2E-Fall ist gruen.

**Warum dieser Plan allein in Welle 4 laeuft:** siehe `14.2-01-PLAN.md`, `<objective>`. Zudem
fasst Plan 14.2-10 (D-1, Fund 3) dieselbe Datei `Select.tsx` an — dort geht es um die Farbe des
Pflichtfeld-Sterns, hier um `useId`/`htmlFor`. Zwei Befunde, zwei Commit-Saetze, zwei Wellen
(D-02, D-14).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-CONTEXT.md
@.planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-PATTERNS.md
@.planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-NACHWEIS-D01.md
@.planning/phases/14-absicherung-und-auslieferung/deferred-items.md

**Umgebung:** Server auf `127.0.0.1:3100`, Vite auf `1420` (Plan 14.2-01). Playwright
ausschliesslich ueber `node ../node_modules/@playwright/test/cli.js`. Kein `git push`, kein
Deployment (D-03).

<interfaces>
Der Zielzustand von `Select.tsx` ist das bereits im Projekt vorhandene Muster aus
`Input.tsx:23-50` — hier stehen die Bezeichner, die uebernommen werden:

  const generatedId = useId();
  const inputId  = props.id ?? generatedId;     // in Select sinngemaess: selectId
  const errorId  = `${inputId}-error`;
  const helperId = `${inputId}-helper`;
  const showHelper = !!helperText && !error;

  <label htmlFor={inputId} …>
  <input id={inputId} aria-invalid={error ? true : undefined}
         aria-describedby={error ? errorId : showHelper ? helperId : undefined} … {...props} />
  <p id={errorId} role="alert" …>   /   <p id={helperId} …>

`Textarea.tsx:15-42` traegt dasselbe Muster bereits. `Select.tsx` ist die einzige der drei
Formular-Primitiven ohne. Wichtig: `{...props}` steht in allen dreien **nach** `id={…}` —
uebergibt ein Aufrufer eine eigene `id`, gewinnt sie, und `htmlFor` folgt ihr ohnehin, weil
`inputId` aus `props.id` abgeleitet ist.

Die Aufrufstelle, heutiger Stand (`EditUserModal.tsx:862-871`):

  <Select label="Rolle" value={role} onChange={…}
          options={[{value:'employee',label:'Mitarbeiter'},{value:'admin',label:'Administrator'}]}
          required />

Das Nachbarfeld `<Input name="weeklyHours" …>` bei `:873` ist die Schreibvorlage fuer die
`name`-Uebergabe.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Select.tsx traegt das WR-16-Muster (F-3)</name>
  <files>desktop/src/components/ui/Select.tsx</files>
  <read_first>
    - `.planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-PATTERNS.md`, Abschnitt „F-3" — die vollstaendig zitierte Fundstelle, das Vorbild und die Praezisierung, dass `{...props}` `name`/`id` technisch bereits durchreicht
    - `.planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-CONTEXT.md`, Abschnitt D-06 — die Festlegung „der Code ist falsch, nicht der Test"
    - `desktop/src/components/ui/Input.tsx:1-84` — die Vorlage vollstaendig, inklusive des WR-16-Begruendungskommentars
    - `desktop/src/components/ui/Textarea.tsx` vollstaendig — die zweite, bereits umgestellte Primitive
    - `desktop/src/components/ui/Select.tsx:1-65` — die zu aendernde Datei vollstaendig
  </read_first>
  <action>
`Select.tsx` bekommt exakt das Muster aus `Input.tsx`, ohne eigene Erfindungen:

- `useId` aus `react` importieren (die Importzeile lautet heute
  `import { SelectHTMLAttributes, forwardRef } from 'react';`).
- Im Funktionsrumpf vor `selectStyles`: `generatedId` aus `useId()`, `selectId` aus
  `props.id ?? generatedId`, `errorId` als `${selectId}-error`, `helperId` als
  `${selectId}-helper`, `showHelper` als `!!helperText && !error`.
- Das `<label>` bei Zeile 19 bekommt `htmlFor={selectId}`.
- Das `<select>` bei Zeile 24 bekommt `id={selectId}`, `aria-invalid={error ? true : undefined}`
  und `aria-describedby={error ? errorId : showHelper ? helperId : undefined}` — jeweils
  **vor** dem `{...props}`, damit ein Aufrufer sie ueberschreiben kann, genau wie in `Input.tsx`.
- Das Fehler-`<p>` bei Zeile 51 bekommt `id={errorId}`; das Hilfetext-`<p>` bei Zeile 56
  bekommt `id={helperId}` und seine Bedingung wird auf `showHelper` umgestellt (heute
  `helperText && !error`, inhaltsgleich — die Umstellung haelt beide Primitiven zeichengleich).
- Der Klassenstring des `<select>`, die `options`-Schleife, `forwardRef`, `displayName` und die
  DOM-Struktur (Label direkt gefolgt vom Feld) bleiben **unveraendert**, damit bestehende
  Selektoren weiter greifen.

Darueber ein Begruendungskommentar nach dem Vorbild des WR-16-Kommentars in `Input.tsx:9-22`
— er nennt woertlich **F-3**, dass `Select` die letzte der drei Primitiven ohne dieses Muster
war, und dass die Ergaenzung rein additiv ist (kein Aufrufer muss etwas uebergeben).

**Ausdruecklich nicht anfassen (Scope Fence, D-02):** Der Pflichtfeld-Stern bei Zeile 21
(`<span className="text-red-500 ml-1">*</span>`) ist **D-1, Fund 3** und wird in Plan
14.2-10 geaendert, nicht hier. Seine Zeile bleibt in diesem Commit zeichengleich.

Commit (nur diese Datei):
`fix(14.2-F-3): Select erzeugt htmlFor/id-Paar und bindet Fehler und Hilfetext an`
  </action>
  <acceptance_criteria>
    - `cd desktop && grep -q "useId" src/components/ui/Select.tsx`
    - `cd desktop && grep -q "htmlFor={selectId}" src/components/ui/Select.tsx`
    - `cd desktop && grep -q "id={selectId}" src/components/ui/Select.tsx`
    - `cd desktop && grep -q "aria-invalid" src/components/ui/Select.tsx && grep -q "aria-describedby" src/components/ui/Select.tsx`
    - `cd desktop && grep -q 'id={errorId}' src/components/ui/Select.tsx && grep -q 'id={helperId}' src/components/ui/Select.tsx`
    - `cd desktop && grep -c 'text-red-500 ml-1' src/components/ui/Select.tsx` liefert weiterhin **1** — der Pflichtfeld-Stern ist in diesem Commit unveraendert (D-1 gehoert Plan 14.2-10)
    - Der Kommentar ueber `Select` nennt woertlich `F-3`
    - `cd desktop && npx tsc --noEmit` Exit 0 und `cd desktop && npm run check:rules` Exit 0
    - `git show --stat HEAD` zeigt genau `desktop/src/components/ui/Select.tsx`
  </acceptance_criteria>
  <verify>
    <automated>cd desktop && npx tsc --noEmit && npm run check:rules && grep -q "useId" src/components/ui/Select.tsx && grep -q "htmlFor={selectId}" src/components/ui/Select.tsx && echo GATE_OK</automated>
  </verify>
  <done>`Select.tsx` traegt dasselbe WR-16-Muster wie `Input.tsx` und `Textarea.tsx`; der Pflichtfeld-Stern ist unangetastet.</done>
</task>

<task type="auto">
  <name>Task 2: name am Rollenfeld und E2E-Nachweis (F-3)</name>
  <files>desktop/src/components/users/EditUserModal.tsx</files>
  <read_first>
    - `desktop/src/components/users/EditUserModal.tsx:855-895` — die Aufrufstelle des Rollenfelds und das Nachbarfeld `<Input name="weeklyHours" …>` als Schreibvorlage
    - `desktop/tests/user-edit.spec.ts:295-340` — der rote E2E-Fall „Change employee role from employee to admin" vollstaendig, insbesondere der verwendete Selektor
    - `.planning/phases/14-absicherung-und-auslieferung/14-ABNAHME-SICHT.md`, Abschnitt „3. Die drei E2E-Dateien", Fehlschlag 2 — die gemessene Laufzeitabfrage `{"name":null,"id":"","val":"employee"}`
  </read_first>
  <action>
**A —** Der `<Select label="Rolle" …>`-Aufruf bei `EditUserModal.tsx:862` bekommt
`name="role"` und `id="role"`. `name` ist der Traeger des E2E-Selektors und macht das Feld in
einem Formular adressierbar; `id` ist optional (die Primitive erzeugt seit Task 1 selbst eine),
wird hier aber ausdruecklich gesetzt, damit `label for="role"` einen stabilen, im Bericht
nachvollziehbaren Wert traegt statt einer generierten React-Kennung. Reihenfolge und
Schreibweise nach dem Vorbild des Nachbarfelds `<Input name="weeklyHours" …>` bei `:873`.

**B — Weitere `<Select>`-Aufrufe erheben, aber nicht mitaendern.** Der Ausfuehrende sucht
projektweit (`grep -rn "<Select" desktop/src`) alle Aufrufstellen und listet im SUMMARY, welche
kein `name` uebergeben. **Geaendert wird nur das Rollenfeld** — jede weitere Stelle waere ein
zweiter Befund (D-02). Die Liste geht als Eintrag nach
`.planning/phases/14-absicherung-und-auslieferung/deferred-items.md`, damit niemand sie ein
zweites Mal suchen muss.

**C — E2E-Nachweis, gemessen statt behauptet.** Mit laufendem Server (127.0.0.1:3100) und
laufendem Vite (1420):

    cd desktop && node ../node_modules/@playwright/test/cli.js test user-edit.spec.ts --reporter=list

Erwartung: **beide** zuvor roten Faelle sind jetzt gruen — „Deactivate and reactivate employee"
(seit Plan 14.2-03) und „Change employee role from employee to admin" (dieser Plan). Die
woertlichen Reporterzeilen und die Gesamtzahl der Datei gehoeren ins SUMMARY.

Zusaetzlich die Laufzeitabfrage, die den Ausgangsbefund erzeugt hat, gegenrechnen — mit einem
kurzen Playwright-Skript unter `desktop/tests/messungen/f3-rollenfeld.mjs` (kein `*.spec.ts`,
damit die E2E-Suite dreiteilig bleibt): Dialog „Benutzer bearbeiten" oeffnen und
`document.querySelectorAll('[role="dialog"] select')` erheben, je Treffer `name`, `id` und den
`htmlFor`-Wert des zugehoerigen `<label>`. Erwartung: `name === "role"`, `id === "role"`, und
es existiert ein `<label for="role">` mit dem Text „Rolle". Ausgangswert zum Vergleich:
`[{"name":null,"id":"","val":"employee"}]`. Beide Ausgaben gehoeren woertlich ins SUMMARY.

**Nicht erlaubt:** `desktop/tests/user-edit.spec.ts` zu aendern. Der Selektor
`select[name="role"]` bleibt, wie er ist (D-06). Der Ausfuehrende belegt das mit
`git diff --stat` gegen den Planbeginn.

Commit(s) — Quelltextaenderung und Messskript getrennt, beide zu F-3:
`fix(14.2-F-3): Rollenfeld traegt name und id`
`test(14.2-F-3): Messung des Rollenfelds und der Label-Anbindung`
  </action>
  <acceptance_criteria>
    - `cd desktop && grep -q 'name="role"' src/components/users/EditUserModal.tsx && grep -q 'id="role"' src/components/users/EditUserModal.tsx`
    - `git diff --stat <planbeginn>..HEAD -- desktop/tests/user-edit.spec.ts` ist **leer** — der Test wurde nicht angepasst (D-06)
    - `cd desktop && node ../node_modules/@playwright/test/cli.js test user-edit.spec.ts --reporter=list` — **0 failed**; die woertlichen Reporterzeilen beider zuvor roter Faelle stehen im SUMMARY
    - Die Messung liefert fuer das Rollenfeld `{"name":"role","id":"role"}` und ein `<label for="role">` mit dem Text `Rolle`; die Ausgabe steht woertlich im SUMMARY, daneben der Ausgangswert `{"name":null,"id":"","val":"employee"}`
    - `cd desktop && node ../node_modules/@playwright/test/cli.js test --list` zaehlt weiterhin genau **23** Faelle in genau **drei** Dateien
    - Das SUMMARY enthaelt die vollstaendige Liste aller `<Select>`-Aufrufstellen ohne `name`, und `deferred-items.md` einen entsprechenden Eintrag
    - `cd desktop && npx tsc --noEmit` Exit 0 und `npm run check:rules` Exit 0
  </acceptance_criteria>
  <verify>
    <automated>cd desktop && npx tsc --noEmit && npm run check:rules && node ../node_modules/@playwright/test/cli.js test user-edit.spec.ts --reporter=list 2>&1 | tail -25</automated>
  </verify>
  <done>Das Rollenfeld traegt `name="role"` und `id="role"`, `user-edit.spec.ts` laeuft ohne roten Fall, und der Test ist unveraendert geblieben.</done>
</task>

<task type="auto">
  <name>Task 3: Abschlussgate und D-01-Nachweis</name>
  <files>.planning/phases/14-absicherung-und-auslieferung/deferred-items.md</files>
  <read_first>
    - `.planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-NACHWEIS-D01.md` — die Ausgangswerte, gegen die gemessen wird
    - `.planning/phases/14-absicherung-und-auslieferung/deferred-items.md`, letzter Abschnitt — Format der bestehenden Eintraege
  </read_first>
  <action>
Ohne weitere Codeaenderung:

1. `cd server && npx tsc --noEmit` — Exit 0.
2. `cd desktop && npx tsc --noEmit` — Exit 0.
3. `cd desktop && npm run check:rules` — Exit 0.
4. `cd server && npx vitest run` — rote Menge nicht gewachsen; rot nur die drei
   vorbestehenden. Zahl gruen/rot ins SUMMARY.
5. **Zwischenstand des E2E-Gates festhalten:** ein Lauf **aller drei** Dateien
   (`node ../node_modules/@playwright/test/cli.js test --reporter=list`). Erwartung: **0 rot**.
   Das ist der erste Zeitpunkt der Phase, an dem das E2E-Gate erreichbar ist; der Endnachweis
   folgt in Plan 14.2-13. Zahl gruen/rot/uebersprungen ins SUMMARY.
6. **D-01-Nachweis:** `check:protected-tables --markdown` vor und nach dem Plan, alle fuenf
   Zeilenzahlen und SHA-256 identisch, beide Tabellen ins SUMMARY.
7. `deferred-items.md`, neuer Abschnitt `## Aus Plan 14.2-04 (F-3)`: die Liste der
   `<Select>`-Aufrufstellen ohne `name`, plus alles Weitere, was aufgefallen ist und in keinen
   der elf Befunde faellt.
8. Kein `git push`, kein Deployment (D-03).

Commit: `docs(14.2-04): Abschlussgate F-3, D-01-Pruefsummen und zurueckgestellte Nebenbefunde`
  </action>
  <acceptance_criteria>
    - `cd server && npx tsc --noEmit` Exit 0 und `cd desktop && npx tsc --noEmit` Exit 0
    - `cd desktop && npm run check:rules` Exit 0
    - `cd server && npx vitest run` endet mit hoechstens 3 roten Tests, und die roten sind genau die drei vorbestehenden
    - `cd desktop && node ../node_modules/@playwright/test/cli.js test --reporter=list` endet mit **0 failed**; die Zeile `N passed / 0 failed / K skipped` steht woertlich im SUMMARY
    - Fuer alle fuenf geschuetzten Tabellen: Zeilenzahl vorher = nachher **und** SHA-256 vorher = nachher, alle fuenf Paare als Tabelle im SUMMARY (D-01)
    - `deferred-items.md` enthaelt einen neuen Abschnitt `## Aus Plan 14.2-04 (F-3)` mit der Liste der `<Select>`-Aufrufstellen ohne `name`
    - `git log --oneline` seit Planbeginn zeigt genau drei Commits — jeder einzeln revertierbar (D-02)
    - `git log origin/main..HEAD` ist nicht leer und wurde nicht gepusht (D-03)
  </acceptance_criteria>
  <verify>
    <automated>cd server && npx tsc --noEmit && npx vitest run 2>&1 | tail -20 && DATABASE_PATH=./database/development.db npm run check:protected-tables && cd ../desktop && npx tsc --noEmit && npm run check:rules && node ../node_modules/@playwright/test/cli.js test --reporter=list 2>&1 | tail -10</automated>
  </verify>
  <done>Alle vier Gates sind gruen, die drei E2E-Dateien laufen ohne roten Fall, und die fuenf geschuetzten Tabellen sind mit Zahlen als unberuehrt belegt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aufrufer-Props → DOM-Attribute des `<select>` | `{...props}` schreibt beliebige Attribute auf ein Formularelement |
| Formularfeld → Hilfsmittel (Screenreader) | Ein Feld ohne zugaenglichen Namen liefert einer Person mit Hilfsmittel keine Information |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.2-04-01 | Tampering | `{...props}` kann `id`/`aria-*` der Primitive ueberschreiben | accept | Beabsichtigt und in `Input.tsx:20-21` bereits so dokumentiert: uebergibt ein Aufrufer eine eigene `id`, gewinnt sie, und `htmlFor` folgt ihr, weil `selectId` aus `props.id` abgeleitet ist. Kein Vertrauensuebergang — alle Aufrufer sind Projektquelltext, keine Fremdeingabe |
| T-14.2-04-02 | Information Disclosure | Ein stabiler Selektor `select[name="role"]` erleichtert automatisierte Zugriffe | accept | Der Selektor beschreibt ein Feld in einer bereits Admin-only erreichbaren Maske; die Durchsetzung liegt serverseitig (`requireAdmin` auf `PUT /api/users/:id`). Ein Attributname schafft keine Berechtigung |
| T-14.2-04-03 | Elevation of Privilege | Das Rollenfeld setzt die Rolle eines Nutzers | accept | Unveraendert: die Rollenaenderung wird serverseitig geprueft (`PUT /api/users/:id`, `requireAdmin`); dieser Plan aendert weder Route noch Service, nur die Adressierbarkeit des Feldes |
| T-14.2-04-04 | Repudiation | Nachweis ohne Zahlen | mitigate | Die Abnahmekriterien verlangen die woertliche Laufzeitabfrage vorher/nachher und die Reporterzeilen, nicht den Satz „Test ist gruen" |
| T-14.2-04-05 | Tampering | Ein roter Test wird durch Anpassen des Tests „behoben" | mitigate | `git diff --stat` auf `desktop/tests/user-edit.spec.ts` muss leer sein — das ist ein Abnahmekriterium, kein Vorsatz (D-06) |

**Keine Paketinstallation in diesem Plan** — kein `npm install`; `useId` ist Teil von React 19,
das bereits Abhaengigkeit ist. Damit ist kein Legitimitaets-Gate und kein `T-14.2-SC`-Eintrag
erforderlich.
</threat_model>

<verification>
1. `cd desktop && node ../node_modules/@playwright/test/cli.js test user-edit.spec.ts --reporter=list` — 0 failed
2. `cd desktop && node ../node_modules/@playwright/test/cli.js test --reporter=list` — 0 failed ueber alle drei Dateien
3. `git diff --stat` auf `desktop/tests/user-edit.spec.ts` — leer
4. Laufzeitabfrage des Rollenfelds: `{"name":"role","id":"role"}` plus `<label for="role">`
5. `cd desktop && npx tsc --noEmit && npm run check:rules` — beide Exit 0
6. `cd server && npx tsc --noEmit` — Exit 0
7. `cd server && npx vitest run` — hoechstens 3 rot, und zwar genau die drei vorbestehenden
8. D-01-Pruefsummen der fuenf Tabellen: Differenz 0
9. `git log --oneline` — genau drei Commits, kein Push
</verification>

<success_criteria>
- Das Rollenfeld traegt `name="role"` und ein `<label for>`-Paar
- `Select.tsx` traegt dasselbe WR-16-Muster wie `Input.tsx` und `Textarea.tsx`
- Die drei E2E-Dateien laufen ohne roten Fall (drittes Erfolgskriterium der Phase)
- Der Test wurde nicht umgeschrieben
- Die fuenf geschuetzten Tabellen sind unberuehrt (D-01)
- Drei getrennte, einzeln revertierbare Commits (D-02)
</success_criteria>

<output>
Create `.planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-04-SUMMARY.md` when done
</output>
