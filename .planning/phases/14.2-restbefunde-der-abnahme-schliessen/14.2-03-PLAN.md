---
phase: 14.2-restbefunde-der-abnahme-schliessen
plan: 03
type: execute
wave: 3
depends_on: ["14.2-02"]
files_modified:
  - desktop/src/pages/UserManagementPage.tsx
autonomous: true
requirements: []
decisions: [D-01, D-02, D-03, D-04, D-14, D-15]

must_haves:
  truths:
    - "F-4: Ein deaktivierter Nutzer steht ohne Filteraenderung in der Nutzerliste"
    - "F-4: Die Zeile eines deaktivierten Nutzers traegt eine Schaltflaeche 'Bearbeiten' und eine Schaltflaeche 'Reaktivieren'"
    - "F-4: Die Zeile eines soft-geloeschten Nutzers traegt weiterhin ausschliesslich 'Reaktivieren' — die Soft-Delete-Semantik bleibt sichtbar getrennt"
    - "F-4: Die Statusspalte unterscheidet die drei Zustaende aktiv / deaktiviert / archiviert; 'Inaktiv' steht nicht mehr fuer zwei verschiedene Dinge"
    - "F-4: Der E2E-Fall 'Deactivate and reactivate employee' (user-edit.spec.ts:221) ist gruen"
    - "D-01: Keine Zeile in den fuenf geschuetzten Tabellen wurde angefasst (Zeilenzahl und SHA-256 vorher = nachher)"
  artifacts:
    - path: "desktop/src/pages/UserManagementPage.tsx"
      provides: "Standardfilter zeigt deaktivierte Nutzer; dreigeteilte Aktions- und Statusspalte"
      contains: "deletedAt"
  key_links:
    - from: "desktop/src/pages/UserManagementPage.tsx"
      to: "desktop/src/types/index.ts"
      via: "User.deletedAt unterscheidet 'deaktiviert' von 'soft-geloescht' ohne Backend-Aenderung"
      pattern: "user.deletedAt"
    - from: "desktop/src/pages/UserManagementPage.tsx"
      to: "desktop/src/hooks/useUsers.ts"
      via: "useReactivateUser ruft POST /users/:id/reactivate — seit Plan 14.2-02 fuer beide Zustaende zustaendig"
      pattern: "reactivateUser.mutateAsync"
---

<objective>
**F-4 schliessen:** Ein deaktivierter Nutzer ist in der Oberflaeche unerreichbar. Der
Statusfilter steht auf `'active'` (`UserManagementPage.tsx:49`), die Zeile verschwindet
damit aus der Standardansicht — und auch unter „Alle (inkl. Archiv)" traegt sie **keine**
Schaltflaeche „Bearbeiten", sondern ausschliesslich „Reaktivieren" (gemessen:
`[{"t":"Reaktivieren","a":null}]`). Der `if (!user.isActive)`-Zweig bei `:452` ist ein reiner
Zweiweg-Ast und unterscheidet nicht zwischen „bloss deaktiviert" (`deletedAt IS NULL`) und
„soft-geloescht" (`deletedAt IS NOT NULL`), obwohl der `User`-Typ `deletedAt` bereits
mitfuehrt (`desktop/src/types/index.ts:34`) und `getAllUsers()` es bereits liefert.

Zweck: Die Oberflaechenhaelfte des ersten Erfolgskriteriums — „Ein deaktivierter Nutzer laesst
sich in der Oberflaeche wiederfinden und reaktivieren". Die Serverhaelfte steht seit Plan
14.2-02 (F-1); dieser Plan ist ihr eigener, unvermischter Commit-Satz (D-04, D-14).

Ergebnis: Der deaktivierte Nutzer steht ohne Filteraenderung in der Liste, seine Zeile ist
bearbeitbar und reaktivierbar, die Statusspalte benennt die drei Zustaende getrennt, und der
E2E-Fall `user-edit.spec.ts:221` ist gruen — einer der beiden roten Faelle des Ausgangsstands.

**Warum dieser Plan allein in Welle 3 laeuft:** siehe `14.2-01-PLAN.md`, `<objective>` — die
Phase ist durchgehend seriell.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-CONTEXT.md
@.planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-PATTERNS.md
@.planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-NACHWEIS-D01.md
@.planning/phases/14-absicherung-und-auslieferung/deferred-items.md

**Umgebung:** Server auf `127.0.0.1:3100`, Vite auf `1420`, Umleitung ueber
`desktop/.env.development.local` (Plan 14.2-01). Playwright ausschliesslich ueber
`node ../node_modules/@playwright/test/cli.js`, **niemals** `npx playwright`. Anmeldung
`admin` / `admin123`. Kein `git push`, kein Deployment (D-03).

<interfaces>
Die Werte, gegen die dieser Plan arbeitet — aus dem Quelltext entnommen:

  // desktop/src/types/index.ts:16-35
  interface User { ...; isActive: boolean; status?: 'active'|'inactive'; deletedAt?: string|null }

  // Serverseitig (userService.getAllUsers, :253-260) berechnet:
  //   isActive = (status = 'active' AND deletedAt IS NULL) ? 1 : 0
  // Daraus folgen fuer die Oberflaeche genau drei Zustaende:
  //   aktiv         user.isActive === true
  //   deaktiviert   user.isActive !== true && (user.deletedAt == null)
  //   archiviert    user.deletedAt != null

  // desktop/src/pages/UserManagementPage.tsx, heutiger Stand
  :49      const [statusFilter, setStatusFilter] = useState<'active'|'all'|'inactive'>('active')
  :85-90   Filterlogik: 'active' -> u.isActive, 'inactive' -> !u.isActive
  :170-175 clearFilters() setzt statusFilter auf 'active'
  :320-329 <Select> mit den Optionen 'active' | 'all' | 'inactive'
  :342     Sichtbarkeit "Filter zuruecksetzen": statusFilter !== 'active'
  :438-445 Statusspalte: zweiwertiges Badge 'Aktiv' / 'Inaktiv'
  :450-499 Aktionsspalte: if (!user.isActive) -> nur "Reaktivieren", sonst Bearbeiten/Key/Loeschen
  :516     zweite Sichtbarkeitsbedingung derselben Art im Leerzustand
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardfilter zeigt deaktivierte Nutzer, Statusspalte benennt drei Zustaende (F-4)</name>
  <files>desktop/src/pages/UserManagementPage.tsx</files>
  <read_first>
    - `.planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-PATTERNS.md`, Abschnitt „F-4" — beide Fundstellen woertlich zitiert und die Klarstellung, welcher E2E-Fall welchen Befund nachweist
    - `.planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-CONTEXT.md`, Abschnitt D-04 — die Festlegung, dass F-1 und F-4 zusammengehoeren, aber getrennte Commit-Saetze bilden
    - `desktop/src/pages/UserManagementPage.tsx:44-105` — Filterzustaende und die Filterlogik vollstaendig
    - `desktop/src/pages/UserManagementPage.tsx:165-180` — `clearFilters()`
    - `desktop/src/pages/UserManagementPage.tsx:305-350` — die drei Filter-`Select`s und die erste Sichtbarkeitsbedingung
    - `desktop/src/pages/UserManagementPage.tsx:415-450` — die Statusspalte in ihrer heutigen zweiwertigen Form
    - `desktop/src/types/index.ts:16-35` — der `User`-Typ mit `isActive`, `status`, `deletedAt`
  </read_first>
  <action>
**A — Standardwert des Statusfilters.** `:49` setzt den Anfangswert von `'active'` auf
`'all'`. Begruendung als Kommentar darueber, der woertlich **F-4** nennt: Ein deaktivierter
Nutzer muss ohne Filteraenderung auffindbar sein; der bisherige Vorgabewert machte den Zustand
zur Sackgasse (NB-1, E2E-Fehlschlag 1).

Mit dieser Aenderung ziehen **zwingend** drei weitere Stellen mit, sonst entsteht ein
inkonsistenter Zustand:
- `clearFilters()` (`:173`) setzt `statusFilter` auf `'all'`, nicht mehr auf `'active'`.
- Die Sichtbarkeitsbedingung der Schaltflaeche „Filter zuruecksetzen" bei `:342` — der
  Teilausdruck `statusFilter !== 'active'` wird zu `statusFilter !== 'all'`.
- Dieselbe Bedingung ein zweites Mal im Leerzustand bei `:516` — gleiche Aenderung.
Der Ausfuehrende prueft per Suche nach `statusFilter !== `, dass keine vierte Stelle
uebersehen wurde.

**B — Beschriftungen des Statusfilters.** Die `Select`-Optionen bei `:320-329` benennen heute
`'inactive'` als „🗄️ Nur Archiv (Gelöscht)", filtern aber ueber `!u.isActive` und treffen
damit auch bloss deaktivierte Nutzer. Das ist eine falsche Beschriftung, keine Meinungsfrage.
Zielzustand: drei Optionen mit **zutreffenden** Beschriftungen —
`'active'` → „Nur Aktive", `'all'` → „Alle", `'inactive'` → „Nur Inaktive (deaktiviert und
archiviert)". Der Filterausdruck bei `:85-90` bleibt unveraendert; er passt dann zur
Beschriftung.

**C — Statusspalte, drei Zustaende.** Das Badge bei `:438-445` ist zweiwertig
(`Aktiv` / `Inaktiv`) und macht damit „deaktiviert" und „archiviert" ununterscheidbar. Das ist
Teil desselben Befunds: Ohne diese Unterscheidung kann der Bedienende nicht erkennen, warum
die eine Zeile „Bearbeiten" anbietet und die andere nicht. Zielzustand — drei Zustaende nach
der Ableitung aus dem `<interfaces>`-Block:

| Bedingung | Beschriftung | Farbfamilie |
|---|---|---|
| `user.isActive` | `Aktiv` | gruen (Bestand unveraendert) |
| `!user.isActive && user.deletedAt == null` | `Deaktiviert` | amber |
| `user.deletedAt != null` | `Archiviert` | grau (Bestand unveraendert) |

Fuer die amberfarbene Variante wird das bereits im Projekt verwendete Bandenpaar genommen —
hell `bg-amber-100 text-amber-800`, dunkel `dark:bg-amber-900/20 dark:text-amber-400`, in
derselben Form wie die bestehenden Badges derselben Zelle. **Dark Mode ist Pflicht**
(`.claude/CLAUDE.md`); jede neue Klasse traegt ihre `dark:`-Entsprechung.

**Kein `any`.** Die Zustandsableitung wird als eine benannte Hilfsfunktion oder ein
`const`-Ausdruck mit einem engen Vereinigungstyp (`'active' | 'deactivated' | 'archived'`)
geschrieben, nicht als verschachtelte Ternaerkette in vier Stellen kopiert — sie wird in
Task 2 ein zweites Mal gebraucht.

**Ausdruecklich nicht anfassen (Scope Fence):** Die Spalte „Stunden/Woche" (`:448`,
`{user.weeklyHours ?? 40}h`) zeigt Stammdaten statt der gueltigen Periode — das ist **F-2**
und wird in Plan 14.2-08 behoben, nicht hier (D-02).

Commit (nur diese Datei):
`fix(14.2-F-4): deaktivierte Nutzer stehen in der Liste, Status benennt drei Zustaende`
  </action>
  <acceptance_criteria>
    - `cd desktop && grep -n "useState<'active' | 'all' | 'inactive'>('all')" src/pages/UserManagementPage.tsx` liefert einen Treffer (Zeilenumbrueche/Leerzeichen duerfen abweichen; entscheidend ist der Anfangswert `'all'`)
    - `cd desktop && grep -c "statusFilter !== 'active'" src/pages/UserManagementPage.tsx` liefert **0**
    - `cd desktop && grep -c "statusFilter !== 'all'" src/pages/UserManagementPage.tsx` liefert **2** (die Sichtbarkeitsbedingung bei `:342` und die im Leerzustand)
    - `cd desktop && sed -n '/const clearFilters/,/};/p' src/pages/UserManagementPage.tsx | grep -q "setStatusFilter('all')"`
    - `cd desktop && grep -c "Nur Archiv (Gelöscht)" src/pages/UserManagementPage.tsx` liefert **0**
    - `cd desktop && grep -q "Deaktiviert" src/pages/UserManagementPage.tsx && grep -q "Archiviert" src/pages/UserManagementPage.tsx`
    - Jede in dieser Aenderung neu hinzugekommene Tailwind-Farbklasse hat eine `dark:`-Entsprechung in derselben `className` — per Sichtpruefung des Diffs belegt und im SUMMARY bestaetigt
    - `cd desktop && grep -c ": any" src/pages/UserManagementPage.tsx` liefert **0**
    - `cd desktop && npx tsc --noEmit` endet mit Exit 0
    - `git show --stat HEAD` zeigt genau `desktop/src/pages/UserManagementPage.tsx`
  </acceptance_criteria>
  <verify>
    <automated>cd desktop && npx tsc --noEmit && grep -q "setStatusFilter('all')" src/pages/UserManagementPage.tsx && test "$(grep -c "statusFilter !== 'active'" src/pages/UserManagementPage.tsx)" = "0" && echo GATE_OK</automated>
  </verify>
  <done>Der Standardfilter zeigt alle Nutzer, alle vier Stellen ziehen mit, und die Statusspalte unterscheidet aktiv, deaktiviert und archiviert — hell und dunkel.</done>
</task>

<task type="auto">
  <name>Task 2: Aktionsspalte in drei Zweige und E2E-Nachweis (F-4)</name>
  <files>desktop/src/pages/UserManagementPage.tsx</files>
  <read_first>
    - `desktop/src/pages/UserManagementPage.tsx:450-505` — der heutige Zweiweg-Ast der Aktionsspalte vollstaendig
    - `desktop/tests/user-edit.spec.ts:191-235` — der rote E2E-Fall „Deactivate and reactivate employee" vollstaendig; er geht ueber die `isActive`-Checkbox in `EditUserModal`, **nicht** ueber die Reaktivierungs-Mutation
    - `desktop/src/components/users/EditUserModal.tsx:125-140` und `:1025-1045` — die `isActive`-Checkbox (`input#isActive`); es gibt keinen Zustands-Guard, der das Modal fuer einen inaktiven Nutzer verschliesst
    - `desktop/src/hooks/useUsers.ts:292-300` — `useReactivateUser()`, ruft `POST /users/:id/reactivate`; seit Plan 14.2-02 deckt der Endpunkt beide Zustaende ab
    - `.planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-PATTERNS.md`, Abschnitt „Playwright-Muster" — Anmelde-Fixture und der verbindliche Aufrufweg
  </read_first>
  <action>
**A — Die Aktionsspalte bekommt drei Zweige** statt des heutigen `if (!user.isActive)`.
Die Zustandsableitung aus Task 1 wird wiederverwendet, nicht neu geschrieben.

| Zustand | Schaltflaechen in dieser Reihenfolge |
|---|---|
| aktiv | `Bearbeiten` · Passwort-Symbol (`Key`) · `Löschen` (Bestand unveraendert; `Löschen` weiterhin nur fuer fremde Nutzer, `user.id !== currentUser.id`) |
| deaktiviert (`deletedAt == null`) | `Bearbeiten` · `Reaktivieren` |
| archiviert (`deletedAt != null`) | `Reaktivieren` (Bestand unveraendert) |

Der `Bearbeiten`-Knopf des mittleren Zweigs ist derselbe wie im aktiven Zweig
(`onClick={() => setEditingUserId(user.id)}`, `variant`/`size` gleich) und traegt dieselbe
Beschriftung `Bearbeiten` — der E2E-Selektor `button:has-text("Bearbeiten")` muss ihn treffen.
Der `Reaktivieren`-Knopf des mittleren Zweigs ist derselbe wie im archivierten Zweig
(`reactivateUser.mutateAsync(user.id)`, `disabled={reactivateUser.isPending}`).

Fuer den mittleren Zweig **kein** `Löschen` und **kein** Passwort-Zuruecksetzen: Beides waere
eine Ausweitung ueber den Befund hinaus. F-4 verlangt „auffindbar **und** bearbeitbar", nicht
„voller Funktionsumfang fuer deaktivierte Konten". Faellt beim Arbeiten auf, dass eines der
beiden fehlt und gebraucht wird, kommt es als UAT-Kandidat ins SUMMARY, nicht in diesen Plan.

Die drei Zweige tragen je einen einzeiligen Kommentar, der den Zustand benennt; der bestehende
irrefuehrende Kommentar `// Deleted user - show reactivate button` faellt weg, weil er auf den
zusammengefassten Zweig gemuenzt war.

**B — E2E-Nachweis, gemessen statt behauptet.**
Mit laufendem Server (127.0.0.1:3100) und laufendem Vite (1420):

    cd desktop && node ../node_modules/@playwright/test/cli.js test user-edit.spec.ts --reporter=list

Erwartung: Der Fall „Deactivate and reactivate employee" (`user-edit.spec.ts:221`) ist **gruen**.
Der zweite rote Fall „Change employee role from employee to admin" (`:308`) bleibt in diesem
Plan **rot** — seine Ursache ist F-3 und wird in Plan 14.2-04 behoben. Beide Ergebnisse
(gruen/rot je Fall) gehoeren woertlich ins SUMMARY, zusammen mit der Gesamtzahl der Datei.

Zusaetzlich der Bedienweg-Nachweis fuer den mittleren Zweig, mit einem eigenen kurzen
Playwright-Skript (nicht als neue `*.spec.ts`, damit die E2E-Suite unveraendert dreiteilig
bleibt) — abgelegt unter `desktop/tests/messungen/f4-aktionsspalte.mjs`, aufgerufen ueber
`require('playwright-core')` bzw. `import('playwright-core')`:
1. Als Admin anmelden, zur Benutzerverwaltung, einen Testnutzer ueber die Oberflaeche
   deaktivieren (`EditUserModal` → Haken bei `input#isActive` entfernen → speichern).
2. **Ohne** den Filter anzufassen die Zeile suchen und die Beschriftungen aller Schaltflaechen
   der Zeile erheben. Erwartung: genau `["Bearbeiten","Reaktivieren"]`.
3. Auf `Reaktivieren` klicken und danach den Zustand erneut erheben. Erwartung: Statusbadge
   `Aktiv`, Schaltflaechen wieder `Bearbeiten` + Passwort + `Löschen`.
Die gemessenen Beschriftungslisten gehoeren woertlich ins SUMMARY — der Ausgangsbefund war
`[{"t":"Reaktivieren","a":null}]`, dagegen wird verglichen.

Commit (Quelltextaenderung und Messskript getrennt halten; beide gehoeren zu F-4, deshalb
duerfen es zwei Commits sein, aber niemals ein zweiter Befund darin):
`fix(14.2-F-4): Aktionsspalte unterscheidet deaktiviert und archiviert`
`test(14.2-F-4): Bedienweg-Messung der Aktionsspalte`
  </action>
  <acceptance_criteria>
    - `cd desktop && grep -c "Deleted user - show reactivate button" src/pages/UserManagementPage.tsx` liefert **0**
    - `cd desktop && sed -n '/flex justify-end space-x-2/,/<\/div>/p' src/pages/UserManagementPage.tsx | grep -c "deletedAt"` liefert mindestens **1** — die Aktionsspalte wertet den dritten Zustand aus
    - `cd desktop && node ../node_modules/@playwright/test/cli.js test user-edit.spec.ts --reporter=list` — der Fall „Deactivate and reactivate employee" ist **passed**; die woertliche Reporterzeile steht im SUMMARY
    - Im selben Lauf ist „Change employee role from employee to admin" weiterhin **failed** (Ursache F-3, Plan 14.2-04) — auch das steht woertlich im SUMMARY, damit niemand es fuer eine Regression haelt
    - `desktop/tests/messungen/f4-aktionsspalte.mjs` existiert und ist **kein** `*.spec.ts` — `cd desktop && node ../node_modules/@playwright/test/cli.js test --list` zaehlt weiterhin genau **23** Faelle in genau **drei** Dateien
    - Die Messung liefert fuer den deaktivierten Nutzer die Schaltflaechenliste `["Bearbeiten","Reaktivieren"]`; die Ausgabe steht woertlich im SUMMARY
    - Nach dem Klick auf `Reaktivieren` zeigt die Zeile das Badge `Aktiv`; die Ausgabe steht woertlich im SUMMARY
    - `cd desktop && npx tsc --noEmit` Exit 0 und `cd desktop && npm run check:rules` Exit 0
  </acceptance_criteria>
  <verify>
    <automated>cd desktop && npx tsc --noEmit && npm run check:rules && node ../node_modules/@playwright/test/cli.js test user-edit.spec.ts --reporter=list 2>&1 | tail -25</automated>
  </verify>
  <done>Die Zeile eines deaktivierten Nutzers traegt „Bearbeiten" und „Reaktivieren", der E2E-Fall `user-edit.spec.ts:221` ist gruen, und die gemessenen Schaltflaechenlisten stehen im SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Abschlussgate und D-01-Nachweis</name>
  <files>.planning/phases/14-absicherung-und-auslieferung/deferred-items.md</files>
  <read_first>
    - `.planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-NACHWEIS-D01.md` — die Ausgangswerte, gegen die gemessen wird
    - `.planning/phases/14-absicherung-und-auslieferung/deferred-items.md`, letzter Abschnitt — Format der bestehenden Eintraege
  </read_first>
  <action>
Ohne weitere Codeaenderung:

1. `cd server && npx tsc --noEmit` — Exit 0.
2. `cd desktop && npx tsc --noEmit` — Exit 0.
3. `cd desktop && npm run check:rules` — Exit 0.
4. `cd server && npx vitest run` — rote Menge nicht gewachsen; rot nur die drei
   vorbestehenden. Zahl gruen/rot ins SUMMARY.
5. **D-01-Nachweis:** `cd server && DATABASE_PATH=./database/development.db npm run check:protected-tables -- --markdown`
   vor und nach dem Plan. Alle fuenf Zeilenzahlen und SHA-256 identisch. Beide Tabellen ins
   SUMMARY. Die E2E-Laeufe legen Testnutzer in `users` an — das ist zulaessig; steht in einer
   der fuenf geschuetzten Tabellen eine Abweichung, ist der Plan **nicht** fertig, sondern die
   Ursache aufzuklaeren.
6. Was beim Arbeiten aufgefallen ist und in keinen der elf Befunde faellt — insbesondere die
   Frage nach Passwort-Zuruecksetzen und Loeschen fuer deaktivierte Konten — geht als Eintrag
   nach `deferred-items.md`, Abschnitt `## Aus Plan 14.2-03 (F-4)`, und als UAT-Kandidat ins
   SUMMARY fuer Plan 14.2-13.
7. Kein `git push`, kein Deployment (D-03).

Commit: `docs(14.2-03): Abschlussgate F-4, D-01-Pruefsummen und zurueckgestellte Nebenbefunde`
  </action>
  <acceptance_criteria>
    - `cd server && npx tsc --noEmit` Exit 0 und `cd desktop && npx tsc --noEmit` Exit 0
    - `cd desktop && npm run check:rules` Exit 0
    - `cd server && npx vitest run` endet mit hoechstens 3 roten Tests, und die roten sind genau die drei vorbestehenden; die Zahl gruen/rot steht im SUMMARY
    - Fuer alle fuenf geschuetzten Tabellen: Zeilenzahl vorher = nachher **und** SHA-256 vorher = nachher, alle fuenf Paare als Tabelle im SUMMARY (D-01)
    - `deferred-items.md` enthaelt einen neuen Abschnitt `## Aus Plan 14.2-03 (F-4)`
    - `git log --oneline` seit Planbeginn zeigt genau drei Commits (Filter/Status, Aktionsspalte + Messung, Dokumentation) — jeder einzeln revertierbar (D-02)
    - `git log origin/main..HEAD` ist nicht leer und wurde nicht gepusht (D-03)
  </acceptance_criteria>
  <verify>
    <automated>cd server && npx tsc --noEmit && npx vitest run 2>&1 | tail -20 && DATABASE_PATH=./database/development.db npm run check:protected-tables && cd ../desktop && npx tsc --noEmit && npm run check:rules</automated>
  </verify>
  <done>Alle vier Gates sind gruen, die fuenf geschuetzten Tabellen sind mit Zahlen als unberuehrt belegt, und die Nebenbefunde sind vermerkt statt mitgenommen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Oberflaeche → Reaktivierungsendpunkt | Eine Schaltflaeche fuer einen deaktivierten Nutzer schafft einen Weg, der Zugang wiederherstellt |
| Oberflaeche → Bearbeitungsdialog | „Bearbeiten" wird fuer einen Zustand sichtbar, fuer den es bisher nicht sichtbar war |
| Statusdarstellung → Bedienentscheidung | Ein Badge, das zwei verschiedene Zustaende gleich benennt, fuehrt zu falschen Entscheidungen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.2-03-01 | Elevation of Privilege | Sichtbare „Reaktivieren"/„Bearbeiten"-Schaltflaechen fuer deaktivierte Konten | mitigate | Die Seite ist bereits Admin-only (`useUsers(true)`, serverseitig `requireAdmin` auf `GET /api/users`); die Sichtbarkeit einer Schaltflaeche schafft keine Berechtigung. Die Durchsetzung liegt unveraendert serverseitig und ist in Plan 14.2-02, Test 4 gemessen (403/401) |
| T-14.2-03-02 | Tampering | Ein archivierter (soft-geloeschter) Nutzer wird versehentlich wie ein bloss deaktivierter behandelt | mitigate | Die Zustandsableitung stuetzt sich auf `user.deletedAt`, das `getAllUsers()` bereits liefert; der archivierte Zweig behaelt ausschliesslich „Reaktivieren". Der Bedienweg-Nachweis in Task 2 misst die Schaltflaechenliste je Zustand |
| T-14.2-03-03 | Information Disclosure | Der Standardfilter zeigt jetzt auch archivierte Personendaten | accept | Die Seite ist Admin-only und die Option „Alle (inkl. Archiv)" existiert bereits; der Vorgabewert entscheidet nur, welcher der bereits zugaenglichen Ausschnitte zuerst gezeigt wird. Keine neuen Felder werden ausgeliefert |
| T-14.2-03-04 | Repudiation | Reaktivierung ohne Spur | accept | Der Serverweg protokolliert bereits (`logger.info` in `reactivateUser`); die Oberflaeche fuegt keinen zweiten Weg hinzu, sondern macht den bestehenden erreichbar |
| T-14.2-03-05 | Spoofing | Messskript trifft den falschen Server | mitigate | Alle Aufrufe gehen an `127.0.0.1`; die Desktop-Umleitung steht in `desktop/.env.development.local` auf Port 3100 (B-1) |

**Keine Paketinstallation in diesem Plan** — `playwright-core` und `@playwright/test` sind
bereits im Repo-Root vorhanden (Fassung 1.57.0, gemessen). Kein `npm install`, damit kein
Legitimitaets-Gate und kein `T-14.2-SC`-Eintrag erforderlich.
</threat_model>

<verification>
1. `cd desktop && node ../node_modules/@playwright/test/cli.js test user-edit.spec.ts --reporter=list` — „Deactivate and reactivate employee" passed
2. Bedienweg-Messung: Schaltflaechenliste des deaktivierten Nutzers ist `["Bearbeiten","Reaktivieren"]`
3. `cd desktop && node ../node_modules/@playwright/test/cli.js test --list` — weiterhin 23 Faelle in drei Dateien
4. `cd desktop && npx tsc --noEmit && npm run check:rules` — beide Exit 0
5. `cd server && npx tsc --noEmit` — Exit 0
6. `cd server && npx vitest run` — hoechstens 3 rot, und zwar genau die drei vorbestehenden
7. D-01-Pruefsummen der fuenf Tabellen: Differenz 0
8. `git log --oneline` — genau drei Commits, kein Push
</verification>

<success_criteria>
- Ein deaktivierter Nutzer steht ohne Filteraenderung in der Liste und ist bearbeitbar
- Ein archivierter Nutzer bleibt sichtbar davon getrennt
- Die Statusspalte benennt aktiv, deaktiviert und archiviert getrennt, hell und dunkel
- Der E2E-Fall `user-edit.spec.ts:221` ist gruen
- Die fuenf geschuetzten Tabellen sind unberuehrt (D-01)
- Drei getrennte, einzeln revertierbare Commits (D-02)
</success_criteria>

<output>
Create `.planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-03-SUMMARY.md` when done
</output>
