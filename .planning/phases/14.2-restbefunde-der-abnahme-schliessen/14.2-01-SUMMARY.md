---
phase: 14.2-restbefunde-der-abnahme-schliessen
plan: 01
subsystem: testing
tags: [better-sqlite3, sha256, playwright, vitest, produktionsschutz, env-config]

# Dependency graph
requires: []
provides:
  - "protectedTables.ts: nebenwirkungsfreies Modul mit PROTECTED_TABLES, measureProtectedTables, printProtectedTables, formatProtectedTablesMarkdown"
  - "protectedTablesChecksum.ts + npm-Skript check:protected-tables: D-01-Nachweiswerkzeug fuer alle zwoelf folgenden Plaene der Phase"
  - "14.2-NACHWEIS-D01.md: Ausgangswerte der Phase (vier Gates, drei E2E-Dateien, D-01-Pruefsummen) — Vergleichsbasis fuer 14.2-02 bis 14.2-13"
  - "Pruefumgebung: Server auf 127.0.0.1:3100 und Vite auf 1420 gegen server/database/development.db, dauerhaft laufend"
  - "B-1 geschlossen: keine Produktspur zeigt mehr auf localhost:3000"
affects: [14.2-02, 14.2-03, 14.2-04, 14.2-05, 14.2-06, 14.2-07, 14.2-08, 14.2-09, 14.2-10, 14.2-11, 14.2-12, 14.2-13]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "D-01-Nachweis: better-sqlite3 direkt oeffnen mit {readonly:true, fileMustExist:true} + PRAGMA query_only=ON, connection.js NICHT importieren (CR-02)"
    - "Messfunktionen fuer geschuetzte Tabellen zentral in protectedTables.ts statt lokal je Skript dupliziert"

key-files:
  created:
    - server/src/scripts/protectedTables.ts
    - server/src/scripts/protectedTablesChecksum.ts
    - .planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-NACHWEIS-D01.md
  modified:
    - server/src/scripts/purgeFutureOvertimeRows.ts
    - server/package.json
    - server/src/scripts/validateOvertimeDetailed.ts
    - desktop/.env
    - desktop/.env.development
    - desktop/.gitignore

key-decisions:
  - "D-01-Baseline aus 14.2-01 (nicht die 14.1-01-Referenz) ist ab jetzt die Vergleichsbasis der Phase — Abweichung benannt, nicht verrechnet"
  - "E2E-Ausgangsstand 12/9/2 statt der erwarteten 19/2/2 — sieben zusaetzliche rote Faelle durch Testdaten-Verschmutzung erklaert, nicht gefixt (ausserhalb Scope), als UAT-Kandidat vermerkt"

requirements-completed: []

# Metrics
duration: 19min
completed: 2026-08-25
---

# Phase 14.2 Plan 01: D-01-Werkzeug, Prüfumgebung, B-1 Summary

**Wiederverwendbares D-01-Prüfsummenwerkzeug (readonly, ohne `connection.js`-Import) für alle zwölf Folgepläne, Ausgangswerte der ganzen Phase festgeschrieben, B-1-Produktspuren auf 127.0.0.1 gezogen**

## Performance

- **Duration:** 19 min
- **Started:** 2026-08-25T21:24:21Z
- **Completed:** 2026-08-25T21:43:46Z
- **Tasks:** 3
- **Files modified:** 9 (davon `desktop/.env` gitignored, nicht Teil eines Commits)

## Accomplishments
- Nebenwirkungsfreies Modul `protectedTables.ts` mit den vier geforderten Exporten, `purgeFutureOvertimeRows.ts` additiv umgestellt (verhaltensgleich, geprüft per Trockenlauf vor/nach der Umstellung — identische Zeilenzahlen und SHA-256)
- Kommandozeilenwerkzeug `protectedTablesChecksum.ts` — öffnet ausschließlich readonly + `PRAGMA query_only=ON`, lädt `connection.js` nicht (CR-02), Produktionsschutz per `assertNotProduction()`, `npm run check:protected-tables`
- Prüfumgebung aufgebaut und über die gesamte Bearbeitungszeit lauffähig gehalten: Server auf `127.0.0.1:3100` (Health-Check bestätigt), Vite auf `1420` mit `desktop/.env.development.local` (gitignored, Vorrang vor `.env.development` per Vite-Startlog bestätigt)
- Ausgangswerte aller vier Gates, der drei E2E-Dateien und der fünf geschützten Tabellen mit Zahlen in `14.2-NACHWEIS-D01.md` festgehalten — Vergleichsbasis für die restlichen zwölf Pläne
- B-1 geschlossen: `validateOvertimeDetailed.ts` und beide `desktop/.env*`-Dateien zeigen nicht mehr auf `localhost:3000`

## Task Commits

Each task was committed atomically:

1. **Task 1: Prüfsummen-Werkzeug für die fünf geschützten Tabellen (D-01)** - `8f2ed46` (chore)
2. **Task 2: Prüfumgebung aufbauen und die Ausgangswerte der Phase festschreiben** - `e6d089f` (docs)
3. **Task 3: B-1-Produktspur** - `f28d18d` (fix)

**Zusätzlicher Housekeeping-Commit** (nicht im Plan vorgesehen, siehe Abweichungen): `b6ef33c` (chore)

_Note: Kein TDD in diesem Plan — reine Werkzeug-/Dokument-/Konfigurationsarbeit._

## Files Created/Modified
- `server/src/scripts/protectedTables.ts` - nebenwirkungsfreies Modul: `PROTECTED_TABLES`, `measureProtectedTables`, `printProtectedTables`, `formatProtectedTablesMarkdown`
- `server/src/scripts/protectedTablesChecksum.ts` - CLI-Werkzeug, readonly + query_only, Produktionsschutz, `--markdown`/`--label=`
- `server/src/scripts/purgeFutureOvertimeRows.ts` - lokale Deklarationen entfernt, importiert/re-exportiert aus `protectedTables.ts`, verhaltensgleich
- `server/package.json` - `check:protected-tables`-Skript ergänzt
- `.planning/phases/14.2-restbefunde-der-abnahme-schliessen/14.2-NACHWEIS-D01.md` - Ausgangswerte der Phase (Gates, E2E, D-01)
- `server/src/scripts/validateOvertimeDetailed.ts` - `API_BASE` auf `127.0.0.1:3000/api`, B-1-Kommentar
- `desktop/.env.development` - `VITE_API_URL` auf `127.0.0.1:3000/api`
- `desktop/.env` - identisch angepasst (gitignored, nicht Teil eines Commits, per `grep` verifiziert)
- `desktop/.gitignore` - `test-results/`/`playwright-report/` ergänzt (Housekeeping)

## Decisions Made
- Die in diesem Plan gemessenen D-01-Werte (`time_entries=895`, `absence_requests=62`, `overtime_corrections=5`, `vacation_balance=105`, `vacation_transactions=120`) ersetzen die 14.1-01-Referenzwerte als Vergleichsbasis für 14.2-02 bis 14.2-13 — die Abweichung (+42/+7/0/+46/+50) wurde benannt, nicht verrechnet; plausibel durch spätere 14.1-Pläne und frühere, nicht aufgeräumte E2E-Läufe erklärt (nicht abschließend verifiziert, außerhalb des Task-2-Auftrags).
- E2E-Ausgangsstand 12 grün / 9 rot / 2 übersprungen statt der erwarteten 19/2/2 wurde nicht "repariert" — die sieben zusätzlichen roten Fälle sind Testdaten-Verschmutzung (fest verdrahtete Benutzernamen ohne Aufräumschritt, gegen eine bereits mehrfach durchlaufene `development.db`), keiner der elf Befunde dieser Phase. Als UAT-Kandidat in `14.2-NACHWEIS-D01.md` vermerkt statt stillschweigend übergangen.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - fehlende Pflege] Playwright-Testartefakte blieben untracked**
- **Found during:** Task 2 (E2E-Ausgangslauf)
- **Issue:** `desktop/test-results/` (Screenshots, Videos, Traces der neun roten E2E-Fälle) entstand beim Ausgangslauf und war durch kein `.gitignore`-Muster erfasst — wäre nach Protokoll ("Never leave generated files untracked") als offener `??`-Eintrag stehen geblieben.
- **Fix:** `test-results/` und `playwright-report/` zu `desktop/.gitignore` ergänzt.
- **Files modified:** `desktop/.gitignore`
- **Verification:** `git status --short desktop/test-results` liefert nach dem Fix keinen Eintrag mehr.
- **Committed in:** `b6ef33c` (eigener Commit, siehe unten)

---

**Zusätzlicher, nicht im Plan vorgesehener vierter Commit:** Die Abnahmekriterien von Task 3 verlangen wörtlich „git log --oneline seit Planbeginn zeigt genau drei Commits (Werkzeug, Nachweis, B-1)". Tatsächlich stehen **vier** Commits (`8f2ed46`, `e6d089f`, `b6ef33c`, `f28d18d`) — der zusätzliche Housekeeping-Commit `b6ef33c` (`.gitignore`-Ergänzung) wurde eingefügt, um Rule 2 ("kein generiertes File bleibt untracked") zu erfüllen, ohne ihn mit dem Nachweis-Commit zu vermischen (D-02 verlangt unvermischte Commits — ein eigener, thematisch sauberer Commit erfüllt das Prinzip, auch wenn die Zahl von der wörtlichen Abnahmezahl abweicht). Jeder der vier Commits ist einzeln revertierbar; keiner vermischt zwei Befunde.

**Total deviations:** 1 auto-fixed (Rule 2), 1 dokumentierte Abweichung von der wörtlichen "genau drei Commits"-Abnahmezahl.
**Impact on plan:** Kein Scope Creep — beide Abweichungen sind Housekeeping, nicht Funktionsänderung. D-02s Grundsatz (kein vermischter Commit je Befund) bleibt gewahrt.

## Issues Encountered
- Beim ersten Server-Start (Task 2) meldete `curl` unmittelbar nach dem Hintergrundstart `Connection refused` — der Server initialisiert Schema, Migrationen und sechs Jahre Feiertage vor dem `listen()`-Aufruf, das dauert einige Sekunden. Kein Fehler, nur Wartezeit; Health-Check nach erneutem Versuch erfolgreich (`{"status":"ok",...}`).
- `desktop/.env` ist projektweit gitignored (`.gitignore:.env`) und daher nie im Git-Verlauf sichtbar — die B-1-Änderung an dieser Datei ist auf der Platte vorgenommen und per `grep` verifiziert, taucht aber denknotwendig in keinem `git show --stat` auf. Im SUMMARY und im Commit-Text ausdrücklich benannt, nicht verschwiegen.

## User Setup Required

None - keine externe Diensteinrichtung nötig.

## Für die UAT-Sammlung (Plan 14.2-13)

Aus `14.2-NACHWEIS-D01.md` übernommen, hier zur Sichtbarkeit dupliziert:

1. **E2E-Testdaten-Verschmutzung:** Sieben von neun in diesem Plan gemessenen roten E2E-Fällen (`user-creation.spec.ts:106`, `:195`; `user-edit.spec.ts:24`, `:63`, `:98`, `:166`, `:265`) sind keiner der elf Befunde dieser Phase, sondern Folge fest verdrahteter Testnutzernamen ohne `afterEach`-Aufräumschritt in den drei Spec-Dateien, angewendet auf eine über viele frühere Läufe persistente `server/database/development.db`. Betrifft potenziell die E2E-Gates der Pläne 14.2-02 bis 14.2-13, die denselben Lauf als Abschlussgate fahren.
2. **D-01-Referenzwerte weichen von 14.1-01 ab:** `time_entries` +42, `absence_requests` +7, `vacation_balance` +46, `vacation_transactions` +50 gegenüber dem 25.08.2026-Referenzlauf aus Phase 14.1. Plausibel erklärt (spätere 14.1-Pläne, frühere E2E-Läufe), aber nicht abschließend inhaltlich verifiziert.

## Next Phase Readiness

Die Prüfumgebung (Server 3100, Vite 1420) läuft weiter im Hintergrund für die Folgepläne dieser seriellen Phase. `14.2-NACHWEIS-D01.md` liegt als Vergleichsbasis vor. `check:protected-tables` ist als npm-Skript einsatzbereit für den D-01-Nachweis jedes Folgeplans. Kein Blocker für Plan 14.2-02.

---
*Phase: 14.2-restbefunde-der-abnahme-schliessen*
*Completed: 2026-08-25*

## Self-Check: PASSED

Alle erstellten Dateien und alle vier Commit-Hashes (`8f2ed46`, `e6d089f`, `b6ef33c`, `f28d18d`) verifiziert vorhanden.
