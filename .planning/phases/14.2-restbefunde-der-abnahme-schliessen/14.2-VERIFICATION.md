---
phase: 14.2-restbefunde-der-abnahme-schliessen
verified: 2026-08-26T00:00:00Z
status: human_needed
score: 8/10 Erfolgskriterien voll erfüllt, 2/10 nachweislich eingeschränkt erfüllt (nicht geschönt)
overrides_applied: 0
human_verification:
  - test: "Weg B zu F-7 (14.2-U1): einen genehmigten Abwesenheitsantrag stornieren und in der Datenbank prüfen, ob `status='cancelled'` statt `status='rejected'` gewünscht ist"
    expected: "Fachliche Entscheidung des Anwenders, ob ein eigener DB-Zustand eingeführt wird (verletzt D-01, Wechselwirkung mit NB-6-Filter)"
    why_human: "Datenmigration und Business-Entscheidung, keine Fehlerkorrektur"
  - test: "E2E-Reproduzierbarkeit (14.2-U26): dieselben drei Spec-Dateien zweimal hintereinander laufen lassen, ohne zwischenzeitliches Freiräumen der Testnutzer"
    expected: "Zweiter Lauf fällt auf 12/9/2 zurück, weil die Spec-Dateien fest verdrahtete Benutzernamen ohne Aufräumschritt verwenden — Entscheidung über afterEach/Zufallsnamen/eigene Testdatenbank"
    why_human: "Architekturentscheidung über die Testinfrastruktur, keiner der elf Befunde"
  - test: "Tastaturverlust aus V-1/D-13 (14.2-U2): Periodenliste mit Tab bedienen, bis der Chip „Nicht löschbar“ den Fokus trägt"
    expected: "Kein Erklärungstext erscheint (Chromium öffnet `title` bei reinem Tastaturfokus nicht) — Gegenentscheidung möglich (eigenes Portal-Tooltip)"
    why_human: "Barrierefreiheits-Abwägung, die M-2 bewusst getroffen hat; der Verlust ist eine fachliche Frage"
  - test: "43 weitere UAT-Punkte in 14-UAT-SAMMLUNG.md, Abschnitt „Phase 14.2“ (14.2-U3 bis 14.2-U45)"
    expected: "Siehe Tabelle im Dokument — Entscheidungen mit Datenfolge, Dominanz-/Bildschirmurteile, technische Befunde, Kenntnisnahmen"
    why_human: "Von D-15 bewusst gesammelt statt während der autonomen Ausführung gefragt"
---

# Phase 14.2: Restbefunde der Abnahme schließen — Verification Report

**Phase Goal (ROADMAP.md):** Die Befunde, die die maschinelle Abnahme zutage gefördert hat, sind
geschlossen — soweit sie Fehler sind und nicht Entscheidungen.
**Verifiziert:** 2026-08-26
**Status:** human_needed
**Re-Verifikation:** Nein — Erstverifikation (kein vorheriges `14.2-VERIFICATION.md` vorhanden)

Dieser Bericht prüft jede Behauptung der SUMMARYs und des Abschlussberichts (`14.2-ABSCHLUSS.md`)
gegen den Quelltext und gegen selbst ausgeführte Befehle — nicht gegen den Text der Dokumente
selbst. Wo ein Werkzeug lief, ist es unten das Werkzeug, das ich selbst gestartet habe, mit
`{readonly:true}`/`PRAGMA query_only = ON` bei jedem Datenbankzugriff. Ich habe **keine**
Prüfumgebung (Server 3100, Vite 1420) wieder aufgebaut — wie im Auftrag verlangt — und Port 3000
nicht angefasst.

## Die drei harten Zusagen des Anwenders — selbst gemessen

| # | Zusage | Selbst gemessen | Ergebnis |
|---|---|---|---|
| 1 | Keine Zeile in den fünf geschützten Tabellen | `cd server && DATABASE_PATH=./database/development.db npx tsx src/scripts/protectedTablesChecksum.ts --markdown` — eigener Lauf, Tool öffnet mit `{readonly:true}` + `PRAGMA query_only=ON` | **VERIFIED.** Alle fünf Zeilenzahlen und SHA-256 exakt identisch zu `14.2-NACHWEIS-D01.md` Abschnitt 3: `time_entries` 895 (`2aa77240…`), `absence_requests` 62 (`d7b01c62…`), `overtime_corrections` 5 (`8ef120d9…`), `vacation_balance` 105 (`4bbe499b…`), `vacation_transactions` 120 (`f96e3993…`). Zwischenzeitliche Verletzungen durch Pläne 07/08/13 sind in `14.2-ABSCHLUSS.md` §3.1 und in `deferred-items.md` offen benannt (nicht verschwiegen) und jeweils zurückgenommen — genau wie der Auftrag verlangt zu prüfen |
| 2 | Ein unvermischter Commit-Satz je Befund | `git log --oneline 9ee5e71..HEAD` (64 Commits) je Commit auf Befundkürzel geprüft (Skript, kein Commit mit zwei Kürzeln `14.2-F-*`/`14.2-D-*`/`14.2-B-1`/`14.2-V-1`) | **VERIFIED.** Kein Commit trägt zwei Befundkürzel |
| 3 | Kein Push, kein Deployment | `git rev-parse origin/main` | **VERIFIED.** `22f9cdc27c2e3e35aa3de3003b70ad8560b9b8dd` — unverändert. `git status --porcelain` leer |

## Die vier Gates — selbst gefahren

| Gate | Befehl | Eigenes Ergebnis | Soll | Status |
|---|---|---|---|---|
| Server-Typen | `cd server && npx tsc --noEmit` | Exit 0 | Exit 0 | ✓ |
| Desktop-Typen | `cd desktop && npx tsc --noEmit` | Exit 0 | Exit 0 | ✓ |
| Projektregeln | `cd desktop && npm run check:rules` | Exit 0, 5 Gruppen, 3+17+25+19+5 = **69 Tests**, 0 FAIL | grün | ✓ |
| Server-Tests | `cd server && npx vitest run` | **578 grün / 3 rot** (genau `unifiedOvertimeService.test.ts:285`, `:340`, `vacationBackfillService.test.ts:138` — zeichengleich zur dokumentierten Ausgangsmenge) | rote Menge höchstens fünf, nicht wachsend | ✓ |

Die zwei zusätzlichen datumsabhängigen Fälle (`workPeriodChangeService.test.ts` „CR-01…“,
`overtimeFutureCapping.test.ts` „Test 2…“), die der Orchestrator am 26.08. maß, waren in meinem
eigenen Lauf **grün** — deckt sich mit der in den Prompt-Vorgaben genannten Fragilität (beide
kippen mit dem Kalendertag in beide Richtungen, kein Fix dieser Phase, UAT-Punkt 14.2-U27).

## Die elf Befunde — je einer am Quelltext geprüft

| Befund | Geprüft | Ergebnis |
|---|---|---|
| **F-1** | `reactivateUser()` (`server/src/services/userService.ts:853-888`): `WHERE id = ? AND (deletedAt IS NOT NULL OR status = 'inactive')` — deckt beide Zustände; ein aktiver Nutzer (`deletedAt NULL`, `status='active'`) matcht nicht → bleibt 404. Route `POST /api/users/:id/reactivate` (`server/src/routes/users.ts:554-561`): deutsche 404-Meldung `"Benutzer nicht gefunden oder bereits aktiv."`. **Live-Daten geprüft** (readonly): Testnutzer `sicht.p1228` (id 48737) steht jetzt auf `status='active'`, `deletedAt=NULL` — tatsächlich reaktiviert, kein Papierbeleg | ✓ VERIFIED |
| **F-2** | `getAllUsers()` (`server/src/services/userService.ts:274-344`): **eine** SQL-Abfrage mit drei korrelierten Unterabfragen für `currentWeeklyHours`/`currentWorkSchedule`/`currentValidFrom` (kein N+1, keine Schleife). Konsumenten bestätigt: `WorkScheduleDisplay.tsx:74-127`, `AdminDashboard.tsx:343`, `UserManagementPage.tsx:487` (Nutzerlistenspalte) lesen alle `currentWeeklyHours`/`currentWorkSchedule` | ✓ VERIFIED |
| **F-3** | `Select.tsx`: `useId()`, `htmlFor={selectId}`, `id={selectId}` gesetzt, `{...props}` reicht `name` durch. `EditUserModal.tsx:887`: `<Select name="role" …>` | ✓ VERIFIED |
| **F-4** | `UserManagementPage.tsx:80`: `useState<'active'|'all'|'inactive'>('all')` — Vorgabe ist `'all'`, nicht `'active'`. `getUserStatus()` (Z.36-40) unterscheidet `active`/`deactivated`/`archived`; Aktionsspalte zeigt für den mittleren Zustand `["Bearbeiten","Reaktivieren"]` | ✓ VERIFIED |
| **F-5** | `overtimeTransactionRebuildService.ts:142`: `const endDate = monthEnd > today ? today : monthEnd;` — **unbedingt**, nicht an den laufenden Monat gekoppelt. `git diff 9ee5e71..HEAD -- server/src/services/unifiedOvertimeService.ts server/src/services/overtimeLiveCalculationService.ts` ist **leer** — beide bereits korrekten Stellen unangetastet | ✓ VERIFIED |
| **F-6** | `desktop/src/api/exports.ts`: `extractErrorMessage()` in **beiden** `exportDATEV()` und `exportHistoricalCSV()` verwendet; `containsInternalDetails()` sperrt Stacktrace (`at `), `SQLITE_`, SQL-Schlüsselwörter, `/home/`, `C:\` | ✓ VERIFIED |
| **F-7** | `desktop/src/hooks/useAbsenceRequests.ts:326-337`: Meldung folgt `sourceStatus === 'approved'` → „storniert“, sonst „abgelehnt“; DB-Zustand bleibt `status: 'rejected'` (Z.303/318) — **Weg A**, kein Schema geändert (`git diff` gegen Migrationen leer) | ✓ VERIFIED (mit offen benannter Einschränkung — s. u.) |
| **F-8** | `WorkTimeChangeModal.tsx`: `resetForm()` (Z.480-491) ruft `onConflict?.(null)` **nicht mehr** auf; die Rücknahme wandert an Vorschauerfolg/Speichererfolg/`EditUserModal.handleClose`. `zIndexClass="z-[60]"` (Z.644) unverändert gegen `z-50`; `git show 48a8d58` (D-1-Commit) berührt diese Zeile nicht | ✓ VERIFIED |
| **D-1** | `Button.tsx`-Diff zeigt `dark:bg-blue-500→dark:bg-blue-600`, `dark:bg-red-500→dark:bg-red-600` — Kontrastfix über **9 Dateien** in einem Commit (`git show --stat 48a8d58`) | ✓ VERIFIED |
| **D-2** | `git show --stat` der vier D-2-Commits (`41efbac`, `afd1bdd`, `3c961e7`, `8a5d55b`): **keiner** berührt `Button.tsx`. `workTimePeriodActions.tsx`: `!p-2 sm:!px-3 sm:!py-2` (Spezifität über `!important` statt Primitive geändert) | ✓ VERIFIED |
| **V-1** | `13-UI-SPEC.md:9` `revision: 3`; Abschnitt „Barrierefreiheit“ (Z.872-878) beschreibt den `title`-Weg und nennt wörtlich: „Chromium öffnet sie bei Tastaturfokus **nicht**, und ESC schließt sie **nicht**.“ `git show --stat` der V-1-Commits `f9c4c6b`/`b4e3657`: keine `server/src`/`desktop/src`-Datei betroffen | ✓ VERIFIED |

## Scope Fence

| Geschützt | Geprüft | Ergebnis |
|---|---|---|
| 44 ENTSCHEIDUNG-Zeilen (`14-UAT-TRIAGE.md`) | `git diff 9ee5e71..HEAD --stat -- '*14-UAT-TRIAGE*'` | leer — unangetastet |
| 10 Warnungen WR-01…WR-10 (`14-WEITERE-BEFUNDE.md`) | `git diff 9ee5e71..HEAD --stat -- '*14-WEITERE-BEFUNDE*'` | leer — unangetastet |
| Trendpfeil P12-29b, `work_time_accounts.currentBalance` (B-3) | `git log --oneline 9ee5e71..HEAD --name-only` durchsucht, keine Trefferdatei | keine Datei berührt |

## Die zehn ROADMAP-Erfolgskriterien — eigenes Urteil

| # | Kriterium | Urteil | Beleg (selbst geprüft) |
|---|---|---|---|
| 1 | Deaktivierter Nutzer auffindbar und reaktivierbar | **erfüllt** | F-1/F-4 oben; Live-Datenprobe `sicht.p1228` |
| 2 | Desktop zeigt Wochenstunden der gültigen Periode, vor/nach Stichtag | **erfüllt** | F-2 oben; drei Konsumenten bestätigt |
| 3 | Drei E2E-Dateien laufen ohne roten Fall | **eingeschränkt erfüllt — Einordnung ist ehrlich, nicht zu milde** | Der im Abschlussbericht dokumentierte zweite Lauf (21/0/2, Exit 0) ist real belegt (Testliste mit 23 Einzelfällen). Aber: das Ergebnis ist **nicht reproduzierbar ohne manuellen Eingriff außerhalb des Codes** (19 Alt-Testnutzer umbenennen) — ein frischer Lauf fällt auf 12/9/2 zurück, weil die drei Spec-Dateien selbst keinen Aufräumschritt haben. Die Ursache liegt nachweislich außerhalb der elf Befunde (F-3/F-4 sind mit ihren Zielfällen `:221`/`:308` grün). Eine Bewertung als „erfüllt“ wäre zu milde, weil „ohne roten Fall“ kein dauerhafter Codezustand ist; „nicht erfüllt“ wäre zu hart, weil ein echter grüner Lauf mit Exitcode 0 belegt und die eigentlichen Zielfälle behoben sind |
| 4 | Kein Zeitraum in der Zukunft weist Überstunden aus | **erfüllt** | Eigene Prüfung der Rebuild-Deckelung (F-5 oben); NACHWEIS-F5.md 41/62 → 0/62 gegengelesen |
| 5 | DATEV-Fehler als lesbarer Satz, nicht JSON | **erfüllt** | F-6 oben, `extractErrorMessage()` in beiden Exportwegen |
| 6 | Ein Vorgang, ein Name (Schaltfläche/Meldung/DB) | **eingeschränkt erfüllt — Einordnung ist ehrlich** | F-7 oben: Schaltfläche „Stornieren“ und Meldung „storniert“ stimmen; DB speichert nachweislich weiterhin `status='rejected'` (Codezeile 303/318 gelesen). Zwei von drei Stellen — korrekt als eingeschränkt berichtet. Weg B ist eine echte Business-Entscheidung (Schemaänderung, Wechselwirkung mit NB-6-Filter), gehört nicht in einen Fehlerfix |
| 7 | Kontraste ≥4,5:1/3:1, hell und dunkel | **erfüllt für die sechs geprüften Flächen** | D-1 oben; `NACHWEIS-D1.md` mit vorher/nachher-Zahlen gegengelesen, ~35 weitere Dateien mit `text-green-600` (3,30:1) außerhalb der sechs Flächen ehrlich als Folgebefund benannt (14.2-U28), nicht verschwiegen |
| 8 | Trefferflächen ≥32×32 px | **erfüllt für die geprüften Flächen** | D-2 oben; `Button.tsx` selbst unverändert, Kollision betrifft grundsätzlich jeden Aufrufer der Primitive — als UAT-Punkt (14.2-U32) korrekt weitergereicht statt verschwiegen |
| 9 | Jeder Fix eigener Commit, einzeln revertierbar | **erfüllt** | Eigene Commit-Prüfung: 64 Commits, kein gemischtes Befundkürzel |
| 10 | Keine Zeile in den fünf geschützten Tabellen angefasst | **erfüllt** | Eigener Checksummen-Lauf oben — exakt identisch |

**Zusammenfassung (eigenes Urteil, deckt sich mit `14.2-ABSCHLUSS.md`):** 8 von 10 Kriterien
vollständig erfüllt, 2 nachweislich eingeschränkt erfüllt. Beide Einschränkungen sind ehrlich
benannt, mit Zahlen belegt und **nicht** beschönigt — ich habe keine Stelle gefunden, an der die
SUMMARYs oder der Abschlussbericht eine Lücke verharmlost oder eine Zahl unterschlagen hätten.

## UAT-Sammlung

| Prüfung | Ergebnis |
|---|---|
| Neuer Abschnitt „Phase 14.2“ vorhanden | ✓ Zeile 599 in `14-UAT-SAMMLUNG.md` |
| Nummernkreis 14.2-U1 bis 14.2-U45, lückenlos | ✓ `grep -oE "14\.2-U[0-9]+" … \| sort -u \| wc -l` → 45 |
| Abschnitte Phase 11–14.1 unberührt | ✓ `git diff 9ee5e71..HEAD -- 14-UAT-SAMMLUNG.md` zeigt **0 gelöschte Inhaltszeilen** (nur `---`-Header), 89 Einfügungen |
| Punkte konkret genug | Stichprobe 14.2-U1 bis U45 gelesen — jeder Punkt nennt eine ausführbare Prüfhandlung, einen gemessenen Ist-Wert mit Zahl/Commit-Hash und eine konkrete Erwartung. Kein vager Punkt gefunden |

## Warum status: human_needed (nicht passed)

Alle automatisiert prüfbaren Wahrheiten sind VERIFIED, alle vier Gates grün, alle drei harten
Zusagen selbst nachgemessen und bestätigt. Es gibt aber **keine offenen Implementierungslücken**
— stattdessen liegen, wie von D-15 verlangt, 45 Punkte vor, die eine menschliche Entscheidung
brauchen (Weg B zu F-7, der Tastaturverlust aus V-1, die E2E-Reproduzierbarkeit, mehrere
Dominanzurteile am Bildschirm). Nach der Entscheidungsregel des Verifikationsprozesses
(„IF Step 8 produced ANY human verification items → status: human_needed, auch bei vollem
Score“) ist der Status **human_needed**, nicht **passed** — obwohl inhaltlich nichts fehlt. Dies
ist keine Rüge an die Phase: Die Sammlung dieser Punkte statt eines stillen Übergehens ist selbst
Teil des verlangten Ergebnisses (D-15).

## Kleine, nicht blockierende Unstimmigkeit

`14.2-ABSCHLUSS.md` §6 Punkt 1 nennt die beiden `test.skip`-Zeilen als `user-creation.spec.ts:160`
und `:177`; `14.2-NACHWEIS-D01.md` nennt dieselben Fälle unter `:145`/`:160`. Eigene Prüfung am
aktuellen Quelltext: die Zeilen stehen tatsächlich bei **145** und **160**. Vermutlich hat sich die
Zeilennummerierung zwischen zwei Messzeitpunkten der Phase verschoben (andere Fälle wurden
dazwischen bearbeitet). Sachlich ändert das nichts — beide Tests sind und bleiben `test.skip`,
nicht aktiviert. Kein Gap, nur eine Anmerkung zur Präzision der Dokumentation.

---

_Verifiziert: 2026-08-26_
_Verifizierer: Claude (gsd-verifier)_
