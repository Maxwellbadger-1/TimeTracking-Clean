---
phase: 11-datumsabh-ngige-berechnung
plan: 08
type: execute
wave: 3
depends_on: ["11-03", "11-04"]
files_modified:
  - server/src/scripts/validateOvertimeDetailed.ts
  - server/src/scripts/validateOvertimeCalculation.ts
  - server/src/scripts/validateAllTestUsers.ts
  - server/src/scripts/migrateOvertimeToTransactions.ts
  - server/src/scripts/reproduceOvertimeCompDefect.ts
  - .planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-TEIL-08.md
autonomous: true
requirements: [REQ-25, REQ-23]

must_haves:
  truths:
    - "Das Validierungswerkzeug prüft gegen den periodengültigen Maßstab (REQ-25)"
    - "Kein Werkzeug- oder Migrationsskript baut eine zweite Auflösungs- oder Sollstundenlogik"
    - "Kein Werkzeuglauf dieses Plans schreibt in die Vergleichsgrundlage des Nullwirkungs-Nachweises"
  artifacts:
    - path: "server/src/scripts/validateOvertimeDetailed.ts"
      provides: "periodenbewusstes Validierungswerkzeug (npm run validate:overtime:detailed)"
      contains: "WorkPeriodContext"
    - path: ".planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-TEIL-08.md"
      provides: "Teilbeleg für die Zusammenführung der Aufrufer-Checkliste in Plan 11-09"
  key_links:
    - from: "server/src/scripts/validateOvertimeDetailed.ts"
      to: "server/src/services/workPeriodContext.ts"
      via: "dynamischer Import nach dem Produktionsschutz"
      pattern: "createWorkPeriodContext"
---

<objective>
Die Werkzeuge. `npm run validate:overtime:detailed` ist das Instrument, mit dem in diesem
Projekt Überstundenbefunde belegt werden — misst es mit dem alten Maßstab, meldet es bei
jedem Nutzer mit Modellwechsel eine Abweichung, die keine ist (REQ-25).

Purpose: Ein Nachweiswerkzeug, das den falschen Maßstab benutzt, ist schlimmer als keines.
Output: Fünf Werkzeug- und Migrationsskripte periodenbewusst, plus der Teilbeleg für die
Aufrufer-Checkliste.

**Herausgelöst:** Die acht Skripte, die Nutzer per direktem `INSERT INTO users` anlegen,
waren ursprünglich Task 3 dieses Plans. Sie stehen jetzt in Plan 11-11 (dieselbe Welle,
dieselben Abhängigkeiten). Grund: dreizehn Dateien in einem Kontextfenster liegen deutlich
über dem Richtwert, und genau eine davon — `server/scripts/create-admin.ts` — liegt außerhalb
des `tsc`-Umfangs und muss von Hand gefunden und geprüft werden. Sie hat dort einen eigenen
Task mit eigenem Prüfschritt bekommen, statt als dreizehnte Datei am Rand mitzulaufen.

**Zwischenzustand:** `npx tsc --noEmit` bleibt nach diesem Plan rot, solange die übrigen
Pläne der Welle 3 nicht fertig sind. Das Gesamtgrün wird in Plan 11-09 geprüft.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-datumsabh-ngige-berechnung/11-CONTEXT.md
@.planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-CHECKLISTE.md
@server/src/services/workPeriodContext.ts
@.claude/CLAUDE.md

<interfaces>
Neue Signaturen aus Plan 11-04:
  getDailyTargetHours(user: UserPublic, date: Date | string, periods: WorkPeriodContext): number
  calculateAbsenceHoursWithWorkSchedule(user, startDate, endDate, periods): number
  calculateTargetHoursForPeriod(user, fromDate, toDate, periods): number

Aus Plan 11-02: createWorkPeriodContext(), directWorkPeriodLookup

Skript-Muster (Lehre aus Phase 9, Vorbild `server/src/scripts/compareOvertimePaths.ts`):
  `assertNotProduction()` läuft synchron auf Modulebene VOR jedem Import, der transitiv
  `server/src/database/connection.ts` lädt — dieses Modul öffnet die Datenbank auf
  Modulebene (:50) und legt per `mkdirSync` Verzeichnisse an (:13-17). Service-Module
  danach per `await import()`. `workPeriodContext.ts` zieht über `workPeriodService.ts`
  die Datenbankverbindung und gehört deshalb in denselben dynamischen Importblock.

Ist-Stand `validateOvertimeDetailed.ts`:
  :102  let getDailyTargetHours: (user: UserPublic, date: Date | string) => number;
  :473, :634, :682   drei Aufrufe
  :1081-1089        dynamischer Importblock, der die Funktion zuweist

Ist-Stand `validateOvertimeCalculation.ts`:
  :59, :60          typisierte Halter für calculateTargetHoursForPeriod und
                    calculateAbsenceHoursWithWorkSchedule
  :185, :454        calculateTargetHoursForPeriod
  :245, :477        calculateAbsenceHoursWithWorkSchedule
  :527, :528        Zuweisung im dynamischen Importblock

Weitere: validateAllTestUsers.ts:73,:107 · migrateOvertimeToTransactions.ts:131 ·
reproduceOvertimeCompDefect.ts:226

ARBEITSKOPIEN — Zuordnung ist bindend:
  server/database/11-nullwirkung.db     Grundlage des Nullwirkungs-Nachweises (Plan 11-10).
                                        NUR LESEN. KEIN Werkzeuglauf dieses Plans fasst sie
                                        an. Grund: `getUserOvertimeReport()`
                                        (reportService.ts:116) und `getOvertimeSummary()`
                                        (overtimeService.ts:618) rufen
                                        `ensureOvertimeBalanceEntries()` auf und schreiben
                                        beim Lesen. Ein einziger solcher Lauf entwertet den
                                        Vorher/Nachher-Vergleich, und der Vorher-Zustand ist
                                        nicht wiederherstellbar.
  server/database/11-werkzeugprobe.db   Wegwerfkopie für alle Probeläufe dieses Plans.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: validateOvertimeDetailed — der Prüfmaßstab selbst (REQ-25)</name>
  <files>server/src/scripts/validateOvertimeDetailed.ts</files>
  <read_first>
    - `server/src/scripts/validateOvertimeDetailed.ts:90-115` — die Halter-Deklarationen und
      der Kommentar zum Produktionsschutz
    - `:460-490`, `:620-700` — die drei Aufrufstellen im Kontext ihrer Schleifen
    - `:1075-1095` — der dynamische Importblock
  </read_first>
  <action>
    Den Halter bei :102 auf die neue Signatur bringen
    (`(user: UserPublic, date: Date | string, periods: WorkPeriodContext) => number`) und im
    dynamischen Importblock zusätzlich `createWorkPeriodContext` aus
    `../services/workPeriodContext.js` laden — im selben `Promise.all`, nicht davor. Der
    Produktionsschutz oben bleibt unverändert an seiner Stelle.

    An jeder der drei Aufrufstellen den Kontext durchreichen. Wo eine Stelle in einer
    Tages- oder Monatsschleife steht, wird der Kontext EINMAL vor der Schleife erzeugt. Die
    Prüffunktion des Werkzeugs für einen Nutzer und einen Monat erzeugt genau einen Kontext
    und benutzt ihn für alle Vergleichswege dieses Laufs — sonst vergleicht das Werkzeug
    zwei Wege, die verschiedene Periodenstände gesehen haben könnten.

    Die Ausgabe des Werkzeugs erweitern: Für den geprüften Nutzer und Zeitraum werden die
    gefundenen Perioden mit `validFrom`, `validTo` und `weeklyHours` ausgegeben, bevor der
    Vergleich beginnt. Ohne diese Zeile lässt sich ein Befund des Werkzeugs bei einem Nutzer
    mit Modellwechsel nicht deuten.

    Der Kopfkommentar des Skripts bekommt einen Absatz: Ab dieser Phase misst das Werkzeug
    gegen den periodengültigen Maßstab (REQ-25); eine gemeldete Abweichung bei einem Nutzer
    mit Modellwechsel ist ab jetzt ein echter Befund und kein Artefakt des Werkzeugs.

    Der Probelauf zum Beleg der Periodenausgabe läuft gegen die Wegwerfkopie
    `server/database/11-werkzeugprobe.db`, NICHT gegen `11-nullwirkung.db`. Kopie per
    `VACUUM INTO` aus `server/database/development.db` per `node -e` mit `better-sqlite3`
    ziehen (`sqlite3`-CLI ist auf diesem Rechner nicht verfügbar; existiert die Datei
    bereits, vorher löschen — `VACUUM INTO` überschreibt nicht). Der laufende Dev-Server hält
    `development.db` offen, deshalb keine Dateikopie.
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit 2>&1 | grep "validateOvertimeDetailed" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit 2>&1 | grep validateOvertimeDetailed` liefert keine Zeile.
    - `assertNotProduction` steht weiterhin VOR allen datenbankziehenden Importen
      (`grep -n "assertNotProduction\|await import" server/src/scripts/validateOvertimeDetailed.ts`
      belegt die Reihenfolge).
    - `createWorkPeriodContext` wird im dynamischen Importblock geladen, nicht per
      statischem `import` am Dateikopf.
    - Die Periodenausgabe erscheint im Werkzeuglauf — Nachweis über einen echten Lauf gegen
      `server/database/11-werkzeugprobe.db` mit einem beliebigen Nutzer, Ausgabe in der
      SUMMARY. Der protokollierte Befehl enthält
      `DATABASE_PATH=./database/11-werkzeugprobe.db` und nennt weder `11-nullwirkung.db` noch
      `production.db`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Die übrigen Werkzeug- und Migrationsskripte</name>
  <files>server/src/scripts/validateOvertimeCalculation.ts, server/src/scripts/validateAllTestUsers.ts, server/src/scripts/migrateOvertimeToTransactions.ts, server/src/scripts/reproduceOvertimeCompDefect.ts</files>
  <read_first>
    - `server/src/scripts/validateOvertimeCalculation.ts:30-65` — Produktionsschutz und
      Halter-Deklarationen; `:180-190`, `:240-250`, `:450-460`, `:470-480`
    - `server/src/scripts/validateAllTestUsers.ts:60-115`
    - `server/src/scripts/migrateOvertimeToTransactions.ts:120-140`
    - `server/src/scripts/reproduceOvertimeCompDefect.ts:130-145`, `:220-235`
  </read_first>
  <action>
    Jede Fundstelle auf die neuen Signaturen bringen, nach demselben Muster wie Task 1:
    Halter-Typen anpassen, `createWorkPeriodContext` im jeweiligen dynamischen Importblock
    mitladen, Kontext einmal je Prüf-/Migrationslauf erzeugen und durchreichen.

    `validateOvertimeCalculation.ts` hat vier Fundstellen in zwei Funktionen — beide bekommen
    ihren eigenen Kontext am Funktionsanfang.

    `migrateOvertimeToTransactions.ts` schleift über einen längeren Zeitraum: ein Kontext je
    Nutzer-Migrationslauf, nicht je Tag.

    Bei `reproduceOvertimeCompDefect.ts` ist der erwartete Abzug eine Gegenrechnung zum
    Produktivpfad. Er MUSS denselben Maßstab benutzen wie `absenceService` (Plan 11-07),
    sonst reproduziert das Skript ab jetzt einen Defekt, den es selbst erzeugt.

    Kein statischer Import, der die Datenbankverbindung vor `assertNotProduction` zieht.

    Falls für eine dieser Dateien ein Probelauf nötig wird: ausschließlich gegen
    `server/database/11-werkzeugprobe.db`.
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit 2>&1 | grep -E "validateOvertimeCalculation|validateAllTestUsers|migrateOvertimeToTransactions|reproduceOvertimeCompDefect" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` meldet für keine der vier Dateien einen Fehler.
    - In jeder der vier Dateien steht `assertNotProduction` weiterhin vor den dynamischen
      Importen.
    - `grep -c "createWorkPeriodContext" <datei>` ist für jede der vier Dateien mindestens 1.
    - Kein protokollierter Probelauf nennt `11-nullwirkung.db` oder `production.db`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Teilbeleg für Plan 11-09</name>
  <files>.planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-TEIL-08.md</files>
  <read_first>
    - `.planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-CHECKLISTE.md` — die
      Tabellenform, damit der Teilbeleg dieselben Spalten benutzt
  </read_first>
  <action>
    Teilbeleg `11-AUFRUFER-TEIL-08.md` anlegen. Er enthält NUR die Zeilen dieses Plans — die
    fünf Werkzeug- und Migrationsskripte mit Haken bzw. Disposition, in derselben
    Spaltenform wie die Haupttabelle — plus je Datei die wörtliche `grep`-Ausgabe, die belegt,
    dass jede Fundstelle die neue Signatur trägt, und die Ausgabe des Probelaufs aus Task 1.

    Plan 11-09 führt die Teilbelege der Welle 3 zusammen und hakt in der Hauptdatei ab.

    Warum getrennte Dateien statt direkt in `11-AUFRUFER-CHECKLISTE.md`: Die Pläne 11-05 bis
    11-08 und 11-11 laufen in derselben Welle parallel (`parallelization: true`). Schrieben
    sie alle in dieselbe Datei, ginge bei gleichzeitigem Schreiben ein Update verloren — und
    zwar an genau dem Dokument, das der zentrale Vollständigkeitsbeleg für REQ-23 ist. Die
    Zusammenführung passiert deshalb sequenziell in Welle 4.

    Diese Datei NICHT anfassen: `11-AUFRUFER-CHECKLISTE.md`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-TEIL-08.md','utf8');const rows=t.split('\n').filter(l=>l.trim().startsWith('|')&&!/^\|\s*[-: ]+\|/.test(l));console.log('Tabellenzeilen:',rows.length);process.exit(rows.length>=5?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `11-AUFRUFER-TEIL-08.md` existiert und enthält für alle fünf Dateien dieses Plans je
      eine Tabellenzeile in der Spaltenform der Haupttabelle.
    - Je Datei steht die wörtliche `grep`-Ausgabe der Fundstellen im Beleg.
    - Die Probelauf-Ausgabe aus Task 1 steht im Beleg, mit dem wörtlichen Befehl.
    - `git diff --name-only` zeigt für diesen Plan KEINE Änderung an
      `11-AUFRUFER-CHECKLISTE.md`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skript → Datenbankdatei | Skripte öffnen die Datenbank auf Modulebene, bevor Argumente geprüft sind |
| Werkzeugausgabe → Entscheidung des Anwenders | Ein falsch messendes Werkzeug erzeugt falsche Befunde |
| Werkzeuglauf → `11-nullwirkung.db` | Validierungswege schreiben beim Lesen und würden den Nachweis in Plan 11-10 entwerten |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-27 | Tampering | Skript öffnet versehentlich die Produktionsdatenbank | mitigate | `assertNotProduction()` synchron auf Modulebene vor jedem datenbankziehenden Import; Reihenfolge per grep in den Acceptance Criteria geprüft |
| T-11-28 | Repudiation | Validierungswerkzeug meldet Scheinabweichungen | mitigate | REQ-25: Messung gegen den periodengültigen Maßstab, Periodenausgabe im Bericht, Beleg in Plan 11-10 |
| T-11-29 | Tampering | Probelauf schreibt beim Lesen in die Vergleichsgrundlage des Nullwirkungs-Nachweises | mitigate | Alle Probeläufe gegen `11-werkzeugprobe.db`; `getUserOvertimeReport()` und `getOvertimeSummary()` rufen `ensureOvertimeBalanceEntries()` auf — deshalb ist `11-nullwirkung.db` für diesen Plan gesperrt, geprüft per Acceptance Criteria |
| T-11-30 | Tampering | Verlorenes Update an `11-AUFRUFER-CHECKLISTE.md` bei paralleler Welle | mitigate | Dieser Plan schreibt ausschließlich in seinen eigenen Teilbeleg; die Zusammenführung ist sequenziell in Plan 11-09 |
| T-11-SC | Tampering | Paketinstallation | mitigate | Dieser Plan installiert kein Paket |
</threat_model>

<verification>
- `npx tsc --noEmit 2>&1 | grep "src/scripts/"` meldet für die fünf Dateien dieses Plans
  keinen Fehler.
- `npm run validate:overtime:detailed` läuft gegen `server/database/11-werkzeugprobe.db`
  durch und gibt die Perioden des geprüften Nutzers aus.
- `11-AUFRUFER-TEIL-08.md` liegt vor; `11-AUFRUFER-CHECKLISTE.md` ist unverändert.
- Kein Befehl dieses Plans nennt `11-nullwirkung.db` oder `production.db`.
</verification>

<success_criteria>
Das Validierungswerkzeug misst gegen den periodengültigen Maßstab. Kein Werkzeug- oder
Migrationsskript trägt eine zweite Rechenregel. Die Vergleichsgrundlage des
Nullwirkungs-Nachweises ist unberührt geblieben.
</success_criteria>

<output>
Create `.planning/phases/11-datumsabh-ngige-berechnung/11-08-SUMMARY.md` when done
</output>
