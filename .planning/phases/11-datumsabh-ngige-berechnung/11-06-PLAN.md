---
phase: 11-datumsabh-ngige-berechnung
plan: 06
type: execute
wave: 3
depends_on: ["11-04"]
files_modified:
  - server/src/services/overtimeService.ts
  - server/src/services/overtimeService.test.ts
  - server/src/services/overtimeTransactionCentralization.test.ts
  - server/src/services/overtimeLiveCalculationService.ts
  - server/src/services/overtimeLiveCalculationService.test.ts
  - .planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-TEIL-06.md
autonomous: true
requirements: [REQ-23]

must_haves:
  truths:
    - "Kein Aufrufer aus dem overtimeService-Pfad rechnet an der neuen Signatur vorbei (D7)"
    - "Die Live-Anzeige der Überstunden nutzt für jeden Tag die Periode dieses Tages"
    - "Der Legacy-Pfad, der overtime_balance schreibt, ist periodenbewusst"
  artifacts:
    - path: "server/src/services/overtimeService.ts"
      provides: "periodenbewusster Legacy- und Sicherstellungspfad (ensureOvertimeBalanceEntries, ensureDailyOvertimeTransactions, ensureAbsenceTransactions)"
      contains: "WorkPeriodContext"
    - path: "server/src/services/overtimeLiveCalculationService.ts"
      provides: "periodenbewusste Live-Berechnung inklusive getAllWorkingDaysBetween"
      contains: "WorkPeriodContext"
  key_links:
    - from: "server/src/services/overtimeService.ts"
      to: "server/src/services/workPeriodContext.ts"
      via: "ein Kontext je Sicherstellungs-/Monatslauf"
      pattern: "createWorkPeriodContext"
---

<objective>
Der Legacy-Pfad und die Live-Anzeige. D7 verlangt ausdrücklich, dass geprüft wird, dass kein
Aufrufer aus dem `overtimeService.ts`-Pfad an der neuen Signatur vorbei rechnet.

Purpose: `overtimeService.ts` enthält sieben Fundstellen, davon vier in totem Code und drei
im Produktivpfad (`ensureDailyOvertimeTransactions`, `ensureAbsenceTransactions`,
`ensureOvertimeBalanceEntries`). Über den letzten läuft auch der tägliche Cronjob
`fix-overtime.ts`. Die Live-Anzeige (`GET /api/overtime/transactions/live`) ist der Weg, den
der Mitarbeiter im Dashboard sieht.
Output: Beide Dateien periodenbewusst, Tests nachgezogen, D7-Nachweis erbracht.

**Zwischenzustand:** `npx tsc --noEmit` bleibt rot (Pläne 11-07, 11-08 offen). Prüfkriterium
ist, dass in den hier geänderten Dateien kein Fehler mehr steht.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-datumsabh-ngige-berechnung/11-CONTEXT.md
@.planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-CHECKLISTE.md
@.planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md
@server/src/services/workPeriodContext.ts
@.claude/CLAUDE.md

<interfaces>
Neue Signaturen aus Plan 11-04:
  getDailyTargetHours(user: UserPublic, date: Date | string, periods: WorkPeriodContext): number
  calculateAbsenceHoursWithWorkSchedule(user, startDate, endDate, periods): number
  class MissingWorkPeriodError extends Error

Aus Plan 11-02:
  createWorkPeriodContext(): WorkPeriodContext
  directWorkPeriodLookup: WorkPeriodContext

Aus Plan 11-04: server/src/test-support/workPeriodFixtures.ts
  stubWorkPeriodContext(entries), insertTestWorkPeriod(userId, {...})

Fundstellen in `server/src/services/overtimeService.ts` (Stand 09-INVENTAR, Zeilen können
verschoben sein — heutigen Stand per grep prüfen):
  :109  _calculateAbsenceCreditsForMonth        — toter Code (kein Aufrufer, Inventar Z. 88)
  :193  _calculateUnpaidLeaveForMonth           — toter Code (Inventar Z. 89)
  :329  ensureAbsenceTransactionsForMonth PHASE 1 — toter Code (Inventar Z. 90)
  :370  ensureAbsenceTransactionsForMonth PHASE 2 — toter Code (Inventar Z. 93)
  :570  _updateOvertimeTransactionsForDate      — @deprecated, kein Aufrufer (Inventar Z. 94)
  :853  ensureDailyOvertimeTransactions         — PRODUKTIV, via routes/overtime.ts:610
  :1353 ensureAbsenceTransactions               — PRODUKTIV, von :869 aufgerufen
  :454  updateMonthlyOvertime                   — delegiert an unifiedOvertimeService
  :745  ensureOvertimeBalanceEntries            — PRODUKTIV, ruft calculateMonthlyOvertime je Monat

Fundstellen in `server/src/services/overtimeLiveCalculationService.ts`:
  :43   getAllWorkingDaysBetween(startDate, endDate, user)  — exportiert, ruft :59
  :126  userForCalc wird als `{ id, weeklyHours, workSchedule } as UserPublic` gebaut
  :179, :254, :305, :407  vier weitere Aufrufe in calculateLiveOvertimeTransactions
  Die Datei enthält kein INSERT/UPDATE/DELETE — sie ist der nebenwirkungsfreie Anzeigeweg.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: overtimeService — Produktivpfad und toter Code auf die neue Signatur</name>
  <files>server/src/services/overtimeService.ts</files>
  <read_first>
    - Alle neun oben genannten Stellen in `server/src/services/overtimeService.ts` —
      Zeilennummern vorher per `grep -n "getDailyTargetHours(" server/src/services/overtimeService.ts`
      auf den heutigen Stand bringen
    - `server/src/services/overtimeService.ts` um `ensureOvertimeBalanceEntries` herum:
      Wie wird über die Monate geschleift?
    - `.planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md`, Abschnitt
      „overtimeService.ts — Legacy-Pfad" — welche Fundstelle produktiv ist und welche nicht
  </read_first>
  <behavior>
    - `ensureOvertimeBalanceEntries(userId, upToMonth)` bei einem Nutzer mit Stichtag: die
      Monate vor dem Stichtag behalten ihre Sollstunden, der Stichtagsmonat trägt die
      gemischte Summe, die Monate danach die neuen
    - Ein Lauf über zwölf Monate lädt die Perioden des Nutzers genau einmal
    - `ensureDailyOvertimeTransactions` und `ensureAbsenceTransactions` schreiben für Tage
      vor und nach dem Stichtag die jeweils gültigen Sollstunden
    - Für einen Nutzer ohne Modellwechsel bleiben alle Werte unverändert
  </behavior>
  <action>
    Jede Fundstelle auf die neue Signatur bringen. Regel für die Kontextwahl:
    - Funktionen, die über mehrere Tage oder Monate schleifen
      (`ensureOvertimeBalanceEntries`, `ensureDailyOvertimeTransactions`,
      `ensureAbsenceTransactions`, die beiden `_calculate*ForMonth`,
      `ensureAbsenceTransactionsForMonth`): `createWorkPeriodContext()` EINMAL am
      Funktionsanfang, danach durchreichen.
    - Funktionen, die genau einen Tag rechnen (`_updateOvertimeTransactionsForDate`):
      `directWorkPeriodLookup`.
    - `ensureOvertimeBalanceEntries` reicht seinen Kontext zusätzlich an
      `unifiedOvertimeService.calculateMonthlyOvertime(userId, month, periods)` durch — sonst
      baut jeder Monat seinen eigenen Cache und der Vorteil aus D1 verpufft.

    Der tote Code (fünf Fundstellen, im Inventar der Phase 9 einzeln als „kein Aufrufer"
    belegt) wird mitgezogen, nicht gelöscht und nicht auskommentiert. Löschen wäre eine
    Bereinigung außerhalb dieser Phase; ihn stehen zu lassen und nicht zu übersetzen ginge
    nicht, weil `tsc` die Datei prüft. Über jeder dieser fünf Stellen einen einzeiligen
    Vermerk setzen, dass sie laut Inventar 09 keinen Aufrufer hat.

    Kein `any` neu einführen. Keine zweite Auflösungslogik: keine Zeile in dieser Datei liest
    `users.weeklyHours` oder `users.workSchedule`, um daraus Sollstunden zu bilden — falls
    doch eine solche Stelle auftaucht, die per grep nicht über `getDailyTargetHours` läuft,
    wird sie in der SUMMARY als Fund gemeldet (das ist genau der Fall, den Plan 09-04
    übersehen hat).
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit 2>&1 | grep "services/overtimeService.ts" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit 2>&1 | grep "services/overtimeService.ts"` liefert keine Zeile.
    - `grep -c "createWorkPeriodContext" server/src/services/overtimeService.ts` entspricht der
      Anzahl der schleifenden Funktionen aus dem Action-Abschnitt; jede steht am
      Funktionsanfang, nicht in der Schleife.
    - `grep -n "weeklyHours / 5\|weeklyHours/5" server/src/services/overtimeService.ts` liefert
      keine Zeile (keine zweite Kopie der Regel).
    - `calculateMonthlyOvertime` wird aus `ensureOvertimeBalanceEntries` mit dem Kontext als
      drittem Argument aufgerufen.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: overtimeLiveCalculationService — die Anzeige, die der Mitarbeiter sieht</name>
  <files>server/src/services/overtimeLiveCalculationService.ts, server/src/services/overtimeLiveCalculationService.test.ts</files>
  <read_first>
    - `server/src/services/overtimeLiveCalculationService.ts:20-70` — `getAllWorkingDaysBetween`
    - `:110-140` — Aufbau von `userForCalc`
    - `:170-420` — die vier weiteren Fundstellen im Kontext ihrer Schleifen
  </read_first>
  <behavior>
    - `getAllWorkingDaysBetween(start, end, user, ctx)` bei einem Nutzer, dessen Wochenplan
      erst ab dem Stichtag einen freien Dienstag vorsieht: Dienstage vor dem Stichtag sind
      Arbeitstage, danach nicht
    - `calculateLiveOvertimeTransactions` liefert für Tage vor und nach dem Stichtag die
      jeweils gültigen Sollstunden in den Transaktionszeilen
    - Für einen Nutzer ohne Modellwechsel ist die Ausgabe unverändert
  </behavior>
  <action>
    `getAllWorkingDaysBetween` bekommt `periods: WorkPeriodContext` als vierten
    Pflichtparameter (die Funktion ist exportiert und schleift über Tage — ein Vorgabewert
    würde hier einen Cache je Aufruf erzeugen, das ist gewollt, aber der Aufrufer in
    derselben Datei soll seinen eigenen Kontext weiterreichen).

    `calculateLiveOvertimeTransactions` erzeugt EINEN Kontext mit `createWorkPeriodContext()`
    vor der ersten Schleife und reicht ihn an alle fünf Stellen und an
    `getAllWorkingDaysBetween` durch.

    Wichtige Korrektur an `userForCalc` (:126): Das Objekt wird heute als
    `{ id, weeklyHours, workSchedule } as UserPublic` gebaut und trägt kein `hireDate`.
    `getDailyTargetHours` braucht `hireDate` für die D4-Ausnahme (Datum vor Eintritt → 0
    statt Fehler). `hireDate: user.hireDate` ergänzen — der Wert liegt an dieser Stelle
    bereits vor (:116 benutzt ihn für die Bereichsgrenze). Ohne diese Zeile würde ein
    einziger Tag vor dem Eintritt die gesamte Live-Anzeige mit
    `MissingWorkPeriodError` abbrechen lassen.

    Die Datei bleibt nebenwirkungsfrei: kein INSERT, kein UPDATE, kein DELETE.
  </action>
  <verify>
    <automated>cd server && npx vitest run src/services/overtimeLiveCalculationService.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Alle Fälle aus `<behavior>` sind als Tests vorhanden und grün.
    - `grep -n "hireDate" server/src/services/overtimeLiveCalculationService.ts` zeigt die
      Zeile im `userForCalc`-Objekt.
    - `grep -c "createWorkPeriodContext" server/src/services/overtimeLiveCalculationService.ts`
      ergibt 1.
    - `grep -c "INSERT\|UPDATE\|DELETE" server/src/services/overtimeLiveCalculationService.ts`
      ergibt 0 (Kommentarzeilen ausgeschlossen).
    - `npx tsc --noEmit 2>&1 | grep overtimeLiveCalculationService` liefert keine Zeile.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Tests des Legacy-Pfads nachziehen und den D7-Nachweis erbringen</name>
  <files>server/src/services/overtimeService.test.ts, server/src/services/overtimeTransactionCentralization.test.ts, .planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-TEIL-06.md</files>
  <read_first>
    - `server/src/services/overtimeService.test.ts` und
      `server/src/services/overtimeTransactionCentralization.test.ts` — wo Nutzer per
      `INSERT INTO users` angelegt werden
  </read_first>
  <action>
    Beide Testdateien legen Nutzer per direktem `INSERT INTO users` an. Diese Nutzer haben
    keine Periode und lösen nach Plan 11-04 den D4-Fehler aus. Nach jedem solchen INSERT
    `insertTestWorkPeriod(userId, { validFrom: <hireDate des Testnutzers>, weeklyHours,
    workSchedule })` aus den Testhilfen aufrufen, mit denselben Werten, die in `users`
    stehen. Kein eigener INSERT in `user_work_periods`.

    Erwartungswerte der bestehenden Tests dürfen sich nicht ändern. Jede Änderung eines
    Erwartungswerts ist ein Befund für die SUMMARY.

    D7-Nachweis erbringen und im Teilbeleg `11-AUFRUFER-TEIL-06.md` als Abschnitt
    „D7: Legacy-Pfad" ablegen, mit den wörtlichen Befehlen und Ausgaben:
    1. `grep -rn "getDailyTargetHours(" server/src/services/overtimeService.ts` — jede Zeile
       trägt jetzt drei Argumente.
    2. `grep -rn "weeklyHours / 5\|weeklyHours/5" server/src/services --include=*.ts` — keine
       Zeile außerhalb von Kommentaren und Typdefinitionen.
    3. Aufrufkette von `ensureOvertimeBalanceEntries` prüfen: `grep -rn
       "ensureOvertimeBalanceEntries" server/src server/scripts --include=*.ts` — jeder
       Aufrufer (auch `server/scripts/fix-overtime.ts` über `dist/`) läuft über die
       geänderte Funktion und damit über den Kontext. Ergebnis wörtlich festhalten.
    4. Die Zeilen der beiden in diesem Plan geänderten Dienste als Tabellenzeilen mit Haken
       in den Teilbeleg schreiben, in derselben Spaltenform wie die Haupttabelle.

    `11-AUFRUFER-CHECKLISTE.md` wird dabei NICHT angefasst: Die Pläne 11-05 bis 11-08 und
    11-11 laufen in derselben Welle parallel (`parallelization: true`); schrieben sie alle in
    dieselbe Datei, ginge bei gleichzeitigem Schreiben ein Update verloren — an genau dem
    Dokument, das der zentrale Vollständigkeitsbeleg für REQ-23 ist. Plan 11-09 führt die
    Teilbelege sequenziell zusammen und hakt dort ab.
  </action>
  <verify>
    <automated>cd server && npx vitest run src/services/overtimeService.test.ts src/services/overtimeTransactionCentralization.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Beide Testdateien sind grün; kein neuer roter Test gegenüber `11-AUSGANGSZUSTAND.md`.
    - Der Abschnitt „D7: Legacy-Pfad" steht in `11-AUFRUFER-TEIL-06.md` mit den vier
      Befehlen und ihren wörtlichen Ausgaben.
    - Der Teilbeleg enthält je eine Tabellenzeile mit Haken für `overtimeService.ts` und
      `overtimeLiveCalculationService.ts`.
    - `git diff --name-only` zeigt für diesen Plan KEINE Änderung an
      `11-AUFRUFER-CHECKLISTE.md`.
    - Kein Testnutzer bleibt zurück (Aufräumnachweis in beiden Dateien).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `GET /api/overtime/...` → Legacy-/Live-Pfad | Von außen ausgelöste Berechnung mit Schreibwirkung auf `overtime_balance` |
| Cronjob `fix-overtime.ts` → `ensureOvertimeBalanceEntries` | Täglicher, unbeaufsichtigter Schreibvorgang in Produktion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-19 | Tampering | Cronjob überschreibt Salden mit nicht-periodenbewussten Werten | mitigate | `fix-overtime.ts` delegiert seit Phase 9 an `ensureOvertimeBalanceEntries`; D7-Nachweis Punkt 3 belegt, dass jeder Aufrufer über die geänderte Funktion läuft |
| T-11-20 | Denial of Service | `MissingWorkPeriodError` in der Live-Anzeige bricht das Dashboard ab | mitigate | `hireDate` in `userForCalc` ergänzen (D4-Ausnahme greift); Plan 11-09 prüft alle Nutzer der Arbeitskopie durch |
| T-11-21 | Information Disclosure | Fehlermeldung in der HTTP-Antwort | accept | Die Meldung nennt nur `userId` und Datum; die bestehende Fehlerbehandlung der Routen bleibt unverändert |
| T-11-22 | Tampering | Toter Code als zweite Rechenregel | mitigate | Die fünf Fundstellen ohne Aufrufer werden mitgezogen und einzeln als aufruferlos vermerkt statt stillschweigend belassen |
| T-11-49 | Tampering | Verlorenes Update an `11-AUFRUFER-CHECKLISTE.md` bei paralleler Welle | mitigate | Dieser Plan schreibt ausschließlich in seinen eigenen Teilbeleg; die Zusammenführung ist sequenziell in Plan 11-09 |
| T-11-SC | Tampering | Paketinstallation | mitigate | Dieser Plan installiert kein Paket |
</threat_model>

<verification>
- `npx tsc --noEmit 2>&1 | grep -E "overtimeService.ts|overtimeLiveCalculationService.ts"` liefert keine Zeile.
- `npx vitest run src/services/overtimeService.test.ts src/services/overtimeTransactionCentralization.test.ts src/services/overtimeLiveCalculationService.test.ts` grün.
- Abschnitt „D7: Legacy-Pfad" liegt in `11-AUFRUFER-TEIL-06.md`; `11-AUFRUFER-CHECKLISTE.md`
  ist unverändert (Abhaken erfolgt sequenziell in Plan 11-09).
</verification>

<success_criteria>
Legacy-Pfad und Live-Anzeige rechnen periodengetreu. Der D7-Nachweis liegt schriftlich vor,
mit Befehlen und Ausgaben statt einer Behauptung.
</success_criteria>

<output>
Create `.planning/phases/11-datumsabh-ngige-berechnung/11-06-SUMMARY.md` when done
</output>
