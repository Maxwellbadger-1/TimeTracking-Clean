---
phase: 11-datumsabh-ngige-berechnung
plan: 07
type: execute
wave: 3
depends_on: ["11-04"]
files_modified:
  - server/src/services/absenceService.ts
  - server/src/services/exportService.ts
  - server/src/services/absencePeriodAwareness.test.ts
autonomous: true
requirements: [REQ-23]

must_haves:
  truths:
    - "Die Stunden einer Abwesenheit werden für jeden Tag mit der Periode dieses Tages berechnet"
    - "Der Abzug bei Überstundenausgleich (overtime_comp) folgt demselben Maßstab wie die Gutschrift"
    - "Der DATEV-Export weist je Zeiteintrag die am Eintragstag gültigen Sollstunden aus"
  artifacts:
    - path: "server/src/services/absenceService.ts"
      provides: "periodenbewusste Abwesenheitsstunden an allen vier Fundstellen"
      contains: "WorkPeriodContext"
    - path: "server/src/services/exportService.ts"
      provides: "periodenbewusster DATEV-Export"
      contains: "WorkPeriodContext"
    - path: "server/src/services/absencePeriodAwareness.test.ts"
      provides: "Nachweis, dass eine Abwesenheit über einen Stichtag hinweg richtig gerechnet wird"
  key_links:
    - from: "server/src/services/absenceService.ts"
      to: "server/src/utils/workingDays.ts"
      via: "calculateAbsenceHoursWithWorkSchedule mit Nutzer und Kontext"
      pattern: "calculateAbsenceHoursWithWorkSchedule"
---

<objective>
Die vierte Fundstellengruppe: Abwesenheiten und Export. Hier steckt der Fall, der beim
bloßen Umbau von `getDailyTargetHours` durchgerutscht wäre —
`calculateAbsenceHoursWithWorkSchedule` bekommt `workSchedule` und `weeklyHours` als
Parameter und hätte ohne Änderung weiter aus den heutigen Stammdaten gerechnet, ohne dass
der Compiler etwas gemeldet hätte.

Purpose: Genau diese Sorte Fundstelle hat Phase 9 zwölf Kopien einer Regel beschert. Vier
Aufrufe in `absenceService.ts` entscheiden über Urlaubsstunden, den Genehmigungsvorbehalt
bei Überstundenausgleich und die tatsächliche Abbuchung.
Output: Vier Fundstellen in `absenceService.ts`, eine in `exportService.ts`, ein neuer Test.

**Zwischenzustand:** `npx tsc --noEmit` bleibt rot (Plan 11-08 offen). Prüfkriterium ist,
dass in den hier geänderten Dateien kein Fehler mehr steht.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-datumsabh-ngige-berechnung/11-CONTEXT.md
@.planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-CHECKLISTE.md
@server/src/services/workPeriodContext.ts
@.claude/CLAUDE.md

<interfaces>
Neue Signaturen aus Plan 11-04:
  getDailyTargetHours(user: UserPublic, date: Date | string, periods: WorkPeriodContext): number
  calculateAbsenceHoursWithWorkSchedule(
    user: UserPublic, startDate: string, endDate: string, periods: WorkPeriodContext): number

Aus Plan 11-02:
  createWorkPeriodContext(): WorkPeriodContext
  directWorkPeriodLookup: WorkPeriodContext

Aus Plan 11-04: server/src/test-support/workPeriodFixtures.ts
  stubWorkPeriodContext(entries), insertTestWorkPeriod(userId, {...})

Fundstellen (Stand heute, vor der Ausführung per grep bestätigen):
  absenceService.ts:360   getAbsenceRequests-Anreicherung — `user` stammt aus getUserById()
  absenceService.ts:791   Genehmigungsvorbehalt overtime_comp — `user` ist eine ROHE
                          DB-Zeile (`SELECT * FROM users ... as any`), workSchedule als Text
  absenceService.ts:863   Abbuchung overtime_comp — ebenfalls rohe DB-Zeile
  absenceService.ts:1195  calculateAbsenceCredits, Tagesschleife über getDailyTargetHours
  exportService.ts:98     generateDATEVExport, je Zeiteintrag

NICHT in diesem Plan (Disposition steht in 11-AUFRUFER-CHECKLISTE.md):
  absenceService.ts:464, :472, :697, :700 — `countWorkingDaysForUser` zählt Urlaubstage,
  keine Sollstunden. Bleibt in Phase 11 unverändert.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: absenceService — vier Fundstellen, ein Maßstab</name>
  <files>server/src/services/absenceService.ts</files>
  <read_first>
    - `server/src/services/absenceService.ts:350-375` — Anreicherung mit `calculatedHours`
    - `:780-805` — Genehmigungsvorbehalt bei `overtime_comp`
    - `:855-875` — die tatsächliche Abbuchung bei `overtime_comp`
    - `:1176-1205` — `calculateAbsenceCredits` mit der Tagesschleife
    - `grep -n "getUserById" server/src/services/absenceService.ts` — ist die Funktion in
      dieser Datei bereits importiert?
  </read_first>
  <behavior>
    Nutzer mit Stichtag 15.07.2026 (davor 40h, danach 20h, kein Wochenplan), Abwesenheit vom
    13.07. bis 17.07.2026:
    - Die angereicherten `calculatedHours` betragen 28 (2×8 + 3×4), nicht 40 und nicht 20
    - Der Genehmigungsvorbehalt bei `overtime_comp` prüft gegen 28
    - Die Abbuchung bucht 28 ab — Vorbehalt und Abbuchung nennen denselben Wert
    - `calculateAbsenceCredits` für denselben Zeitraum liefert ebenfalls 28
    Ohne Modellwechsel bleiben alle vier Werte unverändert gegenüber heute
  </behavior>
  <action>
    Alle vier Fundstellen auf die neuen Signaturen bringen.

    Bei :360 liegt bereits ein `UserPublic` aus `getUserById()` vor — nur Argumente
    umstellen. Die Anreicherung läuft in einem `rows.map()`: den Kontext EINMAL vor dem `map`
    mit `createWorkPeriodContext()` erzeugen, damit eine Liste mit vielen Anträgen desselben
    Nutzers die Perioden nicht je Antrag lädt.

    Bei :791 und :863 wird der Nutzer heute als rohe Datenbankzeile geladen
    (`SELECT * FROM users ... as any`) — dieses Objekt hat kein geparstes `workSchedule` und
    wird von `getDailyTargetHours` für die D4-Ausnahme mit `hireDate` gebraucht. Beide
    Stellen auf `getUserById(request.userId)` umstellen. Liefert `getUserById` für einen
    soft-gelöschten Nutzer nichts (Datei prüfen, nicht annehmen), bleibt die rohe Abfrage
    stehen und wird zu einem vollständigen `UserPublic`-Objekt ergänzt, das mindestens `id`,
    `hireDate`, `weeklyHours` und das geparste `workSchedule` trägt. Welcher der beiden Wege
    gewählt wurde und warum, gehört in die SUMMARY.

    :791 und :863 rechnen denselben Zeitraum desselben Antrags zweimal. Beide MÜSSEN
    denselben Wert liefern — das ist die Regel, die Phase 9 unter CR-01 als zwölffach kopiert
    vorgefunden hat. Beide Stellen benutzen deshalb dieselbe Funktion mit denselben
    Argumenten; `directWorkPeriodLookup` genügt hier (ein Zeitraum je Vorgang, keine
    Tagesschleife über ein Jahr).

    Bei :1195 den Kontext einmal vor der Tagesschleife erzeugen und durchreichen. Den
    bestehenden Wochenend- und Feiertagsvorfilter dieser Funktion NICHT anfassen — er ist
    Ist-Verhalten.

    Kein neues `any`. Keine zweite Rechenregel.
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit 2>&1 | grep "services/absenceService.ts" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit 2>&1 | grep "services/absenceService.ts"` liefert keine Zeile.
    - `grep -n "SELECT \* FROM users WHERE id = ?" server/src/services/absenceService.ts`
      zeigt keine Zeile mehr, die anschließend in eine Stundenberechnung fließt (oder die
      SUMMARY begründet, warum sie stehen bleiben musste).
    - Die Aufrufe bei :791 und :863 sind wörtlich identisch aufgebaut (gleiche Funktion,
      gleiche Argumentreihenfolge, gleicher Nutzerobjekt-Ursprung).
    - `grep -c "createWorkPeriodContext" server/src/services/absenceService.ts` ergibt 2
      (Anreicherungs-`map` und `calculateAbsenceCredits`).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: exportService — DATEV mit dem Maßstab des Eintragstags</name>
  <files>server/src/services/exportService.ts</files>
  <read_first>
    - `server/src/services/exportService.ts:60-110` — `generateDATEVExport`, besonders wie
      `fullUser` entsteht und wie über die Zeiteinträge geschleift wird
  </read_first>
  <behavior>
    - Ein Export über einen Zeitraum mit Stichtag weist für Einträge vor dem Stichtag die
      alten und danach die neuen Sollstunden aus
    - Ohne Modellwechsel ist die erzeugte Datei unverändert
  </behavior>
  <action>
    Kontext einmal je Export mit `createWorkPeriodContext()` erzeugen — der Export schleift
    über viele Zeiteinträge, teils mehrerer Nutzer. Den Kontext an `getDailyTargetHours`
    durchreichen. Prüfen, ob `fullUser` ein vollständiges `UserPublic` mit `id` und
    `hireDate` ist; falls nicht, ergänzen.
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit 2>&1 | grep "services/exportService.ts" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit 2>&1 | grep "services/exportService.ts"` liefert keine Zeile.
    - `grep -c "createWorkPeriodContext" server/src/services/exportService.ts` ergibt 1, und
      der Aufruf steht außerhalb der Schleife über die Zeiteinträge.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Nachweistest für eine Abwesenheit über den Stichtag</name>
  <files>server/src/services/absencePeriodAwareness.test.ts, .planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-CHECKLISTE.md</files>
  <read_first>
    - `server/src/services/workPeriodService.test.ts` — Muster für markierte Testnutzer und
      Aufräumnachweis
  </read_first>
  <behavior>
    Alle Fälle aus `<behavior>` von Task 1, gegen die Datenbank gefahren (Testnutzer mit zwei
    echten Perioden über `insertTestWorkPeriod`), zusätzlich:
    - Genehmigungsvorbehalt und Abbuchung eines `overtime_comp`-Antrags über den Stichtag
      nennen denselben Stundenwert
    - Ein Antrag, der vollständig vor dem Stichtag liegt, liefert unverändert die alten Werte
  </behavior>
  <action>
    Testdatei anlegen. Testnutzer mit Präfix `t1107-` und zwei Perioden mit Stichtag mitten
    im Monat (15.07.2026). Am Dateiende der Aufräumnachweis.

    Danach die Zeilen für `absenceService.ts` und `exportService.ts` in
    `11-AUFRUFER-CHECKLISTE.md` abhaken; die vier `countWorkingDaysForUser`-Zeilen bleiben
    mit ihrer Disposition stehen und werden NICHT abgehakt.
  </action>
  <verify>
    <automated>cd server && npx vitest run src/services/absencePeriodAwareness.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Alle Fälle aus `<behavior>` sind vorhanden und grün.
    - Der Gleichheitstest zwischen Genehmigungsvorbehalt und Abbuchung ist ein eigener Test,
      keine Nebenzusicherung.
    - Kein Testnutzer mit Präfix `t1107-` bleibt zurück.
    - Die Checklistenzeilen der beiden Dateien sind abgehakt.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `POST/PUT /api/absences` → absenceService | Von außen ausgelöster Vorgang mit Abbuchung vom Überstundenkonto |
| Export → Datei für die Lohnbuchhaltung | Ausgabe verlässt das System |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-23 | Tampering | Vorbehalt und Abbuchung bei `overtime_comp` rechnen verschieden | mitigate | Beide Stellen benutzen dieselbe Funktion mit gleichem Nutzerobjekt; eigener Gleichheitstest |
| T-11-24 | Information Disclosure | Rohe `SELECT * FROM users`-Zeile mit Passwort-Hash im Speicher | mitigate | Umstellung auf `getUserById()`, das den Hash nicht liefert |
| T-11-25 | Repudiation | DATEV-Export mit falschen Sollstunden verlässt das Haus | mitigate | Periodenauflösung je Eintragstag; Nullwirkungs-Nachweis in Plan 11-10 |
| T-11-26 | Denial of Service | Periodenzugriff je Antrag in einer Liste | mitigate | Ein Kontext je `map`/Export, grep-belegt |
| T-11-SC | Tampering | Paketinstallation | mitigate | Dieser Plan installiert kein Paket |
</threat_model>

<verification>
- `npx tsc --noEmit 2>&1 | grep -E "absenceService.ts|exportService.ts"` liefert keine Zeile.
- `npx vitest run src/services/absencePeriodAwareness.test.ts` grün.
- Kein neuer roter Test gegenüber `11-AUSGANGSZUSTAND.md`.
</verification>

<success_criteria>
Abwesenheitsstunden, Überstundenausgleich und DATEV-Export folgen dem Maßstab des jeweiligen
Tages. Vorbehalt und Abbuchung sind nachweislich derselbe Wert.
</success_criteria>

<output>
Create `.planning/phases/11-datumsabh-ngige-berechnung/11-07-SUMMARY.md` when done
</output>
