---
phase: 11-datumsabh-ngige-berechnung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-datumsabh-ngige-berechnung/11-AUSGANGSZUSTAND.md
  - .planning/phases/11-datumsabh-ngige-berechnung/11-SNAPSHOT-VORHER.json
  - .planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-CHECKLISTE.md
autonomous: true
requirements: [REQ-24]

must_haves:
  truths:
    - "Der Saldenstand vor dem Umbau ist maschinenlesbar festgehalten, auf einer Kopie, nicht in Produktion (D5)"
    - "Die Datenvoraussetzung für D4 ist gemessen: jeder Nutzer der Kopie hat eine lückenlose Periodenkette ab hireDate"
    - "Die Liste der nachzuziehenden Aufrufer existiert als Checkliste mit datei:zeile, nicht als Behauptung (REQ-23, Lehre aus Phase 9)"
    - "Der bekannte rote Ausgangszustand der Testsuite ist namentlich festgehalten, damit später niemand ihn für eine neue Regression hält"
  artifacts:
    - path: ".planning/phases/11-datumsabh-ngige-berechnung/11-SNAPSHOT-VORHER.json"
      provides: "Messpunkt vor dem Eingriff ins Rechenwerk"
    - path: ".planning/phases/11-datumsabh-ngige-berechnung/11-AUSGANGSZUSTAND.md"
      provides: "Protokoll mit wörtlichen Befehlen und Ausgaben"
    - path: ".planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-CHECKLISTE.md"
      provides: "Abhakliste aller Fundstellen für die Pläne 11-04 bis 11-09"
  key_links:
    - from: "11-SNAPSHOT-VORHER.json"
      to: "server/src/scripts/snapshotBalances.ts"
      via: "npm run snapshot:balances gegen die Arbeitskopie"
      pattern: "snapshot:balances"
---

<objective>
Messen, bevor gerechnet wird. Dieser Plan ändert keine Zeile Produktivcode. Er stellt drei
Dinge her, ohne die der Rest der Phase nicht belegbar ist: eine Arbeitskopie der
Produktionsdaten mit gemessener Datenvoraussetzung für D4, den Salden-Snapshot vor dem
Umbau, und die Aufrufer-Checkliste.

Purpose: Phase 9 hat gezeigt, was passiert, wenn Vollständigkeit behauptet statt belegt wird
(Plan 09-04 begründete mit „keine Überschneidung", die vierte Kopie der Regel kam erst im
Review ans Licht). Der Snapshot muss vor der Signaturänderung entstehen — danach ist der
Vorher-Zustand nicht mehr herstellbar.
Output: Arbeitskopie, `11-SNAPSHOT-VORHER.json`, `11-AUSGANGSZUSTAND.md`, `11-AUFRUFER-CHECKLISTE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-datumsabh-ngige-berechnung/11-CONTEXT.md
@.planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md
@.claude/CLAUDE.md

<interfaces>
Aus Plan 10-05 (Voraussetzung, siehe Task 2):
  server/src/scripts/snapshotBalances.ts
  npm-Skript: "snapshot:balances": "tsx src/scripts/snapshotBalances.ts"
  Aufruf: npm run snapshot:balances -- --all --asOf=<YYYY-MM-DD> --json=<pfad>
  `--all` liest ungefiltert `SELECT id FROM users` — einschließlich soft-gelöschter Nutzer.
  Nicht filtern.

Aus `server/src/services/workPeriodService.ts` (Plan 10-04):
  export function getWorkPeriods(userId: number): UserWorkPeriod[]
  export function resolveWorkPeriodAt(userId: number, date: string): UserWorkPeriod | null
  export function checkPeriodChain(userId: number): { ok: boolean; findings: string[] }

Aus `server/src/scripts/productionGuard.ts` (Plan 10-02):
  export function assertNotProduction(pfad?: string): void   // Exit 2 bei Auslösung
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Arbeitskopie ziehen und die Datenvoraussetzung für D4 messen</name>
  <files>.planning/phases/11-datumsabh-ngige-berechnung/11-AUSGANGSZUSTAND.md</files>
  <read_first>
    - `.claude/CLAUDE.md`, Abschnitt „Database Rules" (DATABASE_PATH explizit, kein Symlink)
    - `server/src/database/migrationRunner.ts` — Name der Buchführungstabelle für angewandte
      Migrationen (nicht raten, nachlesen)
    - `server/src/services/workPeriodService.ts` — `checkPeriodChain`, `getWorkPeriods`
  </read_first>
  <action>
    Aktive lokale Entwicklungsdatenbank ist `server/database/development.db` (NICHT
    `server/database.db`, das ist eine tote Altlast vom 02.04.2026 und in `.claude/CLAUDE.md`
    fälschlich dokumentiert). Der Dev-Server hält die Datei offen — deshalb keine
    Dateikopie, sondern `VACUUM INTO`.

    Arbeitskopie anlegen: `sqlite3`-CLI ist auf diesem Rechner nicht verfügbar (Lehre aus
    Plan 03-01), deshalb per `node -e` mit `better-sqlite3` und `VACUUM INTO
    './database/11-nullwirkung.db'`. Zielpfad `server/database/11-nullwirkung.db`. Falls die
    Datei bereits existiert, vorher löschen — `VACUUM INTO` überschreibt nicht.

    Danach ausschließlich gegen die Kopie messen (`DATABASE_PATH=./database/11-nullwirkung.db`)
    und jede Zahl mit dem wörtlichen Befehl und der wörtlichen Ausgabe in
    `11-AUSGANGSZUSTAND.md` protokollieren:

    (a) Angewandte Migrationen: 008 und 009 müssen verzeichnet sein. Tabellenname aus
        `migrationRunner.ts` übernehmen.
    (b) `SELECT COUNT(*) FROM users` (ungefiltert) und `SELECT COUNT(*) FROM user_work_periods`.
    (c) Perioden-Abdeckung: Für JEDEN `users.id` (ungefiltert, auch `deletedAt IS NOT NULL`)
        `checkPeriodChain(userId)` aufrufen und zusätzlich prüfen, dass
        `resolveWorkPeriodAt(userId, hireDate)` eine Periode liefert. Jede Auffälligkeit
        einzeln mit userId auflisten. Das ist die Datenvoraussetzung für D4 — ohne sie wird
        der harte Fehler aus D4 zum Betriebsausfall statt zum Datendefekt-Melder.
    (d) Drift-Messung: Anzahl Nutzer, deren `users.weeklyHours` bzw. `users.workSchedule`
        von den Werten ihrer offenen Periode (`validTo IS NULL`) abweichen. Erwartung nach
        Migration 009: null. Jede Abweichung mit userId nennen — sie würde die
        Nullwirkung (D5) verletzen.
    (e) Wochenend-Risiko: Anzahl Nutzer, deren `workSchedule` für `saturday` oder `sunday`
        einen Wert > 0 trägt. Diese Zahl entscheidet in Plan 11-04, ob die Umstellung von
        `calculateAbsenceHoursWithWorkSchedule` Zahlen bewegen kann (die Funktion
        überspringt Wochenenden bedingungslos, `getDailyTargetHours` nicht).
    (f) Letzter `time_entries.date` auf der Kopie — das ist der `--asOf`-Wert für Task 2.

    Kein Zugriff auf die Produktionsdatenbank, kein SSH, kein `sync-dev-db` in dieser Phase.
  </action>
  <verify>
    <automated>cd server && node -e "const D=require('better-sqlite3');const db=new D('./database/11-nullwirkung.db',{readonly:true});console.log(JSON.stringify({users:db.prepare('SELECT COUNT(*) c FROM users').get(),periods:db.prepare('SELECT COUNT(*) c FROM user_work_periods').get()}))"</automated>
  </verify>
  <acceptance_criteria>
    - `server/database/11-nullwirkung.db` existiert und liefert `PRAGMA integrity_check` = `ok`.
    - `11-AUSGANGSZUSTAND.md` enthält für (a) bis (f) je Befehl und Ausgabe wörtlich.
    - Die Anzahl der Nutzer ohne auflösbare Periode am eigenen `hireDate` ist genannt. Ist
      sie > 0, steht im Dokument eine Liste der betroffenen `userId` und der ausdrückliche
      Satz, dass Plan 11-04 (D4) für diese Nutzer einen harten Fehler erzeugen wird.
    - Die Drift-Zahl (d) und die Wochenend-Zahl (e) stehen als einzelne, mit userId belegte
      Zahlen im Dokument — nicht als „keine Auffälligkeiten".
    - Kein Befehl im Protokoll enthält `production.db` oder eine SSH-Verbindung.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Snapshot A und Baseline der Testsuite</name>
  <files>.planning/phases/11-datumsabh-ngige-berechnung/11-SNAPSHOT-VORHER.json, .planning/phases/11-datumsabh-ngige-berechnung/11-AUSGANGSZUSTAND.md</files>
  <read_first>
    - `.planning/phases/10-perioden-fundament/10-05-PLAN.md` — CLI-Vertrag von `snapshotBalances.ts`
    - `server/src/scripts/snapshotBalances.ts` (falls vorhanden)
  </read_first>
  <action>
    Vorbedingung prüfen: Existiert `server/src/scripts/snapshotBalances.ts` nicht oder fehlt
    das npm-Skript `snapshot:balances`, dann diesen Task ABBRECHEN und in der SUMMARY
    vermerken: „Plan 10-05 muss zuerst ausgeführt werden — das Snapshot-Werkzeug ist die in
    D5 benannte Voraussetzung." Kein Ersatzwerkzeug bauen, keine zweite Snapshot-Logik.

    Snapshot A ziehen, ausschließlich gegen die Arbeitskopie:
    `cd server && DATABASE_PATH=./database/11-nullwirkung.db npm run snapshot:balances --
    --all --asOf=<Wert aus Task 1f>
    --json=../.planning/phases/11-datumsabh-ngige-berechnung/11-SNAPSHOT-VORHER.json`

    `--all` nicht durch `--users` ersetzen und die soft-gelöschten Nutzer nicht herausfiltern
    (ausdrückliche Vorgabe aus Phase 10).

    Danach die Baseline der Werkzeuge festhalten, jeweils mit wörtlicher Ausgabe im
    Protokoll:
    - `cd server && npx tsc --noEmit` — muss vor dem Umbau fehlerfrei sein. Ist er es nicht,
      die Fehler wörtlich protokollieren; sie gehören dann nicht zu dieser Phase.
    - `cd server && npx vitest run` — Gesamtergebnis plus die Titel JEDES fehlschlagenden
      Tests. Erwartung aus Phase 9: drei vorbestehende rote Tests (2× in
      `unifiedOvertimeService.test.ts` zu Eintrittsdatum/Sollstunden, 1× in
      `vacationBackfillService.test.ts`). Weicht die tatsächliche Menge davon ab, gilt die
      gemessene Menge, nicht die Erwartung — Abweichung ausdrücklich vermerken.
    Diese Liste ist der vereinbarte Ausgangszustand: Diese Phase behebt die roten Tests
    nicht (out of scope), aber sie darf keinen weiteren erzeugen.
  </action>
  <verify>
    <automated>node -e "const s=require('./.planning/phases/11-datumsabh-ngige-berechnung/11-SNAPSHOT-VORHER.json');console.log('entries:',Array.isArray(s)?s.length:Object.keys(s).length)"</automated>
  </verify>
  <acceptance_criteria>
    - `11-SNAPSHOT-VORHER.json` existiert, ist gültiges JSON und enthält Einträge für alle in
      Task 1 (b) gezählten Nutzer — die Anzahl steht im Protokoll neben der Nutzeranzahl.
    - Der wörtliche Snapshot-Befehl steht im Protokoll und enthält
      `DATABASE_PATH=./database/11-nullwirkung.db` sowie `--all`.
    - Die Titel aller vorbestehend roten Tests stehen namentlich in `11-AUSGANGSZUSTAND.md`.
    - Der `tsc --noEmit`-Ausgangsstand ist protokolliert.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Aufrufer-Checkliste erzeugen</name>
  <files>.planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-CHECKLISTE.md</files>
  <read_first>
    - `.planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md` — die 23
      klassifizierten Fundstellen. Nutzen statt neu suchen; die Grep-Läufe unten dienen nur
      dem Abgleich mit dem heutigen Stand.
  </read_first>
  <action>
    Ein Dokument mit EINER Tabelle: je Zeile eine Fundstelle mit `datei:zeile`, Funktion,
    Produktivpfad (ja/nein mit Beleg aus dem Inventar), zuständiger Plan, Disposition,
    Haken-Spalte (leer). Die Tabelle ist die Abhakliste der Pläne 11-04 bis 11-09.

    Grundlage sind diese vier Läufe, jeweils wörtlich mit Trefferzahl im Dokument
    protokolliert:
    - `grep -rn "getDailyTargetHours(" --include=*.ts server/src`
    - `grep -rn "calculateAbsenceHoursWithWorkSchedule(" --include=*.ts server/src`
    - `grep -rn "calculateTargetHoursForPeriod(" --include=*.ts server/src`
    - `grep -rn "getDailyTargetHours\|calculateAbsenceHoursWithWorkSchedule" --include=*.ts --include=*.tsx desktop/src`

    Zuordnung der Pläne (aus der Wellenstruktur dieser Phase):
    - `server/src/utils/workingDays.ts` → 11-04
    - `unifiedOvertimeService.ts`, `overtimeTransactionRebuildService.ts` → 11-05
    - `overtimeService.ts`, `overtimeLiveCalculationService.ts` → 11-06
    - `absenceService.ts`, `exportService.ts` → 11-07
    - `server/src/scripts/` Validierungs- und Migrationswerkzeuge → 11-08
    - nutzeranlegende Skripte (`seedTest*`, `create*TestUser`, `server/scripts/create-admin.ts`) → 11-11

    Vier Gruppen bekommen eine ausdrückliche Disposition mit Begründung statt eines Hakens:
    1. `server/scripts/fix-overtime.ts` — ruft `getDailyTargetHours` NICHT direkt auf, seit
       Phase 9 delegiert es an `ensureOvertimeBalanceEntries()`. Wird dadurch automatisch
       periodenbewusst. Nicht im `tsc`-Umfang (`tsconfig.json` schließt nur `src/**/*` ein),
       läuft aber bei jedem Deployment und täglich per Cron — als „zieht mit" eintragen und
       in Plan 11-09 gegenprüfen (D7).
    2. `server/debug_*.ts`, `server/test_*.ts` im Serverwurzelverzeichnis — außerhalb von
       `src/`, damit außerhalb des `tsc`-Umfangs, laut Inventar (Zeile 164) kein
       Produktivpfad, `debug_target_calculation.ts` führt sogar eine eigene lokale Kopie der
       Funktion. Disposition: nicht nachgezogen, Begründung im Dokument, keine stille
       Auslassung.
    3. Desktop (`desktop/src/utils/timeUtils.ts`, `WorkScheduleDisplay.tsx`,
       `WorkScheduleEditor.tsx`, `AbsenceRequestForm.tsx`) — rechnet ohne Datenbankzugriff
       aus den über die API gelieferten Stammdaten. Disposition in Plan 11-09 endgültig
       festhalten; hier als offene Zeile mit dem Prüfauftrag eintragen: „zeigt die Anzeige
       einen vergangenen Zeitraum oder nur den heutigen/künftigen Stand?"
    4. `countWorkingDaysForUser` (`absenceService.ts:464,472,697,700`) — zählt Urlaubstage,
       keine Sollstunden. Disposition: in Phase 11 unverändert, mit dem ausdrücklichen
       Vermerk, dass ein Urlaubsantrag über einen Stichtag hinweg in Phase 12/14 zu bewerten
       ist.

    Keine Fundstelle darf ohne Zeile bleiben. Weicht der heutige Stand vom Inventar der
    Phase 9 ab (verschobene Zeilennummern, neue Fundstellen), gilt der heutige Stand und die
    Abweichung wird als solche vermerkt.
  </action>
  <verify>
    <automated>grep -c "^| " .planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-CHECKLISTE.md</automated>
  </verify>
  <acceptance_criteria>
    - Jede Zeile der vier Grep-Läufe (ohne Kommentar- und Doku-Treffer) hat genau eine Zeile
      in der Tabelle; die Zuordnung Treffer → Tabellenzeile ist im Dokument nachvollziehbar.
    - Jede Zeile trägt entweder einen zuständigen Plan oder eine begründete Disposition.
      Keine Zeile ohne beides.
    - Die vier Sondergruppen aus dem Action-Abschnitt stehen mit ihrer Begründung im
      Dokument.
    - Die Trefferzahlen der vier Grep-Läufe stehen wörtlich im Dokument.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Produktionsdaten → lokale Arbeitskopie | Die Kopie enthält echte Personendaten und Salden |
| Arbeitskopie → Git-Repository | Der Snapshot wird als JSON eingecheckt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | Produktionsdatenbank | mitigate | Kein Produktionszugriff in dieser Phase; alle Befehle tragen `DATABASE_PATH=./database/11-nullwirkung.db`, Acceptance Criteria prüfen auf das Fehlen von `production.db` |
| T-11-02 | Information Disclosure | `11-SNAPSHOT-VORHER.json` im Repository | accept | Enthält userId, Monat und Stundenwerte, keine Namen, keine Zugangsdaten; das Repository ist privat; gleiche Praxis wie Plan 10-05 |
| T-11-03 | Denial of Service | laufender Dev-Server hält `development.db` offen | mitigate | `VACUUM INTO` statt Dateikopie; kein Schreibzugriff auf `development.db` in diesem Plan |
| T-11-SC | Tampering | Paketinstallation | mitigate | Dieser Plan installiert kein Paket. Kommt es doch dazu, gilt der Paket-Legitimitäts-Gate der Phase — Ausführung anhalten |
</threat_model>

<verification>
- `server/database/11-nullwirkung.db` existiert, `integrity_check` = `ok`.
- `11-SNAPSHOT-VORHER.json` ist gültiges JSON mit einem Eintrag je Nutzer.
- `11-AUSGANGSZUSTAND.md` und `11-AUFRUFER-CHECKLISTE.md` existieren und sind vollständig.
- `git status` zeigt keine Änderung an Produktivcode durch diesen Plan.
</verification>

<success_criteria>
Der Zustand vor dem Eingriff ist gemessen und belegt: Salden, Datenvoraussetzung für D4,
roter Testausgangszustand, vollständige Aufrufer-Liste. Kein Produktivcode berührt.
</success_criteria>

<output>
Create `.planning/phases/11-datumsabh-ngige-berechnung/11-01-SUMMARY.md` when done
</output>
