---
phase: 11-datumsabh-ngige-berechnung
plan: 04
type: execute
wave: 2
depends_on: ["11-02"]
files_modified:
  - server/src/utils/workingDays.ts
  - server/src/utils/workingDays.test.ts
  - server/src/test-support/workPeriodFixtures.ts
autonomous: true
requirements: [REQ-23]

must_haves:
  truths:
    - "getDailyTargetHours liefert die Sollstunden der Periode, die am übergebenen Datum galt — nicht die des heutigen Stammdatensatzes (REQ-23)"
    - "Die Reihenfolge Feiertag vor workSchedule vor weeklyHours-Fallback vor Wochenende ist unverändert"
    - "Eine fehlende Periode bricht mit klarem Fehler ab; ein Datum vor hireDate liefert weiterhin 0 (D4)"
    - "Jede Aufrufstelle im Projekt wird vom Compiler benannt — die Vollständigkeit der Umstellung ist belegt, nicht behauptet (D3)"
  artifacts:
    - path: "server/src/utils/workingDays.ts"
      provides: "periodenbewusste Sollstunden-Auflösung mit Pflichtparameter"
      contains: "MissingWorkPeriodError"
    - path: "server/src/test-support/workPeriodFixtures.ts"
      provides: "Testhilfen für die Pläne 11-05 bis 11-09: Stub-Kontext ohne Datenbank und echte Testperiode"
      exports: ["stubWorkPeriodContext", "insertTestWorkPeriod"]
  key_links:
    - from: "server/src/utils/workingDays.ts"
      to: "server/src/services/workPeriodContext.ts"
      via: "Pflichtparameter periods: WorkPeriodContext"
      pattern: "WorkPeriodContext"
---

<objective>
Der Eingriff selbst. `getDailyTargetHours` fragt ab hier die Periode, die am übergebenen Tag
galt, und bekommt den Kontext ausdrücklich übergeben statt ihn sich zu holen (D3).

Purpose: Der Compiler-Fehler an jeder Aufrufstelle ist die Beweisführung für REQ-23. Ein
versteckter Datenbankzugriff in einer Utility-Funktion, die in Tagesschleifen läuft, ist
genau die Nebenwirkung, die später niemand findet.
Output: Neue Signatur für drei Funktionen in `workingDays.ts`, Testhilfen, nachgezogene
Tests dieser Datei.

**Erwarteter Zwischenzustand:** Nach diesem Plan ist `npx tsc --noEmit` ROT. Das ist das
Ziel, nicht ein Versehen. Die Fehlerliste ist die Arbeitsliste der Pläne 11-05 bis 11-08 und
wird in diesem Plan gegen `11-AUFRUFER-CHECKLISTE.md` abgeglichen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-datumsabh-ngige-berechnung/11-CONTEXT.md
@.planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-CHECKLISTE.md
@server/src/utils/workingDays.ts
@server/src/services/workPeriodContext.ts
@.claude/CLAUDE.md

<interfaces>
Aus Plan 11-02, `server/src/services/workPeriodContext.ts`:
  export interface WorkPeriodContext { resolve(userId: number, date: string): UserWorkPeriod | null }
  export function createWorkPeriodContext(): WorkPeriodContext
  export const directWorkPeriodLookup: WorkPeriodContext

Ist-Stand `server/src/utils/workingDays.ts` (die drei betroffenen Signaturen):
  export function getDailyTargetHours(user: UserPublic, date: Date | string): number            // :63
  export function calculateAbsenceHoursWithWorkSchedule(
    startDate: string, endDate: string,
    workSchedule: Record<DayName, number> | null, weeklyHours: number): number                  // :117
  export function calculateTargetHoursForPeriod(
    user: UserPublic, fromDate: string | Date, toDate: string | Date): number                   // :316

Ziel-Signaturen:
  export function getDailyTargetHours(
    user: UserPublic, date: Date | string, periods: WorkPeriodContext): number
  export function calculateAbsenceHoursWithWorkSchedule(
    user: UserPublic, startDate: string, endDate: string, periods: WorkPeriodContext): number
  export function calculateTargetHoursForPeriod(
    user: UserPublic, fromDate: string | Date, toDate: string | Date,
    periods: WorkPeriodContext): number
  export class MissingWorkPeriodError extends Error

Reihenfolge, die sich NICHT verschieben darf (`.claude/CLAUDE.md`):
  Feiertag überschreibt alles → 0h; sonst workSchedule; sonst weeklyHours/5; Wochenende im
  Fallback → 0h; weeklyHours = 0 (Aushilfe) → 0h.

Verbot aus `.claude/CLAUDE.md`: kein `toISOString().split('T')[0]`, stattdessen
`formatDate(date, 'yyyy-MM-dd')` (in dieser Datei als `formatDateBerlin` importiert).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getDailyTargetHours löst über die Periode auf</name>
  <files>server/src/utils/workingDays.ts, server/src/test-support/workPeriodFixtures.ts</files>
  <read_first>
    - `server/src/utils/workingDays.ts:40-95` — `getDailyTargetHours` vollständig, jede Zeile
    - `server/src/services/workPeriodContext.ts` (aus 11-02)
    - `.planning/phases/11-datumsabh-ngige-berechnung/11-AUSGANGSZUSTAND.md`, Punkt (c) —
      gibt es auf der Arbeitskopie Nutzer ohne auflösbare Periode?
  </read_first>
  <behavior>
    Nutzer mit zwei Perioden — bis 15.07.2026 vierzig Wochenstunden ohne Wochenplan, ab
    15.07.2026 zwanzig Wochenstunden ohne Wochenplan (halboffen, der 15. gehört zur zweiten):
    - Mittwoch 08.07.2026 → 8
    - Dienstag 14.07.2026 → 8
    - Mittwoch 15.07.2026 → 4
    - Samstag 11.07.2026 → 0 (Fallback-Wochenende)
    Nutzer mit Wochenplan in der zweiten Periode (Mo 8, Di 0, Mi 6, Do 8, Fr 8, Sa 0, So 0):
    - Dienstag nach dem Stichtag → 0, Mittwoch nach dem Stichtag → 6
    Feiertag:
    - Ein Datum, das in `holidays` steht, → 0, unabhängig von der Periode (Feiertag zuerst)
    Aushilfe:
    - Periode mit `weeklyHours = 0`, kein Wochenplan → jeder Tag 0
    D4:
    - Kein Periodentreffer und `dateStr < user.hireDate` → 0, kein Fehler
    - Kein Periodentreffer und `dateStr >= user.hireDate` → `MissingWorkPeriodError`, die
      Meldung nennt `userId` und Datum
    - Kein Periodentreffer und `user.hireDate` fehlt oder ist unbrauchbar →
      `MissingWorkPeriodError` (kein stiller Rückfall auf `users.weeklyHours`)
    Nullwirkung:
    - Stimmen die Periodenwerte mit den Stammdaten überein, liefert die Funktion für jeden
      Testfall der bisherigen Suite denselben Wert wie vorher
  </behavior>
  <action>
    Zuerst `server/src/test-support/workPeriodFixtures.ts` anlegen, weil die Tests dieses und
    der folgenden vier Pläne daran hängen:
    - `stubWorkPeriodContext(entries: Array<{ userId: number; validFrom: string; validTo?:
      string | null; weeklyHours: number; workSchedule?: WorkSchedule | null }>):
      WorkPeriodContext` — baut `UserWorkPeriod`-Objekte im Speicher und löst über
      `resolveWorkPeriodIn` auf. KEIN Datenbankzugriff, keine eigene Auflösungslogik.
    - `insertTestWorkPeriod(userId, { validFrom, validTo, weeklyHours, workSchedule, note })`
      — legt eine echte Periode über `createWorkPeriod` an, `note` trägt einen Testmarker.
      Für Tests, die einen ganzen Berechnungsweg gegen die Datenbank fahren.
    Kein `any` in dieser Datei.

    Dann `getDailyTargetHours` umbauen. Neuer dritter Parameter `periods: WorkPeriodContext`,
    ohne Vorgabewert (D3 — genau deshalb bricht der Compiler an jeder Aufrufstelle).
    Ablauf in genau dieser Reihenfolge:
    1. `dateStr` wie bisher bilden (`formatDateBerlin` bei Date-Objekten) — Zeile unverändert.
    2. Feiertagsabfrage wie bisher, unverändert, VOR allem anderen. Treffer → 0.
    3. `periods.resolve(user.id, dateStr)`.
    4. Kein Treffer: Ist `user.hireDate` eine Zeichenkette im Muster `JJJJ-MM-TT` und
       `dateStr < user.hireDate`, dann 0 zurückgeben (D4-Ausnahme, wie bisher). Sonst
       `logger.error` mit `userId` und `dateStr` und `MissingWorkPeriodError` werfen. Kein
       Rückfall auf `user.weeklyHours` — die Zeilen, die heute `user.weeklyHours` und
       `user.workSchedule` lesen, verschwinden aus dieser Funktion.
    5. Treffer mit `workSchedule`: `getDayName(date)` wie bisher, Wert aus
       `period.workSchedule`.
    6. `period.weeklyHours === 0` → 0.
    7. Wochenendprüfung: die bestehenden Zeilen unverändert übernehmen (`const d = typeof
       date === 'string' ? new Date(date) : date; const dayOfWeek = d.getDay();`). Diese
       Zeilen NICHT umbauen — sie sind Ist-Verhalten, jede Änderung daran wäre eine
       Zahlenbewegung außerhalb dieser Phase.
    8. `Math.round((period.weeklyHours / 5) * 100) / 100`.

    `MissingWorkPeriodError` in derselben Datei exportieren. Die Meldung nennt `userId`,
    Datum und den Hinweis, dass nach Migration 009 jeder Nutzer eine lückenlose Kette ab
    `hireDate` hat und ein Fehlen deshalb ein Datendefekt ist. Keine Namen, keine E-Mail —
    die Meldung kann in einem Log landen.

    Den JSDoc-Block der Funktion vollständig fortschreiben: was der dritte Parameter ist,
    warum er Pflicht ist (D3), was bei fehlender Periode passiert (D4).
  </action>
  <verify>
    <automated>cd server && npx vitest run src/utils/workingDays.test.ts -t "getDailyTargetHours" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Alle Fälle aus `<behavior>` sind als Tests vorhanden und grün.
    - `grep -n "user.weeklyHours\|user.workSchedule" server/src/utils/workingDays.ts` zeigt
      innerhalb von `getDailyTargetHours` keine Zeile mehr.
    - Die Feiertagsabfrage steht weiterhin als erste Prüfung der Funktion.
    - `grep -c "toISOString" server/src/utils/workingDays.ts` ergibt 0.
    - `server/src/test-support/workPeriodFixtures.ts` enthält keine eigene
      Intervall-Auflösung — `grep -n "validTo" ` zeigt nur Zuweisungen, keinen Vergleich.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: calculateAbsenceHoursWithWorkSchedule und calculateTargetHoursForPeriod nachziehen</name>
  <files>server/src/utils/workingDays.ts</files>
  <read_first>
    - `server/src/utils/workingDays.ts:117-165` — `calculateAbsenceHoursWithWorkSchedule`
      vollständig; sie ist eine ZWEITE, eigenständige Kopie der Sollstunden-Regel und liest
      `workSchedule`/`weeklyHours` als Parameter
    - `server/src/utils/workingDays.ts:304-345` — `calculateTargetHoursForPeriod`
    - `.planning/phases/11-datumsabh-ngige-berechnung/11-AUSGANGSZUSTAND.md`, Punkt (e) —
      Anzahl der Nutzer mit Wochenendstunden im Wochenplan
  </read_first>
  <behavior>
    `calculateAbsenceHoursWithWorkSchedule(user, '2026-07-13', '2026-07-17', ctx)` bei
    Stichtag 15.07.2026 (davor 40h ohne Wochenplan, danach 20h ohne Wochenplan):
    - Mo 13. + Di 14. mit je 8, Mi 15. bis Fr 17. mit je 4 → 28
    Feiertag im Zeitraum → dieser Tag zählt 0
    Wochenende im Zeitraum → wie bisher übersprungen
    `calculateTargetHoursForPeriod(user, '2026-07-13', '2026-07-19', ctx)` → derselbe Wert
    wie die Summe der Einzeltage über `getDailyTargetHours`
    Ohne Modellwechsel liefern beide Funktionen exakt die bisherigen Werte
  </behavior>
  <action>
    `calculateAbsenceHoursWithWorkSchedule` bekommt die neue Signatur `(user: UserPublic,
    startDate: string, endDate: string, periods: WorkPeriodContext)`. Innerhalb der
    Tagesschleife wird für jeden Tag die Periode aufgelöst und deren `workSchedule` bzw.
    `weeklyHours` benutzt, wo bisher die Parameter standen.

    Der bestehende Ablauf der Funktion bleibt sonst Zeile für Zeile erhalten — insbesondere
    das bedingungslose Überspringen von Samstag und Sonntag und die eigene Feiertagsabfrage.
    Diese Funktion wird in dieser Phase AUSDRÜCKLICH NICHT mit `getDailyTargetHours`
    zusammengeführt, obwohl sie dieselbe Regel in zweiter Ausfertigung enthält. Grund, der
    als Kommentar über der Funktion stehen muss: `getDailyTargetHours` liefert für einen
    Nutzer mit Wochenendstunden im Wochenplan an Samstag/Sonntag Stunden, diese Funktion
    liefert 0. Eine Zusammenführung würde für solche Nutzer Zahlen bewegen und damit die
    Nullwirkung (D5) verletzen. Der Kommentar nennt die in `11-AUSGANGSZUSTAND.md` Punkt (e)
    gemessene Zahl betroffener Nutzer und verweist die Zusammenführung an Phase 14
    (Testabdeckung).

    Ist diese Zahl größer als null, wird das zusätzlich in der SUMMARY dieses Plans als
    offener Punkt vermerkt — dann weichen die beiden Wege für diese Nutzer schon heute
    voneinander ab, unabhängig von dieser Phase.

    `calculateTargetHoursForPeriod` bekommt `periods` als vierten Pflichtparameter und reicht
    ihn an `getDailyTargetHours` durch. Der Vorfilter `if (isWeekend && !user.workSchedule)
    continue;` entfällt: `getDailyTargetHours` entscheidet über Wochenende, Feiertag und
    Wochenplan selbst und liefert für diese Tage 0 — dasselbe Ergebnis, ein Maßstab. Das ist
    dasselbe Muster, das Phase 9 unter REQ-17 an mehreren Stellen bereits angewandt hat
    (`// REQ-17: Kein eigener Wochenend-/Feiertagsfilter mehr`).
  </action>
  <verify>
    <automated>cd server && npx vitest run src/utils/workingDays.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Alle Fälle aus `<behavior>` sind als Tests vorhanden und grün.
    - `grep -n "isWeekend && !user.workSchedule" server/src/utils/workingDays.ts` liefert
      keine Zeile mehr.
    - Der Begründungskommentar über `calculateAbsenceHoursWithWorkSchedule` nennt die
      gemessene Zahl aus `11-AUSGANGSZUSTAND.md` Punkt (e).
    - Beide Funktionen tragen `periods` als Pflichtparameter ohne Vorgabewert.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Tests dieser Datei nachziehen und die Compiler-Fehlerliste gegen die Checkliste abgleichen</name>
  <files>server/src/utils/workingDays.test.ts, .planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-CHECKLISTE.md</files>
  <read_first>
    - `server/src/utils/workingDays.test.ts` — alle Aufrufe der drei geänderten Funktionen
    - `.planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-CHECKLISTE.md` aus Plan 11-01
  </read_first>
  <action>
    Jeden Aufruf der drei Funktionen in `workingDays.test.ts` auf die neue Signatur bringen.
    Wo ein Test heute ein `user`-Objekt mit `weeklyHours`/`workSchedule` baut, wird daraus ein
    `stubWorkPeriodContext`-Eintrag mit denselben Werten und `validFrom` weit vor dem
    Testdatum; das `user`-Objekt behält `id` und `hireDate`. Die erwarteten Werte der
    bestehenden Tests dürfen sich NICHT ändern — jeder geänderte Erwartungswert ist ein
    Befund und gehört in die SUMMARY, nicht stillschweigend in den Test.

    Danach `cd server && npx tsc --noEmit > /tmp/11-04-tsc.txt 2>&1` laufen lassen und die
    Fehlerliste gegen `11-AUFRUFER-CHECKLISTE.md` abgleichen:
    - Jede Datei in der Fehlerliste MUSS eine Zeile in der Checkliste haben.
    - Jede Checklisten-Zeile mit zugeordnetem Plan 11-05 bis 11-08 MUSS in der Fehlerliste
      auftauchen. Fehlt eine, ist die Umstellung dort unvollständig oder die Fundstelle ist
      toter Code — beides wird in der Checkliste vermerkt, nichts wird angenommen.
    Das Ergebnis dieses Abgleichs als Abschnitt „Compiler-Fehlerliste nach 11-04" in die
    Checkliste schreiben, mit der wörtlichen Fehlerzahl und der Dateiliste.

    `server/src/utils/workingDays.test.ts` selbst muss am Ende dieses Plans grün sein.
  </action>
  <verify>
    <automated>cd server && npx vitest run src/utils/workingDays.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/utils/workingDays.test.ts` ist grün; die Anzahl der Tests ist
      nicht kleiner als vor dem Umbau.
    - Kein Erwartungswert eines bestehenden Tests wurde verändert, oder jede Änderung steht
      einzeln begründet in der SUMMARY.
    - Der Abschnitt „Compiler-Fehlerliste nach 11-04" steht in
      `11-AUFRUFER-CHECKLISTE.md` und enthält die wörtliche Fehlerzahl.
    - Für jede Datei aus der Fehlerliste existiert eine Checklistenzeile und umgekehrt;
      Abweichungen sind einzeln vermerkt.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Berechnung → Periodendaten | Die aufgelöste Periode bestimmt lohnrelevante Sollstunden |
| Service-Fehler → API-Antwort | `MissingWorkPeriodError` kann bis in eine HTTP-Antwort durchschlagen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Denial of Service | `MissingWorkPeriodError` in `getDailyTargetHours` | mitigate | D4 ist gewollt, aber die Kette muss lückenlos sein: Plan 11-03 sorgt für Neuanlagen, Plan 11-01 misst den Bestand, Plan 11-09 prüft alle Nutzer der Arbeitskopie durch |
| T-11-12 | Information Disclosure | Fehlermeldung in der API-Antwort | mitigate | Die Meldung enthält ausschließlich `userId` und Datum, keine Namen, keine E-Mail; sie ist für ein Log gebaut |
| T-11-13 | Tampering | Stiller Rückfall auf `users.weeklyHours` | mitigate | Die Zeilen, die `user.weeklyHours`/`user.workSchedule` lesen, werden aus der Funktion entfernt; grep-Kriterium in den Acceptance Criteria |
| T-11-14 | Repudiation | Unbemerkt falsche Sollstunden nach dem Umbau | mitigate | Nullwirkungs-Nachweis in Plan 11-10 gegen den Snapshot aus 11-01 |
| T-11-SC | Tampering | Paketinstallation | mitigate | Dieser Plan installiert kein Paket |
</threat_model>

<verification>
- `npx vitest run src/utils/workingDays.test.ts` grün.
- `npx tsc --noEmit` ist ROT und die Fehlerliste deckt sich mit der Aufrufer-Checkliste.
- Kein neuer roter Test in `src/utils/` gegenüber der Baseline aus 11-01.
</verification>

<success_criteria>
Die Sollstunden eines Tages kommen aus der Periode dieses Tages. Der Kontext wird
übergeben, nicht geholt. Eine fehlende Periode fällt auf. Die Liste der noch offenen
Aufrufstellen ist maschinell erzeugt und abgeglichen.
</success_criteria>

<output>
Create `.planning/phases/11-datumsabh-ngige-berechnung/11-04-SUMMARY.md` when done
</output>
