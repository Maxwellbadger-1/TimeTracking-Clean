---
phase: 11-datumsabh-ngige-berechnung
plan: 09
type: execute
wave: 4
depends_on: ["11-05", "11-06", "11-07", "11-08", "11-11"]
files_modified:
  - .planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-CHECKLISTE.md
  - .planning/phases/11-datumsabh-ngige-berechnung/11-DESKTOP-DISPOSITION.md
  - .planning/phases/11-datumsabh-ngige-berechnung/11-IDEMPOTENZ-NACHWEIS.md
  - server/src/scripts/seedModelChangeUser.ts
  - server/package.json
  - .planning/ROADMAP.md
autonomous: true
requirements: [REQ-23, REQ-24]

must_haves:
  truths:
    - "Jede Fundstelle der Sollstunden-Regel im Projekt ist entweder nachgezogen oder trägt eine begründete Disposition — belegt durch eine erschöpfende Suche, nicht durch das Abhaken einer geerbten Liste (REQ-23, D7)"
    - "Die Desktop-Anzeigen haben eine schriftliche, begründete Disposition mit Zuordnung zu einer Phase — kein stillschweigendes Offenlassen"
    - "Ein zweifach ausgeführter Rebuild über einen Zeitraum mit echtem Modellwechsel liefert auf echten Daten denselben Datenbestand (REQ-24)"
    - "npx tsc --noEmit ist wieder fehlerfrei und die Testsuite hat gegenüber 11-AUSGANGSZUSTAND.md keinen neuen roten Test"
  artifacts:
    - path: ".planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-CHECKLISTE.md"
      provides: "vollständig abgehakte Aufrufer-Liste mit zusammengeführten Teilbelegen der Welle 3"
      contains: "Erschöpfende Nachsuche"
    - path: ".planning/phases/11-datumsabh-ngige-berechnung/11-DESKTOP-DISPOSITION.md"
      provides: "begründete Entscheidung über desktop/src, mit Wirksamkeitsfenster und Phasenzuordnung"
    - path: ".planning/phases/11-datumsabh-ngige-berechnung/11-IDEMPOTENZ-NACHWEIS.md"
      provides: "REQ-24 auf echten Daten: zwei Rebuild-Läufe, maschinell verglichen"
    - path: "server/src/scripts/seedModelChangeUser.ts"
      provides: "wiederverwendbare Fixture: künstlicher Nutzer mit echtem Modellwechsel, Stichtag mitten im Monat (D6); wird von Plan 11-10 erneut benutzt"
      exports: ["seedModelChangeUser"]
  key_links:
    - from: "server/src/scripts/seedModelChangeUser.ts"
      to: "server/src/services/workPeriodService.ts"
      via: "createWorkPeriod/closeWorkPeriod nach assertNotProduction"
      pattern: "createWorkPeriod"
    - from: ".planning/phases/11-datumsabh-ngige-berechnung/11-IDEMPOTENZ-NACHWEIS.md"
      to: "server/src/services/overtimeTransactionRebuildService.ts"
      via: "zwei Läufe von rebuildOvertimeTransactionsForMonth, kanonisch sortierter Vergleich"
      pattern: "rebuildOvertimeTransactionsForMonth"
---

<objective>
Der phasenweite Vollständigkeits- und Idempotenz-Nachweis. Die Pläne 11-04 bis 11-08 und
11-11 haben jeweils ihren Ausschnitt umgebaut und belegt. Dieser Plan beantwortet die drei
Fragen, die keiner von ihnen beantworten konnte, weil sie nur phasenweit beantwortbar sind:
Ist wirklich jede Fundstelle erfasst? Was geschieht mit dem Desktop? Ist der Rebuild auf
echten Daten wiederholbar?

Purpose: Phase 9 hat vorgeführt, wohin das Gegenteil führt. Plan 09-04 begründete mit
„keine Überschneidung", warum in einer Datei nicht nachgesehen werden müsse; die vierte von
zwölf Kopien derselben Regel kam erst durch den Code-Review ans Licht, und eine davon schrieb
bei einem `GET` in die Produktionsdatenbank. Deshalb ist die Prüfung in Task 1 eine
erschöpfende Nachsuche nach der Regel — nicht das Abhaken einer Liste, die aus einem anderen
Plan geerbt wurde.
Output: vollständige `11-AUFRUFER-CHECKLISTE.md`, `11-DESKTOP-DISPOSITION.md`,
`11-IDEMPOTENZ-NACHWEIS.md`, das wiederverwendbare Fixture-Skript für Plan 11-10.

**Zustand am Ende dieses Plans:** `npx tsc --noEmit` ist wieder fehlerfrei, die Testsuite hat
gegenüber `11-AUSGANGSZUSTAND.md` keinen neuen roten Test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-datumsabh-ngige-berechnung/11-CONTEXT.md
@.planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-CHECKLISTE.md
@.planning/phases/11-datumsabh-ngige-berechnung/11-AUSGANGSZUSTAND.md
@.planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md
@.claude/CLAUDE.md

<interfaces>
Teilbelege der Welle 3 (je Plan eine eigene Datei, damit bei paralleler Ausführung kein
verlorenes Update entsteht — die Zusammenführung ist Task 1 dieses Plans):
  .planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-TEIL-05.md   (Plan 11-05)
  .planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-TEIL-06.md   (Plan 11-06, enthält „D7: Legacy-Pfad")
  .planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-TEIL-07.md   (Plan 11-07)
  .planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-TEIL-08.md   (Plan 11-08)
  .planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-TEIL-11.md   (Plan 11-11)

Aus Plan 11-02, `server/src/services/workPeriodContext.ts`:
  export interface WorkPeriodContext { resolve(userId: number, date: string): UserWorkPeriod | null }
  export function createWorkPeriodContext(): WorkPeriodContext
  export const directWorkPeriodLookup: WorkPeriodContext

Aus Plan 11-04, `server/src/utils/workingDays.ts`:
  getDailyTargetHours(user: UserPublic, date: Date | string, periods: WorkPeriodContext): number
  calculateAbsenceHoursWithWorkSchedule(user, startDate, endDate, periods): number
  calculateTargetHoursForPeriod(user, fromDate, toDate, periods): number
  class MissingWorkPeriodError extends Error

Aus Plan 10-04, `server/src/services/workPeriodService.ts`:
  createWorkPeriod(input): UserWorkPeriod
  closeWorkPeriod(periodId: number, validTo: string): UserWorkPeriod
  getWorkPeriods(userId: number): UserWorkPeriod[]
  checkPeriodChain(userId: number): PeriodChainCheckResult

Aus Plan 10-02, `server/src/scripts/productionGuard.ts`:
  export function assertNotProduction(pfad?: string): void   // Exit 2 bei Auslösung

Aus Plan 11-03, `server/src/services/userService.ts`:
  export function ensureInitialWorkPeriod(user: UserPublic, createdBy?: number | null): UserWorkPeriod | null

Aus `server/src/services/overtimeTransactionRebuildService.ts`:
  export function rebuildOvertimeTransactionsForMonth(...)   // Signatur aus der Datei ablesen

ARBEITSKOPIEN — Zuordnung ist bindend:
  server/database/11-nullwirkung.db     Population der 20 Nutzer aus Plan 11-01.
                                        NUR LESEN. Kein Skript, kein Werkzeuglauf, kein
                                        Seed schreibt hier hinein. Snapshot B in Plan 11-10
                                        hängt daran.
  server/database/11-modellwechsel.db   Wird in Task 3 dieses Plans frisch gezogen. Hier
                                        lebt der künstliche Nutzer mit Modellwechsel.
  server/database/11-werkzeugprobe.db   Wegwerfkopie für Werkzeugläufe, die beim Messen
                                        schreiben (Pläne 11-08/11-11).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Erschöpfende Nachsuche und Zusammenführung der Aufrufer-Checkliste</name>
  <files>.planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-CHECKLISTE.md</files>
  <read_first>
    - `.planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-CHECKLISTE.md` — der Stand
      nach Plan 11-01 und dem Abschnitt „Compiler-Fehlerliste nach 11-04"
    - die fünf Teilbeleg-Dateien `11-AUFRUFER-TEIL-05.md` bis `-08.md` und `-11.md`
    - `.planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md` — die 23
      klassifizierten Fundstellen, insbesondere die Abschnitte „Nicht-Abweichungen mit
      Begründung" und die Desktop-Zeilen 156-166
  </read_first>
  <action>
    Zwei Arbeitsschritte, in dieser Reihenfolge. Der zweite ist der eigentliche Nachweis; der
    erste ist nur Buchhaltung.

    SCHRITT A — Zusammenführen. Die fünf Teilbeleg-Dateien in die Haupttabelle von
    `11-AUFRUFER-CHECKLISTE.md` einarbeiten: je Fundstelle den Haken setzen bzw. die
    Disposition übernehmen, und den Abschnitt „D7: Legacy-Pfad" aus `11-AUFRUFER-TEIL-06.md`
    wörtlich in die Hauptdatei übernehmen. Widersprechen sich zwei Teilbelege (dieselbe
    Fundstelle, verschiedene Aussage), wird der Widerspruch als solcher stehen gelassen und
    in Schritt B aufgelöst — nicht stillschweigend geglättet. Die Teilbeleg-Dateien bleiben
    liegen, sie sind die Quelle.

    SCHRITT B — Erschöpfende Nachsuche. Das ist ausdrücklich KEIN Abgleich gegen die
    geerbte Liste, sondern eine Suche nach der Regel selbst im heutigen Code. Jeder Lauf wird
    mit Befehl und Trefferzahl wörtlich in einem neuen Abschnitt „Erschöpfende Nachsuche"
    protokolliert. Für Zählungen Kommentarzeilen herausfiltern
    (`grep -v "^\s*\*" | grep -v "^\s*//"`), weil sonst die Prosa dieses Umbaus die eigene
    Zählung verfälscht.

    Suchraum: `server/src`, `server/scripts`, das Serverwurzelverzeichnis (`server/*.ts`),
    `desktop/src`. Gesucht wird nach:
    1. den drei Funktionsnamen `getDailyTargetHours`, `calculateAbsenceHoursWithWorkSchedule`,
       `calculateTargetHoursForPeriod` — jeder Treffer muss die neue Signatur tragen oder
       eine Disposition haben;
    2. der flachen Formel in allen Schreibweisen: `weeklyHours / 5`, `weeklyHours/5`,
       `weeklyHours \* 0.2`, `/ 5` in unmittelbarer Nähe von `weeklyHours` — das ist der
       Fundtyp, den Phase 9 zwölffach vorgefunden hat;
    3. Zugriffen auf `user.workSchedule` bzw. `users.workSchedule` und `user.weeklyHours`
       außerhalb von Typdefinitionen, Formularen und `userService.ts` — jeder solche Zugriff,
       der in eine Stundenzahl mündet, ist eine Fundstelle;
    4. `SELECT ... FROM users` in Dateien, die anschließend Sollstunden bilden;
    5. `WorkPeriodContext` — die Gegenprobe: welche Dateien den Kontext bereits benutzen.

    Jeder Treffer wird gegen die Tabelle gehalten. Drei mögliche Ergebnisse je Treffer, und
    nur diese drei: (a) steht bereits in der Tabelle und ist abgehakt; (b) steht in der
    Tabelle mit begründeter Disposition; (c) steht NICHT in der Tabelle — dann ist es ein
    Neufund. Neufunde bekommen eine neue Tabellenzeile, werden in der SUMMARY einzeln
    benannt und dort bewertet: Produktivpfad ja/nein, und wenn ja, wird die Umstellung in
    DIESEM Plan nachgeholt oder als blockierender Befund gemeldet. Ein Neufund im
    Produktivpfad, der ungeprüft bleibt, macht den Plan unvollständig.

    Umgekehrte Richtung ebenfalls prüfen: Jede der 23 Fundstellen aus
    `09-INVENTAR-SOLLSTUNDEN.md` muss in der Tabelle wiederzufinden sein. Fundstellen, deren
    Zeilennummer sich verschoben hat oder die es nicht mehr gibt, werden als solche vermerkt
    (Datei existiert nicht mehr / Funktion entfernt / Zeile verschoben von X auf Y) — nicht
    kommentarlos gestrichen.

    Die Tabelle bekommt am Ende einen Kopfabschnitt mit drei Zahlen: Zeilen gesamt, davon
    abgehakt, davon mit Disposition. Diese drei Zahlen müssen sich addieren. Keine Zeile
    ohne eines von beidem.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-CHECKLISTE.md','utf8');const rows=t.split('\n').filter(l=>l.trim().startsWith('|')&&!/^\|\s*[-: ]+\|/.test(l)&&!/Datei:Zeile|Fundstelle|Disposition\s*\|/i.test(l));const desktop=rows.filter(l=>l.toLowerCase().includes('desktop/'));const offen=rows.filter(l=>!/\[x\]|\[n\/a\]/i.test(l)&&!l.toLowerCase().includes('desktop/'));console.log('Datenzeilen:',rows.length,'davon Desktop (Disposition folgt in Task 2):',desktop.length,'ohne Haken/Disposition:',offen.length);offen.slice(0,10).forEach(l=>console.log(l));process.exit(offen.length===0?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Jede Datenzeile der Tabelle trägt `[x]` (nachgezogen) oder `[n/a]` (begründete
      Disposition); der Prüfbefehl oben endet mit Exit 0.
      **Ausgenommen sind die vier `desktop/`-Zeilen** — sie sind zu diesem Zeitpunkt bewusst
      noch offen; ihre Disposition wird in Task 2 dieses Plans belegt und gesetzt. Der
      Prüfbefehl nimmt sie aus der Exit-0-Bedingung heraus und gibt ihre Anzahl getrennt aus;
      erwartet werden genau 4. Das abschließende Kriterium „keine Zeile mehr offen" trägt Task 2.
    - Der Abschnitt „Erschöpfende Nachsuche" enthält für alle fünf Suchgruppen den wörtlichen
      Befehl und die Trefferzahl; die Zählbefehle filtern Kommentarzeilen heraus.
    - Der Abschnitt „D7: Legacy-Pfad" aus dem Teilbeleg 11-06 steht in der Hauptdatei.
    - Jede der 23 Fundstellen aus `09-INVENTAR-SOLLSTUNDEN.md` ist in der Tabelle
      wiederzufinden oder mit dem Grund ihres Wegfalls vermerkt.
    - Neufunde (in keiner Teilliste enthalten) sind einzeln in der SUMMARY benannt und
      bewertet; steht dort „keine Neufunde", ist das mit den Trefferzahlen belegt.
    - Die drei Zahlen des Kopfabschnitts addieren sich zur Zeilenzahl der Tabelle.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Desktop-Disposition entscheiden und festschreiben</name>
  <files>.planning/phases/11-datumsabh-ngige-berechnung/11-DESKTOP-DISPOSITION.md, .planning/ROADMAP.md, .planning/phases/11-datumsabh-ngige-berechnung/11-AUFRUFER-CHECKLISTE.md</files>
  <read_first>
    - `desktop/src/utils/timeUtils.ts:200-245` — die eigenständige Reimplementierung von
      `calculateAbsenceHoursWithWorkSchedule`
    - `desktop/src/components/absences/AbsenceRequestForm.tsx:50-85` — der einzige Aufrufer
      dieser Funktion; wofür der Wert benutzt wird
    - `desktop/src/components/worktime/WorkScheduleDisplay.tsx:50-70` und
      `desktop/src/components/users/WorkScheduleEditor.tsx:40-50` — was dort gerechnet wird
    - `.planning/phases/09-ein-ma-stab-ein-weg/09-INVENTAR-SOLLSTUNDEN.md:255-262` — die
      Vormerkung aus Phase 9
    - `.planning/phases/11-datumsabh-ngige-berechnung/11-AUSGANGSZUSTAND.md`, Punkte (c)
      und (d)
  </read_first>
  <action>
    Die ROADMAP nennt „Desktop-Anzeigen" ausdrücklich als nachzuziehende Aufrufer; keiner der
    Pläne 11-01 bis 11-08 und 11-11 fasst eine Datei unter `desktop/` an. Dieser Task löst
    das auf. Ein drittes Ergebnis — stillschweigend offen lassen — gibt es nicht.

    ENTSCHIEDEN IST: Der Desktop wird in Phase 11 NICHT nachgezogen. Die Disposition lautet
    „bewusste Auslassung, zugeordnet an Phase 12". Dieser Task setzt sie nicht neu, er belegt
    ihre beiden Voraussetzungen und schreibt sie fest. Die Begründung, die im Dokument stehen
    muss, hat zwei Teile:

    (1) Abhängigkeitskonflikt: Der Desktop rechnet ohne Datenbankzugriff aus den Stammdaten,
    die die API liefert. Um periodengetreu zu rechnen, bräuchte er einen Lesezugang zu den
    Perioden eines Nutzers. Eine solche API existiert nicht, und `11-CONTEXT.md` schließt sie
    für Phase 11 ausdrücklich aus („Keine Oberfläche und keine API zum Eintragen von
    Wechseln — das ist Phase 12"). Der Desktop kann in dieser Phase nicht nachgezogen werden,
    weil das Fehlende erst in Phase 12 entsteht — nicht, weil es aufwendig wäre.

    (2) Wirksamkeitsfenster: Solange kein zweiter Zeitabschnitt entstehen kann, liefert die
    Stammdaten-Rechnung des Desktops dasselbe wie die periodengetreue Rechnung des Servers.
    Genau das ist in Phase 11 der Fall: Migration 009 hat jedem Nutzer genau eine offene
    Periode mit den Werten seiner heutigen Stammdaten gegeben, Plan 11-03 spiegelt jede
    Stammdatenänderung in genau diese offene Periode, und es gibt keinen Weg, eine zweite
    anzulegen. Ab Phase 12 gibt es ihn — ab da, und keinen Tag früher, kann die
    Desktop-Anzeige einem Anwender andere Sollstunden zeigen als der Server.

    DIESE ZWEITE BEGRÜNDUNG IST EINE BEHAUPTUNG, BIS SIE GEMESSEN IST. Deshalb sind in diesem
    Task drei Zahlen gegen `server/database/11-nullwirkung.db` zu erheben und wörtlich mit
    Befehl und Ausgabe ins Dokument zu schreiben (nur lesend, `PRAGMA query_only` oder
    `{readonly:true}`):
    - Anzahl Nutzer mit mehr als einer Periode. Erwartung 0.
    - Anzahl Nutzer, deren offene Periode in `weeklyHours` oder `workSchedule` von der
      `users`-Zeile abweicht. Erwartung 0 (das ist die Drift-Zahl aus 11-01 Punkt (d),
      hier erneut erhoben, weil seither Code gelaufen ist).
    - Anzahl Nutzer ohne offene Periode (`validTo IS NULL` fehlt). Erwartung 0.
    Ist eine dieser Zahlen größer als null, trägt die Begründung (2) nicht. Dann wird das
    Dokument mit dem tatsächlichen Befund geschrieben, die Disposition auf „offen,
    blockierender Befund" gesetzt und der Punkt in der SUMMARY als Blocker gemeldet — nicht
    die Zahl weggeschrieben.

    Das Dokument `11-DESKTOP-DISPOSITION.md` enthält außerdem, je Datei einzeln:
    `desktop/src/utils/timeUtils.ts` (die Reimplementierung selbst),
    `desktop/src/components/absences/AbsenceRequestForm.tsx` (ihr einziger Aufrufer, mit dem
    Satz, wofür der Wert dort dient und ob der Server ihn beim Speichern ohnehin neu
    berechnet — aus dem Code belegen, nicht annehmen),
    `desktop/src/components/worktime/WorkScheduleDisplay.tsx` und
    `desktop/src/components/users/WorkScheduleEditor.tsx` (Anzeige bzw. Eingabe des heutigen
    Modells). Je Datei: rechnet sie über einen vergangenen Zeitraum oder nur über den
    heutigen/künftigen Stand? Das ist die Frage, die Plan 11-01 als Prüfauftrag hinterlegt
    hat, und sie ist aus dem Code zu beantworten.

    Zuletzt die Übergabe an Phase 12, ohne die eine „Zuordnung zu einer Folgephase" nur ein
    Wort wäre: In `.planning/ROADMAP.md` im Abschnitt „### Phase 12: Stundenwechsel bedienen"
    unter „**Umfang**" einen Punkt ergänzen, der die drei Desktop-Dateien namentlich nennt
    und auf `11-DESKTOP-DISPOSITION.md` verweist. Sonst nichts an der ROADMAP ändern —
    insbesondere keine Zeile eines anderen Abschnitts anfassen.

    In `11-AUFRUFER-CHECKLISTE.md` die vier Desktop-Zeilen auf `[n/a]` setzen, mit Verweis
    auf `11-DESKTOP-DISPOSITION.md` in der Dispositionsspalte.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const d=fs.readFileSync('.planning/phases/11-datumsabh-ngige-berechnung/11-DESKTOP-DISPOSITION.md','utf8');const r=fs.readFileSync('.planning/ROADMAP.md','utf8');const p12=r.split('### Phase 12')[1].split('### Phase 13')[0];const ok=['timeUtils.ts','AbsenceRequestForm.tsx','WorkScheduleDisplay.tsx','WorkScheduleEditor.tsx'].every(f=>d.includes(f));console.log('alle vier Dateien im Dokument:',ok,'| Verweis in ROADMAP Phase 12:',p12.includes('11-DESKTOP-DISPOSITION.md'));process.exit(ok&&p12.includes('11-DESKTOP-DISPOSITION.md')?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `11-DESKTOP-DISPOSITION.md` existiert und behandelt alle vier Desktop-Dateien einzeln,
      je mit der Antwort „vergangener Zeitraum / nur heute und künftig" aus dem Code belegt.
    - Die drei Messzahlen stehen mit wörtlichem Befehl und wörtlicher Ausgabe im Dokument;
      der Befehl liest ausschließlich (`readonly`) und nennt
      `server/database/11-nullwirkung.db`.
    - Die Disposition ist ausdrücklich benannt („nicht nachgezogen, zugeordnet an Phase 12")
      oder — falls eine der drei Zahlen von null abweicht — als blockierender Befund
      ausgewiesen und in der SUMMARY gemeldet.
    - `.planning/ROADMAP.md`, Abschnitt Phase 12, „Umfang", enthält den neuen Punkt mit den
      drei Dateinamen und dem Verweis auf `11-DESKTOP-DISPOSITION.md`; `git diff
      .planning/ROADMAP.md` zeigt ausschließlich Zeilen innerhalb des Phase-12-Abschnitts.
    - Die vier Desktop-Zeilen in `11-AUFRUFER-CHECKLISTE.md` tragen `[n/a]` mit Verweis.
    - **Abschließender Vollständigkeitsbeleg:** Derselbe Prüfbefehl wie in Task 1, aber **ohne**
      die Desktop-Ausnahme, endet jetzt mit Exit 0 — es ist keine Datenzeile mehr offen.
      Wörtliche Ausgabe im Nachweisdokument festhalten.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: REQ-24 auf echten Daten — zweifacher Rebuild über einen Modellwechsel, plus Gesamtgrün</name>
  <files>server/src/scripts/seedModelChangeUser.ts, server/package.json, .planning/phases/11-datumsabh-ngige-berechnung/11-IDEMPOTENZ-NACHWEIS.md</files>
  <read_first>
    - `server/src/scripts/snapshotBalances.ts:1-60` — das verbindliche Skript-Muster:
      importsichere Module am Kopf, Argumentauswertung, `assertNotProduction()` synchron,
      erst danach `await import()` der datenbankziehenden Module; und die Begründung, warum
      `DATABASE_PATH` Pflicht ist und keinen stillen Rückfall haben darf
    - `server/src/services/overtimeTransactionRebuildService.ts` — Signatur und
      Rückgabewert von `rebuildOvertimeTransactionsForMonth`, und wie der Monat vor dem
      Neuaufbau geleert wird
    - `server/src/services/workPeriodService.ts` — `createWorkPeriod`, `closeWorkPeriod`,
      halboffenes Intervall
    - `server/src/services/userService.ts` nach Plan 11-03 — `createUser`,
      `ensureInitialWorkPeriod`
    - `.planning/phases/11-datumsabh-ngige-berechnung/11-AUSGANGSZUSTAND.md` — Punkt (f)
      (`asOf`-Datum) und die Liste der vorbestehend roten Tests
  </read_first>
  <action>
    TEIL 1 — die Fixture. `server/src/scripts/seedModelChangeUser.ts` anlegen, dazu in
    `server/package.json` das Skript `seed:model-change`. Das ist bewusst ein eigenes,
    wiederverwendbares Werkzeug und kein Wegwerfblock: Plan 11-10 legt damit denselben Nutzer
    für den Wirkungsnachweis an (D6), und Phase 14 wird ihn erneut brauchen.

    Verhalten: legt genau einen Nutzer mit eindeutigem, markiertem `username` (Präfix
    `t1109-modellwechsel-`) an, über `createUser` aus `userService.ts` — kein eigener
    `INSERT INTO users`, kein eigener `INSERT INTO user_work_periods`. Danach wird die
    Startperiode über `closeWorkPeriod` zum Stichtag geschlossen und über `createWorkPeriod`
    die Folgeperiode angelegt. Vorgaben, alle über CLI-Argumente steuerbar und mit den
    folgenden Vorgabewerten: `hireDate` deutlich vor dem Stichtag, erste Periode 40
    Wochenstunden ohne Wochenplan, Stichtag MITTEN IM MONAT (Vorgabe `2026-05-14`, ein
    Donnerstag — damit sowohl der Monatsanfang als auch das Monatsende in der Prüfung mit
    verschiedenen Modellen belegt sind, D6), zweite Periode 20 Wochenstunden ohne Wochenplan
    und `validTo = null`. Zusätzlich erzeugt das Skript für jeden Werktag im Prüfmonat einen
    Zeiteintrag, damit der Rebuild überhaupt etwas zu rechnen hat; Umfang und Muster der
    Einträge werden in der Ausgabe genannt.

    Zwingend: `assertNotProduction()` synchron auf Modulebene VOR jedem Import, der
    transitiv `server/src/database/connection.ts` lädt (dieses Modul öffnet die Datenbank auf
    Modulebene, Zeile 50, und legt per `mkdirSync` Verzeichnisse an, Zeilen 13-17).
    `DATABASE_PATH` ist Pflicht ohne stillen Rückfall. Das Skript weigert sich zusätzlich
    ausdrücklich zu laufen, wenn der aufgelöste Pfad auf `11-nullwirkung.db` endet — diese
    Kopie ist die Grundlage des Nullwirkungs-Nachweises in Plan 11-10 und muss unberührt
    bleiben. Diese Weigerung ist eine eigene Prüfung mit eigener Fehlermeldung, kein
    Kommentar.

    Ausgabe am Ende: `userId`, die beiden Perioden mit `validFrom`/`validTo`/`weeklyHours`,
    und das Ergebnis von `checkPeriodChain(userId)`. Idempotenz: Ein zweiter Lauf legt
    keinen zweiten Nutzer an, sondern meldet den vorhandenen und beendet sich mit Exit 0.

    Keine Datumsarithmetik über `new Date('JJJJ-MM-TT')` — das parst als UTC-Mitternacht und
    hat in Phase 9 systemisch den letzten Kalendertag jedes Monats verschluckt. Datumswerte
    als Zeichenketten führen; wo ein Tageslauf nötig ist, `formatDate(date, 'yyyy-MM-dd')`
    aus `server/src/utils/timezone.ts` verwenden. `toISOString().split('T')[0]` ist verboten.

    TEIL 2 — der Nachweis. Arbeitskopie `server/database/11-modellwechsel.db` frisch ziehen:
    `VACUUM INTO` aus `server/database/development.db` per `node -e` mit `better-sqlite3`
    (`sqlite3`-CLI ist auf diesem Rechner nicht verfügbar; existiert die Zieldatei bereits,
    vorher löschen — `VACUUM INTO` überschreibt nicht). Der laufende Dev-Server hält
    `development.db` offen, deshalb keine Dateikopie.

    Darauf `seed:model-change` ausführen. Dann `rebuildOvertimeTransactionsForMonth` für den
    Stichtagsmonat zweimal hintereinander laufen lassen und zwischen den Läufen den
    vollständigen Datenbestand des Monats erheben: alle Zeilen aus `overtime_transactions`
    des Nutzers im Monat (Datum, Typ, Stunden, Saldo — Spaltenliste aus der Tabelle ablesen,
    nicht raten) und die `overtime_balance`-Zeile des Monats. Vergleich über kanonisch
    sortiertes JSON mit `cmp` oder einem Byte-Vergleich, nicht über Stichproben. Zusätzlich
    denselben Vergleich für den Vormonat und den Folgemonat, damit belegt ist, dass der
    Rebuild eines Monats die Nachbarmonate nicht bewegt.

    Zweite Zusicherung im selben Lauf, die REQ-24 erst vollständig macht: Der erste Lauf muss
    für die Tage VOR dem Stichtag andere Sollstunden geschrieben haben als für die Tage
    danach. Wären beide gleich, wäre der Idempotenz-Vergleich wertlos, weil er dann einen
    Modellwechsel prüfte, der gar nicht wirkt. Beide Werte wörtlich ins Dokument.

    Ergebnis nach `11-IDEMPOTENZ-NACHWEIS.md`, im Aufbau von
    `.planning/phases/10-perioden-fundament/10-NULLWIRKUNG-NACHWEIS.md`: nummerierte
    Schritte, je Schritt der wörtliche Befehl und die wörtliche Ausgabe, am Ende eine Tabelle
    mit Urteil und Fundstelle, und ein Abschnitt „Was dieser Nachweis NICHT zeigt".

    TEIL 3 — Gesamtgrün. `cd server && npx tsc --noEmit` muss jetzt fehlerfrei sein; die
    Rotphase seit Plan 11-04 endet hier. Danach `cd server && npx vitest run` vollständig,
    Ergebnis und die Titel ALLER fehlschlagenden Tests wörtlich ins Dokument. Verglichen wird
    gegen die Liste in `11-AUSGANGSZUSTAND.md` (Erwartung: dieselben drei vorbestehend roten
    Tests, 2× `unifiedOvertimeService.test.ts`, 1× `vacationBackfillService.test.ts`). Diese
    drei werden NICHT behoben — sie sind der festgehaltene Ausgangszustand. Jeder darüber
    hinausgehende rote Test ist eine Regression dieser Phase und wird in der SUMMARY einzeln
    benannt; ein Plan, der sie unter „Gesamtgrün" verbucht, hat den Zweck dieses Tasks
    verfehlt.
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit && echo "TSC-GRUEN"</automated>
  </verify>
  <acceptance_criteria>
    - `server/src/scripts/seedModelChangeUser.ts` existiert; `grep -n
      "assertNotProduction\|await import" server/src/scripts/seedModelChangeUser.ts` belegt,
      dass die Schutzprüfung vor jedem dynamischen Import steht.
    - Das Skript weigert sich nachweislich, gegen `11-nullwirkung.db` zu laufen — der
      Probelauf mit diesem Pfad und die wörtliche Fehlermeldung stehen im Dokument.
    - `grep -c "INSERT INTO users\|INSERT INTO user_work_periods"
      server/src/scripts/seedModelChangeUser.ts` ergibt 0.
    - `grep -c "toISOString" server/src/scripts/seedModelChangeUser.ts` ergibt 0.
    - `11-IDEMPOTENZ-NACHWEIS.md` enthält für Stichtagsmonat, Vormonat und Folgemonat je
      einen maschinellen Vergleich zweier Läufe mit dem Ergebnis „identisch"; der
      Vergleichsbefehl steht wörtlich da.
    - Die Sollstunden eines Tages vor und eines Tages nach dem Stichtag stehen wörtlich im
      Dokument und sind verschieden.
    - `npx tsc --noEmit` ist fehlerfrei (Ausgabe im Dokument).
    - Die Liste der roten Tests aus `npx vitest run` steht wörtlich im Dokument und ist mit
      der Liste aus `11-AUSGANGSZUSTAND.md` Zeile für Zeile verglichen; Abweichungen sind
      einzeln benannt.
    - `server/database/11-nullwirkung.db` ist unverändert: Nutzerzahl und Zeilenzahl in
      `overtime_balance` stimmen mit den in `11-AUSGANGSZUSTAND.md` protokollierten Werten
      überein (Prüfung lesend, Ausgabe im Dokument).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture-Skript → Datenbankdatei | Das Skript legt Nutzer und Perioden an und öffnet die Datenbank auf Modulebene |
| Nachweisdokument → Abnahmeentscheidung | Auf Basis dieses Dokuments gilt die Phase als vollständig |
| Arbeitskopie 11-nullwirkung.db → Plan 11-10 | Jeder Schreibzugriff hier entwertet den Nullwirkungs-Nachweis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-31 | Tampering | `seedModelChangeUser.ts` läuft gegen Produktion | mitigate | `assertNotProduction()` synchron auf Modulebene vor jedem datenbankziehenden Import; `DATABASE_PATH` Pflicht ohne Rückfall; Reihenfolge per grep in den Acceptance Criteria geprüft |
| T-11-32 | Tampering | Fixture verunreinigt `11-nullwirkung.db` und entwertet den Nachweis in Plan 11-10 | mitigate | Eigene Pfadprüfung im Skript mit eigener Fehlermeldung; Probelauf mit diesem Pfad ist Abnahmekriterium; Nutzerzahl der Kopie wird am Ende gegengeprüft |
| T-11-33 | Repudiation | „Vollständig" ohne Beleg — der Fehler aus Plan 09-04 | mitigate | Task 1 ist eine erschöpfende Nachsuche nach der Regel, nicht ein Abgleich gegen die geerbte Liste; Neufunde sind einzeln zu melden |
| T-11-34 | Repudiation | Idempotenz-Nachweis, der nicht fehlschlagen kann | mitigate | Zusätzliche Gegenprobe: die Sollstunden vor und nach dem Stichtag müssen verschieden sein, sonst prüft der Vergleich einen unwirksamen Modellwechsel |
| T-11-35 | Information Disclosure | Nachweisdokumente im Repository | accept | Sie enthalten `userId`, Datum und Stundenwerte, keine Namen und keine Zugangsdaten; gleiche Praxis wie `10-NULLWIRKUNG-NACHWEIS.md` |
| T-11-36 | Denial of Service | Widersprüchliche Desktop-Anzeige gegenüber dem Server | mitigate | Task 2 belegt das Wirksamkeitsfenster mit drei gemessenen Zahlen und übergibt an Phase 12; bei Abweichung wird die Disposition zum blockierenden Befund |
| T-11-SC | Tampering | Paketinstallation | mitigate | Dieser Plan installiert kein Paket. Kommt es doch dazu, gilt der Paket-Legitimitäts-Gate der Phase — Ausführung anhalten |
</threat_model>

<verification>
- `11-AUFRUFER-CHECKLISTE.md`: keine Datenzeile ohne `[x]` oder `[n/a]`; Abschnitt
  „Erschöpfende Nachsuche" vorhanden; Abschnitt „D7: Legacy-Pfad" vorhanden.
- `11-DESKTOP-DISPOSITION.md` vorhanden, alle vier Dateien behandelt, ROADMAP Phase 12
  verweist darauf.
- `11-IDEMPOTENZ-NACHWEIS.md` vorhanden, drei Monatsvergleiche identisch, Gegenprobe
  bestanden.
- `cd server && npx tsc --noEmit` fehlerfrei.
- `cd server && npx vitest run`: kein roter Test über die drei aus `11-AUSGANGSZUSTAND.md`
  bekannten hinaus.
- `server/database/11-nullwirkung.db` unverändert.
</verification>

<success_criteria>
Jede Fundstelle der Sollstunden-Regel ist nachgezogen oder begründet ausgenommen, belegt
durch eine erschöpfende Nachsuche. Der Desktop hat eine schriftliche Disposition mit
Phasenzuordnung. Ein zweifacher Rebuild über einen echten Modellwechsel liefert auf echten
Daten denselben Bestand. Compiler und Testsuite stehen wieder auf dem Ausgangszustand.
</success_criteria>

<output>
Create `.planning/phases/11-datumsabh-ngige-berechnung/11-09-SUMMARY.md` when done
</output>
