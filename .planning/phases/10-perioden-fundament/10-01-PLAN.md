---
phase: 10-perioden-fundament
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/database/migrations/008_create_user_work_periods.ts
  - server/src/database/migrations/008_create_user_work_periods.test.ts
  - server/src/database/schema.ts
  - server/src/database/schemaMigrationParity.test.ts
  - server/src/types/index.ts
autonomous: true
requirements: [REQ-20, REQ-22]

must_haves:
  truths:
    - "Die Tabelle user_work_periods existiert mit validFrom, validTo, weeklyHours und workSchedule in einer Zeile (REQ-20, D1, D2)"
    - "Ein zweiter Lauf der Migration 008 erzeugt keinen Fehler und keine zweite Tabelle (D7)"
    - "schema.ts und Migration 008 erzeugen wortgleiche Datenbankobjekte — nachgewiesen durch einen Test, nicht behauptet"
    - "users.weeklyHours und users.workSchedule sind unverändert; kein DROP, kein RENAME, kein ALTER (D4, REQ-21)"
  artifacts:
    - path: "server/src/database/migrations/008_create_user_work_periods.ts"
      provides: "Tabelle, Constraints, Indizes und Trigger für user_work_periods"
      contains: "user_work_periods"
    - path: "server/src/database/schema.ts"
      provides: "Spiegel der Tabelle für frisch angelegte und lokale Datenbanken"
      contains: "user_work_periods"
    - path: "server/src/types/index.ts"
      provides: "UserWorkPeriod-Typ ohne any"
      contains: "UserWorkPeriod"
  key_links:
    - from: "server/src/database/schema.ts"
      to: "server/src/database/migrations/008_create_user_work_periods.ts"
      via: "wortgleiche DDL, abgesichert durch schemaMigrationParity.test.ts"
      pattern: "user_work_periods"
---

<objective>
Die Tabelle `user_work_periods` entsteht — als Migration über den bestehenden
`migrationRunner` (D7) und als Spiegel in `schema.ts`, wie es das Projekt seit
`vacation_transactions` (Migration 007 / `schema.ts:219-256`) handhabt.

Diese Phase schreibt noch keine Daten in die Tabelle (das ist Plan 10-03) und liest noch
nichts daraus (das ist Phase 11). `users.weeklyHours` und `users.workSchedule` bleiben
unangetastet (D4) — dadurch ist REQ-21 mechanisch garantiert und nicht nur behauptet.

Purpose: Ohne Tabelle kein Fundament. Alle weiteren Pläne dieser Phase hängen an der hier
festgelegten DDL.
Output: Migration 008, Spiegel in `schema.ts`, Typ `UserWorkPeriod`, zwei Testdateien.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-perioden-fundament/10-CONTEXT.md
@.claude/CLAUDE.md

<ddl_contract>
<!-- Verbindlicher Vertrag. Diese DDL wurde am 22.08.2026 gegen better-sqlite3 in einer
     :memory:-Datenbank durchgespielt: Überlappung, Lücke, zweite offene Periode, der
     Stichtagswechsel aus Phase 12 (UPDATE schließt, INSERT eröffnet), die Korrektur aus
     Phase 13 (UPDATE ohne Datumsänderung) und das Rückgängigmachen (DELETE der letzten
     offenen Periode) verhalten sich wie unten beschrieben. Nicht neu erfinden. -->

CREATE TABLE IF NOT EXISTS user_work_periods (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  userId INTEGER NOT NULL,
  validFrom TEXT NOT NULL,
  validTo TEXT,
  weeklyHours REAL NOT NULL,
  workSchedule TEXT,
  note TEXT,
  createdAt TEXT DEFAULT (datetime('now')),
  createdBy INTEGER,
  UNIQUE(userId, validFrom),
  CHECK (validFrom GLOB '[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]'),
  CHECK (validTo IS NULL OR validTo GLOB '[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]'),
  CHECK (validTo IS NULL OR validTo > validFrom),
  CHECK (weeklyHours >= 0 AND weeklyHours <= 168),
  FOREIGN KEY (userId) REFERENCES users(id) ON DELETE CASCADE,
  FOREIGN KEY (createdBy) REFERENCES users(id)
);

CREATE INDEX IF NOT EXISTS idx_user_work_periods_user_from
  ON user_work_periods(userId, validFrom);

CREATE UNIQUE INDEX IF NOT EXISTS idx_user_work_periods_one_open
  ON user_work_periods(userId) WHERE validTo IS NULL;

CREATE TRIGGER IF NOT EXISTS trg_user_work_periods_insert_guard
BEFORE INSERT ON user_work_periods
BEGIN
  SELECT RAISE(ABORT, 'user_work_periods: Überlappung mit einer bestehenden Periode desselben Nutzers')
  WHERE EXISTS (
    SELECT 1 FROM user_work_periods p
    WHERE p.userId = NEW.userId
      AND (NEW.validTo IS NULL OR p.validFrom < NEW.validTo)
      AND (p.validTo   IS NULL OR NEW.validFrom < p.validTo)
  );
  SELECT RAISE(ABORT, 'user_work_periods: Lücke zur bestehenden Periodenkette desselben Nutzers')
  WHERE EXISTS (SELECT 1 FROM user_work_periods p WHERE p.userId = NEW.userId)
    AND NOT EXISTS (
      SELECT 1 FROM user_work_periods p
      WHERE p.userId = NEW.userId
        AND (p.validTo = NEW.validFrom
             OR (NEW.validTo IS NOT NULL AND p.validFrom = NEW.validTo))
    );
END;

CREATE TRIGGER IF NOT EXISTS trg_user_work_periods_update_guard
BEFORE UPDATE ON user_work_periods
BEGIN
  SELECT RAISE(ABORT, 'user_work_periods: Überlappung mit einer bestehenden Periode desselben Nutzers')
  WHERE EXISTS (
    SELECT 1 FROM user_work_periods p
    WHERE p.userId = NEW.userId AND p.id <> NEW.id
      AND (NEW.validTo IS NULL OR p.validFrom < NEW.validTo)
      AND (p.validTo   IS NULL OR NEW.validFrom < p.validTo)
  );
  SELECT RAISE(ABORT, 'user_work_periods: Lücke zur bestehenden Periodenkette desselben Nutzers')
  WHERE EXISTS (SELECT 1 FROM user_work_periods p WHERE p.userId = NEW.userId AND p.id <> NEW.id)
    AND NOT EXISTS (
      SELECT 1 FROM user_work_periods p
      WHERE p.userId = NEW.userId AND p.id <> NEW.id
        AND (p.validTo = NEW.validFrom
             OR (NEW.validTo IS NOT NULL AND p.validFrom = NEW.validTo))
    );
END;

CREATE TRIGGER IF NOT EXISTS trg_user_work_periods_delete_guard
BEFORE DELETE ON user_work_periods
BEGIN
  SELECT RAISE(ABORT, 'user_work_periods: Löschen würde eine Lücke in der Periodenkette hinterlassen')
  WHERE OLD.validTo IS NOT NULL
    AND EXISTS (SELECT 1 FROM users u WHERE u.id = OLD.userId)
    AND EXISTS (SELECT 1 FROM user_work_periods p
                WHERE p.userId = OLD.userId AND p.id <> OLD.id AND p.validTo = OLD.validFrom)
    AND EXISTS (SELECT 1 FROM user_work_periods p
                WHERE p.userId = OLD.userId AND p.id <> OLD.id AND p.validFrom = OLD.validTo);
END;
</ddl_contract>

<ddl_rationale>
- `validFrom` inklusiv, `validTo` exklusiv, laufende Periode `validTo = NULL` (D1). Deshalb
  ist Anschluss genau `p.validTo = NEW.validFrom` und Überlappung genau die halboffene
  Schnittbedingung oben.
- `GLOB '[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]'` statt `date(validFrom) = validFrom`:
  GLOB ist deterministisch und in einem CHECK unbedenklich, die Datumsfunktionen von SQLite
  sind es nicht zwangsläufig. Das CHECK ist der letzte Riegel gegen die Zeitzonenfalle aus
  Phase 9 — ein durch `toISOString()` verschobener oder anders formatierter Wert kommt gar
  nicht erst in die Tabelle.
- `idx_user_work_periods_one_open` (partieller UNIQUE-Index) erzwingt höchstens **eine**
  offene Periode je Nutzer. Zusammen mit den Triggern ist das die datenbankseitige Erfüllung
  von REQ-22/D3.
- `EXISTS (SELECT 1 FROM users u WHERE u.id = OLD.userId)` im DELETE-Trigger ist kein
  Beiwerk: Ohne diese Bedingung bricht ein `DELETE FROM users` (ON DELETE CASCADE) bei einem
  Nutzer mit drei verketteten Perioden ab, weil die mittlere Zeile den Lücken-Riegel auslöst.
  Gemessen am 22.08.2026: mit der Bedingung räumt die Kaskade alle Perioden ab, ohne sie
  scheitert sie. Testaufräumarbeiten (`DELETE FROM users WHERE id = ?`) hängen daran.
- `note`/`createdBy`/`createdAt`: Nachvollziehbarkeit analog `vacation_transactions`. In
  Phase 10 schreibt sie niemand außer der Bestandsüberführung; Phase 12/13 brauchen sie,
  und eine nachträgliche Spaltenerweiterung auf einer Live-Datenbank ist teurer als jetzt
  eine nullable Spalte mitzuführen.
</ddl_rationale>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 008 — Tabelle, Constraints, Indizes, Trigger</name>
  <files>
    server/src/database/migrations/008_create_user_work_periods.ts,
    server/src/database/migrations/008_create_user_work_periods.test.ts
  </files>
  <read_first>
    - server/src/database/migrations/007_create_vacation_transactions.ts (vollständig — das
      ist das zu kopierende Muster: Kopfkommentar mit WARUM, `export default { async up(db) }`,
      `db.prepare(...).run()` statt `db.exec`, Selbstverifikation der Spalten VOR den Indizes,
      `throw` statt stiller Erfolg)
    - server/src/database/migrationRunner.ts:24-77 (Migrationen laufen in `db.transaction()`;
      ein `throw` verhindert die Eintragung in die `migrations`-Tabelle)
    - Der Abschnitt `<ddl_contract>` dieses Plans
  </read_first>
  <action>
    Lege `008_create_user_work_periods.ts` nach dem Muster von 007 an. Der Standardexport ist
    ein Objekt mit einer `up(db: Database.Database)`-Funktion. Die Funktion enthält **kein**
    `await` — der `migrationRunner` ruft sie innerhalb einer synchronen
    `db.transaction()`-Klammer auf, ein echter asynchroner Schritt würde außerhalb der
    Transaktion landen.

    Setze die DDL aus `<ddl_contract>` unverändert um, jede Anweisung über
    `db.prepare(...).run()` wie in 007. Reihenfolge: Tabelle, dann Selbstverifikation der
    Spalten, dann Indizes, dann Trigger, dann Verifikation von Indizes und Triggern.

    Selbstverifikation, jeweils mit `throw new Error(...)` bei Abweichung, damit der
    `migrationRunner` die Migration nicht als erledigt bucht (die Lehre aus Migration 003,
    dokumentiert im Kopf von 007):
    - `PRAGMA table_info(user_work_periods)` liefert genau die Spalten `id, userId, validFrom,
      validTo, weeklyHours, workSchedule, note, createdAt, createdBy` — fehlende Spalten
      werden namentlich in der Fehlermeldung genannt.
    - `PRAGMA index_list(user_work_periods)` enthält `idx_user_work_periods_user_from` und
      `idx_user_work_periods_one_open`.
    - `SELECT name FROM sqlite_master WHERE type='trigger' AND tbl_name='user_work_periods'`
      liefert die drei Trigger `trg_user_work_periods_insert_guard`,
      `trg_user_work_periods_update_guard`, `trg_user_work_periods_delete_guard`.

    Schreibe in den Kopfkommentar (deutsch, wie 007): warum Perioden statt flacher Felder
    (Auslöser des Milestones v3.0), warum halboffene Intervalle (D1), dass `weeklyHours` und
    `workSchedule` gemeinsam versioniert werden (D2, REQ-20), dass die Trigger die
    Datenbankseite von REQ-22 sind (D3) und dass diese Migration **keine Daten schreibt** und
    `users.weeklyHours`/`users.workSchedule` nicht anfasst (D4, REQ-21). Vermerke, dass die
    Bestandsüberführung in Migration 009 liegt.

    Lege parallel `008_create_user_work_periods.test.ts` an. Die Tests arbeiten
    ausschließlich auf `new Database(':memory:')` — sie dürfen `database/development.db`
    nicht berühren; erzeuge dort eine minimale `users`-Tabelle (`id INTEGER PRIMARY KEY`) und
    rufe dann `migration.up(db)` direkt auf. Abgedeckte Fälle: alle neun Spalten vorhanden;
    beide Indizes vorhanden; alle drei Trigger vorhanden; ein zweiter Aufruf von `up(db)` auf
    derselben Datenbank läuft ohne Fehler durch und lässt die Objektzahl unverändert (D7).
  </action>
  <acceptance_criteria>
    - `server/src/database/migrations/008_create_user_work_periods.ts` existiert und enthält
      keinen `await`-Ausdruck innerhalb von `up`.
    - `grep -c "ALTER TABLE users\|DROP TABLE users\|UPDATE users" server/src/database/migrations/008_create_user_work_periods.ts`
      ergibt `0` (D4 mechanisch geprüft).
    - `npx vitest run src/database/migrations/008_create_user_work_periods.test.ts` meldet
      alle Tests grün, darunter einen Test mit „zweiter Lauf" oder „idempotent" im Namen.
    - Die Testdatei enthält kein `database/development.db` und kein `connection.js`.
    - `npx tsc --noEmit` läuft ohne Fehler durch.
  </acceptance_criteria>
  <verify>
    <automated>cd server && npx vitest run src/database/migrations/008_create_user_work_periods.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    Migration 008 legt Tabelle, zwei Indizes und drei Trigger an, verifiziert ihr eigenes
    Ergebnis und ist zweimal ausführbar. Kein Datensatz wird geschrieben, `users` bleibt
    unberührt.
  </done>
</task>

<task type="auto">
  <name>Task 2: Spiegel in schema.ts, Typ UserWorkPeriod, Gleichheitstest</name>
  <files>
    server/src/database/schema.ts,
    server/src/database/schemaMigrationParity.test.ts,
    server/src/types/index.ts
  </files>
  <read_first>
    - server/src/database/schema.ts:219-256 (`vacation_transactions`-Block inklusive des
      Kommentars „MUSS identisch bleiben mit migrations/007…" — genau diese Form ist zu
      wiederholen) und :452-472 (der gesammelte Index-Block am Ende)
    - server/src/database/connection.ts:30-50 (`initializeConnection()` ruft
      `initializeDatabase()` bei jedem Verbindungsaufbau auf — deshalb muss die Tabelle auch
      hier stehen, sonst fehlt sie in Testläufen und in frisch angelegten Datenbanken)
    - server/src/types/index.ts:2-10 (`WorkSchedule`) und :54-73 (`UserPublic`)
  </read_first>
  <action>
    Ergänze in `initializeDatabase()` einen Block „user_work_periods (Arbeitszeit-Perioden)"
    hinter dem `vacation_transactions`-Block. Übernimm die `CREATE TABLE`-Anweisung aus
    `<ddl_contract>` **zeichengleich** zur Migration und setze denselben Warnkommentar wie bei
    007 darüber: dass dieser Block identisch mit
    `migrations/008_create_user_work_periods.ts` bleiben muss und Abweichungen eine bekannte
    Fehlerquelle sind. Die beiden Indizes gehören in den gesammelten Index-Block am
    Dateiende, die drei Trigger direkt hinter die Tabellendefinition. Passe die
    Abschlussmeldung „All 14 tables created" auf die neue Tabellenzahl an.

    Ergänze in `server/src/types/index.ts` das Interface `UserWorkPeriod` mit den Feldern
    `id: number`, `userId: number`, `validFrom: string`, `validTo: string | null`,
    `weeklyHours: number`, `workSchedule: WorkSchedule | null`, `note: string | null`,
    `createdAt: string`, `createdBy: number | null`. Kommentiere `validFrom` als inklusiv und
    `validTo` als exklusiv (D1) sowie `weeklyHours`/`workSchedule` als gemeinsam versioniert
    (D2). Zusätzlich ein Interface `UserWorkPeriodRow` mit `workSchedule: string | null` für
    die rohe Datenbankzeile — die Umwandlung passiert im Service (Plan 10-04), nicht mit
    `any` und nicht mit einer Typzusicherung.

    Lege `schemaMigrationParity.test.ts` an. Der Test baut zwei Datenbanken als Dateien in
    `os.tmpdir()` mit eindeutigen Namen (nicht `:memory:` — `initializeDatabase()` erzwingt
    WAL und wirft bei einer Speicherdatenbank) und löscht sie in `afterAll`:
    Datenbank A über `initializeDatabase(new Database(pfadA))`, Datenbank B über eine
    minimale `users`-Tabelle plus `migration008.up(db)`. Anschließend wird aus beiden
    `SELECT type, name, sql FROM sqlite_master WHERE tbl_name = 'user_work_periods' ORDER BY
    type, name` gelesen, der `sql`-Text auf einfache Leerzeichen normalisiert (aufeinander
    folgende Weißräume zusammenziehen, Rand trimmen) und beide Ergebnisse mit `toEqual`
    verglichen. Der Test ist der maschinelle Ersatz für den Kommentar „MUSS identisch
    bleiben".
  </action>
  <acceptance_criteria>
    - `grep -c "user_work_periods" server/src/database/schema.ts` ist mindestens `6`
      (Tabelle, zwei Indizes, drei Trigger).
    - `git diff server/src/database/schema.ts` zeigt ausschließlich Hinzufügungen plus die
      angepasste Tabellenzahl in der Abschlussmeldung — keine Änderung an der
      `users`-Tabellendefinition (D4).
    - `npx vitest run src/database/schemaMigrationParity.test.ts` ist grün; ein absichtlich
      eingefügtes zusätzliches Leerzeichen darf ihn nicht rot machen, eine geänderte
      Spaltendefinition muss ihn rot machen (beides einmal ausprobiert und im SUMMARY
      vermerkt).
    - `npx tsc --noEmit` ohne Fehler; `grep -c ": any" server/src/types/index.ts` unverändert
      gegenüber dem Stand vor dem Task.
  </acceptance_criteria>
  <verify>
    <automated>cd server && npx vitest run src/database/schemaMigrationParity.test.ts src/database/migrations/008_create_user_work_periods.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
    `schema.ts` und Migration 008 erzeugen nachweislich dieselben Datenbankobjekte, der Typ
    `UserWorkPeriod` steht ohne `any` bereit, `users` ist unverändert.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migrationslauf → Datenbankdatei | Einziger schreibender Übergang dieses Plans. Keine Netz-, Nutzer- oder API-Eingabe ist beteiligt; die Werte stammen aus der Datei selbst. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | Migration 008 auf einer Bestandsdatenbank | mitigate | Nur `CREATE ... IF NOT EXISTS`; kein `ALTER`, `DROP` oder `UPDATE` auf `users` (durch grep-Kriterium geprüft). Lauf innerhalb `db.transaction()` des `migrationRunner`. |
| T-10-02 | Denial of Service | Trigger auf jedem INSERT/UPDATE | accept | Drei EXISTS-Prüfungen auf einem Index `(userId, validFrom)` bei maximal einstelliger Periodenzahl je Nutzer. Kosten vernachlässigbar; Tabelle wird in Phase 10 nicht in Schleifen beschrieben. |
| T-10-03 | Elevation of Privilege | `ON DELETE CASCADE` auf `users` | accept | Entspricht dem Verhalten aller bestehenden Nutzer-Kindtabellen (`vacation_transactions`, `overtime_balance`). Das Projekt löscht Nutzer per Soft-Delete; ein Hard-Delete räumt bewusst mit ab. |
| T-10-SC | Tampering | npm/pip/cargo-Installationen | accept | Dieser Plan installiert kein Paket. `better-sqlite3` und `vitest` sind bereits Bestand; keine `package.json`-Änderung. Ein Legitimitätscheck entfällt mangels Installation. |
</threat_model>

<verification>
- `cd server && npx tsc --noEmit` — grün (nicht-verhandelbare Randbedingung der ROADMAP).
- `cd server && npx vitest run src/database/` — die beiden neuen Dateien sind grün. Die drei
  projektweit vorbekannten roten Tests (G5, `deferred-items.md` Phase 9) sind nicht Teil
  dieses Verzeichnisses und dürfen die Bewertung nicht verwässern.
- `sqlite3`-Ersatz: Prüfungen laufen über `better-sqlite3` in Node, wie im Projekt üblich.
</verification>

<success_criteria>
- Migration 008 existiert, ist idempotent und verifiziert ihr Ergebnis selbst.
- `schema.ts` spiegelt sie zeichengleich; ein Test beweist das.
- `UserWorkPeriod` und `UserWorkPeriodRow` existieren ohne `any`.
- `users.weeklyHours` und `users.workSchedule` sind unverändert (D4).
- Kein `git push`, kein Deployment in Phase 10 — die Migration darf Produktion erst in
  Phase 14 erreichen. Commits bleiben lokal.
</success_criteria>

<output>
Create `.planning/phases/10-perioden-fundament/10-01-SUMMARY.md` when done.
Notiere darin ausdrücklich: die tatsächliche Objektliste aus `sqlite_master` nach dem
Migrationslauf, das Ergebnis des Gleichheitstests inklusive der beiden Gegenproben, und die
Bestätigung, dass `git diff` an `users` nichts verändert.
</output>
