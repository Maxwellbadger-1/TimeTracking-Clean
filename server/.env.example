# ============================================
# TimeTracking Server - Environment Variables
# ============================================
# NOTE: This file is a simplified template for server-only setup
#
# RECOMMENDED: Use the MASTER .env file in project root instead!
# The server/.env is a symlink to ../.env (all configs in one place)
#
# If you want server-only setup:
# 1. Copy this to server/.env
# 2. Fill in values below
# ============================================

# Server Configuration
PORT=3000
NODE_ENV=development

# Security (CRITICAL!)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-super-secret-session-key-change-this

# CORS
ALLOWED_ORIGINS=tauri://localhost,https://tauri.localhost,http://localhost:1420

# Database
DATABASE_PATH=./database.db

# Logging
LOG_LEVEL=debug

# Backup
BACKUP_SCHEDULE=0 2 * * *
BACKUP_RETENTION_DAYS=30
BACKUP_DIR=./backups

# Rate Limiting
API_RATE_LIMIT=100
LOGIN_RATE_LIMIT=5

# Development
DISABLE_AUTH=false
DEBUG=false

# Database Settings
DB_WAL_MODE=true
DB_TIMEOUT=5000
DB_CACHE_SIZE=2000

# ============================================
# RECOMMENDED SETUP (use master .env):
# ============================================
# Instead of this file, use the centralized config:
#
# 1. Go to project root
# 2. Copy .env.example to .env
# 3. Fill in ALL values (includes SSH, GitHub, etc.)
# 4. server/.env will automatically use it (symlink)
#
# Benefits:
# - One file for ALL configs
# - Scripts can access SSH/GitHub credentials
# - No duplicate configs
# - Easier to maintain
# ============================================
