# Production Environment Variables
# Copy this file to .env on your server and fill in the values

NODE_ENV=production
PORT=3000

# CRITICAL: Change this to a secure random string (min 32 characters)!
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=CHANGE_ME_TO_SECURE_RANDOM_STRING_MIN_32_CHARS

# Database path (relative to server directory)
DATABASE_PATH=./database.db

# Optional: Server URL (for email notifications, etc.)
# SERVER_URL=https://timetracking.yourdomain.com
